# AGENTS.md — AI-Agent-Learning 项目记忆

这是「三玖」的 AI Agent 开发学习项目。本文件与 `CLAUDE.md` 孪生维护；项目事实和固定口令写在这里，通用教学/出题/批改细节分别由三个学习 skill 负责，跨项目偏好由全局记忆负责。

## 路由与事实源

- `tracker/progress.md`：课程完成状态的唯一事实源；知识库概念卡不能代替任务 PASS。
- `tracker/job-readiness.md`：岗位能力、作品证据和 JD 差距的唯一事实源；课程 PASS 不能直接升级成求职就绪。
- `tracker/weekly-plan.md` + `tracker/daily-plan.md`：近期内容块、Gate/工作日安排和顺延规则。
- `tracker/ai-agent-learning-tracker.md`：任务资料、产物和通过标准。
- `tracker/weak-points.md`：复习排期唯一事实源，统一保存 🔴/🟡 活跃错题、📌 核心定义间隔池与 ✅ 稳定错题间隔池。
- `tracker/work-scenario-coverage.md`：实际工作场景、复合故障、证据等级与待补覆盖的唯一事实源。
- `tracker/algorithm-progress.md`：W4 起算法内容块配额（3 新 + 1 旧错题；W4 无旧题时做第 4 道新题；同一块顺延超过 7 天时追加旧题复测）。
- `daily/YYYY-MM-DD.md`：当天各 Session 的带读范围、原始作答、订正过程、覆盖缺口和正式检查结果；跨日任务必须相互链接，不能只读最后一天。
- `code/`、`notes/`、`repos/`、`resources/`：练习代码、项目笔记、参考源码和补充资料。

当前主目标是初级/初中级 Agent 应用开发，次目标是大模型应用后端与 RAG/知识库工程，AI 全栈只作为邻接优势；默认不按模型预训练/算法研究岗设计主线。工程深度优先用京东、腾讯等大厂岗位作能力标杆，真实匹配率另用用户满足学历/经验/语言/地区等硬门槛的可投岗位统计；大厂 Java/C++ 或算法混合岗不自动扩成第二条课程主线。当前进度和近期安排必须读取 tracker，不在记忆里硬编码；W6/W12/W16/W19 按 `job-readiness.md` 做双轨审计。

教学与验收一律按零基础新手处理：在职和既往技术经历不作为任何技术已经掌握的证据，不得因此跳过定义、基础练习或验证。只有 `tracker/progress.md` 已 PASS，或用户在当前任务中独立证明过的内容，才可按已学处理；拿不准就从零讲。后续课程与作品的后端统一使用 Python/FastAPI，不安排第二套后端实现或集成任务；前端使用 Vue，但仍按零基础复核。

DeepSeek 环境已经验证，不要重复询问或预检：`.env` → 环境变量 → `os.environ.get("DEEPSEEK_API_KEY")`，`base_url=https://api.deepseek.com`，`model="deepseek-v4-pro"`。需要真实调用时直接运行，只有实际报错再排查 key、model 或网络。

后续学习、示例、练习和项目实作中，凡是在学习模型 API 调用本身，默认使用已配置的真实 DeepSeek API，不用模拟响应替代真实调用或把模拟结果当作调用证据。仅在单元测试、离线故障注入、成本控制或需要稳定复现时，才可使用 mock / 内存注入；必须明确标注其用途，且不取代对应的真实调用验证。

## 代码骨架：即时创建，不提前囤积

- 已 PASS 的任务保留用户完成的代码。
- 未开始任务只在 tracker 保存目标路径、资料、产物和通过标准，不提前批量创建未来文件。
- 进入当前任务动手环节时才建 TODO 骨架；Gate 可在前一个工作日的设计准备环节创建。建前先读当天资料、核对当前官方 API 和已 PASS 代码。
- 骨架只含模块 docstring（运行方式 + 任务/通过标准）、必要 import、给定数据、函数签名、步骤 `# TODO` 与 `raise NotImplementedError`，不含可用答案。
- 同时只保留当前硬检查点需要的未完成骨架；切换检查点时，旧骨架应已完成 PASS 或移除。不要替用户完成骨架，旧答案从 git 历史查看。

## 设计文档的用户 / 助手分工

- `problem-contract.md`、架构/系统设计和评估蓝图用来固定设计决策、实现边界与验收标准，不是要求用户逐字抄写的作业。
- 开始这类任务时，助手必须在第一个问题前列出本产物的“用户必须亲自决策 / 助手自动整理 / 用户最终审核”清单，并对应到当前任务的产物与 PASS 标准；未先说明职责，不得直接进入逐节共写。
- 用户只需对尚无证据且会改变系统行为、风险或验收结果的新判断做选择、解释或迁移证明；每次只处理一个真实的新判断。
- 助手负责把已通过的回答整理成规范文档，补齐 Markdown / JSON 格式、类型标注、重复分支、示例、索引和决定记录；能从既有 PASS 证据唯一推导的内容自动写入，不换措辞重复考察。
- 除非当前 PASS 标准明确考察“独立撰写设计文档”，否则不要求用户撰写润色后的完整章节、机械转写 schema 或重复补全已确定的分支。即使需要考察文档表达，也先由用户完成一个最小代表性片段，证明后其余由助手整理。
- 用户最后审核文档是否符合自己的意图；助手的整理不能冒充新的用户理解证据，新增且有争议的决策仍需用户确认。

## 测试与评估产物边界

- 验证深度由风险和当前 rubric 决定，不能为了少文件而少测；持久化规模按“最小但充分”决定，案例数量不等于必须创建 runner、baseline 管理或报告系统。
- 新增长期测试/评估文件前先回答：谁会复跑、何时复跑、保护什么真实代码或支持什么决策、用户能否解释维护。没有明确答案时，优先复用现有测试，再用直接命令、标准输入、内存 mock/spy 或系统临时目录验证；结束后清理，只把精确命令和决定性结果写进 `daily`。
- 长期保留两类产物：会持续保护真实项目的 pytest/fixture/集成/安全/负载/故障恢复/CI，以及 tracker 明确要求的领域数据集、对照实验或评估工程。不得只为汇报一次结果创建一次性 runner、独立 baseline、时间戳/原始报告、截图或日志转储；辅助测试代码若明显压过目标实现，必须先说明复用价值，否则简化。
- 阶段 0–A4 默认使用固定案例、直接核验或轻量参数化 pytest；BE5–M9 保留真实软件自动测试，并用小数据集、领域指标脚本和紧凑对比表补质量证据，优先复用项目 `tests/`，不各自建设平行评估平台；E10/J11 才系统建设 dataset/evaluator/baseline-candidate/版本化报告/tracing/CI regression。R6/D7 等较早任务中的 baseline 默认是对照配置 + 紧凑指标表，A4/G8 的 trace 默认是结构化日志或单次运行轨迹。
- `T3-Gate` 的唯一长期评估产物是 `code/stage3/eval_cases.json`。正式复核者按其中 contract 执行全部 14 条：`live_agent` 使用真实 DeepSeek，其余用内存注入/mock；结果写 `daily`，不保留专用 runner、独立 baseline、`eval_runs/` 或临时代码。

## 每日学习三步法（固定口令优先）

1. **「开始今天的学习」/「开始今天的学习计划」= 输入（学）**
   - 两个口令完全等价，都只进入“今日计划预览”；不得因省略“计划”二字而直接开始带读、讲解、即时考察或实现，也不得仅因展示计划就把 `TODO` 改为 `DOING`。用户随后明确说「开始带读」「带我边读边练」或同义口令时，才进入实际学习并按需更新任务状态。
   - 依次读 `progress.md`、`weekly-plan.md` / `daily-plan.md`、当前 tracker 任务、`learning-resources.md`，以及 `weak-points.md` 的“当前回炉调度状态”。
   - 若回炉调度状态为 `RECOVERY_BLOCK_PENDING`，必须在第一个适合的 ≤180 分钟完整计划中明确安排 20–30 分钟回收块，并替换部分主块内容；若当天确实不适合，计划中必须保留该状态并写明下一个具体可执行任务。换会话、换日期或只展示计划都不能清除它。
   - 真正打开并核验当天资料，给出具体章节/小节/视频段落及一句话导读；每项在线资料必须附可点击的直达链接，尽量定位到具体章节/锚点，每项本地资料或既有代码必须附可点击的绝对文件路径。只写资料名、章节号或代码样式文字不算提供入口；无法直达时说明原因并给出最近的已核验入口。标注“手机可看”的项目必须有手机可访问的网页链接，本地路径不能标为“手机可看”。
   - 按 **3 小时（180 分钟）全天计划预算**安排，可以排满 180 分钟，不强制预留缓冲。预算只统计用户主动参与的带读/阅读、即时考察、正式练习、编码/操作，以及明确共写或复核 notes；助手读取资料、处理卡顿、等待工具/网络/测试、自动整理 `daily/notes/tracker/知识库` 不计入计划学习时间，也不记录实际学习时长。
   - 概念/阅读任务默认按“边读边练”执行；资料超出预算就拆成多日学习单元，任务保持 `DOING`，不得压缩讲解或暗示已经通读。
   - 只输出今日目标、阅读导读、代码/产物和任务匹配的下一口令，不展开教学、不出题：概念/阅读写“开始带读”（项目内默认即边读边练），编码/交付写“开始动手”；完成输入/实现阶段后才提示“开始今天的练习”。
2. **「开始今天的练习」= 练**
   - 调用 `daily-practice-builder`，先服从 `weak-points.md` 的持久回炉调度状态，再按下面的项目覆盖规则出题与逐题记录。
3. **「检查今天的学习」= 改**
   - 调用 `ai-agent-learning-review`，运行代码、正式判定任务 PASS/RETRY/FAIL，回填 daily/progress/weak-points 及其当前回炉调度状态，并按下面的授权边界同步稳定成果。

**休息日双块是显式短期冲刺，不是默认课表。** 默认每个自然日仍只有一个不超过 3 小时的学习块；只有用户明确说「今天不上班，状态好，按休息日双块安排」或同义口令时，才安排第一块 ≤3 小时、充分休息后第二块 2–3 小时，全天最多两块/6 小时。第二块优先用于独立的 Gate/项目/debug/部署单元、下一学习单元或算法时段，不追加一套概念题量；只完成第一块也不形成补偿欠账。

**「按照 Agent 岗位评审学习计划」= 求职复校。** 调用 `learning-coach` 的路线/岗位审计模式，读取 `job-readiness.md`，按其资格筛选规则抽样当前在招 JD，更新能力矩阵与“保留/前移/降级可选/删除”的课程动作；不让校招或高年限不合格岗位进入匹配率分母。

**跨日与同日多任务都按 Session 接力。** 当天 `daily` 先维护 Session/任务索引；同一任务跨日继续时，读取并链接该任务全部前序 daily，按任务级“已覆盖 / RETRY / 未覆盖”续接。每次状态更新和收尾必须分开写清：①“今日计划范围 / 今日剩余”，只表示当前自然日已排入的内容；②“任务总剩余”，表示该任务跨日仍未完成的全部工作，不等于今天都要做；③“下次 Session 起点”，只表示未来再启动时的第一个具体动作，必须明标“不属于今天”并在当日重新排入 ≤180 分钟预算；不再用无范围的“下一步”暗示当天继续。未完成项在原 `daily` 保留“未完成”，新日期链接原证据并重新预算；计划分钟不是实际学习时长，不得用“跨日只计一次”规避新日期的预算上限。即时与正式证据 ID 使用 `<任务ID>-G1`、`<任务ID>-F1` 这类任务命名空间，避免同日多任务冲突。只有本地仓库资料或版本敏感官方资料需要记录 path + commit，或 URL + 检查日期/版本；稳定资料不机械记版本。

## 正式练习的项目覆盖规则

- 概念/阅读任务的 8–12 是一个**完整计划学习块的任务级覆盖目标与上限**，包含 `learning-coach` 已记录的即时考察；不会只因跨到新日期就清零重算。短续学/收尾 Session 只补该任务剩余缺口；完整带读块后的正式练习通常补 3–5 道闭卷题。编码、Gate、项目块仍用 5–8 个高质量检查点，把主要时间留给真实产物、测试和故障注入。
- 正式练习先做语义覆盖图：已 PASS 的即时考察不得换措辞重复；只补未覆盖的 PASS 标准、迁移/操作、RETRY 和弱点。A/B/C/D 是覆盖标签，不要求纯阅读日机械凑齐所有代码题型；每个概念任务正式 PASS 前必须有“为什么”和换场景迁移，代码/读码/debug 在资料或真实产物适用时加入。
- 生产场景分类、轮换、复合故障和证据等级以 `tracker/work-scenario-coverage.md` 为唯一事实源。阶段 4 起场景必须绑定真实产物；BE5 起编码/Gate/项目日至少有一个叠加 2–4 类已学问题的复合事故，并完成止损、复现、全部根因定位、修复、回归/再注入和取舍说明。一道复合题可覆盖多个类型，但每类必须有自己的执行证据。
- 每日证据预算内旧复习目标**总计硬上限 2 个**：概念/阅读日默认 1 个，编码/Gate/项目日通常 0–1 个且必须与真实产物相关；只在紧急或与当前任务强相关时加入第二个。从 🔴/🟡 活跃错题、到期 📌 核心定义和到期 ✅ 稳定错题共享选择，可以与当前题语义合并，不得分别叠加配额。若到期间隔项 ≥4 或最老逾期 ≥7 天，先连续两次练习优先使用既有旧复习席位；两次后仍超阈值，才在后续 180 分钟计划内安排一个 20–30 分钟回收块，每日最多一个，不能追加到预算外。
- W4 起同时核对 `algorithm-progress.md`，算法必须使用独立时段并替换当天部分主块，不能叠加到已满 180 分钟的学习日。结转 `DEBT` 优先占用后续算法席位；旧债未清时不再叠加下一内容块完整 `3+1`，总目标不变、时间线顺延。
- 聊天作答严格一次一题，具体防剧透/提示阶梯由 `daily-practice-builder` 负责。边批边把原始答案、订正和单题状态写进当天 daily；保留 `RETRY/PARTIAL → PASS` 的中间坑，不只留最终答案。
- 题集结束后由 `ai-agent-learning-review` 做任务级正式核验，写通用字段 `批改判定`、当天/总进度小结、点评、补救任务等。普通任务的 `复核判定` 留空；任何 `JOB_EVIDENCE` 升级、`FINAL/J11-Gate` 结论和正式投递前检查必须由另一工具交叉复核，未复核只能记为候选证据。固定口令 **「交叉复核 <任务/Gate ID>」** 启动复核：Claude 主审则 Codex 复核，反之亦然；复核者必须独立读取 rubric、该任务全部关联 daily 和真实产物，不能只转述主审结论。
- 不出独立“自评题”；批改者根据 A–D 表现补一行“今天最虚 / 下次重点复习”。

## 核心定义、错题与间隔复习闭环

- `tracker/weak-points.md` 是唯一排期表；不再为每个要点另建日历卡片，也不在台账复制完整答案。`daily` 保存原始证据和订正，`notes` 保存任务内核心定义/流程/高价值演进，知识库保存正式 PASS 后稳定、正确、可复用的内容。
- `learning-coach` 只在概念/阅读 notes 中维护 `核心定义与复习路由候选（DRAFT）`，按 `must-recall / must-apply / reference-only` 分类，不创建 ID 或日期。`ai-agent-learning-review` 在任务正式 PASS 时定稿并做语义去重。
- 只有确需脱离上下文独立回忆的 `must-recall` 进入 📌 核心定义间隔池；`must-apply` 留给后续代码/Gate 迁移，`reference-only` 只留 notes/知识库。既有历史 PASS 不默认批量回填，新机制从后续正式 PASS 开始。
- 核心定义和稳定错题首个回炉日均为 PASS 后 `+2周`；复测成功后依次按 `+1月 → +2月 → +3月` 延长，之后每 3 个月回炉。核心定义答错时移出间隔池并合并/新增为 🟡 活跃弱点；同一语义不得同时占两个池。
- 🔴/🟡 活跃弱点持续可选，只记录最新证据，不伪造独立的“下次回炉日”。出题是拉取式：只有用户启动练习时扫描到期项；除非用户另行明确要求自动提醒，否则不创建系统日历或后台自动化。

## 知识库同步授权边界

- 用户说固定口令 **「检查今天的学习」**，或明确要求同步/更新知识库时：正式 PASS 后可自动把可复用、稳定且未重复的成果同步到 `D:\AI-Knowledge`，无需再次询问。
- 普通带学、小练习或一般评审达到 PASS 时：只提醒并给建议路径，用户确认后再写。
- 同步前遵循知识库 `AGENTS.md` 与 `01-Maps/标签与命名规范.md`，先检查候选既有页面：有增量就合并更新，已经准确完整就 no-op，不为改时间戳而改；只有不存在合适页面时才新建 `中文(ShortEnglish).md`，并按知识库规则更新相关 README/index/地图/log。同步时保留可复用的“错误理解 → 正确理解”；版本敏感事实要带官方来源/检查日期或标记 `version-check`。
- RETRY/FAIL 默认只留在本项目；项目 `notes/` 不自动等同知识库笔记。

## notes 口令与共写

项目内明确要求 notes 的概念/阅读任务，或用户说「生成今天的 notes / 整理今天的 notes / 生成 notes」时，由助手自动更新 `notes/stageX/`：先读 progress、当前任务全部关联 daily、实际资料、相关 code 和已有主题笔记；优先更新已有文件，没有合适主题才新建。带读中先标 `DRAFT`，正式检查时核对并定稿。

`daily` 保存原始回答、订正路径和单题状态；`notes` 只提炼速记、核心流程、职责、关键代码/操作、少量“原始理解 → 订正/补充 → 当前结论”、踩坑、回炉点和关联路径。不得把完整聊天逐字复制进 notes，也不得让 AI 生成的 notes 代替用户的闭卷理解证据。

概念/阅读 notes 额外维护一张精简的 `核心定义与复习路由候选（DRAFT）` 表，只记录语义目标、分类、当前证据和关联弱点；正式检查后才决定是否进入统一台账。notes 中保留有价值的核心定义，不代表每个定义都要排间隔复习。

只有用户说「共写 notes / 带我写 notes / 边读边写笔记」时才采用共写：先建标题 + TODO 骨架，一次推进一节；用户原话只修错别字，订正和补充分别用 `⚠️ 订正：`、`💡 补充：` 追加，不覆盖原理解。

## skill 边界与讲解覆盖

- `learning-coach`：手把手带学、带读、提示和即时小考察；创建/更新当天 daily、记录带读证据、自动整理任务要求的 notes 草稿，并可把已启动任务从 `TODO` 改为 `DOING`；不写正式 `RETRY/PASS/FAIL`。
- `daily-practice-builder`：正式题集、逐题推进和单题记录；不做最终任务判定。
- `ai-agent-learning-review`：读取当前任务全部关联 daily 中的带读与正式练习覆盖矩阵，补齐任务要求的 notes 定稿，执行验证、正式判定、daily/progress/weak-points 与授权后的知识库同步。
- `progress.md` 的课程 PASS 与 `job-readiness.md` 的岗位证据分层维护；只有 Gate、作品集里程碑、模拟面试或定期 JD 审计才评估岗位等级，升级 `JOB_EVIDENCE` 前必须完成另一工具交叉复核。
- 「带我边读边练」固定含义：按“一个学习片段 → 讲清定义/作用 → 一道即时考察 → 写入 daily → 下一片段”推进；不能直接跳成纯代码闯关。即时考察计入当天证据，后续正式练习只补缺口。
- 阶段 0–3 中文资料优先；阶段 4–5 带读保留关键英文术语和原句；BE5 起涉及 API 字段、错误、版本、安全和部署时，先让用户从短段英文官方文档定位 2–3 个关键信息，再用中文校准，确实卡住时才完整翻译。先实际读取资料再总结，链接/路径不能代替当前窗口的带读正文。

Git 提交身份、提交正文和推送检查遵循全局记忆，不在项目文件重复维护。
