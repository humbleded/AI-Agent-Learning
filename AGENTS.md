# AGENTS.md — AI-Agent-Learning 项目记忆（供 codex 使用）

这是「三玖」的 AI Agent 开发学习项目的**项目级记忆 / 协作约定**。codex 在本项目工作时按此执行。

---

## 项目结构（速查）

- `tracker/`：学习路线、总进度、周计划、每日计划、学习资料清单。
- `daily/`：每日打卡（`YYYY-MM-DD.md`）。
- `code/` `notes/` `repos/` `resources/`：练习代码、笔记、参考源码、补充资料。

## 当前情况

- 在职学习，目标岗位：大模型应用 / Agent 应用开发 / AI 全栈开发。
- 调模型用 **DeepSeek API**（兼容 OpenAI SDK，`base_url=https://api.deepseek.com`）。
- 实时进度看 `tracker/progress.md`，近期安排看 `tracker/weekly-plan.md` 和 `daily/`。
- 稳定 PASS 成果同步进 `D:\AI-Knowledge`。

## 用户画像、沟通与讲解约定

- 用户**非技术背景**，不熟悉技术专业术语；提出的需求可能口语化、不完整、甚至含糊。
- **中文输出**。
- 需求含糊或有多种理解时，**先用一两句话确认意图再动手**，别按字面直接猜着执行；口语化/不完整的表述要主动结合项目进度补全上下文。
- **大白话讲解**：用户看不懂的地方用大白话拆开讲，不堆术语；术语第一次出现先用一句人话解释。
- **中文优先**：选资料/视频优先中文（本地 `repos/` 中文章节、菜鸟/MDN/官方中文文档、B 站中文视频）。只有中文看不懂、参数不确定、要核对版本时才查英文官方文档。
- **先看再总结**：给章节/视频段落前，自己先读过/查过再下结论，按真实内容总结，不靠印象编。英文官方文档抓取后翻成中文要点＋大白话；视频拿不到字幕就如实说明、只给到「看哪一节」。
- **缺工具自己装**：需要抓在线文档、取视频字幕等工具时自行安装配置，不来回问用户（只在要花钱或有风险时先确认）。

## 每日学习三步法（固定口令 → 行为）

下面三句是固定口令，**优先按此表走**（覆盖 skill 的默认语义匹配，避免「学习计划」被当成闯关）：

1. **「开始今天的学习计划」= 输入（学）。**
   - 先读 `tracker/progress.md`（当前到哪了）+ `weekly-plan.md` / `daily-plan.md`（本周/当天安排）+ `ai-agent-learning-tracker.md`（当前任务的资料/要做/通过标准）+ `learning-resources.md`，定位今天该学哪个任务。
   - **必须具体**：真的去打开当天要学的资料读一遍（本地 `repos/` 的 `.md` 优先；在线文档用抓取工具），然后告诉用户**具体看哪一章/哪一节、视频看哪一段**，每段配一句话导读（讲什么、对应今天哪个点）。不许只甩「去看第 X 章」这种笼统话。
   - 产出一张当天清单：🎯今日目标 ＋ 📖要看的具体章节/视频段落（带一句话导读，**按每天 ~3h 容量给足、可多带读几节，别只给最低限**；其中手机可看的**纯阅读**项标注一下，方便白天碎片时间随时看，不懂的下班我带导读）＋ ⌨️今天要写的代码/产物 ＋ ✍️学完去「开始今天的练习」。
   - 这一步**只发清单＋导读，不展开教学、不出题**。
2. **「开始今天的练习」= 练。** 调 `daily-practice-builder` 出题，锚定刚学的内容（出题细则见下「每日练习」）。
3. **「检查今天的学习」= 改。** 调 `ai-agent-learning-review` 跑代码、判 PASS/RETRY/FAIL、回填 `daily` 与 `progress`，把稳定成果同步进 `D:\AI-Knowledge`。

## learning-coach 的边界（不抢上面三句）

- 三句固定口令优先走三步法表。
- `learning-coach` 负责「手把手带学/闯关」：用户说「教我 X / 我不会 / 带我学 / 把这题拆开 / 看我的答案 / 接下来学什么」时才用。它是「学」这一步里的**可选深入**——发完清单后，用户可自己看资料，也可喊它带着闯关，再进入「练习」「检查」。
- 要零歧义可直接用斜杠命令：`/learning-coach`、`/daily-practice-builder`、`/ai-agent-learning-review`。

---

## DeepSeek 调用环境（已验证，别再问/别再验证）

- key 链路通：`.env` → 环境变量 → `os.environ.get("DEEPSEEK_API_KEY")`。
- `model="deepseek-v4-pro"` 在线有效（不会报 model not found）。
- 默认 key 和 model 都可用——要做真实调用验证就直接跑，不必先请求授权或提示额度消耗。只有调用**真的报错**时再去排查 key/model/网络。

## 每日练习：检查题当天现出，不写死

- `daily/*.md` 打卡里**不预埋固定题**；每天学完后用 `daily-practice-builder` **按当天真实进度现出题**——分 A 快速回忆 / B 解释比较 / C 代码或操作 / D 应用迁移，并复习前几天的弱点。**不再单出「自评题」**——自评产出（该复习哪）由批改者总结，见下「练习作答与回填流程」。
- **题量/难度**：每套 **11–20 题**，按当天精力/进度增减；难度取「A 档」=在舒适区上推一格（别一上来全面拔高）。每套必含 ≥1 道读码/预测输出题、≥1 道 debug 题、≥1 道改/写代码题、≥1 道迁移题、≥1 道「为什么」题。用户偏好动手题多于纯说理。
- **真实场景题时机**：纯 L1 基础日先不硬塞场景题；约定到 **L1-Gate（CLI chatbot 整合关）和阶段 2 起**，每套 ≥1 道，并**主动提醒用户**补场景题。

## 练习作答与回填流程

- 用户偏好**在聊天里逐题作答**（通常一档一档来），不直接写进文件作答框。
- **默认直接回填，不预告**：批改时边批边把答案写进当天 `daily/YYYY-MM-DD.md` 对应的 `python` 作答框（用户答案 + 订正点 + 单题判定）。
- 批完补底部小结（遇到的问题 / 批改结果，**含批改者根据 A–D 表现总结的一行「今天最虚 / 下次重点复习」，替代自评题**），并附一句简短确认「已回填到 daily 文件」。
- 底部 `Codex 判定 …` 占位行默认**留空**——留给用户单独跑 codex 复核（codex 在本工作流里就是这个「复核批改」的角色），除非用户另说。
- **逐题推进作答**：整套题出完后可先列出全貌给用户看，但作答**一道一道来**——抛出一道、等用户答这道、当场批改给判定，再进入下一道；**默认不要把所有题一次性丢给用户让其一口气全答**（用户主动想多答几道则随他）。
- **记录「中间的坑」（错误 → 订正全过程）**：某题答错、经多轮提示/追问后才答对时，**不能只留最终正确答案**；把中间踩的坑（错误理解、卡在哪、如何一步步纠正过来）一并写进当天 `daily/YYYY-MM-DD.md` 该题作答框，单题判定体现过程（如 `RETRY/PARTIAL → PASS`）。这些坑即后续复习与同步进 `D:\AI-Knowledge` 的「易错点 / 错误理解 → 正确理解」。
