# AGENTS.md — AI-Agent-Learning 项目记忆

这是「三玖」的 AI Agent 开发学习项目。本文件与 `CLAUDE.md` 孪生维护；项目事实和固定口令写在这里，通用教学/出题/批改细节分别由三个学习 skill 负责，跨项目偏好由全局记忆负责。

## 路由与事实源

- `tracker/progress.md`：课程完成状态的唯一事实源；知识库概念卡不能代替任务 PASS。
- `tracker/job-readiness.md`：岗位能力、作品证据和 JD 差距的唯一事实源；课程 PASS 不能直接升级成求职就绪。
- `tracker/weekly-plan.md` + `tracker/daily-plan.md`：依赖驱动内容块、当前知识单元、Session 起点和接力规则；不把 W 当日历周。
- `tracker/ai-agent-learning-tracker.md`：任务资料、产物和通过标准。
- `tracker/weak-points.md`：复习排期唯一事实源，统一保存 🔴/🟡 活跃错题、📌 核心定义间隔池与 ✅ 稳定错题间隔池。
- `tracker/work-scenario-coverage.md`：实际工作场景、复合故障、证据等级与待补覆盖的唯一事实源。
- `tracker/algorithm-progress.md`：W4 起算法内容块配额（3 新 + 1 旧错题；W4 无旧题时做第 4 道新题；同一块顺延超过 7 天时追加旧题复测）。
- `daily/YYYY-MM-DD.md`：按日期保存各 Session 的带读范围、原始作答、订正过程、覆盖缺口和正式检查结果；它不是每日课表，跨日任务必须相互链接，不能只读最后一天。
- `code/`、`notes/`、`repos/`、`resources/`：练习代码、项目笔记、参考源码和补充资料。

当前主目标是初级/初中级 Agent 应用开发，次目标是大模型应用后端与 RAG/知识库工程，AI 全栈只作为邻接优势；默认不按模型预训练/算法研究岗设计主线。工程深度优先用京东、腾讯等大厂岗位作能力标杆，真实匹配率另用用户满足学历/经验/语言/地区等硬门槛的可投岗位统计；大厂 Java/C++ 或算法混合岗不自动扩成第二条课程主线。当前状态必须读取 tracker，不在记忆里复制易失效快照；W6/W12/W16/W19 按 `job-readiness.md` 做双轨审计，每轮把 4–6 个能力标杆与恰好 10 个 `HARD_ELIGIBLE` 可投样本分开。

教学与验收一律按零基础新手处理：在职和既往技术经历不作为任何技术已经掌握的证据，不得因此跳过定义、基础练习或验证。只有 `tracker/progress.md` 已 PASS，或用户在当前任务中独立证明过的内容，才可按已学处理；拿不准就从零讲。后续课程与作品的后端统一使用 Python/FastAPI，不安排第二套后端实现或集成任务。用户已说明会一些 Vue：后续先用最小切片复核，已经独立证明的部分不机械重学；真正进入界面任务时，助手可先核查许可证、维护状态、依赖和安全风险，再从 GitHub 选择并组合开源 Vue 骨架。若在 G8-Gate 提前采用第三方 Vue，用户当场完成来源/许可证/依赖/关键 diff 的最小准入审核，并说明数据流和密钥边界；完整组件状态、SSE、认证、错误处理和亲自修改能力仍在 J11-03 验收。助手完成的机械搭建不计为用户独立证据，也不得在任务开始前提前克隆。J11-02 必须冻结唯一 `product_flagship`；只有它继续建设正式 Vue、公网部署和产品级观测，另一个旗舰保留可复现 API/demo、容器与 E10 评估证据。

A4 后路线固定为依赖驱动组合：`G8-00 → R6-01 → BE5-01 → B0-01/03/04/Gate → BE5-04 → R6-02/S-02 → R6-03/S-06 → BE5-02/03 → G8-01/02/03 → BE5-05/BE5-Gate → R6-Gate → D7-02/S-04 → G8-Gate → D7-01/03/Gate → M9 → E10 → S-01 + J11-02~06 + S-05 → FINAL-Gate → J11-07/08/Gate`。`J11-01` 随真实项目持续积累并在 `J11-06` 前收口；H5 只按真实 debug 触发。A4 已 PASS 的 Agent Loop、ReAct、Plan-and-Solve、Reflection 只作迁移前置，不重新教学或重考。

- LangChain 旗舰是 `code/stage6/engineering_docs_rag/` 的工程文档 RAG 助手；主证据是 Document/Loader/Splitter、Retriever、Tool/Agent Harness、固定两步与 Agentic RAG 对照、引用、权限、评估和部署。
- LangGraph 旗舰是 `code/stage8/incident_change_review_agent/` 的故障诊断与变更评审 Agent；主证据是显式 StateGraph、route/loop/stop、stream、persistence、interrupt/resume、幂等与恢复。
- FastAPI 是两个项目的 HTTP/SSE/认证/输入校验交付层，并统一规定系统出错时返回什么、调用方怎样处理；它不是 Agent 编排框架。先让 Python core 可运行、可测试，再接 API。
- `S-04` 多 Agent 必须以同一数据集的单 Agent 为 baseline，使用带类型约束的角色交接格式、共享/私有 state、`max_handoffs`、总预算、停止和人工升级；无可测净收益就回退单 Agent。
- `D7-03` 负责跨会话长期 Memory 的 CRUD、TTL、provenance、冲突、删除、隔离、PII 和 poisoning；G8 checkpoint/thread state 只负责一次工作流的可恢复执行，不能冒充长期 Memory。

DeepSeek 环境已经验证，不要重复询问或预检：`.env` → 环境变量 → `os.environ.get("DEEPSEEK_API_KEY")`，`base_url=https://api.deepseek.com`，`model="deepseek-v4-pro"`。需要真实调用时直接运行，只有实际报错再排查 key、model 或网络。

后续学习、示例、练习和项目实作中，凡是在学习模型 API 调用本身，默认使用已配置的真实 DeepSeek API，不用模拟响应替代真实调用或把模拟结果当作调用证据。仅在单元测试、离线故障注入、成本控制或需要稳定复现时，才可使用 mock / 内存注入；必须明确标注其用途，且不取代对应的真实调用验证。

## 代码骨架：即时创建，不提前囤积

- 已 PASS 的任务保留用户完成的代码。
- 未开始任务只在 tracker 保存目标路径、资料、产物和通过标准，不提前批量创建未来文件。
- 进入当前任务的实际动手 Session 时才建 TODO 骨架；设计类 Gate 也只在真正启动其设计/实现 Session 时创建。建前先读本单元资料、核对当前官方 API 和已 PASS 代码。
- 骨架只含模块 docstring（运行方式 + 任务/通过标准）、必要 import、给定数据、函数签名、步骤 `# TODO` 与 `raise NotImplementedError`，不含可用答案。
- 同时只保留当前硬检查点需要的未完成骨架；切换检查点时，旧骨架应已完成 PASS 或移除。不要替用户完成骨架，旧答案从 git 历史查看。

## 设计文档的用户 / 助手分工

- `problem-contract.md`、架构/系统设计和评估蓝图用来固定设计决策、实现边界与验收标准，不是要求用户逐字抄写的作业。
- 开始这类任务时，助手必须在第一个问题前列出本产物的“用户必须亲自决策 / 助手自动整理 / 用户最终审核”清单，并对应到当前任务的产物与 PASS 标准；未先说明职责，不得直接进入逐节共写。
- 用户只需对尚无证据且会改变系统行为、风险或验收结果的新判断做选择、解释或迁移证明；每次只处理一个真实的新判断。
- 助手负责把已通过的回答整理成规范文档，补齐 Markdown / JSON 格式、类型标注、重复分支、示例、索引和决定记录；能从既有 PASS 证据唯一推导的内容自动写入，不换措辞重复考察。
- 除非当前 PASS 标准明确考察“独立撰写设计文档”，否则不要求用户撰写润色后的完整章节、机械转写 schema 或重复补全已确定的分支。即使需要考察文档表达，也先由用户完成一个最小代表性片段，证明后其余由助手整理。
- 用户最后审核文档是否符合自己的意图；助手的整理不能冒充新的用户理解证据，新增且有争议的决策仍需用户确认。

## 测试与评估产物边界

- 验证深度由风险和当前 rubric 决定，不能为了少文件而少测；持久化规模按“最小但充分”决定，案例数量不等于必须创建 runner、baseline 管理或报告系统。
- 新增长期测试/评估文件前先回答：谁会复跑、何时复跑、保护什么真实代码或支持什么决策、用户能否解释维护。没有明确答案时，优先复用现有测试，再用直接命令、标准输入、内存 mock/spy 或系统临时目录验证；结束后清理，只把精确命令和决定性结果写进 `daily`。
- 长期保留两类产物：会持续保护真实项目的 pytest/fixture/集成/安全/负载/故障恢复/CI，以及 tracker 明确要求的领域数据集、对照实验或评估工程。不得只为汇报一次结果创建一次性 runner、独立 baseline、时间戳/原始报告、截图或日志转储；辅助测试代码若明显压过目标实现，必须先说明复用价值，否则简化。
- 阶段 0–A4 默认使用固定案例、直接核验或轻量参数化 pytest；BE5–M9 保留真实软件自动测试，并用小数据集、领域指标脚本和紧凑对比表补质量证据，优先复用项目 `tests/`，不各自建设平行评估平台；E10/J11 才系统建设 dataset/evaluator/baseline-candidate/版本化报告/tracing/CI regression。R6/D7 等较早任务中的 baseline 默认是对照配置 + 紧凑指标表，A4/G8 的 trace 默认是结构化日志或单次运行轨迹。
- `T3-Gate` 的唯一长期评估产物是 `code/stage3/eval_cases.json`。正式复核者按文件里规定的输入、预期结果和判定规则执行全部 14 条：`live_agent` 使用真实 DeepSeek，其余用内存注入/mock；结果写 `daily`，不保留专用 runner、独立 baseline、`eval_runs/` 或临时代码。

## 知识单元三步法（固定口令优先）

1. **「开始今天的学习」/「开始今天的学习计划」= 当前知识单元预览**
   - 两个口令完全等价，只预览，不开始带读、即时考察或实现，也不把 `TODO` 改成 `DOING`。
   - 依次读 `progress.md`、`weekly-plan.md` / `daily-plan.md`、当前 tracker 任务、`learning-resources.md` 和 `weak-points.md` 的持久调度状态。
   - 真正打开并核验本单元资料，给出具体章节/小节/视频段落和一句话导读；在线资料给直达链接，本地资料给绝对路径。无法直达时说明原因并给最近的已核验入口。
   - 只输出当前知识单元目标、依赖、资料、产物、PASS 标准、Session 起点和一个任务匹配的下一口令。概念/阅读用「开始带读」，实现/交付用「开始动手」。
   - 若状态为 `RECOVERY_BLOCK_PENDING`，保留它并指出下一次合适的独立恢复 Session；预览、换会话或换日期都不能清除。
2. **「开始带读」/「开始动手」= 启动实际 Session**
   - 创建或更新当天 daily，链接该任务全部前序 daily；真正启动任务后才允许 `TODO → DOING`。
   - Session 可跨日期；按任务级“已覆盖 / RETRY / 未覆盖”继续，不重置题量、证据、算法或回炉状态。
3. **「开始今天的练习」= 正式练习；「检查今天的学习」= 正式验收**
   - 练习调用 `daily-practice-builder`，只补任务覆盖缺口。
   - 检查调用 `ai-agent-learning-review`，核验任务全部 Session 和真实产物，正式判定并按授权边界同步稳定成果。

**「按照 Agent 岗位评审学习计划」= 求职复校。** `learning-coach` 的路线/岗位审计模式读取 `job-readiness.md`，查询深圳/武汉当前岗位并输出路线建议；用户批准后的正式岗位台账写回由 `ai-agent-learning-review` 负责。校招、硬门槛不合格和资格未知岗位不进入匹配率分母。

**跨日与同日多任务都按 Session 接力。** daily 维护 Session/任务索引；每次分别写清“本 Session 目标 / 已覆盖 / 未覆盖”“任务总剩余”和“下次 Session 起点”。新日期链接全部前序证据后从起点继续，不重新排课或重算题量。证据 ID 使用 `<任务ID>-G1`、`<任务ID>-F1` 这类任务命名空间。只有本地仓库资料或版本敏感官方资料记录 path + commit，或 URL + 检查日期/版本；稳定资料不机械记版本。

## 正式练习的项目覆盖规则

- 概念/阅读任务的 8–12 是**任务级覆盖目标与上限**，包含 `learning-coach` 已记录的即时考察；不会只因跨到新日期就清零重算。续学/收尾 Session 只补该任务剩余缺口；完整带读单元后的正式练习通常补 3–5 道闭卷题。编码、Gate、项目仍用 5–8 个高质量检查点，把主要精力留给真实产物、测试和故障注入。
- 正式练习先做语义覆盖图：已 PASS 的即时考察不得换措辞重复；只补未覆盖的 PASS 标准、迁移/操作、RETRY 和弱点。A/B/C/D 是覆盖标签，不要求纯阅读日机械凑齐所有代码题型；每个概念任务正式 PASS 前必须有“为什么”和换场景迁移，代码/读码/debug 在资料或真实产物适用时加入。
- 生产场景分类、轮换、复合故障和证据等级以 `tracker/work-scenario-coverage.md` 为唯一事实源。阶段 4 起场景必须绑定真实产物；BE5 起编码/Gate/项目任务至少有一个叠加 2–4 类已学问题的复合事故，并完成止损、复现、全部根因定位、修复、回归/再注入和取舍说明。一道复合题可覆盖多个类型，但每类必须有自己的执行证据。
- 每个正式练习集的旧复习目标**总计硬上限 2 个**：概念/阅读默认 1 个，编码/Gate/项目通常 0–1 个且必须与真实产物相关；只在紧急或与当前任务强相关时加入第二个。从 🔴/🟡 活跃错题、到期 📌 核心定义和到期 ✅ 稳定错题共享选择，可以与当前题语义合并，不得分别叠加配额。若到期间隔项 ≥4 或最老逾期 ≥7 天，先连续两次练习优先使用既有旧复习席位；两次后仍超阈值，进入下一次合适的独立恢复 Session，每次最多推进 1–2 个目标，有真实证据后再重算。
- W4 起同时核对 `algorithm-progress.md`，算法使用独立 Session。结转 `DEBT` 优先占用后续算法席位；债务达到 4 道时先逐题清债，旧债未清时不启动下一内容块完整 `3+1`，总目标不变。
- 聊天作答严格一次一题，具体防剧透/提示阶梯由 `daily-practice-builder` 负责。边批边把原始答案、订正和单题状态写进当前 daily；保留 `RETRY/PARTIAL → PASS` 的中间坑，不只留最终答案。
- 题集结束后由 `ai-agent-learning-review` 做任务级正式核验，写 `批改判定`、本 Session/任务总进度小结、点评和补救任务。普通任务的 `复核判定` 留空；任何 `JOB_EVIDENCE` 升级、`FINAL-Gate`、`J11-Gate` 结论和正式投递前检查必须由另一工具交叉复核。复核者先独立读取 rubric、全部关联 daily 和真实产物并形成临时结论，再读主审结论；记录同意、分歧、各自证据和处理，分歧未解决不得升级。
- 不出独立“自评题”；批改者根据 A–D 表现补一行“当前最虚 / 下次重点复习”。

## 核心定义、错题与间隔复习闭环

- `tracker/weak-points.md` 是唯一排期表；不再为每个要点另建日历卡片，也不在台账复制完整答案。`daily` 保存原始证据和订正，`notes` 保存任务内核心定义/流程/高价值演进，知识库保存正式 PASS 后稳定、正确、可复用的内容。
- `learning-coach` 只在概念/阅读 notes 中维护 `核心定义与复习路由候选（DRAFT）`，按 `must-recall / must-apply / reference-only` 分类，不创建 ID 或日期。`ai-agent-learning-review` 在任务正式 PASS 时定稿并做语义去重。
- 只有确需脱离上下文独立回忆的 `must-recall` 进入 📌 核心定义间隔池；`must-apply` 留给后续代码/Gate 迁移，`reference-only` 只留 notes/知识库。既有历史 PASS 不默认批量回填，新机制从后续正式 PASS 开始。
- 核心定义和稳定错题首个回炉日均为 PASS 后 `+2周`；复测成功后依次按 `+1月 → +2月 → +3月` 延长，之后每 3 个月回炉。核心定义答错时移出间隔池并合并/新增为 🟡 活跃弱点；同一语义不得同时占两个池。
- 🔴/🟡 活跃弱点持续可选，只记录最新证据，不伪造独立的“下次回炉日”。出题是拉取式：只有用户启动练习时扫描到期项；除非用户另行明确要求自动提醒，否则不创建系统日历或后台自动化。

## 知识库同步授权边界

- 用户说固定口令 **「检查今天的学习」**，或明确要求同步/更新知识库时：正式 PASS 后可自动把可复用、稳定且未重复的成果同步到 `D:\AI-Knowledge`，无需再次询问。
- 普通带学、小练习或一般评审达到 PASS 时：只提醒并给建议路径，用户确认后再写。
- 同步前遵循知识库 `AGENTS.md` 与 `01-Maps/标签与命名规范.md`，先检查候选既有页面：有增量就合并更新，已经准确完整就 no-op，不为改时间戳而改；只有不存在合适页面时才新建 `中文(ShortEnglish).md`，并按知识库规则更新相关 README/index/地图/log。同步时保留可复用的“错误理解 → 正确理解”；版本敏感事实要带官方来源/检查日期或标记 `version-check`。
- RETRY/FAIL 默认只留在本项目；项目 `notes/` 不自动等同知识库笔记。

## notes 口令与共写

项目内明确要求 notes 的概念/阅读任务，或用户说「生成今天的 notes / 整理今天的 notes / 生成 notes」时，由助手自动更新 `notes/stageX/`：先读 progress、当前任务全部关联 daily、实际资料、相关 code 和已有主题笔记；优先更新已有文件，没有合适主题才新建。带读中先标 `DRAFT`，正式检查时核对并定稿。

`daily` 保存原始回答、订正路径和单题状态；`notes` 只提炼速记、核心流程、职责、关键代码/操作、少量“原始理解 → 订正/补充 → 当前结论”、踩坑、回炉点和关联路径。不得把完整聊天逐字复制进 notes，也不得让 AI 生成的 notes 代替用户的闭卷理解证据。

概念/阅读 notes 额外维护一张精简的 `核心定义与复习路由候选（DRAFT）` 表，只记录语义目标、分类、当前证据和关联弱点；正式检查后才决定是否进入统一台账。notes 中保留有价值的核心定义，不代表每个定义都要排间隔复习。

只有用户说「共写 notes / 带我写 notes / 边读边写笔记」时才采用共写：先建标题 + TODO 骨架，一次推进一节；用户原话只修错别字，订正和补充分别用 `⚠️ 订正：`、`💡 补充：` 追加，不覆盖原理解。

## skill 边界与讲解覆盖

- `learning-coach`：手把手带学、带读、提示和即时小考察；创建/更新当前日期的 Session daily、记录带读证据、自动整理任务要求的 notes 草稿，并可把已启动任务从 `TODO` 改为 `DOING`；路线审计只输出建议，不写正式岗位台账，也不写正式 `RETRY/PASS/FAIL`。
- `daily-practice-builder`：正式题集、逐题推进和单题记录；不做最终任务判定。
- `ai-agent-learning-review`：读取当前任务全部关联 daily 中的带读与正式练习覆盖矩阵，补齐任务要求的 notes 定稿，执行验证、正式判定、daily/progress/weak-points 与授权后的知识库同步；用户批准路线/岗位审计方案后，由它负责正式 `job-readiness.md` 台账写回。
- `progress.md` 的课程 PASS 与 `job-readiness.md` 的岗位证据分层维护；只有 Gate、作品集里程碑、模拟面试或定期 JD 审计才评估岗位等级，升级 `JOB_EVIDENCE` 前必须完成另一工具交叉复核。
- 「带我边读边练」固定含义：按“一个学习片段 → 讲清定义/作用 → 一道即时考察 → 写入 daily → 下一片段”推进；不能直接跳成纯代码闯关。即时考察计入任务证据，后续正式练习只补缺口。
- 阶段 0–3 中文资料优先；阶段 4–5 带读保留关键英文术语和原句；BE5 起涉及 API 字段、错误、版本、安全和部署时，先让用户从短段英文官方文档定位 2–3 个关键信息，再用中文校准，确实卡住时才完整翻译。先实际读取资料再总结，链接/路径不能代替当前窗口的带读正文。

Git 提交身份、提交正文和推送检查遵循全局记忆，不在项目文件重复维护。
