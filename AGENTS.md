# AGENTS.md — Codex 在本项目的角色与分工

这是「三玖」的 AI Agent 开发学习项目。项目由**用户 + 两个 AI 助手（Claude、Codex）**协作。以下是给 Codex 的固定规则，请遵守。

## 与 Claude 的分工

- **Claude 负责（不归 Codex）**：日常实时带练、讲解、苏格拉底式提问、给提示、**批改用户的练习答案**、改/检查练习代码。这些依赖对话上下文。
- **Codex（你）负责**：进度同步与知识库整理这类「看文件就能做、不需要实时对话」的批处理工作，即 `ai-agent-learning-review` 技能的职责：
  - 读 `tracker/`、`daily/`、`code/`，运行验证；
  - 更新 `tracker/progress.md` 的任务状态；
  - 把稳定的 PASS 结果同步进 `D:\AI-Knowledge`（概念放 `02-Concepts/<主题>/`，并登记该目录的 `README.md` 索引）。

### 边界（谁写哪个文件）

- 当天 `daily/YYYY-MM-DD.md` 里的**逐题答案 + 批改判定 + 批改小结**由 **Claude** 写好（那是批改产物）。
- **你（Codex）据此**更新 `tracker/progress.md` 和 `D:\AI-Knowledge`，**不要重新批改、不要推翻 Claude 的判定**，除非用户明确要求复核。

## 协作约定

1. 两个助手**不共享对话记忆，只共享文件**——文件是唯一事实来源。
2. **不要重复 Claude 已做的事**（如重新批改练习）；同一步骤只跑一遍，避免双方各自重读文件浪费 token。
3. 同步时以 daily 文件里「Claude 批改结果 / 判定」为准。

## 用户与环境（与 Claude 记忆保持一致）

- 用户是**非技术背景**的学习者，不熟术语，需求可能口语化/不完整/含糊：解释先讲大白话、再上术语；意图含糊先澄清。
- DeepSeek 调用环境**已验证可用**（key 链路通、`model="deepseek-v4-pro"` 有效），不必反复询问或验证；跑真实调用不用请求授权。
