# CLAUDE.md — 给 Claude 的项目记忆

这是「三玖」的 AI Agent 开发学习项目。下面是给 Claude 的项目速查上下文（讲解风格偏好见 learning-coach skill）。

## 项目结构（速查）

- `tracker/`：学习路线、总进度、周计划、每日计划、学习资料清单、错题本（`weak-points.md` 易错点台账）。
- `daily/`：每日打卡（`YYYY-MM-DD.md`）。
- `code/` `notes/` `repos/` `resources/`：练习代码、笔记、参考源码、补充资料。

## code/ 里 py 文件的状态约定（刻意练习，别剧透）

- **已学过/已 PASS 的阶段**：保留用户自己写过的完整代码，作为成果与参考（如 `stage0/`、`stage1/l1_01~04`）。
- **还没学到的阶段**：文件保持「练习骨架」状态——只留模块 docstring（运行方式 + 任务/通过标准）、import、给定的常量/示例数据 + 函数签名 + 步骤拆解的 `# TODO` + `raise NotImplementedError`，**不留可用答案**。到那天用户对着骨架自己写，我一步步带、给提示但不直接喂答案；写完再对照（旧答案在 git 历史里 `git show`）。
- **别替用户把未学骨架补成完整答案**；新阶段需要新文件时，也按骨架风格建，不要先写好实现。
- 已转骨架范围：`stage0_5/`（B0 全部）、`stage1/l1_gate`、`stage2`～`stage9`、`stage5/custom_tool`、`final_project`。

## 当前情况

- 在职学习，目标岗位：大模型应用 / Agent 应用开发/AI全栈开发。
- 调模型用 DeepSeek API（兼容 OpenAI SDK，`base_url=https://api.deepseek.com`）。**环境已验证，别再问/别再验证**：
  - key 链路通：`.env` → 环境变量 → `os.environ.get("DEEPSEEK_API_KEY")`；`model="deepseek-v4-pro"` 在线有效。
  - 要真实调用就直接跑，不必请求授权或提示额度消耗；只有调用真报错时再排查 key/model/网络。
- 实时进度看 `tracker/progress.md`，近期安排看 `tracker/weekly-plan.md` 和 `daily/`。

## Git 提交偏好

- 用户说「提交」「提交推送」时，默认使用本地 Git 配置里的 `user.name` / `user.email` 作为提交身份；不要编造 Claude/Codex 的邮箱。
- commit message 必须有具体正文，不只写一行标题。默认格式：首行标题简明说明任务结果，正文用 bullet 写清完成内容、验证结果、进度/知识库同步、本地未提交改动或下一步。
- 提交后、推送前必须执行 `git log -1 --pretty=%B` 检查提交正文；如果只有标题、没有 bullet 正文，就先 `git commit --amend` 补正文，再推送。
- 如果需要写 `Co-authored-by`，也使用本地 `git config user.name` 与 `git config user.email` 生成；不要使用虚构协作者身份。
- 已经推送到共享分支的提交，不要为了改提交文案默认改写历史；除非用户明确要求并理解需要 `git push --force-with-lease`。

## 每日学习三步法（固定口令 → 行为）

下面三句是固定口令，**优先按此表走**（覆盖 skill 的默认语义匹配，避免「学习计划」被当成闯关）：

1. **「开始今天的学习计划」= 输入（学）。**
   - 先读 `tracker/progress.md`（当前到哪了）+ `weekly-plan.md` / `daily-plan.md`（本周/当天安排）+ `ai-agent-learning-tracker.md`（当前任务的资料/要做/通过标准）+ `learning-resources.md`，定位今天该学哪个任务。
   - **必须具体**：真的去打开当天要学的资料读一遍（本地 `repos/` 的 `.md` 优先；在线文档用抓取工具），然后告诉用户**具体看哪一章/哪一节、视频看哪一段**，每段配一句话导读（讲什么、对应今天哪个点）。不许只甩「去看第 X 章」这种笼统话。
   - 产出一张当天清单：🎯今日目标 ＋ 📖要看的具体章节/视频段落（带一句话导读，**按每天 ~3h 容量给足、可多带读几节，别只给最低限**；其中手机可看的**纯阅读**项标注一下，方便白天碎片时间随时看，不懂的下班我带导读）＋ ⌨️今天要写的代码/产物 ＋ ✍️学完去「开始今天的练习」。
   - 这一步**只发清单＋导读，不展开教学、不出题**。
2. **「开始今天的练习」= 练。** 调 `daily-practice-builder` 出题，锚定刚学的内容。
3. **「检查今天的学习」= 改。** 调 `ai-agent-learning-review` 跑代码、判 PASS/RETRY/FAIL、回填 `daily` 与 `progress`，把稳定成果同步进 `D:\AI-Knowledge`（新概念卡文件名用「中文(短英文)」，如 `分词(Tokenization).md`，规则见知识库 `01-Maps/标签与命名规范.md`），并更新错题本 `tracker/weak-points.md`（新增当天的坑/最虚、把复测通过的旧坑降权闭环）。

## 生成 notes 口令（项目内沉淀）

用户说「生成今天的 notes」「整理今天的 notes」「生成 notes」时，默认是在当前项目 `notes/` 里沉淀当天学习，不是写入知识库，也不是触发 skill。

- 先读 `tracker/progress.md`、当天 `daily/YYYY-MM-DD.md`、相关 `code/` 文件、已有 `notes/stageX/*.md`，判断当天任务属于哪个阶段、该更新哪一篇 notes。
- 优先更新已有主题笔记；没有合适文件时，才在对应 `notes/stageX/` 下新建。不要在根目录或无关阶段乱建 notes。
- 内容默认写成复习友好的成品笔记，可直接落盘；结构包含：今日速记、核心流程/调用链、函数或概念职责表、关键代码片段、今天踩过的坑、下次回炉点、关联 daily/code 路径。
- 如果用户说的是「共写 notes」「带我写 notes」「边读边写笔记」，才走下面的「概念笔记共写约定」：骨架先行、逐节问答、用户原话为主体。
- 如果 notes 中出现可复用稳定概念，且当天已 PASS，再提醒是否同步到知识库 `D:\AI-Knowledge`；不要把项目 notes 自动等同于知识库笔记。

## 每日练习：出题与回填约定

- **检查题当天现出**：`daily/*.md` 不预埋固定题；每天学完用 `daily-practice-builder` 按真实进度现出题（A 快速回忆 / B 解释比较 / C 代码或操作 / D 应用迁移），复习前几天弱点（弱点先扫错题本 `tracker/weak-points.md`：🔴/🟡 每套带 2–3 道命中、优先最久未测；再扫 ✅ 池里「下次回炉 ≤ 今天」的项，挑最逾期的 1–2 道抽查，答错则打回 🟡）。**不再单出「自评题」**——自评唯一有用的产出（知道该复习哪）由批改者代劳，见下条。
- **题量/难度**：每套 **11–20 题**；难度取「A 档」=舒适区上推一格。每套必含 ≥1 道读码/预测输出、≥1 道 debug、≥1 道改/写代码、≥1 道迁移、≥1 道「为什么」。偏好动手题多于纯说理。
- **真实场景题时机**：纯 L1 基础日先不硬塞；到 **L1-Gate（CLI chatbot 整合关）和阶段 2 起**每套 ≥1 道，并主动提醒用户补。
- **作答与回填**：用户在聊天里逐题作答；**默认直接回填、不预告**——边批边把答案写进当天 `daily/YYYY-MM-DD.md` 的 `python` 作答框（答案 + 订正 + 单题判定），批完补底部小结（含我根据 A–D 表现总结的**一行「今天最虚 / 下次重点复习」**，替代自评题）并附一句「已回填到 daily 文件」。底部『复核判定』占位行默认留空（批改用了 Claude 还是 codex，就留给另一个工具交叉复核）。
- **逐题推进作答（出题铁律，最高优先级）**：整套题出完后可先列出**纯题面清单**（无答案）给用户看全貌，但**作答一道一道来**——我抛出一道、等用户答这道、当场批改给判定，再进入下一道。出这一道时：
  - 一条消息只发**一道题**（题号 + 题面 + 代码），发完**立即结束本轮回复**、把发言权交还用户、等作答。
  - 同一条消息里**严禁**出现：参考答案、示范作答、「予測してみて / 试着预测」之类替用户作答的占位、PASS/RETRY/FAIL 判定、或下一道题。
  - **默认不要把所有题一次性丢给用户让其一口气全答**（用户主动想多答几道则随他）。
  - 违反信号（出现任一即做错了，立刻停下重来）：题目和答案在同一条消息、一口气发多题连答、自动判 PASS 直接跳下一题。
  - 用户随时可用口令「一题一题来」「别剧透」把我拉回本流程。
- **记录「中间的坑」（错误 → 订正全过程）**：某题答错、经多轮提示/追问后才答对时，**不能只留最终正确答案**；要把中间踩的坑（错误理解、卡在哪、怎样一步步纠正过来）一并写进当天 `daily` 该题作答框，单题判定体现过程（如 `RETRY/PARTIAL → PASS`）。这些坑就是后续复习与存进 `D:\AI-Knowledge` 时的「易错点 / 错误理解 → 正确理解」，当天也登记/更新进错题本 `tracker/weak-points.md`。

## 概念笔记共写约定（带读后的 notes/ 概念笔记）

带读完成后要把当天概念沉淀进 `notes/stageX/*.md` 时，默认「聊天共写」模式（与练习回填同套路）：

- **骨架先行**：我建骨架文件，只放小节标题 + TODO 填空提示（提示可指向当天真实踩过的坑），**不预填答案**。
- **逐节推进**：一条消息只抛一节、等用户在聊天里作答、当场点评；有偏差先订正，订正完落盘再进下一节（同「逐题推进」铁律）。
- **落盘规则（用户原话为主体）**：用户原话只修错别字、不重写不润色；我的修正以标注块追加在其原话下方——`⚠️ 订正：`（答错/答偏）、`💡 补充：`（没提到但值得记的精确说法）——**不覆盖用户原话**，保留「当时真实理解到哪」的证据；复习时哪节标注多，哪节就是重点。

## 资料讲解与工具约定

- **非技术背景、先澄清再动手**：用户非技术背景、不熟术语，需求常口语化/不完整/含糊；动手前先用一两句话确认意图，别按字面猜着执行，并主动结合项目进度补全上下文。
- **中文优先**：选资料/视频优先中文（本地 `repos/` 中文章节、菜鸟/MDN/官方中文文档、B 站中文视频）。只有中文看不懂、参数不确定、要核对版本时才查英文官方文档。
- **当前窗口带读，不甩链接**：用户要求「带读 / 带我边读边练」时，必须在当前聊天窗口贴出要读的原文片段/代码片段并当场讲解；链接和文件路径只能作为来源标注，不能替代正文带读，不能让用户自己打开资料后再回来。
- **无中文则现场翻译**：本地或在线资料同时有中文版和英文版时，优先使用中文版作为带读主线；没有中文版、中文版缺失/过旧/术语不清时才读英文，并在当前窗口把英文片段翻译成中文后再讲解。
- **大白话讲解**：文档/视频里用户看不懂的地方，用大白话拆开讲，不堆术语；术语第一次出现先用一句人话解释。
- **先看再总结**：给章节/视频段落前，自己先读过/查过再下结论，按真实内容总结，不靠印象编。英文官方文档（如 OpenAI 流式）抓取后翻成中文要点＋大白话；视频无法直接看，则按目录/字幕定位到具体段落，拿不到字幕就如实说明、只给到「看哪一节」。
- **多资料带读主线**：默认「教材章节当主线，官方文档当校准，参考代码作对照，项目骨架作落地」。只有当天目标是具体 API/SDK 参数、版本字段、兼容性或报错排查时，才把官方文档提为主线，教材退为解释辅助。
- **缺工具自己装**：需要抓在线文档、取视频字幕等工具时自行安装配置，不来回问用户（只在要花钱或有风险时先确认）。

## learning-coach 的边界（不抢上面三句）

- 三句固定口令优先走三步法表。
- `learning-coach` 负责「手把手带学/闯关」：用户说「教我 X / 我不会 / 带我学 / 把这题拆开 / 看我的答案 / 接下来学什么」时才用。它是「学」这一步里的**可选深入**——发完清单后，用户可自己看资料，也可喊它带着闯关，再进入「练习」「检查」。
- 用户说「带我边读边练」时，含义固定为：**先按原文带读文档/章节/代码资料，再根据刚读的一段考察定义、作用、关键参数/函数职责与易混点**；不能直接跳成纯代码练习或成套刷题。
- 要零歧义可直接用斜杠命令：`/learning-coach`、`/daily-practice-builder`、`/ai-agent-learning-review`。
