# CLAUDE.md — 给 Claude 的项目记忆

这是「三玖」的 AI Agent 开发学习项目。下面是给 Claude 的项目速查上下文（讲解风格偏好见 learning-coach skill）。

## 项目结构（速查）

- `tracker/`：学习路线、总进度、周计划、每日计划、学习资料清单。
- `daily/`：每日打卡（`YYYY-MM-DD.md`）。
- `code/` `notes/` `repos/` `resources/`：练习代码、笔记、参考源码、补充资料。

## 当前情况

- 在职学习，目标岗位：大模型应用 / Agent 应用开发/AI全栈开发。
- 调模型用 DeepSeek API（兼容 OpenAI SDK，`base_url=https://api.deepseek.com`）。**环境已验证，别再问/别再验证**：
  - key 链路通：`.env` → 环境变量 → `os.environ.get("DEEPSEEK_API_KEY")`；`model="deepseek-v4-pro"` 在线有效。
  - 要真实调用就直接跑，不必请求授权或提示额度消耗；只有调用真报错时再排查 key/model/网络。
- 实时进度看 `tracker/progress.md`，近期安排看 `tracker/weekly-plan.md` 和 `daily/`。

## 每日学习三步法（固定口令 → 行为）

下面三句是固定口令，**优先按此表走**（覆盖 skill 的默认语义匹配，避免「学习计划」被当成闯关）：

1. **「开始今天的学习计划」= 输入（学）。**
   - 先读 `tracker/progress.md`（当前到哪了）+ `weekly-plan.md` / `daily-plan.md`（本周/当天安排）+ `ai-agent-learning-tracker.md`（当前任务的资料/要做/通过标准）+ `learning-resources.md`，定位今天该学哪个任务。
   - **必须具体**：真的去打开当天要学的资料读一遍（本地 `repos/` 的 `.md` 优先；在线文档用抓取工具），然后告诉用户**具体看哪一章/哪一节、视频看哪一段**，每段配一句话导读（讲什么、对应今天哪个点）。不许只甩「去看第 X 章」这种笼统话。
   - 产出一张当天清单：🎯今日目标 ＋ 📖要看的具体章节/视频段落（带一句话导读）＋ ⌨️今天要写的代码/产物 ＋ ✍️学完去「开始今天的练习」。
   - 这一步**只发清单＋导读，不展开教学、不出题**。
2. **「开始今天的练习」= 练。** 调 `daily-practice-builder` 出题，锚定刚学的内容。
3. **「检查今天的学习」= 改。** 调 `ai-agent-learning-review` 跑代码、判 PASS/RETRY/FAIL、回填 `daily` 与 `progress`，把稳定成果同步进 `D:\AI-Knowledge`。

## 每日练习：出题与回填约定

- **检查题当天现出**：`daily/*.md` 不预埋固定题；每天学完用 `daily-practice-builder` 按真实进度现出题（A 快速回忆 / B 解释比较 / C 代码或操作 / D 应用迁移 / E 自评），复习前几天弱点。
- **题量/难度**：每套 **11–20 题**；难度取「A 档」=舒适区上推一格。每套必含 ≥1 道读码/预测输出、≥1 道 debug、≥1 道改/写代码、≥1 道迁移、≥1 道「为什么」。偏好动手题多于纯说理。
- **真实场景题时机**：纯 L1 基础日先不硬塞；到 **L1-Gate（CLI chatbot 整合关）和阶段 2 起**每套 ≥1 道，并主动提醒用户补。
- **作答与回填**：用户在聊天里逐题作答；**默认直接回填、不预告**——边批边把答案写进当天 `daily/YYYY-MM-DD.md` 的 `python` 作答框（答案 + 订正 + 单题判定），批完补底部小结并附一句「已回填到 daily 文件」。底部 `Codex 判定 …` 占位行默认留空（留给用户单独跑 codex 复核）。

## 资料讲解与工具约定

- **非技术背景、先澄清再动手**：用户非技术背景、不熟术语，需求常口语化/不完整/含糊；动手前先用一两句话确认意图，别按字面猜着执行，并主动结合项目进度补全上下文。
- **中文优先**：选资料/视频优先中文（本地 `repos/` 中文章节、菜鸟/MDN/官方中文文档、B 站中文视频）。只有中文看不懂、参数不确定、要核对版本时才查英文官方文档。
- **大白话讲解**：文档/视频里用户看不懂的地方，用大白话拆开讲，不堆术语；术语第一次出现先用一句人话解释。
- **先看再总结**：给章节/视频段落前，自己先读过/查过再下结论，按真实内容总结，不靠印象编。英文官方文档（如 OpenAI 流式）抓取后翻成中文要点＋大白话；视频无法直接看，则按目录/字幕定位到具体段落，拿不到字幕就如实说明、只给到「看哪一节」。
- **缺工具自己装**：需要抓在线文档、取视频字幕等工具时自行安装配置，不来回问用户（只在要花钱或有风险时先确认）。

## learning-coach 的边界（不抢上面三句）

- 三句固定口令优先走三步法表。
- `learning-coach` 负责「手把手带学/闯关」：用户说「教我 X / 我不会 / 带我学 / 把这题拆开 / 看我的答案 / 接下来学什么」时才用。它是「学」这一步里的**可选深入**——发完清单后，用户可自己看资料，也可喊它带着闯关，再进入「练习」「检查」。
- 要零歧义可直接用斜杠命令：`/learning-coach`、`/daily-practice-builder`、`/ai-agent-learning-review`。
