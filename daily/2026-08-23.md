# 2026-08-23 学习记录

- 计划学习预算：单个 `A4-Gate-HITL` 交付块，最多 150 分钟；只统计用户主动读码、决策、代表性编码、故障分析与最终审核，不记录实际学习时长。助手读取、建立已授权骨架、机械接线、等待测试和自动写回不计入预算。
- 当前任务：`A4-Gate` 研究摘要 Agent，父任务状态 `DOING`。
- 当前学习单元：`A4-Gate-HITL`｜高风险动作 action-bound confirmation、allow / deny 与零副作用。
- 跨日边界：2026-08-22 只完成第二块 preflight 并发出 `G1`；用户答案发生在今天，因此今天按新的单块预算记录，不把昨天的 150 分钟计划伪造成跨日已消耗时长。

## 当日 Session / 任务索引

| Session | 任务 / 学习单元 | 前序证据 | 当前状态 |
| --- | --- | --- | --- |
| S1 | `A4-Gate-HITL`｜高风险动作人工确认 allow / deny | [2026-08-22 S3](2026-08-22.md#休息日第二块启动session-s3a4-gate-hitl) 发出的 `G1`；复用 2026-08-05 `G6 / G7 = PASS` | `G1 = PARTIAL → PASS`；用户代表性实现 `I1a = PASS`；I1b/I1c 机械验证 PASS；`U1 = PARTIAL → PASS`，HITL 教学 / 实现单元闭合，父 Gate 仍待 eval、五问与正式复核 |

## A4-Gate 全任务证据接力

本 Session 已重新承认并读取 A4-Gate 的前序 daily；已经通过的输入、工具、主链、日志、恢复和风险分级不换措辞重复考察：

- [2026-08-05](2026-08-05.md)：官方资料定向带读、Problem Contract 起点、`G6 / G7` 风险分级与人工介入。
- [2026-08-06](2026-08-06.md)：Problem Contract 第 1～11 节设计完成，父任务保持 `DOING`。
- [2026-08-07](2026-08-07.md)：C1 输入与沙箱路径边界。
- [2026-08-08](2026-08-08.md)：C2 两个真实工具适配器。
- [2026-08-09](2026-08-09.md)：C3a～C3c 工具注册、Action 校验与真实模型 / 工具主链。
- [2026-08-10](2026-08-10.md)：C3d 来源、Reflection / Refinement prompt 与 validator 边界。
- [2026-08-11](2026-08-11.md)：Reflection → Refinement → validator 集成。
- [2026-08-12](2026-08-12.md)：C4a-1 共享 request context、step 与七字段日志。
- [2026-08-13](2026-08-13.md)：五步正常轨迹和调用账本收口。
- [2026-08-17](2026-08-17.md)：工具同参数一次重试与六步恢复轨迹。
- [2026-08-18](2026-08-18.md)：工具失败状态、真实执行来源与公开映射。
- [2026-08-19](2026-08-19.md)：Action correction、完整链预算与公开停止。
- [2026-08-20](2026-08-20.md)：Candidate / Reflection 协议与内容失败恢复。
- [2026-08-21](2026-08-21.md)：Candidate / Reflection Provider/API 受控恢复。
- [2026-08-22](2026-08-22.md)：Refinement 四域共享 recovery 正式 PASS、迁移题与 HITL G1 题面。

## 当前资料、预算与范围

- 任务事实源：`tracker/progress.md`、`tracker/ai-agent-learning-tracker.md`；本 Session 已闭合 HITL 子单元，父 `A4-Gate` 仍缺 14 条 eval、五问、合同总体定稿和整关正式复核。
- 回炉状态：`NORMAL`，到期项 `WP-28 / WP-27 / WP-29`；本 Gate 实现 Session 不安排不相关算法复习，也不把它们记成已完成的第 2 个旧复习优先席位。
- 当前合同：`code/stage4/problem-contract.md` 第 4、6、8、9 节；公开状态只允许 `success / invalid_input / tool_failure / insufficient_evidence / needs_manual`，当前七字段调用日志只接受真实 `model_call / tool_call`。
- 当前代码：`code/stage4/a4_gate_research_summary_agent.py` 已有纯 confirmation 消费器、固定停止映射和显式注入高风险执行器；生产 `TOOL_REGISTRY` / schema 仍只有两个只读工具，fake 只存在于一次性内存验证中，没有持久化或注册。
- 今日计划范围：完成 G1 定向订正；通过后创建唯一 HITL 骨架，完成代表性实现、内存 fake / spy 和用户最终审核。若订正或 debug 扩展，则优先闭合当前检查点并把后续范围顺延，父任务保持 `DOING`。
- 今日排除范围：14 条 `eval_cases.json`、五个 Gate 必答问题、合同总体定稿、父 Gate 正式复核，以及 LangGraph persistence / interrupt / resume。

## `A4-Gate-HITL-G1`｜确认必须精确绑定本次动作

### 用户原始答案

> 选择：B
>
> 绑定规则：
>
> - `confirmation.request_id` 必须与 `planned_action.request_id` 精确匹配：`"req-current"`。
> - `confirmation.tool_name` 必须与 `planned_action.tool_name` 精确匹配：`"fake_delete_source"`。
> - `confirmation.arguments` 必须与 `planned_action.arguments` 深度匹配：`{"relative_path": "sample.txt"}` 必须完全一致，不能变成 `other.txt`。
> - `confirmation.decision` 必须存在，且只能是 `"allow"` 或 `"deny"`。
> - 该 confirmation 只能用于当前这次冻结动作，不能被保存后重复使用。
>
> allow：
>
> - fake 调用：1 次
> - step / 日志：新增一次真实 `tool_call` step / 执行日志
> - 行为：客户端继续执行
>
> deny：
>
> - fake 调用：0 次
> - step / 日志：不新增真实 `tool_call` step；只记录拒绝 / 安全停止日志
> - 行为：安全停止
>
> 缺失 confirmation：
>
> - fake 调用：0 次
> - step / 日志：不新增真实 `tool_call` step；只记录未确认 / 安全停止日志
> - 行为：安全停止
>
> 错绑 confirmation：
>
> - fake 调用：0 次
> - step / 日志：不新增真实 `tool_call` step；只记录绑定失败 / 安全停止日志
> - 行为：安全停止
>
> 公开停止：
>
> - status：`blocked`
> - sources：`[]`

### 批改

- 正确：选择 B，并把 `request_id / tool_name / arguments` 与冻结动作精确绑定；`decision` 使用显式枚举而不是 truthiness。
- 正确：精确 allow 才调用 fake 1 次并新增一次真实 `tool_call` step / 日志；deny、缺失和错绑确认均为 fake 0 次、真实工具 step 0 次并安全停止。
- 正确：确认门前没有取得真实来源，所以公开安全停止的 `sources=[]`。
- 缺口 1：`blocked` 不在本项目 `ALLOWED_STATUSES` 与 Problem Contract 的五种公开状态中，不能新增第六种状态。
- 缺口 2：当前固定七字段 `run_context.logs` 只记录真实 `model_call / tool_call`。拒绝、缺失或错绑分支没有真实调用，因此 step 与这份调用日志都必须保持不变；若未来需要单独的安全决策审计日志，必须另行设计 schema，不能静默塞进当前调用日志。
- 缺口 3：“不能被保存后重复使用”目前只是政策句，没有可执行的客户端消费机制；必须说明客户端怎样识别该 confirmation 已经消费，并在再次使用时于 fake 调用前拒绝。
- 教学检查点状态：`A4-Gate-HITL-G1 = PARTIAL`。只补上述三点，不重答已经正确的 B、字段绑定和四条 fake 调用次数。

### `A4-Gate-HITL-G1-R1`｜三点定向订正

请只回答：

1. 从现有五种公开状态中，把 `blocked` 改成哪一个？`sources=[]` 保持不变。
2. 明确写出 deny、缺失和错绑分支对当前 `run_context.step` 与七字段 `run_context.logs` 的变化；不要新增第三种 event type。
3. 把“一次性使用”改成可执行规则：客户端保存什么已消费标识、在什么时点检查、在什么时点标记为已消费，以及第二次提交同一 confirmation 时 fake 调用次数是多少。当前只需行为规则，不写代码。

完成标准：公开状态属于既有合同；零真实调用分支不伪造调用日志；confirmation 的一次性消费能由客户端状态和检查顺序实际执行，而不只是自然语言承诺。

### 用户定向订正原答

> 1. 公开停止：
>
> - status：`needs_manual`
> - sources：`[]`
>
> 2. deny / 缺失 / 错绑：
>
> - run_context.step 分别增加：deny `0`、缺失 confirmation `0`、错绑 confirmation `0`
> - 七字段 run_context.logs 分别增加：deny `0` 条、缺失 confirmation `0` 条、错绑 confirmation `0` 条
>
> 3. 一次性消费：
>
> - 已消费标识：`confirmation_id`。由客户端在生成 confirmation 时创建的一次性 UUID/nonce，作为该授权单的唯一标识。不能仅使用 `request_id + tool_name + arguments`，因为同一请求内可能出现完全相同的动作，三字段无法区分两份授权。
> - 检查时点：收到 confirmation 后，在执行 fake 工具调用之前，先校验绑定字段是否与 `planned_action` 精确匹配，然后检查该 `confirmation_id` 是否已存在于客户端的 `consumed_confirmation_ids` 集合中。若已存在，直接判定为复用，安全停止。
> - 标记为已消费的时点：通过检查后，并且在执行 fake 工具调用之前，立即将该 `confirmation_id` 加入 `consumed_confirmation_ids` 集合。这样即使 fake 之后失败，该授权也不会被再次使用。
> - 第二次提交同一 confirmation 时 fake 调用：`0` 次。因为在执行 fake 之前就会因 `confirmation_id` 已消费而被拒绝，副作用不会发生。

### 定向订正批改

- 正确：公开停止复用既有 `needs_manual`，且门前没有真实工具来源，故 `sources=[]`。
- 正确：deny、缺失与错绑均未发生真实调用，所以共享 `step` 和固定七字段调用日志都增加 `0`；安全决策如需审计，应使用未来单独定义的安全审计表面。
- 正确：独立 `confirmation_id` 能区分同一请求内参数完全相同的两份授权；动作绑定检查、复用检查和副作用之间的顺序明确可执行。
- 正确：“先消费授权、再执行副作用”实现的是 **at-most-once（至多一次）**：即使 fake 随后失败，这份授权也不能重放；若要重试动作，必须重新取得一份新的显式授权。
- 精度补充：未来若进入并发或多进程场景，“检查未消费 + 写入已消费集合”必须由原子操作或临界区保护；当前 A4 V1 为同步代表性实现，本检查点不扩展持久化与并发控制。
- 教学检查点状态：`A4-Gate-HITL-G1 = PARTIAL → PASS`。这只证明 HITL 行为合同已理解，不等于父 `A4-Gate` 正式 PASS。

## `A4-Gate-HITL-I1a`｜实现纯确认校验与一次性消费器

### 本切片在做什么、为什么先做它

当前先实现 `consume_action_bound_confirmation()`：它位于危险工具之前，只负责回答“这份人工决定是否能放行当前冻结动作”，并在放行或拒绝决定被处理前消费其唯一 ID。它不执行 fake，不接触生产 `TOOL_REGISTRY`，也不接触 `run_context`。

这样把两类职责拆开：本切片证明**不可信 confirmation 无法越过授权门且不能重放**；后续编排切片再证明只有 exact allow 会执行 fake 1 次并写一条真实 `tool_call` 账本，而其余分支都公开映射为 `needs_manual`。

### 用户 / 助手分工

- 用户必须亲自实现：该函数内部的输入合同检查、动作精确绑定、replay 检查、先消费后返回，以及 exact 内部结果。
- 助手已完成：根据已 PASS 的 G1 决策建立唯一 TODO 骨架、冻结本切片合同与边界。
- 用户完成后由助手验证：直接内存断言、输入不变性、消费集合变化、fake 0 次、`run_context` 零变化和生产注册表零污染；通过后再机械接线 fake 与公开结果映射。

### 已创建的唯一骨架

- 文件：`code/stage4/a4_gate_research_summary_agent.py`
- 函数：`consume_action_bound_confirmation(planned_action, confirmation, consumed_confirmation_ids)`
- 当前只有合同 docstring、一个 `TODO` 和 `raise NotImplementedError`；没有答案实现。
- 骨架已经通过 `python -m py_compile`，只证明语法可导入，不代表行为通过。

### 你现在具体要实现的精确顺序

1. 先守住调用方不变量：`planned_action` 必须是内建字典且恰好有 `request_id / tool_name / arguments`；前两者是非空内建字符串，`arguments` 是内建字典。`consumed_confirmation_ids` 必须是只含非空内建字符串的内建集合。任一不合法都抛 `ValueError`，不能伪装成普通安全停止。
2. `confirmation is None`：返回 exact `{"outcome": "stop", "reason": "missing_confirmation"}`，集合不变。
3. 非空 confirmation 是不可信边界数据：要求内建字典、exact 五键 `confirmation_id / request_id / tool_name / arguments / decision`，字段类型合法，且 decision 精确为 `allow` 或 `deny`。任何形状、类型或枚举错误返回 exact `stop / invalid_confirmation`，不能抛成程序错误，集合不变。
4. 将 confirmation 的 `request_id / tool_name / arguments` 与冻结动作逐项精确比较；参数字典使用深度相等，不做补键、删键或路径纠正。任一不符返回 exact `stop / confirmation_mismatch`，集合不变。
5. 动作绑定通过后再查 `confirmation_id`；若已在集合中，返回 exact `stop / confirmation_replayed`，集合保持不变。
6. 对 fresh 且合法绑定的 confirmation，先把 ID 加入集合，再根据 decision 返回：allow → exact `allow / confirmed`；deny → exact `stop / user_denied`。fresh deny 也必须消费，因为该 ID 表示一份已处理的人工决定，不能随后把同一 ID 的 decision 改成 allow 重放。
7. 所有返回字典都必须恰好两个字符串键 `outcome / reason`。不要用 truthiness 分流：字符串 `"stop"` 本身也是真值。

### 安全与非目标边界

- 不得调用模型、工具或 fake，不得导入或修改生产工具注册表，不得创建公开三字段结果。
- 不得给函数增加 `run_context` 参数，不得占 step 或写七字段日志；除消费集合外，不得修改三个输入对象。
- `confirmation_id` 负责唯一标识和 replay 防护，不单独证明签发者身份。本 Gate 假设 confirmation 只由受信客户端确认界面创建并传入，绝不能把模型生成的字典当成 confirmation；签名令牌、进程重启恢复和多实例原子存储留到后续阶段。
- 当前是同步单进程 V1；不要在本切片扩展锁、数据库或持久化。

### I1a 完成标准

- 函数不再抛 `NotImplementedError`，且只实现上述纯门逻辑。
- fresh allow / fresh deny 都在返回前消费 ID；missing / invalid / mismatch / replay 不产生新的集合变化。
- 没有 fake、模型、工具、step、七字段日志或公开结果副作用。
- 不实现后续编排，不提前创建 fake 或 eval 文件。

用户完成代码后回复：`改完了 I1a`。本检查点未通过行为断言前，不能进入 fake 执行接线。

### I1a 用户完成信号与实现证据

用户完成信号：

> 改完了 I1a

实际实现位于 `consume_action_bound_confirmation()`，用户独立完成了以下控制流：

- 对可信 `planned_action` 和 `consumed_confirmation_ids` 使用 exact 内建类型、exact keys、非空字符串与集合成员检查；非法调用方状态抛 `ValueError`。
- 将 confirmation 依次分类为 missing、invalid、mismatch、replayed；这些分支均返回 exact 两字段内部结果且不新增消费记录。
- 对 fresh、合法、精确绑定的 confirmation，先执行 `consumed_confirmation_ids.add(confirmation_id)`，再按 allow / deny 返回；fresh deny 同样消费 ID。
- 函数签名没有 fake、模型、工具或 `run_context`，实现未访问生产 `TOOL_REGISTRY`，也未构造公开三字段结果。

### I1a 直接验证

验证方式：不创建一次性 runner；从项目根目录按文件路径加载模块，使用内存字典、集合与快照执行直接断言。

决定性覆盖：

- fresh exact allow：返回 exact `allow / confirmed`，消费 `conf-1`。
- 同一 allow replay：返回 exact `stop / confirmation_replayed`，集合无新增变化。
- fresh exact deny：返回 exact `stop / user_denied`，仍消费 `conf-2`。
- missing：1 类；invalid confirmation：8 类，包含 `True / 1 / "yes"` 等 truthy 或错类型 decision；mismatch：4 类，包含 request、tool、顶层参数和嵌套参数变化；均 stop 且不消费。
- caller invariant：6 类非法 planned action 与 5 类非法消费集合均抛 `ValueError`，没有伪装成安全停止。
- 输入对象快照保持不变；外部 `run_context.step / logs` 保持不变；生产 `TOOL_REGISTRY` 仍恰好只有 `read_material / search_web`。

命令与结果：

- `.venv/Scripts/python.exe -m py_compile code/stage4/a4_gate_research_summary_agent.py`：PASS。
- 内存断言：`1 allow + 1 replay + 1 deny + 1 missing + 8 invalid + 4 mismatch + 6 caller-invariant + 5 consumed-set` 全部 PASS。
- `git diff --check`：退出码 0；仅有 Windows 工作区未来 LF → CRLF 的提示，不是语法或空白错误。
- 第一次临时断言入口曾尝试按 `code.stage4` 包路径导入，但仓库的 `code/` 不是 Python package，故在触及用户函数前得到 `ModuleNotFoundError`；改为按文件路径加载后全部通过，该测试入口问题不计入用户实现结果。

### I1a 批改

- Correctness：PASS。分支优先级、exact 返回、绑定比较、replay 和消费时点全部符合冻结合同。
- Completeness：PASS。fresh deny 消费、非法 truthy decision、嵌套参数错绑和调用方不变量均已覆盖。
- Causality：PASS。实现结构体现“先确认当前动作、再防重放、先消费后副作用”的 at-most-once 因果顺序。
- Precision：PASS。未把 `"stop"` 当布尔值分流，未把安全停止伪装成调用日志或公开新状态。
- 教学检查点状态：`A4-Gate-HITL-I1a = PASS`。这证明纯确认门实现通过，不等于 fake 编排、公开结果映射、危险 eval 或父 Gate 已通过。

## `A4-Gate-HITL-I1b/I1c`｜助手机械接线与故障注入

### 为什么不再要求用户重复实现

用户已在 G1 冻结 allow / deny / 缺失 / 错绑 / replay 的副作用与公开停止规则，并在 I1a 独立实现最关键的动作绑定、一次性消费和原因优先级。后续固定安全 summary、显式注入 fake、沿用既有 `reserve_call_step() / append_call_log()` 模板和整理合同，均可由这些 PASS 证据唯一推导，属于助手机械接线，不换措辞要求用户重写。

### 实际代码增量

- `build_high_risk_confirmation_stop_result()`：只接受五种固定 stop 分类，映射为 exact 三字段 `needs_manual`、`sources=[]`；summary 使用固定安全文案，不拼接原始 confirmation、参数、异常正文或内部日志。
- `execute_high_risk_action_with_confirmation()`：返回 exact 两键 `tool_result / stop_result`；stop 直接公开停止，allow 才执行显式注入的内存 action executor。
- 新增执行器与请求账本绑定：调用方同时传入 `action_executor_name` 与 callable，名称必须和 `planned_action.tool_name` 精确匹配；同时要求 `confirmation.request_id == planned_action.request_id == run_context.request_id`。调用方配置或跨请求错绑在消费 ID 前以 `ValueError` 暴露，防止“用户确认 A、客户端执行 B”或“请求 A 的授权记到请求 B 的账本”。
- fake 没有进入 `TOOL_REGISTRY` 或 `build_tool_schemas()`；生产能力仍只有两个只读工具。
- 对 action arguments 使用防御性深复制后再传给 fake，避免执行器修改嵌套参数时反向篡改已经冻结的 `planned_action`。
- exact allow 的顺序为：确认门先消费 ID → 防御复制参数 → `reserve_call_step()` → fake → 成功或异常均写一条真实 `tool_call` 日志；异常日志只保存异常类型名，随后保持原异常 identity 上抛。

### 机械同步的 Problem Contract

第 7、9、11 节已将旧 `confirmed == True / False` 更新为 action-bound confirmation：exact allow 1/1/1；deny / missing / invalid / mismatch / replay 0/0/0；受信客户端签发 ID、fresh deny 也消费、fake 显式注入、执行器名称绑定、生产注册表零污染，以及并发 / 持久化留后续阶段。

### I1b/I1c 内存故障注入结果

- fresh exact allow：fake 1 次、step `0 → 1`、logs `0 → 1`；新增日志恰好七字段，`event_type="tool_call"`、`model=None`、工具名精确、`error=None`。
- deny / missing / invalid / mismatch / replay：五类均 fake 0、step +0、logs +0；公开 stop result 恰好三字段，`status="needs_manual"`、非空固定 summary、`sources=[]`；只有 fresh deny 新消费 ID。
- fake 抛出自定义异常：首次 fake 1、step +1、logs +1，日志只记异常类名；原异常对象 identity 上抛，confirmation ID 保持已消费。再次提交同一 confirmation 时 fake、step、logs 均不再增加，并得到 replay 的 `needs_manual`。
- `MAX_STEPS` 已满：fresh exact allow 在确认门消费 ID 后由既有预算门拒绝，fake 0、step / logs 不变；再次提交同一 confirmation 仍先因 replay 停止，不能利用预算失败重复授权。
- 输入不变性：planned action 与 confirmation 保持不变；即使 fake 修改收到的嵌套参数副本，也不能反向改变冻结动作。
- 能力隔离：测试前后 `TOOL_REGISTRY` 的键和值 identity 不变，schema 名仍恰好为 `read_material / search_web`，不存在 `fake_delete_source`。
- 验证：内存断言 PASS；`py_compile` PASS；`git diff --check` 退出码 0。

### 助手机械接线的独立审计闭环

- 首次独立审计判定 `RETRY`：初版只验证 confirmation ↔ planned action 与 planned action.tool name ↔ executor name，却遗漏 `planned_action.request_id == run_context.request_id`。这会允许请求 A 的合法授权执行 fake，却把真实调用日志记入请求 B，属于跨请求授权 / 账本错绑。
- 该缺口属于助手 I1b 机械接线，不改变用户 I1a 的纯确认门 `PASS`。
- 修复：在调用 `consume_action_bound_confirmation()` 之前增加 planned action ↔ run context 的 request ID 精确比较，并同步 Problem Contract 三方绑定链。
- 再注入：`planned_action / confirmation = req-A`、`run_context = req-B` 时，抛 `ValueError`；ID 消费 0、fake 0、step +0、logs +0、整个 run context 快照不变。
- 独立复核最终判定：`PASS`。此前 allow、五类 stop、fake 异常后 replay 与生产注册表隔离结论保持通过。

实现验证状态：I1b/I1c 代码与合同已通过助手机械验证；它们尚不是新的用户理解证据，需完成下面唯一一次代码轨迹审核。

## `A4-Gate-HITL-U1`｜fake 失败后的重放组合审核

请读取 `execute_high_risk_action_with_confirmation()`，只分析这一条组合轨迹：

- 初始 `run_context.step = 4`，已有 4 条合法调用日志。
- `planned_action`、action executor name 与 fresh confirmation 都精确绑定；`decision="allow"`，`confirmation_id="conf-timeout"` 尚未消费。
- fake 在第一次真实调用时抛出同一个 `TimeoutError` 对象。
- 随后客户端不生成新授权，而是再次提交完全相同的 confirmation；第二次 fake 如果被调用本可正常返回，但这不是允许你据此放行的理由。

请按“第一次提交 / 第二次提交”分别回答：

1. `conf-timeout` 是否已消费？fake 的本次与累计调用次数是多少？
2. `run_context.step` 和日志条数分别变成多少；若新增日志，它的 `event_type / tool_name / error` 应是什么？
3. 本次函数是正常返回还是异常向上暴露；若正常返回，公开 `status / sources` 是什么？
4. 为什么第二次不能因为“上次 fake 超时、这次可能成功”而重用原 confirmation？

完成标准：区分“真实调用已经发生并必须记账”和“同一授权是否仍可复用”；正确追踪先消费、真实调用、异常日志、原异常传播与 replay 短路，不把 fake 超时误当成获得第二次副作用权限。

### U1 用户原始答案

> 第一次提交（confirmation 合法，但 fake 调用超时）
>
> - `conf-timeout` 已消费；在检查通过后、执行 fake 之前加入 `consumed_confirmation_ids`。
> - fake 本次 1 次、累计 1 次。
> - `run_context.step` 增加 1，七字段日志增加 1 条。
> - 新日志：`event_type=tool_call`、`tool_name=fake_delete_source`、`error=timeout（或具体超时错误信息）`。
> - 函数正常返回；工具超时被视为工具失败，通过公开结果返回，不向上抛异常。
> - 公开 `status=tool_failure`、`sources=[]`。
>
> 第二次提交：
>
> - `conf-timeout` 已在第一次消费，本次检查发现它已存在。
> - fake 本次 0 次、累计 1 次。
> - step 增加 0、七字段日志增加 0 条，不新增日志。
> - 函数正常返回安全停止；公开 `status=needs_manual`、`sources=[]`。
> - 原 confirmation 是一次性授权单；第一次动作已尝试，可能已产生部分副作用或进入未知状态。复用会让一张授权触发第二次危险动作，可能造成重复删除、状态不一致或安全事件，因此必须在副作用前拒绝。

### U1 批改

- 正确：第一次在 fake 前已经消费 ID；fake 真实调用 1 次，所以共享 step 与七字段日志各增加 1。初始均为 4，因此绝对值应变成 step 5、日志 5 条。
- 正确：第二次 replay 在 fake 前短路，本次 fake / step / logs 都增加 0，累计 fake 仍为 1；正常返回 exact `needs_manual`、`sources=[]`。
- 正确：能解释超时不等于“确认未使用”。危险动作可能已经部分发生或处于未知状态，at-most-once 必须优先阻止同一授权造成第二次副作用。
- 缺口 1：当前日志冻结的是异常**类型名**，代码使用 `error=type(exc).__name__`。题设为内建 `TimeoutError`，所以 exact 值是 `"TimeoutError"`；不是宽泛的 `"timeout"`，也不能记录可能含敏感信息的异常正文。
- 缺口 2：当前函数在 `except` 中追加失败工具日志后执行裸 `raise`，因此保持同一个 `TimeoutError` 对象向上暴露；该分支没有走到任何正常 `return`，也没有在本函数内构造 `tool_failure` 或其他公开三字段结果。若未来需要公开映射，必须由更外层明确接管，不能把“捕获并记日志”误解为“异常已转换为公开结果”。
- 教学检查点状态：`A4-Gate-HITL-U1 = PARTIAL`。第二次 replay 与一次性授权因果已经通过，只订正第一次异常处理，不重复回答正确部分。

### `A4-Gate-HITL-U1-R1`｜只订正第一次 fake 超时

请只回答两点：

1. 第一次新增日志的 `error` exact 字符串是什么？为什么不能写异常正文？
2. `append_call_log(...)` 之后紧跟裸 `raise`。因此当前函数是正常返回公开三字段结果，还是让什么异常怎样向上暴露？这一分支是否存在可填写的公开 `status / sources`？

不要重答 ID 消费、fake 次数、step / logs 或第二次 replay；这些部分已经通过。完成标准：能区分“catch 住是为了记账”与“catch 后转换为公开结果”，并按当前代码而不是理想设计判断真实行为。

### U1-R1 用户定向订正原答

> 1. 第一次新增日志的 `error` exact 字符串是 `"TimeoutError"`。
>
> 为什么不能写异常正文：`error` 字段只记录 `type(exc).__name__`，即稳定的异常类型名。异常正文可能包含路径、参数、服务信息或其他敏感内容，写进日志会破坏日志结构、泄露内部信息，并且不利于后续聚合和审计。
>
> 2. `append_call_log(...)` 后紧跟裸 `raise`，因此当前函数不是正常返回公开结果。它会让原来的同一个 `TimeoutError` 异常对象继续向上传播，即裸 `raise` 原样重抛当前捕获的异常。
>
> 这一分支没有可填写的公开 `status / sources`：二者均不适用。

### U1-R1 批改与 HITL 单元收口

- 正确：exact 日志值为 `"TimeoutError"`；能解释类型名既稳定、便于聚合，也避免异常正文泄露路径、参数、服务信息或其他内部内容。
- 正确：能沿实际 `except → append_call_log → raise` 代码判断原异常对象 identity 向上暴露，而不是根据理想设计虚构公开 `tool_failure`。
- 正确：既然本函数没有正常返回，第一次提交不存在可填写的公开 `status / sources`；未来若要映射，必须由更外层显式负责。
- 教学检查点状态：`A4-Gate-HITL-U1 = PARTIAL → PASS`。
- HITL 单元教学结论：G1 动作绑定合同、I1a 用户代表性实现、I1b/I1c 机械接线与独立安全复核、U1 组合故障审核全部闭合。该结论仍只是父 Gate 的一个子单元，不把 `A4-Gate` 改为 PASS。

## 本 Session 收尾与接力边界

- 今日计划范围：`A4-Gate-HITL` 已完成；G1、I1a 与 U1 均有用户理解 / 实现证据，I1b/I1c 有直接故障注入和独立复核证据。
- 今日剩余：当前已排入的 HITL 学习单元没有剩余；不把新的 eval 设计、五问或整关复核追加到今日预算外。
- 任务总剩余：创建并冻结自包含的 14 条 `eval_cases.json`（10 normal / 3 failure / 1 danger，至少 3 holdout）；执行完整数据集并记录分项结果；回答五个 Gate 必答问题；总体定稿 Problem Contract；由 `ai-agent-learning-review` 做父 Gate 正式复核。
- 下次 Session 起点（**不属于今天**）：先按设计产物分工列出“用户必须决定 / 助手机械整理 / 用户最终审核”；助手依据现有合同展示完整 14 案例推荐矩阵，用户只做一次会改变验收覆盖的最终批准——确认 3 个 failure 席位所代表的失败域，以及 N08（新 topic 迁移）/ N09（新沙箱相对路径迁移）/ N10（输入规范化迁移）作为首次运行前冻结的 3 条 holdout。确认后助手再建立 `code/stage4/eval_cases.json` 的唯一骨架。该工作必须在未来 Session 重新排入不超过 180 分钟的预算后开始。
- 未开始产物边界：本 Session 不创建 `code/stage4/eval_cases.json`、runner、baseline、报告目录或临时持久测试文件。
