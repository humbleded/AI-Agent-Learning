# 每日学习打卡

日期：2026-06-13

学习时长：

完成任务编号：P0-08 文件、JSON、CSV（PASS）

今日定位：

- 主线目标：完成 P0-08 的文件读写、路径处理、JSON 序列化与反序列化。
- 今天已经完成：阅读 `文件读写`、`操作文件和目录`、`序列化`；`code/stage0/p0_08_progress_file.py` 已能运行。
- 今天补上的点：已把主流程改成读取 `resources/stage0_tasks.txt`，构造任务状态列表，写入 `resources/stage0_progress.json`，再用 Python 读回验证。
- 今日先不强行推进：CSV。P0-08 主线按文件读写和 JSON 判定通过，CSV 作为可选扩展保留。

看了哪些视频/哪一集：

- CS50P Week 6 `File I/O`（可选补充 / 未记录具体进度）

读了哪些文档/哪一节：

- 廖雪峰 Python 教程：文件读写（已看完）
- 廖雪峰 Python 教程：操作文件和目录（已看完）
- 廖雪峰 Python 教程：序列化（已看完）

代码/产物位置：

- `daily/2026-06-13.md`
- `code/stage0/p0_08_progress_file.py`
- `resources/stage0_tasks.txt`
- `resources/stage0_progress.json`

运行提示：

进入项目：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
```

优先使用项目虚拟环境运行：

```powershell
.\.venv\Scripts\python.exe -m py_compile code\stage0\p0_08_progress_file.py
.\.venv\Scripts\python.exe code\stage0\p0_08_progress_file.py
```

如果临时只用系统 Python：

```powershell
python -m py_compile code\stage0\p0_08_progress_file.py
python code\stage0\p0_08_progress_file.py
```

本次运行结果记录：

```text
['学习文件读写', '学习 JSON', '练习异常处理']
任务数量： 3
jsonpath: C:\Users\26823\Desktop\AI-Agent-Learning\resources\stage0_progress.json
```

复查说明：脚本还会打印 `D:/` 下的文件列表和 `D:\` 绝对路径，这是练习 `os.listdir()` / `os.path.abspath()` 留下的额外输出，不影响 P0-08 主线通过；后续可以清理。

当前生成的 `resources/stage0_tasks.txt`：

```text
学习文件读写
学习 JSON
练习异常处理
```

当前生成的 `resources/stage0_progress.json`：

```json
[
  {
    "name": "学习文件读写",
    "status": "todo"
  },
  {
    "name": "学习 JSON",
    "status": "todo"
  },
  {
    "name": "练习异常处理",
    "status": "todo"
  }
]
```

今日任务清单：

- [x] 阅读 `文件读写`
- [x] 阅读 `操作文件和目录`
- [x] 阅读 `序列化`
- [x] 运行 `code/stage0/p0_08_progress_file.py`
- [x] 确认脚本能生成 `resources/stage0_tasks.txt`
- [x] 确认脚本能生成 `resources/stage0_progress.json`
- [x] 确认 JSON 能被 Python 重新解析
- [x] 回答 P0-08 三个检查题第一版
- [x] 不看原代码，复写一个最小版文件/JSON脚本
- [ ] 用自己的话解释 `Path(__file__).resolve().parents[2]`
- [ ] 用自己的话解释 `read_text()` / `write_text()` 为什么要写 `encoding="utf-8"`
- [x] 用自己的话解释 `json.dumps()` 和 `json.loads()` 的区别
- [x] 让 Codex 批改 P0-08，决定 PASS 还是继续 RETRY

我的问答答案：

1. 文本文件和 JSON 文件分别适合保存什么？

```text
文本文件适合保存文字，方便人阅读，JSON适合保存信息，给程序之间传递信息。
```

1. 文件路径错误时如何定位？

```text
查看报错信息。
```

1. 读写文件为什么要考虑编码？

```text
因为语言编码可能不一样，比如中文用GBK，其他的用utf-8，用不同的编码读取，可能会导致乱码。
```

遇到的问题：

```text
1. 代码是 Codex 生成的，我只能大概看懂它做什么。
2. 如果不看原代码，自己可能还写不出来。
3. 对路径、文件读写、JSON 序列化之间的完整流程还没有形成肌肉记忆。
```

我最不确定的点：

```text
1. `Path(__file__).resolve().parents[2]` 为什么能定位项目根目录。
2. 什么时候用普通文本文件，什么时候用 JSON。
3. `json.dumps()` / `json.loads()` 和文件读写本身是什么关系。
4. 如果文件不存在或路径写错，应该按什么顺序排查。
```

希望 Codex 检查：

- 检查 `code/stage0/p0_08_progress_file.py` 是否能运行。
- 检查 JSON 是否真的能被 Python 重新解析。
- 批改我的 3 个检查题答案。
- 帮我把这个任务拆成一个能自己复写的小练习。
- 判断今天是 P0-08 PASS，还是先 RETRY 一轮。

Codex 判定：

```text
PASS
```

Codex 当天进度总结：

```text
P0-08 通过。

已验证：
1. `code/stage0/p0_08_progress_file.py` 可以通过 `py_compile`。
2. 脚本可以运行。
3. 脚本从 `resources/stage0_tasks.txt` 读取任务文本。
4. 脚本把任务转换为包含 `name` 和 `status` 的字典列表。
5. 脚本写入 `resources/stage0_progress.json`。
6. 生成的 JSON 可以被 `json.loads()` 重新解析为 Python list。
7. 第二轮检查题能说明 `json.dumps()` / `json.loads()`、路径排查、`ensure_ascii=False` 和完整流程。

保留建议：
脚本里还有多余的 `D:/` 目录列表输出，后续可以清理；这不影响 P0-08 主线通过。
```

Codex 总进度总结：

```text
当前已通过：P0-01、P0-02、P0-03、P0-04、P0-05、P0-06、P0-07、P0-08。
下一项：P0-09 HTTP 请求。
```

Codex 问答点评与补充：

```text
结论：PASS

做得对：
1. 第一轮问答方向正确，第二轮问答把 JSON、路径排查、编码和流程补完整了。
2. 代码已经从项目内 `resources/stage0_tasks.txt` 读取文本，而不是读取临时的 `D:/timezone.txt`。
3. `write_json()` 和 `read_json()` 已经被主流程调用，生成的 JSON 能重新解析。
4. 你能说出 `json.dumps()` 是 Python 数据到 JSON 字符串，`json.loads()` 是 JSON 字符串到 Python 数据。

仍可改进：
1. `read_json(json_path)` 当前只是调用，没有把返回值保存成 `loaded` 再打印；不过没有报错已经证明 JSON 可解析。
2. `os.listdir('D:/')` 和 `os.path.abspath('D:/')` 属于额外练习输出，建议下次清理掉，让脚本输出更聚焦。
3. 当前路径是绝对路径，能跑；以后更推荐用 `Path(__file__).resolve().parents[2]` 拼项目内路径，这样换机器也更稳。

下一步：
进入 P0-09 HTTP 请求。P0-08 的文件读写和 JSON 主线已经可以标为通过。
```

Codex 对问题/不确定点的解释：

```text
今天先记住最小流程：

1. 准备路径：
   - 找到项目根目录。
   - 拼出输入文件路径。
   - 拼出输出 JSON 路径。

2. 读取文本：
   - 如果输入文件不存在，先创建一个示例文件。
   - 用 `read_text(encoding="utf-8")` 读内容。
   - 用 `splitlines()` 按行拆开。
   - 用 `strip()` 去掉空白。

3. 转成结构化数据：
   - 把每一行任务变成一个 dict。
   - 例如：`{"name": "学习文件读写", "status": "todo"}`

4. 写入 JSON：
   - 用 `json.dumps(data, ensure_ascii=False, indent=2)` 把 Python 数据转成 JSON 字符串。
   - 用 `write_text(..., encoding="utf-8")` 写入文件。

5. 读回 JSON：
   - 用 `read_text(encoding="utf-8")` 读 JSON 字符串。
   - 用 `json.loads(...)` 把 JSON 字符串转回 Python 数据。
```

补救任务：

- [x] 补救 1：不看 `p0_08_progress_file.py`，新建或临时写一个最小脚本，完成“写入 txt -> 读取 txt -> 写入 json -> 读回 json”。
- [x] 补救 2：把下面 4 个函数名按顺序写出来，并说明每个函数做什么：
  - `ensure_task_file()`
    `ensure_task_file()`：检查任务文本文件是否存在，如果不存在就创建一个示例文件。
  - `read_tasks()`
    `read_tasks()`：用utf-8编码读取任务文本文件的内容，并返回任务列表。
  - `build_progress()`
    `build_progress()`：把任务列表转换成一个包含任务名称和状态的字典列表。
  - `main()`
    `main()`：主函数，调用其他函数完成整个流程。
- [x] 补救 3：回答第二轮检查题。

第二轮检查题：

1. `json.dumps()` 和 `json.loads()` 分别把什么变成什么？

```text
json.dumps()把Python数据变成JSON字符串，json.loads()把JSON字符串变成Python数据。
```

1. 如果程序提示找不到 `resources/stage0_tasks.txt`，你会按哪 3 步排查？

```text
1. 检查当前工作目录是否正确。
2. 检查 `resources/stage0_tasks.txt` 是否真的存在。
3. 检查代码中拼接的路径是否正确。
```

1. 为什么这里写 JSON 时用了 `ensure_ascii=False`？

```text
1. 因为 JSON 默认会把非 ASCII 字符转义，设置 `ensure_ascii=False` 可以让中文等字符正常显示。
```

1. 不看原代码，请写出这个任务的 5 步流程，不要求写代码。

```text
1. 准备路径：找到项目根目录，拼出输入文件路径和输出 JSON 路径。
2. 读取文本：如果输入文件不存在，先创建一个示例文件。用 `read_text(encoding="utf-8")` 读内容，用 `splitlines()` 按行拆开，用 `strip()` 去掉空白。
3. 转成结构化数据：把每一行任务变成一个字典，例如 `{"name": "学习文件读写", "status": "todo"}`。
4. 写入 JSON：用 `json.dumps(data, ensure_ascii=False, indent=2)` 把 Python 数据转成 JSON 字符串，用 `write_text(..., encoding="utf-8")` 写入文件。
5. 读回 JSON：用 `read_text(encoding="utf-8")` 读 JSON 字符串，用 `json.loads(...)` 把 JSON 字符串转回 Python 数据。
```

下一步：

- P0-08 已标为 PASS。
- 下一步进入 P0-09 HTTP 请求。
- 可选清理：删除 `p0_08_progress_file.py` 中多余的 `D:/` 列目录输出，把绝对路径改成项目相对路径。
