# 每日学习打卡

- 日期：2026-07-25
- 当前任务：A4-04 Plan-and-Solve
- 任务状态：`DOING`
- 今日环节：跨日正式练习

## Session / 任务索引

- Session 1 — A4-04 正式练习：承接 [daily/2026-07-22.md](2026-07-22.md) 的 G1–G4、[daily/2026-07-23.md](2026-07-23.md) 的 G4–G7 与 C1–C4，以及 [daily/2026-07-24.md](2026-07-24.md) 的 C4–C6 实现证据。
- 真实产物：`code/stage4/a4_04_plan_solve_demo.py`。
- 通过标准：输出计划、执行结果与事实性复盘；必答范围为 Planning 解决的问题、错误计划的处理边界、简单任务无需规划的原因。

## 正式题集预检与语义覆盖图

- 已覆盖且本题集排除重复：
  - G1：Planner 与 Executor 职责、Executor 的原始目标/完整计划/历史结果上下文。
  - G2：代码围栏缺失时的 `split → [1] → IndexError → []` 与格式契约。
  - G3：错误历史会继续污染后续步骤，当前静态 Executor 不会自动校验或重规划。
  - G4：`print` 只显示、`return` 才把值交给调用方。
  - G5：固定工作流与动态 Planning 的选择判据，以及新约束后的状态更新、重评选项和重规划。
  - G6：计划/执行结果/事实复盘三项验收，以及事实复盘与 Reflection 的边界。
  - G7：Planning、Replanning、Reflection 的职责和产物区别。
  - C1–C6：安全解析、Planner、携带 `(step, result)` 历史的 Executor、三分支复盘、完整编排、确定性 demo。
- 本次只补 3 个高价值目标：
  1. 当前真实代码遇到模型适配器返回 `None` 时的级联故障诊断、最小修复和回归设计；合并旧弱点 `WP-08`，不额外占第二个旧题席位。
  2. 对 C5 曾出现的“屏幕输出正确但空计划无复盘 / 返回值类型错误”做换表面独立代码审查。
  3. 把当前静态 Plan-and-Solve 迁移到需要 Replanning 的新场景，说明状态、计划和轨迹应怎样变化，而不重复三术语分类题。
- 计划证据：G1–G7 的 7 个合格语义目标 + 本次 F1–F3，任务级目标为 10 个，位于 8–12 上限内。
- 生产绑定：F1 使用当前真实产物的内存故障注入与实际堆栈；不创建一次性 runner 或报告。
- 旧复习选择：`WP-08`（异常路径返回 `None` 会毒化 history），与当前 F1 合并；其余活跃/到期项不叠加。
- 工作场景：F1 绑定当前代码、失败轨迹和接口契约；普通口述最多形成 `EXPLAINED`，不在练习阶段升级场景台账。
- 算法：W4 仍有 4 道新题 `DEBT`，但本次未安排独立算法时段，不追加到 A4-04 正式题集。
- 计划学习投入：本次 3 题预计 60–90 分钟用户主动分析/作答，订正按实际需要替换剩余题目，不记录实际用时。

## 正式练习

### A4-04-F1 — 模型调用异常后的级联故障（C/D，合并 WP-08）

- 来源：当前真实产物 `code/stage4/a4_04_plan_solve_demo.py`。
- 安全故障注入：
  - 假适配器依次返回有效三步计划、`"r1"`、`None`、`"r3"`。
  - 实际共调用 4 次；执行第 3 步时的 prompt 已包含 `('s2', None)`。
  - 三步均被打印，随后在 `build_recap` 的 `(step, result.strip())` 抛出 `AttributeError: 'NoneType' object has no attribute 'strip'`。
- 题面：假设 `None` 来自未来真实模型适配器的异常分支。请完成同一个事故分析：
  1. 从最早出现问题的位置开始，按 `llm_call → execute_plan/results/history → 第 3 步 prompt → build_recap` 顺序解释故障如何传播。
  2. 选择最小且合适的修复边界，写出关键 Python 代码，并说明该边界必须维持什么输入/输出契约；同时说明为什么只把 `result.strip()` 改成一个“能接受 None”的写法仍不够。
  3. 给出两个精确回归检查：一个只验证异常返回边界，一个验证完整三步运行不崩且 history 中不再出现 `None`。写清输入和断言。
- 当前状态：已于 2026-07-29 跨日作答；批改、订正与回炉证据见 [daily/2026-07-29.md](2026-07-29.md)。
