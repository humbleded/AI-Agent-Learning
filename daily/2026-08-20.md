# 2026-08-20 学习记录

- 当前任务：`A4-Gate` 研究摘要 Agent
- 当前学习单元：第一块 `A4-Gate-C4b-2a｜候选摘要响应失败的受控恢复` 与第二块 `A4-Gate-C4b-2b-1｜Reflection 响应失败的受控恢复` 均已收口
- 任务状态：`DOING`（沿用 `tracker/progress.md`；本页记录教学 / 实现接力，不产生父任务正式判定）
- 第一块计划学习预算：180 分钟——定向阅读 25 分钟、行为决策与轨迹表 25 分钟、用户实现 100 分钟、故障注入与回归 20 分钟、用户最终审核 10 分钟；该块已收口
- 第二块授权与计划学习预算：用户在第一块完成后明确说“开启今天第二个学习块”，据此显式启用仅限 2026-08-20 的休息日第二块；第二块 150 分钟——定向材料与行为决策 25 分钟、用户实现 90 分钟、故障注入与回归 25 分钟、用户最终审核 10 分钟。全天计划容量 330 分钟，不超过双块 360 分钟上限；第二块不追加算法、旧复习或另一套概念题量，未完成则按真实停点顺延、不形成补偿欠账
- 实际学习时长：不记录；助手读取、创建骨架、运行命令、等待模型 / 工具和自动写回不计入计划预算

## 当日 Session / 任务索引

| Session | 任务 | 环节 | 关联前序 daily | 当前状态 / 下一步 |
| --- | --- | --- | --- | --- |
| S1 | `A4-Gate-C4b-2a` | 候选摘要协议 / 内容失败的恢复预算与最小实现 | [2026-08-05](2026-08-05.md)、[2026-08-06](2026-08-06.md)、[2026-08-07](2026-08-07.md)、[2026-08-08](2026-08-08.md)、[2026-08-09](2026-08-09.md)、[2026-08-10](2026-08-10.md)、[2026-08-11](2026-08-11.md)、[2026-08-12](2026-08-12.md)、[2026-08-13](2026-08-13.md)、[2026-08-17](2026-08-17.md)、[2026-08-18](2026-08-18.md)、[2026-08-19](2026-08-19.md) | `DOING`；`G1 / G2 / G3 / I1a～I1h / I2 / U1 = PASS`，`C4b-2a` 教学 / 实现子单元已收口；无产品代码 TODO |
| S2 | `A4-Gate-C4b-2b-1` | Reflection 协议 / 内容失败的完整链预算、至多一次受控恢复与安全停止 | 本页 S1；同上 12 份 A4-Gate 前序 daily | `DOING`；`G1 / I1a～I1d / U1 = PASS`，助手同构整理的 `I1e / I1f / I2` 已验证；子单元已收口，无产品代码 TODO |

## 任务证据接力

### Session S1｜`A4-Gate-C4b-2a`

任务状态与学习单元：父任务 `A4-Gate = DOING`。本 Session 只处理“真实工具已经成功、候选模型 API 已返回，但候选响应协议或内容不可用”时的受控恢复。Provider / HTTP 异常重试分类、Reflection 响应失败、Refinement / 最终 validator 失败、HITL、14 条 eval 和正式 Gate 复核不在本单元。

关联前序 daily：上表列出的 12 份 A4-Gate Session 证据。2026-08-14～16 没有学习记录，不补建空白 daily。

资料定位与版本：

- `code/stage4/problem-contract.md` 第 6 节“Agent 循环、停止与恢复”及第 8 节“失败与拒绝边界”，重点为 `max_steps=6`、失败后只能依据本轮真实证据修订、不可返回未通过校验的候选。
- `code/stage4/a4_gate_research_summary_agent.py` 的 `reserve_call_step()`、`generate_candidate_summary()`、`reflect_refine_and_validate()`、`run_research_summary_once()`；当前仓库基线 `HEAD da7751f2525c5de3a2ecb7a9da9d94bed7d7f7d3`，工作树在 Session 启动前干净，源码活动 `# TODO` / `raise NotImplementedError` 为 `0/0`。
- DeepSeek Chat Completions API `Responses`：https://api-docs.deepseek.com/api/create-chat-completion/#responses（2026-08-20 核验）；`finish_reason` 当前列出 `stop / length / content_filter / tool_calls / insufficient_system_resource`，assistant `message.content` 为 nullable。

看了哪些视频/哪一集：无；本次为交付冲刺，不安排视频。

读了哪些文档/哪一节：仅选读上述合同、当前函数和官方响应 schema；没有通读整份 DeepSeek API 或重新带读 A4-05 Reflection。

已覆盖目标及证据位置：C1/C2、C3a～C3d 正常工具 / Candidate / Reflection / Refinement / validator 双路径；C4a-1 统一 step、七字段日志、五步正常链、请求隔离与第 7 次调用硬门；C4a-2 工具重试 / 终点映射；C4b-1 一次 Action correction 与完整链预算；本日 G1 / G2 / G3、I1a～I1h 与 I2 已闭合，固定并验证候选恢复预算、响应白名单、失败来源语义、纯 helper、至多一次恢复编排、生产接线、公开映射及端到端回归。详见本页后续原始证据。

RETRY / 仍需迁移：当前没有活动教学或代码 RETRY。I2 的内存端到端故障注入、旧链回归与真实 DeepSeek `sample.txt` 正常链均通过；U1 已确认最终调用轨迹、停止结果和人工接管来源语义符合意图。

未覆盖 PASS 标准 / 下次起点：本单元没有剩余检查点；父任务之后仍有候选 Provider/API 异常分类与恢复、Reflection 失败恢复、Refinement / 最终 validator 失败恢复、HITL allow / deny、14 条 `eval_cases.json`、五个 Gate 必答问题、Problem Contract 定稿和正式 Gate 复核。

代码/产物位置：`code/stage4/a4_gate_research_summary_agent.py`；`code/stage4/eval_cases.json` 尚未创建，本 Session 不提前创建。

notes 路径与状态：无；本代码切片不新建项目 notes。

### 用户 / 助手职责

- 用户必须亲自决定：候选响应失败后，哪些调用轨迹仍允许发起一次候选恢复，并解释为什么剩余预算必须容纳完整可信下游链；策略冻结后完成代表性核心控制流。
- 助手自动完成：根据已通过合同整理状态表、在行为决策通过后即时创建唯一 TODO 骨架、准备无落盘 fake / spy、执行验证并记录原始证据；不替用户写核心答案。
- 用户最终审核：模型 / 工具调用次数、step / logs、真实工具是否被重复执行、公开结果及来源行为是否符合自己的意图。

### Preflight：回炉与算法

- `tracker/weak-points.md` 持久状态为 `NORMAL`，不是 `RECOVERY_BLOCK_PENDING`。按 2026-08-20 只读扫描，到期项为 `CD-005 / WP-08 / WP-28`，最老逾期 7 天；规则要求先在后续正式练习使用既有优先席位，不直接在本 Gate 编码 Session 追加回收块。
- `WP-08` 已在 `daily/2026-08-17.md` 的 Tool Message 新表面完成教学迁移，本日不换措辞重复。另两项与当前实现不直接相关，因此本 Session 旧复习目标为 0。
- W4 算法已经 `PASS`、债务为 0；今天不启动新算法内容块。

## 定向讲解与即时判断

### `A4-Gate-C4b-2a-G1`｜候选恢复的完整链预算策略

当前在做什么：先冻结候选响应失败后的停止 / 恢复策略，不写代码。这里的“候选响应失败”只指候选模型调用已经获得 API 响应并记录成功调用事件，但其 `choices / finish_reason / message / tool_calls / content` 未通过客户端协议或非空内容守门。

为什么需要：正常链在候选模型之后还要调用 Reflection 与 Refinement；额外候选恢复也占一个 step。若只判断“当前还没到 6”，可能恢复完候选后再也没有空间完成可信下游链，形成无法交付的半成品状态。

与前后步骤及最终产物的关系：G1 只决定何时允许候选恢复；通过后才创建当前唯一预算 / 控制流 TODO。工具证据已经取得，候选恢复不得重新执行工具；最终仍必须经过 Reflection、Refinement 和客户端 validator 才可能返回 `success`。

题目：见当前聊天中的同 ID 问题。一次只处理“哪些轨迹允许候选恢复及其预算因果”，暂不决定 Provider/API 异常、失败结果 `sources` 或最终公开映射文案。

原始作答：

> A.允许，因为是模型调用失败，可以允许重试，且步骤足够
>
> B.停止，因为调用模型失败了，步骤不足以支持后续了
>
> C.停止，因为步骤不足以支持后续了

订正与补充：三个停止 / 恢复判断均正确。A 路径的坏 Candidate 已占 step 3，剩余 step 4～6，刚好容纳 Candidate recovery、Reflection、Refinement；B / C 路径的坏 Candidate 已占 step 4，只剩两个 step，少于完整恢复链所需的三个，因此必须在新的恢复调用发生前停止。术语需校准：这里不是“模型调用没有发生”，而是“模型 API 已返回，但候选响应未通过客户端协议 / 内容校验”；该次真实调用仍占 step 并保留调用日志。恢复只复用本轮真实 Tool Observation，不重新执行工具。

单题状态：`PASS`。

当前任务累计即时考察有效证据数（跨日）：沿用全部前序证据；本 Session 新增 2 个 PASS 证据（G1、G2）。

仍需迁移 / RETRY 的目标：G1 / G2 无 RETRY；I1a 纯预算门已通过，下一硬检查点是实现候选响应纯分类器。

### `A4-Gate-C4b-2a-G2`｜候选响应失败的自动恢复白名单

当前在做什么：在预算门已闭合后，区分哪些“API 已返回但 Candidate 不可用”的失败允许消耗唯一一次候选恢复，哪些必须 fail-closed 停止。这里只决定恢复白名单，不处理 `.create()` 抛出的网络 / HTTP / Provider 异常，也不决定失败结果的 `sources` 和公开文案。

为什么需要：不同 `finish_reason` 与响应形态的风险不同；若用一个宽泛 `except RuntimeError` 全部重试，可能把安全过滤、Provider 容量故障或未知协议漂移错误地当成普通格式修订。

与前后步骤及最终产物的关系：G2 决定后续纯分类器和恢复控制流的分支；只有同时满足“失败类型在白名单内、局部恢复尚未用过、完整链预算允许”时，才可发起 Candidate recovery。完整问题与五类输入见当前聊天。

原始作答：

> A.允许恢复一次，因为是模型没有返回结果，再次请求一次查看结果
>
> B.允许恢复一次，因为是长度达到上限，再次请求一次减少长度
>
> C.允许恢复一次，因为可能是模型没有遵守Prompt的约定，重试一次看能不能遵守
>
> D.立即停止，因为想尝试绕开安全过滤，不能继续执行
>
> E.立即停止，因为继续尝试也无法识别，继续一样还是会错误

订正与补充：五类白名单判断全部正确。A 的精确含义是“API 已返回，但响应结构或正文不能形成可用 Candidate”，不是模型调用没有发生；A / B / C 允许的是复用同一份真实 Tool Observation 发起一次约束更明确的 Candidate 修订，不是原样盲重试。B 必须要求更短但仍满足证据约束；C 必须再次明确禁止 Tool Action。D 停止是为了避免自动修订演变成绕过安全过滤；E 停止是因为资源不足、缺失或未知协议原因不能由内容修订可靠解决，Provider/API retry 留给后续独立切片。

单题状态：`PASS`。

## 工程验证

运行提示：G1 / G2 / G3 与 I1a～I1h 已完成；当前源码活动 `TODO / NotImplementedError = 0/0`，I2 只使用直接命令、内存 fake / spy 与系统临时目录，不创建长期 runner、baseline 或报告文件。

### `A4-Gate-C4b-2a-I1a`｜候选恢复完整链纯预算门

用户原始提交：

```python
if not type(current_step) is int or not (1 <= current_step <= MAX_STEPS):
    raise ValueError("current_step 必须是位于 1 到全局上限之间的整数")

remaining_steps = MAX_STEPS - current_step
return remaining_steps >= 3
```

验证命令与关键结果：

- `.venv/Scripts/python.exe -m py_compile code/stage4/a4_gate_research_summary_agent.py`：`PASS`。
- 合法域映射：`{1: True, 2: True, 3: True, 4: False, 5: False, 6: False}`，全部 `PASS`，且返回值均为内建 `bool`。
- 非法域：`0 / 7 / True / False / 3.0 / "3" / None` 均抛 `ValueError`，`PASS`。
- 只读审查：函数只读 `MAX_STEPS`、只创建局部变量，不接收 / 修改 context，不占 step、不写日志、不调用模型 / 工具；本次源码未越过当前 helper。

批改与订正：核心行为与纯函数边界已经通过。首轮产物曾为 `PARTIAL`：已完成实现仍残留一个活动 `TODO(A4-Gate-C4b-2a)`，且需求要求用能表达“候选完整恢复链所需最少 step”的清晰名字承载数值 `3`，首轮仍是裸字面量。用户随后删除 TODO，新增模块常量 `MIN_STEPS_FOR_CANDIDATE_RECOVERY_CHAIN = 3` 并在预算判断中引用，没有重写已通过逻辑。

复核结果：再次执行 `py_compile`、合法域映射与全部非法域测试，结果仍全部通过；命名常量值为 `3`，Agent 源码活动 `TODO / NotImplementedError = 0/0`。

当前检查点状态：`PASS`。

Session 启动基线：工作树原本干净、`HEAD da7751f...`、Agent 源码活动 TODO / `NotImplementedError` 为 `0/0`；I1a 复核后仍为 `0/0`。

### `A4-Gate-C4b-2a-I1b`｜候选响应纯分类器

当前产物：已创建 `classify_candidate_summary_response(response)` 的唯一 TODO 骨架。它只把不可信响应转换为固定三键内部分类结果，不接收 context / client / messages / tool_result，不执行预算判断、模型 / 工具调用、恢复或公开结果映射。

用户首轮实现证据：实现了局部 `make_classification()` 与安全属性读取函数，先判 `finish_reason`，再在严格 `stop` 分支检查 message / tool_calls / content；最终合法分支保留原始非空正文。首轮原始关键判断包括：

```python
if not has_choices or type(choices) is not list or len(choices) != 1:
    ...
if type(finish_reason) is not str:
    ... "unknown_finish_reason" ...
if not has_content or type(content) is not str or not content.strip():
    ... "invalid_candidate_content" ...
```

首轮固定 reason 原文：`invalid_candidate_response_structure / content_filtered / candidate_output_truncated / unexpected_candidate_tool_action / insufficient_system_resource / missing_finish_reason / unknown_finish_reason / invalid_candidate_content`。

验证命令与关键结果：

- `py_compile`：`PASS`。
- 内存 fake 精确合同矩阵：`8/25 PASS`；行动级 `valid / recoverable / terminal` 方向为 `24/25`，多数失败由四个重复出现的 reason 名称不匹配造成，不是 17 个独立控制流错误。
- 纯函数 / 不修改输入探针：`PASS`；未调用 step、日志或 client factory。
- 正确覆盖：合法正文保留原文；`length / tool_calls / content_filter / insufficient_system_resource / unknown` 的大类方向；安全过滤与资源不足优先于坏正文；非空 Tool Action 优先于空正文；坏属性没有泄漏 `AttributeError / TypeError`。

首轮需订正：

1. 固定 reason 必须使用已冻结合同：`invalid_response_shape / length / unexpected_tool_calls / content_filter`，不能自定义同义字符串。
2. `choices` 只要求是非空列表，并沿用第一项；`len(choices) != 1` 错误新增了“必须恰好一项”的未授权限制。
3. `finish_reason is None` 属于 `missing_finish_reason`，不是 `unknown_finish_reason`。
4. message 缺少 `content` 字段属于 `invalid_response_shape`；字段存在但值为 `None`、非字符串或空白，才是 `invalid_candidate_content`。
5. 实现完成后仍残留活动 `TODO(A4-Gate-C4b-2a-I1b)`，需在复核前删除。

首轮检查点状态：`RETRY`（教学代码检查点，不是父任务正式判定）；保留已通过的分类顺序、纯函数边界和辅助函数，只做上述窄修正后复核。

第二轮原始修正：已把结构、截断、安全过滤和 `finish_reason="tool_calls"` 分支改为多数冻结 reason；已让多个 choices 沿用第一项；已把显式 `finish_reason=None` 归为 `missing_finish_reason`；已拆出 content 字段缺失分支。第二轮仍保留的关键原文：

```python
if not has_choices or type(choices) is not list:
    ...
choice = choices[0]
...
if tool_calls:
    ... "unexpected_candidate_tool_action" ...
...
if not has_content:
    ... "missing_candidate_content" ...
```

第二轮复核：`py_compile = PASS`，内存 fake 精确合同矩阵提升为 `23/26 PASS`，纯函数 / 不修改输入探针继续 `PASS`。三个失败为：

1. `choices=[]` 没有在取 `choices[0]` 前被拒绝，泄漏原生 `IndexError`；非列表或空列表都必须归 `recoverable / invalid_response_shape`，多项才沿用第一项。
2. `finish_reason="stop"` 且 message 带非空 Tool Action 时仍返回旧 reason `unexpected_candidate_tool_action`；必须与 `finish_reason="tool_calls"` 分支统一为 `unexpected_tool_calls`。
3. message 缺少 `content` 字段时新增了未冻结 reason `missing_candidate_content`；按已冻结合同必须归 `invalid_response_shape`。

实现完成的活动 `TODO(A4-Gate-C4b-2a-I1b)` 仍未删除。

当前检查点状态：`RETRY`；只修正上述三个失败与一个 TODO 清理项后，原样复跑同一矩阵。

第三轮原始修正：活动 TODO 已删除；`finish_reason=None` 当前改为 `unknown_finish_reason`；缺少 content 属性当前并入 `invalid_candidate_content`。磁盘最终代码仍保留以下关键分支：

```python
if not has_choices or type(choices) is not list:
    ...
choice = choices[0]
...
if tool_calls:
    ... "unexpected_candidate_tool_action" ...
...
if not has_content or type(content) is not str or not content.strip():
    ... "invalid_candidate_content" ...
```

第三轮复核：`py_compile = PASS`，活动 `TODO / NotImplementedError = 0/0`，纯函数 / 不修改输入探针继续 `PASS`；按今日已冻结的同一精确合同矩阵为 `22/26 PASS`。四个未通过点是：

1. `choices=[]` 仍在 `choices[0]` 泄漏 `IndexError`。
2. 显式 `finish_reason=None` 被改为 `unknown_finish_reason`；今日冻结合同要求 `missing_finish_reason`。
3. stop 响应中的非空 `message.tool_calls` 仍使用旧 reason `unexpected_candidate_tool_action`，而不是 `unexpected_tool_calls`。
4. message 缺少 `content` 属性被并入 `invalid_candidate_content`；今日冻结合同要求结构类 `invalid_response_shape`，只有字段存在但值非法才归内容类。

当前检查点状态：`RETRY`；鉴于用户已多次尝试，下一轮提供最小替换片段，只修改四个明确位置，不改变其余已通过实现。

第四轮原始修正：在外壳门补回空列表检查；显式 `finish_reason=None` 恢复为 `missing_finish_reason`；两个意外 Tool Action 入口统一为 `unexpected_tool_calls`；缺少 content 属性与存在但值非法重新分流；活动 TODO 已清理。

第四轮复核：

- `py_compile = PASS`。
- 同一内存 fake 精确合同矩阵：`26/26 PASS`。
- 纯函数 / 不修改输入 / 不调用预算门探针：`PASS`。
- 活动 `TODO / NotImplementedError = 0/0`。
- 助手只机械规范了第四轮粘贴片段的缩进，没有改变用户分类逻辑。

当前检查点状态：`PASS`。I1b 已收口；前两轮 reason / 多 choices / 缺字段订正轨迹保留为原始学习证据。

### `A4-Gate-C4b-2a-I1c`｜候选修订消息构造器

当前产物：助手根据已通过的 G2 白名单整理了 `RECOVERABLE_CANDIDATE_RESPONSE_REASONS`，并创建唯一 TODO 骨架 `build_candidate_recovery_messages()`。该函数只构造一条证据受限的 user 修订指令，不接收失败原始 Candidate，不调用模型 / 工具，不占 step、不写日志、不检查预算。

用户首轮实现：校验 `base_messages` 为非空内建列表、最后一项 role 为 tool，并校验 reason 属于冻结集合；按 reason 选择固定前缀，浅复制原列表后追加 exact `role / content` 两键 user message。原始共同指令为：

> 请只依据消息历史中既有的真实 Tool Observation 重新生成候选摘要；不要调用任何工具，不要补充 Observation 未提供的事实，只输出候选摘要正文。

首轮验证：

- `py_compile = PASS`，活动 `TODO / NotImplementedError = 0/0`。
- 内存消息合同矩阵：`70/84 PASS`。
- 已通过：四个 recoverable reason 均可构造；terminal / 未知 / 非字符串 / 旧别名拒绝；返回新内建列表且长度 +1；全部原消息顺序与 identity 不变；`out[-2] is base[-1]`；原列表与 Tool message 不变；新消息严格两键且 role 为 user；坏 Candidate assistant 尾部拒绝；没有模型 / 工具 / step / 日志 / 预算副作用。
- 14 个失败集中于三个根因：
  1. 最后一项只检查 dict + `role="tool"`，未要求 exact 三键，也未守住非空字符串 `tool_call_id / content`；缺字段、空值和 extra key 共 6 条未拒绝。
  2. 共同指令没有对四类统一明确“生成非空正文”以及“不要输出 JSON 或最终 `status / summary / sources`”；对应 7 条语义约束未命中。
  3. `length` 已要求“更短但完整”，但没有明确“从头重写、不要续写截断文本”；1 条专用约束未命中。

当前检查点状态：`RETRY`（教学代码检查点，不是父任务正式判定）；保留已经通过的 reason 映射、浅复制、identity 和零副作用逻辑，只补三组窄边界后复核。

第二轮原始修正：最后 Tool message 已要求 exact `role / tool_call_id / content` 三键；共同修订指令已明确非空纯文本、禁止 JSON 与最终 `status / summary / sources` 结构；`length` 已明确“从头重写、不要续写截断文本”。

第二轮复核：`py_compile = PASS`，活动 `TODO / NotImplementedError = 0/0`，同一消息合同矩阵提升为 `81/84 PASS`。三个失败都来自同一输入门缺口：字段键存在，但还未验证值为非空内建字符串；空白 `tool_call_id`、`content=None`、空白 `content` 仍被接受。缺字段与 extra key 已正确拒绝，其余 prompt、identity、reason 和纯函数检查继续通过。

当前检查点状态：`RETRY`；只需给 `tool_call_id / content` 补内建字符串与 `strip()` 非空门，不改其他已通过逻辑。

第三轮原始修正：

> 字符串门补好了

用户在 exact 三键门之后读取 `tool_call_id / content`，两者都用 `type(value) is str` 与 `value.strip()` 非空检查；非法值统一抛 `ValueError`，没有改动已通过的 reason、prompt、浅复制或 identity 逻辑。

第三轮复核：

- 项目 `.venv` 下 `py_compile = PASS`。
- 原样复跑同一内存消息合同矩阵：`84/84 PASS`；空白 `tool_call_id`、`content=None`、空白 `content` 三个原失败点全部关闭。
- 四类可恢复 reason、terminal / 未知 / 旧别名拒绝、Tool 尾消息 exact shape、前缀逐项 identity、失败 Candidate 隔离、输入不变和无模型 / 工具 / step / 日志副作用全部继续通过。
- 创建下一检查点骨架前，活动 `TODO / NotImplementedError = 0/0`。

当前检查点状态：`PASS`。I1c 已收口，前两轮 `70/84 → 81/84` 的订正轨迹保留为原始学习证据。

### `A4-Gate-C4b-2a-I1d`｜单次候选恢复模型调用

当前在做什么：把 I1c 已构造好的恢复消息发给模型恰好一次，并复用全局 step 与七字段调用日志。该 wrapper 返回的仍是不可信原始响应；下一控制流检查点才会用 I1b 分类器判断它是否恢复成功。

为什么需要：I1c 只生成安全消息，不产生真实调用证据；若把请求、解析、再次恢复和 Reflection 混在一个函数中，就无法分别证明调用次数、账本一致性和“最多恢复一次”的边界。

当前产物：已创建唯一 TODO 骨架 `request_candidate_recovery_response()`。本检查点只实现输入门、必要时创建 client、调用前 `reserve_call_step()`、一次 `.create()`、成功 / 异常互斥日志，以及成功原样返回 / 异常原 identity 抛出；不接主链，不调用 builder、classifier、工具、Reflection、Refinement 或公开结果构造器。

用户原始提交：

> I1d 写好了

用户实现：在任何外部动作前验证恢复消息最后两项为 exact Tool message 与 exact user recovery message；复用现有 schema 和可注入 client，在模型调用前占用共享 step 并计时；以 `tool_choice="none"` 发起恰好一次调用；成功与异常分支分别追加一条七字段模型日志，成功原样返回 response，异常只记录类型名并用裸 `raise` 原样抛出。用户还保留了已完成的骨架 TODO 注释，没有残留 `NotImplementedError`。

复核结果：

- 项目 `.venv` 下 `py_compile = PASS`，`git diff --check` 无错误。
- 不落盘 I1d fake / spy 矩阵：`66/66 PASS`。
- success：模型恰好调用一次，raw response identity 保留，step 只增加一次，且只追加一条 exact 七字段成功日志。
- API 正常返回 `None`：仍按一次成功调用记账并原样返回，没有在 wrapper 内分类或重试。
- SDK 异常：同一异常对象继续抛出，只记录安全类型名，不泄漏异常正文，也没有第二次请求。
- `step == MAX_STEPS`：模型 / timer / 新日志均为 0，context 与消息保持不变。
- 非法 messages：在 schema / client / reserve / timer / model / log 前拒绝；合法路径传递同一消息对象，参数严格含 `tool_choice="none"`。
- builder、classifier、完整链预算门、工具、Reflection、Refinement 与公开结果构造器的 bomb spy 全为 0 次。
- 行为验证后，助手只机械删除已失效的 I1d TODO 注释，没有改变用户核心逻辑；活动 `TODO / NotImplementedError = 0/0`。

当前检查点状态：`PASS`。I1d 只证明单次恢复请求及其共享账本正确；尚未证明预算门、一次性限制、I1b 再分类与下游链已经编排在一起。

### `A4-Gate-C4b-2a-I1e`｜至多一次候选恢复编排

当前在做什么：把 I1a 完整链预算门、I1c 消息 builder、I1d 单次模型调用和 I1b 响应分类器连接成一个私有编排函数，仍停在第二次 Candidate 的内部三键分类，不接 `generate_candidate_summary()`、Reflection 或公开结果映射。

为什么需要：四个组件分别通过只能证明零件正确，不能证明调用顺序、预算不足时零模型调用、SDK 异常后不分类，以及第二次仍 recoverable 时不会进入循环。I1e 用单一无循环调用图固定这些组合行为。

助手自动整理：新增窄内部类型 `CandidateRecoveryCompletionBudgetError`，只保存合法 `current_step` 与冻结的安全 `recoverable_reason`，异常正文为固定文本；它不构造公开结果，也不包含原始响应、prompt、Observation 或日志。没有给 `run_context` 增加恢复计数器。

当前产物：已创建唯一 TODO 骨架 `attempt_candidate_recovery_once()`。输入只接受 I1b 的 exact `recoverable / 白名单 reason / None` 三键结果；固定顺序为输入门 → I1a 预算 → I1c builder → I1d 一次调用 → I1b 再分类。预算不足抛窄异常且后续组件均为 0 次；第二次分类无论 valid / terminal / recoverable 都按对象 identity 原样返回，不再次恢复。

用户原始提交：

> I1e 写好了

用户实现：先精确校验 I1b recoverable 三键结果，再验证原消息仍以有效真实 Tool Observation 结尾，并从同一 `run_context` 读取 step；预算不足时抛专用窄异常，预算允许时依次调用 builder、I1d 和 I1b，并直接返回第二次分类结果。实现中没有循环、递归、第二个恢复调用点或 context counter，且已清理 TODO / `NotImplementedError`。

复核结果：

- 项目 `.venv` 下 `py_compile = PASS`，`git diff --check` 无错误，活动 `TODO / NotImplementedError = 0/0`。
- 不落盘组合矩阵：`91/91 PASS`；独立只读复核结论一致，无行为阻断。
- exact 顺序为 budget → builder → I1d → I1b；base messages、builder 结果、client、context、raw response 与最终分类均按要求保持对象 identity。
- step 4 抛 `CandidateRecoveryCompletionBudgetError(current_step=4, recoverable_reason=...)`，builder / I1d / I1b 均为 0 次，context 与 messages 不变。
- 第二次分类的 1 个 valid、4 个 terminal reason 与 4 个 recoverable reason 全部原样返回，每个 helper 恰好一次，没有循环。
- raw `None` 经真实 I1b 得到 exact `recoverable / invalid_response_shape / None`，没有再次恢复。
- builder、I1d、I1b 哨兵异常均保持原对象 identity，并正确短路所有下游组件。
- 真实 I1c → I1d → I1b fake SDK 组合从 step 3 推进到 4，只产生一次模型调用和一条七字段成功日志，且 `tool_choice="none"`。
- 用同一 context 误调用第二次时，第二次由完整链预算门阻断；两次 invocation 的恢复模型调用总数仍为 1。
- 工具、Reflection、Refinement 和公开结果构造器均为 0 次；context 键仍严格为 `request_id / step / logs`。

当前检查点状态：`PASS`。I1e 已证明至多一次 Candidate recovery 的私有组合控制流；尚未接生产 Candidate 主链。最终失败来源语义当时仍待决定，已在下方 G3 冻结；公开停止映射仍未实现。

### `A4-Gate-C4b-2a-G3`｜候选恢复最终失败时的真实来源语义

当前在做什么：在生产接线和公开 `needs_manual` 映射前，冻结失败结果中 `sources` 的含义。场景限定为真实工具已经成功并取得白名单来源，但首次 Candidate 与唯一恢复都没有形成合法摘要，或在候选阶段遇到 terminal / 完整链预算停止。

为什么需要：合同允许失败前取得的真实来源原样保留，也允许不用它们；代码不能自行决定 `sources` 表示“已取得的证据池”还是“支撑已验证摘要的已采用来源”。该选择会直接改变人工接管体验和误解风险。

当前检查点状态：`AWAITING_USER_DECISION`；尚未创建新的代码 TODO。

用户原始回答：

> 我更加偏向当候选恢复最终失败并返回 needs_manual 时，我希望 sources 保留已经取得的真实白名单来源，把失败结果中的 sources 理解成“已验证摘要采用的来源”，在失败说明中明确指出这些来源没有支撑出一份已通过校验的摘要。
>
> 两种哪种用的比较多？

初评：行为偏好与风险提示方向正确，但语义仍为 `PARTIAL`。若没有任何摘要通过校验，就不能同时把保留下来的来源称为“已验证摘要采用的来源”；保留来源时，它们只能表示已真实取得、供人工接管检查的证据池。若坚持“已验证摘要采用的来源”这一字段定义，失败时应使用 `[]`。

官方模式校准（2026-08-20 核验）：

- Amazon Bedrock Agentic Retrieval 把检索结果 `results` 与 `generatedResponse.citations` 分开；citations 映射答案文本区间到 supporting results。`Retrieve` 也可以只返回检索结果而不生成答案：https://docs.aws.amazon.com/bedrock/latest/userguide/kb-test-agentic-retrieve.html
- RFC 9457 允许错误对象使用 problem-specific extension members 承载额外诊断信息，而不是改变既有字段语义：https://www.rfc-editor.org/rfc/rfc9457.html#section-3.2
- JSON:API 把 primary `data` 与 `errors` 分开，并允许错误对象用 `meta` 承载非标准错误元信息：https://jsonapi.org/format/#error-objects

校准结论：没有公开统计可给出两种设计的精确占比；从上述规范与官方产品结构看，更常见的结构是“答案引用”和“已检索证据 / 失败诊断”分字段。映射到本项目固定三字段合同：若 `sources` 继续表示已采用引用，失败时 `[]` 更符合常见语义；若为人工接管保留真实来源，则必须明确规定非 success 状态下 `sources` 表示 `retrieved evidence for handoff`，并在 summary 中声明没有摘要通过校验。这是有意的固定-schema 折中。

用户最终确认：

> 按人工接管证据池保留

最终决定：候选阶段因 terminal、完整链预算不足或唯一恢复耗尽而返回 `needs_manual` 时，`sources` 原样保留本轮工具实际取得且通过白名单校验的来源；此时它们只表示供人工接管复核的证据池，不表示存在一份已通过校验的摘要，也不作为失败说明本身的引用。`summary` 必须明确说明没有 Candidate / 摘要通过校验，并说明这些来源仅供人工复核。固定三字段 V1 接受这一有意折中；若未来扩展 schema，优先把 answer citations 与 retrieved evidence 拆成不同字段。

当前检查点状态：`PASS`；行为已同步到 `code/stage4/problem-contract.md`，下一步才创建生产 Candidate 主链接入的唯一代码骨架。

### `A4-Gate-C4b-2a-I1f`｜候选阶段 resolver 与窄停止信号

当前在做什么：把 I1b 的首次分类、I1e 的至多一次恢复，以及 G3 的真实人工接管证据池组合成一个 stage-level resolver。它是生产主链接入前的最后一个核心控制点；通过后，旧 Candidate 手写解析段只需机械替换为一次 resolver 调用，公开入口再单独映射 `needs_manual`。

为什么先单独闭合：现有 `generate_candidate_summary()` 的旧段同时靠近首次分类、恢复、来源派生和公开错误边界。把 resolver 先验收，可以让本检查点只证明 Candidate 阶段控制流，不同时改动 Reflection、Refinement、validator 或公开三字段结果。

助手自动整理：新增无 TODO 的内部窄类型 `CandidateSummaryStageStopError`，只保存 `stop_kind / candidate_reason / recovery_attempts / current_step / allowed_sources`；来源列表被浅复制，异常正文固定，不含原始 response、Candidate、messages、Observation 正文、tool result、请求值或日志。新增唯一 TODO 骨架 `resolve_candidate_summary_stage()`，暂未修改 `generate_candidate_summary()` 与 `run_research_summary_once()`。

用户实现合同：

1. 先用 I1b 分类 `initial_response`；首次 `valid` 原样返回 Candidate 正文，首次 `terminal` 不调用 I1e。
2. 首次 `recoverable` 时只调用 I1e 一次；二次 `valid` 原样返回，二次 `terminal` 或 `recoverable` 都停止，绝不递归或第二次恢复。
3. 只捕获 `CandidateRecoveryCompletionBudgetError` 并转换为 `insufficient_completion_steps`；builder、I1d、分类器、Provider / SDK 与来源派生的其他异常保持原对象上抛。
4. 只有确定停止时才调用 `derive_allowed_sources(normalized_request, tool_name, tool_result)`；来源只能来自真实工具白名单，保持顺序，不能从失败 Candidate 或模型响应中读取。
5. 三种停止映射：首次 / 恢复后 terminal → `terminal_response`、恢复次数分别 0 / 1；二次仍 recoverable → `recovery_exhausted / 1`；恢复前预算不足 → `insufficient_completion_steps / 0`。
6. 不重新执行工具，不调用 Reflection / Refinement / validator / `make_result()`，不修改输入消息或把失败 Candidate 加入历史。

怎样算完成：删除唯一 TODO 与 `NotImplementedError`；首次 valid、recoverable→valid、首次 terminal、recoverable→terminal、recoverable→recoverable、step 4 预算不足六组代表轨迹均符合上述调用次数与停止事实；来源派生顺序不变，非预算异常保持 identity，输入对象和共享 context 除 I1e 的一次真实恢复调用外无额外副作用。

用户原始提交：

> I1f 写好了

用户实现：先用 I1b 分类首次响应；valid 原样返回，terminal 直接派生真实白名单来源并发出 stage stop，recoverable 只进入 I1e 一次。只捕获候选完整链预算窄异常并转换为 `insufficient_completion_steps`；恢复后二次 valid 返回，terminal / recoverable 分别映射为 `terminal_response / recovery_exhausted`。所有停止分支只通过 `derive_allowed_sources()` 取得来源，预算异常使用显式 exception chaining，其他异常不宽泛捕获。

复核结果：

- 本地主审不落盘等价矩阵：`91/91 PASS`；首次 valid、4 类首次 terminal、4 类 recoverable→valid、4 类 recoverable→4 类 terminal、4 类 recoverable→4 类 recoverable、4 类 step 4 预算停止及异常 identity 全部通过。
- 实际 I1b → I1e → I1c → I1d 组合使用内存 fake SDK：恢复成功只调用模型一次并令 step / logs 各增加 1；恢复仍失败也只调用一次；step 4 预算停止时 SDK 为 0 次且 context 不变。
- 独立只读复核：六类 fake / spy `45/45 PASS`，真实组件聚焦组合 `3/3 PASS`，没有阻断问题。
- `AST_PARSE = PASS`、`git diff --check` 退出 0、源码活动 `TODO / NotImplementedError = 0/0`；验证用系统临时脚本与本轮生成的目标 `.pyc` 已清理，没有新增长期 runner 或报告文件。
- 来源只由 derive seam 产生，参数 identity 与顺序保持，异常内保存新 list；异常正文固定，不含 Candidate、Observation、资料正文或内部日志。
- 非预算的 classifier / I1e / Provider / SDK / derive 异常均保持原对象 identity；resolver 未调用真实工具、Reflection、Refinement、validator 或 `make_result()`。

当前检查点状态：`PASS`。I1f 已形成可独立复用、来源安全且最多恢复一次的候选阶段 resolver；父任务 `A4-Gate` 仍为 `DOING`。

### `A4-Gate-C4b-2a-I1g`｜把 resolver 接入生产 Candidate 主链

当前在做什么：删除 `generate_candidate_summary()` 中第二次模型响应的旧手写 `choices / finish_reason / message / tool_calls / content` 解析分支，改由已经通过的 `resolve_candidate_summary_stage()` 统一产生合法 `candidate_summary` 或向上抛出 stage stop。

为什么需要：I1f 目前只是通过验证的内部组件；只有接到真实 Candidate 调用之后，正常响应、terminal、一次恢复和预算停止才会实际进入生产控制流。本检查点只做接线，不捕获 `CandidateSummaryStageStopError`，因此不会提前决定公开结果文案。

用户实现合同：

1. 在第二次 Candidate 模型调用成功记账后，调用一次 `resolve_candidate_summary_stage()`，把 `response2 / messages / normalized_request / tool_name / tool_result / client / run_context` 原样接入。
2. 将 resolver 返回的字符串保存为现有 `candidate_summary`，继续复用原三键中间返回对象；不得重写已通过的返回键或复制工具结果。
3. 不追加 `response2` 到消息历史，不重新执行工具，不再次手写解析 Candidate，也不捕获 stage stop 或其他异常。
4. 不修改 `run_research_summary_once()`、Reflection、Refinement、validator、`make_result()` 或现有 I1a～I1f。

怎样算完成：删除唯一 TODO 与 `NotImplementedError`；正常 valid 路径 resolver 恰好 1 次且仍返回原三键中间对象；recoverable 路径仍最多增加一次模型调用；stage stop 按原对象向上传播；生成函数没有调用公开 mapper、Reflection 或第二次工具执行。

用户原始提交：

> I1g 写好了

用户实现：在第二次 Candidate 模型调用完成成功日志后，以同一 `response2 / messages / normalized_request / tool_name / tool_result / client / run_context` 调用一次 I1f resolver，把返回正文赋给原 `candidate_summary`，并继续复用原三键中间返回。旧 `choices / finish_reason / message / tool_calls / content` 手写解析已全部删除；没有捕获 stage stop，也没有修改公开入口或 Reflection 链。

复核结果：

- 本地主审生产路径 fake / spy：`47/47 PASS`。resolver 调用位置、七个参数 identity、真实 Tool Observation 尾消息、三键返回、正常无恢复、一次恢复成功、首次 terminal、恢复耗尽和 step 4 预算停止全部通过。
- 正常 valid 路径仍为两次模型调用加一次工具调用，resolver 恰好 1 次；recoverable→valid / recoverable 的 Candidate recovery 都只增加一次模型调用，step / logs 各精确增加 1。
- terminal 与预算停止的 `CandidateSummaryStageStopError` 原对象穿过 `generate_candidate_summary()`；没有调用公开 mapper、Reflection、Refinement、validator 或第二次工具。
- 全文件旧 `choice2 / assistant_message2 / response2.choices` 与旧 Candidate 解析文案均为 0；即使注入“属性访问即爆炸”的 `response2`，resolver seam 仍可直接收到原对象。
- 独立只读复核同样判定 `PASS`，确认 `run_research_summary_once()` 仍未捕获候选 stop，I1g 没有越过公开映射边界。
- `AST_PARSE = PASS`、`git diff --check` 退出 0、I1g 验收时源码活动 `TODO / NotImplementedError = 0/0`；系统临时验证脚本和本轮目标 `.pyc` 已清理。

当前检查点状态：`PASS`。I1g 已完成生产 Candidate 主链接入；父任务 `A4-Gate` 仍为 `DOING`。

### `A4-Gate-C4b-2a-I1h`｜候选阶段停止的公开 `needs_manual` 窄映射

当前在做什么：只在 `run_research_summary_once()` 的既有 try / except 边界捕获 `CandidateSummaryStageStopError`，把三种内部停止事实转换成严格三字段的公开 `needs_manual` 结果。这里不再决定是否恢复，只负责把已经冻结的事实安全地告诉调用方。

为什么需要：I1g 之后，Candidate 阶段失败已经能安全停止，但异常仍会离开公开入口。I1h 要补齐最后一层“内部信号 → 用户可理解结果”，同时守住 G3：真实来源作为人工接管证据池保留，却不能被误解成已验证摘要的引用。

用户实现合同：

1. catch 后先要求 `type(exc) is CandidateSummaryStageStopError`；子类或非冻结对象继续上抛，不做宽泛映射。
2. 只依据 `stop_kind / candidate_reason / recovery_attempts / current_step / allowed_sources` 构造说明，不读取或拼接原始 response、Candidate、prompt、Observation 正文、tool result 或日志。
3. 为 4 个 terminal reason 与 4 个 recoverable reason 提供固定、安全、可区分的公开解释；未知 reason 不使用兜底文案伪装，继续上抛。
4. `terminal_response` 必须区分恢复次数 0 / 1；`recovery_exhausted` 必须说明唯一一次证据受限恢复仍未得到合法 Candidate；`insufficient_completion_steps` 必须说明当前 step、全局上限以及恢复 + Reflection + Refinement 所需 3 个调用，而且恢复模型调用没有发生。
5. 所有分支的 `summary` 都必须明确：没有任何 Candidate / 摘要通过校验；`sources` 仅是本轮真实工具取得、供人工复核的证据池，不代表已验证摘要引用；Agent 已停止自治并交还人工。
6. 使用 `make_result("needs_manual", summary_text, list(exc.allowed_sources))` 返回固定三字段结果；公开 sources 使用新列表，不与异常字段共享容器。
7. 不修改 generate / resolver、旧工具与 Action 异常映射、Reflection / Refinement / validator，也不捕获 Provider / SDK 或其他 RuntimeError。

怎样算完成：删除唯一 TODO 与 `NotImplementedError`；三种 stop kind、8 个安全 reason、terminal 的 0 / 1 次恢复分支都返回 exact 三键 `needs_manual`；来源顺序与值不变但 list identity 分离；summary 含必需的失败与人工接管语义且无原始敏感内容；子类、未知 / 伪造 stop 与非目标异常不被映射。

用户原始提交：

> I1h 写好了

用户实现：在公开入口精确捕获 `CandidateSummaryStageStopError`，先拒绝子类与字段类型 / 合法域不匹配的篡改对象，再按 terminal / recoverable reason 的两个固定文案表构造三类停止说明。terminal 精确区分恢复 0 / 1 次；恢复耗尽与预算不足分别说明唯一恢复事实和完整链所需 3 个 step。所有分支追加“无摘要通过校验 / sources 仅为人工证据池 / 非验证引用 / 停止自治”共同语义，并使用新的 sources list 调用 `make_result()`。

复核结果：

- 本地主审公开映射矩阵：`262/262 PASS`。覆盖 4 个 terminal reason × attempts 0 / 1、4 个 recovery exhausted reason、4 个 budget reason、来源值 / 顺序 / list identity、公共文案和敏感信息不回显。
- 子类、未知 / 错类型 / 跨类 reason、非法 attempts / step、空 / 空白 sources 等篡改对象均按原异常 identity 上抛；普通 Provider `RuntimeError` 也未被误映射。
- 旧 `AutoRecoveryExhaustedError` 代表映射保持 `needs_manual + []`，正常生成仍以原参数 identity 进入 Reflection 并按结果 identity 返回。
- 独立只读复核：合法映射 `16/16 PASS`、篡改 exact 异常 `14/14` 上抛、非目标异常 `3/3` 上抛、旧 catches `5/5` 与正常 Reflection 回归均通过；没有阻断问题。
- `AST_PARSE = PASS`、`git diff --check` 退出 0、源码活动 `TODO / NotImplementedError = 0/0`；系统临时验证脚本与本轮目标 `.pyc` 已清理。

当前检查点状态：`PASS`。I1h 已把候选阶段失败安全映射为保留真实人工接管证据池的公开 `needs_manual`；父任务 `A4-Gate` 仍为 `DOING`。

### `A4-Gate-C4b-2a-I2`｜端到端故障注入与旧链回归

当前在做什么：不再新增产品代码，直接从 `run_research_summary_once()` 注入完整模型 / 工具轨迹，验证 I1a～I1h 组合之后的新候选恢复链和旧 Action、工具、Reflection 控制流能同时成立。

验证范围：正常 topic / relative_path 五步链、Candidate 一次恢复成功的六步链、首次 terminal、恢复后 terminal、唯一恢复仍 recoverable、step 4 完整链预算停止、三类公开人工接管证据池、Provider / SDK 与篡改异常边界、Action correction / tool retry / 旧 `tool_failure / insufficient_evidence` 映射、不发生第 7 步副作用，以及至少一次真实 DeepSeek 正常链。

持久化边界：I2 复用直接命令与系统临时验证脚本，结束即清理；只把精确命令类别和决定性结果写入本 daily，不创建 `tests/`、runner、baseline、原始日志或报告文件。

验证结果：

- 内存端到端主链：`16` 类场景、`123/123 PASS`。覆盖正常 relative_path / topic 五步链、Candidate recovery 成功六步链、首次 terminal、恢复后 terminal、恢复耗尽、Action correction 与 tool retry 后的 step 4 候选预算停止、Action correction 正常成功、tool retry 正常成功、不可重试工具失败、搜索零证据、二次非法 Action、Candidate / recovery Provider 异常与 invalid input。
- 正常五步链日志顺序精确为 `model → tool → model → model → model`；Candidate recovery 六步链为 `model → tool → model → model → model → model`，结束恰为 `MAX_STEPS=6`，scripted client 无第七个响应，若发生第 7 次调用会立即失败。
- 两条 step 4 候选失败轨迹分别由一次 Action correction 和一次 tool retry 产生；两者都在 Candidate recovery 模型调用前停止，公开 `needs_manual.sources == ["sample.txt"]`，同时明确没有摘要通过校验且来源仅为人工接管证据池。
- 旧控制流回归：非法 Action 不执行工具；Action correction 与 tool retry 的正常六步成功链保持；`tool_failure / insufficient_evidence` 仍使用空 sources；Provider / SDK 异常保持原对象 identity；invalid input 保持模型 / 工具 0 次。
- 真实 DeepSeek relative_path 正常链：输入 `{"input_type":"relative_path","value":"sample.txt"}`，退出码 0，返回 `status="success"`、非空中文摘要与 `sources=["sample.txt"]`；证明真实 Action → read_material → Candidate → Reflection → Refinement / validator 未被新接线破坏。
- `AST_PARSE = PASS`、`git diff --check` 退出 0、源码活动 `TODO / NotImplementedError = 0/0`；系统临时 I2 验证脚本已删除，没有持久化 runner / 报告。

当前检查点状态：`PASS`。I2 已证明候选失败恢复新链与既有 Action、工具、Reflection 正常链可以共同成立。

### `A4-Gate-C4b-2a-U1`｜用户最终轨迹审核

审核对象：G1～G3 冻结的行为、I1a～I1h 的产品实现，以及 I2 的端到端 / 真实 API 证据。用户只需确认最终可观察行为是否符合自己的意图，不重复抄写 helper 或公开文案。

用户原始审核：

> U1 通过

当前检查点状态：`PASS`。用户确认最终可观察行为符合意图；本单元不再追加产品代码或测试。

算法内容块状态与证据（W4 起）：W4 `PASS`、债务 0；本日未安排算法。

## 当前范围与接力

- 今日计划范围 / 今日剩余：本日单块只包含 `A4-Gate-C4b-2a` 的定向材料、行为决策、实现、故障注入、回归和用户审核；G1 / G2 / G3、I1a～I1h、I2 与 U1 全部通过，本日计划范围已完成，没有今日剩余。
- 任务总剩余：候选 Provider/API 异常分类与恢复、Reflection 失败恢复、Refinement / 最终 validator 失败恢复、HITL allow / deny、14 条 `eval_cases.json`、五个 Gate 必答问题、Problem Contract 定稿和正式 Gate 复核；这些不因本子单元闭合而视为今天待办。
- 下次 Session 起点（不属于今天）：先界定 Reflection 响应失败恢复的第一个可闭合行为切片，包括失败等价类、是否允许恢复、完整链预算与安全停止；下一自然日需重新排入不超过 180 分钟的计划，本日不创建该切片骨架。

## 休息日第二块启动｜Session S2｜`A4-Gate-C4b-2b-1`

- 状态演进：上方“今日剩余为 0 / 下次 Session 不属于今天”是第一块收口时的真实状态。用户随后显式启动第二块，因此把原接力点在同一自然日重新纳入独立的 150 分钟预算；不改写第一块历史结论，也不启动第三块。
- 当前学习单元：只处理“Reflection 模型 API 已正常返回，但反馈响应的协议或内容不可用”时的受控恢复。Candidate Provider / API 异常、Reflection Provider / API 异常、Refinement 响应 / JSON / 最终 validator 失败、HITL、14 条 eval 和父 Gate 正式复核均不在本切片。
- 关联前序 daily：[2026-08-05](2026-08-05.md)、[2026-08-06](2026-08-06.md)、[2026-08-07](2026-08-07.md)、[2026-08-08](2026-08-08.md)、[2026-08-09](2026-08-09.md)、[2026-08-10](2026-08-10.md)、[2026-08-11](2026-08-11.md)、[2026-08-12](2026-08-12.md)、[2026-08-13](2026-08-13.md)、[2026-08-17](2026-08-17.md)、[2026-08-18](2026-08-18.md)、[2026-08-19](2026-08-19.md) 与本页 S1。2026-08-14～16 没有学习记录，不补建空白 daily。
- 资料定位与版本：`code/stage4/problem-contract.md` 第 4、6、8 节；`code/stage4/a4_gate_research_summary_agent.py` 的 `request_reflection_feedback()`、`reflect_refine_and_validate()`、`run_research_summary_once()`，仓库基线 `HEAD c8ad8cf063340d0ac30d8c0d0b48f6add379c145`，工作树在 S2 启动前干净，源码 SHA-256 `FB269B974AF5524CDD25F397EA15F0D1AA82DCBBB9794031AB6776096CE057B7`，活动 `# TODO / raise NotImplementedError = 0/0`，`py_compile` 与 `git diff --check` 退出 0；DeepSeek Chat Completions `Responses` 官方文档于 2026-08-20 核验，非流式 choice 的 `finish_reason` 取值仍为 `stop / length / content_filter / tool_calls / insufficient_system_resource`，`message.content` 仍为 nullable。
- 已覆盖且不重复：Reflection 的职责、正常 `stop + 无 tool_calls + 非空 feedback` 外壳、一次 Reflection 与一次 Refinement 的共享 step / 七字段日志、最终 success validator；Candidate 阶段已经通过的响应失败分类、真实来源证据池语义和“完整可信下游链预算优先于盲目重试”原则只迁移复用，不换措辞重考。
- 本切片唯一新行为判断：Reflection 首次失败后，恢复成功仍必须再完成 Refinement；因此一次 Reflection recovery 需要连同后续 Refinement 一起预留 2 个调用 step。正常五步链在 Reflection 失败发生于 step 4 时仍有 2 步；Action correction、tool retry 或 Candidate recovery 已消费额外 step 的链通常在 Reflection 失败发生于 step 5 时只剩 1 步，两者不能采用相同恢复动作。
- 用户必须亲自完成：先沿两条调用轨迹判断是否允许发起 Reflection recovery，并解释为什么预算不足时不能“先试一次再看”；行为冻结后完成一个代表性的最小控制流实现，最后审核 step / logs、模型与工具调用次数、公开停止结果及来源语义。
- 助手自动完成：从既有 PASS 证据迁移相同响应分类与安全来源规则，整理重复状态表和公开文案；用户的预算判断通过后才即时创建唯一 TODO 骨架，并准备无落盘 fake / spy、运行验证、记录证据，不替用户写核心实现答案。
- 回炉与算法：`tracker/weak-points.md` 持久状态仍为 `NORMAL`，不是 `RECOVERY_BLOCK_PENDING`。按 2026-08-20 只读扫描，到期项为 `CD-005 / WP-08 / WP-28`，最老逾期 7 天；当前应在后续正式练习优先使用既有旧复习席位，本 Gate 编码第二块不追加无关旧题，且 `WP-08` 已于 2026-08-17 在 Tool Message 新表面迁移。W4 算法已 `PASS`、债务 0，本块不启动新算法时段。

### `A4-Gate-C4b-2b-1-G1`｜Reflection recovery 的完整链预算

题目前置：沿用 Candidate 阶段已通过的失败分类，本题只决定“可恢复 Reflection 失败发生后，是否还有资格真正发起恢复”，不要求重新背分类表，也不提前设计 Refinement / validator 自身的失败恢复。

请分别沿调用轨迹 A / B 回答三点：① Reflection 首次失败后是否发起一次 Reflection recovery；② 若允许，后续调用的精确顺序和最终可能占到的 step；③ 为什么不允许的轨迹不能“先恢复一次，成功后再看剩余预算”。

- 轨迹 A：`step 1 Action → step 2 Tool success → step 3 Candidate valid → step 4 Reflection recoverable failure`。
- 轨迹 B：`step 1 Action → step 2 Tool retryable failure → step 3 Tool retry success → step 4 Candidate valid → step 5 Reflection recoverable failure`。

用户原始回答（当前只提交轨迹 A）：

> A：
> - 是否恢复：是
> - 后续调用与 step：后续执行Refinement，最多占2个
> - 原因：步数够，也不是安全边界问题，所以可以重新发起一次

当前判定：轨迹 A `PARTIAL`；轨迹 B `UNANSWERED`，整个 G1 尚未通过。

- 已正确：选择允许恢复，并识别出当前剩余预算足以容纳两次调用。
- 待订正 1：“后续执行 Refinement”漏掉了本切片真正要发起的 `Reflection recovery`；必须从 step 4 之后把两个调用按因果顺序分别写出。
- 待订正 2：“最多占 2 个”说明了调用数量，但题目要求的是每个调用对应的精确 step，以及整条链最终可能到达哪个 step。
- 待订正 3：“不是安全边界问题”过于宽泛；需要说明为什么必须在 recovery 前一次性确认“恢复 + 后续 Refinement”都装得下，不能只确认当前还能调用一次模型。

#### `A4-Gate-C4b-2b-1-G1-A-R1`｜只补轨迹 A 的顺序与因果

从已经发生的 `step 4 Reflection recoverable failure` 往后逐次编号：先写本切片准备恢复的阶段，再写恢复反馈通过后才能进入的阶段；最后用一句话说明为什么这两个调用必须作为完整链一起预留。当前不回答轨迹 B，也不创建产品代码 TODO。

用户订正：

> A：
> - 是否恢复：是
> - 后续调用与 step：
>   - step 5：Reflection recovery
>   - step 6：Refinement
>   - 整条链最多到达 step 6：Refinement
> - 原因：必须把 Reflection recovery和 Refinement作为完整链一起预留，因为只完成前者还不能产生 最终结果。

轨迹 A 当前状态：`PARTIAL → PASS`。

- 调用顺序和全局 step 均正确：失败的初次 Reflection 已占 step 4，恢复调用占 step 5，只有恢复得到合法反馈后才允许 Refinement 占 step 6。
- 因果闭合：Reflection recovery 只产生可用反馈，不直接产生可交付的最终三字段候选；必须把后续 Refinement 一起纳入调用前预算门。
- 表达中的空格不影响语义，不要求机械重写。轨迹 A 已通过，后续不换措辞重复考察。

#### `A4-Gate-C4b-2b-1-G1-B`｜额外调用已占步时的预算迁移

现在只回答原题轨迹 B：`step 1 Action → step 2 Tool retryable failure → step 3 Tool retry success → step 4 Candidate valid → step 5 Reflection recoverable failure`。

请写：① 是否发起 Reflection recovery；② 若允许，后续调用与精确 step；若不允许，Agent 在哪个 step 后停止；③ 为什么不能“先恢复一次，成功后再看是否有 Refinement 预算”。轨迹 A 已 PASS，不再重答。整个 G1 当前状态仍为 `DOING`，产品代码 TODO 继续保持 0。

用户原始回答：

> B：
> - 是否发起 Reflection recovery：否
> - 后续调用与 step；如果不恢复，则在哪个 step 后停止：Reflection failure停止
> - 原因：为什么不能“先恢复一次，成功后再检查是否还有 Refinement 预算”：因为后续没有步骤了

当前判定：轨迹 B `PARTIAL`；整个 G1 仍为 `DOING`。

- 已正确：选择不发起 Reflection recovery，守住了不能启动不完整可信链的行为方向。
- 待订正 1：“Reflection failure 停止”需要精确到已经记账的 step，并说明尚未使用的最后一个 step 是否会被调用。
- 待订正 2：“后续没有步骤了”是计数错误。`MAX_STEPS = 6`、当前 `step = 5`，仍存在一个调用位置；真正的阻断原因是剩余调用位置少于完整的 `Reflection recovery + Refinement` 两步需求。

#### `A4-Gate-C4b-2b-1-G1-B-R1`｜只修剩余步数与半链因果

只补四个空：`remaining_steps = 6 - 5 = ?`；若贸然恢复，`Reflection recovery` 会占 step `?`，之后 `Refinement` 需要 step `?`；因此 Agent 应在已经发生的 step `?` Reflection failure 后停止，并且不发起恢复调用。当前不创建产品代码 TODO。

用户订正：

> B-R1：
> - remaining_steps = 6 - 5 =1
> - 如果贸然恢复，Reflection recovery 会占 step 6
> - 恢复成功后，Refinement 将需要 step 7
> - 因此 Agent 应在已经发生的 step5 Reflection failure 后停止，并且不发起恢复调用

轨迹 B 当前状态：`PARTIAL → PASS`；整个 `A4-Gate-C4b-2b-1-G1 = PASS`。

- 计数正确：step 5 后只剩 1 个调用位置，而完整恢复链需要 2 个；若 recovery 占 step 6，Refinement 将需要被全局硬门拒绝的 step 7。
- 停止位置正确：在已记账的 step 5 Reflection failure 后停止，不调用 step 6，避免产生只有反馈、没有最终候选的半成品状态。
- G1 冻结行为：轨迹 A 允许 step 5 Reflection recovery → step 6 Refinement；轨迹 B 在 step 5 后安全停止。两个分支都在 recovery 调用前检查完整链预算，而不是把 `MAX_STEPS` 当成必须耗尽的目标。

### `A4-Gate-C4b-2b-1-I1a`｜Reflection recovery 完整链纯预算门

现在把 G1 已通过的行为翻译为一个最小、无副作用的代码边界。它只回答“当前剩余 step 是否至少能容纳 Reflection recovery 与后续 Refinement”，不执行恢复、不读取响应、不修改 `run_context`，也不构造公开 `needs_manual`。

助手已根据 G1 的冻结结论自动加入常量 `MIN_STEPS_FOR_REFLECTION_RECOVERY_CHAIN = 2`，并在 `code/stage4/a4_gate_research_summary_agent.py` 创建唯一 TODO 骨架 `has_reflection_recovery_completion_budget(current_step)`；常量只是已通过决策的规范化，不冒充用户代码证据。

用户当前只需实现该函数体，完成标准：

1. `current_step` 只接受内建 `int`，且必须位于 `1..MAX_STEPS`；`bool`、浮点数、字符串和越界整数抛 `ValueError`。
2. 合法输入只返回内建 `bool`；step 4 返回 `True`，step 5 和 step 6 返回 `False`。
3. 计算使用动态常量 `MAX_STEPS` 与 `MIN_STEPS_FOR_REFLECTION_RECOVERY_CHAIN`，不把 `6 / 2 / 4` 重复写死在函数体。
4. 函数保持纯度，不接收或修改 `run_context`，不调用 `reserve_call_step()`、日志、模型、工具、恢复函数、异常停止信号或 `make_result()`。

骨架创建验证：`py_compile` 与 `git diff --check` 退出 0；源码 SHA-256 `D05E23AF9BBA23C306027BA744852E975E049A1C032D380F2700C2E7173F341A`；活动产品代码 TODO / `NotImplementedError = 1/1`，未创建其他骨架。

用户完成口令：`改完了 I1a`。

用户实现保留的核心行为：严格使用 `type(current_step) is int` 与 `1 <= current_step <= MAX_STEPS`；随后计算 `remaining_steps = MAX_STEPS - current_step`，并与 `MIN_STEPS_FOR_REFLECTION_RECOVERY_CHAIN` 比较。没有写死 `6 / 2 / 4`，也没有新增副作用调用。

决定性无落盘验证：

- 合法 step 1～6 的值与返回类型共 12 项全部通过：`1/2/3/4 → True`，`5/6 → False`，每项均为内建 `bool`。
- `True / False / 1.0 / "4" / None / list / dict / 0 / -1 / 7` 共 10 个非法输入均抛 `ValueError`。
- 动态把 `MAX_STEPS / 所需链步数` 改为 `9 / 3` 后，step 6 为 `True`、step 7 / 9 为 `False`；证明函数读取常量而非写死当前阈值。
- 把 `reserve_call_step / append_call_log / create_deepseek_client / make_result` 替换为 bomb 后调用仍通过且 bomb 计数为 0；AST 同时确认没有调用这些副作用，也没有出现写死的整数 `2 / 4 / 6`。
- 聚焦矩阵总计 `29` 项 PASS；`py_compile` 与 `git diff --check` 退出 0；用户实现后源码 SHA-256 `8351622B1D4E912752BF4BE1DF5B0BD84770893C217A23BB4458D99AEE036C12`；活动 TODO / `NotImplementedError = 0/0`。

当前检查点状态：`A4-Gate-C4b-2b-1-I1a = PASS`。这只证明纯预算门，不等于 Reflection recovery 已请求、分类、编排或接入生产链。

### `A4-Gate-C4b-2b-1-I1b`｜复用已有文本响应分类器

本检查点不让用户重新实现已经在 Candidate 阶段通过的 SDK 层级守门。助手依据既有 PASS 证据创建一个薄适配器骨架 `classify_reflection_feedback_response(response)`：它必须调用 `classify_candidate_summary_response(response)`，只把阶段字段和一个阶段专属原因转换为 Reflection 语义。

精确内部合同：

- 返回值严格只有 `classification / reason / feedback` 三个键。
- 既有分类为 `valid / None / candidate_summary` 时，返回 `valid / None / feedback`，保留原非空文本对象和值；不在本层额外 `strip()`。
- 既有原因 `invalid_candidate_content` 只改名为 `invalid_feedback_content`；`invalid_response_shape / length / unexpected_tool_calls` 及全部 terminal 原因保持原值。
- 非 valid 分支的 `feedback` 必须为 `None`；不得让失败响应内容进入后续 prompt。
- 必须复用既有分类器，不复制 `choices → finish_reason → message → tool_calls → content` 遍历；不得修改响应、占 step、写日志、调用模型 / 工具、检查预算、执行恢复、发出停止信号或构造公开结果。

骨架创建验证：`py_compile` 与 `git diff --check` 退出 0；源码 SHA-256 `BEC2EB76EDF6A51A34D60C6E7BE3D59284973D3EA70CD70B16A426AFD1714AA3`；活动 TODO / `NotImplementedError = 1/1`，没有创建其他骨架。

当前检查点状态：`DOING / 待用户实现`；完成后回复 `改完了 I1b`。

用户实现保留的核心行为：先且只调用一次 `classify_candidate_summary_response(response)`；随后读取既有分类中的 `reason`，仅把 `invalid_candidate_content` 改写为 `invalid_feedback_content`；最终用字典字面量返回严格的 `classification / reason / feedback` 三键，并把既有 `candidate_summary` 原样迁移到 `feedback`。函数没有复制 SDK 响应遍历，也没有新增 `.strip()`、恢复或副作用调用。

决定性无落盘验证：

- 真实既有分类器的 SDK 形状矩阵覆盖合法 `stop`、空 / 缺失外壳、`None` / 空白正文、`length`、choice / message 两种意外 tool calls、`content_filter`、`insufficient_system_resource`、缺失 / 错类型 / 未知 `finish_reason`；全部得到严格三键、正确分类与原因，非 valid 的 `feedback` 均为 `None`。
- 合法反馈使用带首尾空白的动态字符串，输出保持原值和对象 identity，输入响应深拷贝前后相等；证明适配层没有二次清洗或修改响应。
- 用九组替身分类结果隔离适配层：每组都确认既有分类器恰好调用一次且收到同一响应对象，只有 `invalid_candidate_content` 被改名，其他 recoverable / terminal 原因保持不变，源分类字典未被修改。
- AST 确认函数体唯一调用是 `classify_candidate_summary_response`，没有触碰预算门、step、日志、模型 / 工具、恢复、停止或公开结果入口；两层聚焦矩阵总计 `74` 项 PASS。
- 独立只读静态复核确认复用次数、精确三键、原因映射、原文保留、输入不变与零副作用均无阻断问题。非 valid 的 `feedback is None` 依赖已冻结的上游分类器合同，当前边界成立，也避免在薄适配层复制防御逻辑。
- `py_compile` 与 `git diff --check` 退出 0；用户实现后源码 SHA-256 `48DF0F48D537A3CE995EA470485851DCD3724A9052434261CF4CE88193C1F652`；活动产品代码 TODO / `NotImplementedError = 0/0`。

当前检查点状态：`A4-Gate-C4b-2b-1-I1b = PASS`。这只证明 Reflection 反馈响应能被安全归类；尚未构造恢复请求、占用恢复 step、限制唯一恢复次数或接入 Reflection → Refinement 主链。

### `A4-Gate-C4b-2b-1-I1c`｜Reflection recovery 纯 prompt 构造器

当前在做什么：把一次已归类为 recoverable 的 Reflection 失败转换为新的安全评审 prompt。它沿用初次 Reflection prompt 已封装的真实工具结果、Candidate、`allowed_sources` 与 `review_instructions`，但函数签名不接收失败响应或失败正文，所以不可信输出没有机会被拼回恢复上下文。

为什么是独立检查点：I1a 只判断完整链预算，I1b 只分类不可信响应；I1c 只准备下一次调用的可信输入。把三者分开后，可以分别证明“是否允许恢复”“为什么失败”“恢复模型能看到什么”，尚不占用 recovery step，也不会误触发 Refinement。

助手已创建唯一 TODO 骨架 `build_reflection_recovery_prompt(reflection_prompt, recoverable_reason)`；用户当前只实现函数体。完成标准：

1. `reflection_prompt` 只接受非空内建 `str`；字符串子类、非字符串与空 / 纯空白均抛 `ValueError`。校验时可用 `.strip()` 判断是否为空，但不得用清洗后的值替换原 prompt。
2. `recoverable_reason` 只接受非空内建 `str`，且必须属于 `RECOVERABLE_REFLECTION_RESPONSE_REASONS` 的四项；terminal、未知值、字符串子类与非字符串均抛 `ValueError`。
3. 四种原因必须选择不同的固定安全提示：`invalid_response_shape` 要求从头重做同一评审；`invalid_feedback_content` 要求重新提供可用非空反馈；`length` 明确从头重写、不得续写截断文本并更短但完整；`unexpected_tool_calls` 明确本阶段禁止 Tool Action、只能返回反馈文本。
4. 返回一个新的非空内建字符串，以传入的 `reflection_prompt` 原文作为完整、逐字符不变的前缀，再追加 reason 专属提示与公共恢复合同。公共合同要求严格遵守原 `review_instructions`、只重新完成同一 Reflection、只输出非空纯文本反馈；全部通过时仍只输出精确短语“无需改进”，否则逐项给出“问题、证据、修改建议”。
5. 恢复指令不得要求 JSON，不得要求或伪造最终 `status / summary / sources` 对象，不得允许工具调用或模型常识；函数参数与局部逻辑均不得接收、读取或回填失败的原始 response / content。
6. 函数保持纯度：不修改输入，不检查预算，不占 step，不写日志，不调用模型 / 工具，不执行恢复，不构造公开结果，也不发出停止信号。实现顺序可概括为：`严格输入门 → reason 固定提示 → 原 prompt + 分隔符 + 公共恢复合同`。

骨架创建验证：`py_compile` 与 `git diff --check` 退出 0；源码 SHA-256 `CC6266D90631B4EBC0DDD5130CC8D3BBC954BA1FC6D82124DE5DA054D4FE8125`；活动产品代码 TODO / `NotImplementedError = 1/1`，未创建第二个未完成骨架。

当前检查点状态：`DOING / 待用户实现`；完成后回复 `改完了 I1c`。

用户实现保留的核心行为：两个输入都使用 `type(...) is str` 的严格类型门，并只用 `.strip()` 判空；随后按四种 recoverable reason 选择固定阶段提示，再把公共 Reflection recovery 合同追加到未经清洗的原 `reflection_prompt`。函数签名没有失败 response / content，且函数体只有局部字符串构造与返回。

决定性无落盘验证：

- 四种 recoverable reason 均返回非空内建字符串，四个结果互不相同；每个结果都以带首尾空白和动态标记的原 prompt 作为逐字符不变的完整前缀，原标记恰出现一次，输入值未被修改。
- 公共合同逐分支核验：同一 Reflection、原 `review_instructions`、真实工具结果、Candidate、`allowed_sources`、禁止工具与模型常识、只输出非空纯文本反馈、禁止 JSON / 最终三字段对象、精确“无需改进”及“问题、证据、修改建议”全部存在。
- 原因专属语义分别通过：坏外壳要求从头重做同一评审；坏反馈要求重新提供非空反馈；长度截断要求从头、不续写、更短且完整；意外 Tool Action 明确禁止工具并只能返回反馈文本。
- `reflection_prompt` 的 `None / 空 / 纯空白 / int / float / bool / list / dict / str 子类`，以及 reason 的同类坏类型、terminal 原因、候选阶段原因与未知原因全部稳定抛 `ValueError`。
- 签名严格只有两个可信输入；AST 中除 `type / strip / ValueError` 外没有函数调用，也没有预算、step、日志、client、模型、工具、恢复、Refinement、停止或公开结果表面。聚焦矩阵总计 `120` 项 PASS。
- 独立只读静态复核逐行确认严格输入门、四原因提示、原 prompt 保真、公共 Reflection 合同、失败响应不可进入及零副作用均完整，没有阻断或非阻断问题。
- `py_compile` 与 `git diff --check` 退出 0；用户实现后源码 SHA-256 `B5DC5786F0E3EFFDFF2402B614931AE900E6A256B5D7DB3B0FE6A09F61F1F4E2`；活动产品代码 TODO / `NotImplementedError = 0/0`。

当前检查点状态：`A4-Gate-C4b-2b-1-I1c = PASS`。这只证明恢复 prompt 的可信输入边界成立；尚未发起恢复模型调用、验证共享 step / 日志、限制唯一恢复次数或接入 Reflection → Refinement 主链。

### 助手机械整理｜Reflection 原始响应请求层

依据 2026-08-13 已通过的 Reflection 单次模型调用、共享 step、计时与七字段日志证据，助手把旧 `request_reflection_feedback()` 拆为两层；这只是已通过行为的代码整理，不计作新的用户理解证据：

- `request_reflection_response()` 成为模型调用与账本写入的唯一所有者：合法输入后恰好 reserve 一个 step、调用模型一次、写日志一次；API 正常返回的任意对象（包括 `None` / 坏外壳）原样返回给后续分类器，不在 transport 层解析。
- Provider / SDK 异常仍只在该层记录安全异常类型，并用裸 `raise` 保留原异常 identity；不做隐式第二次请求。
- 旧 `request_reflection_feedback()` 保留为正常主链的兼容包装器：只调用原始响应层一次，再执行原来已经通过的外壳检查与 `content.strip()`；不重复创建 client、reserve step、计时或写日志，原七类坏响应的具体 `RuntimeError` 文案保持不变。
- 新增 `ReflectionRecoveryCompletionBudgetError` 窄异常，只携带合法域内的 `current_step / recoverable_reason`，固定异常正文不回显失败响应。

无落盘整理回归：原始响应 identity、`None` 透传、请求参数、一次 step / 七字段日志、Provider 异常 identity 与脱敏日志、step 6 硬门零副作用、兼容接口合法反馈与七类旧坏外壳、正常 Reflection → Refinement → validator 轨迹共 `39` 项 PASS；预算窄异常合法 / 非法构造矩阵 `30` 项 PASS。`py_compile` 与 `git diff --check` 退出 0。

### `A4-Gate-C4b-2b-1-I1d`｜一次 Reflection recovery 编排

当前在做什么：把 I1a 的完整链预算门、I1c 的安全 prompt、机械整理后的唯一原始请求层和 I1b 分类器按严格顺序接起来。它只负责“首次 Reflection 已经 recoverable 后，是否以及怎样发起唯一一次 recovery”；首次 valid / terminal 分流、恢复失败后的阶段停止、成功反馈进入 Refinement 都留给后续 resolver。

助手已创建唯一 TODO 骨架 `attempt_reflection_recovery_once(reflection_prompt, initial_classification, client=None, *, run_context)`；用户当前只实现函数体。完成标准：

1. `reflection_prompt` 必须是非空内建 `str`，只允许 `.strip()` 判空，不改写原文；坏类型、字符串子类、空 / 纯空白在任何 helper、client、step 或日志动作前抛 `ValueError`。
2. `initial_classification` 必须是内建 `dict` 且严格只有 `classification / reason / feedback` 三键；`classification` 必须是内建字符串 `"recoverable"`，`reason` 必须是内建字符串且属于 `RECOVERABLE_REFLECTION_RESPONSE_REASONS`，`feedback` 必须是 `None`。额外 / 缺少键、valid / terminal、未知或跨阶段 reason、非 `None` feedback 全部提前抛 `ValueError`。
3. `run_context` 必须是包含 `step` 的内建 `dict`；该 step 表示首次失败 Reflection 已经调用并记账。不得创建新 context、复制 / 替换其 schema 或自行增加恢复计数；step 的类型与合法域交给已通过的预算门验证。
4. 固定顺序必须是：全部输入门 → `has_reflection_recovery_completion_budget(current_step)` → `build_reflection_recovery_prompt(reflection_prompt, reason)` → `request_reflection_response(recovery_prompt, client, run_context=run_context)` → `classify_reflection_feedback_response(raw_response)` → 原样返回第二次分类对象。
5. 预算不足时，使用当前 step 与安全 reason 抛 `ReflectionRecoveryCompletionBudgetError`；必须发生在 prompt builder、client factory、step、时钟、日志和模型调用之前。轨迹 A 的当前 step 4 应允许 recovery；轨迹 B 的当前 step 5 应在零下游调用下停止。
6. 预算充足时 builder、原始响应请求层和分类器各恰好调用一次；raw response 必须以原 identity 交给分类器，第二次分类无论 valid / recoverable / terminal 都原样返回，不循环、递归或再次恢复。
7. 本函数不得直接调用 `reserve_call_step / append_call_log / create_deepseek_client`，不得调用旧 `request_reflection_feedback()`，不得捕获 Provider / SDK 异常，也不得调用 Refinement、工具、公开结果构造器或停止映射；不修改原 prompt、首次分类字典或共享 context schema。

骨架创建验证：上述机械整理矩阵通过后，`py_compile` 与 `git diff --check` 退出 0；源码 SHA-256 `54BBD63FB0B72CBB473C64C213E845103E8A4E0307D1A68642A305FCAC0C65B1`；活动产品代码 TODO / `NotImplementedError = 1/1`，没有第二个未完成骨架。

当前检查点状态：`DOING / 待用户实现`；完成后回复 `改完了 I1d`。

用户实现保留的核心行为：严格完成 prompt、首次 exact recoverable 分类和共享 context 三层输入门；随后只读取现有 `step / reason`，先经过完整链预算门，预算不足即抛专用窄异常，预算允许才依次调用安全 prompt builder、唯一原始响应请求层和 Reflection 分类器，并直接返回第二次分类对象。

决定性无落盘验证：

- 坏 prompt、坏首次分类与坏 context 均在 budget / builder / requester / classifier 之前抛 `ValueError`；非法 step 只触达纯预算门，所有下游调用为 0。额外 / 缺少键、valid / terminal、错类型与字符串子类、terminal / 跨阶段 / 未知 reason、非 `None` feedback 均被 exact 输入门拒绝。
- step 4 的替身轨迹严格为 `budget → builder → request_reflection_response → classifier → return`；valid / recoverable / terminal 三种第二次分类对象都保持 identity 原样返回，各层恰好一次，原 prompt、首次分类和纯 spy context 未被修改。
- step 5 只调用预算门，随后抛 exact `ReflectionRecoveryCompletionBudgetError(current_step=5, recoverable_reason=...)`；builder / requester / classifier 为 0，context 与尚未使用的 step 6 保持不变。
- Provider sentinel 在 builder 与唯一 requester 后按原异常 identity 上抛，classifier 为 0，没有 catch、重试或二次恢复。
- 真实 builder + 原始请求层 + 分类器组合覆盖二次 valid、`length` recoverable 与 `content_filter` terminal：每条都只调用 recovery SDK 一次，从 step 4 推进到 step 5，只写一条成功模型日志，并保留 step 6 给后续 Refinement；真实 Provider 异常只写一条脱敏失败日志并原样上抛。
- AST 确认没有循环、递归、`try`，没有直接触碰 step / 日志 / client factory、旧兼容接口、Refinement、工具、validator 或公开结果。主聚焦矩阵 `223` 项 PASS。
- 独立只读复核另以 `217` 项矩阵确认 exact 输入门、四 reason、调用顺序与 identity、step 4 / 5 / 6、窄异常和异常短路均无阻断或非阻断问题。
- `py_compile` 与 `git diff --check` 退出 0；用户实现后源码 SHA-256 `52789B24B4EBD664815142314982945C3CED51C7BF8D96045AC76C1A5A210AED`；活动产品代码 TODO / `NotImplementedError = 0/0`。

当前检查点状态：`A4-Gate-C4b-2b-1-I1d = PASS`。用户已完成本切片要求的代表性最小控制流实现；与 Candidate 阶段同构的首次分流、停止异常、公开文案与主链接线由助手依据现有 PASS 证据自动整理，不换名称重复考察。

### 助手自动整理｜`A4-Gate-C4b-2b-1-I1e / I1f`｜阶段 resolver、生产接线与公开停止映射

本段是依据用户已经通过的 G1 与 I1a～I1d、以及前一切片已经审核通过的 Candidate 同构模式完成的机械整理，不计作新的用户理解或独立编码证据：

- 新增专用 `ReflectionFeedbackStageStopError`，只保存 `stop_kind / reflection_reason / recovery_attempts / current_step / allowed_sources` 五项安全事实；不保存失败反馈、raw response、prompt、Candidate、Tool Observation 正文、Provider 错误或日志。构造时复制真实来源列表，并拒绝不可能的“step 4 预算不足”信号。
- `resolve_reflection_feedback_stage()` 首先只调用 I1b 分类器：首次 valid 保留原产品路径的 `feedback.strip()` 行为；首次 terminal 不恢复；首次 recoverable 恰好进入 I1d 一次。恢复后 valid 才返回反馈，terminal 或仍 recoverable 都停止；只捕获 I1d 的预算窄异常并保留异常链，不捕获 Provider / SDK 或其他失败。
- `reflect_refine_and_validate()` 改为 `raw Reflection response → resolver → valid feedback 才构造 Refinement → 最终 validator`。同一 client、同一共享 context 与同一份真实 `allowed_sources` 贯穿全链；resolver 不重新执行工具或重复派生来源。
- `run_research_summary_once()` 只在 Reflection 调用边界捕获 exact 阶段停止信号；再次验证字段、reason / attempts 组合与预算事实，并把信号的 step / attempts 与本轮真实调用增量、信号来源与从真实工具结果重新派生的来源逐值逐顺序对账后，才返回 `needs_manual`。公开结果直接复制重新派生的可信来源，只表示人工接管证据池；文案明确“已有合法 Candidate，但没有最终摘要完成 Reflection、Refinement 与客户端校验”。子类、篡改信号、Provider / SDK、Refinement 与 validator 异常继续原样向上抛。
- 已把冻结规则同步到 `code/stage4/problem-contract.md` 第 6 节：Reflection 协议 / 内容失败至多恢复一次，恢复前必须同时预留 recovery 与 Refinement 两个 step；失败响应不得回填，不得重新执行工具；terminal、预算不足或唯一恢复仍失败时安全停止。该合同更新不扩展到尚未设计的 Provider、Refinement 或 validator 恢复。

自动整理状态：`I1e / I1f = 已验证`。这些是助手根据既有 PASS 证据完成的重复接线，不单独升级用户掌握度。

### `A4-Gate-C4b-2b-1-I2`｜端到端故障注入、边界与正常链回归

决定性无落盘验证：

- stop signal + resolver 矩阵 `136` 项 PASS：覆盖首次 valid、4 个 terminal reason、4 个 recoverable reason 分别通向恢复 valid / terminal / recoverable、step 5 的四类预算停止、来源防御性复制、严格输入门、异常 cause 与 Provider identity。
- 公开映射矩阵 `158` 项 PASS：覆盖三种 stop kind、terminal attempts `0/1`、四类 terminal / recoverable reason、严格三字段结果、来源顺序和值保持但列表 identity 隔离、固定安全文案，以及子类 / 篡改字段 / 假预算事实与非目标异常原样上抛。
- 完整入口 fake SDK 矩阵 `50` 项 PASS：正常五步成功；step 4 Reflection failure → step 5 recovery → step 6 Refinement；首次 terminal；recovery 后 terminal / recoverable；Candidate recovery 已占 step 4 后，step 5 Reflection failure 在零恢复 / 零 Refinement下停止；初次 / 恢复 Provider、Refinement Provider / 坏 JSON及 validator 来源失败均保持既有边界。
- topic 路径内存 fake search `5` 项 PASS：恢复成功轨迹使用一次真实形态的搜索工具证据，恢复 prompt 同时保留 URL 与 snippet，最终来源通过白名单校验。
- 首轮最后两处加固后的定向复归 `34` 项 PASS：step 4 的假预算停止构造被拒绝；step 5 停止保留未使用 step 6；公开结果明确合法 Candidate 与未完成最终链；篡改 reason 和非目标异常按 identity 上抛。AST 边界 `9` 项 PASS，确认 I1d 无循环 / catch，resolver 只捕获 `ReflectionRecoveryCompletionBudgetError`，生产链不再调用旧兼容 parser，公开入口只新增专用 Reflection stop catch。
- 独立终审随后发现一个仅位于助手公开映射层的防御性缺口：正常 resolver 不会产生，但一个 exact 类型、枚举合法却伪造 `current_step / recovery_attempts / allowed_sources` 的内部停止对象仍可能被映射。助手据此新增 `reflection_start_step` 调用差额对账、当前共享 step 对账和真实来源重新派生；公开结果改为直接复制重新派生来源。该问题不在用户实现的 I1d，不记作用户 RETRY。
- 加固后本地主链端到端复归 `24` 项 PASS：正常五步、step 4 恢复成功、首次 terminal、唯一恢复耗尽、Candidate recovery 后 step 5 预算停止均保持正确；伪造来源和伪造 step / attempts 均按原异常 identity 上抛。独立只读复核在行为最终快照上完成全 reason 矩阵 `83/83`、生产回放 / 来源重派生 / 伪造信号 / MAX_STEPS / topic / 异常边界 `16/16`、AST `11/11`，无行为阻断。独立复核指出的两处过时 docstring 已随后机械更新；最终 AST smoke `7` 项 PASS。
- 真实 DeepSeek `relative_path=sample.txt` 正常生产链再次返回 `{"status":"success","summary":"这是 T3-03 文件工具的沙箱测试文件。如果你能读到这行，说明正常读取成功。","sources":["sample.txt"]}`，证明新分流没有破坏既有真实五步路径；故障注入仍使用 fake / spy 以稳定制造无法要求真实服务随机返回的坏协议响应。
- 最终 `py_compile` 与 `git diff --check` 退出 0；活动产品代码 TODO / `NotImplementedError = 0/0`；U1 状态头机械更新后的当前源码 SHA-256 `2581ABFD1B08ECCEF854A16BD77A7AA00A33BF4A3E8B1CDC4DCBFAEE1FC2A968`。

自动工程状态：`I2 = 已验证`。用户随后完成 U1 审核，当前子单元已经收口；不再要求用户重复实现或复述同构 wiring。

### `A4-Gate-C4b-2b-1-U1`｜用户最终行为审核

审核范围只包含三条可观察轨迹：①首次 Reflection 在 step 4 可恢复且 recovery 成功时，step 5 recovery 后必须继续 step 6 Refinement；②首次 Reflection 在 step 5 可恢复时，不发起 recovery、不消耗 step 6，返回人工接管；③首次 / 恢复后的 terminal 或唯一恢复仍失败时，不进入 Refinement，公开来源只是真实证据池，不代表最终摘要引用。用户只需确认这些轨迹和来源语义是否符合自己的意图，不再复述 helper 或重写代码。

用户原始审核结论：

> U1 通过

判定：`PASS`。用户确认三条最终调用轨迹、预算停止行为和人工接管来源语义符合自己的意图；没有提出新的行为差异或待修项。该结论只关闭 `A4-Gate-C4b-2b-1` 教学 / 实现子单元，不构成父任务 `A4-Gate` 的正式 PASS。

## 第二块当前范围与接力

- 今日计划范围 / 今日剩余：第二块计划中的定向判断、代表性用户实现、助手同构接线、故障注入、回归、真实正常链验证与 U1 用户审核均已完成；`G1 / I1a～I1d / U1 = PASS`，`I1e / I1f / I2` 已由助手验证。第二块已收口，今日剩余为 0；不追加第三块或新切片。
- 任务总剩余：父任务 `A4-Gate` 仍需 Candidate / Reflection Provider/API 异常分类与恢复、Refinement / 最终 validator 失败恢复、HITL allow / deny、14 条 `eval_cases.json`、五个 Gate 必答问题、Problem Contract 总体定稿和正式 Gate 复核。当前 Reflection 协议 / 内容失败子单元已经闭合，但父任务继续保持 `DOING`。
- 下次 Session 起点（不属于今天）：从 Candidate / Reflection Provider/API 异常的分类边界与恢复预算开始；必须在未来自然日重新排入不超过 180 分钟的计划，今天不创建该切片骨架。
