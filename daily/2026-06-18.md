# 每日学习打卡

日期：2026-06-18（周四）

学习时长：目标 ~2h（工作日）

完成任务编号：L1-04 流式输出（计划）+ L1-05 预览

今日定位：

- 工作日：写 L1-04 流式输出（逐步显示模型输出），并预览 L1-05 参数概念。
- 加班 fallback：累了就只读 OpenAI 流式输出文档 + 参数文档，代码挪到节后。
- 明天起端午放假（6/19–21）全休。**L1-05 参数实验 + L1-Gate 闯关安排在节后 6/27–28 周末**，今天不强求做完。

今日推荐资料（计划要读的，带链接；实际看了/读了填到下面两栏）：

- [OpenAI 流式输出 Streaming](https://platform.openai.com/docs/guides/streaming-responses)（DeepSeek 接口一致）
- [OpenAI API 参考（参数 temperature 等）](https://platform.openai.com/docs/api-reference/responses/create)（为 L1-05 预览）
- （可选视频）流式 vs 非流式输出对比小节

看了哪些视频/哪一集：

（做完后填你实际看的）

读了哪些文档/哪一节：

（做完后填你实际读的）

代码/产物位置：

- code/stage1/l1_04_stream_chat.py

运行提示：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
.\.venv\Scripts\python.exe code\stage1\l1_04_stream_chat.py
```

今日任务清单：

- [ ] 写 `l1_04_stream_chat.py`：逐步显示模型输出
- [ ] 程序退出不留异常堆栈
- [ ] 回答 L1-04 3 个检查题
- [ ] 预览 L1-05 参数概念（temperature、token 成本）

今日练习题（草稿示例：6/18 当天用 daily-practice-builder 按真实进度增删；锚定 `l1_04_stream_chat.py` 要改成 DeepSeek 流式）：

A. 快速回忆

1. DeepSeek 流式：`client.chat.completions.create(..., stream=True)` 返回的是一个可迭代对象。每次循环拿到的 `chunk` 里，模型新吐出的那一小段文本在哪个字段？（提示：`chunk.choices[0].delta.content`）

B. 解释与比较

2. 流式输出（`stream=True`）和一次性输出（`stream=False`）：从"用户体感"和"代码怎么收结果"两方面，各说一点区别。
3. `l1_04_stream_chat.py` 现在写的是 `OPENAI_API_KEY` + OpenAI 的 `responses`，而且没 key 时会走"模拟流式"假分支。你用的是 DeepSeek。要把它改成真流式，需要改哪 3 处？（提示：key 名 / `client` 的 base_url 和调用方式 / 用 `stream=True` 迭代 `delta`）

C. 代码/操作

4. 写出"逐字打印、不换行、最后补一个换行"的核心两行：循环内 `print(片段, end="", flush=True)`，循环结束后 `print()`。为什么要加 `flush=True`？
5. 续上题：DeepSeek 流式里，`delta.content` 有时会是 `None`（比如最后一块）。打印前应该怎么判断一下，才不会把 `None` 打出来或报错？

D. 应用与迁移

6. （接 L1-04 通过标准"程序退出不留异常堆栈"）流式中途网络断了，怎么收尾才不会甩一堆异常堆栈？用 `try/except` 包住迭代，`except` 里应该做什么、打印什么？

E. 自评

7. 今天最不确定的概念是什么？哪一题是猜的？

遇到的问题：

我最不确定的点：

希望 Codex 检查：

- 检查 `l1_04_stream_chat.py` 能否逐步输出、退出是否干净
- 批改 L1-04 检查题
- 判定 L1-04 是 PASS 还是 RETRY

Codex 判定：

Codex 当天进度总结：

Codex 总进度总结：

Codex 对问题/不确定点的解释：

Codex 问答点评与补充：

Codex 超前内容提示：

补救任务：

下一步（节后）：

- 6/19–21 端午全休。
- 6/22 起恢复；L1-05 参数实验 + L1-Gate 闯关排在 6/27–28 周末，阶段 1 收尾。
