# 每日学习打卡

日期：2026-06-18（周四）

> 注：本关 L1-05 为上周四（6/18）排定的任务，实际于 2026-06-23 补做并检查（端午假期顺延，当天补「上周四 + 昨天 + 今天」三天进度）。L1-04 已在 `daily/2026-06-17.md` 收录 PASS，故本文件由原 L1-04 计划改记为 L1-05 实绩。

学习时长：约 2h（学 + 实验 + 检查）

完成任务编号：L1-05 参数实验与成本意识

今日定位：

- 「学」：learning-coach 带学采样参数（temperature / top-k / top-p）+ DeepSeek 推荐温度 + token 成本。
- 「练」：写 `l1_05_params_experiment.py`，对同一问题跑 3 组 temperature，打印回答 + usage。
- 「检查」：实跑验证 + 三问作答批改 + 回填。

看了哪些视频/哪一节：

- 未看视频，以文档为主。

读了哪些文档/哪一节：

- Hello-Agents 第 3 章 3.2.1《模型采样参数》（`repos/hello-agents/docs/chapter3/第三章 大语言模型基础.md:517-543`）：temperature 的 Softmax / 温度系数、Top-k、Top-p 原理与区别。
- DeepSeek 官方《温度设置》：默认 1.0；代码/数学 0.0、数据分析 1.0、对话/翻译 1.3、创意 1.5。
- DeepSeek 官方《模型与价格》：v4-pro 输入命中 0.025 / 未命中 3 / 输出 6（元/百万 token）；成本 = 输入(缓存命中/未命中) + 输出。

代码/产物位置：

- `code/stage1/l1_05_params_experiment.py`
- `code/stage1/l1_05_params_experiment.md`
- 知识库卡片：`D:/AI-Knowledge/02-Concepts/LLM/sampling-params-and-cost.md`

运行提示：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
$env:PYTHONUTF8=1; .\.venv\Scripts\python.exe code\stage1\l1_05_params_experiment.py
```

实跑结果（同一问题「给橘猫起 5 个有创意的名字」，只变 temperature）：

| temperature | prompt_tokens | completion_tokens | total | 名字风格 |
|---|---|---|---|---|
| 0.0 | 24 | 411 | 435 | 经典稳妥（金不换 / 琥珀 / 陈皮） |
| 1.3 | 24 | 464 | 488 | 发散（木星·流浪地球梗 / 姜戈） |
| 1.5 | 24 | 453 | 477 | 玩梗（欧皇 / 芝士 / 扶苏） |

关键观察：三组 `prompt_tokens` 都是 24（temperature 不改输入、不改单价）；输出 token 各异（回答长短才决定输出成本）；温度越高名字越「放飞」。

我的问答答案（L1-05 通过标准三问 + 教练批改）：

```python
# Q1 差异观察：低温 vs 高温
# 答：低温稳，高温野。
# 判定 PASS（一句话抓住本质）。

# Q2 参数选择
# 答：起名是创意任务，低/高都行（看要经典还是发散）；写代码选低温，要稳健、不需要太多不确定。
# 判定 PASS（判断有依据：写代码低温 = 可复现）。

# Q3 成本三问
# ① temperature 变高意味着什么？
#   答：回答会变得更随机。
#   订正：更精确说法是「把概率差距压平 → 冷门词也能冒出 → 更发散/有创意，但可能不连贯」，
#         「随机」是表象，「差距压平」是机制。  判定 PARTIAL → PASS
# ② 哪类任务适合低随机性？
#   答：写代码、数学题之类严谨任务，保证相同输入相同输出（可复现）。  判定 PASS
# ③ token 成本由哪些部分构成？
#   答：输入(system+历史+本轮) + 输出(模型回答)。
#   订正：输入还分「缓存命中(0.025)/未命中(3)」两种价；输出最贵(6)。
#         完整答案 = 输入(分缓存命中/未命中) + 输出，且输出最贵。  判定 RETRY → PASS
```

中间的坑（错误理解 → 正确理解）：

- 把「top-k=1 / temperature→0 最死板」误接到「高温更极端」那端 → 纠正：`k=1` ≡ `T→0` ≡ 贪心，等于「低温到底」，与高温(放飞)**相反**。判断方向先问「更死板还是更放飞」。
- Q3 漏了「缓存命中/未命中」与「输出最贵」→ 已补。
- temperature「变高=更随机」表述偏浅 → 升级为「差距压平 → 发散」的机制说法。

遇到的问题：

- `call_model` 原本开着思考模式（`reasoning_effort` / `thinking`），会干扰温度实验 → 实验里关掉，温度效果才干净，也不白烧 token。
- 数学公式（Softmax、`e^(z/T)`）一开始完全看不懂、以为是乱码 → 经「抽签箱 + 放大器」类比后理解：公式只是「分数先除以 T，再翻成百分比」，温度调的是「概率差距悬殊度」。

我最不确定的点：

- 采样参数的数学直觉（已用类比攻克，建议过两天用「抽签箱」自测复述一遍）。
- token 成本里「缓存命中/未命中」的区别要记牢。

希望 Codex 检查：

- `l1_05_params_experiment.py` 是否正确传 `temperature`、关思考模式、返回 `usage`。
- 三问作答是否到位。

Codex 判定：PASS（L1-05 参数实验与成本意识：3 组 temperature 实跑成功、有真实输出对比、能解释参数选择；三问经订正后全部 PASS）

Codex 当天进度总结：

- 学：learning-coach 5 关全 PASS（抽签/Softmax → temperature 陡平 → top-k/top-p 砍名单 → DeepSeek 温度表 → token 成本）。
- 练：`l1_05_params_experiment.py` 写完并实跑，3 组温度真实调用 DeepSeek，打印回答 + usage。
- 代码正确性：`temperature` 传入、关思考模式、`usage` 返回、出错/无 key 路径都返回 `(字符串, None)` 二元组，保证 `main` 拆包不崩（延续 L1-04「错误别返回 None 毒化后续」的思维）。

Codex 总进度总结：

- Stage0 全 PASS；Stage1：L1-01 ~ L1-05 全 PASS。仅剩 `L1-Gate`（CLI chatbot 整合关）即可收尾阶段 1。
- 工程基础 B0-01 ~ B0-Gate 仍按触发点穿插补。

Codex 对问题/不确定点的解释：

- Softmax = 把分数(logits)换成百分比的工具；temperature 公式只多了「每个分数先除以 T」，小 T 拉大差距(陡/死板)、大 T 压平差距(平/放飞)；`T=0`=贪心。
- top-k / top-p 砍候选名单；`k=1` ≡ `T→0` ≡ 贪心。
- 成本：输入(缓存命中/未命中) + 输出，输出最贵；`usage` 看 `prompt_tokens`/`completion_tokens`/`total_tokens`；history 越长输入越大越贵。

Codex 问答点评与补充：

- 总体：概念吃得扎实，能把「成本」和「上下文管理」串起来（面试加分点）。代码迁移到位，错误返回形状一致。
- 需巩固：① temperature 用「差距压平」机制说，而非只说「随机」；② token 成本要含「缓存命中/未命中 + 输出最贵」。
- 更强版本见知识库卡片 `sampling-params-and-cost.md` 的「常见坑」清单。

Codex 超前内容提示：

- 当前够用：会调 temperature + 懂成本即达标。
- 属于后续阶段（先不要求）：① `top_p`/采样深入调优 → 真实项目调参；② token 成本 + tracing → J11-05 可观测；③ 多厂商成本/延迟对比 → S-01。

补救任务：

- 无（L1-05 全 PASS）。可选：把 `l1_05_params_experiment.md` 的「差异观察 / 参数选择 / 三问」誊写完整（实跑数据 + 上面订正后的答案）；可加跑 `[0.0, 0.0, 1.5, 1.5]` 亲眼看「低温两次几乎一样、高温两次明显不同」。

今天最虚 / 下次重点复习：

- 最虚：采样参数的数学直觉（Softmax / 温度系数）—— 已用「抽签箱」类比攻克，过两天自测复述；以及 token 成本的「缓存命中/未命中」要记牢。

下一步：

- `L1-Gate` API 入门闯关（CLI chatbot：多轮 + 流式 + 退出 + 错误提示 + 历史长度限制）—— 整合关，放精力好的整块时间（基本=周末）做。
