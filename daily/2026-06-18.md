# 每日学习打卡

日期：2026-06-18（周四）

学习时长：目标 ~2h（工作日）

完成任务编号：L1-04 流式输出（计划）+ L1-05 预览

今日定位：

- 工作日：写 L1-04 流式输出（逐步显示模型输出），并预览 L1-05 参数概念。
- 加班 fallback：累了就只读 OpenAI 流式输出文档 + 参数文档，代码挪到节后。
- 明天起端午放假（6/19–21）全休。**L1-05 参数实验 + L1-Gate 闯关安排在节后 6/27–28 周末**，今天不强求做完。

今日推荐资料（计划要读的，带链接；实际看了/读了填到下面两栏）：

- [OpenAI 流式输出 Streaming](https://platform.openai.com/docs/guides/streaming-responses)（DeepSeek 接口一致）
- [OpenAI API 参考（参数 temperature 等）](https://platform.openai.com/docs/api-reference/responses/create)（为 L1-05 预览）
- （可选视频）流式 vs 非流式输出对比小节

看了哪些视频/哪一集：

（做完后填你实际看的）

读了哪些文档/哪一节：

（做完后填你实际读的）

代码/产物位置：

- code/stage1/l1_04_stream_chat.py

运行提示：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
.\.venv\Scripts\python.exe code\stage1\l1_04_stream_chat.py
```

今日任务清单：

- [ ] 写 `l1_04_stream_chat.py`：逐步显示模型输出
- [ ] 程序退出不留异常堆栈
- [ ] 回答今日练习题（15 题）
- [ ] 预览 L1-05 参数概念（temperature、token 成本）

今日练习题（按新基调重出：15 题 / A 档难度 / 含 debug + 改代码；6/18 当天用 daily-practice-builder 按真实进度增删；锚定 `l1_04_stream_chat.py`，今天要把它改成 DeepSeek 真流式）：

A. 快速回忆

1. 今天把 `l1_04_stream_chat.py` 改造成 DeepSeek 真流式后，核心两行长这样（`client` 已用 `OpenAI(api_key=..., base_url="https://api.deepseek.com")` 建好）：

```python
stream = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": question}],
    stream=True,
)
for chunk in stream:
    piece = chunk.choices[0].delta.content
    if piece is not None:
        print(piece, end="", flush=True)
```

凭记忆答两问：(1) 因为传了 `stream=True`，`create(...)` 返回的 `stream` 是什么类型的东西、用什么方式取数据（一次性拿到完整文本，还是需要怎么处理）？(2) 每个 `chunk` 里那一小段【增量文本】要从哪个字段取？把完整字段路径默写出来（就是上面 `piece = ___` 的右边）。

1. 改造后逐字显示用的是这行（`piece` 是本次 `chunk` 的增量文本，比如 `"你"`）：

```python
print(piece, end="", flush=True)
```

分别说清这两个参数各自的作用：
(1) `end=""` 改变了 `print` 的什么默认行为？如果去掉它（写成 `print(piece, flush=True)`），假设流依次产生 delta：`"你"`、`"好"`、`"呀"`，终端会显示成什么样、和带 `end=""` 时有什么不同？
(2) `flush=True` 是干什么的？不写它，对「逐字一个个蹦出来」这个效果有什么影响？

1. 间隔复习 L1-01（非流式）。假设有一个 `stream=False` 的非流式写法如下（这里只考「取完整文本的字段路径」，模型名等用占位）：

```python
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": prompt}],
    stream=False,
)
reply = ______   # 凭记忆默写这一行右边，取出完整回复文本
```

(1) 默写 `reply =` 右边那个完整表达式（从 `response` 一路取到那段文本）。
(2) 用一句话说清：上面这个 `stream=False` 的取法，和今天 L1-04 流式里 `chunk.choices[0].delta.content` 的取法，差别在哪（一个取的是什么、另一个取的是什么）？

B. 对比与解释

1. 对比「一次性返回（`stream=False`）」和「流式返回（`stream=True`）」这两种取结果的方式，请从【用户体感】和【代码怎么收结果】两方面说清差别。给你两段对照代码（都是受控示例，非流式只考字段路径，模型名用占位）：

A. 非流式写法示例（`stream=False`）：

```python
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": question}],
    stream=False,
)
text = response.choices[0].message.content
print(text)
```

B. 流式（`l1_04` 改造后要写成的样子）：

```python
stream = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": question}],
    stream=True,
)
for chunk in stream:
    piece = chunk.choices[0].delta.content
    if piece is not None:
        print(piece, end="", flush=True)
print()
```

请回答：(1) 用户体感上，A 和 B 在「等待 → 看到字」这件事上有什么不同？(2) 代码怎么收结果上，A 拿到的 `create()` 返回值是什么类型、用哪个字段取完整文本；B 拿到的返回值是什么类型、需要怎么处理才能拼出完整文本？注意 A 取的是 `message.content`，B 取的是 `delta.content`，分别代表什么。

1. 在 `l1_04` 流式迭代里有这么一句取增量的代码：

```python
for chunk in stream:
    piece = chunk.choices[0].delta.content
    if piece is not None:
        print(piece, end="", flush=True)
```

为什么 `piece` 在 `print` 之前要先判 `if piece is not None`？请解释 `piece` 什么时候会是 `None`，并给出「如果不判 `None`」会出现的两种坏结果对照：

坏写法甲（直接 print）：

```python
for chunk in stream:
    piece = chunk.choices[0].delta.content
    print(piece, end="", flush=True)
```

坏写法乙（边收边拼字符串）：

```python
text = ""
for chunk in stream:
    piece = chunk.choices[0].delta.content
    text += piece
```

分别说明甲、乙各会出什么问题（甲的屏幕上会多出什么、乙会抛什么异常），以及为什么。

1. `l1_04_stream_chat.py` 现在在【没设 key】时会走下面这个分支：

```python
if not os.getenv("OPENAI_API_KEY"):
    print("未设置 OPENAI_API_KEY。这里先模拟流式输出：")
    for chunk in ["这是", " 一个", " stream", " 练习。"]:
        print(chunk, end="", flush=True)
    print()
    return
```

而改造后真正的 DeepSeek 流式核心是：

```python
stream = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": question}],
    stream=True,
)
for chunk in stream:
    piece = chunk.choices[0].delta.content
    if piece is not None:
        print(piece, end="", flush=True)
print()
```

请对比这两段：(1) 不管你问什么问题（`question` 是什么），上面那个「模拟流式」分支打印出来的内容会不会变？为什么？(2) 为什么说这个模拟分支「没真正调用模型」？它和真·DeepSeek 流式在「字是从哪来的、是否发了网络请求给模型」上差在哪？(3) 它逐块 `print` + `flush` 这一点，和真流式是不是一样的？

C. 实操（预测 / debug / 命令 / 读码）

1. 假设你写了下面这段「迭代流对象 + print」的代码，并且**假设流式依次产生这些 `delta.content`**（注意中间和结尾各有一个 `None`，这模拟真实流里第一块只带 role、结束块没有内容的情况）：

```python
stream = ["你", "好", None, "，三", "玖", None]  # 每个元素模拟 chunk.choices[0].delta.content

text = ""
for piece in stream:
    if piece is None:
        continue
    print(piece, end="", flush=True)
    text += piece
print()
print(repr(text))
```

请回答：(1) 终端**逐字打印**出来的那一行（`print(piece, ...)` 累计出来的）完整字符串是什么？(2) 最后 `print(repr(text))` 这一行会打印出什么？（说明：`repr` 会把字符串带上引号显示。）

1. 下面是三玖照着「DeepSeek 真流式」写的 `stream_answer`，运行后程序**没有报错、也能出结果**，但现象不对：终端上不是一个字一个字往外蹦，而是卡几秒后整段答案**一次性**全部冒出来。代码里只埋了**一个** bug。

```python
import os
from openai import OpenAI

def stream_answer(question):
    client = OpenAI(
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com",
    )
    stream = client.chat.completions.create(
        model="deepseek-chat",
        messages=[{"role": "user", "content": question}],
        stream=True,
    )
    for chunk in stream:
        piece = chunk.choices[0].delta.content
        if piece is not None:
            print(piece, end="")
    print()
```

请回答：(1) bug 在哪一行/哪个写法；(2) 为什么会导致「不是逐字、而是最后一次性出现」这个症状；(3) 怎么改。

1. 另一个版本，三玖把 L1-01 的「一次性返回」写法和 `stream=True` 混在了一起。代码只埋了**一个** bug：

```python
import os
from openai import OpenAI

def stream_answer(question):
    client = OpenAI(
        api_key=os.getenv("DEEPSEEK_API_KEY"),
        base_url="https://api.deepseek.com",
    )
    response = client.chat.completions.create(
        model="deepseek-chat",
        messages=[{"role": "user", "content": question}],
        stream=True,
    )
    print(response.choices[0].message.content)
```

请回答：(1) bug 在哪一行；(2) 运行时大致会出现什么症状；(3) 怎么改成正确的逐字流式输出。

1. 场景：项目根目录是 `C:\Users\26823\Desktop\AI-Agent-Learning`，里面有一个名为 `.venv` 的虚拟环境（Windows），脚本在 `code\stage1\l1_04_stream_chat.py`。你现在打开了一个**全新的 PowerShell 窗口**，当前目录就停在项目根目录，还没激活过任何环境。请写出：用这个 `.venv` 来跑 `l1_04_stream_chat.py` 的命令。两种方式都给出：(a) 先激活 `.venv` 再运行；(b) 不激活、直接用 `.venv` 里的 python 解释器运行。

2. 看现在仓库里【还没改造】的 `l1_04_stream_chat.py` 的 `stream_answer` 开头这几行：

```python
def stream_answer(question):
    if not os.getenv("OPENAI_API_KEY"):
        print("未设置 OPENAI_API_KEY。这里先模拟流式输出：")
        for chunk in ["这是", " 一个", " stream", " 练习。"]:
            print(chunk, end="", flush=True)
        print()
        return
    # ... 下面才是真正调模型的分支
```

请回答：(1) 这个「模拟流式输出」的假分支，在**什么条件下**会被触发？(2) 三玖用的是 DeepSeek（key 存在环境变量 `DEEPSEEK_API_KEY` 里，并没有设置 `OPENAI_API_KEY`），那么对他来说，这个假分支是不是**总会**触发？(3) 简述这说明今天改造时为什么必须动 key 名这一处。

D. 改代码与迁移

1. 把 `code/stage1/l1_04_stream_chat.py` 里的 `stream_answer(question)` 从「OpenAI responses 接口」改造成「DeepSeek 真流式」，请写出新版 `stream_answer` 函数的完整代码。

现状（待改造）：

```python
import os

def stream_answer(question):
    if not os.getenv("OPENAI_API_KEY"):
        print("未设置 OPENAI_API_KEY。这里先模拟流式输出：")
        for chunk in ["这是", " 一个", " stream", " 练习。"]:
            print(chunk, end="", flush=True)
        print()
        return
    try:
        from openai import OpenAI
        client = OpenAI()
        with client.responses.stream(
            model=os.getenv("OPENAI_MODEL", "gpt-4.1-mini"),
            input=question,
        ) as stream:
            for event in stream:
                if event.type == "response.output_text.delta":
                    print(event.delta, end="", flush=True)
            print()
    except Exception as exc:
        print(f"流式输出失败：{exc}")
```

硬性要求（每条都要落实，这就是判分点）：

2. 构造 client：`OpenAI(api_key=os.getenv("DEEPSEEK_API_KEY"), base_url="https://api.deepseek.com")`。
3. 改用 chat.completions（不是 responses）：`client.chat.completions.create(model="deepseek-chat", messages=[{"role": "user", "content": question}], stream=True)`，它返回一个【可迭代对象】。
4. 迭代取增量：`for chunk in stream:` 里 `piece = chunk.choices[0].delta.content`（是 `delta.content`，不是 `message.content`）。
5. `piece` 可能是 `None`（第一块只带 role、或结束块），所以 `print` 之前要先判 `if piece is not None:`（或 `if piece:`），否则会打印出字面的 `None`。
6. 逐字显示：`print(piece, end="", flush=True)`；整个循环结束后再补一个 `print()` 换行。

请把符合以上 6 点的 `stream_answer` 完整函数写出来。

1. 把今天的流式输出迁移进 L1-03 多轮聊天 `code/stage1/l1_03_chat.py`。它现在的主循环是一次性拿完整答案（非流式）：

```python
from l1_01_first_call import call_model

def main():
    history = []
    while True:
        question = input("你：").strip()
        if question.lower() == "exit":
            break
        if not question:
            print("请输入内容，或输入 exit。")
            continue
        answer = call_model(build_prompt(history, question))   # 一次性返回完整文本
        print("AI：", answer)
        history.extend([("user", question), ("assistant", answer)])
        history = trim_history(history)
```

问题：现在要让 AI 的回复【逐字显示】在屏幕上，但 `history` 里仍然要存【这一轮完整的答案文本】（因为下一轮 `build_prompt` 要把它拼进 prompt 给模型看，不能只存第一块 delta）。

关键矛盾：流式时模型是一块块 `delta.content` 吐出来的，你 `print` 完就没了，怎么同时得到「完整答案」存回 `history`？（提示：迭代时一边 print 一边把每块累加到一个字符串里，比如 `full += piece`。）

请描述你的改造思路，并写出主循环里被替换的那几行关键代码（从拿到 stream、迭代逐字打印、累加 `full`、到把 `full` 存进 `history`）。假设你已经有一个返回可迭代 stream 的函数 `stream_model(prompt)`（内部就是 chat.completions + `stream=True`）。

1. 对应今天的通过标准「程序退出不留异常堆栈」。场景：用 DeepSeek 真流式输出时，模型已经吐了前几块 delta（屏幕上已经打印了一部分文字），这时【网络断了 / 请求超时】，迭代 stream 的循环中途抛异常。看这两段写法，回答下面的问题：

写法 A（没保护）：

```python
stream = client.chat.completions.create(model="deepseek-chat", messages=msgs, stream=True)
for chunk in stream:
    piece = chunk.choices[0].delta.content
    if piece is not None:
        print(piece, end="", flush=True)
print()
```

写法 B（用 try/except 包住迭代）：

```python
try:
    stream = client.chat.completions.create(model="deepseek-chat", messages=msgs, stream=True)
    for chunk in stream:
        piece = chunk.choices[0].delta.content
        if piece is not None:
            print(piece, end="", flush=True)
    print()
except Exception as exc:
    # ←这里该写什么？
    ...
```

请回答：(1) 写法 A 在「打印了一半时网络断」会发生什么？用户在终端会看到什么（结合通过标准说，为什么不合格）？(2) 写法 B 的 except 里具体该做哪几件事？特别是：要不要先补一个换行？给用户打印的提示应该包含哪两类信息（参照 L1-02 的「错误提示=具体原因+可操作下一步，不甩 traceback」）？请给出 except 块里你会写的 2~3 行代码。

E. 复盘

1. 回看今天 L1-04 你做过的题（围绕 `code/stage1/l1_04_stream_chat.py` 改成 DeepSeek 真流式：`stream=True` 返回可迭代对象、迭代取 `chunk.choices[0].delta.content`、判 `None`、`print(..., end="", flush=True)`）。请挑出其中【你是半猜半蒙答对 / 最没底】的一题，然后说清楚两件事：

（a）当时你具体在哪个点上犹豫、几乎要写错——请用一个【具体的二选一】来描述，例如「记不准取增量到底是 `chunk.choices[0].delta.content` 还是 `chunk.choices[0].message.content`」「记不准结尾那个 `print()` 该不该加、加在循环里还是循环外」「记不准 `flush=True` 是写在 `print` 里还是另外调一个函数」；
（b）现在让你不靠任何提示，把这个点的正确写法写出来（关键那一行代码 / 或一句话规则即可）。

如果你觉得今天每题都完全有把握、没有半猜的，也要说出来，并补一句：那把『迭代时取增量文本』那一行的完整代码默写一遍当复核（应为 `piece = chunk.choices[0].delta.content`）。

遇到的问题：

我最不确定的点：

希望 Codex 检查：

- 检查 `l1_04_stream_chat.py` 能否逐步输出、退出是否干净
- 批改 L1-04 检查题
- 判定 L1-04 是 PASS 还是 RETRY

Codex 判定：

Codex 当天进度总结：

Codex 总进度总结：

Codex 对问题/不确定点的解释：

Codex 问答点评与补充：

Codex 超前内容提示：

补救任务：

下一步（节后）：

- 6/19–21 端午全休。
- 6/22 起恢复；L1-05 参数实验 + L1-Gate 闯关排在 6/27–28 周末，阶段 1 收尾。
