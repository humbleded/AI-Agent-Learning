# 每日学习打卡

- 日期：2026-07-21
- 当前任务：A4-03 ReAct
- 任务状态：DOING（带读阶段，不是正式判定）
- 今日环节：A4-03 正式练习（带读与代码教学检查点已完成）
- 前序证据：`daily/2026-07-16.md` 已完成 Hello-Agents 4.2 引言与 4.2.1 带读

## 今日目标与范围

- 先收口 G1 中“工具执行、Observation 回填、下一轮 Thought 读取”的主语边界。
- G1 通过后，下一单元进入 Hello-Agents 4.2.2“工具的定义与实现”。
- 今日仍按单学习块、全天总预算不超过 3 小时；正式练习与收尾也计入该预算。

## 带读与即时考察

### G1 — ReAct 工作流程（跨日续答）

- 题目：在天气工具场景中，判断 Action 出现时是否已执行工具、工具结果叫什么且由谁产生，以及下一轮 Thought 如何利用结果。
- 原始作答：

  > 1.Action 出现时，天气工具没有执行
  > 2.在 ReAct 中叫observation,由客户端执行的工具产生
  > 3.下一轮 Thought会把由observation回写到历史中一起判断是否满足目标

- 做对的部分：
  - 正确指出 `Action` 只是调用意图，出现时天气工具尚未真正执行。
  - 正确识别工具返回结果叫 `Observation`；“由客户端执行的工具产生”已抓住工具是真实结果来源这一主线。
- 当前缺口：第 3 句的主语存在歧义。需要明确区分“谁把 Observation 追加到历史”和“谁在下一轮读取历史并据此判断”。`Thought` 不能被表述成回填动作的执行者。
- 提示 / 订正路径：提示 1——把第 3 句拆成两个动作，分别填主语：`___ 回写 Observation`；`___ 读取包含 Observation 的历史并继续判断`。
- 修订作答：

  > 客户端把 Observation 写入历史；下一轮LLM读取这段历史，再判断目标是否满足

- 订正结论：已准确分开客户端的回填职责与 LLM 的读取/判断职责。
- 单题状态：PARTIAL → PASS。

### G2 — 4.2.2 工具的定义与实现

- 已带读范围：Hello-Agents 4.2.2 全节，包括 SerpApi 依赖与密钥、工具三要素、`search()` 的结果解析、`ToolExecutor` 注册/展示/查找，以及手动调用测试。
- 核心目标：说明工具的 Name / Description / Execution Logic 各自作用；沿 `registerTool → getAvailableTools → getTool → function(input) → Observation` 追踪一次调用。
- 与已学内容的边界：T3 已证明客户端是真实执行者；本节只补 ReAct 文本轨迹如何把工具菜单和本地函数注册表接入循环，不重复考原生 `tool_calls` 数量分层。
- 即时考察：给定 `Weather` 工具的注册信息和一次 `Action`，判断 Name、Description、Execution Logic 分别在哪一步发挥作用，以及工具返回值进入哪个槽位。
- 原始作答：

  > 1.Description帮助LLM判断是否应该选择天气工具， 具体是"查询指定城市的实时天气和空气质量，输入为城市名"
  > 2.客户端根据Weather找到get_weather
  > 3.get_weather是方法名，是访问天气的API
  > 4.get_weather("Singapore") 的返回值进入 ReAct 的Observation

- 做对的部分：准确对应了 Description、Name 和 Observation；也找到了 `get_weather` 是实际执行入口。
- 当前缺口：第 3 点把本地 `get_weather` 工具函数与它所调用的外部天气 API 混成同一个对象。当前示例中 `get_weather` 是注册到客户端的 Execution Logic；外部 API 是它内部访问的远程服务。
- 提示 / 订正路径：提示 1——只改第 3 句，同时说清“`get_weather` 是什么”和“它内部调用什么”。
- 修订作答：

  > get_weather 是函数，它内部会去调用外部的天气API

- 订正结论：已准确分开本地工具函数和外部天气 API。
- 单题状态：PARTIAL → PASS。

### G3 — 4.2.3（1）提示词设计 +（2）核心循环

- 已带读范围：Hello-Agents 4.2.3 的（1）与（2），并对照 `repos/hello-agents/code/chapter4/ReAct.py` 的模板、构造函数和 `run()` 前半段。
- 范围声明：这是 4.2.3 的前半个清晰片段，不宣称完成整节；输出解析、Finish/工具执行、Observation 回填和运行实例留到后续片段。
- 核心目标：说明 `{tools}` / `{question}` / `{history}` 如何形成每轮输入；解释 `self.history = []`、`current_step`、`while current_step < self.max_steps` 和空响应 `break` 的作用。
- 读码校准：教材称其为“系统提示词”，但参考代码实际把完整模板放进 `{"role": "user"}`；它承担控制指令作用，却不是 API 消息层的 `system` 角色。
- 即时考察：给定 `max_steps=2` 且对象中残留旧 history，预测 `run()` 开始后的历史、首轮 prompt 的动态内容，以及未完成两轮后能否发起第三次 LLM 调用。
- 原始作答：

  > 1.不在，会清空旧的 Observation
  > 2.tools：getAvailableTools，question：新加坡今天的天气如何？，history里暂时只有系统提示词
  > 3.不会发起第三次LLM调用，第三次current_step=2，不满足小于max_steps=2的条件，不会进入循环了

- 做对的部分：正确判断 `run()` 会清空旧轨迹；正确判断两轮后 `current_step` 保持为 2，下一次条件检查 `2 < 2` 为假，因此不会发起第三次 LLM 调用。
- 当前缺口：第 2 点写的是部分“生成来源”，不是三个占位符此刻真正装入的值；并且把模板正文误算进 `{history}`。`getAvailableTools()` 的返回字符串才进入 `{tools}`；模板中的控制指令位于 `{history}` 外部。
- 提示 / 订正路径：提示 1——只重写第 2 点，分别填写第一次 `.format()` 时 `{tools}`、`{question}`、`{history}` 的实际内容。注意先执行了 `self.history = []`，再执行 `"\n".join(self.history)`。
- 第一次修订与追问：

  > system提示词不是会一直在history里面吗？第一次调用 LLM 时,{tools} =getAvailableTools()，返回工具菜单的字符串。{question} =新加坡今天的天气如何？。{history} =[]。

- 本轮校准：
  - `{tools}` 与 `{question}` 已修正准确。
  - 该参考实现每轮重新构造完整模板，所以固定控制指令会被再次发送，但没有存进 `self.history`；`self.history` 只保存后续代码追加的 Action / Observation。
  - `self.history` 在 Python 中是空列表 `[]`；随后 `history_str = "\n".join(self.history)` 得到空字符串 `""`，`.format(history=history_str)` 最终把空字符串填入 `{history}`。
- 提示 / 订正路径：提示 2——只回答两个变量的实际值：`self.history = ?`；第一次 prompt 中 `{history} = ?`。
- 第二次修订：

  > self.history =[],第一次 prompt 中的 {history} =[]

- 当前缺口：`self.history` 已答对，但 `{history}` 仍写成了列表；实际传给 `.format()` 的是 `history_str`。
- 提示 3 / 最小执行验证：

  ```python
  items = []
  history_str = "\n".join(items)
  print(repr(items), type(items).__name__)
  print(repr(history_str), type(history_str).__name__)
  ```

  实际结果：`[] list` 与 `'' str`；渲染后的片段是 `'History: '`。
- 最终修订：

  > 第一次 prompt 中的 {history} = ''，类型是 str

- 订正结论：已分清 `self.history` 的空列表状态与 `.join()` 后填入 prompt 的空字符串。
- 单题状态：PARTIAL → PASS（经提示 3）。

### G4 — 4.2.3（3）输出解析 +（4）工具执行 +（5）Observation 整合

- 已带读范围：Hello-Agents 4.2.3 的（3）–（5），并对照 `ReAct.py` 中 `_parse_output()`、`_parse_action()`、`_parse_action_input()` 及 `run()` 的 Finish/执行/回填分支。
- 范围声明：本片段完成 ReAct 核心闭环代码；4.2.3（6）运行实例尚未带读。
- 核心目标：从 LLM 文本提取 Thought/Action；区分 `Finish[...]` 与工具 Action；追踪工具查找、真实执行、错误 Observation，以及成功后写入 history 的两行内容。
- 参考代码校准：教材片段直接用正则提取 Finish；仓库实际代码调用 `_parse_action_input()`。无效工具格式会追加一条错误 Observation 后 `continue`；找不到工具名会把错误字符串作为 Observation。
- 即时考察：沿 `Weather[Singapore] → 工具结果 → history → Finish[...]` 追踪两轮，说明解析值、执行者、回填内容与停止行为。
- 原始作答：

  > 1.第一轮 _parse_output() 得到的 thought是`我需要查询实时天气。`，action是`Weather[Singapore]`
  > 2._parse_action() 得到的 tool_name是Weather，tool_input是Singapore
  > 3.客户端执行get_weather，第一轮结束后，self.history 中会精确新增`Weather[Singapore]`和工具返回的`"34°C, AQI=160"`
  > 4.第二轮不会调用工具了，run() 返回`今天不适合户外跑步。`，这条 Finish不会进入 self.history

- 做对的部分：Thought/Action、工具名/输入、客户端执行者、Finish 停止与返回值均判断正确；也正确指出 Finish 不进入该实现的 history。
- 当前缺口：第 3 点写出了两个字段的值，但没有写出 `append()` 实际保存的完整字符串，漏掉 `Action: ` 与 `Observation: ` 前缀。
- 提示 / 订正路径：提示 1——只改第 3 点，逐字套用两行代码：`f"Action: {action}"` 与 `f"Observation: {observation}"`。
- 第一次修订：

  > 第一条：Action: Weather[Singapore]
  > 第二条：Observation: "34°C, AQI=160"

- 本轮校准：第一条完全正确；第二条额外加入了双引号。示例里的 `"34°C, AQI=160"` 是 Python 源码中的字符串字面量，双引号是定界符，不属于函数返回值的运行时内容；f-string 插入的是内容本身。该边界与既有 `WP-16` 的“定界符不属于数据内容”语义相关，但本题不额外增加复习题量。
- 提示 / 订正路径：提示 2——只重写第二条，保留 `Observation: ` 前缀，去掉不属于运行时值的定界符。
- 第二次修订：

  > 第二条：Observation:34°C, AQI=160

- 最终校准：前缀与运行时内容均已正确；参考代码的 f-string 在冒号后还包含一个空格，因此逐字结果是 `Observation: 34°C, AQI=160`。这只是排版精度，不构成概念缺口，不再重复追问。
- 单题状态：PARTIAL → PASS（附空格精度校准）。

### G5 — 4.2.3（6）运行实例与“是否该 Finish”

- 已带读范围：Hello-Agents 4.2.3（6）完整运行实例，包括首轮 Search、搜索 Observation、第二轮 Thought 与 Finish。
- 核心目标：区分“程序因 `Finish[...]` 正常返回”和“现有证据已经充分满足用户目标”；识别 Thought 承认证据不足却立即 Finish 的过早停止。
- 资料时效说明：示例运行日期为 2025-09-08，其中手机型号只作为轨迹样本，不作为 2026 年的当前事实记忆。
- 即时考察：从代码控制流看，这次是否正常停止？从任务质量看，是否应该停止？为什么？如果继续，下一步应发出什么类型的 Action（不用写当前具体手机答案）？
- 原始作答：

  > 1.从代码控制流看，这次是正常停止
  > 2.从任务质量看，此时不应该停止
  > 3.我感觉应该调用外部API去查询哪部手机是最近发布的

- 做对的部分：准确区分了代码层正常停止与任务层证据不足；也能把“最新”转换为“比较发布时间”这一下一步信息需求。
- 精度校准：在当前 ReAct 接口中，LLM 不直接调用任意外部 API，而应发出一个已注册工具的 Action，例如更有针对性的 `Search[...]`；客户端执行该工具，工具函数内部才可能访问外部 API。若要直接使用专门的产品 API，需要先把它注册成可用工具。
- 订正结论：停止条件与下一步取证方向均已掌握，工具接口措辞经校准即可。
- 提示使用：无。
- 单题状态：PASS。

### G6 — 4.2.4 ReAct 的特点、局限性与调试技巧

- 已带读范围：Hello-Agents 4.2.4 全节，包括 3 个主要特点、4 个固有局限与 5 类调试手段。
- 核心目标：从完整轨迹说明 ReAct 为什么可观察、能逐步纠错并能协同工具；识别多轮串行调用、格式依赖和局部循环风险；按“完整 prompt → 原始输出 → 工具输入/输出”定位故障，并用 `max_steps` 提供硬性循环上限。
- 已有证据去重：`max_steps` 的硬性循环上限已由 G3 的“两轮后不能发起第三次调用”证明，本题只补新的故障定位能力，不重复考察。
- 即时考察：某 ReAct Agent 连续输出错误格式 `Action: Weather Singapore`，解析器始终取不到合法 Action。请回答：先检查哪两份原始材料来区分是模型没按格式输出还是解析器写错？若完整 prompt 已写了格式规则，但模型仍频繁漏掉方括号，可以向 prompt 加入什么内容？如果原始输出其实已经是合法的 `Action: Weather[Singapore]`，却仍然解析失败，应优先调试哪一层？
- 原始作答：

  > 1.应先检查完整 prompt和LLM 原始输出。prompt查看提示格式有错误没有，LLM 原始输出看是否按prompt的格式输出action
  > 2.加入多样本示例(Few-shot)
  > 3.调试解析器层

- 做对的部分：检查材料、两层故障归因与 Few-shot 修复方向全部准确；能够先看证据，再判断是模型没有遵循协议还是解析器没有正确实现协议。
- 精度补充：Few-shot 中应放入一到两个完整、格式正确的成功轨迹，而不只是继续堆叠抽象规则。
- 提示使用：无。
- 单题状态：PASS。

### G7 — Agentic Design Patterns Chapter 5：Tool Use 选读

- 选读范围：双语版 Chapter 5 的 `Tool Use Pattern Overview`、`Practical Applications & Use Cases`、`At a Glance`、`Key Takeaways` 与 `Conclusion`；不展开 LangChain、CrewAI、Google ADK 的框架专属代码示例，因为 A4-03 当前目标是掌握 ReAct/工具闭环并完成最小代码产物，不是学习三个框架的 API。
- 核心目标：理解 Tool Use 是比 Function Calling 更宽的架构模式；把结构化函数调用的 6 步闭环映射到已经学过的 ReAct；识别“结构化调用请求”仍只是执行意图，不是工具结果。
- 新增边界：Function Calling 通常指模型生成函数名与参数的结构化请求；Tool Use 还可以涵盖 API、数据库查询、代码执行、设备控制，甚至把任务委派给另一个专用 Agent。
- 即时考察：模型返回 `{"name":"get_inventory","arguments":{"product_id":"SKU-7"}}`。请说明：①它是库存结果还是结构化调用请求；②谁负责真正执行；③函数返回 `{"stock":0}` 后，这个值先回到哪里；④为什么不能把 Tool Use 仅仅理解成调用普通 Python 函数？
- 原始作答：

  > 1.构化调用请求
  > 2.客户端负责真正执行
  > 3.回到Observation
  > 4.Tool Use不止包含调用Python函数，还可能包括外部API，查询数据库等请求

- 做对的部分：正确识别结构化调用请求、客户端执行者与 Observation 回填位置，也能把 Tool Use 扩展到 API 和数据库等外部能力。
- 精度补充：第 1 点按语义应为“结构化调用请求”；`{"stock":0}` 是工具结果，先作为工具结果/Observation 回到 Agent 上下文，再由下一轮 LLM 处理。
- 提示使用：无。
- 单题状态：PASS。

### C1 — 代码产物：解析一轮 Thought / Action

- 目标文件：`code/stage4/a4_03_react_agent.py`。
- 骨架状态：目标文件原先不存在；现已按项目规则只创建当前硬检查点所需的模块说明、`re` import、样例文本和 `parse_llm_output()` TODO，未写可用答案。
- 当前要求：实现 `parse_llm_output(text)`，支持 Thought 跨行、在 `Action:` 前停止；Action 读取到文本末尾；两者去除首尾空白；缺失字段返回 `None`。
- 最小验收：样例应得到 `("我需要查询实时天气。", "Weather[Singapore]")`；另需验证多行 Thought 和缺少 Action 两种输入。
- 用户反馈：

  > 没有什么思路，带我写

- 现场代码状态：学习者已移除原骨架末尾的 `raise NotImplementedError`，函数体尚未加入解析或返回逻辑；当前调用会隐式返回 `None`，暂不据此判错。
- 拆分检查点 C1a：先只构造 `thought_match`，再从捕获组得到 `thought`；暂不处理 Action。
- 提示 1：把 `Thought:` 当起点、换行后的 `Action:` 当停止标志；需要 `re.DOTALL` 让 `.` 可以跨行，并用非贪婪捕获避免吃掉 Action。
- 提示 2 / 带写起步：给出仅负责定位 Thought 捕获区间的 `re.search(...)` 语句，并逐段解释 `\s*`、`(.*?)`、正向预查与 `re.DOTALL`；学习者只需自行补全“匹配成功取第 1 捕获组并 strip，否则为 None”的赋值语句。
- C1a 作答：

  > thought =thought_match.group(1).strip() if thought_match else None

- C1a 结论：赋值逻辑完全正确；匹配存在时读取第 1 捕获组并清理空白，不存在时返回 `None`。
- 文件复核：学习者实际上已进一步独立补全 `thought_pattern`、`action_pattern`、两次 `re.search`、两个字段取值和元组返回。
- 直接验证：普通样例、多行 Thought、缺少 Action 三类约定输入全部通过（`3/3 required cases passed`）。
- 非阻塞加固点：额外输入在 Thought 正文中包含字面量 `Action:` 时，当前未锚定字段行的正则会提前截断，并把后续两段一起当成 Action；该案例不在 C1 既定验收内，暂不因此否定 C1，后续解析加固时处理。
- 检查点状态：PASS（使用提示 2；既定验收 3/3）。

### C2 — 代码产物：解析 Tool[input]

- 骨架状态：C1 通过后已移除旧 TODO，并在同一目标文件新增当前唯一未完成骨架 `parse_action(action)`。
- 当前要求：把完整的 `名称[输入]` 拆为 `(tool_name, tool_input)`；工具名只接受字母、数字或下划线；无效结构返回 `(None, None)`。
- 最小验收：`Weather[Singapore] → ("Weather", "Singapore")`；`Finish[今天不跑步] → ("Finish", "今天不跑步")`；`Weather Singapore → (None, None)`。
- 捕获组预测作答：

  > 1.group(1) 是Weather
  > 2.group(2) 是Singapore
  > 3.Weather Singapore 不能否匹配成功，不是规定的格式，解析不出来匹配的函数名

- 预测结论：三个判断均正确；第 3 点精确说法是整个模式匹配失败，因此两个捕获组都不存在。
- 文件复核：学习者已自行补全 `pattern`、匹配、两个捕获组取值和失败返回分支。
- 直接验证：三个基础案例全部通过；新增的完整结构边界 `Weather[Singapore]trailing-junk` 失败，当前 `re.match` 只保证从字符串开头匹配，仍会忽略结尾垃圾。
- 提示 1：将匹配 API 改为检查整个字符串的 `re.fullmatch`；同时传入 `re.DOTALL`，使括号内输入需要跨行时仍可作为一个完整 Action 处理。
- 修订作答：

  > match = re.fullmatch(pattern,action,re.DOTALL)

- 修订验证：基础有效 Action、Finish、无效空格格式、尾部垃圾、多行输入共 `5/5 C2 cases passed`。
- 检查点状态：PARTIAL → PASS（经提示 1）。

### C3 — 代码产物：构造每轮 ReAct Prompt

- 去重依据：T3-Gate 已正式 PASS，工具注册、白名单查找和客户端执行不重复出题；本检查点只练 ReAct 新增的“每轮把轨迹重新放入 prompt”。
- 骨架状态：C2 通过后已移除旧 TODO，新增给定的 `REACT_PROMPT_TEMPLATE` 与当前唯一未完成函数 `build_prompt(question, tools_description, history)`。
- 当前要求：用 `"\n".join(history)` 生成字符串，再把 `tools`、`question`、`history` 三个值填入模板；不得修改原列表。
- 最小验收：空历史时 `History:` 后为空；两条历史按原顺序、以单个换行连接；工具菜单和问题必须原样出现。
- 用户实现：

  ```python
  history_text = "\n".join(history)  # 将历史记录列表连接为字符串
  return REACT_PROMPT_TEMPLATE.format(
      tools=tools_description,
      question=question,
      history=history_text,
  )
  ```

- 文件复核：学习者在文件中使用等价变量名 `history_str` 与中间变量 `prompt`，返回内容语义一致。
- 直接验证：空历史、两条有序历史、工具/问题原样出现且不修改原列表，共 `3/3 C3 checks passed; history unchanged`。
- 检查点状态：PASS。

### C4 — 代码产物：ReAct 主循环

- 复用边界：给定本地 `Weather` 工具、静态工具描述与 `TOOLS` 注册表；这些是 T3 已 PASS 的能力，本任务不重复验收。`llm_call` 采用依赖注入，先用内存假响应稳定验证轨迹，之后再接已经验证过的 DeepSeek 客户端。
- 最终要求：新运行使用空 history；每轮构造 prompt、调用 LLM、解析 Thought/Action；处理 Finish 或执行工具；回填完整 Action/Observation；最多调用 `max_steps` 轮并打印轨迹。
- C4a 当前要求：只写循环前半段——`history = []`，按 `1..max_steps` 循环，每轮调用 `build_prompt(...)`、`llm_call(prompt)`、`parse_llm_output(...)`，打印步数和非空 Thought；工具与 Finish 分支下一小步再写。
- C4a 用户实现：正确创建空 history，使用 `range(1, max_steps + 1)`，并依次连接 `build_prompt → llm_call → parse_llm_output → Thought 打印`。
- 文件协作记录：聊天提交与用户本地保存同时发生，短暂形成两份相同 C4a；已只删除重复副本，保留学习者逻辑与后续 TODO。首次导入时的 `NameError` 来自该并发重复/缩进状态，不归因于用户的数据流答案。
- C4a 直接验证：注入内存假 LLM、`max_steps=2`，得到 2 次 LLM 调用、2 条解析后的 Thought，且 C4b/C4c 的 `NotImplementedError` 仍按预期保留。中文断言经 PowerShell 管道出现编码差异后改用 ASCII 标记复验通过。
- C4a 检查点状态：PASS。
- C4b1 当前要求：仍在循环内部；没有 Action 时打印原因并返回 `None`；否则调用 `parse_action(action)`；若工具名为 `Finish`，打印最终答案并返回 `tool_input`。本小步不执行 Weather，也不回填 history。
- C4b1 用户实现：缺失 Action 分支返回 `None`；调用 `parse_action(action)`；`tool_name == "Finish"` 时打印并返回 `tool_input`。
- C4b1 直接验证：缺失 Action 稳定停止；Finish 返回 `final answer`；两条路径的工具调用次数均为 0。
- C4b1 检查点状态：PASS。
- C4b2 当前要求：若 `tool_name` 或 `tool_input` 无效，生成“无效 Action” Observation；否则从 `TOOLS` 查找函数，存在则执行 `tool_function(tool_input)`，不存在则生成“未找到工具” Observation；最后打印 Action 与 Observation。本小步暂不追加 history。
- C4b2 用户实现：正确覆盖无效格式、`TOOLS.get(tool_name)`、存在时调用 `tool_function(tool_input)`、不存在时错误 Observation，以及 Action/Observation 打印。
- C4b2 直接验证：有效 Weather 工具仅执行 1 次且收到 `Singapore`；未知 Search 与无效 `Weather Singapore` 均未误调用工具并成功生成 Observation。
- C4b2 检查点状态：PASS。
- C4c 当前要求：在每轮工具/错误 Observation 产生后，依次向 history 追加精确字符串 `Action: {action}` 与 `Observation: {observation}`；循环自然耗尽后打印最大步数原因并返回 `None`，替换暂留的 `NotImplementedError`。
- C4c 用户实现：在每轮 Observation 打印后依次追加 `Action: {action}` 与 `Observation: {observation}`；循环自然耗尽后打印最大步数原因并返回 `None`。
- C4c 直接验证 1：注入两轮内存假 LLM，第一轮执行 `Weather[Singapore]`，第二轮 prompt 精确包含 `Action: Weather[Singapore]` 与 `Observation: 34°C, AQI=160`；随后 `Finish[今天不适合户外跑步。]` 正确返回最终答案，Weather 工具仅调用 1 次。
- C4c 直接验证 2：注入持续请求 Weather 的假 LLM，并设 `max_steps=2`；实际恰好发生 2 次 LLM 调用与 2 次工具调用，随后稳定打印最大步数原因并返回 `None`，未进入第三轮。
- 代码卫生：`git diff --check` 通过。
- C4c 检查点状态：PASS。
- C4 主循环状态：PASS（C4a、C4b1、C4b2、C4c 均通过）。

### C5 — 可直接运行的确定性 ReAct 演示

- 缺口验证：直接执行 `.venv/Scripts/python.exe code/stage4/a4_03_react_agent.py` 时退出码为 0，但输出行数为 0；原因是文件只有函数定义，尚无演示 LLM 与脚本入口。
- 去重边界：真实 DeepSeek SDK/客户端调用已在 T3-Gate 正式通过，本检查点不重复考察；使用依赖注入的确定性 `demo_llm`，只验证 A4-03 的 ReAct 控制循环与可运行产物。
- C5a 当前要求：实现 `demo_llm(prompt)`。当 prompt 尚未包含天气 Observation 时，返回严格的两行 `Thought` + `Action: Weather[Singapore]`；当 prompt 已包含 `Observation: 34°C, AQI=160` 时，返回严格的两行 `Thought` + `Action: Finish[今天不适合户外跑步。]`。
- C5a 用户实现：使用完整字符串 `Observation: 34°C, AQI=160` 判断是否已有工具结果；首轮返回 `Weather[Singapore]`，次轮返回 `Finish[新加坡的实时天气是 34°C，空气质量指数为 160。]`。
- 语义校准：最终答案没有照抄最初的“是否适合户外跑步”示例，而是准确回答“新加坡天气如何”；后续直接运行入口将使用与该答案匹配的天气问题，因此不构成错误。
- C5a 直接验证：无历史分支、含 Observation 的 Finish 分支、`parse_llm_output → parse_action` 协议，以及接入 `run_react` 后恰好两轮完成的完整集成均通过；`py_compile` 通过。
- C5a 检查点状态：PASS。
- C5b 当前要求：在 `if __name__ == "__main__":` 下调用 `run_react`，问题使用“新加坡今天的天气如何？”，注入 `demo_llm`，并设置 `max_steps=3`；移除暂留的 `NotImplementedError`。
- C5b 用户实现：在 `__main__` 分支调用一次 `run_react`，正确传入天气问题、`demo_llm` 与 `max_steps=3`，未修改其他函数。
- C5b 直接验证：按文件顶部命令直接执行，退出码为 0；轨迹恰好两轮，第一轮打印 `Weather[Singapore]` 与 `Observation: 34°C, AQI=160`，第二轮打印 Finish 最终答案，未触发最大步数提示。
- 代码卫生：`py_compile` 与 `git diff --check` 均通过。
- C5b 检查点状态：PASS。
- C5 可运行演示状态：PASS（C5a、C5b 均通过）。

## 当前覆盖与剩余范围

- 合格证据数：7（G1–G7 PASS）。
- 已完成资料范围：Hello-Agents 4.2 ReAct 全节；ADP Chapter 5 与 A4-03 直接相关的概述、用例、速览、关键要点与结论选读。框架专属示例明确不在本任务范围。
- 已完成代码范围：C1–C5；目标文件可直接运行，完整轨迹、Observation 回填、Finish 与最大步数保护均有可执行证据。
- 剩余范围：正式练习尚未开始；A4-03 的任务级正式判定也尚未执行，因此 `progress` 继续保持 `DOING`。
- 笔记状态：本任务 tracker 产物为代码 demo，未要求独立项目 notes；带读与订正证据已完整保存在本 daily。
- 下一单一动作：用户发出固定口令“开始今天的练习”，由正式练习流程读取 G1–G7 与 C1–C5 证据，只补尚未覆盖的迁移/综合检查，不重复已通过题目。

## 正式练习

### 学习定位

- 为什么学这个：ReAct 是阶段 4 中把 LLM 判断、客户端工具执行和可观察轨迹连接成循环的基础模式。
- 学完能搞懂 / 能做什么：能够判断一次停止是否既符合代码控制流又真正满足目标，并能在真实代码中定位协议解析与停止校验缺陷。
- 对接下来的影响：这是 A4-04 Planning、A4-05 Reflection 与 A4-Gate 中轨迹排错、安全停止的直接前置。

### 出题预检与语义去重

- 当前事实：A4-03 仍为 `DOING`；G1–G7 与 C1–C5 教学检查点均已通过，目标文件可直接运行并已验证完整两轮轨迹与 `max_steps`。
- 已覆盖、排除重复：Thought/Action/Observation 基本职责、客户端回填主语、空 history、完整历史字符串、工具注册/执行、正常 Finish、代码停止与目标满足的初次区分、两轮步数上限、原始 prompt/输出/工具结果调试顺序、Tool Use 与 Function Calling 边界。
- 尚需正式迁移：空 `Finish` 的停止校验；当前解析器在字段标签进入 Thought 正文时的真实故障定位与修复；确定性假 LLM 能证明与不能证明的测试边界。
- 题量：计划 3 个正式高价值检查点；若全部形成合格证据，当天总有效考察为 10（G1–G7 + F1–F3），不额外制造回忆题。
- 旧复习目标：选择到期 `WP-12`“代码兜底 ≠ 约束模型”，语义合并进 F1；`WP-15` / `WP-17` 已被 G1/G7 覆盖，其余到期项与当前任务无直接关系，均不叠加。
- 工作场景：F2 绑定当前真实代码与安全注入的解析故障；A4-Gate 才正式引入的日志/指标场景不提前升级证据等级。
- 算法：W4 仍有 4 道新题欠账，但 daily/weekly 明确要求独立算法时段替换主块，今天不追加。

### 复习回炉

- ID / 类型：`WP-12` / 到期稳定错题。
- 合并位置：F1（空 `Finish[]` 的 prompt 软约束与客户端硬校验）。
- 原始答案 / 执行证据：用户独立说明“prompt 是软校验，模型可能不严格按 prompt 生成，需要客户端做硬校验”。
- 订正路径与单项结果：无需提示即准确区分 prompt 的事前软约束与客户端的事后硬校验；`WP-12` 本身判为 PASS。F1 的代码顺序错误另按当前任务检查点订正，不回写成 WP-12 失败。
- 建议台账去向：建议正式复核者按稳定错题复测成功处理；具体间隔与日期由正式复核者决定。

### F1 — 空 Finish 的停止校验（D：应用迁移 + 为什么）

- 故障输入：首轮假 LLM 返回 `Thought: 信息不足\nAction: Finish[]`。
- 已核实症状：当前 `run_react` 打印空的 `Final Answer:`，立即返回空字符串；不执行工具，也不触发最大步数保护。
- 作答要求：定位导致该结果的具体分支与检查顺序；写出不修改 `parse_action` 的最小 Finish 分支，使非空答案正常返回、空答案进入无效 Action Observation 并继续循环；解释为什么 prompt 中写了 `Finish[最终答案]` 仍不能代替客户端校验。
- 原始作答：

  > 1.因为tool_name匹配上了，直接输出结果了
  > 2.if not tool_name or not tool_input:
  >     observation = "无效的 Action 格式。
  > if tool_name == "Finish":
  >     print(f"Final Answer: {tool_input}")
  >     return tool_input
  > 3.prompt是软校验，模型可能并不是严格按照prompt生成的，这时候需要客户端去做硬校验

- 做对的部分：第 1 点识别到 `tool_name == "Finish"` 会直接返回；第 3 点准确解释 prompt 只能软约束模型，客户端仍需硬校验，合并复习的 `WP-12` 已独立通过。
- 当前缺口：第 2 点把无效检查移到前面，但仍使用两个相互独立的 `if`。空输入进入第一个分支后只给 `observation` 赋值，随后仍会进入 `tool_name == "Finish"` 分支并返回空字符串，因此故障没有修复。错误字符串还少一个闭合引号，但主要缺口是控制流。
- 提示 1：保留现有分支顺序，只看 `if tool_name == "Finish":` 这一行。它现在只检查工具名；请思考还必须满足哪个条件，才能允许真正返回最终答案。只需重写这一行条件。
- 第一次订正：

  > if tool_name == "Finish" and not tool_input :

- 第一次订正缺口：布尔方向相反。空字符串的 `not tool_input` 为 `True`，因此该条件仍会在答案为空时进入 Finish 返回分支；有内容时反而为 `False`。
- 提示 2：条件需要在 `tool_input` 有内容时为真、为空时为假；请使用正向真值判断，不要使用 `not`。
- 第二次订正：

  > if tool_name == "Finish" and tool_input :

- 订正结论：条件方向正确；只有非空答案能进入 Finish 返回分支，空输入会落入后续 `if not tool_name or not tool_input`，生成错误 Observation 并继续循环。
- 文件同步：已把学习者亲自写出的条件应用到 `code/stage4/a4_03_react_agent.py`，未代写其他逻辑。
- 直接验证：持续返回 `Finish[]` 且 `max_steps=2` 时，LLM 恰好调用 2 次、两轮均生成“无效的 Action 格式” Observation、没有打印 Final Answer，最终返回 `None`；非空 `Finish[done]` 仍正常返回，原天气 demo 回归通过。
- 代码卫生：`py_compile` 与 `git diff --check` 通过。
- 单题状态：PARTIAL → PASS（经提示 2）。

### F2 — 字段标签进入 Thought 正文时的解析故障（C：真实代码调试）

- 故障输入：`Thought: 我在说明 Action: 只是调用意图。\nAction: Weather[Singapore]`。
- 已核实症状：当前 `parse_llm_output` 得到 `thought="我在说明"`，并把 `action` 错取为 `只是调用意图。\nAction: Weather[Singapore]`；随后 `parse_action` 返回 `(None, None)`。
- 作答要求：结合当前两条正则解释误判根因；利用“`re.MULTILINE` 让 `^` 匹配每一行开头”这一给定信息，修改实际文件，使字段标签只有出现在行首时才被识别，同时保留多行 Thought 与 Action 读取到末尾的既有能力；完成后说明改了哪些模式/flags。
- 原始实现：学习者把两次 `re.search` 的 flags 从 `re.DOTALL` 改为 `re.MULTILINE | re.DOTALL`，但 `thought_pattern` 与 `action_pattern` 保持原样，未加入行首锚点。
- 做对的部分：正确加入了题目给定的 `re.MULTILINE`，并保留 `re.DOTALL`，没有破坏“`.` 跨行”的既有能力。
- 当前缺口：`MULTILINE` 只改变 `^` / `$` 的含义，不会自动把普通的 `Thought:` / `Action:` 限制到行首；两条 pattern 没有 `^`，因此故障输入仍得到 `thought="我在说明"` 和错误的多行 action，`parse_action` 仍失败。
- 直接复现：重新导入实际文件并运行原故障输入，错误结果与修复前完全一致（`CONFIRMED: fault still reproduces`）。
- 提示 1：pattern 中共有三个“字段标签位置”需要行首约束——Thought 起始标签、Thought 停止预查里的 Action 标签、Action 提取起始标签。另因 `MULTILINE` 会让 `$` 也匹配每一行结尾，为保留多行 Thought，真正的整段文本结尾应使用 `\Z`，不要继续用 `$`。
- 单题状态：PARTIAL，等待修改两条 pattern；现有 flags 可以保留。
- 学习者反馈：`我不会正则表达式`。据此确认原 F2 直接要求组合行首锚点、预查、跨行与整段结尾，超出了已验证前置；暂停整题，不把缺失前置当成新的独立错误。
- 文件再检查：学习者另尝试把 Thought 停止位置改为 `(?=\nAction:|$)`，说明已在尝试用换行区分字段；但 `action_pattern` 仍未锚定，且 `MULTILINE` 下 `$` 会匹配每一行结尾，因此尚未满足多行 Thought 与行首字段约束。
- 前置拆分 R1：只讲 `^` 在 `re.MULTILINE` 下表示“每一行的开头”，并让学习者补全 Action 字段的行首锚点；暂不要求理解或改写完整 Thought pattern。
- R1 原始作答：

  > action_pattern = r"^Action:\s*(.*)"

- R1 结论：PASS。能把 `^` 放到字段标签前，使 `Action:` 只有位于行首时才作为字段起点；该学习者代码已原样同步到目标文件。
- 前置拆分 R2：区分 `MULTILINE` 下的 `$`（一行结尾）与 `\Z`（整段文本结尾），只补 Thought 停止预查中的“全文结束”符号。
- R2 第一次作答：

  > (?=^Action:$|\Z)

- 做对的部分：已把“整段文本真正结束”写成 `\Z`。
- 当前缺口：在 `^Action:` 后额外加入 `$`，会要求 Action 标签所在行在冒号后立刻结束；实际字段是 `Action: Weather[Singapore]`，冒号后还有内容，因此左侧条件无法命中。
- R2 提示 1：左侧只需识别“行首出现 `Action:` 前缀”，不要要求它同时位于行尾；保留右侧 `\Z`，删除左侧多余的行尾限制后重写整个预查。
- R2 订正作答：

  > (?=^Action:|\Z)

- R2 结论：PARTIAL → PASS（经提示 1）。能区分“Action 行首前缀”与“整段文本结束”，不再误用 `$` 限制 Action 行必须在冒号后结束。
- 前置拆分 R3：不引入新符号；按语义顺序拼接 `^Thought:`、标签后的可选空白 `\s*`、非贪婪捕获 `(.*?)`、Action 前可选空白 `\s*`、以及已通过的停止预查 `(?=^Action:|\Z)`，形成完整 `thought_pattern`。
- R3 第一次作答：

  > thought_pattern = r"^?=Action:\s*(.*?):|\Z)"

- 当前缺口：片段顺序被打乱；字符串开头误写成 Action 停止条件，漏掉 `Thought:` 字段标签，`(?=...)` 的括号结构也被拆开。说明逐个符号已有局部认识，但尚不能独立组合整条 pattern。
- R3 提示 4（半成品）：固定前四块为 `^Thought:\s*(.*?)\s*`，只让学习者把已经通过的完整停止预查填到末尾，不再要求一次自行组织五块。
- R3 第二次作答：

  > thought_pattern = r"^Thought:\s*(.*?)\s*|\Z"

- 第二次缺口：仍未把已通过的停止预查 `(?=^Action:|\Z)` 作为完整整体填入，只留下 alternation 的尾部 `|\Z`；这会把整条正则拆成两个独立选项，不能表达“捕获 Thought 后在 Action 前停止”。
- 提示阶梯结论：已完成自主尝试、逐符号提示、片段拼装与半成品填空，仍无法组合；按规则进入完整答案支持。提供 `thought_pattern = r"^Thought:\s*(.*?)\s*(?=^Action:|\Z)"`，要求学习者原样替换，并用一个短问题确认 `(.*?)` 的捕获职责。
- 完整答案支持后的理解检查：

  > Thought: 我在说明 Action: 只是调用意图。最终会进入 group(1)

- 理解校准：回答正确；`(.*?)` 的第 1 捕获组会保留完整 Thought 正文，正文中的字面量 `Action:` 不再被当作字段边界。
- 文件复核：学习者已把给定完整 `thought_pattern` 原样替换进目标文件；此前独立写出的 `^Action:` pattern 与 `MULTILINE | DOTALL` flags 也保留正确。
- 直接验证：原故障输入现正确得到完整 Thought 与 `Weather[Singapore]`；普通样例、多行 Thought、缺少 Action 共 4 个解析案例通过；F1 空 Finish 防线与天气 demo 两条循环回归通过；`py_compile`、`git diff --check` 通过。
- F2 最终状态：RETRY（实际缺陷已在完整答案支持下修复且回归通过，但完整正则未独立构造，不能把绿色结果记为独立正式 PASS）。
- F2 补救题：使用带两个前导空格的 `  Action:` 作为换表面输入，只考 `^` 的行首语义和当前函数实际返回，不再要求组合正则；该补救不额外计入原计划 3 个正式检查点。
- 补救题预检：当前代码实测返回 `("ready\n  Action: Weather[Singapore]", None)`。
- F2 补救题原始作答：

  > 1.不会识别成Action 字段
  > 2.thought是精确的ready，action是None
  > 3.开头的空格被识别了，匹配的时候识别不到，返回None了

- 做对的部分：正确判断带前导空格的标签不满足 `^Action:`，因此 `action` 为 `None`；原因也抓住了 `^` 要求 A 必须是行首第一个字符。
- 当前缺口：未识别的文本不会被解析器自动丢弃。Thought 的停止预查找不到行首 Action 后，只能在 `\Z` 命中，因此 `group(1)` 会继续捕获换行和整条缩进的 Action 文本；精确 thought 应包含 `ready`、换行、两个空格和 `Action: Weather[Singapore]`。
- 补救题状态：PARTIAL，等待只订正第 2 点的精确元组。
- 补救题第二次作答：

  ```text
  (thought, action) = (ready
    Action: Weather[Singapore], None)
  ```

- 做对的部分：已准确保留 `ready` 后的换行、两个前导空格和整条未被识别的 `Action:` 文本，并继续判断 `action=None`；说明控制流追踪缺口已经补上。
- 当前仅剩缺口：题目要求的是精确 Python 元组，但 `ready...Singapore]` 没有字符串引号，因此这段写法不是合法 Python 表达式。需要把完整 Thought 值写成一个字符串，并在字符串中表示换行与两个空格。
- 补救题状态：PARTIAL，等待最后一次语法精确订正；不重新考解析原理。
