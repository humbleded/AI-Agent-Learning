# 每日学习打卡

日期：2026-06-15（周一）

学习时长：目标 ~2h（工作日，轻）

完成任务编号：L1-02 单轮问答（计划）

今日定位：

- 工作日轻量档：L1-02 是个小任务，争取一个晚上写完。
- 加班 fallback：如果加班累了，就只读 OpenAI Text Generation 文档 + 看一段 DLAI 对话格式小节，把写代码挪到周末或周三机动日，不硬写。
- 不碰 Gate（Gate 安排在周末）。

今日推荐资料（计划要读的，带链接；实际看了/读了填到下面两栏）：

- [OpenAI 文本生成 Text Generation](https://platform.openai.com/docs/guides/text)（消息、单轮请求；DeepSeek 接口一致）
- （可选视频）[DeepLearning.AI《Building Systems with the ChatGPT API》](https://www.deeplearning.ai/short-courses/building-systems-with-the-chatgpt-api/)：对话格式 / 消息组织

看了哪些视频/哪一集：

（做完后填你实际看的）

读了哪些文档/哪一节：

（做完后填你实际读的）

代码/产物位置：

- code/stage1/l1_02_ask.py

运行提示：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
.\.venv\Scripts\python.exe code\stage1\l1_02_ask.py
```

今日任务清单：

- [ ] 写 `l1_02_ask.py`：用户输入一个问题，模型回答一次
- [ ] 处理空输入
- [ ] 回答 L1-02 3 个检查题
- [ ] （加班则降档）只读文档 + 看视频，代码顺延

今日练习题（草稿示例：6/15 当天用 daily-practice-builder 按真实进度增删；锚定 L1-01 已过 → 做 L1-02，并复习 P0 弱点）：

A. 快速回忆

1. 在 `l1_01_first_call.py` 的 `call_model` 里，`messages` 列表中 `role="system"` 和 `role="user"` 这两条分别起什么作用？
2. 从 `client.chat.completions.create(...)` 拿到 `response` 后，要取出"模型回复的纯文本"，写出那一行完整表达式。（不是 `response` 本身）

B. 解释与比较

3. `l1_02_ask.py` 第一行是 `from l1_01_first_call import call_model`。为什么 L1-02 不自己重写一遍调用模型的代码，而是 import L1-01 的 `call_model`？这对"以后想换模型 / 换 base_url"有什么好处？
4. 对比两种写法：(a) `call_model` 里 `if not api_key: return "未设置..."`；(b) 不检查、直接调用。从"用户最终看到什么"的角度，说明 (a) 为什么比 (b) 好。（对应 L1-02：如何给用户显示错误提示）

C. 代码/操作

5. 预测输出：运行 `l1_02_ask.py`，在 `请输入问题：` 处直接按回车（空输入），程序会打印什么？为什么它不会去调用模型？（看 `l1_02_ask.py` 的代码作答）
6. 写出"在项目根目录、用 `.venv` 的 python 运行 `l1_02_ask.py`"的完整 PowerShell 命令。
7. `call_model` 里有 `load_dotenv()`。它具体做了什么？如果你的 key 是配在系统环境变量里（不在 `.env`），少了这行 `call_model` 还能取到 key 吗？为什么？

D. 应用与迁移

8. （接 6/13 的 P0-08 弱点）`call_model` 用 `except Exception as exc: return f"调用模型失败：{exc}"` 把异常变成可读文本——这和你在 P0-08 学的"`json.loads()` 解析失败要处理"是同一类思路。请说明：把模型返回的一段 JSON 字符串用 `json.loads()` 解析、但字符串格式坏了时，怎么写才不会让整个程序崩？
9. 迁移：假设哪天要把模型从 DeepSeek 换成另一家（只需改 `base_url` 和 `api_key`）。因为调用被封装在 `call_model` 这一个函数里，你只要改哪几行？`l1_02_ask.py` 需要跟着改吗？为什么？

E. 自评

10. 今天最不确定的概念是什么？
11. 上面哪一题你是猜的？

遇到的问题：

我最不确定的点：

希望 Codex 检查：

- 检查 `l1_02_ask.py` 是否能运行、能否处理空输入
- 批改 L1-02 检查题
- 判定 L1-02 是 PASS 还是 RETRY

Codex 判定：

Codex 当天进度总结：

Codex 总进度总结：

Codex 对问题/不确定点的解释：

Codex 问答点评与补充：

Codex 超前内容提示：

补救任务：
