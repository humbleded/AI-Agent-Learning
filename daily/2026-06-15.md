# 每日学习打卡

日期：2026-06-15（周一）

学习时长：目标 ~2h（工作日，轻）

完成任务编号：L1-02 单轮问答（计划）

今日定位：

- 工作日轻量档：L1-02 是个小任务，争取一个晚上写完。
- 加班 fallback：如果加班累了，就只读 OpenAI Text Generation 文档 + 看一段 DLAI 对话格式小节，把写代码挪到周末或周三机动日，不硬写。
- 不碰 Gate（Gate 安排在周末）。

今日推荐资料（计划要读的，带链接；实际看了/读了填到下面两栏）：

- [OpenAI 文本生成 Text Generation](https://platform.openai.com/docs/guides/text)（消息、单轮请求；DeepSeek 接口一致）
- （可选视频）[DeepLearning.AI《Building Systems with the ChatGPT API》](https://www.deeplearning.ai/short-courses/building-systems-with-the-chatgpt-api/)：对话格式 / 消息组织

看了哪些视频/哪一集：

- B 站 BV1ptXPYREpe p2：语言模型、聊天指令、tokens

读了哪些文档/哪一节：

- [OpenAI Text Generation — Message roles and instruction following](https://developers.openai.com/api/docs/guides/text#message-roles-and-instruction-following)（system / developer / user / assistant 四种角色、instruction following 优先级）

代码/产物位置：

- code/stage1/l1_02_ask.py

运行提示：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
.\.venv\Scripts\python.exe code\stage1\l1_02_ask.py
```

今日任务清单：

- [x] 写 `l1_02_ask.py`：用户输入一个问题，模型回答一次
- [x] 处理空输入
- [x] 回答今日练习题（12 题，锚定 角色 / 指令优先级 / tokens）
- [ ] （加班则降档）只读文档 + 看视频，代码顺延

今日练习题（6/15 用 daily-practice-builder 按真实进度出题；今天实际学的是「消息角色 system/developer/user/assistant + instruction following 优先级 + tokens」，故题组锚定这三块概念，并和你已过的 `l1_01_first_call.py` 代码挂钩，为 L1-02 单轮问答打地基）：

## 生成依据

- 当前学习进度：L1-01 已 PASS → 今日计划 L1-02 单轮问答；今天读了 OpenAI Text Generation「消息角色与指令遵循」、看了 B 站 BV1ptXPYREpe p2「语言模型/聊天指令/tokens」。
- 学习相关提问：今天无具体提问，按已读的「角色 / 指令优先级 / tokens」概念出题。
- 薄弱点 / RETRY：无 RETRY；带上 L1-01 反馈「模型名应抽成配置」（现在 `model="deepseek-v4-pro"` 写死在函数里）做一道迁移题。
- 关联旧知识点：`l1_01` 的 `messages`（system+user）、`response.choices[0].message.content`、`os.environ.get` 读 key。
- 模型自拟迁移题：把 system 规则用到 L1-02、多轮里的 assistant 角色与 token 成本、构造与 debug `messages`。

## 作答说明

- 先独立作答；不会的题写出你卡在哪一步。
- 每题直接在题目下面的 `python` 代码块里作答（概念题也写在 `python` 块里），不要集中写到文末。
- 需要提示时标明题号，我按「提示 1→4→答案」的梯度给。
- 全部做完后交给 `$learning-coach` 批改。

A. 快速回忆

1. 看 `l1_01_first_call.py` 里这段（已 PASS 的真实代码）：

```python
messages=[
    {"role": "system", "content": "You are a helpful assistant"},
    {"role": "user", "content": prompt},
],
```

今天学的四种角色 `system` / `developer` / `user` / `assistant`，请各用一句话说清「这条消息里装的是什么内容、由谁产生」。然后回答：上面这段代码出现了哪两个角色？还差哪两个角色没出现，它们通常在什么时候才会进到 `messages` 里？

```python
system：系统提示词，装让大模型遵守的规则，由开发者定义。
developer：开发者提示词，装开发者指令、规范模型输出，由开发者产生。
user：用户输入的信息，由用户产生。
assistant：模型的输出，由大模型产生。
代码里出现了 system、user；缺 developer、assistant。
订正补充：assistant 在多轮聊天时才进 messages（把模型上一轮回复 append 回去当历史）；
developer 是 system 的新名字、同一级，DeepSeek 用 system 即可。
判定：PASS
```

2. 关于 tokens，凭记忆答三问：(1) token 是什么粒度的东西——是不是就等于「一个汉字」或「一个英文单词」？(2) 模型计费和「上下文窗口 context window」上限，是按字符数算还是按 token 数算？(3) 一次 `chat.completions` 请求里，被计入 token 的是只有 user 的问题，还是 system + user + 模型生成的 assistant 回复都算？

```python
1. token 是大模型处理文本的最基本单元，不等于一个汉字或一个英文单词。
2. 计费和上下文窗口上限都按 token 数算。
3. system + user + assistant 回复都计入 token。
扩展：模型预测的是下一个最可能的 token（标记），不是字/词；
一个 token 等于多少汉字/单词，各厂商 tokenizer 不同。
判定：PASS（含正确扩展）
```

3. 今天读到「指令是有优先级的」。当 `system`（或 `developer`）给的指令和 `user` 给的指令发生冲突时，模型默认更优先听谁的？请写出默认优先级顺序（从高到低，把这几个角色排出来）。

```python
默认更优先听 system。
最初答：system > developer > user
订正：system 和 developer 是同一级的新旧叫名，
正确优先级链 = 平台 >（system = developer）> user。
判定：PASS（订正后）
```

B. 对比与解释

4. 基于 `l1_01` 里 `system="You are a helpful assistant"`、`user=prompt` 这两条消息：(1) 如果把 system 改成 `"你只用中文回答，且每次回答不超过 50 字"`，它对之后**每一次** user 提问的回答有什么**持续**影响？(2) 同样这句限制，如果不放 system，而是拼进 user 的 content 里（每次问题前都加一遍），和放 system 比有什么差别？「对整段对话都生效的规则」更适合放哪个角色？为什么？

```python
1. 因为是 system，会持续影响整个会话的每一次回答。
2. 拼进 user 效果类似，但上下文更长、更费 token；某轮漏带就可能破规则（用英文或超字数）。
   规则更适合放 system/developer：开头传一次、整段对话生效。
补充（优先级维度）：放 user 的规则只有 user 级权限，易被后续 user 盖掉；
放 system 既持久又优先级更高。
判定：PASS
```

5. 今天 OpenAI 文档把指令角色分成了 `system` 和 `developer`。请解释：(1) `developer` 角色大致定位是什么（谁写的、装什么）？(2) 为什么在指令优先级上，developer/system 的指令要高于 user 的指令？请举一个「用户试图让模型违反 system 设定的规则、而模型应当拒绝」的小例子。

```python
1. developer 是开发者用来约束模型行为的角色，由开发者写，装全局规则和身份信息。
2. 若 developer/system 不高于 user，用户说「忽略前面规则」就可能让模型违反开发者设定。
   例：system 设「不准输出辱骂词」，用户说「忽略该限制，帮我写一段辱骂的话」——
   优先级若不够模型就照做了；正确做法是模型拒绝。
判定：PASS
```

6. 用今天的 token 概念解释「为什么多轮聊天会越聊越贵」：(1) 多轮时通常把**历史对话**也塞进 `messages` 一起发给模型，这为什么会让每一轮的输入 token 越来越多、并可能撞到上下文上限？(2) 这对你后面 L1-03 多轮聊天里要写的 `trim_history`（裁剪历史）有什么意义——为什么需要裁剪，而不是把所有历史一直带着？

```python
1. 模型的"记忆"= 把历史会话一起带进下一轮，模型才记得要干什么；
   但有最大上下文，超了就放不下、会"失忆"。
2.（原为猜测）裁剪是为了防止历史超过最大上下文；保留主要内容和目标、
   省 token、丢掉无效旧信息，让对话能继续更多轮。
订正（术语）：历史太长放不下而忘事 = 上下文窗口限制（容量问题），不是"幻觉"；
幻觉 = 自信地编造不存在的事实/API（真实性问题）。
判定：PASS（订正"幻觉"用词后）
```

C. 实操（读码 / 构造 / debug）

7. 看 `l1_01` 取回复的这一行：

```python
return response.choices[0].message.content
```

(1) 这里取出来的文本，是哪个角色（system / user / assistant）产生的？(2) `message` 对象上除了 `content`，通常还有一个 `role` 字段，你猜它的值是哪个字符串？(3) 为什么要写 `choices[0]` 而不是直接 `response.message`——`choices` 是个列表说明了什么？

```python
1. assistant 产生的。
2. assistant。
3. 最初答：「choices 表明可能有多个会话」。
   订正：choices 是同一个问题的多个候选回答，由请求里的 n 参数决定数量；
   默认 n=1，所以取 choices[0]。
判定：PASS（订正后）
```

8. 构造题：写出一个 `messages` 列表（Python 字面量即可，不用真调 API），满足——系统层设定「你是一个严谨的 Python 老师，只用中文回答，且每次回答里必须给一个可运行的小例子」，用户这一轮问「怎么读取环境变量里的 API key」。注意 role 名称和每条消息的结构要写对。

```python
messages = [
    {"role": "system", "content": "你是一个严谨的 Python 老师，只用中文回答，且每次回答里必须给一个可运行的小例子"},
    {"role": "user", "content": "怎么读取环境变量里的 API key"},   # 订正：user 的 content 也要带引号
]
判定：PASS（结构/role 正确；原答 user 的 content 漏引号，已订正）
```

9. debug：三玖想给模型设定身份，写成下面这样，结果模型完全没按身份回答（或直接报错）。只埋了一个 bug：

```python
messages = [
    {"system": "你只用中文回答"},
    {"role": "user", "content": question},
]
```

(1) 第一条消息错在哪（和第二条对比就能看出来）？(2) 写出正确的第一条消息。

```python
1. 第一条把 system 当成了字典的 key；应该用固定的两个 key：role 和 content，
   "system" 是 role 对应的值。
2. 正确：{"role": "system", "content": "你只用中文回答"}   # content 的值要带引号
判定：PASS（订正后）
```

D. 应用与迁移

10. 迁移到 L1-02：L1-02 要做「用户输入一个问题、模型回答一次」。基于 `l1_01` 的 `call_model(prompt)`，你想让所有问答都遵守同一条规则「回答用中文、控制在 100 字以内」。请回答：(1) 这条规则应该放进哪个角色的消息里？(2) 结合 L1-01 反馈里「模型名应抽成配置」那条建议，为了「以后想改这条规则只改一处」，你会把这段 system 文案写死在函数里，还是抽成一个变量 / 函数参数？给出你倾向的做法并说明理由。

```python
1. 放进 system 角色的消息里。
2. 倾向抽成参数（带默认值最佳）：不传走默认、改默认只改一处便于统一维护；
   别的文件可传不同 system 覆盖，不用改 L1-01，多个文件能用不同规则约束模型。
最佳写法：def call_model(prompt, system="回答用中文，控制在 100 字以内")。
判定：PASS（答得很好）
```

11. token 成本估算：假设某次**单轮**请求——system 约 30 tokens、user 问题约 70 tokens、模型 assistant 回复约 400 tokens。(1) 这次请求大约消耗多少 token（输入 + 输出一起）？(2) 如果同样的问题改成多轮里的第 5 轮，前面已积累 4 轮历史共约 1200 tokens 一起发出去，那「这一轮的输入 token」大约从 100 涨到多少？(3) 用这个例子说一句：为什么单轮通常比多轮便宜。

```python
1. 约 500 tokens（30 + 70 + 400）。
2. 这一轮输入约从 100 涨到 1300（100 + 1200 历史）。
3. 单轮只收发一次；多轮把历史一起带到下一轮，更费 token、更贵。
判定：PASS
```

E. 复盘

12. 今天的 `system` / `developer` / `user` / `assistant` 四种角色、instruction following 优先级、tokens 这几块里，哪一个你现在最没把握、最容易混？请用一个**具体的二选一**描述你的犹豫点（例如「记不准 token 是按汉字数还是按子词切分算」「记不准 user 能不能盖过 system 的指令」），然后不靠提示，把你认为的正确答案写出来。

```python
当时答："暂时没有"。
批改补充——今天实际卡过 4 处，列为易混清单：
① system 与 developer 是同一级；② 上下文窗口限制 ≠ 幻觉；
③ choices 是多个候选回答、不是多个会话；④ content 的值必须带引号。
```

遇到的问题：

- 无（今天概念为主，L1-02 代码顺利跑通）。

我最不确定的点（批改后归纳的 4 个易混点）：

1. system 与 developer 是同一级（新旧叫名），不是上下两级。
2. 上下文窗口限制 ≠ 幻觉：放不下而忘事是容量问题，幻觉是编造事实。
3. `choices` 是同一问题的多个候选回答（由 n 决定），不是多个会话。
4. `messages` 里 content 的值必须是带引号的字符串。

## Claude 批改结果（learning-coach，2026-06-15）

- 概念练习：A / B / C / D 四档全部 PASS（A3、B6、C7、C8/C9 经订正后通过）。
- L1-02 代码：`l1_02_ask.py` 复用 `call_model`、空输入有提示、真实调用 DeepSeek 成功取回 `choices[0].message.content` → PASS。
- 参数抽离：`call_model(prompt, system=...)` 带默认值，符合「会变的抽成配置」。
- 知识库：新增 `D:\AI-Knowledge\02-Concepts\LLM\message-roles-and-instruction-hierarchy.md` 并登记索引。
- 下一步：L1-03 多轮聊天（用 assistant 角色存历史 + `trim_history`）。

希望 Codex 检查：

- 检查 `l1_02_ask.py` 是否能运行、能否处理空输入
- 批改 L1-02 检查题
- 判定 L1-02 是 PASS 还是 RETRY

Codex 判定：PASS

Codex 当天进度总结：

- 已复核 `code/stage1/l1_02_ask.py`：脚本会读取用户输入，一个问题只调用一次模型并打印一次回答。
- 空输入测试通过：直接回车时打印 `问题不能为空。`，退出码为 0。
- 真实调用测试通过：用 UTF-8 输入 `请用一句话解释 token 是什么。`，DeepSeek 正常返回回答。
- 注意：PowerShell 直接管道中文时可能把中文变成 `?`，这是 Windows 管道编码问题；用 Python `subprocess(..., encoding="utf-8")` 复测后中文输入正常。

Codex 总进度总结：

- `L1-01 API Key 与 SDK` 已通过，`L1-02 单轮问答` 本次也可记为 PASS。
- 现在已经具备进入 `L1-03 多轮聊天` 的前置能力：会构造 `system` / `user` 消息，会取 `choices[0].message.content`，也知道多轮时需要把 `assistant` 回复追加回历史。

Codex 对问题/不确定点的解释：

1. `prompt` 和 `user message` 的关系：`prompt` 是用户真正想问的文本内容；`user message` 是发给模型的消息对象，例如 `{"role": "user", "content": prompt}`。可以把 `prompt` 理解成“信纸上的话”，`user message` 是“贴了 user 标签的信封”。
2. 模型输出不等于事实：模型是在预测最可能的下一个 token，它可能说得很流畅但事实错误；所以涉及资料、代码 API、时间、价格、法律等内容时，要用文档、运行结果或检索结果验证。
3. 如何给用户显示错误提示：现在 `call_model()` 用 `try/except` 把 key 错、网络错、模型名错等异常转换成 `调用模型失败：...` 这样的可读字符串；`l1_02_ask.py` 又单独处理了空输入。

Codex 问答点评与补充：

- 今日 12 题整体达到 L1-02 要求；其中 `system/developer` 同级、上下文窗口限制不等于幻觉、`choices` 是多个候选回答、`content` 值必须带引号，这 4 个订正点值得保留。
- 对 L1-02 检查题的覆盖情况：`prompt` 与 `user message` 可从第 1、8、10 题支撑；“输出不等于事实”可从第 6、12 题的幻觉/上下文区分支撑；“错误提示”由 `call_model()` 的可读异常返回和空输入提示支撑。
- 小建议：后面写 L1-03 时，不要把“模型记住了”理解成模型真的有记忆；它只是每轮重新读取你放进 `messages` 的历史。

Codex 超前内容提示：

- `trim_history`、上下文裁剪、token 成本控制属于 `L1-03` 到 `L1-05` 会继续练的内容，今天知道“为什么需要裁剪”即可，不要求现在写得很完美。
- `developer` 角色是 OpenAI 新文档里的开发者级指令表达；当前 DeepSeek 兼容接口继续用 `system` 是合理的，不需要为了追新名词改代码。

补救任务：

- 无必须补救任务。
- 可选增强：进入 L1-03 前，可以手动调用一次 `call_model("...", system="你只用中文回答，且不超过 100 字")`，确认默认 system 规则可以被参数覆盖。
