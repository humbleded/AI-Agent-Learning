# 2026-08-18 学习记录

- 当前任务：`A4-Gate` 研究总结 Agent
- 任务状态：`DOING`（沿用 `tracker/progress.md`；本页只记录跨日教学接力，不产生父任务正式判定）
- 今日计划学习预算：用户在第一块完成后明确确认“今天不上班、状态良好，并确认启用今天的休息日第二块”。第一块 150 分钟已闭合 `A4-Gate-C4a-2b1`；充分休息后启动第二块 150 分钟，只处理 `A4-Gate-C4a-2b2`。全天计划容量为 300 分钟，不超过休息日双块 360 分钟上限
- 第二块分配：25 分钟核对冻结合同、真实代码与唯一新控制流判断，90 分钟完成最小实现，35 分钟做无落盘故障注入、既有链回归和用户结果审核；助手读取、建骨架、运行验证与自动写回不计入这 150 分钟
- 预算边界：本次双块授权只适用于 2026-08-18，不转成默认课表；第二块不追加旧复习、算法或另一套概念题量。若本切片未收口则按真实停点顺延，不形成补偿欠账
- 实际学习时长：不记录

## 当日 Session / 任务索引

| Session | 任务 | 环节 | 关联前序 daily | 当前状态 / 下一步 |
| --- | --- | --- | --- | --- |
| S1 | `A4-Gate-C4a-2b1` | 专用终止信号订正 → 最终失败状态映射实现 | [2026-08-17](2026-08-17.md) S3 / `A4-Gate-C4a-2b1-G1-R1` | `G1-R1 / I1 / 用户审核 = PASS`；本学习块已完成 |
| S2 | `A4-Gate-C4a-2b2` | 真实不可重试工具失败 / 搜索成功零证据的固定三字段映射 | 本页 S1 / 当前接力 | `G1 / I1 / U1 = 教学 PASS`；本学习单元已收口，父 `A4-Gate` 仍为 `DOING` |

## 任务证据接力

### Session S1｜`A4-Gate-C4a-2b1`

- 当前学习单元：把两种已冻结的自治终点通过专用内部信号传到公开入口，再映射为固定三字段 `needs_manual`：① 首次可重试失败后，第二次可重试工具尝试仍失败；② 首次可重试失败后，重试前已无剩余 step，第二次工具不得发生。
- 关联前序 daily：[2026-08-05](2026-08-05.md)、[2026-08-06](2026-08-06.md)、[2026-08-07](2026-08-07.md)、[2026-08-08](2026-08-08.md)、[2026-08-09](2026-08-09.md)、[2026-08-10](2026-08-10.md)、[2026-08-11](2026-08-11.md)、[2026-08-12](2026-08-12.md)、[2026-08-13](2026-08-13.md)、[2026-08-17](2026-08-17.md)。8 月 14～16 日没有学习记录，不补建空白 daily。
- 资料定位与版本：`code/stage4/problem-contract.md` 第 4 节输出合同、第 6 节数值护栏 / 失败恢复、第 8～9 节拒绝与人工接管；`code/stage4/a4_gate_research_summary_agent.py` 的 `reserve_call_step()`、`make_result()`、`execute_tool_call()`、`generate_candidate_summary()` 与 `run_research_summary_once()`；仓库基线 `HEAD 801b08f5ffafd65f45ef047362cab4ceddebf4e0`，C4a-2a 是尚未提交的既有用户改动，必须保留。
- 已覆盖且不重复：`needs_manual` 状态名、固定三字段、阈值 / 最后错误 / 证据缺口说明、无真实来源 `sources=[]`、`MAX_STEPS=6`、`MAX_TOOL_RETRIES=1`、retry 分层、同参数重试、原子尝试、七字段日志、无第三次调用、失败不进入 Tool Observation。
- 当前实现目标：只新增专用 `AutoRecoveryExhaustedError` 内部信号、两个精确发出点和公开入口的窄映射；正常五步、首败后成功六步、首次不可重试、空结果、未知动作、候选恢复、HITL 和 eval 不在本切片扩写。
- 回炉 preflight：`weak-points.md` 持久状态为 `NORMAL`。按 2026-08-18 只读重算，到期项仍为 `CD-005 / WP-08` 共 2 项，最老逾期 5 天，未达到数量 ≥4 或逾期 ≥7 天阈值；`WP-08` 已在 8 月 17 日同任务获得教学迁移证据，本日不重复，`CD-005` 与本实现不相关，因此本编码 Session 不另设旧复习目标。
- 算法 preflight：W4 已 `PASS`、结转债务 0；今天未排独立算法时段。

## 带学检查点

### `A4-Gate-C4a-2b1-G1-R1`｜专用信号与窄映射订正

用户原始订正：

> 1.generate_candidate_summary 在step是否耗尽和是否达到最大重试值下发出AutoRecoveryExhaustedError，至少携带触发原因，最后一个真实工具错误，实际工具尝试次数这三类信息
> 2.run_research_summary_once只捕获AutoRecoveryExhaustedError这种异常，捕获后返回needs_manual.正常内部三键继续进入Relfection,其他的RuntimeError继续向上传播
> 3.因为execute_tool_call只负责记录这一次发生了什么，不知道之前步骤有没有失败，重试机会是否用完，也不拥有整个 Agent 的公开三字段结果合同，因此不能决定整个请求最终是否进入 needs_manual

判定：教学 `PASS`。

- 已准确说明 `generate_candidate_summary()` 负责识别两个精确自治终点，并发出只代表自动恢复耗尽的 `AutoRecoveryExhaustedError`；信号携带触发原因、实际工具尝试次数和最后真实错误，足以让公开入口构造准确交接说明。
- 已准确说明 `run_research_summary_once()` 只捕获该专用类型并早返回 `needs_manual`；正常内部三键继续进入 `Reflection`，其他 `RuntimeError` 继续传播，不被伪装成人工接管。原文 `Relfection` 统一校准为 `Reflection`，不影响技术判断。
- 已准确恢复 `execute_tool_call()` 的请求局部职责：一次调用只负责一次原子工具尝试所需的校验、占 step、执行、计时和日志；它不知道历史失败 / 剩余 retry，也不拥有公开三字段合同。
- 精度校准：“达到最大重试值”在本固定合同下不是仅检查常量 `MAX_TOOL_RETRIES == 1`，而是确认那一次允许的额外真实尝试已经发生且仍失败；“重试前无 step”则是在第二次真实调用之前精确识别总 step 已满。该校准无需重答。
- `G1` 最终状态：`PASS`。这证明层级判断可以进入真实实现，不等于 `C4a-2b1` 或父 Gate 正式 PASS。

## 实现检查点

### `A4-Gate-C4a-2b1-I1`｜专用恢复耗尽信号与最终三字段映射

- 当前状态：教学 `RETRY`；用户已提交首版实现，异常对象本身成立，但真实重试与公开结果映射尚未完成，且回归破坏了已通过的 C4a-2a。
- 用户只修改：`code/stage4/a4_gate_research_summary_agent.py` 中当前 `C4a-2b1-I1` 标记的类体和三个 TODO 位置；保留既有 C4a-2a 同参数重试、`execute_tool_call()`、`reserve_call_step()`、消息构造、Reflection / Refinement 与公开成功合同。
- 本实现完成后由助手使用无落盘内存 fake / spy 验证二次失败、重试前无 step、首败后成功、首次成功、窄异常传播、日志 / 副作用卫生和严格三字段结果；不创建 runner、baseline、原始报告或 `eval_cases.json`。
- 骨架核验：模块 `py_compile` 退出 0，`git diff --check` 无新增错误；当前新增一个只在实例化时触发的 `NotImplementedError`，既有 C4a-2a 成功恢复逻辑未被助手改写。

用户提交口令：

> 改完了 C4a-2b1-I1 专用信号映射

首版实现中做对的部分：

- `AutoRecoveryExhaustedError` 已保存 `reason / attempts / last_error`，并调用父类初始化为带非空说明的 `RuntimeError`；这个窄信号类型可以保留。
- 没有添加 `except RuntimeError` 或 `except Exception` 一把兜底，因此没有把其他协议错误直接伪装成 `needs_manual`。

静态审查与无落盘内存 fake / spy 的决定性结果：

```text
GENERATE_EXCEPTION_TYPE=AutoRecoveryExhaustedError
GENERATE_REASON=6
GENERATE_ATTEMPTS=1
GENERATE_LAST_ERROR='timeout'
GENERATE_MODEL_CALLS=1
GENERATE_TOOL_CALLS=1
GENERATE_STEP=3
GENERATE_LOG_STEPS=[1, 2]
GENERATE_LOG_EVENTS=['model_call', 'tool_call']
PUBLIC_EXCEPTION_TYPE=AutoRecoveryExhaustedError
PUBLIC_RESULT=NONE
PUBLIC_TOOL_CALLS=1
```

完整无落盘矩阵共 13 个场景：7 个通过、6 个失败。通过项包括异常对象字段与非空文案、首次工具成功五步链及严格三字段、首次不可重试普通错误传播、畸形工具结果普通错误传播、真实工具 `OSError` 原样传播、模型协议 `RuntimeError` 传播、普通 `RuntimeError` 未被吞；失败项正好集中在本切片要求保护的二次失败、step 满、两种公开映射、首败后成功六步回归，以及隔离注入专用信号后的公开映射。

判定依据：

- 首次可重试失败后，代码没有再次调用 `execute_tool_call()`，而是直接执行 `run_context["step"] += 1` 再抛异常；所以真实工具尝试只有 1 次，却出现没有工具副作用、计时和日志的幽灵 step 3，昨天已通过的“首败后成功六步链”也不可能继续。
- 两个发出点都使用整数 `reason=MAX_STEPS`（实际值 6）、`attempts=1` 和第一次错误；尚无 `reason="max_tool_retries" / attempts=2 / 第二次真实错误` 分支，无法区分两个自治终点。
- `run_research_summary_once()` 仍然裸调用 `generate_candidate_summary()`；专用异常直接泄漏，`PUBLIC_RESULT=NONE`，没有形成严格三字段 `needs_manual`。
- `step > MAX_STEPS` 被当前编排层另行改成 `RuntimeError`，与 `reserve_call_step()` 对非法状态使用的既有 `ValueError` 分类不一致；本切片只应识别精确的 `step == MAX_STEPS` 终点，不接管非法状态。
- 四处活动 TODO 尚未收口，`MAX_TOOL_RETRIES` 除定义外未再使用。

`I1-R1` 最小订正目标：

1. 保留已正确的专用异常类；删除手工 `step += 1`，恢复 C4a-2a 的同工具名、同原始参数、同标准化请求、同 `run_context` 的第二次 `execute_tool_call()`，让原子入口自行占 step、执行、计时和记日志。
2. `step == MAX_STEPS` 时在第二次调用前发出 `reason="max_steps" / attempts=1 / 第一次真实错误`；`step < MAX_STEPS` 时必须先完成第二次真实调用，只有第二次返回 `ok is False and retryable is True` 才发出 `reason="max_tool_retries" / attempts=2 / 第二次真实错误`，成功则继续原正常链。
3. 公开入口只捕获 `AutoRecoveryExhaustedError`，用 `make_result()` 立即返回 `status="needs_manual"`、包含触发原因 / 实际尝试次数 / 最后错误 / 证据缺口与停止交接的非空 summary、`sources=[]`；正常内部三键继续 Reflection，其他异常原样传播。
4. 不在本编排层新增 `step > MAX_STEPS` 错误分类，并清除本切片四处活动 TODO。

本次是实现检查点教学 `RETRY`，不改 `tracker/progress.md`，也不构成父 `A4-Gate` 的正式判定。

#### `A4-Gate-C4a-2b1-I1-R1`｜恢复真实重试和窄映射

用户提交口令：

> C4a-2b1-I1-R1 恢复真实重试和窄映射

本轮相对首版的有效进展：

- 首次可重试失败后，第二次 `execute_tool_call()` 现在确实发生；二次失败场景实测工具 2 次、模型 1 次、日志 3 条、`attempts=2`，且没有第三次工具调用。
- 公开入口开始只捕获 `AutoRecoveryExhaustedError` 并早返回；Reflection 没有在专用终点后继续执行，普通 `RuntimeError / ValueError / OSError` 仍能传播，没有出现宽泛吞错。
- 首次工具成功五步链仍 `PASS`：工具 1 次、模型 4 次、step / 日志严格为 `1..5`，公开结果严格为 `status / summary / sources`；正常请求 context、request ID 和日志保持隔离。

相同 13 场景无落盘 fake / spy 结果仍为 `7 PASS / 6 FAIL`：

| 场景 | 实测 | 未满足合同 |
| --- | --- | --- |
| 二次可重试失败 | 工具 2、模型 1、`step=4`、日志 step `[1,2,4]`；信号 `reason="6" / attempts=2 / last_error="first timeout"` | 应为连续 step `[1,2,3]`、`reason="max_tool_retries"`、最后错误为第二次错误 |
| 重试前 step 满 | 初始 step 4 后仅模型 step 5、首工具 step 6，无第二次副作用；`attempts=1` 和首错正确 | `reason` 实测为字符串 `"6"`，应为 `"max_steps"` |
| 首败后第二次成功 | 第二次工具确实成功，但随后仍无条件发出专用异常；只返回 `{"needs_manual": true}`，模型仅 1 次、日志 `[1,2,4]` | 应接住第二次成功结果并继续候选摘要、Reflection、Refinement，形成完整六步成功链 |
| 第二次变为不可重试失败 | 也被无条件映射为单键人工接管结果 | 不属于本切片专用终点，应继续既有普通失败路径 |
| 两种公开映射 | 已窄捕获且早返回，但结果均为 `{"needs_manual": true}` | 必须经 `make_result()` 返回严格 `status / summary / sources`，且 summary 使用专用异常事实 |
| 隔离注入正确专用异常 | Reflection 为 0，但仍返回单键字典 | 证明捕获边界正确、映射内容未实现 |

决定性根因：

- 第二次原子调用前仍手工执行 `run_context["step"] += 1`；随后 `execute_tool_call()` 又通过 `reserve_call_step()` 自行占步，造成没有调用 / 计时 / 日志的幽灵 step 3。
- 第二次 `execute_tool_call()` 的返回值没有赋给变量或覆盖 `tool_result`，所以代码无法判断第二次成功、可重试失败或不可重试失败；调用后无条件抛信号，`last_error` 也仍取第一次错误。
- `str(MAX_STEPS)` 只是把整数 6 变成字符串 `"6"`，不是语义分类 `"max_steps"` 或 `"max_tool_retries"`。
- 公开入口捕获时没有绑定异常对象，并直接返回单键字典，没有使用 `make_result()`、三类异常事实和固定三字段合同。

判定：`I1-R1 = 教学 RETRY`。为缩小调试面，`I1-R2` 先只修 `generate_candidate_summary()` 的内层控制流：删除手工 step、保存第二次真实返回、按第二次真实结果分流，并使用两个精确 reason；公开入口的三字段映射保留到内层复测通过后再收口。本轮不改 `tracker/progress.md`。

#### `A4-Gate-C4a-2b1-I1-R2`｜内层真实结果分流

用户提交口令：

> 改完了 C4a-2b1-I1-R2 内层真实结果分流

本轮已通过的核心行为：

- 已恢复 `MAX_TOOL_RETRIES > 0` 边界；值 0 时工具仅尝试 1 次，值 1 时总计 2 次，预置第三个结果未消费。
- 已删除手工 step；第二次调用继续经 `execute_tool_call()` 原子占步、计时和记日志，并把返回值覆盖回 `tool_result`。
- 首败后第二次成功完整六步链 `PASS`：工具 2、模型 4、step / 日志连续为 `[1,2,3,4,5,6]`，只有第二次成功结果进入唯一 Tool Observation，最终严格三字段 `success`。
- 二次可重试失败除 reason 外均通过：工具 2、模型 1、step / 日志 `[1,2,3]`、无第三次调用、`attempts=2`、`last_error="second timeout"`。
- 重试前 step 满除 reason 外均通过：初始 step 4 后只有模型 step 5、首工具 step 6，无第二次副作用 / 计时 / 日志，`attempts=1`、`last_error="first timeout"`。
- 第二次 `retryable=False` 与畸形 dict 均不发专用信号，分别走既有普通失败守门；第二次 Python 异常也保持原异常传播。

无落盘内层矩阵：`7` 个场景中 `5 PASS / 2 FAIL`。两个失败均是稳定原因码：

```text
二次可重试失败：reason 实测为整数 3，要求 "max_tool_retries"
重试前 step 满：reason 实测为整数 6，要求 "max_steps"
```

剩余内层边界：

- `reason=run_context["step"]` 记录的是当前计数，不是终止原因；两个发出点必须改为冻结的语义字符串。
- 第二次调用仍被 `if run_context["step"] < MAX_STEPS` 包住。精确的 `== MAX_STEPS` 已提前转为目标专用信号后，其余状态应直接进入原子入口：正常小于上限时继续占步，非法大于上限时由 `reserve_call_step()` 保持既有 `ValueError` 分类，而不是绕开校验落入普通工具失败。
- `git diff --check` 当前因源码第 792 行行尾空格退出 1；这是卫生项，删除空格即可，逻辑无需重写。

公开入口不参与本轮 R2 判定，但用户提前编辑的分支仍待 R3 收口：`except` 没有绑定专用异常对象，且在候选生成抛异常时引用尚未赋值的 `candidate_summary`，会引发新的 `UnboundLocalError`；本轮先不继续修改该入口。

判定：`I1-R2 = 教学 RETRY（已接近通过）`。原位只改两个 reason、移除 `< MAX_STEPS` 包裹并整理缩进、清除行尾空格；同一内层矩阵通过后再进入 R3 公开三字段映射。

##### `A4-Gate-C4a-2b1-I1-R2` 原位订正复核

用户提交口令：

> 改完了 C4a-2b1-I1-R2 reason 原位订正

复核结果：内层无落盘矩阵 `9 PASS / 0 FAIL`。

- 二次可重试失败：专用信号严格为 `reason="max_tool_retries" / attempts=2 / last_error="second timeout"`；工具 2、模型 1、step / 日志 `[1,2,3]`，无第三次调用。
- 重试前 step 满：专用信号严格为 `reason="max_steps" / attempts=1 / last_error="first timeout"`；从初始 step 4 得到日志 `[5,6]`，没有第二次工具副作用、计时或日志。
- 首败后第二次成功：完整六步严格三字段 `success`；工具 2、模型 4、step / 日志 `[1,2,3,4,5,6]`。
- 第二次不可重试与畸形结果：均走既有普通 `RuntimeError`，不发专用信号，无第三次调用。
- `MAX_TOOL_RETRIES=0/1`：分别只有 1 / 2 次工具尝试，边界正确。
- 第一次工具返回后分别注入 `step=7` 与 `step="corrupt"`：第二次进入既有原子入口，由 `reserve_call_step()` 原样抛“超过最大步骤限制 / 必须是非负整数”的 `ValueError`；没有第二次工具副作用，也没有被专用信号伪装。

用户完成全部行为判断后，助手只做机械收口：统一该分支缩进、移动第二个 TODO 注释到对应调用前、拆分长条件并清除行尾空格；没有改动判断条件、reason、尝试次数、工具参数或结果分流。收口后 `py_compile` 退出 0，源码 `git diff --check` 退出 0；只有 Git 的 LF→CRLF 工作区提示，不是空白错误。

判定：`A4-Gate-C4a-2b1-I1-R2 = 教学 PASS`。该结论只证明专用信号的内层识别与轨迹边界，不等于完整 I1 或父 Gate PASS；下一唯一学习单元为 `I1-R3`：公开入口读取专用异常事实并返回严格三字段 `needs_manual`。

#### `A4-Gate-C4a-2b1-I1-R3`｜公开三字段映射

用户提交口令：

> 改完了 C4a-2b1-I1-R3 公开三字段映射

完整无落盘矩阵：`15` 个场景中 `13 PASS / 2 FAIL`。两项失败只来自两种公开 `needs_manual.summary` 的相同合同问题。

本轮已通过：

- `try` 只包住 `generate_candidate_summary()`；`except` 只捕获并绑定 `AutoRecoveryExhaustedError as exc`，没有宽泛吞掉其他错误。
- 正常内部 `candidate_summary / tool_name / tool_result` 三键在捕获之后读取，并继续 Reflection；不再出现未赋值变量问题。
- 两种终点都已经“返回而不是抛专用异常”，顶层键严格为 `status / summary / sources`，`status="needs_manual"`、`sources=[]`，没有 request ID / logs 等第四个顶层字段。
- 二次失败保持工具 2、模型 1、step / 日志 `[1,2,3]`；step 满保持工具 1、模型 1、日志 `[5,6]`；两者都没有候选第二模型、Reflection 或 Refinement。
- 正常五步、恢复六步、首次 / 二次不可重试、首次 / 二次畸形结果、模型 `RuntimeError`、工具 `OSError`、下游 `ValueError`、choices 协议错误、七字段日志和请求隔离全部通过。

两项实际失败结果：

```python
{
    "status": "needs_manual",
    "summary": {
        "reason": "max_tool_retries",
        "attempts": 2,
        "last_error": "second timeout",
    },
    "sources": [],
}

{
    "status": "needs_manual",
    "summary": {
        "reason": "max_steps",
        "attempts": 1,
        "last_error": "first timeout",
    },
    "sources": [],
}
```

阻断原因：

- `make_result()` 的 `summary` 合同是非空 `str`，当前传入的是 `dict`；Python 类型标注不会自动在运行时拒绝它，因此顶层三键看似正确，但返回语义仍错误。
- `reason / attempts / last_error` 是外层构造说明所需的内部事实，值应写进一段用户可读文字，不能以嵌套键结构泄漏到公开结果。
- 当前内容只保存三个原始值，尚未说明具体阈值 `MAX_STEPS=6 / MAX_TOOL_RETRIES=1`、证据缺口，以及 Agent 已停止自治并交还用户决定下一步。

判定：`I1-R3 = 教学 RETRY`。原位只把 summary dict 改为准确的非空字符串：根据 `exc.reason` 选择对应阈值文案，写入 `exc.attempts / exc.last_error`，并补齐证据不足与停止交接；仍通过 `make_result("needs_manual", summary_text, [])` 返回。内层与 try / except 边界不再修改。

##### `A4-Gate-C4a-2b1-I1-R3` summary 字符串原位订正与最终收口

用户提交口令：

> 改完了 C4a-2b1-I1-R3 summary 字符串原位订正

用户提交首先完成了结构性订正：`summary` 成为非空字符串，严格三字段、窄捕获、真实 attempts / last_error、证据不足和停止交接语义均成立；提交中的异常类名、内部字段标签与 `... / ……` 仍是调试占位文案。由于 G1 已证明层级判断，固定三字段与交接措辞也有既有 PASS 证据，剩余文案可从冻结合同唯一推导，不再作为新的用户判断。助手按既定分工机械完成：

- 将 `max_steps / max_tool_retries` 分别整理为“总步骤上限 6 / 工具重试上限 1”的用户说明；去除异常类名、内部字段标签、占位符和重复信息。
- 只保留一段自然 summary：准确阈值、实际工具尝试次数、最后真实错误、可信证据缺口、停止自治并交由人工决定下一步。
- 移除四处 `C4a-2b1-I1` TODO，更新模块头与 `run_research_summary_once()` docstring，统一缩进、长行和注释位置；未改动用户已经通过的内层条件、工具参数、尝试次数或 try / except 边界。

机械收口后完整无落盘矩阵：`15 PASS / 0 FAIL`。

1. 二次可重试失败公开结果：

```python
{
    "status": "needs_manual",
    "summary": "自动恢复已停止：已达到工具重试上限 1；工具实际尝试 2 次；最后错误：second timeout。当前尚未取得足够的可信证据，已停止自治并交由人工决定下一步。",
    "sources": [],
}
```

轨迹：工具 2、模型 1、step / 日志 `[1,2,3]`；无第三次工具、候选第二模型、Reflection 或 Refinement。

2. 重试前无剩余 step 公开结果：

```python
{
    "status": "needs_manual",
    "summary": "自动恢复已停止：已达到总步骤上限 6；工具实际尝试 1 次；最后错误：first timeout。当前尚未取得足够的可信证据，已停止自治并交由人工决定下一步。",
    "sources": [],
}
```

轨迹：从初始 step 4 得到模型 step 5、首工具 step 6；工具 1、模型 1、日志 `[5,6]`；无第二次工具副作用 / 计时 / 日志，也无下游模型。

其余 13 项回归全部通过：两种内层专用信号、首次成功五步、首败后成功六步、首次 / 二次不可重试、首次 / 二次畸形结果、模型 `RuntimeError`、工具 `OSError`、下游 `ValueError`、模型协议错误和两请求隔离。所有日志仍严格七字段，所有普通异常保持原对象传播，未授权分支没有误映射，公开结果没有泄漏 `reason / attempts / last_error / request_id / logs / tool_result`。

最终卫生：`py_compile` 退出 0；源码与本页 `git diff --check` 退出 0，仅有 Git 的 LF→CRLF 工作区提示；本切片 TODO / `NotImplementedError` 为 0。

判定：`A4-Gate-C4a-2b1-I1 = 教学 PASS`。这只闭合当前实现检查点与 C4a-2b1 教学范围，不等于父 `A4-Gate` 正式 PASS；父任务继续 `DOING`，正式判定仍留给 `ai-agent-learning-review`。

##### 用户最终结果审核

用户原始确认：

> 确认 C4a-2b1 两条轨迹符合预期

审核结论：用户确认以下两条最终行为均符合设计意图：

- 二次可重试失败：工具 2 次、模型 1 次、日志 `[1,2,3]`，严格三字段 `needs_manual`，无第三次工具或下游模型。
- 重试前 step 满：工具 1 次、模型 1 次、日志 `[5,6]`，严格三字段 `needs_manual`，无第二次工具副作用 / 计时 / 日志或下游模型。
- 两条分支均未扩大到首次 / 二次不可重试、畸形结果或普通 Python 异常。

`A4-Gate-C4a-2b1` 的用户决策、真实实现、决定性验证和最终意图审核现已全部闭合；本页仍只记录教学证据，不修改父任务在 `tracker/progress.md` 中的 `DOING` 状态。

## 第一块收口时的接力（已由第二块启动接续）

- 今日计划范围 / 今日剩余：`A4-Gate-C4a-2b1` 的 G1、I1、15/15 矩阵与用户最终审核全部完成；本学习块今日剩余为 0，不再启动新的失败映射或 Gate 单元。
- 任务总剩余：本切片完成后仍有首次不可重试 / 空结果等其他失败状态映射、未知动作与候选校验恢复、HITL allow / deny、14 条 `eval_cases.json`、正式 Gate 复核与五个必答问题；它们不等于今天都要做。
- 下次 Session 起点（明确不属于今天）：在新的自然日重新纳入 ≤180 分钟预算后，从 `A4-Gate-C4a-2b2` 开始，只处理 `retryable=False → tool_failure` 与工具成功但空结果 `→ insufficient_evidence` 两个固定三字段映射；未知动作 / 候选校验恢复、HITL、eval 和父 Gate 复核继续留后。
- notes 状态：本代码切片不新建 notes；原始答案与订正只保存在 daily。

## 休息日第二块启动｜Session S2｜`A4-Gate-C4a-2b2`

- 授权与预算：用户在第一块完成后明确确认今天不上班、状态良好并启用休息日第二块；第二块计划 150 分钟，全天计划容量 300 分钟。
- 休息状态：用户原文确认“已经充分休息”；休息日第二块的授权、状态与前置休息条件均已满足，现正式进入 G1。休息和等待时间不计入学习预算。
- 当前学习单元：只把两个已经冻结的证据终点转换为公开固定三字段结果：① **真实工具已经执行**后返回 `ok is False, retryable is False`，不重试并返回 `tool_failure`；② `search_web` 真实执行成功但返回 `ok is True, results=[]`，返回 `insufficient_evidence`、`sources=[]`，不凭模型常识补写摘要。
- 资料定位与版本：`code/stage4/problem-contract.md` 第 4 节输出契约、第 5 节两种工具返回、第 6 节失败 / 恢复 / 停止和第 8 节失败边界；`code/stage4/a4_gate_research_summary_agent.py` 的 `make_result()`、`read_material()`、`search_web()`、`execute_tool_call()`、`generate_candidate_summary()` 与 `run_research_summary_once()`；当前仓库基线 `HEAD 1434adcdd6af3ab14c55dff547c08baea9059340`，启动前工作树 clean，`py_compile` 与 `git diff --check` 均退出 0。
- 关联前序 daily：[2026-08-05](2026-08-05.md)、[2026-08-06](2026-08-06.md)、[2026-08-07](2026-08-07.md)、[2026-08-08](2026-08-08.md)、[2026-08-09](2026-08-09.md)、[2026-08-10](2026-08-10.md)、[2026-08-11](2026-08-11.md)、[2026-08-12](2026-08-12.md)、[2026-08-13](2026-08-13.md)、[2026-08-17](2026-08-17.md) 与本页 S1。8 月 14～16 日没有学习记录，不补建空白 daily。
- 已覆盖且不重复：固定 `status / summary / sources`、`needs_manual` 两条终点、一次同参数重试、请求局部 step、七字段日志、真实工具一次调用对应一个 step / 一条日志、失败 Observation 不进入消息历史、普通异常窄传播和公开结果不泄漏内部字段。
- 当前唯一新证据：当前 `execute_tool_call()` 的执行前 action / schema 拒绝与真实工具的确定性失败都可能返回同形的 `{ok: False, retryable: False}`。本块必须根据“这份结果是否确实来自一次真实工具执行”的来源证明区分它们，不能只看两个布尔字段或错误文案；只把后者映射为 `tool_failure`。
- 明确不在本切片：未知工具 / 坏 JSON / schema / 参数错误的 action correction，候选摘要 / Reflection / 最终 validator 失败恢复，空白 `read_material.content` 的扩展合同，HITL allow / deny、14 条 `eval_cases.json`、五个 Gate 必答问题、父 Gate 正式复核、G8-00 和算法。
- 用户必须亲自完成：先通过 G1 判断三种结果的业务状态、证明真实工具是否执行的可观察依据和停止位置；通过后替换当前唯一 TODO 骨架，完成两个最小映射并审核故障矩阵结果。
- 助手自动完成：按 G1 已证明的边界即时建立不含答案的 TODO 骨架，运行无落盘 fake / spy、既有正常 / 重试 / `needs_manual` 回归，整理重复三字段文案和 daily 证据；不冒充新的用户理解。
- 用户最终审核：确认两种终点的工具 / 模型次数、step / 日志、严格三字段和无伪造来源符合意图，并确认没有吞掉执行前 action 错误或越界实现后续恢复。
- 回炉 / 算法：`weak-points.md` 持久状态仍为 `NORMAL`；截至 2026-08-18 到期 `CD-005 / WP-08` 共 2 项、最老逾期 5 天，未触发回收块。`WP-08` 已在 8 月 17 日绑定本产物取得迁移证据，第二块不重复；W4 算法 `PASS`、债务 0，本块不排算法。
- 当前骨架边界：G1 会决定怎样证明“真实执行”，因此在该判断通过前不先写带答案倾向的代码结构；G1 通过后立即建立唯一 `C4a-2b2-I1` TODO / `NotImplementedError`，不提前批量创建后续骨架。

### `A4-Gate-C4a-2b2-G1`｜同形失败结果的真实执行来源边界

现在在做什么：先区分三份表面相似或相邻的工具结果应该走哪种业务终点，并确定在当前真实代码里如何证明工具到底有没有执行。这里的“来源证明”（provenance）是指：不仅看结果字典长什么样，还要有可观察事实证明它来自执行前拒绝，还是来自一次真实工具副作用之后。

为什么需要：`execute_tool_call()` 在白名单、JSON、schema 或参数绑定校验失败时，会在调用真实工具前返回 `ok=False, retryable=False`；真实 `read_material()` / `search_web()` 也可能在执行后返回相同两个布尔值。若只写 `if not ok and not retryable: tool_failure`，就会把本应留给后续 action correction 的模型动作错误提前吞掉。

与前后关系：C4a-1 已证明每次真实模型 / 工具调用才会独立占 step 并留下七字段日志；C4a-2b1 已证明内层精确识别、公开入口窄映射的分层。本题只把这两份已通过证据迁移到新的失败表面，随后才进入唯一代码 TODO。

请判断以下三例：

1. 模型选择未注册工具 `delete_file`。`execute_tool_call()` 在 `TOOL_REGISTRY` 校验处返回 `{"ok": False, "retryable": False, ...}`；真实工具调用次数为 0，`run_context` 只有第一次模型的 step / 日志。
2. 模型合法选择 `read_material`，参数也全部通过；真实工具函数执行一次后确认文件不存在，返回 `{"ok": False, "retryable": False, "error": "文件不存在"}`；账本新增一条 `tool_call`。
3. 模型合法选择 `search_web`；真实搜索执行成功并返回 `{"ok": True, "results": []}`；账本也新增一条成功 `tool_call`。

请用自己的话回答三点：

1. 三例分别应该进入什么状态或后续范围？尤其说明第 1 例为什么不能因为 `retryable=False` 就返回 `tool_failure`。
2. 在不依赖错误文案的前提下，`generate_candidate_summary()` 可以比较调用前后的哪些 `run_context` / 七字段日志事实，证明第 2、3 例发生过真实工具调用，而第 1 例没有？
3. 第 2、3 例一旦识别，应在哪次模型调用之前停止？最终 `sources` 应是什么；哪些候选摘要、Reflection 或 Refinement 调用必须为 0？

完成标准：能同时守住业务状态、真实执行来源、严格三字段与下游零调用；不要求现在写代码，也不要求提前设计未知动作的完整恢复流程。当前状态：`DOING / 待作答`。

#### `A4-Gate-C4a-2b2-G1`｜首次作答

用户原始答案：

> 1. 案例 1：应该进入needs_manual状态，因为选择了压根就没有注册的工具，所以不能通过retryable=False 就返回 tool_failure
>    案例 2：应该进入tool_failure状态，因为调用工具成功了，但是因为文件不存在所以失败。
>    案例 3：应该进去insufficient_evidence状态，因为调用工具成功，但是results=[]，表示内容不足，所以失败
>
> 2. 调用前记录的request_id
>    调用后比较step和logs
>
> 3. 在第二次调用模型之前停止；
>    sources = []
>    候选摘要 / Reflection / Refinement = 1/0/0

教学判定：`PARTIAL`。

- 已做对 1：案例 2 的业务状态 `tool_failure` 正确；案例 3 的 `insufficient_evidence` 和 `sources=[]` 也正确。
- 已做对 2：已经意识到不能只看 `retryable=False`，并选择使用 step / logs 作为真实执行证据，而不是根据错误文案猜测。
- 已做对 3：两个目标终点都应在第二次模型调用之前停止，Reflection 和 Refinement 均不应发生。
- 缺口 1：案例 1 不能在第一次动作错误后立即进入 `needs_manual`。冻结合同要求未知工具 / schema / 参数错误在仍有 step 时交回 Agent 修正；只有修正仍耗尽 `max_steps` 才进入 `needs_manual`。本切片尚不实现 action correction，因此只要求它保持后续范围 / 现有未映射错误，绝不能被新分支包装为 `tool_failure` 或提前包装成 `needs_manual`。
- 缺口 2：案例 2 的“调用工具成功了”容易把“真实执行发生过”和“工具业务结果成功”混为一谈。精确说法应是：工具**确实执行了一次**，但返回的业务结果是失败；因此映射为 `tool_failure`。
- 缺口 3：`request_id` 只能证明事件属于同一个请求；第一次模型事件和后续工具事件本来就共享它，单独记录它不能证明工具执行。必须明确记录调用前的 step 与 logs 长度，调用后检查二者是否各新增一次，并核对新增日志的 `event_type == "tool_call"`；执行前拒绝则三项都不增加。
- 缺口 4：这里的“候选摘要模型”就是工具之后的第二次模型，而第一次模型只负责选择工具。因此目标分支中模型总调用数为 1，但候选摘要模型 / Reflection / Refinement 应分别为 `0 / 0 / 0`，不是 `1 / 0 / 0`。

当前只做 `G1-R1` 定向订正：不重答已经正确的案例 3 状态、`sources=[]` 或停止位置；只补四点——① 案例 1 当前应留给哪里、何时才可能 `needs_manual`；② 把案例 2 改成“真实执行发生但结果失败”的精确表述；③ 写出 step / logs 调用前后怎样变化以及新增日志必须是什么事件；④ 写出模型总调用数与候选摘要 / Reflection / Refinement 各自调用数。通过前仍不创建代码 TODO。

#### `A4-Gate-C4a-2b2-G1-R1`｜真实执行证明与调用次数订正

用户原始订正：

> 1. 案例 1 当前留给后续单元；
>    只有Agent不能自洽时才进入 needs_manual。
>
> 2. 案例 2 是真实工具调用了，但业务结果没有成功，
>    所以进入 tool_failure。
>
> 3. 调用前记录step_before和logs_length_before；
>    真实执行后step == step_before + 1
>    len(logs) == logs_length_before + 1
>    新增日志的 event_type == "tool_call"；
>    执行前拒绝则step 不变
>    logs 长度不变
>    没有新增 tool_call 日志。
>
> 4. 模型总调用数 = 1
>    候选摘要 / Reflection / Refinement = 0 / 0 / 0

教学判定：`PARTIAL（只剩一个精度点）`。

- 第 2～4 点全部通过：已经准确区分“真实执行发生”与“业务结果失败”；能够用调用前后的 step / logs 精确证明来源；执行前拒绝保持三项不变；目标终点的模型总次数与三个下游调用数也完全正确。
- 第 1 点的“留给后续单元”正确，但“Agent 不能自洽”不是当前合同中的可观察条件，无法写成确定性测试。这里的精确终点是：未知工具 / 坏参数先在剩余 step 内进入 action correction；只有纠正仍不能产生合法动作并耗尽 `MAX_STEPS` 时，才返回 `needs_manual`。
- 当前唯一 `G1-R2`：只把“只有 Agent 不能自洽时”改写为包含 action correction 与 `MAX_STEPS` 的一条可测试规则。第 2～4 点不再重答；通过后立即建立 `C4a-2b2-I1` 唯一 TODO 骨架。

#### `A4-Gate-C4a-2b2-G1-R2`｜action correction 的可测试终点

用户原始订正：

> 案例 1 先在还有剩余 step内进入 action correction；
> 只有仍不能生成合法动作并耗尽 MAX_STEPS 时，才进入 needs_manual。

教学判定：`PASS`。

- 已把“Agent 不能自洽”的模糊描述收紧为可观察控制流：尚有 step 时修正动作；仍未生成合法动作并耗尽 `MAX_STEPS` 时才进入 `needs_manual`。
- `G1` 最终证据现已同时覆盖三个状态边界、真实执行来源证明、执行前零工具副作用、严格三字段和下游零调用。它只证明可以进入实现，不等于 `C4a-2b2` 或父 `A4-Gate` 正式 PASS。
- 助手按已通过判断建立唯一 `C4a-2b2-I1` 骨架：只接好调用前 step / logs 长度、窄内部信号签名、终点识别 helper 签名和公开入口窄捕获位置；三个 TODO 均保留 `NotImplementedError`，没有写入状态判断或三字段答案。骨架建立后 `py_compile` 与 `git diff --check` 均退出 0。

## 实现检查点

### `A4-Gate-C4a-2b2-I1`｜两个真实工具终点的窄信号与公开映射

当前在做什么：替换当前唯一检查点的三个 TODO，使最终一次工具结果在确有真实执行来源时，分别把不可重试失败和搜索成功零证据传到公开入口，再返回固定三字段结果。

为什么需要：当前不可重试工具失败仍被普通 `RuntimeError` 抛出；合法零命中则会继续调用候选摘要模型，浪费 step 并给模型凭常识补写的机会。G1 已证明不能只看结果字典，必须同时核对真实执行来源。

与前后关系：复用 C4a-2b1 已通过的“候选生成层精确识别 → 专用内部信号 → 公开入口窄映射”结构；不修改 C4a-1 原子调用账本、C4a-2a 一次重试或 C4a-2b1 `needs_manual`。本实现通过后才运行无落盘故障矩阵，未知动作恢复、HITL 与 eval 仍留后。

当前骨架位置：

1. `TerminalToolOutcomeError`：保存两个固定终点之一和构造用户说明所需的最小 detail；不得在异常类里直接返回公开结果。
2. `raise_for_terminal_tool_outcome()`：用传入的 `step_before / logs_length_before` 与当前 context 证明真实执行；只对两个精确终点发出专用信号，其他结果返回 `None` 并继续既有控制流。
3. `run_research_summary_once()` 的 sibling `except TerminalToolOutcomeError`：只捕获本专用类型，经 `make_result()` 返回对应严格三字段结果。

用户只完成这三个 TODO：

- 内部信号只允许 `tool_failure / insufficient_evidence`，保存非空 detail，并正确初始化父类；不接受任意状态，不泄漏 logs、request ID 或完整内部对象。
- 真实执行证明必须同时核对：step 恰好增加 1、logs 恰好增加 1、新增事件为当前工具的 `tool_call`，并且该日志属于同一请求 / 当前 step。不能根据 error 文案判断。
- `tool_failure` 只识别“当前最终结果来自真实工具执行、`ok is False`、`retryable is False`”；覆盖第一次直接失败和首败后第二次变为不可重试失败。执行前 action / schema 拒绝不得命中。
- `insufficient_evidence` 只识别“当前最终结果来自真实 `search_web`、`ok is True`、`results` 字段确实是 `list` 且长度为 0”。缺字段、`None`、空字符串、空字典不能用 falsy 判断冒充合法零命中。
- 公开映射的键必须恰好为 `status / summary / sources`；summary 为非空、用户可读字符串，说明真实失败或证据缺口与停止原因；无真实来源时 `sources=[]`。不得直接返回异常的 `__dict__` 或新增内部字段。

严格禁止修改：

- `execute_tool_call()` 的执行前校验、一次真实尝试、计时、七字段日志和原结果返回；
- 现有一次重试与 `AutoRecoveryExhaustedError → needs_manual` 分支；
- Tool Observation、第二模型、Reflection、Refinement 和最终 success validator；
- `prepare_request()`、公开函数签名、HITL、eval 或未知动作恢复。

可观察完成标准：

- 第一次真实不可重试失败：模型 1、工具 1、step / logs `[1,2]`，返回严格 `tool_failure`，零下游调用。
- 首败后第二次不可重试：模型 1、工具 2、step / logs `[1,2,3]`，summary 使用第二次真实错误，无第三次工具或下游模型。
- 搜索成功零命中：模型 1、工具 1、step / logs `[1,2]`，工具日志仍为成功 `error=None`，返回严格 `insufficient_evidence`，零下游调用。
- 未注册工具 / 坏参数不误映射；`results=None` 等畸形结构不误映射；工具 Python 异常保持原对象传播。
- 原五步成功、首败后成功六步、两种既有 `needs_manual`、七字段日志和请求隔离不回归。

用户完成口令：`改完了 C4a-2b2-I1 两个工具终点映射`。收到后由助手运行无落盘 fake / spy 与既有链回归，不新建 runner、baseline、报告或 `eval_cases.json`。

#### `A4-Gate-C4a-2b2-I1`｜用户首次提交与决定性复核

用户原始提交：

> 改完了 C4a-2b2-I1 两个工具终点映射

静态与卫生结果：

- `py_compile` 退出 0，模块语法可导入。
- `git diff --check` 未通过：源码第 699、706 行存在行尾空格。
- 改动范围没有触碰 `execute_tool_call()`、一次重试、`AutoRecoveryExhaustedError`、Tool Observation、第二模型、Reflection、Refinement 或 validator；窄 sibling `except` 和 `make_result(..., [],)` 的位置正确。

无落盘内存 fake / spy 端到端矩阵：`10` 个场景中 `3 PASS / 7 FAIL`。

| 场景 | 结果 | 决定性实际轨迹 |
| --- | --- | --- |
| 首次真实不可重试失败 | FAIL | 返回 `needs_manual` 而非 `tool_failure`；模型 1、工具 1；日志 `[1,2]`，但 context 变成 `step=3 / logs=2` |
| 首败可重试、第二次不可重试 | FAIL | 返回 `needs_manual` 而非 `tool_failure`；已正确采用第二次错误；日志 `[1,2,3]`，但 context 为 `step=4 / logs=3` |
| `search_web` 成功且 `results=[]` | FAIL | 返回 `needs_manual` 而非 `insufficient_evidence`；停止位置正确，但 context 为 `step=3 / logs=2` |
| 未注册工具执行前拒绝 | FAIL | 未误映射状态且真实工具 / 工具日志为 0，但 step 被从 1 改成 2 |
| `results=None` 畸形结果 | FAIL（轨迹） | 正确未映射为零证据并最终抛 `ValueError`，但日志 step 为 `[1,2,4]`、context step 4 |
| 原五步 success | FAIL（回归） | 公开结果仍 success，但日志 step 为 `[1,2,4,5,6]`、context `step=6 / logs=5` |
| 首败后成功六步链 | FAIL（阻断） | Refinement 前因步骤耗尽而失败；日志 `[1,2,3,5,6]`、context `step=6 / logs=5` |
| 两次可重试失败 `needs_manual` | PASS | step / logs `[1,2,3]`，使用第二次错误，无第三次工具或下游模型 |
| 重试前无剩余 step `needs_manual` | PASS | 注入 step 4 后日志 `[5,6]`，无第二次工具副作用 |
| 工具抛 `OSError` | PASS | 原异常对象传播，step / logs `[1,2]`，工具日志 `error="OSError"` |

直接最小探针进一步确认：

```text
执行前拒绝：检查前 step/logs = 1/1，检查后 = 2/1
真实工具失败：检查前已有 step/logs = 2/2，检查后 = 3/2
任意 status + 空 detail：异常类错误接受
正确 tool_failure 专用信号：公开端仍返回 needs_manual
```

教学判定：`C4a-2b2-I1 = RETRY`。

已做对：

- 第一次和第二次工具调用前都正确保存了 step / logs 长度；重试覆盖结果时也同步刷新快照，因此结构上能够使用第二次真实错误。
- helper 插在重试结束后的最终工具结果与普通失败守门之间，停止位置正确。
- 新公开分支只捕获 `TerminalToolOutcomeError`，没有宽泛吞掉模型协议、Python 或 validator 错误；`make_result()` 与 `sources=[]` 已接入。
- 两种既有 `needs_manual` 和工具 Python exception 未回归。

最早根因：

- `raise_for_terminal_tool_outcome()` 为了检查 step 又调用 `reserve_call_step()`，把纯读取判断变成了额外真实占步。这个“幽灵 step”没有对应日志，污染执行前拒绝、正常五步和恢复六步。
- 来源判断只检查“大于 / 变多”，没有要求 step / logs 恰好各增加 1，也没有核对新增日志的七字段、工具名、request ID 和当前 step。
- 专用异常接受任意状态与任意 detail；helper 又发出 `failure / success` 并传完整字典，而不是冻结的 `tool_failure / insufficient_evidence` 与非空安全字符串。
- 零证据判断没有限制 `tool_name == "search_web"` 和 `results` 的精确 list 类型；非终点还返回了 results，违反 `-> None`。
- 公开端把两个专用状态统一硬编码为 `needs_manual`，并使用包含内部字典的 `str(exc)` 作为 summary。

当前唯一订正 `C4a-2b2-I1-R1`：先只收口内部信号与来源识别，不修改公开 `except`。

1. `TerminalToolOutcomeError` 只允许 `tool_failure / insufficient_evidence`；`detail` 必须是去除两端空白后仍非空的 `str`。非法状态、空字符串和非字符串立即拒绝；合法时保存规范化字段并初始化父类。
2. helper 删除所有 `reserve_call_step()`；只读取 `current_step` 与现有 logs。真实执行必须满足：step 恰好为 `step_before + 1`、logs 恰好为 `logs_length_before + 1`，且唯一新增日志恰好七字段、`event_type="tool_call"`、`tool_name` 等于当前工具、`request_id` 等于 context、日志 step 等于 current step。任一不满足直接 `return None`。
3. 真实不可重试失败只在 `ok is False / retryable is False / error 为非空 str` 时发出 `tool_failure`，detail 只保存最终真实错误字符串。
4. 零证据只在当前工具精确为 `search_web`、`ok is True`、`results` 是 list 且长度为 0 时发出 `insufficient_evidence`，detail 使用非空用户说明；其他结果只 `return None`。
5. 删除两处行尾空格；不要修改快照接线、重试分支或公开映射。R1 只跑异常类与 helper 的直接矩阵；通过后再用 R2 单独收口公开三字段和完整回归。

用户完成口令：`改完了 C4a-2b2-I1-R1 内部信号与来源识别`。

#### `A4-Gate-C4a-2b2-I1-R1`｜用户首次订正与直接 helper 复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1 内部信号与来源识别

本轮严格只检查 R1 的异常类与 helper；公开入口仍故意保留给 R2，未把其当前 `needs_manual` 映射计入 R1 成败。

静态与卫生结果：

- 源码可 `compile()`，语法通过。
- `git diff --check` 仍未通过：源码第 699、706 行保留行尾空格。
- 改动没有越界触碰 `execute_tool_call()`、两处快照接线、一次重试、`AutoRecoveryExhaustedError`、Tool Observation、第二模型、Reflection、Refinement 或公开 `except`。
- 已删除 helper 中错误的 `reserve_call_step()`；直接探针确认所有本轮 helper 调用前后 context 均完全不变，上一版“幽灵 step”根因已修复。
- 已把两个内部状态名改为 `tool_failure / insufficient_evidence`，零命中也已限定当前工具为 `search_web`；非目标结果返回 `None`。

根代理从标准输入运行的无落盘直接矩阵为 `8 / 21 PASS`：

| 检查组 | PASS / 总数 | 决定性实际结果 |
| --- | ---: | --- |
| 异常类 | 2 / 5 | 两个合法状态可构造；未知状态、空 detail、字典 detail 仍被接受 |
| 目标与非目标工具结果 | 6 / 8 | preflight、`results=None / "" / {}`、非搜索空列表与正常非空搜索均正确无信号；两个目标终点都抛普通 `TypeError` |
| 畸形 provenance | 0 / 8 | step 跳增 2、logs 新增 2、缺 / 多字段、错误 event / tool / request / log step 全部穿过守门并触发 `TypeError`，没有 fail closed |

最早的可执行失败是 `TerminalToolOutcomeError` 仍未落实自己的构造合同：

- 构造器仍直接保存任意 `status / detail`，没有只允许两个状态，也没有要求 detail 为去空白后非空的字符串。
- helper 两处只传 `status`，没有传必填 `detail`。因此真实不可重试失败与 `search_web results=[]` 实际都会先抛出 `TypeError: ... missing 1 required positional argument: 'detail'`，不会发出预期专用信号。

来源证明也仍未完成：当前只检查 `step > step_before` 和 `len(logs) > logs_length_before`，没有证明二者恰好各 `+1`，也没有读取唯一新增日志并核对 `CALL_LOG_FIELDS`、`event_type / tool_name / request_id / step`。因此无关日志或跳步也能冒充当前真实工具执行。

教学判定：`C4a-2b2-I1-R1 = RETRY`。正确修复“helper 不得自己占 step”是实质进展，但异常信号尚不能成立，provenance 也尚不能排除假来源；这不是父 `A4-Gate` 的正式判定。

当前只做更小的 `C4a-2b2-I1-R1a`，先让“专用信号本身”成立，不同时修改 provenance 或公开入口：

1. `TerminalToolOutcomeError` 只允许 `tool_failure / insufficient_evidence`；否则立即拒绝。
2. `detail` 必须是 `str`，且 `strip()` 后非空；保存规范化后的值并用它初始化父类。未知状态、空白 detail、字典 detail 均必须拒绝。
3. 真实不可重试分支额外要求 `error` 是非空字符串，并把最终 `error.strip()` 作为 `tool_failure` 的 detail；不能漏传 detail，也不能传完整结果字典。
4. 零命中先取局部 `results`，只接受 `isinstance(results, list) and len(results) == 0`，并给 `insufficient_evidence` 传一个固定、非空、用户可读的 detail。
5. 本次不要改 step / logs provenance 守门和公开 `except`；前者留给紧接着的 R1b，后者仍留给 R2。

R1a 只验异常类的 5 个构造边界和两个目标信号 payload；用户完成口令：`改完了 C4a-2b2-I1-R1a 异常合同与目标信号`。

#### `A4-Gate-C4a-2b2-I1-R1a`｜用户提交与信号 payload 复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1a 异常合同与目标信号

本轮没有经过公开入口，也没有把尚待 R1b 完成的畸形 provenance 计入；只在一份满足当前基础 step / logs 条件的 context 上验证异常构造与目标 / 非目标 payload。源码语法通过，所有 helper 探针前后 context 均完全不变；`git diff --check` 仍在源码第 705、712 行报告行尾空格。

无落盘直接矩阵：`8 / 16 PASS`。

| 检查组 | PASS / 总数 | 决定性实际结果 |
| --- | ---: | --- |
| 异常类 | 4 / 5 | 两个合法状态均能把字符串 detail 去空白；普通未知状态和纯空白 detail 被拒绝；非空字典 detail 仍被接受 |
| 不可重试失败 payload | 0 / 5 | 正常最终错误、缺失 error、`None`、空白字符串、整数五种结果都发出 `tool_failure`，detail 均为完整 `tool_result` 字典 |
| 搜索零命中与非目标 | 4 / 6 | `None / "" / {}` 与非搜索空列表正确无信号；精确 `[]` 的信号 detail 是完整字典，自定义非 list 对象也能靠 `obj == []` 与 `len(obj) == 0` 冒充零命中 |

已做对：

- 普通未知字符串状态会被拒绝；两个合法状态名正确。
- 合法字符串 detail 会规范化，纯空白字符串会被拒绝，父类也已初始化。
- `ok / retryable` 继续使用 `is True / is False`；非目标搜索结果不发信号。
- 未修改 provenance、公开 `except`、原子工具调用或下游控制流，范围遵守。

最早根因是构造器当前先写“如果已经是字符串就 strip”，再写 `if not detail`。后一句只检查 truthiness（转成布尔值后真假），并不检查类型；非空字典、非空列表、整数 1 等都是真值，因此会被错误接受。两个 helper 分支又恰好把完整 `tool_result` 字典作为 detail，导致内部对象进入信号与异常字符串。

教学判定：`C4a-2b2-I1-R1a = RETRY`。当前先把剩余内容再拆成两个最小步骤，避免同时调异常类与两个业务分支。

当前唯一 `C4a-2b2-I1-R1a-R1`：只修改 `TerminalToolOutcomeError.__init__()`，helper 一行也不要改。

1. 先做状态类型与值守门：status 必须是字符串且只属于 `tool_failure / insufficient_evidence`。
2. 再做 detail 类型守门：只要 detail 不是字符串就立即拒绝；不能先看它是真值还是假值。
3. 类型通过后才执行 `detail = detail.strip()`；规范化后为空再拒绝。
4. 最后保存规范化后的 `status / detail` 并用它们初始化父类。

可观察完成标准：两个合法状态与带空白 detail 能构造并保存去空白值；未知状态、空白 detail、非空字典 detail、整数 detail 均立即拒绝。完成口令：`改完了 C4a-2b2-I1-R1a-R1 异常类类型门`。

#### `A4-Gate-C4a-2b2-I1-R1a-R1`｜异常类严格类型门复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1a-R1 异常类类型门

限定范围审计：相较上一版只修改 `TerminalToolOutcomeError.__init__()`；helper、provenance、公开 `except`、原子工具调用、重试与下游控制流均未再修改。本轮不把这些未完成位置计入判定。

无落盘构造器矩阵：`8 / 8 PASS`。

- 两个合法状态均构造成功，带制表 / 回车 / 换行与首尾空白的 detail 被规范化为纯文本。
- 实例是 `RuntimeError`；`status / detail / args / str()` 均使用规范化后的值，原始控制空白没有泄漏。
- 未知字符串 status、整数 status、字典 status、纯空白 detail、字典 detail、整数 detail 均由 `ValueError` 立即拒绝。
- 验证顺序已收紧为：status 类型 → status 精确值 → detail 类型 → `strip()` → 非空 → 保存 → 初始化父类；不再用 truthiness 代替类型验证。

教学判定：`C4a-2b2-I1-R1a-R1 = PASS`。这只证明专用异常自身的运行时合同成立，不代表 R1a、I1 或父 `A4-Gate` 已通过。

当前唯一 `C4a-2b2-I1-R1a-R2`：只修改 `raise_for_terminal_tool_outcome()` 中两个目标结果的条件与 payload；保持当前 provenance 守门原样，公开 `except` 仍不修改。

1. 不可重试失败：先取最终 `error`；只有 `ok is False`、`retryable is False`、`error` 是字符串且 `error.strip()` 后非空，才发出 `tool_failure`。detail 必须是最终 `error.strip()`，不能是完整 `tool_result`。
2. 搜索零命中：先取局部 `results`；只有当前工具精确为 `search_web`、`ok is True`、`isinstance(results, list)` 且 `len(results) == 0`，才发出 `insufficient_evidence`。detail 使用固定、非空、用户可读的证据缺口说明，不能是完整 `tool_result`。
3. 缺失 / `None` / 空白 / 非字符串 error，`results=None / "" / {}`，非搜索空列表，以及可伪装 `== []` 但不是 list 的自定义对象均返回 `None`；helper 不修改 context。
4. 不修改 step / logs 精确 provenance、公开入口、重试或下游控制流；这些继续分别留给 R1b 与 R2。

完成标准：在一份满足当前基础来源门的 context 上，11 个目标 / 非目标 payload 案例全部通过，专用异常不保存或输出完整 `tool_result`。完成口令：`改完了 C4a-2b2-I1-R1a-R2 目标分支与安全 payload`。

#### `A4-Gate-C4a-2b2-I1-R1a-R2`｜目标分支与安全 payload 复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1a-R2 目标分支与安全 payload

限定范围遵守：相较上一版只修改 helper 内的局部 `error / results` 提取、两个目标条件与 payload；没有修改异常类、provenance、公开 `except`、原子工具调用、重试或下游控制流。

约定的 11 项无落盘 helper 矩阵为 `10 / 11 PASS`；根代理另加一个“可与空列表比较但不支持 `len()`”的非 list 对象后，扩展矩阵为 `10 / 12 PASS`。所有 context 在 helper 前后均完全不变。

已通过：

- 不可重试失败 5 / 5：带空白最终错误正确发出 `tool_failure`，detail 精确为 `second-and-final-error`；缺失 / `None` / 空白 / 非字符串 error 均返回 `None`。
- `results=None / "" / {}`、非搜索工具的空列表，以及实现 `obj == []` 与 `len(obj) == 0` 但不是 list 的对象均没有误发信号。
- 两个分支都不再把完整 `tool_result` 作为 detail；工具失败信号及其父异常字符串未泄漏完整内部字典。

失败集中在搜索零命中一个分支：

1. 标准成功结果 `{"ok": True, "results": []}` 通常没有 `error` 字段；当前命中后用 `detail=error.strip()`，所以实际抛出 `AttributeError: 'NoneType' object has no attribute 'strip'`，没有发出 `insufficient_evidence`。
2. 当前先执行 `results == []` 和 `len(results)`，最后才检查 `isinstance(results, list)`。类型门执行太晚：一个能与空列表比较但没有长度协议的非 list 对象会先在 `len(results)` 抛 `TypeError`；list 子类也可能通过重载相等运算被错误拒绝。

教学判定：`C4a-2b2-I1-R1a-R2 = RETRY`。不可重试失败子分支已经取得教学 PASS 证据，不再重写；只订正搜索零命中。

当前唯一 `C4a-2b2-I1-R1a-R2-R1`：只修改搜索零命中的 `if` 与其 `raise`。

1. 删除 `results == []`；它既多余，又会调用自定义 equality。
2. 条件按安全短路顺序检查：当前工具是 `search_web` → `ok is True` → `isinstance(results, list)` → `len(results) == 0`。只有通过类型门之后才能调用 `len()`。
3. `insufficient_evidence` 的 detail 直接使用一条固定、非空、用户可读的证据缺口说明；完全不要读取成功结果中的 `error`，也不要包含完整结果字典。
4. 不修改已通过的 `tool_failure` 分支、provenance、公开入口或其他控制流。

完成标准：标准 `results=[]` 与空的 list 子类都发出 `insufficient_evidence`；`None / "" / {}`、非搜索空列表、可伪装相等的非 list（无论有无 `__len__`）均返回 `None` 且不抛异常；所有 context 不变。完成口令：`改完了 C4a-2b2-I1-R1a-R2-R1 搜索零命中安全分支`。

#### `A4-Gate-C4a-2b2-I1-R1a-R2-R1`｜搜索零命中安全分支复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1a-R2-R1 搜索零命中安全分支

限定范围遵守：相较上一版只修改搜索零命中的 `if / raise`；已通过的异常类与 `tool_failure` 分支保持原样，provenance、公开 `except` 和其他控制流未改。

无落盘搜索分支矩阵：`11 / 11 PASS`。

- 标准 `results=[]` 与一个重载 `__eq__` 为恒假值的空 list 子类均发出 `insufficient_evidence`。
- 两种合法形状的 detail 完全一致，为固定、非空、用户可读字符串；不读取 `error`，也不包含完整 `tool_result`。
- `None / "" / {}`、非搜索空列表和 `ok=1` 均返回 `None`。
- 一个能伪装 `== []` 且实现 `len()==0` 的非 list，以及一个能伪装相等但没有 `__len__` 的非 list，均返回 `None`；其 `__eq__ / __len__` 调用次数保持 0，证明类型门在这些操作之前短路。
- 所有 context 在 helper 前后完全不变；源码语法通过。

教学判定：`C4a-2b2-I1-R1a-R2-R1 = PASS`。结合先前构造器 `8 / 8`、不可重试 payload `5 / 5` 与本轮搜索矩阵，`C4a-2b2-I1-R1a = PASS`：专用异常合同和两个目标结果 payload 已闭合。该结论仍不包含真实执行 provenance 或公开三字段映射，因此 I1 / 父 Gate 均未通过。

当前唯一 `C4a-2b2-I1-R1b`：只替换 helper 最前面的 step / logs 来源守门；保持异常类、两个已通过的目标分支与公开 `except` 原样。

这里的 fail closed 是：任何来源事实缺失、类型不对或不精确时，不猜测“应该是真实工具”，只 `return None` 让现有未映射控制流继续。

真实执行必须同时证明：

1. `step_before / logs_length_before` 是非负的严格整数（不能让 bool 冒充 int）；当前 `run_context["step"]` 也是非负严格整数，且精确等于 `step_before + 1`。
2. `run_context["logs"]` 确实是 list，长度精确等于 `logs_length_before + 1`；不是“只要变多”即可。
3. 唯一新增项 `logs[logs_length_before]` 确实是 dict，并且键集合恰好等于 `CALL_LOG_FIELDS`，不能缺字段或加字段。
4. 新增日志必须同时满足：`event_type == "tool_call"`、日志 `tool_name` 等于当前 helper 参数、日志 `request_id` 等于 context 的非空字符串 request ID、日志 `step` 等于当前 context step。
5. 任一不满足都返回 `None`，且 helper 不得修改 step、logs 或任何 context 内容；不得调用 `reserve_call_step()`。

R1b 不修改两个业务终点条件，不修改公开入口。完成标准：有效第一次工具与重试后的第二次工具来源均能让已通过目标信号正常发出；step / logs 未增、跳增、多日志、非 list / 非 dict、缺 / 多字段、错误 event / tool / request / log step、畸形类型全部无信号且无异常，context 始终不变。完成口令：`改完了 C4a-2b2-I1-R1b 精确 provenance 守门`。

#### `A4-Gate-C4a-2b2-I1-R1b`｜精确 provenance 首次复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1b 精确 provenance 守门

范围方面：本轮只修改 helper 的 provenance 区，没有调用 `reserve_call_step()`，也没有修改已通过的异常类 / 两个业务分支、公开 `except`、原子工具调用、重试或下游控制流。`step_before / logs_length_before / current step` 的非负严格 int 守门和 logs 为 list 的守门已经写入，这些部分正确。

验证结果：

- 扩展 provenance 矩阵 `27 / 32 PASS`；最早失败是合法首次真实工具来源被静默返回。
- 根代理加入缺失 request ID、长度不足索引与实际 mutation 哨兵后的聚焦矩阵为 `5 / 15 PASS`。
- 源码语法通过；`git diff --check` 在当前第 714、733 行仍报告行尾空格。

决定性失败：

1. 精确条件极性写反：`step == step_before + 1` 和 `len(logs) == logs_length_before + 1` 正是合法来源，当前却在这两个条件成立时 `return`。因此合法首次工具、重试后的第二次工具和合法搜索零命中全部到不了业务 signal。
2. 当 step 与 logs 两项都不满足精确 `+1` 时，两次错误的提前 return 都不触发，代码反而直接进入业务判断；“均无增加”“均跳增 2”、缺字段和错误 event 的伪来源都实际发出了 `tool_failure`。
3. 新日志检查位于业务 signal 之后，不能对 signal 守门；而且使用 `new_log[...] = ...` 赋值，把错误 event / tool / request / step 改写成期望值。聚焦 mutation 哨兵确认 helper 调用后 context 已变化，这违反只读审计证据合同。
4. `run_context["request_id"]` 直接索引使缺字段实际抛出 `KeyError`；在未先证明日志长度精确的情况下索引 `logs[logs_length_before]`，长度不足实际抛出 `IndexError`。两者都应 fail closed 返回 `None`。

教学判定：`C4a-2b2-I1-R1b = RETRY`。当前严格整数 / logs 类型守门可保留，但完整来源证明、顺序与只读性尚未成立；不能进入 R2。

当前唯一 `C4a-2b2-I1-R1b-R1`：只重写 helper 中业务分支之前的 provenance 前缀，并删除业务分支之后旧的 `new_log` 赋值块；两个已通过的业务分支逐行保持原样。

正确控制顺序：

1. 先确认 `run_context` 是 dict；用 `.get()` 安全取得 `step / logs / request_id`，缺字段不得抛异常。
2. 严格验证非负 int 快照和 current step、logs 是 list、request ID 是非空严格字符串、当前 `tool_name` 是非空严格字符串。
3. **不满足** `current_step == step_before + 1` 时返回；**不满足** `len(logs) == logs_length_before + 1` 时返回。验证代码写的是“发现不匹配就拒绝”，不是“匹配就拒绝”。
4. 只有长度精确通过后才安全取得 `new_log = logs[logs_length_before]`；要求其为 dict 且键集合恰好为 `CALL_LOG_FIELDS`。
5. 对 `event_type / tool_name / request_id / step` 做只读严格类型和值比较；任一不匹配返回。绝不能用赋值“修正”日志。
6. 全部 provenance 通过后，才进入原有两个业务分支；删除业务分支后方当前那段 `new_log` 校验 / 赋值代码。

完成标准：合法首次、重试第二次和合法搜索三项均发出已通过 signal；所有伪来源、缺字段、短日志和畸形类型都返回 `None`、不抛异常；mutation 哨兵前后 context 深拷贝完全相同。完成口令：`改完了 C4a-2b2-I1-R1b-R1 provenance 拒绝顺序与只读比较`。

#### `A4-Gate-C4a-2b2-I1-R1b-R1`｜拒绝顺序与只读比较复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1b-R1 provenance 拒绝顺序与只读比较

范围遵守：本轮只修改 helper 的 provenance 前缀，没有改动已通过的异常合同、两个业务分支、公开 `except`、原子工具调用、重试或下游控制流；没有调用 `reserve_call_step()`，旧的日志赋值块已经删除。

无落盘 helper provenance / mutation 矩阵：`30 / 37 PASS`。三条合法哨兵全部通过：

- 首次真实不可重试失败发出 `tool_failure`，detail 为最终 `first-final-error`。
- 重试后的第二次真实不可重试失败发出 `tool_failure`，detail 为第二次最终 `second-final-error`。
- 真实 `search_web results=[]` 发出 `insufficient_evidence`，detail 为固定安全说明。

step / logs 无增加、单项未增加、单项或双项跳增、多日志、短日志、logs 非 list、错误 event / tool / request / log step，以及 context request ID 和三个整数事实的多种普通畸形输入均正确返回 `None`。全部 37 份可变输入在 helper 调用前后与深拷贝完全一致，证明本轮已修正拒绝极性、把现有字段比较放到业务 signal 前，并消除了显式 mutation。

剩余 7 个矩阵失败集中在安全形状门：

1. `run_context` 为 `None / list / str` 时，代码在类型门之前调用 `.get()`，实际抛 `AttributeError`。
2. context 缺少 `request_id` 时使用方括号直接索引，实际抛 `KeyError`。
3. 唯一新增日志不是 dict 时直接调用 `.get()`，实际抛 `AttributeError`。
4. 新增日志缺少 `duration_ms` 或多出 `extra` 字段时，当前四项比较仍可通过并错误发出 `tool_failure`；尚未证明键集合恰好等于 `CALL_LOG_FIELDS`。

额外严格类型探针发现：新增日志的 `event_type / tool_name / request_id` 若是重载相等运算的非字符串对象，以及 helper 的 `tool_name` 参数为非字符串但与日志值相等时，当前都会错误发出 `tool_failure`。这说明“比较结果相等”仍不能代替字段自身的严格类型证明。当前源码语法可导入；`git diff --check` 仍在 helper 的两个空白行报告行尾空格，这留到行为闭合后的机械清理，不作为本轮最早失败。

教学判定：`C4a-2b2-I1-R1b-R1 = RETRY`。本轮已经修对合法来源的通路、拒绝条件极性、检查顺序和只读性；不重写这些已通过部分。R1b 尚未 PASS，因此暂不进入公开映射 R2，父 `A4-Gate` 仍为 `DOING`。

当前唯一 `C4a-2b2-I1-R1b-R2`：只补 provenance 的安全形状门与严格字段绑定；保持当前精确 `+1`、只读顺序和两个业务分支逐行原样。

1. 在任何 `.get()` 前确认 `run_context` 是 dict；用 `.get()` 一次性取得局部 `current_step / logs / request_id`，缺字段只返回 `None`，不能抛异常。
2. 除现有快照 / current step 守门外，要求 helper 参数 `tool_name` 是非空严格字符串；request ID 也必须是非空严格字符串。后续比较只复用这些已验证的局部值。
3. 保留当前 step 与日志长度“不精确 `+1` 就返回”的正确极性。长度通过后才取得唯一新增项，先证明它是 dict，再证明 `set(new_log.keys()) == set(CALL_LOG_FIELDS)`。
4. exact keys 通过后，再分别要求日志 `event_type / tool_name / request_id` 是严格字符串、日志 `step` 是严格 int（拒绝 bool），随后只读比较其值与 `tool_call / 当前工具 / 当前请求 / current_step`；任一不满足都返回 `None`。
5. 不新增赋值，不调用 reserve，不修改已通过业务分支、公开入口或其他控制流。

完成标准：三条合法哨兵继续发出原 signal；上述 7 条失败全部变为正常 `None`；缺 / 多字段、非 dict 容器、缺 request ID、非字符串或相等运算伪装字段、非字符串工具参数均不抛异常、不发 signal；所有 context 仍完全不变。完成口令：`改完了 C4a-2b2-I1-R1b-R2 provenance 形状与严格字段绑定`。

##### R1b-R2 实现澄清｜`tool_result` 类型门检查的是谁

用户原始问题：

> 为什么if isinstance(tool_result, dict) is False:不行？

澄清：这条判断本身可以工作，也应保留；`isinstance()` 固定返回布尔值，因此 `is False` 能判断 `tool_result` 不是 dict，只是 `if not isinstance(tool_result, dict):` 更符合 Python 常用写法。当前 RETRY 不是因为这条语句无效，而是它只为 `tool_result` 建立类型门，不能证明另一个独立参数 `run_context` 是 dict，也不能证明稍后从 logs 取出的 `new_log` 是 dict。类型注解不会在运行时自动拦截畸形参数，所以必须分别在各对象第一次调用 `.get()` 之前验证各自的类型。

#### `A4-Gate-C4a-2b2-I1-R1b-R2`｜形状门与严格字段绑定复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1b-R2 provenance 形状与严格字段绑定

复核过程中第一次读取恰好撞到编辑器保存中间态，曾短暂看到不完整的 `isinstance()`；随后按稳定落盘版本重新读取并运行，当前第 725～726 行是完整的 `if not isinstance(new_log, dict): return None`，源码 `compile()` 通过。瞬时半文件不计入本轮判定。

范围遵守：本轮继续只修改 helper provenance 前缀；已通过的异常合同、两个业务分支、公开 `except`、原子工具调用、重试、调用前快照和下游控制流均未改。没有 reserve、日志赋值或 context mutation。

完整无落盘 helper-only provenance / mutation 扩展矩阵原始结果为 `43 / 57 PASS`。其中“任意未知但非空字符串工具名必须在 helper 再查注册表”属于当前冻结标准之外：真正的 `execute_tool_call()` 已在执行前完成注册表校验，而未知 action recovery 明确留给后续；剔除这一项后，本切片适用矩阵为 `43 / 56 PASS`，剩余 13 项失败。所有输入前后均与深拷贝一致。

本轮已通过：

- 首次真实不可重试、重试后的第二次不可重试和真实搜索零命中三条合法来源继续发出正确 signal。
- `tool_result / run_context` 非 dict、缺 step / logs / request ID、logs 非 list、step / logs 增量不精确、new_log 非 dict 等输入全部正常返回 `None`，不再抛 `AttributeError / KeyError`。
- 快照与 current step 的严格整数门、先长度后索引、精确 `+1` 极性、业务 signal 的后置顺序和完整只读性均保持正确。

剩余失败：

1. new_log 缺少 `duration_ms` 或多出 `extra` 时仍错误发出 `tool_failure`；当前只证明它是 dict，没有证明键集合恰好等于 `CALL_LOG_FIELDS`。
2. 日志 `event_type / tool_name / request_id / step` 仍只做宽松相等；自定义相等对象可以冒充前三个字符串或整数 step，`2.0 == 2` 也能让浮点 step 冒充整数。
3. helper 参数 `tool_name` 尚无精确非空字符串门；`None / bool / int / 空串 / 空白串 / 自定义相等对象` 与日志放入相同值时均会误发 signal。
4. context request ID 当前改为 `isinstance(request_id, str)`；普通非字符串已经能拒绝，但 str 子类仍会通过。根代理定向探针确认 request ID str 子类和 tool name str 子类都能误发 `tool_failure`，因此这里应继续遵守已冻结的“严格内置字符串”，而不是只证明“属于 str 家族”。

这里不扩张验证 `model / duration_ms / error` 的值类型，也不在 helper 重做 `TOOL_REGISTRY` 白名单：当前 R1b 只要求七个键恰好存在，并严格绑定能证明“本次、当前工具、当前请求、当前 step”的四个字段；其他日志值已由既有 `append_call_log()` 合同负责，未知 action recovery 仍是后续单元。

教学判定：`C4a-2b2-I1-R1b-R2 = RETRY`。安全容器形状、缺键读取、new_log dict 门、精确增量与只读性已经取得本轮证据，不再重写；R1b 尚未 PASS，仍不能进入公开映射。

当前唯一 `C4a-2b2-I1-R1b-R2-R1`：只补 exact keys 与四个绑定字段的严格类型，不修改已经通过的形状门、增量门或业务分支。

1. 在 provenance 前缀中要求 helper 参数 `tool_name` 为严格内置 str 且去空白后非空；不在这里追加注册表白名单。
2. context `request_id` 使用严格内置 str 门并保持非空；容器对象 `tool_result / run_context / logs / new_log` 继续使用当前 `isinstance` 形状门即可。
3. new_log 证明为 dict 后、读取任何字段前，要求其键集合与 `CALL_LOG_FIELDS` 恰好相等。
4. exact keys 通过后，分别证明日志 `event_type / tool_name / request_id` 是严格内置 str，日志 `step` 是严格内置 int（拒绝 bool / float）；随后再只读比较为 `tool_call / 当前工具 / 当前请求 / current step`。
5. 其余字段值类型、两个业务 payload、公开入口、重试和下游控制流均不修改。

完成标准：三条合法来源继续通过；缺 / 多键、四个绑定字段的自定义相等对象、float / bool step、非法或 str 子类工具名 / request ID 全部返回 `None`，不抛异常、不发 signal；所有 context 继续保持不变。完成口令：`改完了 C4a-2b2-I1-R1b-R2-R1 exact keys 与严格绑定类型`。

#### `A4-Gate-C4a-2b2-I1-R1b-R2-R1`｜exact keys 与严格绑定类型复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1b-R2-R1 exact keys 与严格绑定类型

范围遵守：本轮只修改 helper provenance；没有扩张注册表白名单，没有验证 `model / duration_ms / error` 的值类型，也没有修改两个业务分支、公开入口、重试或下游控制流。

无落盘 helper-only 冻结矩阵：`48 / 49 PASS`；全部 49 份 context 在调用前后与深拷贝一致。

已通过：三条合法来源、缺 / 多七字段、event / tool / request 的自定义相等对象与 str 子类、float / 自定义对象日志 step、非法 / str 子类 helper tool_name、str 子类 context request ID、所有普通形状 / 缺字段 / 精确增量回归均得到预期结果。`tool_name` 与 context request ID 的严格非空内置 str 门、exact keys，以及日志前三个字符串绑定字段的严格类型和值比较全部成立。

唯一失败位于日志 step：当前使用 `isinstance(log_step, int)`。Python 中 `bool` 是 `int` 的子类；在 current step 为 1 时，`isinstance(True, int)` 与 `True == 1` 同时为真，定向探针实际错误发出了 `tool_failure`。这不满足冻结的“日志 step 必须是严格内置 int”。

教学判定：`C4a-2b2-I1-R1b-R2-R1 = RETRY`。只剩一行类型门；其他 48 项不重写，R1b 仍未整体 PASS。

当前唯一 `C4a-2b2-I1-R1b-R2-R1-R1`：只把日志 step 的 `isinstance(..., int)` 收紧为与 current step / 两个快照一致的严格内置 int 判断，再保留现有值比较；不得改其他行。

完成标准：普通整数日志 step 与三条合法来源继续通过；`True / False`、float、int 子类和自定义相等对象均返回 `None`；49 项矩阵全部通过、context 不变。完成口令：`改完了 C4a-2b2-I1-R1b-R2-R1-R1 日志 step 精确 int 门`。

#### `A4-Gate-C4a-2b2-I1-R1b-R2-R1-R1`｜日志 step 精确 int 门复核

用户原始提交：

> 改完了 C4a-2b2-I1-R1b-R2-R1-R1 日志 step 精确 int 门

限定范围通过：相较上一稳定版本，只把日志 step 的 `isinstance(..., int)` 收紧为严格内置 int 门，随后保留与 current step 的值比较；异常类、其他 provenance、业务 signal、公开入口、重试和下游控制流均未修改。

无落盘 helper-only 冻结矩阵：`49 / 49 PASS`；全部 context 前后与深拷贝一致。普通内置 int 与三条合法来源继续通过；`bool / float / int 子类 / 自定义相等对象` 日志 step 全部在相等比较前拒绝。缺 / 多字段、严格字符串绑定、所有形状与增量边界也没有回归。

教学判定：`C4a-2b2-I1-R1b-R2-R1-R1 = PASS`；累计 `C4a-2b2-I1-R1b provenance = PASS`。结合已通过的 R1a 异常合同 / 两个安全 payload，累计 `C4a-2b2-I1-R1 = PASS`。这些结论只覆盖内部信号、目标条件与真实执行来源；公开结果尚未完成，因此整个 I1 与父 `A4-Gate` 均未通过。

当前唯一 `C4a-2b2-I1-R2`：只修改 `run_research_summary_once()` 的 `except TerminalToolOutcomeError`，把已验证的专用信号映射为公开固定三字段；不再修改异常类、helper、generate、重试或下游控制流。

现在在做什么：内部层已经能可靠地区分“真实不可重试失败”和“真实搜索成功但无证据”；公开入口必须把这两个内部信号翻译成调用者能使用的 `status / summary / sources`，而不能一律伪装成 `needs_manual`。

为什么需要：当前窄捕获类型和 `make_result()` / `sources=[]` 结构正确，但仍写死 `status="needs_manual"`，并把 `str(exc)` 的内部包装文本 `Terminal tool outcome: status=..., detail=...` 直接公开。它既违反冻结状态映射，也把内部信号表示当成了用户说明。

用户只修改当前 sibling `except TerminalToolOutcomeError`：

1. 使用异常已验证的 `exc.status` 作为公开状态；只能产生 `tool_failure` 或 `insufficient_evidence`。
2. 根据 `exc.status / exc.detail` 构造非空、用户可读的 summary：`tool_failure` 要说明真实工具失败且不可重试、最终错误以及已停止后续摘要生成；`insufficient_evidence` 要说明搜索成功但无真实证据、已经停止且不会凭模型常识补写。
3. 保持 `sources=[]`，并继续经 `make_result()` 返回，使顶层键恰好为 `status / summary / sources`。
4. 不公开 `str(exc)`、异常包装、异常 `__dict__`、logs、request ID 或完整 `tool_result`；detail 本身可作为安全信息嵌入用户说明。
5. 保持相邻 `AutoRecoveryExhaustedError → needs_manual`、普通异常传播、所有内部 helper 与正常链原样。

可观察完成标准：两个专用异常分别得到严格三字段 `tool_failure` 和 `insufficient_evidence`；summary 非空并含对应失败 / 证据缺口与停止原因，`sources=[]`；结果中不存在 `Terminal tool outcome:`、内部状态包装或额外字段；相邻 `needs_manual` 与普通异常行为不回归。完成口令：`改完了 C4a-2b2-I1-R2 公开三字段窄映射`。

#### `A4-Gate-C4a-2b2-I1-R2`｜公开三字段窄映射首次复核

用户原始提交：

> 改完了 C4a-2b2-I1-R2 公开三字段窄映射

范围遵守：本轮只修改 sibling `except TerminalToolOutcomeError`；内部异常、provenance、generate、重试、相邻 `AutoRecoveryExhaustedError` 和正常 / 下游控制流均未改。

无落盘 public 异常注入矩阵：`2 / 4 PASS`。按“public 先通过再跑完整集成”的顺序，在语义失败后停止，没有把尚未运行的完整 I1 fake / spy 冒充为证据。

已通过：

- 两个分支的机器状态分别为 `tool_failure / insufficient_evidence`，均通过 `make_result()` 返回 exact `status / summary / sources` 三字段，`sources=[]`。
- 不再公开 `str(exc)`、`Terminal tool outcome:`、内部状态包装、logs、request ID 或完整结果对象。
- 两条专用异常命中后下游调用为 0；相邻 AutoRecovery 仍返回 `needs_manual`，普通 `RuntimeError` 同一对象原样传播。

失败：两个分支都把字面占位语“非空、用户可读的真实失败与停止说明”当成实际 summary。注入 `tool_failure detail="FINAL_DISK_ERROR_SENTINEL"` 后，公开说明不包含最终错误，也没有说明真实工具不可重试与停止；`insufficient_evidence` 也没有说明搜索成功但零证据、停止和不凭模型常识补写。两状态的人类说明完全相同。

说明：前一轮示例结果中的这句话本意是描述“这里应放什么”，确实容易被当成可直接复制的字符串；本轮要把元描述替换为结合 `exc.detail` 的真实运行时内容。

教学判定：`C4a-2b2-I1-R2 = RETRY`。状态、三字段、空来源、窄捕获和安全不泄漏已经通过，不再改；只订正两个 summary。

当前唯一 `C4a-2b2-I1-R2-R1`：保持现有 `except`、状态分支、`make_result()` 与 `sources=[]`，只把两条占位 summary 换成真实用户说明。

1. `tool_failure` summary 必须使用安全的 `exc.detail`，说明工具确实已执行、结果不可重试、最终错误是什么，以及已停止后续摘要生成。
2. `insufficient_evidence` summary 必须使用安全的 `exc.detail`，说明搜索成功但没有可用于摘要的真实证据，已停止后续生成且不会凭模型常识补写摘要或来源。
3. 两条文案语义必须可区分；不得出现占位语、异常包装或内部对象。可在分支内构造 `summary_text`，最后统一 `make_result(exc.status, summary_text, [])`，也可保留两个 return。
4. 不修改任何已通过的内部 helper、状态条件、相邻 catch 或正常链。

完成标准：再次注入最终错误 sentinel 时，`tool_failure.summary` 实际包含该 sentinel 与不可重试 / 停止语义；`insufficient_evidence.summary` 实际包含搜索成功零证据 / 停止 / 不补写语义；其余 2 个已通过边界不回归。完成口令：`改完了 C4a-2b2-I1-R2-R1 两条真实用户 summary`。

#### `A4-Gate-C4a-2b2-I1-R2-R1`｜真实用户 summary 与累计集成复核

用户原始提交：

> 改完了 C4a-2b2-I1-R2-R1 两条真实用户 summary

public 无落盘异常注入：`4 / 4 PASS`。

- `tool_failure` 返回 exact 三字段、`sources=[]`，summary 明确工具已真实执行、不可重试，包含最终安全 detail 与停止说明。
- `insufficient_evidence` 返回 exact 三字段、`sources=[]`，summary 明确搜索成功但零证据、停止以及不凭模型常识补写摘要 / 来源。
- 相邻 AutoRecovery 仍为 `needs_manual`；普通 `RuntimeError` 同一对象原样传播。
- 两个专用公开终点的候选摘要、Reflection、Refinement 均为 `0 / 0 / 0`；没有内部异常包装或额外字段。

完整 public → generate → execute / helper → 下游无落盘 fake / spy 集成：`16 / 16 PASS`。

1. 首次真实不可重试：模型阶段 action / candidate / reflection / refinement=`1 / 0 / 0 / 0`，工具 1 次，step / logs `0 → 2`，返回 `tool_failure`。
2. 首次可重试、第二次不可重试：工具同参数执行 2 次，step / logs `0 → 3`，summary 只使用第二次最终错误，返回 `tool_failure`，下游 0。
3. `search_web results=[]`：模型阶段 `1 / 0 / 0 / 0`，工具 1 次，step / logs `0 → 2`，返回 `insufficient_evidence`。
4. 未注册工具与已注册工具参数 preflight 拒绝：均只有首次模型 step / log `0 → 1`，真实工具 0 次，没有伪造 `tool_call`，普通 `RuntimeError` 传播，不误映射。
5. `results=None / "" / {}`、非搜索空列表、自定义伪空列表对象均不误发专用状态，沿既有结构 / JSON 边界失败。
6. 正常成功链保持 `0 → 5`；首败后同参数重试恢复保持 `0 → 6`。
7. 二次可重试失败与重试前预算耗尽仍分别返回 `needs_manual`，轨迹为 `0 → 3` 与合法预置上下文 `4 → 6`，下游 0。
8. 工具 Python 异常在 `0 → 2` 后以同一异常对象传播，工具日志记录安全异常类型。
9. 双请求 context / logs 容器 / request ID 相互隔离；每个请求内部 ID 始终稳定。
10. 所有新增日志均为 exact 七字段、连续 step、正确 event / tool / request 绑定和非负内置整数耗时。

行为全绿后，助手只做机械清理：删除 3 个已完成 I1 TODO、两处 trailing whitespace，统一缩进 / 运算符空格 / 长条件换行，更新精确 `+1`、入口停止映射和函数 docstring 的过时说明，并在两句 summary 间加入分隔空格；没有修改业务条件。清理后独立重载复测仍为 public `4 / 4 PASS`、完整集成 `16 / 16 PASS`，内存 `compile()` PASS，`git diff --check` 退出 0；仅有 Git 的 LF → CRLF 提示，不是错误。

教学判定：`C4a-2b2-I1-R2-R1 = PASS`；累计 `I1-R2 = PASS`，结合既有 `I1-R1 = PASS`，整个 `C4a-2b2-I1 = PASS`。这只是当前学习实现检查点，不是父 `A4-Gate` 的正式 PASS。

### `A4-Gate-C4a-2b2-U1`｜用户最终审核

当前不再改代码。用户只需审核上述三条目标轨迹、unknown / preflight 不误映射、正常 / retry / needs_manual 回归与公开文案是否符合自己的设计意图；已经通过的 G1 判断不换措辞重考。

完成标准：若没有异议，明确确认这组行为与次数 / step / logs / 停止位置 / 三字段符合意图；若有异议，只指出具体场景与期望差异。确认口令：`审核通过 C4a-2b2 三条终点与回归矩阵`。

#### `A4-Gate-C4a-2b2-U1`｜用户最终审核确认

用户原始审核：

> 审核通过 C4a-2b2 三条终点与回归矩阵

教学判定：`C4a-2b2-U1 = PASS`。

- 用户已明确接受三条目标轨迹：首次真实不可重试失败、首次可重试后第二次真实不可重试失败、`search_web` 成功但 `results=[]`；对应公开状态、调用次数、step / logs、下游停止位置和严格三字段符合其设计意图。
- 用户同时接受 unknown / preflight 不误映射，以及正常五步、重试恢复六步、两种既有 `needs_manual`、工具 Python 异常和请求隔离的回归结果。
- 本确认只关闭当前教学单元。累计 `C4a-2b2-G1 / I1 / U1 = 教学 PASS`；不替代 `ai-agent-learning-review` 对父 `A4-Gate` 的正式检查，也不把 `tracker/progress.md` 中的父任务从 `DOING` 改为 `PASS`。

## 今日休息日双块收口

- 今日计划范围 / 今日剩余：第一块 `A4-Gate-C4a-2b1` 与第二块 `A4-Gate-C4a-2b2` 均已完成各自的判断、实现、无落盘验证和用户最终审核；2026-08-18 已授权的两块学习内容全部闭合，今日剩余为 0，不再启动第三块或新的 Gate 单元。300 分钟是全天计划容量，不记录或反推实际学习时长。
- 任务总剩余：父任务 `A4-Gate` 仍需未知工具 / 坏 JSON / schema / 参数错误的 action correction、候选摘要 / Reflection / 最终 validator 失败恢复、HITL allow / deny、14 条 `eval_cases.json`、五个 Gate 必答问题与正式 Gate 复核；这些都不等于今天还要继续完成，`G8-00` 也尚未启动。
- 下次 Session 起点（明确不属于今天）：在新的自然日先重新读取 `progress.md`、当日计划源与 `weak-points.md`，重新纳入单块不超过 180 分钟的预算；若事实源没有改变，则从“未知工具 / 坏 JSON / schema / 参数错误在剩余 step 内进入 action correction，耗尽 `MAX_STEPS` 才进入 `needs_manual`”的冻结合同与当前代码切入。到时再确定该学习单元编号并即时建立唯一 TODO 骨架，今天不提前创建。
- notes / 知识库：本代码切片不新建项目 notes；原始作答、订正和验证证据保留在本 daily。当前只是普通教学收口，未触发知识库自动同步。
