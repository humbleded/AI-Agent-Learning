# 2026-08-10 学习记录

- 当前任务：`A4-Gate` 研究总结 Agent
- 任务状态：`DOING`（S1 C3c 与 S2 的 C3d-C1 / C2a / C2b / C3a 已正式通过；A4-Gate 后续范围顺延）
- 第一块短续学计划预算：90 分钟
- 第一块预算拆分：C3c-R1 请求 / SDK 对象订正 20 分钟 + Observation / 第二响应 fake 协议 25 分钟 + 两条真实 DeepSeek 正常链 30 分钟 + 回归与交接 15 分钟
- 休息日第二块计划预算：180 分钟；其中 C3d 155 分钟，末段强制间隔回收 25 分钟
- 当天累计计划学习预算：270 分钟（第一块 90 + 第二块 180），低于休息日双块 360 分钟上限；今天最多两块，不再安排第三块
- 实际学习时长：不记录；助手读取、自动记录、测试运行和 API 等待不计入学习预算

## 当日 Session / 任务索引

| Session | 任务 | 范围 | 状态 |
| --- | --- | --- | --- |
| S1 | `A4-Gate` | C3c 真实 DeepSeek 两轮正常链续接 | 已完成今日范围 |
| S2 | `A4-Gate` | C3d Reflection、来源真实性 / 子集硬校验与最终三字段 | C3d-C1 / C2a / C2b / C3a 正式 `PASS`；模型 / JSON 集成顺延 |
| S3（第二块末段） | `RECOVERY_BLOCK_PENDING` | 25 分钟到期回收：`WP-15` → `WP-02` | 正式复核完成：二者升档；积压重算后重新签发未来 pending |

## Session S1｜A4-Gate-C3c 跨日续接

### 前序证据与边界

- 完整前序：`daily/2026-08-05.md`、`2026-08-06.md`、`2026-08-07.md`、`2026-08-08.md`、`2026-08-09.md`。
- 2026-08-09 已闭合：C3a 两工具注册 `35/35 PASS`；C3b 主矩阵 `14/14 PASS`、追加边界 `9/9 PASS`、清理后跨层回归 `13/13 PASS`。
- 2026-08-09 C3c 基线：宽松 / 忠实 fake 合并检查 `8/21 PASS`；首次 R1 仍混用旧 functions 协议，R1 子矩阵 `2/10 PASS`。
- 本次起点：`A4-Gate-C3c-R1-R1`，只核验第一请求封装；随后才处理 SDK 属性对象、tool_call_id、Observation 和第二响应。
- 今日仍不进入：Reflection、来源真实性 / 最终 success、日志、重试 / max_steps、HITL、14 条 eval。
- 2026-08-09 的强制回收块已在练习层完成 2/2，本日不短时重复出题；`weak-points.md` 的持久状态仍留给正式复核统一处理。

### `A4-Gate-C3c-R1-R1`｜第一请求封装回归

前一日要求：

1. 初始 messages 为 system + user，user JSON 使用 `ensure_ascii=False`。
2. 将 `build_tool_schemas()` 返回的 list 原样传给 `tools`，不传按名称索引的 dict。
3. 第一次请求包含 `model / messages / tools / stream=False / extra_body.thinking.disabled`，无顶层 `thinking`。
4. 本最小单元暂时允许第一响应仍因 `.get()` 报错；只检查请求 kwargs。

用户跨日提交：

> 改完了

静态观察：已加入 system + user、中文 JSON、原始 tools list、`stream=False` 与 `extra_body={"thinking":{"type":"disabled"}}`；第一响应后的 `.get()` 按约定仍未修，等待聚焦执行确认。

- 当前状态：待执行第一请求 6 项 fake 回归。

R1-R1 聚焦结果：`6/6 PASS`。

- model：PASS。
- tools 为 `build_tool_schemas()` 原始 list：PASS。
- `stream=False`：PASS。
- thinking 只通过 `extra_body={"thinking":{"type":"disabled"}}`：PASS。
- 初始消息为 system → user：PASS。
- user JSON 保留中文：PASS。
- 随后按预期在 `message.get("tool_calls")` 处出现 `AttributeError`；该错误不影响 R1-R1 判定，而是下一个已隔离单元。

- `A4-Gate-C3c-R1-R1` 判定：`PASS`。

### `A4-Gate-C3c-R1-R2`｜SDK message / tool_call 属性访问

- 当前唯一订正：
  1. 从 `response1.choices[0].message` 取得并保存第一响应对象。
  2. 使用 `message.tool_calls`，不使用 `message.get()`；必须恰好一个，否则抛出具体 `RuntimeError`，不能返回未完成的最终三字段对象。
  3. 使用 `tool_call.function.name` 与 `tool_call.function.arguments`；保留 `tool_call.id` 供下一单元回填。
- 暂时允许：执行 C3b 后，当前手工重建 assistant 字典的 `.get()` 仍会失败；本次只要求忠实 SDK 对象能完成选取并使 search spy 恰好执行 1 次。
- 完成标准：不再在第一 `message/tool_call` 读取处报错；fake 的真实工具调用次数为 1；下一处若停在 assistant/tool 回填，归入 R2。
- 用户订正：已使用 `message.tool_calls`、`tool_call.id`、`tool_call.function.name / arguments`；合法单调用可进入 C3b 并使 search spy 执行 1 次。
- R1-R2 子矩阵：`1/4 PASS`。
  - 恰好一个 Tool Call：PASS，SDK 属性读取与 C3b 执行正确。
  - None / 空 / 两个 Tool Calls：工具均未执行，但抛出 `ValueError`，与当前要求的明确 `RuntimeError` 不一致。
- 根因：分支中主动 `raise ValueError(...)`，外围却写 `except RuntimeError`；异常类型不同，except 永远不会捕获这些 ValueError。
- 当前判定：`PARTIAL`。属性层已通过，不重写；只修异常类型后回归 4 项。

### `A4-Gate-C3c-R1-R2-R1`｜统一协议偏差异常类型

- 当前唯一订正：无调用与多调用分支直接抛出带具体原因的 `RuntimeError`。这里无需先抛 ValueError 再捕获；可删除无作用的 try / except。
- 完成标准：None、空列表、多个调用均抛 RuntimeError 且工具调用 0 次；单调用继续执行 1 次，子矩阵 `4/4 PASS`。
- 用户订正：移除无作用的 try / except，无 / 多 Tool Calls 直接抛出具体 RuntimeError；SDK 属性访问保持不变。
- 回归：`4/4 PASS`。
  - 恰好一个：search spy 执行 1 次。
  - None、空列表、两个调用：均抛 RuntimeError，工具执行 0 次。
- R1-R2-R1 判定：`PASS`。

### `A4-Gate-C3c-R1`｜最终结果

- 第一请求封装 `6/6 PASS`；SDK Tool Call 与异常分支 `4/4 PASS`。
- R1 最终判定：`PASS`。真实 DeepSeek 仍未调用。
- 当前已证明：system/user、tools list、stream、non-thinking、SDK 属性链、恰好一个 Tool Call、name / arguments 与 C3b 执行。

### `A4-Gate-C3c-R2`｜assistant 原消息与 Tool Observation 回填

- 当前代码已保持 assistant → tool 的顺序，并让 content 使用 `json.dumps(..., ensure_ascii=False)`；这两项方向正确。
- 当前缺口：
  - assistant 被手工重建为 dict，而不是追加第一次 SDK 返回的完整 message 对象。
  - Tool Message 使用 `"id": tool_call_id`；协议要求字段名是 `"tool_call_id"`。
  - Tool Message 额外写了 `name`，本检查点最小合同不需要。
- 当前唯一订正：保存第一响应 message 变量并直接 `messages.append(message)`；tool 字典只保留 `role / content / tool_call_id`，ID 必须精确等于唯一 Tool Call 的 `id`。
- 完成标准：第二请求的 messages 恰好为 system → user → 原始 assistant 对象 → tool 字典；tool content 是保留中文的 JSON 字符串且反序列化后等于真实 tool_result；tool_call_id 精确对应。
- 用户订正：Tool Message 已改为精确 `tool_call_id`，content 使用 `ensure_ascii=False`；同时追加了原始 assistant_message，但没有删除旧的手工重建 assistant 字典。
- R2 fake：`8/11 PASS`。
  - Tool Message 键集合、ID、字符串类型、中文保留、JSON round-trip、真实工具单次执行全部 PASS。
  - 第二请求实际顺序为 system → user → 手工 assistant dict → 原始 assistant 对象 → tool，共 5 条；因此“恰好 4 条 / 第三条原对象 / 第四条 tool”三项 FAIL。
- 当前判定：`PARTIAL`。Observation 本身已通过，只剩重复 assistant 一个根因。

### `A4-Gate-C3c-R2-R1`｜删除重复 assistant

- 当前唯一订正：删除手工 `messages.append({"role":"assistant", ...})` 整块，只保留一次 `messages.append(assistant_message)`，随后追加 tool_result_message。
- 完成标准：第二请求 messages 恰好 4 条，顺序 system → user → 同一 assistant 对象 → tool；其余 8 个 PASS 保持，R2 达到 `11/11 PASS`。
- 用户订正：删除手工 assistant dict，只保留原始 assistant_message，再追加 tool_result_message。
- R2 回归：`11/11 PASS`。
  - 第二请求 messages 恰好 4 条，顺序 system → user → 同一 assistant 对象 → tool。
  - 原始 assistant 对象只出现一次。
  - Tool Message 键、精确 ID、字符串 / 中文、JSON round-trip 和工具单次执行全部保持 PASS。
- R2 最终判定：`PASS`。

### `A4-Gate-C3c-R3`｜第二请求与候选摘要边界

- 当前第二请求只有 `model / messages / tools`，缺 `stream=False`、non-thinking extra_body 和禁止继续选工具的 `tool_choice="none"`。
- 当前直接索引 choices 并返回 content；尚未把空 choices、第二次再次请求工具、content 为 None / 空 / 纯空白转换为明确 RuntimeError。
- 当前唯一订正：
  1. 第二次 create 使用与第一次相同的 model / messages / tools / stream / extra_body，并设 `tool_choice="none"`。
  2. 检查第二响应 choices / message；不得再有 tool_calls。
  3. content 必须是非空字符串（用 `.strip()` 判断）；否则抛出具体 RuntimeError。
  4. 返回已有中间三字段，不添加最终 status / sources。
- 可选同单元补强：第一响应 choices 为空时也转为具体 RuntimeError，避免泄漏 IndexError；不实现重试。
- 完成标准：正常候选、第二调用字段、无再次工具、None / 空白 content、空 choices 均通过 fake；随后做全协议回归，只有全绿才允许真实调用。
- 用户实现：第二次请求已补齐 `stream=False`、显式 non-thinking 与 `tool_choice="none"`；第二响应已拒绝再次 Tool Call，并校验 `content` 为非空字符串；第一响应也补上 choices 空值保护。
- R3 fake 矩阵：`13/15 PASS`。
  - 正常候选、第二请求字段、第一响应 choices 为 None / 空、第二响应 message 为 None、再次 Tool Call、content 为 None / 空串 / 纯空白均 PASS。
  - 第二响应 `choices is None` 时仍在 `response2.choices[0]` 泄漏 `TypeError`；`choices == []` 时泄漏 `IndexError`。
- 当前判定：`PARTIAL`。只剩第二响应在索引前缺少 choices 保护，不重做其余 13 项。

### `A4-Gate-C3c-R3-R1`｜第二响应 choices 保护

- 当前唯一订正：在读取 `response2.choices[0].message` 之前，先检查 `response2.choices is None or len(response2.choices) == 0`，命中时抛出说明“第二次模型调用未返回任何选择”的具体 `RuntimeError`。
- 用户报告“改好了”后读取磁盘文件，尚未看到该保护；聚焦复现仍为：None → `TypeError`，空列表 → `IndexError`。
- 完成标准：这两个分支都变为明确 `RuntimeError`，正常分支及另外 13 项保持，R3 达到 `15/15 PASS`。
- 用户第二次订正：把 `assistant_message2 = response2.choices[0].message` 移到了既有 message/content 校验之后，但仍未加入 `response2.choices` 的空值保护。
- 代表分支回归：正常候选、第二响应 choices=None、第二响应 choices=[] 三项均在首次读取尚未赋值的 `assistant_message2` 时泄漏 `UnboundLocalError`；语法编译通过，但执行顺序不成立。
- 当前唯一订正顺序：先保护 `response2.choices` → 再给 `assistant_message2` 赋值 → 最后执行现有 message / tool_calls / content 校验。
- 状态：`RETRY`，等待用户只调整这三段的先后关系并补上 choices 条件。
- 用户最终订正：已在读取 `response2.choices[0].message` 前加入 None / 空列表保护，执行顺序恢复为 choices 保护 → message 赋值 → message / tool_calls / content 校验。
- R3 完整回归：`15/15 PASS`。
  - 正常候选及第二请求的 model / tools / stream / non-thinking / `tool_choice="none"` 全部保持。
  - 第一、第二响应 choices 为 None / 空列表均转为明确 RuntimeError。
  - 第二响应 message 为 None、再次 Tool Call、content 为 None / 空串 / 纯空白均转为 RuntimeError。
- `A4-Gate-C3c-R3-R1` 判定：`PASS`；R3 最终判定：`PASS`。

### `A4-Gate-C3c`｜双路径正常链假客户端回归

- 对 `topic → search_web` 与 `relative_path → read_material` 两条路径执行同一组内存 fake / spy 协议验证；fake 只证明客户端控制流，不替代真实模型证据。
- 结果：`68/68 PASS`。
  - 每条路径恰好两次模型调用；第一次请求使用 model / tools / `stream=False` / 显式 non-thinking，无旧 functions 字段。
  - 初始消息恰好 system → user；用户 JSON 可逆且保留中文。
  - 第二次请求补充 `tool_choice="none"`；消息恰好 system → user → 原始 assistant 对象 → tool 字典。
  - 原始 assistant 只回填一次；Tool Message 只含 role / tool_call_id / content，ID 精确配对，中文 JSON 可逆并等于真实工具结果。
  - 两个 spy 分别只执行一次，实参精确为 `人工智能` 与 `sample.txt`。
  - 返回键恰好为 candidate_summary / tool_name / tool_result，不含最终 status / sources。
- 当前状态：两条正常链的协议主体闭合；真实调用前继续检查第一响应的无效结构，避免把正常样例通过误写成全部边界闭合。

### `A4-Gate-C3c-R4a`｜第一响应与 Tool Call 外壳校验

- 机械清理后语法编译与差异检查通过；独立只读审计发现第二响应已有完整保护，但第一响应尚有相邻的无效结构未拦截。
- 聚焦复现：
  - 第一响应 `message=None` → 泄漏 AttributeError；工具 0 次、模型 1 次。
  - 唯一 Tool Call 的 `function=None` → 泄漏 AttributeError；工具 0 次、模型 1 次。
  - `tool_call.id` 为 None、整数、空串、纯空白 → 当前都会执行工具 1 次、调用模型 2 次，并把无效 ID 回填。
  - 模型把规范化参数偷偷改写时，C3b 正确返回 `ok=False` 且工具 0 次，但当前 C3c 仍调用第二次模型并返回候选；这是后续单独的 R4b 正常链失败边界，不与本次外壳校验混改。
- 当前唯一订正：在任何工具执行前，依次保证第一 assistant message 存在、唯一 Tool Call 的 function 存在、tool_call_id 是非空字符串；任一失败都抛出具体 RuntimeError。
- 完成标准：上述 6 个无效外壳分支均为 RuntimeError，工具调用 0 次且第二模型调用 0 次；正常 ID 的双路径协议和 R3 `15/15` 保持。
- 用户首次实现：增加 assistant message / function / ID 校验，但把第一 assistant 的 `content is None` 也判成“没有消息”，且 ID 条件只拒绝“已经是字符串且为空”。
- R4a 聚焦矩阵：`4/7 PASS`。
  - PASS：message=None、function=None、ID 为空串、ID 为纯空白；均抛 RuntimeError，工具 0 次、模型 1 次。
  - FAIL：ID=None、ID=整数仍执行工具 1 次并调用模型 2 次；正常非空 ID 配合合法的 `assistant.content=None` 被错误拒绝，工具 0 次。
- 根因一：Tool Call assistant 的 content 可以为 None；只需判断 assistant_message 本身是否为 None。
- 根因二：ID 必须同时满足“是字符串”和“去空白后非空”；当前 `isinstance(...) and 空` 没有覆盖非字符串。
- 当前判定：`PARTIAL`。只改这两个布尔条件，再回归同一 7 项，不重写已通过的 function / 空 ID 分支。
- 用户最终订正：第一 assistant 只判断 message 本身是否为 None；ID 条件改为“非字符串或去空白后为空即拒绝”。
- R4a 回归：`7/7 PASS`。
  - message=None、function=None、ID=None / 整数 / 空串 / 纯空白均抛 RuntimeError；工具 0 次、模型 1 次。
  - 合法非空 ID 且 assistant.content=None 的正常 Tool Call 可执行工具 1 次并完成两次模型调用。
- 防回归：R3 `15/15 PASS`；topic / relative_path 双路径正常链 `68/68 PASS`；语法编译与差异检查通过。
- `A4-Gate-C3c-R4a` 判定：`PASS`。

### `A4-Gate-C3c-R4b`｜失败 Tool Observation 不得冒充正常候选

- 已复现：模型把 `query` 从规范化值改写后，C3b 正确返回 `{"ok": false, ...}`，真实工具执行 0 次；但 C3c 仍发起第二次模型调用，并接受非空候选摘要作为中间返回。
- 当前范围选择：完整纠错、重试、max_steps 与失败 Observation 回填留给后续控制器；当前函数只负责“两轮正常链”，因此遇到 `tool_result.ok is not True` 必须立即以 RuntimeError 停止，不能进入第二次摘要调用。
- 当前唯一订正：在 `execute_tool_call()` 返回后、构造 Tool Message 与第二次模型调用前检查结果的 ok 字段；不是严格 True 时抛出说明无法生成正常候选摘要的 RuntimeError。
- 完成标准：
  - 参数被模型改写 → 工具 0 次、模型 1 次、RuntimeError。
  - 已执行工具但工具返回 ok=False → 工具 1 次、模型 1 次、RuntimeError。
  - 两条 ok=True 正常链、R4a 7/7、R3 15/15 全部保持。
- 用户首次实现：检查位于 Tool Message 与第二次调用之前，但使用 `not tool_result.get("ok", True)`，会把缺失 ok 默认成成功，也会接受整数 1 等真值。
- R4b 聚焦矩阵：`3/5 PASS`。
  - PASS：参数被模型改写 → 工具 0 次 / 模型 1 次 / RuntimeError；工具返回明确 ok=False → 工具 1 次 / 模型 1 次 / RuntimeError；严格 ok=True → 工具 1 次 / 模型 2 次 / 正常候选。
  - FAIL：缺失 ok、ok=1 都被当作成功，继续调用第二次模型并返回候选。
- 当前判定：`PARTIAL`。只需把成功条件收紧为 `tool_result.get("ok") is True`；等价地，停止条件是 `is not True`，不再提供默认 True。
- 用户追问：`if not tool_result.get("ok", False)` 是否可替代；经说明确认该写法只检查 truthiness，会放过整数 1、非空字符串和非空容器，不能证明字段是严格布尔 True。
- 用户最终订正：改为 `if tool_result.get("ok") is not True`。
- R4b 完整矩阵：`5/5 PASS`。
  - 参数改写、明确 ok=False、缺失 ok、ok=1 均停止在第二次模型调用之前。
  - 严格 ok=True 保持正常两轮候选链。
- 防回归：R4a `7/7 PASS`、R3 `15/15 PASS`、topic / relative_path 双路径 fake 正常链 `68/68 PASS`；语法编译与差异检查通过。
- `A4-Gate-C3c-R4b` 判定：`PASS`。

#### R4b 概念追问｜`is not True` 与普通真假判断

- `if value` / `if not value` 会调用 Python 的真值规则；1、非空字符串、非空容器都属于 truthy。
- `value is True` / `value is not True` 检查对象身份；只有真正的布尔单例 True 通过严格成功合同。
- `value != True` 也不适合严格校验，因为 Python 中 `1 == True` 为 True；本边界使用 identity 是有意的协议类型校验，不是普通业务分支的风格偏好。

### `A4-Gate-C3c`｜真实 DeepSeek 双路径证据

两条真实调用均使用 `deepseek-v4-pro`、`stream=False`，并在两轮请求中显式传入 `extra_body={"thinking":{"type":"disabled"}}`；未输出 API key。

#### 真实链 1｜topic

- 规范化请求：`{"input_type":"topic","value":"人工智能"}`。
- 模型选择：`search_web`；客户端严格校验 query 与 value 相等后执行。
- 真实 Observation：`ok=True`，取得 3 条中文 Wikipedia 结果：
  1. 人工智能 — `https://zh.wikipedia.org/w/index.php?curid=317`
  2. 生成式人工智慧 — `https://zh.wikipedia.org/w/index.php?curid=8371968`
  3. 深度求索 — `https://zh.wikipedia.org/w/index.php?curid=8991178`
- 第二次模型调用成功；候选摘要包含概述、技术方法与跨学科性、生成式 AI、开源趋势、AGI 目标和上述 3 个来源链接。
- 返回键恰好为 candidate_summary / tool_name / tool_result，未包含最终 status / sources。

#### 真实链 2｜relative_path

- 规范化请求：`{"input_type":"relative_path","value":"sample.txt"}`。
- 模型选择：`read_material`；客户端严格校验 relative_path 与 value 相等后执行。
- 真实 Observation：

```json
{
  "ok": true,
  "content": "这是 T3-03 文件工具的沙箱测试文件。\n如果你能读到这行，说明正常读取成功。\n",
  "truncated": false
}
```

- 第二次模型调用成功；候选摘要明确说明该文件是 T3-03 沙箱测试文件，并以读取到的两行内容作为功能正常的证据。
- 返回键恰好为 candidate_summary / tool_name / tool_result，未包含最终 status / sources。

#### C3c 当前判定

- fake 协议与失败边界：全部通过。
- 真实模型选工具 → 客户端执行 → JSON Observation / tool_call_id 回填 → 模型候选摘要：topic 与 relative_path 两类输入均通过。
- 最终聚焦回归：C1 请求合同、C2 文件工具安全边界、C3a schema / 注册表、C3b 白名单与请求绑定、C3c fake smoke 合计 `20/20 PASS`；语法编译和差异检查通过。
- 模块 docstring 已机械更新为 C3c 完成；错误提示机械澄清为“第一次模型调用”和“Tool Call ID 必须是非空字符串”，未改变已验证控制流。
- `A4-Gate-C3c`：`PASS`。这里只闭合 Reflection 前的正常链中间产物，不等于 A4-Gate 完成，也不允许把候选对象标成最终 success。

#### C3c 协议追问｜为什么不能把 tool_calls 直接当 Tool Message

用户问题：

> `tool_result_message = {"role": "tool", "tool_call_id": tool_call_id, "content": json.dumps(tool_result, ensure_ascii=False)}` 为什么不直接用 tool_calls，而是要构建一个

教学说明：`assistant_message.tool_calls` 是模型发出的调用请求，包含调用 ID、工具名与模型生成的参数，但不含客户端真实执行结果；该完整 assistant message 已作为第三条消息回传。客户端按请求执行后才得到 `tool_result`，必须新建 `role="tool"` 消息，把结果序列化到字符串 content，并用 `tool_call_id` 指明它回复哪一个请求。直接再次放入 tool_calls 只是在重复“请调用工具”，不能形成 Observation。若有多个 tool_calls，客户端应为每个调用分别执行并生成一条对应 ID 的 tool message；本检查点合同只接受恰好一个。

- 当前状态：概念已说明；随后 R3、R4a、R4b 与真实双路径均已完成，本追问不改变 R2 `PASS`。

## 正式检查｜A4-Gate-C3c 与 2026-08-09 回炉块

### 覆盖矩阵与任务边界

| 范围 | 正式结论 | 决定性证据 / 未覆盖项 |
| --- | --- | --- |
| Problem Contract | 已覆盖 | 输入、输出、工具、正常链、失败恢复、日志、HITL 与 eval 蓝图已冻结；仍保持 DRAFT，不能代替实现证据 |
| C1 请求 / 结果合同 | 已覆盖 | 前序正式聚焦 `133/133 PASS`；本次 topic / path 规范化与非法输入冒烟无回归 |
| C2a / C2b 真实工具 | 已覆盖 | 文件沙箱与 Wikipedia 适配器前序闭合；本次真实文件读取和代表 HTTP 状态 / 结构回归通过 |
| C3a 工具注册 | `PASS` | 前序 `35/35 PASS`；本次 Schema / 注册表精确结构回归通过 |
| C3b 白名单执行 | `PASS` | 前序主矩阵 `14/14` + 边界 `9/9`；本次错工具、坏 JSON、字段漂移、错路由和参数改写均在工具前拒绝 |
| C3c 真实两轮正常链 | `PASS` | fake 双路径 `68/68`、R3 `15/15`、R4a `7/7`、R4b `5/5`、跨层 `20/20`；主审重新聚焦 `89/89`；真实 topic / relative_path 均通过 |
| C3d Reflection / 来源真实性 / 最终三字段 | 未覆盖 | 尚未把候选摘要与本轮允许来源集合做子集硬校验，也没有最终 `status / summary / sources` 成功对象 |
| 控制器 / 日志 / 恢复 / HITL | 未覆盖 | 尚无七字段结构化日志、统一 `max_steps=6`、工具失败最多一次重试、`needs_manual`、危险动作允许 / 拒绝 |
| A4 固定 eval | 未覆盖 | `code/stage4/eval_cases.json` 尚不存在；10 正常 + 3 失败 + 1 危险、至少 3 条 holdout 和预冻结阈值均未执行 |

这里的未覆盖项是 Gate 后续计划范围，不是 C3c 缺陷。因此正式状态是 `A4-Gate-C3c = PASS`、父任务 `A4-Gate = DOING`，不是 `RETRY`，更不能提前标成 `PASS`。

### 工程验证与决定性结果

- 基线 commit：`11886030b11ef9a22e1fce65a4b5ebf55f90fd0a`。
- 当前 A4 文件 SHA-256：`207D680926E25A7E4CF21353CC9EB99EB4BD48DC1D809DFA33012D43BD799C05`。
- 语法与导入：`.venv\\Scripts\\python.exe -W error -m py_compile code\\stage4\\a4_gate_research_summary_agent.py` 通过；无副作用导入输出 `IMPORT_OK ['read_material', 'search_web'] 2`。
- 主审无落盘聚焦命令：通过 PowerShell here-string 把 89 项内存 fake / spy 断言送入 `.venv\\Scripts\\python.exe -`；结果 `FORMAL_FOCUSED_PASS 89/89`。覆盖 C1/C2 代表边界、C3a 精确 Schema、C3b 严格分发、C3c 两条协议正常链、第一 / 第二响应无效外壳、参数改写和非严格 `ok=True`。
- 独立只读审计另跑无落盘矩阵：`FORMAL_INLINE_CHECKS=112/112 PASS`、`FAILED_IDS=[]`；与主审结论一致，不构成项目要求的跨工具岗位证据复核。
- 真实调用命令：通过 stdin 载入模块，对 `{"input_type":"topic","value":"人工智能"}` 和 `{"input_type":"relative_path","value":"sample.txt"}` 依次执行 `prepare_request()` 与 `generate_candidate_summary()`；两条均使用真实 `deepseek-v4-pro`。
- 真实 topic 复核：选中 `search_web`，`ok is True`，得到 3 条 Wikipedia 证据，候选摘要 907 字符，返回键恰好为 `candidate_summary / tool_name / tool_result`。
- 真实 path 复核：选中 `read_material`，`ok is True`，读取 41 个字符，候选摘要 295 字符，返回键同样恰好为上述三项。
- `git diff --check -- code/stage4/a4_gate_research_summary_agent.py daily/2026-08-09.md daily/2026-08-10.md`：无空白错误；仅有 Git 的 LF → CRLF 提示。

测试 / eval / 性能 / 安全摘要：fake 只用于可重复验证协议与故障控制流，没有冒充真实模型证据；topic 和 path 两条真实 DeepSeek 主链均重新运行。非法工具 / 参数、无效 Tool Call 外壳和失败 Observation 会在真实工具或第二次模型调用前停止，且没有输出 API key。本次未做性能 / 延迟基准；Reflection、最终来源、恢复循环、HITL 与正式 A4 eval 仍明确未执行。

### 工作场景与复合故障证据

故障包 A（`WS-01 + WS-03`）：旧 `functions / function_call`、错误 thinking 字段、把 SDK 对象当 dict、重复 assistant、错误 Tool Message 字段和空 choices 保护顺序均有真实复现；根因是混用旧协议、消息职责重复和索引前未保护对象。修复为当前 `tools / tool_calls`、两轮显式 non-thinking、原 assistant 只回填一次、按 ID 构造 Tool Message，并以 R1～R4a、双路径 fake、跨层回归和真实调用再次验证。`WS-01` 由 `EXECUTED → DIAGNOSED_AND_FIXED`；`WS-03` 保持 `DIAGNOSED_AND_FIXED` 并补充新证据。

故障包 B（`WS-07 + WS-05` 局部）：未知工具、坏 JSON、错字段 / 路由 / value、非法 Tool Call ID，以及 `ok=False`、缺少 `ok`、`ok=1` 曾被注入；根因是模型 Schema 不是客户端硬边界，以及 truthiness 不能证明严格布尔成功。修复为白名单 + 本轮规范化请求精确绑定、非空字符串 ID、只有 `tool_result.get("ok") is True` 才进入第二轮。再次注入后非法分支调用次数符合合同，正常 topic/path 无回归。`WS-07` 保持 `DIAGNOSED_AND_FIXED`；C3c 的 WS-05 局部只到 `DIAGNOSED_AND_FIXED`，全局既有 `RECOVERED_UNDER_FAULT` 不降级。

当前的 `RuntimeError` 是防止假成功的确定性止损，不等于自动重试、恢复、`needs_manual` 或 HITL。A4-Gate 强制的“模型 / 工具失败 → 七字段日志定位 → 重试或不可恢复判断 → 安全停止 → 再注入”复合事故尚未完成。

### 回炉台账正式重算

- `CD-001`：练习层 `PARTIAL → PASS`；首答已独立完成分类、B 的参数 / 路由、C 的跨请求边界与最小修复，定向追问未展示答案，遗漏的 A / C 存放位置由用户独立补齐。因此成功升至 `+1月`，下次回炉 2026-09-09。
- `CD-002`：练习层最终为提示后的 `PARTIAL → PASS`，但间隔 recall 未独立通过。首答跳过 Chat Template，第一次订正仍把 X 的模板 / tokenizer 接给 Y；完整 Y 归属链由助手明确校准后才被复述。因此移出核心定义池，合并为活跃 `WP-32`，不生成新间隔日期。
- `WP-01`：C3a～C3c 再次出现点名字段 / 分支未逐项落实，保持 🔴，最新证据更新为 2026-08-10。
- 新增 `WP-33`：两天内先后让 `bool` 冒充整数、让 truthy 值冒充协议成功；在没有独立迁移证据前记为 🔴。
- 重算后仍有 4 个到期项：`WP-02`、`WP-04`、`WP-15`、`WP-24`；最老到期日 2026-07-15。2026-08-09 的 pending 实例已完成，但积压仍达到阈值，故重新签发 `RECOVERY_BLOCK_PENDING`。2026-08-10 是未预留回收块的 90 分钟短续学，不追溯追加。

### Gate、算法、岗位与知识库边界

- Gate 预设阈值 / holdout：Problem Contract 已冻结 10 + 3 + 1 与至少 3 条 holdout；本次没有 `eval_cases.json`，不存在可报告的 A4 正式通过率，fake 断言数不能冒充 Gate eval。
- 算法内容块状态：本日无算法项；`tracker/algorithm-progress.md` 保持 W4 `PASS`、新题 4/4、债务 0，不启动下一算法内容块。
- 岗位证据候选：不适用。当前只是未完成 A4-Gate 的子检查点，没有完整 Gate、版本化 eval、恢复 / HITL 或岗位证据交叉复核；`job-readiness.md` 不更新，不产生 `JOB_EVIDENCE` 候选。
- 知识库同步：严格布尔边界已同步到 `D:\\AI-Knowledge\\02-Concepts\\Python\\真值判断与严格布尔校验(Truthiness).md`，并已登记 Python 索引；既有 `函数调用(Function Calling).md` 已准确覆盖“完整 assistant + 独立 role=tool + tool_call_id + 字符串 Observation”，本次 no-op，不创建重复概念页。

### 批改与小结

批改判定：`A4-Gate-C3c = PASS`；`A4-Gate = DOING`。

主审工具：Codex（`ai-agent-learning-review`）。

复核判定（普通任务默认留空；JOB_EVIDENCE、FINAL/J11-Gate、投递前必须由另一工具填写）：

当天进度小结：完成 C3c fake 协议、失败外壳与失败 Observation 边界，并重新跑通真实 DeepSeek topic / relative_path 两条正常链；今日 90 分钟短续学范围全部收口。

总进度小结：A4-Gate 已完成 Problem Contract、C1、C2a/C2b、C3a/C3b/C3c；仍缺 C3d Reflection / 来源真实性 / 最终输出、七字段日志、重试 / 停止 / `max_steps`、HITL、14 条 eval 与正式 Gate 复核。

对问题 / 不确定点的解释：`tool_calls` 是模型提出的调用请求，`role="tool"` 才是客户端执行后的 Observation 回复；二者职责、数据和消息位置不同，不能直接互换。`RuntimeError` 当前只防止正常链返回假候选，不等于最终恢复合同。

问答点评与补充：做得稳的是能在每次最小订正后保持既有 PASS，并最终让模型动作严格绑定用户的规范化请求、跑通真实双路径。主要薄弱点是初次实现容易沿用旧协议或 dict 习惯猜 SDK 对象，对 None / 空容器的保护顺序、重复消息和协议字段精确类型需要多轮故障注入才能闭合。

今天最虚 / 下次重点复习：版本化 SDK/API 的精确对象协议，以及“先保护容器 / 对象再索引、严格 `is True` 而不是 truthiness”的客户端边界。下次在 C3d 继续区分模型软评审与客户端来源硬校验，避免非空候选或虚构 URL 冒充最终 success。

超前内容提示：不并行启动日志、恢复、HITL 或 eval；先闭合 C3d 的来源子集门与 Reflection，再按合同顺序推进。

补救任务：C3c 无阻塞性补救，不重复运行同一正常链凑证据；下一检查点直接进入 C3d。

## 当前范围与交接

- 今日计划范围：闭合 C3c fake 协议，并在 fake 全部通过后真实运行 topic 与 relative_path 两条正常链。
- 今日计划范围完成：C3c fake 协议、失败边界、topic 与 relative_path 两条真实 DeepSeek 正常链均闭合。
- 今日剩余：无；不在本次 90 分钟短续学范围外启动 Reflection 或恢复循环。
- A4-Gate 任务总剩余：Reflection / 来源真实性与最终三字段合同、七字段日志、重试 / 停止 / max_steps、HITL、14 条 eval、正式 Gate 复核。
- 下次 Session 起点（不属于今天）：启动 C3d，把 candidate_summary 与本轮真实工具证据接入 Reflection，先验证摘要引用来源只能来自本轮 Observation，再决定是否允许构造最终三字段结果。

## Session S2｜休息日第二块：A4-Gate-C3d

### 状态演进与启动边界

- S1 收尾时的“今日剩余：无”和“下次 Session 起点（不属于今天）”是在尚未授权第二块时成立的真实历史状态，原记录保持不变。
- 用户随后明确表示今天不上班、状态好，并要求按休息日双块安排；在前序休息提示后回复“开始动手”，现正式启动第二块。本记录不编造或登记实际休息分钟。
- 第二块由 S2 的 155 分钟 C3d 交付范围与末段 S3 的 25 分钟回收范围组成；`A4-Gate` 继续保持 `DOING`，本 Session 不写正式任务判定。

### 前序证据接力

- [2026-08-05](2026-08-05.md) S3：A4-Gate 启动、官方资料定位与初始合同决策。
- [2026-08-06](2026-08-06.md) S2：Problem Contract 第 1～11 节、来源真实性、停止、日志与 HITL 设计。
- [2026-08-07](2026-08-07.md) S1：C1 固定结果与输入边界实现。
- [2026-08-08](2026-08-08.md) S1：C2a `read_material` 与 C2b `search_web` 真实工具适配。
- [2026-08-09](2026-08-09.md) S2：C3a / C3b 闭合并启动 C3c。
- 本页 S1：C3c fake 协议、失败外壳与真实 DeepSeek topic / relative_path 两条正常链正式闭合。
- 已覆盖的 C1、C2、C3a、C3b、C3c 不换措辞重考；当前唯一活动实现单元是 C3d。

### 资料、产物与本块边界

- `code/stage4/problem-contract.md`：只复核第 4 节输出合同、第 6 节正常成功路径第 5～6 步、第 8 节来源拒绝边界和第 11 节“软评审 + 硬校验”决定。
- `code/stage4/a4_gate_research_summary_agent.py`：从已通过的 `generate_candidate_summary()` 中间对象继续；主产物仍只修改这个文件。
- `code/stage4/a4_05_reflection_writer.py`：复用已 PASS 的 Reflection → Refinement → 同标准硬检查模式，不重讲 Reflection 定义。
- `code/stage2/pr2_04_extract_json.py`：复用已 PASS 的 JSON 模式、空内容保护、`json.loads` 与 Python 端校验边界。
- 不新建一次性 runner、baseline、原始报告或 eval 平台；`problem-contract.md` 继续保持 `DRAFT`，没有独立 notes 要求。

### 用户 / 助手分工

- 用户必须亲自完成：C3d 新控制流、真实 Observation 到允许来源白名单的映射、Reflection 候选与客户端硬门连接，以及相关故障的定位和修正。
- 助手自动完成：把已经冻结的合同整理成当前唯一 TODO 骨架；运行无落盘内存验证、真实调用与回归；保存原始实现、订正轨迹和结果。
- 用户最终审核：最终来源是否确实来自本轮 Observation，以及实现是否符合此前亲自确认的 Problem Contract。

### 本块计划范围与保护

- C3d 主范围 155 分钟：合同到实现映射 20 分钟、用户实现 85 分钟、故障注入与回归解释 30 分钟、真实双路径复核与交接 20 分钟。
- 第二块末段 25 分钟固定用于 `RECOVERY_BLOCK_PENDING`，已计入 180 分钟并等量压缩 C3d；C3d 若超出时间盒，剩余顺延，不能挤占回收块。
- 回收候选暂定 `WP-04`、`WP-15`，尚未出题；实际进入“开始今天的练习”后，由 `daily-practice-builder` 根据本块真实产物完成 preflight 再最终锁定，最多 2 项且严格一次一题。
- 当前没有新的回收作答、单项结果或清除证据，`RECOVERY_BLOCK_PENDING` 保持不变，只有正式检查可以重新计算。
- 不属于本块：七字段日志、统一 `max_steps` 控制器、工具重试 / 恢复 / `needs_manual`、HITL、`eval_cases.json`、14 条正式 eval、算法、下一阶段、知识库同步或另一套概念题。

### `A4-Gate-C3d-C1`｜从真实工具结果构造允许来源白名单

- 当前在做什么：先实现一个不调用模型的确定性函数，把 C3c 的规范化请求、实际工具名和真实工具结果转换成后续 Reflection / 最终校验唯一可使用的来源列表。
- 为什么需要：如果先让模型自由生成来源，再让模型自己声明来源真实，虚构 URL 仍可能伪装成非空成功结果；客户端必须先从 Observation 固定白名单。
- 与前后步骤的关系：C3c 已提供 `candidate_summary / tool_name / tool_result`；C3d-C1 只建立 Ground truth 白名单，之后才逐步加入 Reflection Prompt、结构化修订候选与最终三字段硬校验。
- 当前骨架：`derive_allowed_sources(normalized_request, tool_name, tool_result)`；合法输入返回保持工具顺序的 `list[str]`，结构、路由或来源字段非法时确定性拒绝。本检查点不生成摘要，也不返回最终 `success`。
- 可观察完成标准：内存输入至少覆盖合法 relative_path、合法 topic 两条 URL，以及一个路由或结构拒绝；不调用 DeepSeek、不调用真实工具、不改 C3c 正常链。
- 当前状态：TODO 骨架已创建，等待用户实现；尚无用户作答或教学检查点结果。

#### `A4-Gate-C3d-C1`｜用户第一次实现与聚焦诊断

用户提交状态：

> 改完了

用户第一次实现原样快照：

```python
def derive_allowed_sources(
    normalized_request: dict[str, str],
    tool_name: str,
    tool_result: dict[str, object],
) -> list[str]:
    if tool_result.get("ok") is not True:
        raise ValueError("工具结果不成功，无法构造来源白名单")
    if tool_name == "read_material":
        if normalized_request.get("input_type") != "relative_path":
            raise ValueError("工具名与 input_type 路由不一致")
        relative_path = normalized_request.get("value")
        if not relative_path:
            raise ValueError("规范化请求缺少 value 字段")
        return [relative_path]
    elif tool_name == "search_web":
        if normalized_request.get("input_type") != "topic":
            raise ValueError("工具名与 input_type 路由不一致")
        results = tool_result.get("results")
        if not isinstance(results, list):
            raise ValueError("工具结果缺少 results 字段或类型不正确")
        sources = []
        for item in results:
            url = item.get("url")
            if not isinstance(url, str) or url.strip() == "":
                raise ValueError("工具结果中的 url 字段非法")
            sources.append(url)
        return sources
    else:
        raise ValueError(f"未知的工具: {tool_name}")
```

首次聚焦矩阵：`C3D_C1_FOCUSED=10/13`。

- 已通过：relative_path 正常映射；topic 两条 URL 按工具顺序映射；`ok=1` 不能冒充严格布尔成功；两种路由错配；`results` 非列表；URL 非字符串 / 空白；未知工具。
- 失败 1：relative_path 的 `value=123` 被返回为 `[123]`，违反 `list[str]` 合同。
- 失败 2：relative_path 的 `value="   "` 被返回为空白字符串列表；普通 truthiness 不能证明“非空字符串”。
- 失败 3：`results=["not-a-dict"]` 在 `item.get()` 泄漏 `AttributeError`，没有按函数合同确定性转成 `ValueError`。
- 最小共同根因：在确认运行时对象的精确类型前，先使用了真值判断或字典 `.get()`；类型注解不会自动执行运行时校验。
- `results=[] -> []` 在本检查点不是缺陷：它只表示允许来源集合为空；禁止空来源返回最终 `success` 属于后续最终校验 / `insufficient_evidence` 边界。
- 语法可编译；`git diff --check` 另报告未知工具分支有一个行尾空格，属于后续助手机械清理，不是行为根因。

教学检查点状态：`RETRY`。两条正常映射和多数拒绝边界已闭合；当前只做 R1：在读取路径值后确认它是去空白后非空的字符串，并在搜索条目调用 `.get()` 前确认条目是字典。不得改动正常映射、来源顺序、严格 `ok is True` 或 C3c。

#### `A4-Gate-C3d-C1-R1`｜用户第一次订正与复跑

用户提交状态：

> 改完了

用户订正后的关键实现原样快照：

```python
relative_path = normalized_request.get("value")
if isinstance(relative_path, str) and relative_path.strip() == "":
    raise ValueError("规范化请求缺少 value 字段")
if not relative_path:
    raise ValueError("规范化请求缺少 value 字段")
return [relative_path]

for item in results:
    if not isinstance(item, dict):
        raise ValueError("工具结果中的条目不是字典类型")
    url = item.get("url")
    if not isinstance(url, str) or url.strip() == "":
        raise ValueError("工具结果中的 url 字段非法")
    sources.append(url)
```

R1 聚焦矩阵：`C3D_C1_FOCUSED=12/13`。

- 已修复：`value="   "` 现在抛出 `ValueError`；`results=["not-a-dict"]` 现在也按函数合同抛出 `ValueError`，不再泄漏 `AttributeError`。
- 保持通过：两条正常映射、来源顺序、严格 `ok is True`、两种路由错配、`results` 类型、URL 类型 / 空白和未知工具均未回退。
- 唯一剩余失败：relative_path 的 `value=1` 仍返回 `[1]`，没有满足返回 `list[str]` 的运行时合同。
- 根因：`isinstance(relative_path, str) and relative_path.strip() == ""` 只会拒绝“已经是字符串且为空白”的值；整数 `1` 会让前半段为假，并且又能通过后面的普通 truthiness 检查。
- 最小订正语义：只保留一个联合非法判断——“不是字符串 **或** 去空白后为空”时拒绝；不要再用第二个普通 truthiness 判断承担类型校验。
- 工程结果：语法编译通过；`git diff --check` 仍只报告未知工具分支的一个行尾空格，属于助手机械清理项，不是行为缺陷。

教学检查点状态：`RETRY`。本次订正已闭合 2 个原失败项；当前只做 R2 的一处条件修正。先验证 `value=1`、`value="   "`、`value="sample.txt"` 三项，再复跑完整矩阵；通过前不创建 Reflection 的下一个 TODO 骨架。

#### `A4-Gate-C3d-C1-R2`｜用户第二次订正与检查点收口

用户提交状态：

> 改完了

用户 R2 关键实现原样快照：

```python
relative_path = normalized_request.get("value")
if not isinstance(relative_path, str) or relative_path.strip() == "":
    raise ValueError("规范化请求缺少 value 字段")
return [relative_path]
```

- 决定性三项：`value=1 -> ValueError`、`value="   " -> ValueError`、`value="sample.txt" -> ["sample.txt"]`，结果 `3/3 PASS`。
- 完整聚焦矩阵：`C3D_C1_FOCUSED=13/13`；两条正常映射、来源顺序、严格成功、路由、路径类型、结果 / 条目 / URL 结构和未知工具全部通过。
- 独立只读扩展矩阵：`16/16 PASS`，另覆盖缺失 `ok`、空结果和输入对象不被修改；没有发现 C3c 主体回归。
- 工程验证：语法编译通过。助手随后只做机械清理：移除已完成的 C3d-C1 TODO、统一循环缩进、清除行尾空格，并把模块说明切换到下一个活动检查点；这些整理不冒充用户理解证据。

教学检查点状态：`PASS`。用户通过两轮真实订正证明了：类型注解不执行运行时校验；要先证明对象类型，再调用该类型的方法或依赖其返回合同；普通 truthiness 不能替代“类型 + 非空”的精确边界。

### `A4-Gate-C3d-C2a`｜构造 Reflection 的完整证据输入

- 当前在做什么：实现 `build_reflection_prompt(...)`，把规范化请求、候选摘要、实际工具名、本轮完整 Observation 和 C3d-C1 的允许来源列表组织成一个 Reflection 文本输入。
- 为什么需要：Reflection 是模型做的“软评审”，只有看见完整 `content / results` 才能判断摘要结论是否有证据；只给 URL 白名单只能证明来源边界，不能证明正文结论受证据支持。
- 与前后步骤的关系：C3d-C1 已固定模型最多可以使用的来源；本检查点只准备评审输入。通过后才开放 Reflection 模型调用与 Refinement，最后仍由 Python 做三字段和来源子集硬校验。
- 用户要做：完成当前唯一 TODO 函数，让返回的 prompt 明确包含五类输入，并写清三类评审标准；有问题时要求逐项输出“问题、证据、修改建议”，全部通过时只输出精确短语“无需改进”。可复用本文件已通过的 `json.dumps(..., ensure_ascii=False)`，不要让 Python 字典的显示形式替代明确的 Observation 序列化。
- 示例边界：测试可给五类输入分别放入独特 sentinel，确认它们都真实出现在返回字符串中；函数不得调用 DeepSeek / 工具，不解析 JSON，不修订摘要，也不构造最终 `success`。
- 可观察完成标准：五类输入、三项评审标准和两种反馈协议都能从实际返回 prompt 中观察到；模型调用与工具调用均为 0。此检查点不要求 prompt 文案逐字一致，但职责和边界必须完整。
- 当前状态：单一 TODO 骨架已创建，等待用户实现；尚未创建 Refinement、最终 validator、控制器、日志、恢复、HITL 或 eval 骨架。

#### `A4-Gate-C3d-C2a`｜用户第一次实现与聚焦诊断

用户提交状态：

> 改完了

用户第一次实现原样快照：

```python
def build_reflection_prompt(
    normalized_request: dict[str, str],
    candidate_summary: str,
    tool_name: str,
    tool_result: dict[str, object],
    allowed_sources: list[str],
) -> str:
    if not isinstance(candidate_summary, str) or candidate_summary.strip() == "":
        raise ValueError("候选摘要必须是非空字符串")
    if not isinstance(tool_name, str) or tool_name.strip() == "":
        raise ValueError("工具名必须是非空字符串")
    if not isinstance(tool_result, dict):
        raise ValueError("工具结果必须是字典类型")
    if not isinstance(allowed_sources, list) or not all(isinstance(s, str) for s in allowed_sources):
        raise ValueError("allowed_sources 必须是字符串列表")
    if tool_name == "read_material":
        if "content" not in tool_result:
            raise ValueError("工具结果缺少 content 字段")
    elif tool_name == "search_web":
        if "results" not in tool_result:
            raise ValueError("工具结果缺少 results 字段")
    if "sources" in tool_result:
        for source in tool_result["sources"]:
            if source not in allowed_sources:
                raise ValueError(f"工具结果中的来源 {source} 不在 allowed_sources 中")
    review_instructions = (
        "请逐项检查候选摘要是否由本轮工具结果支持，"
        "确保来源仅来自 allowed_sources，"
        "并检查修订候选能否满足 status / summary / sources 三字段成功合同。"
        "有问题时逐项输出‘问题、证据、修改建议’；全部通过时只输出精确短语‘无需改进’。"
    )
    prompt = {
        "normalized_request": normalized_request,
        "candidate_summary": candidate_summary,
        "tool_name": tool_name,
        "tool_result": tool_result,
        "allowed_sources": allowed_sources,
    }
    prompt["review_instructions"] = review_instructions
    return json.dumps(prompt, ensure_ascii=False)
```

无模型 sentinel 聚焦矩阵：`C3D_C2A_SENTINEL=11/17`；语法编译通过。

- 已通过：返回可解析的字符串；五类输入均能 JSON round-trip；已要求摘要由本轮结果支持、来源仅来自 allowlist；两种反馈协议均进入实际 prompt；模型与工具 spy 调用数均为 0。
- 缺口 1：`content / results`、禁止使用模型常识、以及“allowlist 不能替代正文证据”仍只存在于 Python TODO 注释，没有进入模型实际收到的 `review_instructions`。
- 缺口 2：只点名“status / summary / sources 三字段成功合同”，却没有把合同内容写入 prompt。Reflection 不会自动读取 `problem-contract.md`；实际文本必须说明 `status` 是字符串 `success`、`summary` 是非空字符串、`sources` 是非空字符串列表且为 `allowed_sources` 的子集。
- 层级混淆：`tool_result["sources"]` 检查放错对象。真实 `read_material` 工具结果提供 `content`，`search_web` 提供 `results[*].url`；顶层 `sources` 属于后续最终候选对象。该硬校验应留给后续 validator，本 prompt builder 不应检查工具结果中不存在的最终字段。
- 最小共同根因：把必须交给模型的规则留在代码注释或外部合同名称中，同时把最终对象与工具 Observation 的字段层级混在一起。

教学检查点状态：`RETRY`。当前只做 R1：保留五字段 payload、已有输入保护和 JSON 序列化；删除 `tool_result["sources"]` 检查块，并把上述完整规则真正写进 `review_instructions`。通过前不创建 Refinement 或最终 validator 骨架。

#### `A4-Gate-C3d-C2a-R1`｜用户订正与检查点收口

用户提交状态：

> 改完了

用户 R1 的决定性修改原样快照：

```python
review_instructions = (
    "请逐项检查候选摘要是否由本轮工具结果支持，"
    "摘要结论只能依据本轮 read_material.content 或 search_web.results"
    "不得使用模型常识补充工具没有取得的事实。"
    "allowed_sources 只能证明来源来自本轮 Observation，不能替代正文证据。"
    "成功候选的具体合同："
    "status 必须是字符串 \"success\"；"
    "summary 必须是非空字符串；"
    "sources 必须是非空字符串列表；"
    "sources 必须是 allowed_sources 的子集。"
    "有问题时逐项输出‘问题、证据、修改建议’。"
    "全部通过时只输出精确短语‘无需改进’。"
)
```

- 错层的 `tool_result["sources"]` 检查已由用户删除；工具 Observation 与最终候选字段不再混用。
- 主 sentinel 矩阵：`C3D_C2A_SENTINEL=17/17`；五类输入、证据限制、禁止模型常识、allowlist / 正文职责边界、具体三字段合同和两种反馈协议均进入实际返回文本。
- relative_path 迁移：真实 path 形状中的 `content` 与 `allowed_sources=["sample.txt"]` JSON round-trip 通过。
- 纯函数边界：模型客户端和工具 spy 调用数均为 0；语法编译通过。
- 助手机械收尾：移除已完成 TODO、规范注释 / 条件换行，并在相邻字符串之间补句号；这些格式整理不冒充用户理解证据。

教学检查点状态：`PASS`。用户已证明模型只能依据实际 prompt 上下文执行规则：代码注释和外部合同名称不能替代模型可见的具体约束；同时能把 Observation、allowlist 与最终候选三层职责分开。

### `A4-Gate-C3d-C2b`｜依据 Reflection 反馈构造结构化候选 prompt

- 当前在做什么：实现 `build_refinement_prompt(...)`，把前五类真实输入和 Reflection 的 `feedback` 组织成 Refinement 文本，要求模型只返回一个结构化三字段候选。
- 为什么需要：Reflection 只指出问题，不会自动产生客户端可解析的最终对象；这一层把“审稿意见”交给“改稿者”，但最终是否可信仍由后续 Python validator 决定。
- 与前后步骤的关系：C2a 已固定评审输入和反馈协议；C2b 只固定修订输入与 JSON 输出协议。通过后才开放真实模型请求、`json.loads` 和最终硬校验。
- 用户要做：完成当前唯一 TODO 函数，让实际 prompt 包含 `normalized_request / candidate_summary / tool_name / tool_result / allowed_sources / feedback` 六类输入。反馈精确为“无需改进”时保留原摘要事实含义但仍整理成三字段 JSON；反馈有问题时只依据反馈与本轮证据修订。
- 输出与证据边界：结论只能来自本轮 `content / results`，不得补模型常识；`sources` 只能从 `allowed_sources` 选择，不得扩大白名单。只输出一个 JSON 对象，前后不得有解释文字或 Markdown 围栏，键恰为 `status / summary / sources`；实际 prompt 内必须给出三字段 JSON 示例。
- 成功合同：`status == "success"`；`summary` 为非空字符串；`sources` 为非空字符串列表并且是 `allowed_sources` 的子集。这里仍只是给模型的软约束，不能替代后续 Python 硬门。
- 可观察完成标准：六个唯一 sentinel 都进入实际返回字符串；反馈两分支、证据限制、精确三字段 / 类型 / 非空 / 子集、JSON-only 和禁止额外文本协议均可观察；模型与工具调用为 0。
- 当前状态：单一 TODO 骨架已创建；尚未加入模型请求、`response_format`、`json.loads`、最终 validator、日志、恢复、HITL 或 eval。

#### `A4-Gate-C3d-C2b`｜用户第一次实现与聚焦诊断

用户提交状态：

> 改完了

用户第一次实现原样快照：

```python
def build_refinement_prompt(
    normalized_request: dict[str, str],
    candidate_summary: str,
    tool_name: str,
    tool_result: dict[str, object],
    allowed_sources: list[str],
    feedback: str,
) -> str:
    refinement_instructions = (
        "请根据 Reflection 反馈对候选摘要进行修订，"
        "修订应严格依据本轮工具结果，不得添加模型常识。"
        "sources 必须从 allowed_sources 中选择，不能扩大白名单。"
        "输出必须是一个 JSON 对象，键为 status / summary / sources，"
        "示例：{\"status\": \"success\", \"summary\": \"...\", \"sources\": [\"...\"]}。"
        "成功候选必须满足：status == \"success\"，summary 为非空字符串，"
        "sources 为非空字符串列表且是 allowed_sources 的子集。"
        "如果反馈为‘无需改进’，保留候选事实含义；"
        "其他反馈只依据本轮证据修订。"
    )
    prompt = {
        "normalized_request": normalized_request,
        "candidate_summary": candidate_summary,
        "tool_name": tool_name,
        "tool_result": tool_result,
        "allowed_sources": allowed_sources,
        "feedback": feedback,
    }
    prompt["refinement_instructions"] = refinement_instructions
    return json.dumps(prompt, ensure_ascii=False)
```

无模型 sentinel 聚焦矩阵：`C3D_C2B_SENTINEL=18/22`；语法编译通过。

- 已通过：返回字符串；六类输入完整 JSON round-trip；“无需改进”与有问题反馈两分支；禁止模型常识；来源只从 allowlist 选择且不得扩大；JSON 示例；成功三字段的值 / 类型 / 非空 / 子集；模型和工具 spy 调用数均为 0。
- 缺口 1：实际 Refinement 文本只写“本轮工具结果”，没有明确点名证据字段只能是 `read_material.content` 或 `search_web.results`。
- 缺口 2：写了“输出必须是一个 JSON 对象”，但没有明确“只输出 JSON、前后不得有额外解释文字”。模型仍可能返回 `下面是结果：{...}`。
- 缺口 3：没有明确禁止 Markdown 代码围栏，模型仍可能返回被 `````json`` 包裹的对象，导致后续直接 `json.loads` 失败。
- 缺口 4：“键为 status / summary / sources”没有排除额外键；应明确键恰为这三项或对象仅包含这三项。
- 最小共同根因：自然语言方向已经正确，但机器消费协议仍有歧义；模型可生成“人看起来像 JSON、客户端却不能直接解析或合同字段漂移”的输出。

教学检查点状态：`RETRY`。当前只做 R1：保留六字段 payload、反馈分支、证据 / 来源限制、JSON 示例和成功合同；只扩写 `refinement_instructions`，补齐上述四个无歧义约束。通过前不创建模型调用或解析骨架。

#### `A4-Gate-C3d-C2b-R1`｜用户订正与检查点收口

用户提交状态：

> 改完了

用户 R1 的决定性修改原样快照：

```python
refinement_instructions = (
    "请根据 Reflection 反馈对候选摘要进行修订，"
    "修订应严格依据本轮工具结果，不得添加模型常识。"
    "文件路径分支只能依据 read_material.content；搜索分支只能依据 search_web.results。"
    "只输出一个 JSON 对象；JSON 前后不得有任何解释、标题、提示语或其他文字。"
    "不得返回这种形式：```json{'status': 'success', ...}```"
    "sources 必须从 allowed_sources 中选择，不能扩大白名单。"
    "JSON 对象的键必须恰好是 status、summary、sources；不得增加其他键。"
    "示例：{\"status\": \"success\", \"summary\": \"...\", \"sources\": [\"...\"]}。"
    "成功候选必须满足：status == \"success\"，summary 为非空字符串，"
    "sources 为非空字符串列表且是 allowed_sources 的子集。"
    "如果反馈为‘无需改进’，保留候选事实含义；"
    "其他反馈只依据本轮证据修订。"
)
```

- 主 sentinel 矩阵：`C3D_C2B_SENTINEL=22/22`；六类输入、两种 feedback、精确证据字段、禁止模型常识、allowlist、JSON-only、无前后文字、禁代码围栏、精确三键、示例和成功合同全部进入实际 prompt。
- topic / relative_path 迁移均通过；path 形状中的 `read_material.content`、`allowed_sources=["sample.txt"]` 与 `feedback="无需改进"` 无损 round-trip。
- 纯函数边界：模型客户端与工具 spy 调用数均为 0；语法编译和 diff-check 通过。
- 助手机械收尾：移除已完成 TODO，并把禁止围栏的例子改为等义的明确文字“不得使用 Markdown 代码围栏（```）包装 JSON”；不改变用户已证明的行为边界。

教学检查点状态：`PASS`。用户已把“看起来像 JSON”收紧为客户端可直接消费的 JSON-only 协议，并能明确限制证据字段、键集合和来源子集。

### `A4-Gate-C3d-C3a`｜最终 success 候选的客户端硬校验

- 当前在做什么：实现 `validate_success_result(candidate, allowed_sources)`，用纯 Python 判断一个已解析的模型候选能否成为最终 `success`；非法时统一抛出 `ValueError`，合法时通过已有 `make_result()` 返回固定三字段对象。
- 为什么需要：C2a / C2b 的 prompt 只是模型软约束；即使模型自述“已遵守”，仍可能返回空摘要、错类型、额外字段或虚构 URL。客户端硬门必须独立于模型判断。
- 与前后步骤的关系：C1 固定 allowlist，C2a / C2b 固定模型可见规则；本检查点先把最终安全门冻结，再接模型调用和 `json.loads`，避免未校验的模型输出提前成为 success。
- 用户要做：先校验 `allowed_sources` 是由非空字符串组成的列表；再要求 `candidate` 是字典且键恰为 `status / summary / sources`。`status` 必须是字符串 `"success"`；`summary` 必须是去空白后非空的字符串；`sources` 必须是非空 `list[str]`，每项去空白后非空且都属于 allowlist。
- 子集语义：合法 `sources` 可以只使用 allowlist 的一部分，不要求等于全部来源；只要混入一个不在本轮 Observation 的路径 / URL，整个候选都必须拒绝，不能删除伪造项后静默放行。
- 返回边界：全部通过后只调用 `make_result("success", summary, sources)` 构造固定对象；不得直接信任模型字典，不在此函数调用 Reflection、模型或工具，也不增加 `needs_manual` / 重试等恢复分支。
- 可观察完成标准：内存矩阵至少覆盖合法 path、合法 topic 子集、非字典、缺 / 多键、非严格 success、空 / 错类型摘要、空 / 错类型 / 非字符串来源、全伪造和真假混合来源；模型与工具调用为 0。
- 当前状态：单一 TODO 骨架已创建；尚未加入模型请求、`response_format`、`json.loads`、恢复、日志、HITL 或 eval。

#### `A4-Gate-C3d-C3a`｜用户第一次实现与聚焦诊断

用户提交状态：

> 改完了

用户第一次实现原样快照：

```python
def validate_success_result(
    candidate: object,
    allowed_sources: list[str],
) -> dict[str, object]:
    if not isinstance(allowed_sources, list) or not all(
        isinstance(source, str) for source in allowed_sources
    ):
        raise ValueError("allowed_sources 必须是字符串列表")
    if not isinstance(candidate, dict):
        raise ValueError("候选摘要必须是字典类型")
    expected_keys = {"status", "summary", "sources"}
    if set(candidate.keys()) != expected_keys:
        raise ValueError(f"候选摘要的键必须恰好是 {expected_keys}")
    status = candidate.get("status")
    if not isinstance(status, str) or status.strip() != "success":
        raise ValueError("status 必须是字符串 'success'")
    summary = candidate.get("summary")
    if not isinstance(summary, str) or summary.strip() == "":
        raise ValueError("summary 必须是非空字符串")
    sources = candidate.get("sources")
    if not isinstance(sources, list) or not all(
        isinstance(source, str) and source.strip() != "" for source in sources
    ):
        raise ValueError("sources 必须是非空字符串列表")
    if not set(sources).issubset(set(allowed_sources)):
        raise ValueError("sources 必须是 allowed_sources 的子集")
    return {
        "status": "success",
        "summary": summary,
        "sources": sources,
    }
```

- 主聚焦矩阵：`C3D_C3A_FOCUSED=22/27`；独立只读扩展矩阵为 `57/63`，结论一致。
- 已通过：合法 path、topic 完整集合与 proper subset；非 dict、缺 / 多键；status 错类型 / 普通错值；summary 错类型 / 空白；sources 错类型 / 非字符串 / 空白；全越界与真假混合越界；输入不修改；模型和工具调用为 0。
- 缺口 1：`allowed_sources` 只检查元素是字符串，没有拒绝 `""` 或纯空白字符串；同时应明确列表本身非空。
- 缺口 2：`status.strip() != "success"` 会把 `" success "` 规范化后错误放行；这里的合同是原值精确等于 `"success"`，客户端不能替模型修正状态。
- 缺口 3：`sources` 未显式检查列表非空；`all(...)` 对空列表返回 `True`（vacuous truth），因此 `sources=[]` 被错误放行。
- 缺口 4：成功分支直接返回字典，三个合法案例中 `make_result()` 调用次数均为 0，没有遵守已冻结的固定结果构造边界。
- 最小共同根因：集合成员检查主体正确，但“列表非空”不能由 `all()` 推导；精确协议字段不能先 `strip()` 再比较；已有构造器必须真正进入成功返回路径。

教学检查点状态：`RETRY`。当前只做 R1：为 allowlist 补列表非空和元素 strip 非空；status 改为原值精确比较；sources 显式非空；最终唯一成功 return 改为 `make_result("success", summary, sources)`。其余已通过逻辑保持，不创建下一个骨架。

#### `A4-Gate-C3d-C3a-R1`｜用户第一次订正与聚焦诊断

用户提交状态：

> 改完了

用户 R1 的决定性修改原样快照：

```python
status = candidate.get("status")
if not isinstance(status, str) or status != "success":
    raise ValueError("status 必须是字符串 'success'")

sources = candidate.get("sources")
if sources is None or len(sources) == 0:
    raise ValueError("sources 字段缺失")
if not isinstance(sources, list) or not all(
   not isinstance(source, str) or source.strip() != "" for source in sources
):
    raise ValueError("sources 必须是非空字符串列表")

return make_result("success", summary.strip(), sources)
```

- 独立只读超集：`68/72 PASS`。本地主矩阵的原始输出为 `25/32`；其中“是否保留 summary 首尾空白”是额外保守探针，不属于当前合同阻塞项，候选输入本身未被修改，决定性缺口与独立审计一致。
- 已修复：`status=" success "` 现在拒绝；`sources=[]` 现在拒绝；三个合法 path / full set / proper subset 都经过 `make_result()`；输入不修改；模型与工具调用为 0。
- 剩余缺口 1：`allowed_sources` 仍只检查 `isinstance(source, str)`，未明确列表非空或元素 `strip()` 后非空；`["a", "   "]` 会错误放行，甚至让非法案例进入 `make_result()`。
- 剩余缺口 2：sources 在证明是 `list` 前调用 `len()`；`sources=1` 会泄漏 `TypeError`，没有按函数合同转成 `ValueError`。
- 剩余缺口 3：sources 元素谓词写成 `not isinstance(...) or ...`，使非字符串反而满足 `all()`；不可哈希的字典 / 列表随后在 `set(sources)` 泄漏 `TypeError`。
- 最小共同根因：结构保护顺序应固定为“先容器类型 → 再容器非空 → 再用正向谓词验证每个元素 → 最后做集合 / 成员关系”。

教学检查点状态：`RETRY`。当前只做 R2：allowlist 与 sources 都按上述固定顺序写成完整 guard，元素谓词统一为 `isinstance(source, str) and source.strip() != ""`；其余逻辑保持，不创建下一个骨架。

#### `A4-Gate-C3d-C3a-R2`｜用户第二次订正与回归诊断

用户提交状态：

> 改完了

用户 R2 的两个 guard 原样快照：

```python
if not isinstance(allowed_sources, list) or not all(
    isinstance(source, str) or source.strip() == "" for source in allowed_sources
):
    raise ValueError("allowed_sources 必须是字符串列表")

sources = candidate.get("sources")
if sources is None or len(sources) == 0:
    raise ValueError("sources 字段缺失")
if not isinstance(sources, list) or not all(
   not isinstance(source, str) or source.strip() == "" for source in sources
):
    raise ValueError("sources 必须是非空字符串列表")
```

- 本地主矩阵：`C3D_C3A_R2_FOCUSED=14/23`；独立完整超集为 `61/72`，两者均确认 R2 引入行为回归。
- 回归：合法 path、topic full set、topic proper subset 三例全部被错误拒绝，合法 `make_result()` 调用为 0；空白 source 反而成为 success。
- 异常泄漏：allowlist 含非字符串时出现 `AttributeError`；`sources=1` 与 `sources=[{}] / [[]]` 分别泄漏 `TypeError`。
- 保持通过：padded status、空 sources、普通越界来源仍拒绝；输入不修改；模型和工具调用为 0。
- 根因 1：allowlist 的 `isinstance(...) or strip()==""` 让任何字符串都因左侧为真而通过，非字符串又会继续调用不存在的 `.strip()`。
- 根因 2：sources 的 `not isinstance(...) or strip()==""` 把合法非空字符串算成假，却把非字符串或空白字符串算成真。
- 正确的“合法元素”定义只有一个：`isinstance(source, str) and source.strip() != ""`；两处 guard 都应对这一定义执行 `not all(...)`。
- 保护顺序仍必须是：先 list 类型 → 再列表非空 → 再元素谓词；sources 完成前三步后才允许进入集合子集检查。

教学检查点状态：`RETRY`。当前只做 R3，直接按教练给出的两个完整 guard 订正，不再自行改换 `and / or / == / !=`；status、summary、exact-key、子集和 `make_result()` 路径保持不变。

#### `A4-Gate-C3d-C3a-R3`｜用户第三次订正与单根因收缩

用户提交状态：

> 改好了

用户 R3 的两个决定性 guard 原样快照：

```python
if not isinstance(allowed_sources, list) or not all(
    isinstance(source, str) or source.strip() != "" for source in allowed_sources
):
    raise ValueError("allowed_sources 必须是字符串列表")

sources = candidate.get("sources")
if sources is None or len(sources) == 0:
    raise ValueError("sources 字段缺失")
if not isinstance(sources, list) or not all(
    isinstance(source, str) or source.strip() != "" for source in sources
):
    raise ValueError("sources 必须是非空字符串列表")
```

- 本地聚焦矩阵：`C3D_C3A_R3_FOCUSED=9/19`；独立完整超集为 `64/72 PASS`。两套矩阵的案例规模不同，但对剩余根因的结论一致。
- 已恢复：合法 path、topic full set、topic proper subset；三例均各调用 `make_result()` 一次。padded status、空 sources、缺 / 多键、全越界与真假混合来源继续被拒绝；输入不修改；模型和工具调用均为 0。
- 仍错误放行：allowlist 中的空白字符串，以及 candidate sources 中的空白字符串；这些非法案例会进入 `make_result()`。
- 仍泄漏异常：allowlist / sources 中含非字符串时会在 `.strip()` 泄漏 `AttributeError`；标量 `sources=1` 会因先调用 `len()` 泄漏 `TypeError`。
- 唯一元素谓词根因：R3 仍写成 `isinstance(source, str) or source.strip() != ""`。左侧对任何字符串都为真，所以空白字符串被放行；左侧对非字符串为假，又会继续执行右侧 `.strip()`。合同要求两个条件同时成立，因此只能使用 `isinstance(source, str) and source.strip() != ""`。
- 两个容器还需各自显式检查非空；candidate sources 必须在确认是 list 后才能做任何 `len` / 元素 / 集合操作。

教学检查点状态：`RETRY`。为避免同时改两段继续混淆，R4a 只订正 `allowed_sources` 完整 guard；candidate `sources` guard 暂时保留，待 R4a 的 5 个白名单案例通过后再单独处理。

#### `A4-Gate-C3d-C3a-R4a`｜允许来源容器硬门闭合

用户提交状态：

> 改好了

用户 R4a 的决定性实现原样快照：

```python
if (
    not isinstance(allowed_sources, list)
    or not allowed_sources
    or not all(
        isinstance(source, str) and source.strip() != ""
        for source in allowed_sources
    )
):
    raise ValueError("allowed_sources 必须是非空字符串列表")
```

- 本地聚焦矩阵：`C3D_C3A_R4A_LOCAL=7/7`；独立只读扩展矩阵：`24/24 PASS`。
- 已通过：合法 path / topic allowlist；非 list、空 list、含非字符串、含空串、含纯空白字符串均统一为 `ValueError`。
- 两个合法案例各进入一次 `make_result()`；非法 allowlist 案例均未进入 `make_result()`。
- 所有 `candidate` / `allowed_sources` 输入前后保持不变；模型、`read_material`、`search_web` 调用均为 0。
- 关键订正已稳定为三层保护：先证明容器是 list，再证明非空，最后用正向短路谓词同时证明每项是字符串且去空白后非空。
- 语法编译与 `git diff --check` 通过；本轮只判定第一段 allowlist guard，不外推 candidate `sources` 或整个 validator。

教学检查点状态：`PASS`（仅 `C3d-C3a-R4a`）。下一唯一小步为 `C3d-C3a-R4b`：把 candidate `sources` 的重复判断合并成同样的三层完整 guard，确保标量在 `len()` / `set()` 前确定性拒绝。

#### `A4-Gate-C3d-C3a-R4b`｜候选来源 guard 首次订正

用户提交状态：

> 第二段改好了

用户新增的完整 guard 原样快照：

```python
sources = candidate.get("sources")
if sources is None or len(sources) == 0:
    raise ValueError("sources 字段缺失")
if (
    not isinstance(sources, list)
    or not sources
    or not all(
        isinstance(source, str) and source.strip() != ""
        for source in sources
    )
):
    raise ValueError("sources 必须是非空字符串列表")
```

- 本地聚焦矩阵：`C3D_C3A_R4B_LOCAL=13/14`；独立完整超集：`82/84 PASS`。
- 新增的第二个完整 guard 本身正确：tuple / `None` / 空列表 / 非字符串元素 / 空串 / 空白串均能由 list → 非空 → 元素正向谓词确定性拒绝。
- 唯一失败：`sources=1`；独立超集补充的 `sources=True` 同样在进入新 guard 前泄漏 `TypeError`。
- 根因是旧预检查 `if sources is None or len(sources) == 0` 仍留在新 guard 前；对整数或布尔值执行 `len()` 时，尚未完成 list 类型证明。
- 其余边界保持通过：合法 path / topic full / proper subset、越界 / 混合越界、allowlist 回归、状态 / 摘要 / exact-key 合同；`make_result()` 仅在三个合法案例各调用一次；输入不修改，模型与工具调用为 0；语法与 `git diff --check` 通过。

教学检查点状态：`RETRY`（仅 `C3d-C3a-R4b`）。R4b-R1 只删除旧的两行预检查，让已经正确的新完整 guard 成为 `sources` 的唯一入口；不重写新 guard，也不改其他已通过逻辑。

#### `A4-Gate-C3d-C3a-R4b-R1`｜最终客户端硬门闭合

用户提交状态：

> 旧判断删好了

用户本轮唯一行为修改：保留 `sources = candidate.get("sources")`，删除其后的旧 `sources is None or len(sources) == 0` 预检查，让完整 list → 非空 → 元素谓词 guard 成为唯一入口。

- 本地完整矩阵：`C3D_C3A_FINAL_LOCAL=58/58`；独立超集：`84/84 PASS`。
- 决定性回归：`sources=None / 1 / True / []` 均统一为 `ValueError`，不再泄漏 `TypeError`；tuple、非字符串 / 不可哈希元素、空串、空白串同样拒绝。
- 合法 path、topic 完整集合与 proper subset 均通过；来源全越界或混合越界均整体拒绝，不删除伪造项后静默放行。
- allowlist、candidate 字典 / exact-key、精确 `status`、非空 `summary` 的全部既有边界保持通过。
- `make_result()` 只在合法案例调用；所有输入保持不变；模型、`read_material`、`search_web` 调用均为 0。
- 助手在 PASS 后只做机械收口：删除已完成 C3d-C3a TODO、整理 allowlist guard 缩进，并把模块说明改为 C3d-C3a 已完成；没有改变行为，也没有创建下一个骨架。
- 机械清理后同口径本地回归仍为 `C3D_C3A_POST_CLEANUP=58/58`，`MAKE_RESULT_VALID_ONLY=5/5`，内存编译与 `git diff --check` 通过。

教学检查点状态：`PASS`（`C3d-C3a validate_success_result`）。这是最终 success 候选的纯客户端硬门闭合，不等于 C3d 或 A4-Gate 正式 PASS；Reflection / Refinement 两次模型调用、原始 JSON 解析、端到端 fake / 真实双路径仍未实现。

### 当前三层范围

- 本块计划范围：只完成 C3d Reflection、来源真实性 / 子集硬校验与最终三字段，并在末段执行一次 25 分钟到期回收。
- 本块当前剩余：C3d-C1、C3d-C2a、C3d-C2b 与 C3d-C3a 已闭合；S2 本次实现范围在 validator 处收口，后续模型调用 / JSON 解析 / C3d 集成顺延，不在今天继续打开；现在转入已固定预留的 S3 25 分钟间隔回收，不记录或倒推实际耗时。
- A4-Gate 任务总剩余：C3d 之外仍有七字段日志、统一控制器 / `max_steps`、工具失败重试与安全停止、HITL、14 条 eval 和正式 Gate 复核。

### S2 交接

- 本次已覆盖：从本轮真实 Observation 派生 allowlist；构造 Reflection prompt；构造 Refinement JSON-only prompt；对不可信 Python 候选执行精确三字段、成功非空和来源子集硬校验。
- C3d 未覆盖：两次真实 / fake 模型调用编排、`response_format`、原始响应外壳、`json.loads`、解析后调用 validator，以及 topic / path 端到端验证。
- 今天下一步：启动 S3 的 25 分钟 `RECOVERY_BLOCK_PENDING`；在真实作答和正式重算前，pending 状态保持不变。
- 下次 A4 Session 起点（不属于今天）：重新纳入未来 ≤180 分钟计划后，创建唯一的两次 Reflection → Refinement 模型调用骨架；当前不提前创建。

## Session S3｜25 分钟间隔回收块

### Preflight 与范围保护

- durable state：`RECOVERY_BLOCK_PENDING`；本 Session 是第二块末段已预留的 25 分钟回收，不追加到 180 分钟之外，也不记录或倒推实际时长。
- 到期候选：`WP-02`、`WP-04`、`WP-15`、`WP-24`；旧目标总上限为 2，严格一次一题。
- 最终顺序：先 `WP-15`，首题若无需扩展补救再进入 `WP-02`。
- `WP-15` 选择理由：到期 2026-07-19；今天虽已沿真实 Observation 完成 allowlist / prompt / validator，但“模型原始 JSON 文本 → Python 对象 → validator”的精确异常与停止层仍需迁移证明。Observation 主语与回填位置沿用 C3c 已有证据，本题不重复问定义。
- `WP-02` 选择理由：到期 2026-07-15，为当前最老；今天 S2 未覆盖 HTTP，且可绑定已 PASS 的 `search_web` 非 2xx 控制流，避免重复旧题表面。
- 语义去重排除：`WP-04` 今天已由纯 Python allowlist / prompt builder / validator、零模型与工具调用反复覆盖，立即再问主要测短时记忆；`WP-24` 与当前 JSON-only 路径无真实正则切口。
- 本 Session 不加入 C3d 新检查点、算法、工作场景升级或岗位证据；`A4-Gate` 保持 `DOING`。
- 本地代码依据：`code/stage4/a4_gate_research_summary_agent.py`，HEAD `33c5565bee3fba739ce732a8b299df13f4fc22ca` 上有当天未提交改动；R6 题面已用项目 `.venv` 做无落盘可答性探针。
- pending 清除边界：练习层只保存原始作答、订正和单项结果；只有后续 `ai-agent-learning-review` 能更新 `weak-points.md`、计算升降档或重算 durable state。

### `A4-Gate-R6`｜`WP-15`：坏 JSON 的精确异常与停止层

- 本次复习目标 ID / 类型：`WP-15` / 到期稳定错题间隔复测。
- 与当前题是否语义合并：是；只借用当前 C3d 的 validator 作为新表面，证据仍归 `WP-15`，不计为新的 C3d 覆盖。
- 题面：

```python
import json
import runpy

validate = runpy.run_path(
    "code/stage4/a4_gate_research_summary_agent.py"
)["validate_success_result"]

raw_candidate = (
    '{"status":"success","summary":"文件摘要",'
    '"sources":["sample.txt"]'
)

validator_calls = 0

try:
    candidate = json.loads(raw_candidate)
    validator_calls += 1
    validate(candidate, ["sample.txt"])
except Exception as exc:
    print(type(exc).__name__, validator_calls)
```

问题：运行后打印的两个值分别是什么？它们说明错误停在“raw JSON 文本 → Python 对象 → validator”三层中的哪一层？请用一句话解释，先不要改代码。

- 原始作答 / 执行证据：`JSONDecodeError 0,错误停在raw JSON 文本，因为raw_candidate不是标准的json`
- 提示 / 订正路径：首答独立完成，无提示。批改补充的精确表述是：错误发生在 `json.loads(raw_candidate)` 的“raw JSON 文本 → Python 对象”解析步骤；因此尚未生成 candidate 字典，`validator_calls` 保持 0，validator 没有运行。
- 单项结果：`PASS`。精确异常名、调用计数、停止层与因果均正确；没有混成 `TypeError`、模型失败或工具失败。Observation / 客户端回填主语沿用今天 C3c 已有执行证据，本题不重复考。
- 台账去向 / 下次回炉：建议正式检查确认独立通过后由 `ai-agent-learning-review` 为 `WP-15` 升档；练习层不填写日期，也不提前修改 pending。

### `A4-Gate-R7`｜`WP-02`：非 2xx 响应与显式状态分支

- 本次复习目标 ID / 类型：`WP-02` / 到期稳定错题间隔复测。
- 与当前题是否语义合并：是；使用已 PASS 的当前 `search_web` 作为变化后的真实代码表面，证据归 `WP-02`，不启动统一 retry controller。
- 题面：

```python
import runpy
from unittest.mock import patch

module = runpy.run_path(
    "code/stage4/a4_gate_research_summary_agent.py"
)
search_web = module["search_web"]
requests_module = search_web.__globals__["requests"]


class FakeResponse:
    status_code = 503

    def __init__(self):
        self.json_calls = 0

    def json(self):
        self.json_calls += 1
        return {"query": {"search": []}}


fake = FakeResponse()

with patch.object(requests_module, "get", return_value=fake):
    result = search_web("人工智能")

print(result["ok"], result["retryable"], fake.json_calls)
```

问题：运行后打印的三个值分别是什么？`requests.get()` 是否会仅因为收到 503 就自动抛异常？为什么 `fake.json()` 会或不会执行？请沿当前 `search_web` 的真实控制流说明。

- 原始作答 / 执行证据：`False True 0,不会仅因为收到 503 就自动抛异常，因为请求是成功的，但是因为服务器错误没有返回有效值。因为执行fake.json()前先判断了响应对象返回结果，503返回的结果是False，所以不会执行fake.json()`
- 提示 / 订正路径：首答无提示。三个输出值、requests 不会仅因 503 自动抛异常、正文解析未执行均正确；需要定向订正两处因果措辞：①“请求是成功的”容易把“成功收到 HTTP Response”误说成 HTTP 业务成功，503 本身仍是服务端错误状态；②“503 返回的结果是 False”不精确，503 是 `response.status_code`，`ok=False` 是 `search_web` 的显式 `status_code >= 500` 分支构造出来的工具结果。用户第一次定向补答：`1.requests.get() 得到响应对象`；`2.503存在response.status_code`；`3.False是search_web() 显式构造的工具结果中的 result["ok"]`；`4.因为response.json()前先执行了status_code的判断，大于500的时候直接返回了`。前三层已精确闭合；最后一句把代码的 `>= 500` 说成“大于 500”，仍漏掉边界值 500。用户第二次最小补答：`因为response.json()前先执行了status_code的判断，大于等于500的时候直接返回了`，已与真实 `status_code >= 500` 分支一致。
- 单项结果：`PARTIAL → PARTIAL → PASS`。最终能区分“收到 Response”“HTTP 状态码”“工具结果 ok 字段”三层，说明 requests 不会仅因 503 自动抛异常，并准确指出显式 `>= 500` 早返回使 `response.json()` 调用次数为 0。
- 台账去向 / 下次回炉：建议正式检查核对完整订正链后，由 `ai-agent-learning-review` 决定 `WP-02` 的升档与具体日期；练习层不提前修改 pending。

### S3 练习层收口

- 回收目标：2/2，且未超过每日最多一个回收块、最多两个旧目标的硬上限。
- `A4-Gate-R6 / WP-15`：首答独立 `PASS`；精确绑定 `json.JSONDecodeError`、`validator_calls=0` 与 raw JSON 解析停止层。
- `A4-Gate-R7 / WP-02`：`PARTIAL → PARTIAL → PASS`；行为预测始终正确，订正后精确区分 Response、`response.status_code`、工具结果 `ok` 与 `>= 500` 早返回。
- 未出第三题；没有追加 C3d、算法、工作场景或岗位证据范围。
- 今天最虚 / 下次重点复习：HTTP 控制流中的“成功收到响应”不等于 HTTP 状态成功；描述分支时必须精确绑定对象 / 字段，并保留 `>=` 等边界运算符。
- 回炉状态边界：本次练习层已留下两项真实原始作答、订正与单项结果，但 `RECOVERY_BLOCK_PENDING` 尚未由正式检查重算；本页不改 `weak-points.md`，也不预写下一回炉日期。
- 今日计划范围 / 今日剩余：S1、S2 本次实现范围与 S3 回收练习层均已收口；今天不再开第三块或第三个旧目标。
- 任务总剩余：A4-Gate 仍为 `DOING`；C3d 尚缺两次模型调用、响应外壳、`json.loads`、validator 接力与 topic/path 端到端，之后仍有日志、恢复 / 停止、HITL、14 条 eval 和正式复核。
- 下次 A4 Session 起点（不属于今天）：在未来重新纳入 ≤180 分钟计划后，创建并实现唯一的 Reflection → Refinement 两次模型调用骨架。
- 下一口令：`检查今天的学习`，交给 `ai-agent-learning-review` 运行代码、正式复核当天证据并重算回炉台账。

## 正式检查｜S2 C3d 当前切片与 S3 回收块

本节追加复核 S2 / S3；前文对 S1 `A4-Gate-C3c = PASS` 的正式检查继续有效，不覆盖其历史证据。

### 覆盖矩阵与正式判定

| 范围 | 正式结论 | 决定性证据 / 未覆盖项 |
| --- | --- | --- |
| C3c 两轮候选链 | `PASS` | 沿用本页前序正式检查；本轮再跑忠实 fake 代表矩阵 `81/81`，真实 DeepSeek topic / path 再次通过 |
| C3d-C1 `derive_allowed_sources` | `PASS` | path 精确映射本轮规范化相对路径；topic 按顺序映射本轮 `results[*].url`；错路由、非严格成功与坏结构确定性拒绝，独立 `24/24` |
| C3d-C2a `build_reflection_prompt` | `PASS` | 实际 prompt 携带请求、候选、工具名、完整 Observation 与 allowlist；明确正文证据、禁模型常识、来源边界、三字段合同和反馈协议，独立 `24/24` |
| C3d-C2b `build_refinement_prompt` | `PASS` | 六类输入、两种 feedback、JSON-only、无前后说明 / 围栏、精确三键、类型 / 非空 / 子集合同均进入实际 prompt，独立 `43/43` |
| C3d-C3a `validate_success_result` | `PASS` | 不可信对象经过 exact-key、精确 status、非空 summary / sources 与来源子集硬门；非法输入 fail-closed，合法分支才调用 `make_result`，独立 `69/69` |
| C3d 模型 / JSON 集成 | 未覆盖 | 尚无 Reflection → Refinement 两次模型调用、响应外壳、`response_format`、raw `content` → `json.loads` → validator，也没有 C3d topic/path fake + 真实端到端 |
| 控制器 / 日志 / 恢复 / HITL | 未覆盖 | 尚无统一 `max_steps=6`、七字段结构化日志、模型 / 工具失败一次重试、`needs_manual`、危险动作允许 / 拒绝和完整安全停止 |
| A4 固定 eval | 未覆盖 | `code/stage4/eval_cases.json` 不存在；10 正常 + 3 失败 + 1 危险、至少 3 条 holdout 和预冻结分项阈值均未执行 |

因此，今天完成的各教学子检查点均可正式通过；未开始部分是后续计划范围，不倒判当前切片为 `RETRY`。整个 `A4-Gate` 仍必须保持 `DOING`，不能用纯函数断言数或 prompt 构造器冒充完整 Agent / Gate 通过。

### 工程验证与决定性结果

- 当前基线：HEAD `33c5565bee3fba739ce732a8b299df13f4fc22ca`；A4 文件 SHA-256 `D034FB619F35432977C606F15C52F158CA6A9FECC7E8DF34694F54CFFD153086`。
- 主审无落盘代表矩阵：通过 stdin 导入当前模块，覆盖 C3c topic/path 两轮 fake、Observation 回填、C3d allowlist、两个 prompt 及 validator 正反例，结果 `76/76 PASS`、失败列表为空。
- 独立只读 C3c 代表矩阵：`81/81 PASS`；覆盖忠实 SDK 属性对象、两条路径、两轮消息与第一 / 第二响应坏外壳。
- 独立只读 C3d 分层矩阵：C1 `24/24`、C2a `24/24`、C2b `43/43`、C3a `69/69`，合计 `160/160 PASS`、`FAILED_IDS=[]`；模型 / 工具 sentinel 调用为 0，`make_result` 只在合法候选调用。
- 真实 DeepSeek 主审复跑：topic 选中 `search_web`，工具 `ok is True`，3 个允许 URL，候选摘要 732 字符；relative_path 选中 `read_material`，工具 `ok is True`，1 个允许路径，候选摘要 198 字符。两条均为非空候选，且构造出的 Reflection prompt 完整包含本轮真实 Observation。
- 独立真实复跑也通过：topic / path 分别选中预期工具并返回精确三键；候选长度受模型采样影响而不同，不作为合同阈值。
- 语法检查使用系统临时 cfile，`PY_COMPILE=PASS`，未在仓库留下 `__pycache__`；`git diff --check` 无 whitespace error，只有 Git 的 LF → CRLF 提示。

测试 / eval / 性能 / 安全摘要：内存 fake 用于稳定复现协议和坏输入，真实 DeepSeek 用于证明当前 C3c 正常模型链，两者没有互相冒充。C3d 当前只证明纯函数、结构、类型、来源边界和 fail-closed；尚未运行 C3d 模型调用。未做性能 / 延迟 benchmark。来源越界、混合越界、空白和错类型能被客户端拒绝，但这不等于已具备 retry、恢复、HITL 或完整 Agent 安全停止。

### 工作场景与故障闭环

- `WS-01`：模型候选的最终合同从宽松字典收敛为精确三键、精确 `success`、非空摘要 / 来源和来源子集；保持 `DIAGNOSED_AND_FIXED`。
- `WS-03`：分层 sentinel 与非法类型超集复现了 `and/or` 写反、`all([])` 真空通过、类型证明前调用 `len/.strip/set` 导致错误放行或异常泄漏；修复顺序稳定为“list 类型 → 非空 → 元素合同 → 集合 / 子集”，独立 C3d `160/160`；保持 `DIAGNOSED_AND_FIXED`。
- `WS-07`：允许来源只能从本轮 Observation 派生，虚构来源和“合法 + 虚构”的混合来源都整体拒绝；白名单成员身份仍不能代替 `content/results` 正文支持；保持 `DIAGNOSED_AND_FIXED`。
- 本次没有“失败 → 七字段日志定位 → 重试 / 不可恢复判断 → 安全停止 → 再注入”完整轨迹，因此不升级 `WS-02` / `WS-05`，也不把任何场景写成新的 `RECOVERED_UNDER_FAULT`。

### S3 回炉台账正式重算

- `WP-15`：`A4-Gate-R6` 首答独立给出 `JSONDecodeError`、`validator_calls=0` 和 raw JSON → Python 对象的停止层；同日 C3c / C3d 已有真实 Observation 执行与回填证据。本次间隔复测成功，从 `+2周` 升至 `+1月`，下次回炉 2026-09-10。
- `WP-02`：`A4-Gate-R7` 保留真实的 `PARTIAL → PARTIAL → PASS`。首答已经独立答对三个输出、requests 不会仅因 503 自动抛异常，以及正文解析前显式早返回；两次追问只补精确对象层级与 `>= 500` 边界。因此本次间隔复测成功，从 `+2周` 升至 `+1月`，下次回炉 2026-09-10；不另建同义 HTTP 活跃弱点。
- C3d 的新增错误不另造重复 ID：合并更新 `WP-01`（prompt 点名规则必须进入实际字符串）、`WP-12`（Observation / allowlist / 软评审 / 硬门 / 正文证据分层）和 `WP-33`（精确类型与容器 guard 顺序）。
- 本次 S3 承接的 pending 实例已有两项真实作答与单项结果，正式完成。升档后到期项只剩 `WP-04`（2026-07-19）与 `WP-24`（2026-08-05）：数量 `2 < 4`，但最老项逾期 22 天，仍触发“最老逾期 ≥7 天”。因此重算后重新签发一份新的 `RECOVERY_BLOCK_PENDING`。
- 今天已经使用每天唯一一个回收块，新 pending 不能在 2026-08-10 再执行；必须放入后续第一个合适的 ≤180 分钟完整计划，以 20–30 分钟替换等量主任务范围。

### Gate、算法、岗位与知识库边界

- Gate 预设阈值 / holdout：Problem Contract 仍为 DRAFT；14 条 eval 文件与 holdout 尚不存在，当前 `76/76`、`81/81`、`160/160` 都只是对应实现切片的回归证据，不是 A4-Gate 正式通过率。
- 算法内容块：本日 S2 / S3 没有算法项；`tracker/algorithm-progress.md` 保持 W4 `PASS`、4/4、`DEBT=0`，不启动下一算法内容块。
- 岗位证据候选：不适用。A4-Gate 未完成，也没有版本化正式 eval、恢复 / HITL 或跨工具岗位证据复核；`tracker/job-readiness.md` 不更新，不产生 `JOB_EVIDENCE` 候选。
- 知识库同步：已合并更新 `D:\AI-Knowledge\02-Concepts\Agent\反思机制(Reflection).md`，补入 Observation 派生 allowlist、prompt 软约束、客户端来源子集硬门，以及“来源身份不等于正文支持”；更新 Agent 索引并登记 `log.md`。结构化输出、函数调用、HTTP 和严格布尔既有页面已准确覆盖 S3 与当天其他结论，本次保持 no-op；没有新建 A4-Gate PASS 项目页或复盘页。严格健康检查通过：212 个 Markdown、1840 个链接、0 断链、0 缺 frontmatter。

### 批改与全日收口

批改判定：`A4-Gate-C3c = PASS`；`A4-Gate-C3d-C1 / C2a / C2b / C3a = PASS`；S3 recovery `2/2` 正式完成；`A4-Gate = DOING`。

主审工具：Codex（`ai-agent-learning-review`）。

复核判定（普通任务默认留空；JOB_EVIDENCE、FINAL/J11-Gate、投递前必须由另一工具填写）：

当天进度小结：S1 正式闭合 C3c；休息日 S2 将 C3d 推进到 Observation 来源白名单、Reflection / Refinement prompt 与最终客户端硬门；S3 完成两个到期项的真实回收并正式升档。当天最多两块、每天最多一个回收块和旧目标最多两个的边界均未突破。

总进度小结：A4-Gate 已完成 Problem Contract、C1、C2、C3a～C3c 与 C3d 的四个纯函数子检查点；整个 Gate 仍缺 C3d 模型 / JSON 端到端、七字段日志、统一控制器 / `max_steps`、失败重试与安全停止、HITL、14 条 eval 和最终正式复核。

对问题 / 不确定点的解释：`allowed_sources` 是客户端根据本轮工具执行事实算出的“最多可以引用什么”，Reflection 仍需读取完整正文判断“结论有没有证据”。来源在白名单内只能证明来源身份，没有自动证明摘要中的每一句都受正文支持。

问答点评与补充：做得稳的是坚持按最小检查点逐段修复，最终让合法 path / topic、proper subset、虚构 / 混合来源和所有坏容器边界都形成可执行证据；S3 也能准确定位 JSON 解析停止层和 HTTP 早返回。主要薄弱点仍是第一次把规则真正落到代码 / prompt 时容易写反布尔连接词、漏掉非空前提，或只把要求留在注释里；后续应先把合同拆成正向谓词，再按保护顺序实现。

今天最虚 / 下次重点复习：容器 guard 的顺序与正向谓词，以及 HTTP 控制流中“成功收到 Response”不等于 HTTP 状态成功；描述分支时要精确绑定对象、字段和 `>=` 等边界运算符。

超前内容提示：不启动 G8-00，也不并行打开日志、恢复、HITL 或 eval。先完成 C3d 模型调用与 JSON 解析集成，再按 Problem Contract 顺序推进。

补救任务：今天已完成切片没有阻塞性补救；新 pending 与 A4 后续都必须重新进入未来计划，不能在今天追加第三块。

- 今日计划范围 / 今日剩余：S1、S2 当前实现切片与 S3 回收均已正式收口；今日剩余为无。
- 任务总剩余：C3d 两次模型调用、响应外壳、`json.loads` → validator、topic/path 双路径端到端；其后为七字段日志、统一控制器 / `max_steps`、重试 / 安全停止、HITL、14 条 eval 与正式 Gate 复核。
- 下次 Session 起点（不属于今天）：在后续新的 ≤180 分钟完整计划中先为新 `RECOVERY_BLOCK_PENDING` 锁定 20–30 分钟预算；A4 编码从唯一的 Reflection → Refinement 两次模型调用骨架开始，依次闭合响应外壳、JSON 解析、validator 接力和双路径验证。
