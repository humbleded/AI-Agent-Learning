# 每日学习打卡

日期：2026-07-08（周三）

学习时长：

完成任务编号：T3-04 外部 API 工具

看了哪些视频/哪一集：（无）

读了哪些文档/哪一节：

- `repos/agents-course/units/zh-CN/unit1/tools.md`：什么是工具、工具如何运作、工具说明书要素。
- OpenAI Function Calling 文档：The tool calling flow。
- `code/stage3/t3_04_public_api_tool.py`
- `code/stage0/p0_09_http_request.py`
- `code/stage0_5/b0_02_http_probe.py`

代码/产物位置：

- `code/stage3/t3_04_public_api_tool.py`

运行提示：

```powershell
.\.venv\Scripts\python.exe .\code\stage3\t3_04_public_api_tool.py
```

今日练习题（T3-04 外部 API 工具）：

## A. 快速回忆

**A1** `public_api_tool` 补的是 LLM 的哪个短板？分别说明 LLM、客户端程序、工具函数负责什么。

```python
# 用户答：
# public_api_tool 这个工具，补的是 LLM不能调用外部API的短板
# 1.LLM能Action，输出JSON，包含对应的工具名和参数
# 2.LLM本身不能调用工具，是需要客户端解析JSON并调用工具。
# 3.public_api_tool 具体帮它调用了外部API，返回结果给客户端，客户端把结果作为观察/tool message放回上下文中
# 判定：PASS
# 补充：LLM 负责判断和生成工具调用请求；客户端负责解析、执行工具、回填工具结果；public_api_tool 负责真实请求外部 API，并返回稳定结构化结果。
```

**A2** 判断哪些问题适合 LLM 直接回答，哪些应该调用工具：解释 `response.status_code`、查询 GitHub API 当前状态、计算 `123456 * 789`。

```python
# 用户答：
# A.适合 LLM 直接回答，因为这是已有的内容，模型可以根据训练的数据回答出来
# B.应该调用工具，因为需要去调用外部API，模型本身没有调用工具的能力。需要客户端去调用
# C.应该调用工具，模型本身没有精确的计算的计算器，需要调用工具来帮助计算
# 判定：PASS
# 补充：B 更精确说是模型本身没有联网/发 HTTP 请求的能力；C 应调用 calculator_tool，而不是 public_api_tool。
```

**A3** 为什么必须写 `response = requests.get(url, timeout=5)`，不能只写 `requests.get(url, timeout=5)`？

```python
# 用户答（第1轮）：
# requests.get(url, timeout=5)会返回一个response 的响应体，response 是用来获取响应消息的。后面response 会用来获取响应结果和响应字段。
# 判定：PARTIAL
# 订正：response 不是单纯响应体，而是响应对象，里面有状态码、响应头和正文；需点名 response.ok / response.status_code / response.headers。
# 用户答（第2轮）：
# requests.get(url, timeout=5)会返回一个response 的响应对象，response 是用来获取响应对象的。后面response 会用来获取响应结果和响应字段。比如response.ok，response.status_code，response.headers
# 判定：PASS
```

**A4** 为什么不直接返回完整 `response.json()`，而只返回必要字段？

```python
# 用户答：
# 返回完整的response.json()会占用太多的上下文，导致模型的注意力被分散，可能会提取不到关键信息。而且当前问题不需要无关字段，只返回需要的字段，这样也可以帮助模型节省token，提取关键信息
# 判定：PASS
```

**A5** 为什么 `except requests.Timeout` 要写在 `except requests.RequestException as exc` 前面？

```python
# 用户答：
# 1.Timeout属于RequestException的子类
# 2.如果先写RequestException，异常都会被RequestException捕获到，永远不会走到Timeout的分支
# 判定：PASS
```

**A6** 为什么 404 不会进入 `except requests.RequestException`？解释 404 和网络异常的区别。

```python
# 用户答（第1轮）：
# 因为404不是异常，只是一种状态码。
# 判定：PARTIAL
# 订正：还需说明 404 是服务器正常返回响应但资源不存在；网络异常是请求过程中没正常拿到响应。
# 用户答（第2轮）：
# 404 是服务器已经正常返回了响应，只是告诉你资源不存在；
# 网络异常是请求过程中没能正常拿到响应，比如超时、连接失败、URL 不合法。
# 判定：PASS
```

## B. 解释与比较

**B1** 用人话写出 `public_api_tool` 的工具说明书 schema。

```python
# 用户答：
# 工具名：public_api_tool
# 工具用途：调用外部的API，用来判断调用成功还是失败
# 参数 url：外部url的地址，类型是字符串
# 返回：成功返回：{"ok": response.ok, "status_code": response.status_code, "server":response.headers.get("Server") ,},失败返回{"ok": False,"error":"调用失败"}
# 判定：PASS
# 补充：用途更精确是调用公开 API，并把响应摘要成适合模型读取的必要字段；成功字段还应包含 rate_limit_limit / rate_limit_remaining。
```

**B2** 对比 `raise requests.Timeout("请求超时")` 和 `return {"ok": False, "error": "请求超时"}`，哪种更适合作为 Agent 工具返回？

```python
# 用户答：
# return {"ok": False, "error": "请求超时"}更适合作为 Agent 工具返回，因为对于程序来说可以继续执行后续代码，对于模型来说也可以提取error的内容展示给用户
# 判定：PASS
```

**B3** “只要 `requests.get(url, timeout=5)` 没有抛异常，就说明这次 API 调用一定成功了。”这句话对不对？

```python
# 用户答：
# 不对，因为响应结果中状态码可能是404之类的，表示请求成功了，但是资源不存在。虽然是请求成功了，但是还是没有调用成功，response.ok是False,status_code是404.
# 判定：PASS
# 补充：更精确说是网络层拿到了服务器响应，但 HTTP 业务结果不一定成功。
```

## C. 代码/操作练习

**C1** 预测 `public_api_tool("https://api.github.com/not-found-for-t3-04")` 的返回结构。

```python
# 用户答（第1轮）：
# result的结果是{"ok":False,"status_code":404,"server":"api.github.com"}
# 判定：PARTIAL
# 订正：ok=False、status_code=404 对；server 字段来自响应头，不是 URL 主机名，实测 GitHub 更像 "github.com"。
# 用户答（第2轮）：
# serverserver从响应头中获取的，不是URL的主机名
# 判定：PASS
```

**C2** 找出异常分支里使用 `response.ok` 的 bug。

```python
# 用户答：
# BUG在"ok": response.ok中，因为是在网络层请求失败的，都获取不到响应，所以不存在响应对象，response.ok也会报错
# 判定：PASS
```

**C3** 补全发起 GET 请求并保存响应对象的一行代码。

```python
# 用户答：
# response=requests.get(url,timeout=5)
# 判定：PASS
```

**C4** 补全成功返回结构中的 2 个 rate limit 字段。

```python
# 用户问：
# 2 个 rate limit 字段作用是什么
# 补充：rate limit 是外部 API 的调用次数额度；X-RateLimit-Limit 表示总额度，X-RateLimit-Remaining 表示剩余额度。
# 用户答：
# return {
#     "ok": response.ok,
#     "status_code": response.status_code,
#     "server": response.headers.get("Server"),
#     "rate_limit_limit":response.headers.get("X-RateLimit-Limit"),
#     "rate_limit_remaining":response.headers.get("X-RateLimit-Remaining")
# }
# 判定：PASS
```

## D. 应用与迁移

**D1** 把 `public_api_tool` 接回 Tool Calling 链路：用户问“现在 https://api.github.com 能访问吗？”

```python
# 用户答：
# 1.用户把外部地址发给了模型
# 2.LLM输出需要调用的工具名称public_api_tool 和参数https://api.github.com,通常是已JSON的形式传给客户端'{"tool_name":"public_api_tool","url":"https://api.github.com"}'
# 3.客户端负责解析和调用工具，把JSON解析成dict，读取public_api_tool去调用，public_api_tool("https://api.github.com")
# 4.public_api_tool 执行后返回调用结果，如{"ok": true, "status_code": 200},.工具结果返回给客户端，客户端把返回结果通过Observation/tool message放回到模型的上下文中
# 5.如果结果是想要的，模型会把结果返回给用户，如果不是想要的，继续调用工具
# 判定：PASS
# 补充：链路完整，但编号有点错位。题目第 1 点问 LLM 负责什么，答案里第 1 点写成了用户行为；更标准的说法是：LLM 根据用户问题判断需要 public_api_tool，并生成工具名和参数。public_api_tool 只负责发请求和返回结构化 dict；Observation/tool message 回填由客户端负责。
```

**D2** 如果 `rate_limit_remaining` 只剩 `"1"`，模型回答用户时还应该提醒什么？

```python
# 用户答：
# 还应该提醒只剩下一次访问机会了。因为下一次调用完之后，后续访问就会不成功了
# 判定：PASS
# 补充：核心判断正确。更精确说是“当前限流窗口内额度快用完了，应提醒减少重复调用或稍后再试”；额度通常会在一段时间后重置，不是永久不能访问。
```

**D3** 迁移题：如果以后写 `weather_api_tool(city)`，参考 `public_api_tool`，至少应该具备哪 4 个设计点？

```python
# 用户答：
# 工具职责边界：当模型需要获取某一地方的天气时候调用
# 不返回原始 JSON，只获取需要的字段，比如"ok","status_code","address","weather"等字段。调用时先判断连接是否超时，在判断是否有其他异常情况。
# 判定：PASS
# 补充：已覆盖职责边界、必要字段、timeout、异常处理四个设计点。更精确说，timeout 是在 requests.get(..., timeout=5) 中设置；异常处理应先捕获 Timeout，再捕获 RequestException。天气工具的摘要字段可按需求设计为 city/temperature/weather/update_time/source 等。
```

**D4** 根据工具返回 `{"ok": False, "status_code": 404, "server": "github.com", "rate_limit_limit": "60", "rate_limit_remaining": "42"}` 判断模型该如何回答用户。

```python
# 用户答：
# 1.不能说“网络连接失败”
# 2.404是连接成功，但是访问的路径资源不存在
# 3.补充“errer”的信息，提示资源不存在
# 判定：PARTIAL
# 订正：第 1、2 点正确；第 3 点不应补充不存在的 error 字段。这个返回结构里没有 error，能补充的是 server="github.com"、rate_limit_remaining="42" 等已有字段；“资源不存在”应从 status_code=404 解释出来。
# 用户订正：
# rate_limit_remaining的信息？提示用户在当前限流窗口内还剩42次调用？
# 判定：PARTIAL -> PASS
```

**D5** 用 4 句话串起 `public_api_tool` 的完整安全/稳定链路，必须包含 `timeout`、`RequestException`、`必要字段`、`Observation`。

```python
# 用户答：
# 当public_api_tool获取到调用和参数时，调用requests去调用url，如果超时返回timeout异常，其他网络层异常通过RequestException捕获并抛出。请求成功了只返回必要字段，"ok"，"status_code"，"server"，"rate_limit_limit"，"rate_limit_remaining"。，把这个dict结果作为Observation放回到上下文中，让模型去判断是否回答用户
# 判定：PARTIAL
# 订正：主链路基本对，但 RequestException 不应“捕获并抛出”，而是捕获后返回 {"ok": False, "error": "..."}；另外更精确说是客户端解析工具调用后，把 url 传给 public_api_tool，工具只负责请求和返回 dict，客户端负责把 dict 作为 Observation 回填。
# 用户订正：
# 客户端解析 LLM 的工具调用后，把 url 参数传给 public_api_tool，调用requests去调用url，如果超时返回timeout异常，其他网络层异常通过RequestException捕获后返回稳定 dict。请求成功了只返回必要字段，"ok"，"status_code"，"server"，"rate_limit_limit"，"rate_limit_remaining"。，把这个dict结果作为Observation放回到上下文中，让模型去判断是否回答用户
# 判定：PARTIAL
# 订正：RequestException 已修正；timeout 也不应说“返回 timeout 异常”，而应说“通过 requests.Timeout 捕获后返回 {"ok": False, "error": "请求超时"}”。
# 用户订正（第2轮）：
# 客户端解析 LLM 的工具调用后，把 url 参数传给 public_api_tool，调用requests去调用url，如果超时，捕获后返回稳定 dict，其他网络层异常通过RequestException捕获后返回稳定 dict。请求成功了只返回必要字段，"ok"，"status_code"，"server"，"rate_limit_limit"，"rate_limit_remaining"。，把这个dict结果作为Observation放回到上下文中，让模型去判断是否回答用户
# 判定：PARTIAL -> PASS
# 补充：链路已达标。更精确可写成：超时由 requests.Timeout 捕获后返回 {"ok": False, "error": "请求超时"}；其他请求异常由 requests.RequestException 捕获后返回 {"ok": False, "error": "..."}。
```

我的作答：

遇到的问题：

- 一开始把 `response` 说成“响应体”，经订正后改为“响应对象”，并能点名 `response.ok/status_code/headers`。
- 一开始把 404 只说成“状态码”，经订正后补全为“服务器正常返回响应，但资源不存在；网络异常是没正常拿到响应”。
- 一开始把 `server` 误认为 URL 主机名，经订正后能说明它来自响应头 `Server`。
- D5 一开始把 `RequestException` 说成“捕获并抛出”、把超时说成“返回 timeout 异常”；经订正后能改为捕获异常并返回稳定 dict。

我最不确定的点：

- rate limit 字段的作用刚补过，后续需要再抽查一次。

希望复核/检查：

批改判定：

Codex 正式复核判定：**T3-04 PASS**

代码验证已通过：

- `python -m py_compile code/stage3/t3_04_public_api_tool.py`：语法通过。
- `python code/stage3/t3_04_public_api_tool.py`：默认请求 `https://api.github.com` 返回 `ok=True`、`status_code=200`、`server="github.com"`。
- 函数级验证：404 路径返回 `ok=False/status_code=404`，非法 URL 返回 `ok=False/error`，模拟 `requests.Timeout` 返回“请求超时”，模拟 `requests.RequestException` 返回“请求失败：...”

练习判定：**PASS**

- A 快速回忆：6 题通过。A3/A6 经订正后能说清 response 是响应对象、404 是正常响应里的状态码而不是网络异常。
- B 解释比较：3 题通过。能解释稳定 dict 比抛异常更适合作为 Agent 工具返回，也能说明没有异常不等于 HTTP 业务成功。
- C 代码操作：4 题通过。能补出 `response = requests.get(url, timeout=5)`、成功摘要字段和 rate limit 字段；能发现异常分支里不能使用不存在的 `response.ok`。
- D 应用迁移：5 题通过。能把 `public_api_tool` 接回 Tool Calling 链路，并迁移到天气工具设计；D4/D5 经订正后能避免编造不存在的 `error` 字段，能把 Timeout/RequestException 都改成稳定错误返回。

复核判定（另一工具交叉复核，默认留空）：

当天进度小结：

完成 T3-04 外部 API 工具的带读、实现、正式练习与代码复核。`public_api_tool()` 已能请求公开 API，设置 `timeout=5`，成功时只返回 `ok/status_code/server/rate_limit_limit/rate_limit_remaining` 等必要字段；非法 URL、超时和请求异常都会返回稳定 `ok/error` 结构。判定 **PASS**。

总进度小结：

阶段 3 Tool Calling 已完成 T3-01 函数调用概念、T3-02 计算器工具、T3-03 文件工具、T3-04 外部 API 工具。下一步进入 T3-Gate 准备，把计算器、文件读取、外部 API 三个工具整合成多工具助手。

对问题/不确定点的解释：

今天最重要的区分是：`response` 是响应对象，不只是响应体；404/500 属于服务器正常返回的 HTTP 状态码，不会被 `requests.RequestException` 自动捕获；超时、连接失败、非法 URL 等才属于请求过程异常。异常分支里可能没有 `response`，所以必须返回 `{"ok": False, "error": ...}`，不能写 `response.ok`。

问答点评与补充：

今天整体理解稳定，尤其是“LLM 生成工具调用、客户端执行工具、工具返回 dict、客户端回填 Observation”这条主语链路已经比较清楚。主要易错点集中在两个地方：一是把响应对象/响应头/URL 主机名混在一起；二是异常处理里容易说成“抛出异常”，但 Agent 工具更需要捕获异常并返回稳定结构。

超前内容提示：

T3-Gate 会把计算器、文件读取、外部 API 三个工具放进同一个工具注册表。到那时难点不再是单个工具函数怎么写，而是模型选择工具、客户端分发工具、工具结果太长/失败时如何回填给模型。

补救任务：

下次优先抽查三点：1. `server` 字段来自 `response.headers.get("Server")`，不是 URL 主机名；2. 404 不进 `except RequestException`；3. `requests.Timeout` / `requests.RequestException` 分支都应该返回稳定 `{"ok": False, "error": ...}`。
