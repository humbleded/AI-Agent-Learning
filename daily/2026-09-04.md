# Session 学习证据日志

日期：2026-09-04

> 本文件按日期保存原始证据，不是每日课表，也不记录计划或实际学习时长。同日多任务各占一个 Session；跨日续学必须链接该任务全部前序 daily。

## 当日 Session / 任务索引

| Session | 任务 | 环节 | 关联前序 daily | 当前状态 / 下次 Session 起点 |
| --- | --- | --- | --- | --- |
| S1 | `G8-00` LangGraph 基础工作流 | 跨日正式练习续答、批改、生产修改和回归测试 | [2026-08-25](2026-08-25.md)、[2026-08-26](2026-08-26.md)、[2026-08-31](2026-08-31.md)、[2026-09-01](2026-09-01.md)、[2026-09-03](2026-09-03.md) | `DOING`；`G8-00-F1 = WAITING → PARTIAL → PASS`；`WP-28 = PASS`；正式练习已完成，下一口令“检查今天的学习” |

## 任务证据接力

### Session S1｜任务 `G8-00`

任务状态与知识单元：`G8-00 = DOING`。本 Session 跨日续接 2026-09-03 发出的唯一正式检查点 `G8-00-F1`；不重置 `G8-00-G1～G11`、`I1～I9` 的既有证据，也不生成同义 `F2`。

关联前序 daily：

- [2026-08-25](2026-08-25.md)：`G8-00-G1/G2 = PASS`。
- [2026-08-26](2026-08-26.md)：`G8-00-G3 = PARTIAL → RETRY → PASS`。
- [2026-08-31](2026-08-31.md)：`G8-00-G4～G7 = PASS`。
- [2026-09-01](2026-09-01.md)：`G8-00-G8～G10 = PASS`，实现启动。
- [2026-09-03](2026-09-03.md)：G8-00 v1、长期 pytest 与职责映射闭合，并发出 `G8-00-F1`；该文件中的 `WAITING` 是 9 月 3 日历史状态，本次跨日答案与订正保存在当前文件，不倒改历史。

本 Session 目标：保存并批改 `G8-00-F1` 的跨日原始作答；把用户给出的四分支方案机械写入生产文件；按用户重新指定的分工由助手补维护型回归测试；只让用户订正尚未闭合的精确调用次数。

资料与产物复核：

- 题面与故障注入条件见 [2026-09-03](2026-09-03.md) 的 `G8-00-F1`。
- 生产文件：`code/stage8/incident_change_review_agent/incident_graph.py`。
- 回归文件：`code/stage8/incident_change_review_agent/test_incident_graph.py`。
- 本地来源身份：当前 Git `HEAD = 0db733f8bef176e4ea93a4f94b04fad253f22a82`；上述两个 stage8 文件当前仍是 worktree 未跟踪文件，因此本文只声称核验当前 worktree，不声称该 commit 已包含它们。
- 当前持久回炉状态沿用 `NORMAL`，没有 `RECOVERY_BLOCK_PENDING`；本次只处理已经选中的 `WP-28`，不混入算法债务或后续 persistence/HITL 内容。

## 正式练习

### `G8-00-F1`｜跨日原始作答与初次批改

以下是用户于 2026-09-04 提交的第一轮答案；只整理 Markdown 显示格式，不静默修正其中的调用次数或措辞。

#### A｜修改前行为

用户原始作答：

> 1. `START → organize_facts → review_evidence → run_readonly_diagnostic`（第 1 次）`→ review_evidence → run_readonly_diagnostic`（第 2 次）`→ review_evidence → max_steps_stop → END`
>
> 2. `evidence: []`
>
> `evidence_status: "unknown"`
>
> `requested_tool: "get_recent_errors"`
>
> `step_count: 2`
>
> `stop_reason: "max_steps_reached"`
>
> `conclusion: None`
>
> 3. `get_recent_errors` 调用了 2 次，`get_latency_summary` 调用一次，模型调用 0 次。
>
> 4. 因为 `review_evidence` 的逻辑中，只要 `logs` 或 `alerts` 任一非空，就固定请求 `"get_recent_errors"`，从不请求 `"get_latency_summary"`。白名单只是“允许执行哪些工具”的约束，不会自己决定“此时应该请求哪个工具”。在本输入下，`get_recent_errors` 返回空证据，导致无法形成足够证据，只能消耗步数直到 `max_steps_reached`，因此 `get_latency_summary` 永远不会被调用。

初次批改：

- A1：`PASS`。完整路线与修改前的当前代码一致；其中 `START/END` 是入口/终止标记，中间七次才是业务 Node 调度。
- A2：`PASS`。六个最终字段全部正确。
- A3：`PARTIAL`。`get_recent_errors=2`、模型 `=0` 正确；`get_latency_summary` 应为 `0`，不是 `1`。这也与 A4 的“永远不会被调用”相矛盾。
- A4：`PASS`。已经正确区分“工具在白名单中获准”与“业务 Node 实际生成了这个工具请求”。

决定性自检：`run_readonly_diagnostic` 每执行一次只调用一个工具，并让 `step_count + 1`。修改前最终 `step_count=2`，所以总工具调用数只能是 2；不可能同时出现 `recent=2` 与 `latency=1`。如果 latency 工具实际执行一次，它会返回当前 alert，证据随后变为充足并调用模型，也会与用户正确写出的 `evidence=[] / conclusion=None / model=0` 冲突。

#### B｜生产修改方案与落盘

用户原始作答：

```python
def review_evidence(
    state: IncidentState,
) -> dict[str, str | None]:
    """审查当前证据，并返回证据状态与下一工具请求的局部更新。"""

    if state["evidence"]:
        return {"evidence_status": "enough", "requested_tool": None}
    elif not state["alerts"] and not state["logs"]:
        return {"evidence_status": "insufficient", "requested_tool": None}
    elif state["logs"]:
        return {
            "evidence_status": "unknown",
            "requested_tool": "get_recent_errors",
        }
    else:
        return {
            "evidence_status": "unknown",
            "requested_tool": "get_latency_summary",
        }
```

批改与证据归属：

- 分支语义正确。最后的 `else` 在前三条规则排除后精确表示“没有 evidence、有 alerts、没有 logs”，因此请求 latency 工具；函数仍只返回局部更新，不执行工具或增加 `step_count`。
- 用户提交时，真实源码仍是旧的 `elif state["logs"] or state["alerts"]`。助手随后按照用户已经给出的方案机械写入，没有替用户设计新分支；同时把过时的“三个互斥分支”注释改成“四条互斥规则”。
- 除 `review_evidence()` 及对应注释外，没有修改 router、Edge、工具函数、白名单或 State schema。

#### C｜用户调整测试分工

用户原始作答：

> 测试是由你来做。

分工与归属：

- 用户明确把本题 C 的 pytest 实现交给助手。
- 助手扩展已有 `test_incident_graph.py`，新增两组参数化 maintained regression；测试是项目真实产物，但标记为 `VERIFIED（助手实现）`，不冒充用户独立 pytest 编码证据。
- 两个参数场景都用 tuple 保存给定信号、在每次测试中转成新的 list；`invoke` 与 `stream` 分属不同测试实例，各自创建 fresh State、两个工具 spy 和 fake 模型，避免 reducer、可变字段或调用次数跨运行污染。

新增覆盖：

1. `test_signal_specific_tool_selection_reaches_conclusion`：分别对 alerts-only / logs-only 完整比较最终 State、输入不变、两个工具精确调用次数、模型一次和真实 Provider 零次。
2. `test_signal_specific_tool_selection_streams_exact_route`：分别验证五个完整 v2 `updates` parts、业务 Node 顺序、首次 review 的工具请求、诊断 evidence 增量、二次 review、结论、两个工具与模型调用次数、输入不变和真实 Provider 零次。

执行过程与结果：

```text
第一次测试：2 failed, 10 passed in 1.04s
失败原因：助手插入新增函数的位置错误，切断了原有 stream 测试；原测试后半段错误落入新函数，触发 NameError("parts_iterator")。
归属判断：这是助手编写测试时的结构错误，不是用户 B 的生产逻辑失败。

修复：把原有 stream 测试后半段移回原函数；新增参数化测试保持为两个独立模块级函数。

最终 pytest：12 passed in 0.72s
py_compile：PASS
worktree 身份核对：两个 stage8 文件仍未纳入 Git；不使用空的 `git diff` 输出冒充内容差异验证
```

最终两条新业务路线均已通过：

```text
alerts-only：
  get_recent_errors=0
  get_latency_summary=1
  model=1
  evidence=["payment p99 latency high"]
  evidence_status="enough"
  requested_tool=None
  step_count=1
  stop_reason=None
  conclusion="fake graph conclusion"

logs-only：
  get_recent_errors=1
  get_latency_summary=0
  model=1
  evidence=["payment request timeout"]
  evidence_status="enough"
  requested_tool=None
  step_count=1
  stop_reason=None
  conclusion="fake graph conclusion"

两条 stream Node 顺序：
  organize_facts
  → review_evidence
  → run_readonly_diagnostic
  → review_evidence
  → build_conclusion
```

#### D｜判断依据

用户原始作答：

> 1. 不正确，测试预期必须服从本题输入和产品规则，而不是照抄当前实现。当前实现得到 `max_steps_reached` 是因为 `review_evidence` 的工具选择逻辑错误，导致不断请求 `get_recent_errors` 却拿不到任何证据，步数耗尽。若把测试预期改成 `max_steps_reached`，等于承认错误行为是正确结果，掩盖了缺陷，会让回归测试失去意义。
>
> 2. LangGraph 的 Node / Edge 拓扑定义流程骨架：节点执行顺序、条件路由、终止边。负责“怎么走”，不决定“走哪条”的业务内容。
>
> `review_evidence` 查看当前已经整理出的事实和已经收集到的证据，判断“现在的证据够不够继续得出结论”，并决定是否需要调用工具获取更多信息。
>
> `APPROVED_READONLY_TOOLS`：安全白名单。
>
> `run_readonly_diagnostic`：安全处理一次只读诊断请求，并返回对应的局部 State 更新；校验并执行允许的只读工具。
>
> 3. 工具名称必须受 `APPROVED_READONLY_TOOLS` 白名单限制，应用代码需要精确控制。交给 LLM 会引入不确定性，可能请求未批准的工具、漏掉必要的工具，或产生幻觉。LLM 应该专注于需要语言理解和推理的结论生成，而不是控制流程或选择工具。
>
> 4. 诊断流程具有有状态的多步循环、条件路由、终止条件、证据累积等复杂控制流。LangGraph 提供了声明式图、状态管理、reducer、条件边、流式更新等能力，使流程清晰可维护，也便于测试和扩展。
>
> 5. 比如普通的两数之和，使用普通函数就可以了。

初次批改：

- D1：`PASS`。准确说明测试预期必须来自输入语义与产品规则，不能为了绿灯追随错误实现；同时在新的 Graph 路由场景中闭合 `WP-28`。
- D2：`PASS`，核心职责区分正确。精确补充：拓扑声明允许存在的 Node 和路径，挂在条件 Edge 上的 `route_after_review` 会根据结构化 State 选择本次实际下一站；`review_evidence` 负责证据判断和本题确定性的工具请求；白名单同时保存“获批工具名 → 函数对象”的映射；诊断 Node 不负责选择工具，而是拒绝未批准请求、执行一个映射工具、校验结果、归一化失败并让 `step_count + 1`。
- D3：`PASS`，按本题范围成立。范围校准：结论不是“所有 Agent 永远不能让 LLM 提议工具”；开放语义场景可以让 LLM 提议，但应用代码仍必须控制白名单、参数、预算、执行、错误和停止边界。本题四条选择规则已经明确且有限，所以直接由 Python 判断更可靠。
- D4：`PASS`。已经从共享 State、多步循环、条件分支、业务停止、reducer 与流式更新说明 Graph 的实际价值，没有把单个 `if/elif` 的复杂度误当成引入 Graph 的原因。
- D5：`PASS`。两数之和足以证明“单步、确定性、无共享流程状态或回环的任务继续用普通函数”；它不是当前项目的相邻例子，但不影响该语义目标。更贴近本项目的例子可以是规范化 service 名称、检查 JSON 必填字段或校验工具结果是否为 `list[str]`。

### 初次单题结论与定向订正

`G8-00-F1 = WAITING → PARTIAL`。

已闭合内容不再重答：A1/A2/A4、B、C 的工程验证、D1～D5 均已闭合。当前只剩 A3 的一个精确调用次数错误；这是一道题内的定向订正，不新增 `F2`。

定向订正要求：只重新填写修改前的三个调用次数，并用一句话说明三者为什么不能是 `2 / 1 / 0`：

```text
get_recent_errors = ?
get_latency_summary = ?
model = ?

一句话自检：
```

### `G8-00-F1-A3`｜第二轮定向订正

用户原始订正：

> `get_recent_errors = 2`
>
> `get_latency_summary = 0`
>
> `model = 0`
>
> 因为 `max_steps=2` 下若 `get_recent_errors` 被调用 2 次，`step_count` 已耗尽，路由会直接进入 `max_steps_stop`，既不会再有 `get_latency_summary` 的调用机会，也不会到达 `build_conclusion` 调用模型，所以三者不可能同时为 `2 / 1 / 0`。

订正批改：`PASS`。

- 三个修改前调用次数均正确。
- 理由正确使用“一个诊断 Node 只执行一个工具、每次让 `step_count + 1`、业务上限为 2”的数量边界，而不是只改答案数字。
- 两次错误工具请求都只拿到空 evidence；达到上限后进入 `max_steps_stop`，因此既没有 latency 工具的调用机会，也不会进入 `build_conclusion`。
- 独立复核同意本次订正足以闭合 A3；已通过内容不再换措辞重考。

最终单题结果：`G8-00-F1 = WAITING → PARTIAL → PASS`。

订正后只读回归：

```text
12 passed in 0.93s
```

## 复习回炉

- 本次复习目标 ID / 类型：`WP-28`，到期稳定错题——测试预期必须服从精确输入语义与业务规则。
- 与当前题是否语义合并：是，并入 `G8-00-F1-D1`，没有增加额外题量。
- 原始作答 / 执行证据：用户明确拒绝把错误实现产生的 `max_steps_reached` 改成测试预期，并解释那会承认错误行为、掩盖缺陷、使回归测试失去意义。
- 订正路径：无需订正；该旧目标已经在新的 LangGraph 路由场景中独立迁移正确。
- 单项结果：`PASS`。
- 建议台账去向：由 `ai-agent-learning-review` 在正式验收时确认本次稳定复测并重算间隔；当前不修改 `tracker/weak-points.md`，不预填下一次回炉日期。

## 批改与小结

批改判定：`G8-00-F1 = WAITING → PARTIAL → PASS`。这是正式练习的单题结果，不是父任务的正式 PASS / RETRY / FAIL 判定；`G8-00` 继续保持 `DOING`。

本 Session 进度小结：修改前路线、最终 State、工具不可达根因、四分支生产方案、测试预期原则、职责边界和 Graph 适用性均已闭合；用户方案已机械落盘，助手按用户分工补完的维护型测试最终 `12/12 PASS`。A3 的精确调用次数经过一次定向订正后闭合，正式练习不再有未答或 `RETRY` 项。

任务总进度小结：正式题集只有 `G8-00-F1`，任务级合格考察达到 12 个上限，不生成同义 `F2`。下一步使用固定口令“检查今天的学习”，交给 `ai-agent-learning-review` 重新运行最终代码、核对全部跨日证据，并决定 G8-00 父任务的正式 PASS / RETRY / FAIL、台账与授权后的知识库同步。

当前最虚 / 下次重点复习：本轮最虚的是第一次没有用 `step_count` 守恒关系交叉检查工具调用总数；订正已经闭合，后续继续用“Node 次数 + 计数字段 + 最终 State”三者互相校验。

补救任务：`G8-00-F1-A3` 已完成；当前没有新的练习补救题。

复核判定：
