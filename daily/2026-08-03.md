# 每日学习打卡

- 日期：2026-08-03
- 当前任务：A4-05 Reflection
- 任务状态：`DOING`
- 今日环节：跨日续接 A4-05 带读与即时考察 G1–G7

## Session / 任务索引

- Session 1 — A4-05-G1–G7 跨日带读与批改：承接 [daily/2026-07-30.md](2026-07-30.md) 的带读 Session 1 与 G1 题面；继续承认 [daily/2026-07-23.md](2026-07-23.md) 已通过的事实复盘 / Planning / Replanning / Reflection 职责边界，不重复考察。
- Session 2 — A4-05 实现启动：用户发送 `开始动手`；按“即时创建、不预填答案”规则创建目标脚本，只开放 C1 硬约束评估器。

## A4-05 跨日接力

- 当前资料：Hello-Agents `4.4–4.4.5` 与配套 `Reflection.py`；ADP Chapter 4 的 trade-offs、At a Glance、Key Takeaways 与 Conclusion 任务相关选段；本地仓库 commit 分别为 `c869791653fa634e368af174b91364a7e6ea3484`、`415e5ba6942ba8aa677cce60b0e02c5c7dd5818c`。ADP 其余框架实现案例尚未通读，不计入当前范围。
- 已覆盖：Reflection 的职责边界与三阶段数据依赖；Memory 读写接口；三类 prompt 契约；主循环、停止条件与调用轨迹；调用和串行延迟成本；适用场景；同标准比较、回退保护与质量不退化判断。
- 当前检查点：A4-05-C1 公告硬约束评估器。
- 后续未覆盖：三类 prompt、单轮 Reflection 编排、最佳稿/比较逻辑、真实 DeepSeek 适配器与主程序运行证据。
- 目标产物：`code/stage4/a4_05_reflection_writer.py`，已创建当前 C1 TODO 骨架，尚不可运行完整任务。
- notes 状态：tracker 未要求 A4-05 独立 notes，当前不新增同义笔记。

## A4-05-G1 — 三阶段的数据依赖

### 原始作答

> Reflection输入:初稿写了日期，但漏了地点
> Reflection输出:缺少地点
> Refinement输入：初稿和缺少地点的评审反馈
> Refinement输出：优化后包含日期和地点的公告任务

### 批改

- 做对：Reflection 的输出准确落在“缺少地点”的评审反馈，而不是误写成修订稿；Refinement 保留了初稿和反馈，并正确产出同时含日期和地点的新版公告。
- 决定性缺口：Reflection 与 Refinement 的输入都遗漏了原始任务“公告必须同时写明日期和地点”。没有原始任务，评审阶段缺少判断标准；优化阶段也可能只修地点却丢掉日期等原约束。
- 单题状态：`RETRY`（接近，仅需补一个贯穿对象）。
- 提示阶梯：提示 1——检查公式中的 `Task` 是否同时进入 `Reflection(Task, O_i)` 和 `Refinement(Task, O_i, F_i)`。

### RETRY1

- 订正要求：只重写 `Reflection 输入` 和 `Refinement 输入` 两行，并写出场景中的原始任务；两条输出无需重答。
- 原始订正作答：

  > Reflection 输入：原始任务和生成的初稿
  > Refinement 输入：原始任务和生成的初稿和评审反馈

- 批改：Reflection 输入已完整包含 `Task + O_i`；Refinement 输入已完整包含 `Task + O_i + F_i`。四类对象的数据依赖闭合，且没有把反馈误当作改进稿。
- 单题结论：`RETRY → PASS`。

## A4-05-G2 — 短期记忆的两种读取方式

- 资料范围：Hello-Agents `4.4.2 案例设定与记忆模块设计`，以及配套 `code/chapter4/Reflection.py` 的 `Memory` 类；本地仓库 commit `c869791653fa634e368af174b91364a7e6ea3484`。
- 已讲目标：`records` 按时间保存 `execution/reflection`；`get_trajectory()` 序列化完整轨迹；`get_last_execution()` 逆序寻找最近一条执行结果，而不是直接取列表末项。
- 题面：内存依次保存 `execution: 初稿A → reflection: 反馈1 → execution: 改进稿B → reflection: 反馈2`。现在一个审计提示词明确需要完整历史，而下一次反思只需要最新执行稿。请分别选择 `get_trajectory()` 或 `get_last_execution()`，并写出各自应返回的内容范围。注意：这是对两个读取接口的假设场景；教材示例主循环并未实际调用 `get_trajectory()`。
- 原始作答：

  > 完整历史：使用add_record ；返回范围是 所有记录
  > 最新执行稿：使用 get_last_execution；返回内容是 最近一次的执行结果

- 做对：`get_last_execution()` 与“最近一次执行结果”的对应准确；也知道完整历史应覆盖全部记录。
- 决定性缺口：`add_record()` 是写入接口，只执行 `append()`，源码没有 `return`，因此调用结果是 `None`；它不能读取或格式化完整历史。完整历史必须使用专门的读取/序列化接口。
- 单题状态：`RETRY`（第二行已通过，只订正第一行）。
- 提示阶梯：提示 1——寻找名称中含 `trajectory`、会遍历 `self.records` 并返回格式化字符串的方法。

### RETRY1

- 订正要求：只重写“完整历史”这一行；“最新执行稿”无需重答。
- 原始订正作答：

  > 完整历史：使用 get_trajectory；返回范围是 全部的记录。

- 批改：已把完整历史正确绑定到读取/序列化接口 `get_trajectory()`，并明确覆盖全部记录；与先前已通过的 `get_last_execution()` 共同闭合两种读取需求。
- 单题结论：`RETRY → PASS`。

## A4-05-G3 — 三类提示词的职责与评审契约

- 资料范围：Hello-Agents `4.4.3 Reflection 智能体的编码实现` 中“（1）提示词设计”，以及配套 `Reflection.py` 的三个 prompt template；本地仓库 commit `c869791653fa634e368af174b91364a7e6ea3484`。
- 已讲目标：Execution 只产初稿；Reflection 按明确标准产出具体、可执行的反馈；Refinement 结合任务、上一版和反馈产出修订稿。角色描述不能代替评审标准与输出契约。
- 题面：某个 Reflection prompt 已经包含原始任务和待评审公告，但评审指令只有“请严格评价，并给出建议”。请把这句改写为可执行的评审指令：评审“日期是否存在、地点是否存在、总长度是否不超过 80 字”；有问题时逐项输出“问题、证据、修改建议”，全部通过时只输出“无需改进”。最后再用一句话说明为什么原指令不够。
- 原始作答：

  > 请严格评价，并检查日期，地点是否存在，检查总长度是否不超过80字。有问题时逐渐输出"问题、证据、修改建议"。只有全部通过时才只能能回答"无需改进"。请直接输出你的反馈，不要包含任何额外的解释。

- 做对：三个评审标准全部写入；问题分项字段、通过时的停止标记和“只输出反馈”的边界均已覆盖。
- 需要订正：
  1. 关键输出契约写成“逐渐输出”，应表达“逐项输出”；“才只能能回答”也需整理为无歧义的停止条件。这里不是普通文案润色，提示词中的一个词会改变模型执行方式。
  2. 题目要求的最后一句因果解释未作答：还需说明原来的“请严格评价”为什么不能形成稳定、可复核的评审。
- 单题状态：`RETRY`（主体正确，只补精确措辞与因果说明）。
- 提示阶梯：提示 1——“严格”只描述语气；想一想缺少明确检查清单和输出格式时，不同调用会出现什么问题。

### RETRY1

- 订正要求：无需重写完整 prompt；只提交“改正后的输出规则”与“原指令不足原因”两行。
- 原始订正作答：

  > 改正后的输出规则：请严格评价，并检查日期，地点是否存在，检查总长度是否不超过80字。有问题时逐项输出"问题、证据、修改建议"。只有全部通过时,只能能回答"无需改进"。请直接输出你的反馈，不要包含任何额外的解释。
  > 原指令不足原因：评审范围模糊，范围太大，模型不知道评审边界在哪。不一定满足任务需求

- 批改：已把“逐项输出”、三个评审标准、问题字段和通过条件写清；因果说明也准确指出评审边界模糊会让模型无法稳定对齐任务需求。“只能能”是遗留笔误，按非概念性错误校准为“只输出”，不要求机械重抄。
- 单题结论：`RETRY → PASS`。

## A4-05-G4 — 主循环、提前停止与调用轨迹

- 资料范围：Hello-Agents `4.4.3` 中“（2）智能体封装与实现”，以及配套 `Reflection.py` 的 `ReflectionAgent.run()`；本地仓库 commit `c869791653fa634e368af174b91364a7e6ea3484`。
- 已讲目标：初始 Execution 在循环外只调用一次；每轮先 Reflection 并记录反馈，命中“无需改进”就立即 `break`，否则才调用 Refinement；最终返回 `get_last_execution()` 找到的最近执行稿。
- 题面：设 `max_iterations = 3`。模型依次返回：初稿 `draft0`；第 1 轮反馈“缺少地点”；第 1 轮改进稿 `draft1`；第 2 轮反馈“无需改进”。请不运行代码，依次写出：模型总调用次数、Memory 的记录顺序、最终返回值，以及第 3 轮是否执行。
- 原始作答：

  > 模型总调用次数：4次
  > Memory 记录顺序：1. 初稿 draft0
  > 2. 第 1 轮反馈：缺少地点
  > 3. 第 1 轮改进稿 draft1
  > 最终返回值：包含了地点的优化稿
  > 第 3 轮是否执行：没有执行第三轮

- 做对：模型总调用 4 次正确；初稿、第一轮反馈、第一轮改进稿的前三条顺序正确；命中第二轮停止条件后不会执行第三轮也正确。
- 决定性缺口：第二轮反馈“无需改进”在检查 `if` 和执行 `break` 之前已经通过 `add_record("reflection", feedback)` 写入 Memory，当前记录顺序漏掉了这条。最终返回方向是优化稿，但题目给定的实际值应精确写为 `draft1`，因为 `get_last_execution()` 会跳过末尾的 Reflection 记录。
- 单题状态：`RETRY`（调用次数和第三轮已通过，只订正 Memory 尾项与精确返回值）。
- 提示阶梯：提示 1——沿代码顺序看 `add_record("reflection", feedback)` 和 `if "无需改进" ...: break` 谁先发生。

### RETRY1

- 订正要求：只重写 `Memory 记录顺序` 和 `最终返回值`；其余两项无需重答。
- 原始订正作答：

  > Memory 记录顺序：1. 初稿 draft0
  > 2. 第 1 轮反馈：缺少地点
  > 3. 第 1 轮改进稿 draft1
  > 4. 第 2 轮反馈：无需改进
  >
  > 最终返回值：draft1

- 批改：已补上停止判断前写入的第二轮 Reflection 记录；也准确写出 `get_last_execution()` 跳过末尾反馈后返回的实际执行值 `draft1`。调用次数、写入顺序、提前停止和最终返回四条控制流证据闭合。
- 单题结论：`RETRY → PASS`。

## A4-05-G5 — Reflection 的成本因果链

- 资料范围：Hello-Agents `4.4.5 Reflection 机制的成本收益分析`；ADP Chapter 4 的 trade-offs 与 Key Takeaways；本地仓库 commit 分别为 Hello-Agents `c869791653fa634e368af174b91364a7e6ea3484`、ADP `415e5ba6942ba8aa677cce60b0e02c5c7dd5818c`。
- 已讲目标：区分模型调用/计算成本、串行延迟、提示工程成本和上下文/状态成本；能解释这些成本如何由 `Execution → Reflection → Refinement` 的数据依赖产生，而不只说“更贵、更慢”。
- 边界校准：Hello-Agents 配套 `Reflection.py` 保存完整 Memory，但当前主循环只把最新执行稿放进反思/优化 prompt，并未调用 `get_trajectory()`；因此“完整历史导致上下文持续膨胀”是常见实现（ADP 的 message history 示例）的风险，不应伪装成这份源码已经发生的行为。
- 即时考察：某客服助手原本单次生成即可回复；加入 Reflection 后，每轮必须等待“评审完成”才能开始“优化”，并且每个阶段都调用模型。请只回答两项：① 为什么 API/计算成本增加？② 为什么总延迟增加？两项都要写出形成原因，不能只写“更贵、变慢”。
- 原始作答：

  > ① API/计算成本为什么增加：因为评审和优化都会调用一次模型，导致成本增加
  > ② 总延迟为什么增加：因为后一阶段依赖前一段结果，不能同时开始。并且是串行的。所以总延迟会增加

- 批改：第一项准确指出每个完整改进轮都新增 Reflection 与 Refinement 模型调用；第二项准确指出后一阶段依赖前一阶段结果，无法并行，等待时间会串行累加。两项均给出了成本形成原因。
- 单题结论：`PASS`。

## A4-05-G6 — 适用场景的成本收益权衡

- 资料范围：Hello-Agents `4.4.5` 的适用/不适用场景；ADP Chapter 4 `At a Glance` 的实践建议；仅选读任务相关结论，不宣称通读 ADP 全章。
- 已讲目标：Reflection 更适合质量、准确性和可靠性优先，允许额外延迟，并且存在明确评审标准和可执行改进路径的任务；对于低风险、强实时、一次结果已够用的任务，额外循环通常不划算。
- 即时考察：场景 A 是为内部群聊生成一句可人工修改的欢迎语，要求 2 秒内返回；场景 B 是上线前生成数据库迁移脚本，允许离线等待 10 分钟，并可用约束清单和测试检查正确性。哪一个更适合加入 Reflection，哪一个通常不适合？请分别说明一个决定性理由；不要只回答 A/B。
- 原始作答：

  > 场景 A：不适合 Reflection。因为只是欢迎语，对质量要求不高，且有时间要求，2s内输出
  > 场景 B：适合Reflection。因为是需要迁移数据库脚本，并且允许长时间等待，且要保证正确性和约束性

- 批改：场景 A 正确抓住低风险和强实时，使额外循环收益不足；场景 B 正确抓住高质量/约束要求、可容忍延迟，且题面提供了清单与测试作为评审依据。工程校准：“要保证正确性”表示需要外部测试与审批，Reflection 只能帮助发现和修改问题，不能单独提供正确性保证。
- 单题结论：`PASS`。

## A4-05-G7 — 如何证明没有越改越差

- 资料范围：Hello-Agents `4.4.4 运行实例与分析`；ADP Chapter 4 的代码用例（运行测试或静态分析后再修改）与“按预定义标准评估”原则；配套 `Reflection.py` 的停止条件。
- 已讲目标：把“评审员说无需改进”与“外部证据证明质量未退化”分开；用同一套硬约束、测试/事实来源和质量指标比较初稿与改进稿。硬约束任一回退时拒绝候选稿并保留上一版；只有硬约束不退化且目标指标改善（或满足预设接受门槛）时才接受新版本。
- 来源校准：Hello-Agents 示例展示复杂度从 `O(n * sqrt(n))` 到 `O(n log log n)` 的模型判断，但日志没有运行功能测试或基准；它能说明 Reflection 轨迹，不能单凭该日志证明新代码在所有维度更好。
- 即时考察：初稿 `draft0` 通过 5/5 个功能测试，平均耗时 120 ms；改进稿 `draft1` 通过 4/5 个相同测试，平均耗时 30 ms；模型反馈为“无需改进”。能否把 `draft1` 作为最终稿？请依次写出：`决定`、`证据`、`下一步动作`。
- 原始作答：

  > 决定：不通过
  > 证据：只通过 4/5 个相同测试
  > 下一步动作：加入记忆中，继续下一次优化

- 做对：拒绝 `draft1` 的决定正确；也准确用相同测试从 5/5 退化到 4/5 作为决定性证据，没有被 30 ms 的速度提升或模型“无需改进”覆盖硬约束失败。
- 决定性缺口：“加入记忆并继续优化”可以保留失败轨迹，但没有说明当前认可/返回的最好版本仍应是通过 5/5 的 `draft0`，也没有说明把失败测试证据送入下一轮后必须重跑同一套完整测试才能晋升新稿。若 Memory 的最后一条 `execution` 仍是 `draft1`，直接调用参考源码的 `get_last_execution()` 反而会返回已退化版本。
- 单题状态：`RETRY`（决定与证据已通过，只订正下一步动作）。
- 提示阶梯：提示 1——区分“保存候选稿的历史记录”和“更新当前最佳稿/最终返回值”；失败候选可以留痕，但不能覆盖已验证的最好版本。

### RETRY1

- 订正要求：只重写 `下一步动作`；需交代保留哪一版、把什么证据用于下一轮，以及新稿何时才可被接受。`决定`和`证据`无需重答。
- 原始订正作答：

  > 下一步动作：保留draft1作为当前最佳稿；把失败测试用于下一轮优化；只有新稿测试通过后才接受。

- 做对：已明确把失败测试作为下一轮的修复证据，并要求新稿通过测试后才接受。
- 尚未通过：`draft1` 只通过 4/5，已经违反功能硬约束，不能成为“当前最佳稿”；版本更新或速度更快都不能覆盖这一点。当前最佳稿必须是目前唯一通过 5/5 的版本。
- 单题状态：`RETRY`（只剩当前最佳稿版本选择错误）。
- 提示阶梯：提示 2——比较 `draft0 = 5/5` 与 `draft1 = 4/5`；先选满足全部硬约束的版本，再谈速度。

### RETRY2

- 订正要求：其余两段已通过，只填写：`保留____作为当前最佳稿。`
- 原始订正作答：

  > 保留draft0作为当前最佳稿。

- 批改：已选择目前唯一通过 5/5 功能硬约束的 `draft0`，没有让更快但已退化的 `draft1` 覆盖当前最佳稿；结合 RETRY1 已通过的失败证据回传与完整复验条件，质量回退闭环完整。
- 单题结论：`RETRY → PASS`。

- 当前任务级有效语义证据：8 个已 PASS——既有职责边界、G1 三阶段数据依赖、G2 短期记忆读取接口、G3 提示词职责与评审契约、G4 主循环与提前停止、G5 成本因果链、G6 适用场景、G7 质量不退化与回退保护。

## A4-05 带读阶段收口

- 完整带读：Hello-Agents 第 4 章 `4.4–4.4.5 Reflection` 与配套 `repos/hello-agents/code/chapter4/Reflection.py` 全文件。
- 任务边界选读：ADP Chapter 4 的模式定义、代码/摘要用例、trade-offs、At a Glance、Key Takeaways 与 Conclusion；LangChain/Google ADK 的完整框架实现细节未纳入本单元，不宣称通读整章。
- 三个必答问题覆盖：
  1. Reflection 适合什么任务：G6。
  2. Reflection 会增加哪些成本：G4、G5。
  3. 如何判断没有越改越差：G7。
- 防漏回扫：已覆盖定义、作用、三阶段输入输出、Memory、提示词、控制流、停止条件、适用场景、成本与质量回退；没有用换措辞重复既有职责边界。
- 尚未满足任务通过标准：目标脚本尚未创建，也没有真实 DeepSeek 的初稿、反思、改进稿和对比运行证据；任务继续保持 `DOING`，本次教学检查点通过不等于任务正式 PASS。
- notes 状态：tracker 未要求 A4-05 独立 notes，未新增同义笔记。
- 下一单元：已收到 `开始动手`，实现阶段接续如下。

## A4-05 实现阶段

### A4-05-C1 — 公告硬约束评估器

- 启动口令：`开始动手`。
- 设计顺序：先把日期、地点和 80 字限制做成确定性质量门槛，再连接模型；模型的“无需改进”不能代替该门槛。
- 给定任务：生成公司内部 AI Agent 学习分享会公告；必须包含 `2026年8月8日`、`3号会议室`，去除首尾空白后不超过 80 个字符。
- 用户实现：完成 `evaluate_draft(draft)`，返回 `length`、`date_present`、`location_present`、`within_length`、`all_hard_constraints_pass` 五个字段；最后一项只在前三个硬约束全部通过时为 `True`。
- 当前状态：用户于次日提交；验证与结论见 [daily/2026-08-04.md](2026-08-04.md) 的 A4-05-C1。
