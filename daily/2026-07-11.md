# 每日学习打卡

日期：2026-07-11

目标学习时长：约 6 小时（周末 Gate 整块）

实际学习时长：待填写

完成任务编号：T3-Gate（PASS；正式复核日期 2026-07-12）

看了哪些视频/哪一集：无

读了哪些文档/哪一节：T3-Gate rubric、`code/stage3/t3_gate_tool_assistant.py`、`code/stage3/eval_cases.json`

代码/产物位置：

- `code/stage3/t3_gate_tool_assistant.py`
- `code/stage3/eval_cases.json`

运行提示：项目根目录使用 `.venv/Scripts/python.exe`；正式复核按 `eval_cases.json` 直接执行，不保存专用 runner/baseline/原始报告。

验证命令与关键结果（Gate/项目日填写）：

- `.venv\Scripts\python.exe -m py_compile code/stage3/t3_02_calculator_tool.py code/stage3/t3_03_file_reader_tool.py code/stage3/t3_04_public_api_tool.py code/stage3/t3_gate_tool_assistant.py`：PASS。
- `Get-FileHash -Algorithm SHA256 code/stage3/eval_cases.json`：`76664937408435087A48691EE6EBE6287F0127E154CFB51671823950C67C042F`；两个 fixture SHA 均匹配。
- PowerShell here-string 构造一次性内存 evaluator，并通过 `... | .venv\Scripts\python.exe -` 直接执行 `eval_cases.json` 的四种 mode；正式主审与独立只读复核各跑一次，均为 14/14 PASS。未创建项目 runner、baseline 或报告文件。
- `cmd /d /c "echo exit|.venv\Scripts\python.exe code\stage3\t3_gate_tool_assistant.py"`：本地 `exit` 分支输出“退出程序。”。PowerShell 原生管道会给首行加 BOM，曾导致一次假失败；改用无 BOM 输入后确认不是程序缺陷。
- `git diff --check`：PASS；未发现真实密钥写入项目文件。

测试/eval/性能/安全摘要（Gate/项目日填写）：`t3-gate-v2` 全量 14/14；normal 10/10、failure 3/3、danger 1/1、holdout 3/3，failed IDs = `[]`。真实 DeepSeek 请求 19 次，全部使用 `deepseek-v4-pro`、3 个 tools schema、thinking disabled。组件结果：工具选择与参数 10/10、禁用工具 10/10、工具结果 21/21、轨迹 59/59、最终回答 13/13、安全断言 9/9。首次同版本正式结果，无历史 daily baseline 可比。

本次工作场景/复合故障：受控复合危险批次同时注入未知工具、恶意 URL、302 重定向逃逸和 `requests.Timeout`；另复现 HTTPS 空端口绕过边界并完成修复回归。

场景 ID 与类别：WS-03 测试/回归、WS-05 provider/API 故障、WS-07 工具与 Agent 安全。

故障输入、复现命令与关键日志：`D01` 注入 `delete_everything_tool`、`https://api.github.com@169.254.169.254/...`、允许域名返回 302 到 `http://127.0.0.1/admin`、允许域名抛 Timeout；内存 mock 输出为 Python 工具执行 2、网络请求 2、重定向跟随 0、四条错误结果均保留对应 ID。空端口定向用例覆盖无端口、`:443`、`:8443`、空 `:`、文本端口、越界端口，结果 6/6。

定位证据与全部根因：① 未知名字必须先查 `TOOLS`，不能动态执行；② `urlparse().hostname` 会把 userinfo 后的 metadata IP 识别为真正 host，必须按解析后 host allowlist 拒绝；③ `requests` 默认跟随 302，只校验初始 URL 会产生重定向逃逸；④ `urlparse().port is None` 同时覆盖“未写端口”和“冒号后为空”，它是宽松解析器而非严格校验器；⑤ timeout 是工具执行后的依赖失败，应捕获并返回稳定错误。

修复/止损、定向测试与全量回归：工具名白名单、`json.loads` + dict/schema/字段校验、host/443 allowlist、`netloc.endswith(":")` 空端口拦截、`timeout=5`、`allow_redirects=False`、3xx 稳定拒绝、最大工具轮数 3；空端口定向回归 6/6，随后全量 14/14。

恢复/再次故障注入结果：修复后再次注入 D01，未知工具与 SSRF 在 Python 执行前拦截；302 未跟随；timeout 稳定返回；四个 `tool_call_id` 均有对应结果。F03 同时证明第 4 次模型响应可到达，但 `round_4_call` 不执行，稳定停止。

工作场景证据等级：WS-03 `DIAGNOSED_AND_FIXED`；WS-05 `EXECUTED`；WS-07 `DIAGNOSED_AND_FIXED`。

Gate 预设阈值与 holdout 结果：首次调参前预设 normal ≥ 90%、failure = 100%、danger = 100%、holdout = 100%；冻结 holdout 3 条（占 3/14），正式结果依次为 100%、100%、100%、100%，全部满足。

算法内容块状态与证据（W4 起）：W4 收尾仍为 0/4 道新题，判 `DEBT`，4 道新题欠账结转为 W5 优先项；该欠账不掩盖也不阻断 T3 课程 Gate PASS。

岗位证据候选（仅里程碑填写，普通日留空）：无 `JOB_EVIDENCE` 候选；本次仅支持 Tool Calling 升为 `COURSE_PASS`，仍缺作品级工程、自动测试/README/架构与交叉复核。

## 今日练习题

题集规模：6 个 T3-Gate 高质量检查点，严格逐题推进。

### 学习定位

- 为什么学这个：T3-Gate 要证明的不只是代码能跑，还要能独立解释“模型选择、客户端执行、工具回填”的责任边界。
- 学完能搞懂 / 能做什么：能读懂并调试真实 `tools/tool_calls` 闭环，处理坏参数、工具失败、危险 URL、长结果和最大轮数。
- 对接下来的影响：这是 A4 ReAct/Agent 控制循环的直接前置；责任主语混乱会导致模型自编 Observation 或客户端误执行危险调用。

### 生成依据

- 当前学习进度：T3-01~04 已 PASS，T3-Gate 为当前硬检查点。
- 最近学习问题：`tools` schema 与 `TOOLS` 注册表、SDK message 回填、工具轮数、评估产物边界。
- 薄弱点 / RETRY：WP-14 模型自编 Observation、WP-17 工具菜单与回填主语、WP-21 Action JSON 严格格式；兼顾 WP-16/18。
- 工作场景覆盖：WS-03 尚未正式验证；WS-05、WS-07 需要在 Gate 形成当前代码证据。
- 算法配额：W4 尚欠 4 道新题，另开算法小节处理。
- 关联旧知识点：JSON 字符串 → `json.loads()` → dict → schema/业务校验 → `json.dumps()` 回填。

### A1｜责任边界与完整闭环

场景：用户要求“精确计算 `3.14 + 2.71`，同时读取 `sample.txt` 最多 40 个字符”。

请按发生顺序说明下面四个角色分别负责什么，并明确谁**不能**直接执行 Python 函数：

1. 小写 `tools`
2. 大写 `TOOLS`
3. 客户端程序
4. 模型

你的说明必须覆盖：谁把 schema 传给谁、谁生成 `assistant.tool_calls`、谁解析/校验 `arguments`、谁从注册表执行两个真实函数、谁追加 `role="tool" + tool_call_id`、谁发起第二次模型请求并得到最终回答。

原始作答：

> 1.tools是给模型看的，让模型知道有哪些工具可以调用
> 2.TOOLS是给客户端程序的，模型调用的时候根据工具箱中的函数去进行匹配，然后去执行函数
> 3.客户端负责传递 schema，解析并校验 arguments，执行两个真实函数、谁追加 role="tool" + tool_call_id，发起第二次模型请求并获得最终回答
> 4.生成 assistant.tool_calls，不能直接执行 Python 函数

订正过程：

- 初次判定：`PARTIAL`。
- 已掌握：`tools` 是传给模型的工具 schema 菜单；客户端负责参数解析与校验、真实函数执行、工具结果回填及后续请求；模型只能生成 `assistant.tool_calls`，不能直接执行 Python 函数。
- 待订正 1：第 2 点的执行主语有歧义。模型不会访问 `TOOLS`；应明确是客户端读取并校验 `tool_call.function.name` 后，从 `TOOLS` 注册表查找并调用真实函数。
- 待订正 2：第 4 点还需说明模型在第二次请求中接收 `role="tool"` 的真实执行结果，再据此生成最终自然语言回答。
- 订正作答：

  > 2.TOOLS是给客户端程序的，模型调用的时候，客户端根据工具箱中的函数去进行匹配，然后去执行函数
  > 4.第二次模型请求并获得最终回答是模型读取客户端回填的真实工具结果，再生成最终回答

- 订正判定：第 2 点已把查表和执行的主语明确为客户端；第 4 点已补齐模型读取真实工具结果并生成最终回答的职责。结合原答案第 1、3 点，闭环责任边界完整。

单题状态：`PARTIAL → PASS`。

### A2｜读码预测：调用次数与工具轮数

沿用当前 `run_agent()` 的核心循环：

```python
while True:
    response = client.chat.completions.create(...)
    message = response.choices[0].message

    if not message.tool_calls:
        return message.content

    if tool_rounds >= max_tool_rounds:
        return f"已达到最大工具调用轮数 {max_tool_rounds}，停止执行。"

    messages.append(message)
    for tool_call in message.tool_calls:
        tool_result = execute_tool_call(tool_call)
        messages.append({
            "role": "tool",
            "content": json.dumps(tool_result, ensure_ascii=False),
            "tool_call_id": tool_call.id,
        })
    tool_rounds += 1
```

设 `max_tool_rounds = 3`，初始 `messages` 已有一条 `system` 和一条 `user`。模型按顺序返回：

1. 第一次响应：`tool_calls = [call_calc, call_file]`，分别调用计算器和读文件工具。
2. 第二次响应：`tool_calls = [call_api]`，调用公开 API 工具。
3. 第三次响应：`tool_calls = None`，`content = "三个任务均已完成"`。

三个真实工具都执行成功，过程没有异常。请预测并说明：模型 API 一共调用几次、Python 工具函数一共执行几次、`tool_rounds` 最终是多少；初始两条消息之后，客户端依次追加哪些 `role` 和对应的 `tool_call_id`；`run_agent()` 最终返回什么。最后解释为什么“工具执行次数”不能直接当成“工具轮数”。

原始作答（2026-07-12 续答）：

> 1.模型 API 调用了3次
> 2.Python 工具函数执行2次
> 3.tool_rounds最终值是2
> 4.{"role": "tool", "tool_call_id": tool.id, "content": "tool_calls"}
> 5.返回的是一个调用工具的请求
> 6.一次轮数可以执行多次工具

订正过程：

- 初次判定：`RETRY`。
- 已答对：模型 API 调用 3 次；`tool_rounds` 最终为 2；一次工具轮次可以包含多个 `tool_call`，所以工具执行次数不能直接当成工具轮数。
- 待订正 1：工具执行次数应按两个工具调用列表中的每一个 `tool_call` 分别累计，不能按工具轮次计数。
- 待订正 2：客户端每轮先执行 `messages.append(message)` 保存带 `tool_calls` 的 `assistant` 消息，再为本轮每个调用分别追加一条 `role="tool"` 消息；需要写出完整顺序和三个实际 `tool_call_id`。`content` 是序列化后的真实工具结果，不是字符串 `"tool_calls"`。
- 待订正 3：第三次响应没有 `tool_calls`，应沿着 `if not message.tool_calls` 分支判断 `run_agent()` 的返回值，而不是把它继续当成工具请求。
- 第二次作答：

  >  2.每个工具都执行了一次
  > 4.每轮先追加完整的 assistant 消息，再为每个调用追加一条 role="tool"；ID 来自 tool_call.id，content 是工具结果的 JSON 字符串。
  > 5.最终返回`三个任务均已完成`

- 第二次判定：第 2 点已能区分“每个工具调用”和“每轮”，结合题面三个工具可得总执行数；第 5 点正确走到了无 `tool_calls` 时返回 `message.content` 的分支。第 4 点仍只复述了规则，没有按题目列出两轮中五条追加消息的完整顺序及 `call_calc`、`call_file`、`call_api` 的对应关系。
- 第三次作答：

  > 第一轮：assistant(tool_calls = [call_calc, call_file])->tool(tool_call_id=ool_call_id)
  > 第二轮：assistant(call_api)->tool(tool_call_id=ool_call_id)
  > 第三轮：assistant()->content("三个任务均已完成")

- 第三次判定：已经识别出前两次模型响应包含工具调用、第三次响应包含最终内容，但仍有三处关键不精确：第一轮遗漏了第二条 `role="tool"` 消息；两条工具消息都没有分别使用题面给出的实际调用 ID；第二轮的 `assistant` 应保存 `tool_calls=[call_api]`。此外，第三次响应会在返回分支直接 `return message.content`，不会被 `messages.append(message)` 追加，也不属于工具轮次。
- 第四次作答 / 迁移追问：

  > 不应该追加到messages中，但是我感觉实际场景中应该还是要追加到messages中

- 第四次判定：对当前代码的预测正确——第三次响应命中直接返回分支，不会追加到局部 `messages`。迁移判断也成立：若客户端要持久保存会话并支持用户下一轮追问，应在返回前保存最终 `assistant` 消息；当前 `run_agent()` 的 `messages` 只在单次函数调用中存在，返回后即丢弃，因此不追加不影响本次输出。A2 仍待把三条 `role="tool"` 消息分别绑定到各自的 `call_calc`、`call_file`、`call_api`。
- 第五次作答 / 题面质疑：

  > 这个id就是模型返回给每轮对话的id，你没有给出具体的id

- 第五次判定：用户正确指出题面没有提供真实响应中的 `call_00_...` 字符串；题面里的 `call_calc`、`call_file`、`call_api` 原意是三个 `tool_call.id` 的符号代号，之前称作“实际调用 ID”不够准确。仍需订正所属层级：ID 是模型为每一个 `tool_call` 返回的，不是“每轮对话只有一个 ID”；同一条 `assistant` 响应若含两个 `tool_calls`，就会有两个不同 ID，客户端回填时逐一原样对应。
- 最终订正作答：

  > 一条 assistant 响应包含两个 tool_calls 时，会有两个 tool_call_id。客户端回填时按照对应的工具回填对应的tool_call_id

- 最终判定：已明确掌握 `tool_call_id` 按单个工具调用生成，同一响应可含多个不同 ID；客户端必须让每条工具结果与对应调用 ID 一一匹配。结合前面已订正的模型调用次数、工具执行次数、工具轮数、最终返回分支及持久会话差异，A2 达到单题通过标准。

单题状态：`RETRY → PARTIAL → PARTIAL → PASS`。

### A3｜失败批次调试：参数拦截与真实工具失败

客户端在同一条 `assistant` 响应中收到下面两个工具调用，并按顺序对每一个调用执行 `execute_tool_call()`：

```python
tool_calls = [
    {
        "id": "missing_b",
        "name": "calculator_tool",
        "arguments": '{"operation":"add","a":1}',
    },
    {
        "id": "division_by_zero",
        "name": "calculator_tool",
        "arguments": '{"operation":"div","a":10,"b":0}',
    },
]
```

当前校验/执行顺序是：工具白名单 → `json.loads()` → 必须是 `dict` → 必填字段与多余字段 → 字段类型/取值 → `TOOLS[name](**arguments)`。真实 `calculator_tool` 在除零时返回 `{"ok": false, "error": "除数不能为 0"}`。

请逐个说明：每个调用在哪一步结束、是否真正进入 `calculator_tool`、应返回的关键错误；再给出本批次真实 Python 工具执行总次数，并说明为什么第一个调用失败后仍应继续处理第二个调用和分别回填两个 `tool_call_id`。

原始作答：

> 1.两个调用都在json.loads()结束，因为不是标准的json
> 2.没有真正进入calculator_tool
> 3.missing_b中，'{"operation":"add","a":1}'不是标准的JSON，其次，缺少参数"b"。division_by_zero中，'{"operation":"div","a":10,"b":0}'不是标准的JSON，其次除数为 0了
> 4.真实 Python 工具一次没有执行
> 5.这样可以让模型知道对应的是哪一次哪一个工具的调用

订正过程：

- 初次判定：`RETRY`。
- 关键混淆：代码片段最外层的单引号是 Python 字符串定界符，不属于字符串内容；两个字符串内部的键名都使用 JSON 要求的双引号，因此二者都能被 `json.loads()` 解析为 `dict`。
- `missing_b`：应继续到必填字段校验，因缺少 `b` 被客户端拦截，不进入真实计算器。
- `division_by_zero`：JSON、必填字段和字段取值校验均能通过，会真正执行一次 `calculator_tool`，再由工具返回“除数不能为 0”。因此批次真实工具执行总次数不能记为 0。
- 第 5 点方向正确但不完整：同一响应中的多个 `tool_call` 是独立调用，一个参数失败不应吞掉其他调用；客户端还必须为每个调用都产生对应的 `role="tool"` 结果并原样带回各自 ID，保持工具调用协议闭合，让模型区分每个结果。
- 订正作答：

  > 1.missing_b在必填/多余字段中止，division_by_zero在调用函数时，内部判断中止。
  > 2.missing_b没有进入calculator_tool，division_by_zero进入了calculator_tool
  > 3.missing_b中，缺少参数"b"。division_by_zero中，除数为 0了
  > 4.真实 Python 工具执行了一次
  > 5.这样可以让模型知道对应的是哪一次哪一个工具的调用，多个调用结果彼此独立，每个工具每次调用时都会获取对应role="tool" 回填

- 订正判定：两个 JSON 字符串均能成功解析；`missing_b` 在必填字段检查被客户端拦截，`division_by_zero` 进入真实函数后返回业务错误，执行计数为 1；也已说明批次调用彼此独立并需逐个 ID 回填。措辞上应以客户端为主语——是客户端为每个调用追加对应的 `role="tool"` 消息，而不是工具自己“获取回填”；该措辞不影响本题核心判断。

单题状态：`RETRY → PASS`。

### A4｜真实代码修改：拦截 HTTPS 空端口

当前文件：`code/stage3/t3_gate_tool_assistant.py`

`execute_tool_call()` 的 URL 校验目前把 `parsed_url.port is None` 视为“没有显式端口，使用 HTTPS 默认 443”。但 `urlparse()` 对下面两种输入都会给出 `port is None`：

```text
https://api.github.com     # 合法：没有显式端口
https://api.github.com:    # 非法：写了冒号却没有端口值
```

因此第二种输入会绕过当前端口校验并进入真实工具。

请直接修改当前 `execute_tool_call()`，满足：

- `https://api.github.com`：通过 URL 校验。
- `https://api.github.com:443`：通过 URL 校验。
- `https://api.github.com:8443`：客户端拒绝，不执行工具。
- `https://api.github.com:`：客户端拒绝，不执行工具。
- `https://api.github.com:abc` 与越界端口：稳定返回端口错误，不抛异常、不执行工具。

约束：保留现有 host allowlist 与 HTTPS 校验；不要通过发真实网络请求验证；不要新建 runner、baseline 或报告文件。修改后说明根因、改了哪一段，并把文件保存好，随后由当前练习直接做内存验证。

原始作答 / 代码修改：

> 添加了判断：            if parsed_url.netloc.endswith(":"):
>                 return {
>                     "ok": False,
>                     "error": f"工具 {tool_call_function_name} url 字段端口号不合法",
>                 }
> "https://api.github.com:"会被赋默认值443，为什么还要去判断:？

验证与订正过程：

- 已检查保存后的代码：空端口判断位于 `urlparse(url)` 之后、host/`.port` 检查之前。
- 无网络内存验证：用 stub 替换真实 `public_api_tool`；无端口与 `:443` 均通过并执行 stub，`:8443`、空端口、文本端口、越界端口均返回稳定端口错误且未执行 stub，最终 `6/6`，stub 总执行次数 `2`。
- 验证脚本前两次显示的拒绝案例 `FAIL` 是检查脚本通过 PowerShell 管道传递中文匹配字面量时发生编码误判；改用 Unicode 转义复核后全部通过，被测代码返回值与执行计数始终正确。
- 待订正概念：`urlparse()` 没有给空端口“赋默认值 443”。它只是宽松解析，并让“完全没写端口”和“写了冒号但端口为空”两种情况的 `.port` 都等于 `None`；网络客户端可能在后续因 HTTPS 选择 443，但那不是语法校验。新增 `netloc.endswith(":")` 正是为了先区分并拒绝后者。
- 当前代码第 328 行附近的注释也需同步澄清：显式 `:443` 时 `.port == 443`，不是 `None`。
- 概念订正作答：

  > urlparse() 是宽松解析器，不负责判定空端口是否合法，所以必须通过 netloc.endswith(":") 主动拦截

- 概念判定：根因表述正确。再次检查文件发现空端口判断旁的新注释已加入，但后面的旧注释仍写着“`.port is None`……或者显式写出了 443”；该句尚未同步，显式 `:443` 实际得到整数 `443`。
- 注释修改反馈：

  > 修改完了

- 最终文件核对：注释已改为先说明显式空端口由前置判断拦截，再说明未显式提供端口与显式 `443` 可通过；与后面的 `[None, 443]` 判断一致。结合无网络内存验证 `6/6`、拒绝案例工具执行次数为 0，以及根因说明，A4 达到单题通过标准。

单题状态：`PARTIAL → PASS`。

### A5｜危险批次事故：坏工具调用、SSRF、重定向与超时

同一条 `assistant` 响应给客户端四个调用：

```text
danger_unknown:
  name = delete_everything_tool
  arguments = {}

danger_ssrf:
  name = public_api_tool
  url = https://api.github.com@169.254.169.254/latest/meta-data

danger_redirect:
  name = public_api_tool
  url = https://api.github.com/redirect-test

danger_timeout:
  name = public_api_tool
  url = https://api.github.com
```

无真实网络，故障注入结果固定为：

```text
GET https://api.github.com/redirect-test
→ 302 Location: http://127.0.0.1/admin

GET https://api.github.com
→ raises requests.Timeout
```

请以事故处理人的身份完成这一整个检查点：

1. 逐个判断是在客户端执行前拦截，还是会进入真实 `public_api_tool`；写出关键错误结果。
2. 给出总计：真实 Python 工具执行次数、网络请求次数、是否跟随了重定向，以及应回填多少条 `role="tool"` 消息。
3. 说明若旧实现只校验初始 URL、且 `requests` 默认跟随 302，会造成什么危险；当前代码用哪些最小措施止损和修复。
4. 说明如何再次注入同样故障做定向回归，并写出至少一个关闭自动重定向的取舍或剩余风险。

原始作答：

> 1.danger_unknown，danger_ssrf，danger_redirect在客户端执行前拦截，danger_timeout进入真实工具拦截。danger_unknown关键错误是想直接调用函数删除所有工具，eval?danger_ssrf是请求的地址不合法，danger_redirect被重定向了，danger_timeout是连接超时
> 3.只校验初始 URL、然后自动跟随 302，地址可能指向本地了。但是只会校验初始 URL，不会第二次校验。当前代码是发起请求时禁止重定向
> 4.不知道

事故处理、验证与订正过程：

- 初次判定：`RETRY`。危险输入属于关键安全案例，必须把四条路径、计数和再次故障注入全部答完整。
- 已答对：`danger_unknown` 与 `danger_ssrf` 会在真实工具执行前被客户端拦截；`danger_timeout` 会进入真实工具并得到超时错误；只校验初始 URL 后自动跟随 302 可能访问本地/私网目标，当前工具通过禁用自动重定向止损。
- 待订正 1：未知工具的稳定错误是工具名不在 `TOOLS` 白名单，和 `eval` 无关，也不是“删除所有工具”已经发生。
- 待订正 2：`danger_redirect` 的初始 scheme/host/port 合法，因此会进入 `public_api_tool` 并发出一次请求；工具收到 302 后因为未自动跟随而返回“拒绝重定向”。它不是执行前拦截。
- 待订正 3：第 2 问完全漏答。应分别按“进入真实 Python 工具”“调用 mock 网络”“实际跟随 Location”“原始 tool_call 数量”计算四个总数；无论成功或失败，每个原始调用都要有自己的 `role="tool"` 回填。
- 第 4 问提示 1：查看 `eval_cases.json` 的 `D01` 中 `network_mocks` 和 `expected`。回归时应再次用 mock 注入同样的 302 与 `requests.Timeout`，断言错误、调用计数和未跟随重定向；取舍从“合法 API 也可能使用 3xx”以及“若未来允许重定向，必须逐跳重新校验 Location”思考。
- 第二次作答（按用户原话修正此前截断的记录）：

  > 1.danger_unknown时工具名不存在，不是提供的标准格式，danger_redirect是工具拦截，在请求中显示的禁止。
  > 2.真实 Python 工具执行次数两次，网络请求次数两次，重定向跟随次数0次，role="tool" 回填条数分别是1条
  > 4.用 mock 再次注入同样的 302 和 requests.Timeout，检查稳定错误、调用计数和重定向未跟随。取舍是合法 API 的 3xx 也会被拒绝；若未来允许重定向，必须逐跳重新校验 Location。

- 第二次判定：真实工具执行 2 次、网络请求 2 次、重定向跟随 0 次，以及故障再注入和关闭自动重定向的取舍均正确。仍需修正：未知工具的依据是“不在客户端 `TOOLS` 白名单”，不是“格式不标准”；第 1 问没有完整列出 `danger_ssrf` 与 `danger_timeout`；`danger_redirect` 需明确为进入真实工具、发出一次请求后拒绝 302。四个原始调用确实各回填一条，但题目要求的总数仍需明确写出。
- 第三次作答：

  > danger_unknown：执行前拦截。关键错误：工具名不存在TOOLS的白名单中
  > danger_ssrf：执行前拦截。关键错误：地址不在白名单中
  > danger_redirect：进入工具。关键错误：地址被重定向了
  > danger_timeout：进入工具。关键错误：连接超时
  >
  > role="tool" 回填总数：2

- 第三次判定：四条路径及关键错误已全部判断正确；最后一个协议计数仍错。`execute_tool_call()` 即使在真实函数执行前返回白名单/URL 错误，外层 `for tool_call in message.tool_calls` 仍会把该错误结果追加成一条 `role="tool"` 消息，所以回填数按原始四个 `tool_call` 计，而不是按真正执行的两个工具计。
- 第四次作答：

  > 4条，因为模型调用了4次，不管是客户端拒绝还是其他情况，都需要给模型把id返回去。让模型知道是哪次工具调用

- 第四次判定：回填总数 4 条及“无论执行前拒绝还是工具返回错误都要按 ID 回填”的理由正确；但“模型调用了 4 次”仍混淆层级。本场景是一次模型响应中包含 4 个 `tool_call`，不是模型 API 被调用 4 次。
- 最终订正作答：

  > 模型 API 响应次数：1次
  > 该响应中的 tool_call 数：4次
  > 为什么回填 4 条：因为一次模型的请求中调用了4次工具

- 最终概念判定：已区分一次模型 API 响应与其中四个 `tool_call`；结合前一轮“执行前拒绝也按 ID 回填”的解释，回填协议理解完整。
- 无网络故障再注入：mock `requests.get` 固定返回危险 302 或抛出 `requests.Timeout`，直接执行当前四条调用。未知工具拦截、SSRF 拦截、302 拒绝、超时稳定错误、真实工具执行 2 次、网络请求 2 次、重定向未跟随、`role="tool"` 回填 4 条及四个 ID 保持对应，全部断言通过（`all_pass=true`）。
- 恢复/再注入结论：修复后再次注入相同危险批次仍保持安全停止；合法 3xx 被一并拒绝是当前安全取舍，未来若支持重定向必须逐跳校验新 URL。

单题状态：`RETRY → PARTIAL → PARTIAL → PARTIAL → PASS`。

### A6｜Gate 收尾：最大轮数与分项阈值

先看 `F03_max_tool_rounds`：`max_tool_rounds=3`，脚本模型连续四次都返回一个计算器 `tool_call`。当前循环在每次模型响应后先检查是否已达到最大工具轮数，再决定是否执行本轮工具。

再看两份候选汇总（T3 共 10 normal、3 failure、1 danger，其中 3 条 normal 属于 holdout）：

```text
候选 A：normal 9/10，failure 3/3，danger 1/1，holdout 3/3
候选 B：normal 10/10，failure 3/3，danger 0/1，holdout 3/3
```

T3 阈值：normal ≥ 90%，failure = 100%，danger = 100%，holdout = 100%；各类分别判断，未执行案例阻断 Gate。

请完成最后检查点：

1. 写出 F03 的模型 API 调用次数、真实工具执行次数、最终 `tool_rounds`、未执行的调用 ID，以及稳定停止文本；解释为什么不是只调用模型 3 次。
2. 判断候选 A、B 哪个满足上述数值阈值；说明为什么不能只看 14 条案例的总体通过率。
3. 如果 3 条 holdout 中有 1 条未执行，能否判 Gate 通过？为什么？
4. 按当前评估产物边界，项目长期保留哪个评估文件，正式结果写到哪里；为什么本关不再保存专用 runner、baseline 和原始报告。

原始作答：

> 1.API 调用次数4次，真实工具执行次数3次，最终 tool_rounds返回已达到最大工具调用轮数 3，未执行的调用 ID是round_4_call，停止文本是已达到最大工具调用轮数 3。因为发出了4次调用工具的请求
> 2.A满足数值阈值，因为一些危险错误一个也不能放过，必须全部验证通过
> 3.不能通过
> 4.这条有必要问吗？感觉没有用

验证与订正过程：

- 初次判定：`PARTIAL`。
- 已答对：模型 API 调用 4 次、真实工具执行 3 次、`round_4_call` 不执行；候选 A 满足分项阈值，危险案例不能漏过；holdout 有一条未执行时不能通过。
- 待订正 1：`tool_rounds` 是计数变量，最终值应单独写成数字；稳定停止文本也应完整写出“已达到最大工具调用轮数 3，停止执行。”。第四次模型调用是为了取得第四条模型响应，客户端看到其中仍有工具调用后才发现已达上限并拒绝执行，不是已经执行了第四次工具。
- 第 2 问补充：候选 B 即使总体通过率很高，也因 danger 为 0/1 直接失败；normal、failure、danger、holdout 的阈值分别判断，不能用总体分数稀释关键安全失败。
- 第 4 问计分调整：该问源自近期对评估产物过重的真实问题，用于确认“只保留 `eval_cases.json`、正式结果写 daily”的流程边界；用户认为在本检查点价值低。接受该反馈，从 A6 技术计分中移除，不要求继续作答，也不让它阻断 Gate 练习。
- 第二次作答：

  > 模型 API 调用次数：4次
  > 真实工具执行次数：3次
  > tool_rounds 最终数字：round_4_call
  > 未执行的调用 ID：round_4_call
  > 完整停止文本：已达到最大工具调用轮数 3
  > 为什么必须收到第四次模型响应后才能停止：第四次客户端判断达到最大调用次数给停止掉的。模型才知道后续不需要继续发调用请求了

- 第二次判定：模型 API 4 次、真实工具 3 次、未执行 ID 正确；仍有三处控制流混淆：`tool_rounds` 是数值计数，不能填写调用 ID；停止文本缺少“，停止执行。”；客户端收到第四条响应后直接把停止文本返回给调用者，不会再请求模型，因此不是“让模型知道停止”。必须先收到第四次响应，是因为客户端在看到其中仍含 `tool_calls` 之前，无法知道模型还想继续调用工具。
- 最终订正作答：

  > tool_rounds 最终值：3
  > 完整停止文本：已达到最大工具调用轮数3，停止执行。
  > 为什么必须先收到第四次模型响应，以及停止文本最终返回给谁：客户端停止后直接向 run_agent() 的调用者返回文本，不会再请求模型

- 最终判定：`tool_rounds=3`、稳定停止文本及返回对象均已订正；结合上一轮已说明第四次响应触发客户端上限判断，F03 控制流达到单题通过标准。标点/空格差异不影响语义断言。

单题状态：`PARTIAL → PARTIAL → PASS`。

## 练习过程记录

我的作答：逐题记录在对应题目下。

遇到的问题：有效 JSON 被误判为非法；模型 API 次数、工具轮数、`tool_call` 数、真实函数执行数和 `role="tool"` 回填数多次混淆；一度把最大轮数计数写成调用 ID；初次把空端口理解成已经被赋默认 443。

我最不确定的点：一次模型响应包含多个 `tool_call` 时，各层次数和 ID 如何分别统计，以及停止文本最终返回给谁。

> 下面是批改与小结区：

希望复核/检查：T3-Gate 全部代码、14 条案例、必答问题、WS-03/05/07 与 holdout。

批改判定：`PASS`。6 个检查点均完成订正并达到单题 PASS；代码可运行，14 条正式评估和关键边界全部满足 T3-Gate rubric。

复核判定（普通任务默认留空；JOB_EVIDENCE、FINAL/J11-Gate、投递前必须由另一工具填写）：

当天进度小结：完成原生 `tools/tool_calls` 三工具闭环、坏参数与工具失败隔离、URL/重定向安全、最大轮数控制和分项 Gate 评估；A4 练习中独立修复空端口校验并通过定向及全量回归。

总进度小结：阶段 3 的 T3-01~T3-Gate 全部 PASS；下一主任务为 A4-02。W4 算法 4 道新题形成显式 DEBT，进入 W5 后优先补，不将课程 PASS 冒充算法完成或岗位就绪。

对问题/不确定点的解释：`tool_call_id` 按每个工具调用生成，不按模型轮次生成；每个调用即使在客户端执行前被拒绝，也要回填一条对应的 `role="tool"` 错误结果。`urlparse` 是宽松解析器，HTTPS 默认 443 是后续网络语义，不等于空端口语法已经合法化。单次 `run_agent()` 可直接返回最终 assistant 文本；持久多轮会话才需要在返回前保存它。

问答点评与补充：今天最虚 / 下次重点复习：画一条“模型请求/响应 → 多个 tool_call → Python 执行或客户端拒绝 → 同 ID role=tool → 下一次模型请求”的轨迹，并分别数五种数量。你的安全直觉和修复能力已达标，但计数名词仍需要一次脱离原题的独立回炉。

超前内容提示：A4 会把当前一次工具闭环扩成多步 ReAct/Plan/Reflection；若启用 DeepSeek thinking + tools，还必须完整回传 `reasoning_content`，并增加结构化步骤日志、限步与安全停止。

补救任务：不阻断 T3 PASS。W5 首次练习安排 1 道新轨迹计数题复测 WP-22，并复测“Python 字符串定界符不属于 JSON 内容”；切换 A4 前把已实现代码里的骨架式 `TODO` 改成普通说明或删除。W4 算法 4 道新题按独立算法小节逐题补齐。
