# 每日学习打卡

日期：2026-07-01（周三）

> 注：工作日填充日——队列下一个是 PR2-Gate（Gate 留周末 07-04 整块做，周五 07-03 只做设计准备），按 daily-plan 规则 4 拉「下一阶段概念预习」：**T3-01 函数调用概念**（阶段 3 第一课）。
> 白天手机读 4 节 HF 中文资料；晚上 learning-coach 带读逐句验证（7 题边读边练）＋按新约定共写笔记（骨架先行 → 逐节聊天作答 → 原话落盘 + ⚠️订正/💡补充标注）。带读验收 PASS。

学习时长：

完成任务编号：T3-01 函数调用概念（预习性质；正式 PASS 判定待「检查」回填 progress）

今日目标：

- 搞懂「模型决策 vs 程序执行」：LLM 不执行工具，只生成调用请求；执行、解析、回传全在客户端程序。
- 讲清整条链：用户问题 → 模型选工具+生成参数（Action，吐完即停）→ 程序解析 → 程序执行工具 → 结果当 Observation（`role:"tool"`）拼回 messages 重发 → 模型收尾或再转一轮。
- 区分 Function Calling（能力训练焊进权重、走专用字段 `tool_calls`）vs 手搓 prompt 教学法（临场求、格式脆）；Tool Calling = Function Calling，同一机制两个名字。

看了哪些视频/哪一集：（无）

读了哪些文档/哪一节：

- `repos/agents-course/units/zh-CN/unit1/tools.mdx`、`actions.mdx`、`observations.mdx`
- `repos/agents-course/units/zh-CN/bonus-unit1/what-is-function-calling.mdx`

代码/产物位置：

- `notes/stage3/t3_01_function_calling.md`（共写笔记：用户原话为主体，含两处自纠痕迹）

运行提示：（纯概念日，无代码）

带读中踩的坑（当天记录，「检查」时登记错题本）：

- 中间人 = 客户端程序（先后错猜「工具自己执行」「平台执行」，×2）
- Agent = 模型？→ Agent = LLM + 工具 + 调度程序（客户端）组成的系统
- 「停止并解析」位置（两次放到收尾）→ Action 吐完瞬间停；防模型自编 Observation（把槽位「占掉」）
- Function Calling 名字坑：模型不调用函数、只生成调用请求；能力是训练焊进去的，不是 prompt 求来的

> 以下留给「开始今天的练习」「检查今天的学习」回填。

---

## 今日练习（T3-01 函数调用 · 13 题 · 聊天逐题作答）

> 锚定：T3-01 带读内容＋今日 4 坑；回炉 WP-05+WP-06（06-18 最久未测）、WP-02（06-23）、WP-01（🔴 常驻）。逐题作答、边批边回填。

### A. 快速回忆

**A1** 面试官问「大模型是怎么执行工具的？」——这句问话埋了什么坑？并给出两三句正确回答。
```python
# 用户答（第1轮）：模型不能直接调用工具，需要通过客户端去调用
# ——核心事实 ✅；但「坑在哪」没点名，模型侧的产出与链条收尾没说
# 用户答（第2轮补齐）：① 坑=「执行」这个词，模型不能执行工具，执行本身就是错的预设；
#   ② 模型产出工具的入参，客户端解析并调用工具，工具返回结果给客户端，
#      客户端再返回给模型，模型根据返回结果整理回答用户
# 订正（一毫米）：模型产出的严格说是「工具名 + 入参」——选哪个工具也是模型决定的
# 判定：PARTIAL → PASS
```

**A2** Action 和 Observation 各是什么？各由谁产生（模型 / 我的程序）？
```python
# 用户答：Action 是行动，模型产生的(JSON)；Observation 是客户端产生的，
#        客户端会把结果返回给模型，让模型判断这个结果是否可用
# ——两个归属全对（这题钩子：Observation 若由模型产生=自编事故）
# 补充：Action 精确说=调用请求（工具名+参数）；Observation=工具真实执行结果（含报错）
# 判定：PASS
```

**A3** 回炉 WP-05+WP-06：① temperature 改的到底是什么？② k=1/T→0 的解码叫什么、哪一端、什么特点？
```python
# 用户答：① 调高=压平，概率差距变小，低概率 token 也有机会；
#           调低=拉大差距，低概率 token 几乎不可能再出现
#         ② 贪心算法，在温度低端，输出更稳定
# ——两问全对，从机制层面答的（不是背「随机」）；贪心方向没再接反
# 判定：PASS ×2（WP-05、WP-06 独立复测通过，检查时升 ✅ 设回炉日）
```

### B. 解释与比较

**B1** 手搓 prompt 教工具 vs Function Calling，核心区别？为什么后者格式稳？（能力从哪来？）
```python
# 用户答（第1轮）：FC 是出厂前训练好的、模型内在的记忆；prompt 后期输入属外在，
#                模型可能会忘记，导致格式不稳
# ——能力来源 ✅；但「记忆/忘记」正是笔记里刚校准过的两处措辞，回炉重问
# 用户答（第2轮）：上下文太长导致注意力稀释？焊在模型中
# ——「焊在权重」半对；「注意力稀释」是长上下文的病，短 prompt 也吐歪，不成立
# 用户答（第3轮，提示 WP-12 后）：还是模型的概率问题，预测下一个 token，
#                低概率不是不会出现，它（prompt）不能保证什么
# ——✅ 连到根：prompt=事前软请求，只推高概率不保证；采样永远可能抽中歪格式；
#    FC 稳=训练把守格式焊进权重。「忘记」的正确说法=「读到 ≠ 必须照做」（模型无所谓忘，
#    说明书每轮都在上下文里）
# 判定：PARTIAL → PASS（WP-12 提示后连线成功，属辅助命中，错题本状态不动）
```

**B2** 纠正同事「ChatGPT 就是个 Agent」：Agent 比聪明模型多了什么？
```python
# 用户答：多了工具和客户端。模型本身没有感知外界环境的能力，是工具赋予的；
#        模型通过输出 JSON 和工具名让客户端去调用工具
# ——组件齐（工具+调度程序）、机制对
# 补充（面试收尾句）：光有模型=chatbot，能说「我帮你查」但说完啥也不发生；
#   配上工具+调度循环，说的话才能变成动作
# 判定：PASS
```

**B3** 「停」为什么必须在 Action 吐完立刻发生？不停的最隐蔽事故是什么（点名槽位）？
```python
# 用户答：模型会伪造工具返回结果。槽位名字不知道 →（提示 A2 后）观察？→ ✅ Observation
# ——事故机制答对；「槽位=Observation」的名词绑定提示后才接上（术语↔概念的连线还软）
# 判定：PASS（含一次提示）
```

**B4** 回炉 WP-02：天气工具里 requests.get 遇到 500——① 自动抛异常吗？② Observation 该传什么？
```python
# 用户答：① requests 不会自动抛异常
#         ② 应把错误信息当 Observation 传回模型，模型观察后决定下一步
# ——两问全对；②贴合 observations.mdx「错误信息/状态码也是系统反馈类 Observation」
# 补充：这时 resp.json() 也可能炸（错误页常非合法 JSON），T3-04 要处理
# 判定：PASS（WP-02 独立复测通过，检查时升 ✅ 设回炉日）
```

### C. 代码/操作

**C1** 读码预测：最小分发器片段，① 两行 print 输出？② `func = TOOLS[action["action"]]` 是链条哪一步？
```python
# 用户答：① get_weather / 上海：晴，28°C  ② 取工具 →（翻笔记对照版后）第三步，解析 Action
# ——①全对；②方向对，落步骤经翻自己笔记完成（笔记检索属正常用法）
# 过程注：提问「最小分发器是什么」「7步链是哪7步」——分发器一词现场补定义；
#        7步链指回自己笔记第二节对照版
# 判定：PASS
```

**C2** 找致命 bug：小王只调一次模型、split 取最后一行当答案，模型输出含 Action/Observation/最终回答三行
```python
# 用户答（第1轮）：① 「东京：暴雨,19°C」从工具中来的，工具返回客户端再返回模型 ← 被骗
#                ② 在 Action 行后掐断，模型停，客户端解析 JSON 再调用工具 ✅
# 用户答（第2轮，点出代码里无任何工具执行后）：① 是模型产出来的，实际没有调用工具，
#                模型生成的假结果 ✅ = 「模型自编 Observation」事故实景
# 关键坑（重点记）：B3 能背出「自编 Observation」事故，换成代码实景第一反应仍信了假数据
#   ——假 Observation 格式全对、内容自信，文本上肉眼不可分辨；唯一判别法=看代码有没有真执行
# 判定：① RETRY → PASS；② PASS
```

**C3** 写代码：补全最小分发器三个 TODO（解析 / 执行 / 拼回 Observation）
```python
# 用户答：① action_dict = json.loads(model_output)
#            tool_name = action_dict["action"]; args = action_dict["action_input"]
#         ② result = TOOLS[tool_name](**args)
#         ③ messages.append({"role": "tool", "content": result})
# ——三处全对可跑；②用了 **args 关键字解包（超出轨迹预期，微验证通过：
#   能说出「关键字参数」+ 不加星时整个 dict 变一个位置参数、语义全错）
# 补充：**args 是分发器通用式（任何工具签名都接得住），比手动 args["location"] 更好
# 判定：PASS
```

**C4** 回炉 🔴 WP-01：给 search_orders(user_id: str, days: int) -> str 写工具说明书，四要素缺一不可
```python
# 用户答：① search_orders ② 查询某用户最近 N 天的订单列表，返回 JSON 字符串
#         ③ user_id: str 用户id / days: int 查询最近多少天 ④ str，返回 JSON 字符串
# ——四要素逐项核对全在，点名子项没漏（WP-01 本套命中通过，检查时可降 🟡）
# 补充：输出含义宜再半句「JSON 里装的是订单列表」——模型靠说明书判断「调完拿到什么」
# 判定：PASS
```

### D. 应用与迁移

**D1** 真实场景：订单客服机器人（查实时物流）——①配什么工具 ②模型/程序各干哪几步 ③Observation 是什么实物
```python
# 用户答：① 订单查询工具 OrderQuery(orderid)
#         ② 模型取出订单号+工具名组装 JSON → 客户端解析并调用工具 → 工具返回客户端
#            → 客户端返回模型 → 模型答复用户
#         ③ Observation = 工具返回的内容，本场景=订单 20260701A 的位置信息
# ——三问全对；亮点：「模型取出订单号」正是模型层的独门价值（自然语言→参数的语义抽取）
# 判定：PASS
```

**D2** 迁移：模型吐坏 Action（JSON 无双引号）——①分发器哪行先炸、什么错？②怎么兜底不崩？
```python
# 用户答：① json.loads 那行炸，抛类型错误？ ② 先判断是不是标准 JSON，可以 try except
# 订正：①行对；异常是 json.JSONDecodeError（ValueError 子类），不是 TypeError
#   ——TypeError 是 L1-04 answer += None 那种「类型不匹配操作」；内容坏=值的问题
# 补充：②「判断是否合法 JSON」的唯一方法就是 parse，try/except 即判断本身；
#   进阶（B4 迁移）：except 里把「Action 不是合法 JSON 请重输」当 Observation 发回模型重试
# 判定：PASS（①异常名经订正）
```

---

### 今日练习小结（13 题全 PASS）

- 判定分布：直接 PASS 10 ／ PARTIAL→PASS 2（A1、B1）／ RETRY→PASS 1（C2①）。
- 回炉结果：WP-05、WP-06、WP-02 独立复测通过（检查时升 ✅ 设回炉日 +2 周）；WP-01 命中通过（可降 🟡）；WP-12 辅助命中（状态不动）。
- 新坑候选（检查时登记错题本）：
  1. **「模型自编 Observation」概念能背、代码实景第一眼识别不出**（C2 信了假数据；判别法=看代码有没有真执行）——重点。
  2. 术语↔概念绑定偏软：「被占槽位」→Observation 提示后才接上（B3）；异常名答成 TypeError，实为 json.JSONDecodeError（D2）。
  3. B1 三轮到根：「记忆/忘记」措辞校准当晚即回退，最终以「概率采样+prompt=软请求（WP-12）」收口。
- 超预期：C3 用 `**args` 关键字解包且微验证通过（分发器通用式）。
- **今天最虚 / 下次重点复习**：概念背得出、换代码皮认不出——下套埋「自编 Observation 识别」「prompt=软请求」的代码实景变体；顺带抽查 json.JSONDecodeError。
- 复核判定：（留空，待交叉复核）

---

Codex 判定：**T3-01 PASS**（本次「检查今天的学习」，2026-07-03 复核）

- **判定依据（实跑）**：把用户 C3 分发器答案组装成脚本实跑——`json.loads` → `TOOLS[tool_name](**args)` → `append {"role":"tool"}` 三步 asserts 全过（`result == "巴黎：多云，22°C"`、末条消息 role 正确）；D2 订正实证：坏 Action `json.loads` 抛 `JSONDecodeError` 且 `isinstance(e, ValueError) == True`。通过标准「能准确区分模型决策和程序执行」达成（A1/A2/C2/D1 ＋ 笔记第三节）；tracker 三问答全覆盖：① 模型真正执行工具了吗→否，只产调用请求（A1）；② 工具 schema 的作用→模型认识工具的唯一窗口、四要素（B1/C4）；③ Observation 如何影响下一步→够则收尾、不够再调、报错也是情报（A2/B4）。
- **Codex 当天进度总结**：T3-01 概念闭环——带读 4 节（7 题边读边练）＋共写笔记五节（「概念笔记共写」新约定首跑，原话落盘＋⚠️/💡标注、两处自纠痕迹保留）＋练习 13 题全 PASS。回炉：WP-05/WP-06/WP-02 独立复测通过升 ✅、WP-01 命中通过降 🟡；新增 WP-14（🔴）/WP-15（🟡）。
- **Codex 总进度总结**：阶段 0/1 全 PASS；阶段 2 只差 PR2-Gate（周末整块做，周五设计准备）；阶段 3 T3-01 概念预习先行完成（PR2-Gate 非强制序关卡，概念预习不受限；T3 动手交付在 PR2-Gate 之后）。
- **Codex 对问题/不确定点的解释**：「中间人」在 Claude Code / Codex 场景 = 装在本机的 CLI 程序本身（厂商替写的调度循环），不是「平台/服务器」；Tool Calling = Function Calling，同一机制两个名字（`functions` → `tools`/`tool_calls` 的沿革）。
- **Codex 问答点评与补充**：13 题逐题点评见上作答框（A1/B1 PARTIAL→PASS、C2① RETRY→PASS 被假 Observation 骗——重点坑、C3 `**args` 超预期且微验证通过、D2 异常名订正）。
- **Codex 超前内容提示**：`tools=[...]` / `tool_calls` 专用字段实操属 T3-02（第一次真调 DeepSeek 工具参数）；ReAct 的 Thought 环节属 A4-03；约束解码 06-30 已标超前；`**kwargs` 解包属 P0-05 拓展、已会用。
- **补救任务**：无。WP-14/WP-15 已入错题本，下套练习埋代码实景变体（已记小结）。
