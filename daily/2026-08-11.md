# 2026-08-11 学习记录

- 当前任务：`A4-Gate` 研究总结 Agent
- 任务状态：`DOING`（沿用 `tracker/progress.md`；今天正式启动实现，但未产生新的任务级判定）
- 今日计划学习预算：180 分钟
- 预算拆分：C3d 模型 / JSON 集成 155 分钟 + 强制间隔回收 25 分钟
- 实际学习时长：不记录；助手读取资料、整理记录、运行验证和等待 API 不计入学习预算

## 当日 Session / 任务索引

| Session | 任务 | 范围 | 预算 | 状态 |
| --- | --- | --- | ---: | --- |
| S1 | `A4-Gate` | C3d Reflection → Refinement 模型 / JSON 集成 | 155 分钟 | I1～I4 教学检查点与 topic/path 真实正常链已通过；今日实现范围完成 |
| S2 | `RECOVERY_BLOCK_PENDING` | `WP-04` / `WP-24` 到期回收，正式出题前再做语义去重 | 25 分钟 | 两项练习均已结束；待正式检查重算台账并处理 pending |

## Session S1｜A4-Gate-C3d 模型 / JSON 集成

### 前序证据链与本次接力点

- 完整 A4-Gate 前序 Session：
  - `daily/2026-08-05.md`：官方资料定向带读，`G1～G7`、`I1～I2` 收口。
  - `daily/2026-08-06.md`：Problem Contract 第 1～11 节内容冻结，`I3～I7` 收口；整体合同仍因 Gate 未验收而保持 `DRAFT`。
  - `daily/2026-08-07.md`：C1 固定三字段结果与输入边界正式 `PASS`。
  - `daily/2026-08-08.md`：C2 `read_material` / `search_web` 工具适配正式 `PASS`。
  - `daily/2026-08-09.md`：C3a 工具 Schema、C3b 白名单调度正式 `PASS`；C3c 跨日。
  - `daily/2026-08-10.md`：C3c 正式 `PASS`；C3d 的 allowlist、两个 prompt builder 与最终 validator 四个纯函数切片正式 `PASS`。
- 本次精确起点：把已通过的 `build_reflection_prompt()` 接到一次真实兼容的 Chat Completion 调用，先关闭 SDK 响应外壳和非空反馈边界；通过后再创建 Refinement JSON 调用的下一检查点。
- 已覆盖内容不会换措辞重考：本轮 Observation 的主语、allowlist 与正文证据的区别、Reflection 软评审、最终来源子集硬门，以及 JSON 解析失败时 validator 调用次数为 0。

### 今日范围、排除项与完成标准

今日 S1 只覆盖：

1. Reflection 单次模型调用与响应外壳。
2. Refinement 单次 JSON Output 调用、响应外壳与 `json.loads`。
3. 两段接力到 `validate_success_result()` 的最小编排。
4. topic / relative_path 的忠实 fake、故障注入与真实 DeepSeek 正常链。

今日明确不覆盖：

- 七字段结构化日志。
- 统一 `max_steps=6`、工具失败重试和安全停止。
- HITL 危险动作确认门。
- `eval_cases.json`、14 条固定评估和 A4-Gate 正式结论。
- G8-00 或新的算法内容块。

S1 的可观察完成标准：两条输入路径都实际经过 Reflection → Refinement → 响应检查 → JSON 解析 → validator；坏外壳、空正文、非正常结束、坏 JSON 和来源越界均不能产生 `success`；不创建一次性 runner、baseline 或报告目录。

### 用户 / 助手职责

- 用户必须亲自完成：模型调用编排、响应外壳保护、JSON 解析顺序，以及遇到失败时对实际停止层的解释；对新增且影响行为的边界做最小判断。
- 助手自动完成：建立当前唯一 TODO 骨架、准备不泄露答案的忠实 fake、运行检查、记录原始实现与订正过程，并在通过一个检查点后再打开下一个。
- 用户最终审核：确认代码行为符合 Problem Contract，且今天没有提前混入日志、重试、HITL 或 eval。

### 实际核验资料

本地资料快照：仓库 `HEAD 78b027fa2b5aad80fabba0564c8166b06c2f2352`（本次编辑前基线）。

- `code/stage4/problem-contract.md`：§4 固定输出；§6 第 5～6 步 Reflection 与客户端硬校验；§8 非法候选 / 来源拒绝。
- `code/stage4/a4_gate_research_summary_agent.py`：`generate_candidate_summary()`、两个 prompt builder、`validate_success_result()`。
- `code/stage4/a4_05_reflection_writer.py`：只对照已通过的 `run_reflection_once()` / `create_deepseek_llm_call()` 调用顺序。
- `code/stage2/pr2_04_extract_json.py`：只对照已通过的 `response_format`、响应正文入口与 `json.loads`；不沿用其宽泛异常边界。
- `resources/sandbox/sample.txt`：relative_path 真实链资料。

2026-08-11 核验的 DeepSeek 官方入口：

- [JSON Output — Notice](https://api-docs.deepseek.com/guides/json_mode/#notice)
- [JSON Output — Sample Code](https://api-docs.deepseek.com/guides/json_mode/#sample-code)
- [Chat Completions — Request](https://api-docs.deepseek.com/api/create-chat-completion/#request)
- [Chat Completions — Responses](https://api-docs.deepseek.com/api/create-chat-completion/#responses)

当前接口边界：业务正文位于 `response.choices[0].message.content`；`message.content` 可能为空；`finish_reason == "length"` 可能表示截断。只在期待 JSON 的 Refinement 调用使用 `response_format={"type": "json_object"}`；Reflection 按已冻结合同返回普通反馈文本，不擅自改成 JSON。

### `A4-Gate-C3d-I1`｜Reflection 调用与响应外壳

- 当前在做什么：把已通过的 Reflection prompt 交给 DeepSeek 兼容的 Chat Completions 接口，取回一段可信的、非空的评审反馈文本。
- 为什么需要：prompt builder 只会“写评审单”；模型调用才会“请评审员填写”。Refinement 必须在取得并检查这段反馈后才能开始。
- 与前后步骤的关系：输入来自 `build_reflection_prompt()`；输出会在下一检查点传给 `build_refinement_prompt()`。当前函数不处理 JSON，也不返回最终三字段结果。
- 当前唯一骨架：`request_reflection_feedback(reflection_prompt, client=None) -> str`。
- 当前完成标准：一次请求、不暴露工具、非流式、关闭 thinking；空 choices / 空 message / 非 `stop` 结束 / 意外 tool calls / 空 content 均以明确异常停止；正常时只返回 `content.strip()`。
- 用户原始提交：`改完了`。
- 静态观察：已完成 prompt 非空保护、注入 client 优先、一次非流式模型请求、关闭 thinking、未传 tools / `response_format`、tool_calls / content 检查及 `content.strip()`；这些主干继续保留。
- 首次最小执行：非法空白 prompt 正确抛 `ValueError`，模型调用保持 0 次；忠实 SDK 正常响应已实际调用模型 fake 1 次，但在 `isinstance(response, dict)` 处被错误拒绝，结果为 `ValueError: Reflection 响应不是字典类型`。
- 根因：OpenAI 兼容 SDK 的 `response`、`choice`、`message` 是属性对象，不是业务 JSON 字典；`finish_reason` 属于 `choice` 而不是 `message`，且当前无工具文本调用只接受精确值 `stop`。调用方 prompt 非法属于 `ValueError`，外部响应违反协议应统一为 `RuntimeError`。
- 提示 / 订正：提示 1 已使用——只沿 `response → choices[0] → message → content/tool_calls` 的真实属性层级修正；不提供完整代码，不改变已正确的请求参数。
- 教学检查点状态：`PARTIAL`；请求侧主干正确，正常响应尚不能通过。这不是任务级正式判定。

#### `A4-Gate-C3d-I1-R1`｜SDK 对象层级定向订正

- 用户原始提交：`改完了`。
- 静态观察：已删除 response / message 的 `dict` 类型断言；响应侧异常已改成 `RuntimeError`；正常结束目标值已改成 `stop`。请求侧已通过内容未被破坏。
- 最小执行：模型 fake 恰好调用 1 次，请求键精确为 `extra_body/messages/model/stream`；正常响应停在 `AttributeError: 'types.SimpleNamespace' object has no attribute 'finish_reason'`，尚未返回反馈。
- 根因：目标值已正确改为 `stop`，但代码仍从 `message.finish_reason` 取值；忠实结构中的字段实际位于 `choice.finish_reason`。`message` 只承载当前需要的 `tool_calls` 与 `content`。
- 提示 / 订正：提示 2 已使用——先保存 `choice = choices[0]`，从 `choice` 取 `finish_reason` 和 `message`；本轮只修这个层级，不重写请求部分。
- 教学检查点状态：仍为 `PARTIAL`；下一次先复跑正常响应，成功后才展开坏外壳矩阵。

#### `A4-Gate-C3d-I1-R2`｜正常响应复测

- 用户原始提交：`改完了`。
- 静态观察：`finish_reason` 已改为从 `choices[0]` 读取，`message.tool_calls/content` 的层级保持正确；等待正常响应和坏外壳矩阵执行。
- 忠实 fake 结果：正常 `tool_calls=None` 与 `tool_calls=[]` 两条均返回精确的 `无需改进`；模型各调用 1 次，捕获请求只含 `model/messages/stream/extra_body`，注入 client 时未创建真实客户端。
- 完整响应边界矩阵：`17/22 PASS`；通过了 4 类非法 prompt、response None、choices None / 空 / 错类型、message None、`finish_reason=length`、非空 tool_calls，以及 content None / 空 / 空白 / 错类型。
- 剩余失败：`choices_missing`、`message_missing`、`finish_reason_missing`、`tool_calls_missing`、`content_missing` 共 5 项都泄漏原生 `AttributeError`，没有按函数协议收敛为明确 `RuntimeError`。
- 语法：`PYTHON_COMPILE=PASS`。`git diff --check` 另发现模型请求后有一行尾随空白；它不影响本次逻辑判定，功能闭合后由助手做机械清理。
- 提示 / 订正：提示 3 已使用——用安全属性读取把“字段不存在”先映射成可判断的缺失值，再由本函数抛 `RuntimeError`；`tool_calls` 还要区分“字段缺失”和“字段存在且值为 None”。
- 教学检查点状态：仍为 `PARTIAL`；正常链已通过，只剩坏外壳缺字段的 fail-closed 边界。

#### `A4-Gate-C3d-I1-R3`｜缺字段边界复测与收口

- 用户原始提交：`改完了`。
- 用户订正：使用属性存在性守门，分别覆盖 `choices/message/finish_reason/tool_calls/content`；保留 `tool_calls=None` 与空列表为合法无工具响应。
- 忠实 fake 完整矩阵：`22/22 PASS`，`FAILED_IDS=[]`。正常 None / 空列表两条、4 类非法 prompt、五类属性缺失、None / 空 / 错类型、`length`、意外工具调用与空正文全部得到预期结果。
- client factory 追加探针：未注入 client 时 factory 1 次、模型 fake 1 次、结果精确为 `反馈完成`；注入 client 的主矩阵没有创建真实客户端。
- 语法：`PYTHON_COMPILE=PASS`。已由助手机械删除完成态 TODO 和尾随空白，不改变用户实现行为。
- 有价值的订正演进：把 SDK 属性对象误当业务字典 → 改为真实 `response → choice → message` 层级 → 用属性存在性守门把外部缺字段统一收敛为 `RuntimeError`。
- 教学检查点状态：`PASS`。这只证明 C3d-I1 实现切片，不是 A4-Gate 的正式任务 `PASS`。

### `A4-Gate-C3d-I2`｜Refinement JSON Output 与语法解析

- 当前在做什么：把已通过的 Refinement prompt 交给第二次模型调用，请模型只返回 JSON 文本；客户端先检查 SDK 外壳，再把原始字符串解析成 Python 对象。
- 为什么需要：Reflection 反馈仍是普通文本，不能直接冒充最终结果；Refinement 才负责依据反馈生成固定三字段候选。`response_format` 只能约束 JSON 输出形式，不能替代项目 validator。
- 与前后步骤的关系：输入来自 `build_refinement_prompt()`；本函数输出仍是不可信的解析对象，下一检查点才把它交给 `validate_success_result()`。坏 JSON 必须停在解析层，validator 调用次数为 0。
- 当前唯一骨架：`request_refinement_candidate(refinement_prompt, client=None) -> object`。
- 用户必须亲自完成：JSON Output 请求参数、一个明确的 `max_tokens` 上限及理由、沿用 I1 的响应外壳、`json.loads` 与精确解析异常边界。
- 当前完成标准：一次请求、不暴露工具、非流式、关闭 thinking，传 `response_format={"type": "json_object"}` 与显式正整数 `max_tokens`；只接受 `finish_reason == "stop"` 和非空正文；坏 JSON 转为有明确原因的 `RuntimeError`；合法 JSON 返回解析后的对象，但不调用 validator。
- 用户原始提交：`改完了，max_tokens=1600，取的折中，因为你估计的范围是1200-2000`。
- 静态观察：已完成 prompt / client 边界、单次 JSON Output 请求、`max_tokens=1600`、I1 同型外壳守门、精确 `json.JSONDecodeError` 捕获与 `json.loads` 返回；未调用 validator。等待忠实 fake 执行。
- 参数理由初评：`1600` 本身处于当前三字段 JSON 的合理候选范围；但“因为在助手建议范围中取折中”只说明数值来源，没有独立说明它如何匹配当前正文 / 摘要规模、避免 `length` 截断，以及为什么不把上限无限放大，因果证据待补。
- 忠实 fake JSON 矩阵：`30/30 PASS`，`FAILED_IDS=[]`，`VALIDATOR_CALLS=0`。覆盖合法三字段对象、合法 list/string/number/bool/null、4 类非法 prompt、完整坏外壳、`length`、意外工具调用、空正文、开括号截断、尾随文字、Markdown 围栏坏 JSON，以及 client factory 分支。
- 请求捕获：恰好一次模型调用；精确包含 `model/messages/stream/extra_body/response_format/max_tokens`，`response_format={"type":"json_object"}`、`max_tokens=1600`，不含 tools。语法检查 `PYTHON_COMPILE=PASS`，`git diff --check` 通过。
- 代码执行部分：`PASS`。解析层正确允许合法 JSON 标量 / 容器继续作为不可信对象返回，也正确保证坏 JSON 停在解析层、validator 调用次数为 0。
- 教学检查点状态：`PARTIAL`；只缺 `max_tokens=1600` 的独立工程因果说明。这不是任务级正式判定。

#### `A4-Gate-C3d-I2-G1`｜输出上限取舍迁移

- 类型：当前真实实现的单一口头迁移检查；不要求改代码。
- 问题：若把同一调用的 `max_tokens` 分别改成 `400` 和 `10000`，结合本项目最多约 1000 字符资料正文、最近数百字符候选摘要、三字段 JSON / sources 开销，以及 `finish_reason="length"` 的含义，分别说明两者的主要风险；最后用自己的因果链说明为什么保留 `1600`。
- 用户原始回答：

  > 1.400的风险主要是可能太短了，可能解析的时候截取不全，导致不是一个完整的JSON。解析会报错
  >
  > 2.10000的主要问题是太长了，会造成token的浪费
  >
  > 3.因为path最多读取1000字符，其他加起来有几百字符，先取1600看是多还是少，方便后续调整
  >
  > 4.finish_reason="length" 表示输出可能因为上限而被截断。
  > 判断中是不是需要加入finish_reason="length"的判断？

- 做对：能判断 400 可能让输出在 JSON 完成前被截断，继而导致解析报错；能把 `finish_reason="length"` 与上限截断绑定，并主动询问运行时守门。
- 代码核对：当前 I2 已在 `choice.finish_reason` 上只接受精确 `stop`；因此 `length` 已被包含在“所有非 stop 均拒绝”的判断中，并会在 `json.loads` 前抛 `RuntimeError`，无需另加重复分支。I1 也采用相同边界。
- 待订正 1：不是“解析的时候截取”，而是模型生成阶段先被截断，解析器随后收到残缺字符串才报错。
- 待订正 2：`max_tokens=10000` 是允许上限，不等于每次一定生成或计费 10000 Token；主要问题是放大异常长输出的最坏成本 / 延迟 / 资源边界，而不仅是必然浪费。
- 待订正 3：`MAX_MATERIAL_CHARS=1000` 限制的是输入证据字符，`max_tokens` 限制的是模型输出 Token，不能把输入字符和输出 Token 直接相加得到 1600；应依据预期 summary、有限 sources 与 JSON 开销给输出留余量。
- 待订正 4：尚未回答何种运行证据会调大 / 调小上限。
- 教学检查点状态：`PARTIAL`；代码执行已通过，只需一次最小因果订正，不改代码。

##### `A4-Gate-C3d-I2-G1-R1`｜参数证据定向补答

- 用户原始回答：

  > 1.10000：虽然不一定实际生成满 10000，但把上限放这么大的主要风险是可能会放大异常情况下的风险，一旦模型偏离要求，反复输出，系统会允许它继续生成更久。最坏情况下的成本、延迟和资源占用都会更高。
  >
  > 2.不能用输入的 1000 字符直接相加；应该根据预期输出中的summary，sourcrs等来留余量，所以当前先保留 1600。
  >
  > 3.调整条件：如果真实运行持续出现finish_reason="length"，我会调大；如果长期观察到finish_reason="stop"，我会调小

- 已通过：能说明 10000 是过宽的最坏边界而不是必然消耗量；能纠正输入字符不能与输出 Token 相加，并改为依据预期 `summary/sources` 输出留余量。
- 仍需订正：① 持续出现 `length` 前要先区分“合理目标输出确实接近上限”与“模型偏离合同、异常啰嗦”，后者应先修 prompt / 行为而不是放大上限；② `stop` 只证明正常结束，不证明输出远低于 1600，例如 1590 Token 也可能是 `stop`，因此不能单凭 `stop` 调小。
- 教学检查点状态：`PARTIAL`；只剩“用实际输出占用和质量证据调参”的最小迁移，不改代码。

##### `A4-Gate-C3d-I2-G1-R2`｜三组运行证据判断

- 问题：A 组多次 `length` 但内容是重复跑题；B 组全部 `stop` 但输出约 1500 Token；C 组代表性 topic/path 全部合同通过、均为 `stop` 且输出长期约 250～400 Token。分别判断应先调大、调小还是保持 1600 / 修其他问题，并说明依据。
- 用户原始回答：

  > A 组:先修 prompt/行为，不调上限，因为模型一直重复相同句子并逐渐跑题，可能是因为重复问题导致撞到上限。
  >
  > b:保持1600，因为输出稳定，代表性字段合法
  >
  > c:调小，因为代表性字段合法，摘要质量和来源合同保持通过。但是实际输出长期只有约 250～400 Token，跟1600差距太大

- 判定：`PASS`。A 能区分异常啰嗦与合理目标输出，不用放大上限掩盖 prompt / 行为问题；B 没有把 `stop` 误当成低占用证据，约 1500 Token 时不下调；C 同时具备代表性路径、合同质量与长期低占用证据，才支持保留余量地下调。
- I2 总结：代码矩阵 `30/30 PASS`、validator 0 次；参数取舍从“助手给的范围中取中间值”进展为“依据预期输出与真实运行占用，在质量、截断、成本 / 延迟边界之间调参”。
- `A4-Gate-C3d-I2` 教学检查点：`PASS`。这不是 A4-Gate 的正式任务 `PASS`。

### `A4-Gate-C3d-I3`｜Reflection → Refinement → validator 两段编排

- 当前在做什么：把已经分别通过的 allowlist、两个 prompt builder、两次模型请求和最终硬门按合同顺序接成一个函数。
- 为什么需要：单个零件都通过不代表调用顺序正确；只有同一轮 Observation 派生的来源白名单贯穿 Reflection、Refinement 和 validator，最终 `success` 才能与真实证据绑定。
- 与前后步骤的关系：输入是 C3c 已生成的 `candidate_summary/tool_name/tool_result` 与规范化请求；输出是经过 `validate_success_result()` 的可信三字段结果。下一检查点才把 C3c 与本函数接成 topic/path 完整链。
- 当前唯一骨架：`reflect_refine_and_validate(normalized_request, candidate_summary, tool_name, tool_result, client=None)`。
- 当前完成标准：纯函数边界先于模型；未注入时 client factory 恰好 1 次，两次模型请求共用同一 client；调用顺序为 allowlist → Reflection prompt → feedback → Refinement prompt → parsed candidate → validator；validator 使用同一 allowlist。Reflection 失败时 Refinement / validator 0 次，Refinement / JSON 失败时 validator 0 次，非法最终对象不能返回 `success`。
- 当前不做：不重新执行工具，不调用 `generate_candidate_summary()`，不捕获并改写已有异常，不加入日志 / retry / HITL。
- 用户原始提交：`改完了`。
- 静态观察：已按 allowlist → prompt → feedback → prompt → parsed candidate → validator 的总体形状接线；同一 `allowed_sources` 变量进入首个 prompt 与最终 validator，没有加入工具、候选生成、日志、重试或异常改写。
- 最小合法 path 执行：`TypeError: derive_allowed_sources() missing 1 required positional argument: 'tool_result'`；模型调用 0 次，说明错误正确暴露在第一个纯函数边界；语法检查 `PYTHON_COMPILE=PASS`。
- 根因 1：`derive_allowed_sources()` 的真实签名是 `(normalized_request, tool_name, tool_result)`，当前调用漏传 `tool_name`，导致位置参数整体错位。
- 根因 2：取得 feedback 后再次调用了 `build_reflection_prompt()`；该函数只有五个参数且不接收 feedback，第二段应调用已通过的 Refinement prompt builder。
- 根因 3：当前把可能为 `None` 的 client 分别传给两个请求助手；未注入时两个助手会各建一个客户端，尚未满足“编排层创建一次并共享”的合同。
- 提示 / 订正：提示 1 已使用——逐一对照三个被调函数的真实定义与参数顺序；保留总体流程，只修函数名、缺失参数和 shared client 生命周期。
- 教学检查点状态：`PARTIAL`；尚未进入模型 / validator 执行，不是任务级正式判定。

#### `A4-Gate-C3d-I3-R1`｜接线与共享 client 订正

- 用户原始提交：`改完了`。
- 静态观察：已补齐 `derive_allowed_sources(normalized_request, tool_name, tool_result)`；第二段已改用六参数 `build_refinement_prompt()`；未注入时编排层只创建一次 client，并把同一变量交给两个请求助手。
- spy 矩阵：`7/11 PASS`。已通过注入 client 正常链、Reflection / Refinement / validator 三段失败短路、真实组件忠实 path 两次模型 fake 和禁止越权调用；合法 path 得到精确三字段结果，两次请求分别保持文本 Reflection 与 JSON Refinement。
- 剩余失败：`normal_factory`、`fail_allowlist`、`fail_reflection_prompt`、`invalid_observation_factory_zero`。四项共同轨迹都显示 `factory` 发生在 allowlist / Reflection prompt 纯函数之前。
- 根因：共享 client 的“只创建一次”已实现，但创建位置过早；非法 Observation 或 prompt 输入仍会先创建无用客户端。应把现有 `if client is None` 小块移到 Reflection prompt 成功构造之后、第一次模型调用之前。
- 提示 / 订正：提示 2 已使用——只移动 client 决策块，不改已正确的三个参数、builder、后续共享变量或异常边界。
- 教学检查点状态：`PARTIAL`；只剩 client factory 的纯函数前置顺序。

#### `A4-Gate-C3d-I3-R2`｜纯函数优先与两段编排收口

- 用户原始提交：`改完了`。
- 用户订正：把 `if client is None` 的客户端创建块移到 allowlist 与 Reflection prompt 均成功之后、第一次模型调用之前；两次模型请求继续共享同一 client。
- spy / 忠实 fake 矩阵：`11/11 PASS`，`FAILED_IDS=[]`。覆盖注入 client 与 factory 两条正常链、allowlist / 两个 prompt / 两次模型调用 / validator 各层失败短路、非法 Observation 不创建 client，以及真实组件组成的 relative_path 两段响应链。
- 决定性顺序证据：未注入 client 时为 `allowlist → reflection_prompt → factory → reflection_call → refinement_prompt → refinement_call → validator`；纯函数失败时 factory 与后续副作用均为 0 次。
- 正常结果：Reflection 保持普通文本，Refinement 保持 JSON Output；最终返回精确三字段 `success`，来源为同一轮 allowlist 的子集。编排层没有重新执行工具，也没有加入日志、重试、HITL 或异常改写。
- 语法：`PYTHON_COMPILE=PASS`。已由助手机械删除完成态 TODO，并只整理空格与缩进，不改变用户实现行为。
- `A4-Gate-C3d-I3` 教学检查点：`PASS`。这只证明 C3d 两段编排切片，不是 A4-Gate 的正式任务 `PASS`。

### `A4-Gate-C3d-I4`｜原始请求到可信结果的正常路径接线

- 当前在做什么：把已经通过的 C1 请求准备、C3c 候选生成与 C3d 反思改写接成一次完整正常路径。
- 为什么需要：I3 只从规范化请求和已有 Observation 开始；真实入口还必须确保非法原始请求提前返回，合法 topic / relative_path 只执行一次对应工具，并让 C3c 与 C3d 共用一个客户端。
- 与前后步骤的关系：输入是原始 `request`；C1 负责规范化或返回 `invalid_input`，C3c 负责两次模型调用与恰好一次工具，C3d 再负责 Reflection、Refinement 和最终硬校验。通过本切片后才对 topic / relative_path 跑忠实 fake 与真实 DeepSeek 正常链。
- 当前唯一骨架：`run_research_summary_once(request, client=None) -> dict[str, object]`。
- 当前完成标准：严格按 `prepare_request → invalid 早返回 / shared client → generate_candidate_summary → 取内部三项证据 → reflect_refine_and_validate → return` 接线；非法输入 factory / 模型 / 工具均为 0 次；未注入时整条链只创建 1 个 client，注入时 0 次；同一 client 覆盖 C3c 与 C3d 共四次模型调用；不自行再调工具、不伪造最终结果、不吞掉下游异常。
- 当前不做：不增加 `try/except` 失败映射，不加入日志、重试、HITL、循环限步或 eval。
- 用户原始提交：`改完了`。
- 静态观察：已经把 client 决策放在 C3c 前，并把同一变量继续传给 C3d；没有加入日志、重试、HITL、直接工具调用或异常捕获。当前尚未执行到这些已写分支，因为入口返回值的形状处理错误。
- 最小执行：合法 topic（注入 client）、合法 relative_path（factory 分支）和非法 `None` 三条都抛出同一个 `ValueError: normalized_request 必须是非空字典`；三条的 factory / C3c / C3d 调用次数全部为 0。语法检查 `PYTHON_COMPILE=PASS`，差异格式检查通过。
- 根因：`prepare_request()` 的合同是返回二项元组：合法时为 `(normalized_request, None)`，非法时为 `(None, invalid_result)`。当前把整个二项元组保存到 `normalized_request`，再检查它是不是字典，因此合法与非法分支都无法分流。
- 提示 / 订正：提示 1 已使用——在调用处同时接住两个返回值；只根据 `invalid_result` 是否存在做早返回。不要在 I4 重新发明一套 `normalized_request` 字典校验，因为 C1 已经负责这个边界。
- 教学检查点状态：`PARTIAL`；当前只修 `prepare_request` 的二项返回与非法早返回，修后再继续核验 C3c 内部结果的接力。这里不是任务级正式判定。

#### `A4-Gate-C3d-I4-R1`｜C1 二项返回与非法早返回收口

- 用户原始提交：`改完了`。
- 用户订正：已用两个变量接住 `prepare_request()` 的返回；`normalized_request is None` 时在创建 client 之前原样返回 `invalid_result`，删除了重复的字典类型门。
- 最小执行：非法 `None` 返回精确三字段 `{"status": "invalid_input", "summary": "请求必须是字典类型", "sources": []}`，factory / C3c / C3d 均为 0 次；合法 topic / relative_path 已分别抵达 C3c，注入 client 时 factory 0 次，未注入时 factory 1 次。
- R1 判定：`PASS`。C1 二项返回与非法早返回已闭合；整体 I4 仍为 `PARTIAL`。
- 下一实际失败：C3c 返回的是三键字典，但当前用三个变量直接迭代解包该字典，因此得到的是键名字符串 `"candidate_summary" / "tool_name" / "tool_result"`，而不是三个字段值。spy 在 C3d 入口实际收到这些键名，并以 `AssertionError: candidate_summary 收到的是 'candidate_summary'` 停止。
- 提示 / 订正：提示 2 已使用——先把 `generate_candidate_summary()` 的返回保存为一个内部结果对象，再用 `candidate_summary`、`tool_name`、`tool_result` 三个精确键分别取值；不增加新的 shape validator 或异常映射。
- 当前教学状态：`PARTIAL`；下一次只复测 C3c 字典字段值到 C3d 的接力、shared client 同一性和异常自然传播。

#### `A4-Gate-C3d-I4-R2`｜内部字段接力与双路径收口

- 用户原始提交：`改完了`。
- 用户订正：先保存 C3c 返回的内部字典，再用 `candidate_summary`、`tool_name`、`tool_result` 三个精确键分别取值；C3d 现在收到字段值而不是键名。没有改用 `.values()` / `.get()`，也没有增加新的 validator 或异常映射。
- 主审无网络矩阵：`13/13 PASS`，`FAILED_IDS=[]`。覆盖 5 类非法请求早返回、注入 / factory 两种 shared client 分支、C3c 与 C3d 异常原样传播、内部结果缺键时 `KeyError` 且 C3d 0 次、topic / relative_path 忠实 SDK 形状四模型单工具链，以及禁止直接工具 / JSON / 日志 / 重试调用。
- 忠实 topic fake：调用轨迹为 `model_1 → search_web → model_2 → Reflection → Refinement`；模型 4 次、`search_web` 1 次、`read_material` 0 次，最终精确三字段来源属于本轮搜索 Observation。
- 忠实 relative_path fake：调用轨迹为 `model_1 → read_material → model_2 → Reflection → Refinement`；模型 4 次、`read_material` 1 次、`search_web` 0 次，最终来源精确为规范化后的 `sample.txt`。
- 独立只读 spy：`8/8 PASS`；再次确认非法早返回、两个输入路由、factory 恰好一次、client 对象同一性、缺键短路、异常传播和静态范围，没有编辑文件或调用网络。

##### 真实 DeepSeek topic 正常链

- 输入：`{"input_type": "topic", "value": "人工智能 Agent"}`。
- 结果：精确三字段 `success`；最终摘要 1230 字符，来源为本轮中文 Wikipedia Observation 的 3 个 URL，来源子集检查通过。
- 调用证据：真实模型 4 次；结束原因为 `tool_calls → stop → stop → stop`；真实 `search_web` 1 次、`read_material` 0 次。
- 协议证据：第 3 次 Reflection 没有 `response_format`，第 4 次 Refinement 使用 `response_format={"type": "json_object"}` 与 `max_tokens=1600`；全部检查为 `True`。

##### 真实 DeepSeek relative_path 正常链

- 输入：`{"input_type": "relative_path", "value": "sample.txt"}`。
- 结果：精确三字段 `success`；最终摘要 140 字符，来源精确为 `sample.txt`，与本轮 Observation 一致。
- 调用证据：真实模型 4 次；结束原因为 `tool_calls → stop → stop → stop`；真实 `read_material` 1 次、`search_web` 0 次。
- 协议证据：Reflection 普通文本、Refinement JSON Output、最终来源子集与所有调用次数检查均为 `True`。

- 参数边界：两条代表性真实链都以 `stop` 正常完成，没有 `length`；仅有两个样本，不据此调整 `max_tokens=1600`。
- 助手机械收口：删除 I4 完成态 TODO、整理一个等号空格，并更新模块 docstring；没有改变用户实现行为，也没有创建日志 / 重试 / HITL / eval 骨架。
- `A4-Gate-C3d-I4` 教学检查点：`PASS`。I1～I4 与今日 C3d 模型 / JSON 集成范围均已闭合；这不是 A4-Gate 父任务的正式 `PASS`。

## Session S2｜强制间隔回收

- 持久状态：`tracker/weak-points.md` 当前为 `RECOVERY_BLOCK_PENDING`。
- 计划范围：25 分钟；旧目标总上限为 2，本次最终锁定 `WP-04`（规则版 vs 模型版分类）与 `WP-24`（正则量词 / 转义与精确 Python 字符串）。
- 执行边界：S1 到 155 分钟即停止并顺延未完成实现，保留本块 25 分钟；开始时由 `daily-practice-builder` 做正式 preflight、一次一题并记录真实作答。
- 当前状态：两项回收练习均已结束。第 1 题 `WP-04` 的代码追踪结果为 `PASS`，但因近端语义提示不作为干净的间隔升档证据；第 2 题 `WP-24` 经精确符号确认后为 `PARTIAL → PASS`。`daily-practice-builder` 不更新统一台账，后续正式检查前不能自行清除 pending。

### S2 出题 Preflight 与语义去重

- 当前阶段 / 任务：`A4-Gate = DOING`；今日 C3d-I1～I4 与 topic / relative_path 真实正常链已经通过，不再换措辞重复考模型外壳、JSON、shared client、来源子集或 `max_tokens`。
- 当前回收依据：`weak-points.md` 只剩两个到期间隔项 `WP-04`（到期 2026-07-19）与 `WP-24`（到期 2026-08-05）；最老逾期仍 ≥7 天，因此今天已预留的 25 分钟回收块必须实际执行，且恰好使用这两个旧目标，不叠加第三项。
- `WP-04` 初始去重结论：2026-08-08 曾在“工具是不是模型”追问和 2026-08-10 的纯函数切片中短时触及“零模型调用”，但自 2026-07-05 后没有完成延迟间隔复测；初始 preflight 因此改用真实 `classify / classify_llm` 与 `call_model` spy 做精确调用计数和控制流解释。题后独立复核指出：今日 C3d 刚再次强化“纯函数失败时模型调用为 0”和四次模型调用计数，这仍构成同日语义提示；故本题可作为代码追踪证据，但不能作为干净的间隔升档证据，`WP-04` 的台账去向须建议保持到期。
- `WP-24` 去重结论：2026-08-08 的 HTML 标签清理只使用了相邻正则，不等价于独立构造“多行字段 + 行首边界 + 非贪婪量词 + 精确 Python 元组”；今日 C3d 没有正则切口，因此第二题使用全新的、标签本身含字面星号的 `Draft* / Final*` 表面复测，要求区分标签中的转义星号与捕获组中的重复量词，不复写旧 `Thought / Action` 或 `Reasoning / Decision` 答案。
- 覆盖顺序：先最老的 `WP-04` 代码追踪；若无需补救，再进入 `WP-24` 独立构造。两题都必须包含“为什么”，且严格一次一题。
- 工作场景 / 岗位边界：本块只做稳定错题间隔复测，不冒充 A4-Gate 的日志 / 恢复 / HITL 工作场景升级，也不产生 `JOB_EVIDENCE`。
- 算法边界：W4 内容块已 `PASS`、债务 0；今天没有算法预算，不启动下一内容块。
- 本地依据：`code/stage2/pr2_03_classifier.py`、`code/stage4/a4_03_react_agent.py`、`daily/2026-07-05.md`、`daily/2026-07-22.md`、对应 notes 与知识库概念页；仓库基线 `HEAD 78b027fa2b5aad80fabba0564c8166b06c2f2352`，当前另有今日未提交学习改动。
- 可答性检查：两道题均已用无落盘内存探针确认控制流和输出唯一；探针结果不在题面或作答前记录，避免泄题。

### 学习定位

- 为什么学这个：区分确定性代码决策和真实模型调用，才能正确计算 Agent 的模型步数、成本和失败边界；这是到期的 `WP-04` 间隔复测。
- 学完能做什么：能沿真实函数体判断哪一次调用会进入 LLM，给出精确标签与模型调用次数，而不是凭函数名猜测。
- 对接下来的影响：A4-Gate 后续要实现七字段日志、`max_steps` 和重试；若把纯 Python 分支误算成模型调用，步数与成本日志都会失真。

### `A4-Gate-R8`｜`WP-04`：规则分类与模型分类的调用计数迁移

- 本次复习目标 ID / 类型：`WP-04` / 到期稳定错题间隔复测。
- 与当前任务题是否语义合并：否；使用旧 PR2-03 真实代码的 changed-surface spy，计入今天两个旧目标之一。
- 题目：闭卷先不运行。下面保留了真实 `classify()` / `classify_llm()` 的决定性逻辑，并把 `call_model` 换成计数 spy。请写出最后一行打印的四个精确值，并说明哪一个函数调用让 `model_calls` 增加、另外两个为什么不会增加；解释必须沿函数体控制流，不只写“规则版 / 模型版”标签。

  ```python
  LABELS = ["问题", "投诉", "建议", "闲聊", "其他"]
  RULES = {
      "投诉": ["退款", "差评", "投诉", "太差", "垃圾"],
      "建议": ["建议", "希望", "最好能"],
      "问题": ["怎么", "如何", "为什么", "？", "?"],
      "闲聊": ["你好", "谢谢", "天气"],
  }

  def classify(text):
      for category, keywords in RULES.items():
          if any(keyword in text for keyword in keywords):
              return category
      return "其他"

  model_calls = 0

  def fake_call_model(prompt):
      global model_calls
      model_calls += 1
      return "建议\n"

  def classify_llm(text):
      prompt = "把文本分到固定标签：" + text
      label = fake_call_model(prompt).strip()
      if label not in LABELS:
          label = "其他"
      return label

  r1 = classify("我要退款")
  r2 = classify_llm("我有一些意见想反馈")
  r3 = classify("你好")
  print(r1, r2, r3, model_calls)
  ```

- 用户原始作答：

  > 1.投诉，建议，闲聊，1
  >
  > 2.r2 = classify_llm("我有一些意见想反馈")调用了fake_call_model，会让model_calls增加
  >
  > 3.r1和r3中的classify没有调用fake_call_model，只有r2调用了

- 执行核对：真实代码等价的无落盘 spy 输出为 `投诉 建议 闲聊 1`，与用户四个值一致；模型调用计数精确为 1。
- 做对：`r1` 沿 `classify()` 的 `RULES` 循环命中“退款”并直接返回“投诉”；`r3` 同样命中“你好”并返回“闲聊”，两条路径都没有 `fake_call_model` 调用表达式。只有 `r2` 进入 `classify_llm()` 并实际执行一次 `fake_call_model(prompt)`，返回文本经 `.strip()` 得到“建议”。用户已准确指出决定性调用点和另外两条零调用原因。
- 订正过程：无；首答独立正确。
- 单项结果：`PASS`（代码追踪与调用计数）。
- 间隔证据边界：题后独立语义复核发现今天 C3d 已近距离提示“纯 Python 边界 vs 模型调用计数”；因此本次 `PASS` 不建议直接把 `WP-04` 从 `+2周` 升到 `+1月`。练习层建议的台账去向为“保持到期，未来在无近端提示的 hybrid 规则优先 / LLM fallback 场景复测”；最终由 `ai-agent-learning-review` 重算。
- 旧目标预算：本题仍占用今天两个旧目标席位之一，不再补第三项。

### `A4-Gate-R9`｜`WP-24`：字面星号、非贪婪量词与精确 Python 输出

- 本次复习目标 ID / 类型：`WP-24` / 到期稳定错题间隔复测。
- 与当前任务题是否语义合并：否；使用新的 `Draft* / Final*` 多行字段表面，计入今天两个旧目标中的第二项。
- 独立性边界：闭卷、不运行；题面不提供任何正则片段。字段标签中的星号是原始文本的一部分，不是 Markdown 装饰。
- 题目：给定下面的 `text` 和固定提取骨架：

```python
import re

text = """Draft*: 先核对版本。
第二行引用 Final*: skip，这里只是正文示例。
保留项目符号 * alpha。
仍需查询官方资料。
Final*: search_web[JSON mode]"""

draft_match = re.search(draft_pattern, text, flags)
final_match = re.search(final_pattern, text, flags)

draft = draft_match.group(1).strip() if draft_match else None
final = final_match.group(1).strip() if final_match else None
print((draft, final))
```

请在一次作答中完成三个部分：

1. 写出完整的 `draft_pattern`、`final_pattern` 和两次 `re.search` 共用的 `flags`。要求只把真正位于行首的 `Draft*:` / `Final*:` 识别为字段标签，`Draft*:` 的正文可跨多行，并在真正的 `Final*:` 字段前停止。
2. 写出最后一行打印的精确 Python 元组；两个文本值必须是合法字符串，并用 `\n` 表示字符串内部换行。
3. 逐一解释你写出的反斜杠和星号分别在做什么，并说明为什么第二行正文里的 `Final*:` 不会提前结束 Draft、项目符号 `* alpha` 会被原样保留，而真正行首的 `Final*:` 会成为字段边界。

- 用户原始作答：

  ```text
  1.draft_pattern = r'^Draft\*: (.*?)(?=^Final\*: |\Z)'
  final_pattern = r'^Final\*: (.*)'
  flags = re.MULTILINE | re.DOTALL
  2.('先核对版本。\n第二行引用 Final*: skip，这里只是正文示例。\n保留项目符号 * alpha。\n仍需查询官方资料。', 'search_web[JSON mode]')
  3.正则中的 * 是量词（匹配前一个字符 0 次或多次）。为了匹配字面上的星号字符，必须在它前面加反斜杠进行转义：\*。
  在 draft_pattern 和 final_pattern 里，Draft\*: 与 Final\*: 中的 \* 都是用来匹配文本里真正的星号，而不是作为量词使用。
  捕获正文的 .*? 中的 * 没有转义，它作为量词和 ? 一起构成非贪婪匹配，用来跨行抓取正文。
  正文的结束边界由一个正向前瞻断言 (?=^Final\*: |\Z) 决定。断言中的 ^ 在 re.MULTILINE 模式下匹配任意行的行首。
  第二行内容是 第二行引用 Final*: skip，这里只是正文示例。，其中的 Final*: 前面还有别的字符，不在行首，因此不满足 ^Final\*: 的条件，不会被识别为边界，Draft 继续捕获后续内容。
  项目符号 * alpha 位于 Draft 正文内部，而正文由 (.*?) 捕获，该子表达式会原封不动地保留所有字符（包括星号）。正则只对标签行里的星号做了转义匹配，并没有对正文内容施加任何过滤或特殊处理，因此 * alpha 会被完整保留。
  当匹配进行到最后一行的行首时，^Final\*: 成功匹配，断言成立，(.*?) 的非贪婪捕获立即停止，从而把行首的 Final*: 识别为字段标签，实现了 Draft 与 Final 两个字段的准确分割。
  ```

- 执行核对：严格按用户提交的两条 pattern、flags、题面固定 `text` 与 `.strip()` 骨架运行，两个 match 均成功；原始 Draft 捕获在真实 Final 字段前带一个末尾换行，`.strip()` 后的 `(draft, final)` 与用户提交元组逐字符一致。只移除 `re.DOTALL`、其他不变时，当前 Draft pattern 无法完成多行匹配。
- 做对：标签中的 `\*`、捕获组中的重复量词 `*`、非贪婪 `?`、`re.MULTILINE` 下的行首边界、正文内假标签与项目符号保留、真正 Final 字段停止、合法字符串引号及 `\n` 均正确；最核心的历史错误已经独立修正。
- 当前缺口：题目要求逐一解释自己写出的反斜杠，但首答没有说明前瞻中的 `\Z`；同时把跨行职责笼统归给了 `.*?`，尚未把点号、非贪婪重复和 `re.DOTALL` 的职责分开。
- 订正过程：首答 `PARTIAL`；不重写已通过的 pattern、flags、元组或行首/字面星号说明，只补 `\Z` 的边界语义，以及 `.`、`*?`、`re.DOTALL` 的职责关系。
- 最小补答 1 原始作答：

  > 1.它匹配整个输入字符串的结尾位置。作为唯一的边界让 (.*?) 一直捕获到字符串结束，从而 Draft 字段能成功取出全部剩余内容。
  >
  > 2.`.`默认匹配**除换行符** **\\n** **以外的任意单个字符，`\*?`**它会让前面的 .（或其他模式）尽可能少地匹配字符，**一旦后续的断言（前瞻）能够成功，就立刻停止**。`re.DOTALL`它的作用是**修改** **.** **的行为**，使 . 能够匹配包括换行符 \n 在内的所有字符。
  > 开启 re.DOTALL 后，. 才具备“跨行”能力，可以穿过换行符去捕获多行文本。

- 最小补答 1 核对：`\Z` 的全文末尾位置与无真实 Final 字段时的备选停止语义已说清；`.` 默认不含换行、`re.DOTALL` 扩展点号字符范围，以及前瞻可用时非贪婪重复立即停止的职责也已闭合。唯一未能直接按正确答案解释的是：正文把实际 pattern 中的量词 `*?` 写成了带反斜杠的 `\*?`；两者正则含义不同，而这正是 `WP-24` 的核心转义边界，不能静默按错别字处理。
- 最小补答 1 状态：`PARTIAL`；只需确认捕获组中应使用的精确量词写法，并说明星号前是否有反斜杠，不再重答其他内容。
- 最小补答 2 原始作答：

  > 应写成A，不应该有反斜杠，因为\* 是转义后的**字面星号**，匹配文本里真正的 * 字符。它**无法**匹配多字符的正文

- 最小补答 2 核对：准确选择 `(.*?)`；明确量词星号前不能有反斜杠，并能说明 `\*` 只匹配字面星号、不能承担多字符正文的重复捕获职责。此前待确认的一字符边界已经闭合。
- 单项结果：`PARTIAL → PASS`。首答已独立构造可执行的完整 patterns、flags 与精确元组，补答闭合 `\Z` / DOTALL 职责及一处精确符号复核；历史 `(.\**?)` 式转义错误没有在实际提交的 pattern 中复现。
- 练习层建议的台账去向：`WP-24` 可作为本轮到期复测通过候选，建议由 `ai-agent-learning-review` 核对后从当前档位升至下一间隔档；具体日期由正式 reviewer 计算，本阶段不写入台账。

### S2 练习阶段小结

- 回收块已按预算只处理两个旧目标，没有追加第三项、算法题或 A4-Gate 新实现范围。
- `A4-Gate-R8 / WP-04`：代码输出、调用次数与控制流解释均 `PASS`；但同日 C3d 已近距离强化相同模型调用边界，因此只保留代码追踪证据，建议台账继续保持到期，未来换成无近端提示的规则优先 / LLM fallback 场景复测。
- `A4-Gate-R9 / WP-24`：`PARTIAL → PASS`；全新 `Draft* / Final*` 表面下，完整 patterns、flags、精确 Python 元组和执行结果正确，随后说清字面星号、重复量词、非贪婪、`\Z`、行首与 DOTALL 职责。建议作为升至下一间隔档的候选。
- 今天最虚 / 下次重点复习：知识本身已经能落到正确可执行 pattern；最容易失分的是解释时把实际 `*?` 多写成 `\*?`。下次看到正则先逐字符核对“这是控制量词还是被转义的字面字符”，再写自然语言说明。
- 边界：以上均为练习单项结论，不是 A4-Gate 父任务的正式判定；`tracker/weak-points.md` 的档位、日期和 `RECOVERY_BLOCK_PENDING` 仍由 `ai-agent-learning-review` 正式更新。

## 正式检查｜2026-08-11

### 覆盖矩阵与正式判定

| 范围 | 状态 | 决定性证据 |
| --- | --- | --- |
| A4-Gate Problem Contract、C1、C2、C3a～C3c | 已覆盖 / 既有 PASS | 关联 `daily/2026-08-05.md`～`2026-08-10.md`；本次没有换措辞重考 |
| C3d 纯函数：Observation → allowlist → 两个 prompt → validator | 已覆盖 / 既有 PASS，本次回归通过 | `daily/2026-08-10.md` 的独立 `160/160`；本次完整链继续使用同一 allowlist 并拒绝虚构来源 |
| C3d-I1 Reflection 单次模型调用与 SDK 响应外壳 | 正式 PASS | 教学矩阵 `22/22`；正式矩阵覆盖非法 prompt、缺字段、空正文、非 `stop` 与意外 tool calls，全部 fail-closed |
| C3d-I2 Refinement JSON Output 与解析 | 正式 PASS | 教学矩阵 `30/30`、validator 0 次；正式矩阵确认 `json_object`、`max_tokens=1600`、坏 JSON / 围栏 / `length` 在 validator 前停止 |
| C3d-I3 Reflection → Refinement → validator 编排 | 正式 PASS | 纯函数先于 factory；未注入时 factory 1 次；两次模型请求共用同一 client；所有分层失败均短路 |
| C3d-I4 原始请求到可信三字段正常链 | 正式 PASS | 非法输入零副作用；topic/path 忠实 fake 与真实 DeepSeek 均通过，模型 4 次、对应工具恰好 1 次、最终来源为本轮 Observation 子集 |
| S2 `WP-04` 调用计数 | 题目本身 PASS；不作间隔升档 | 精确输出 `投诉 建议 闲聊 1` 与控制流解释正确；同日 C3d 近端强化过模型调用计数，不是干净延迟证据 |
| S2 `WP-24` 正则迁移 | `PARTIAL → PASS`；升至 `+1月` | 首答完整 pattern/flags/元组可执行；补答闭合 `\Z`、点号 / DOTALL、`\*` 与 `*?` 的一字符边界；下次回炉 2026-09-11 |
| 七字段日志、统一 `max_steps=6`、一次工具重试、失败状态映射 | 未覆盖 / needs transfer | 当前 callable 正常链仍直接传播工具 / 模型异常，没有请求级步数、日志或 `tool_failure/insufficient_evidence/needs_manual` 控制器 |
| HITL fake 高风险工具 allow/deny | 未覆盖 / needs transfer | 合同已冻结，但尚无 `confirmed=True` 调 1 次、`False` 调 0 次的可执行证据 |
| 14 条 `eval_cases.json`、至少 3 条 holdout、分项阈值 | 未覆盖 | `code/stage4/eval_cases.json` 当前不存在，切片矩阵不能冒充 Gate eval |

正式结论：`A4-Gate-C3d = PASS`，S2 回收块实例完成；父任务 `A4-Gate = DOING`。父任务保持 `DOING` 是因为后续范围尚未实现，不是因为今天的 C3d 存在未闭合 `RETRY`。

### 验证命令与关键结果

- 语法：正式检查从当前文件读取源码并调用 Python `compile(..., "exec")`，结果 `PASS`；没有生成 `__pycache__`。
- 主审无落盘 harness：通过 PowerShell 标准输入交给 `.venv\Scripts\python.exe -`，使用忠实 SDK 属性对象、记录型 client、工具 spy 与故障注入；结果 `FORMAL_OFFLINE_PASS`，分组为 Reflection 47、Refinement 52、I3 编排 12、I4 wrapper 16、正则执行 2，合计 `129/129`。
- 独立只读复核：冻结范围合同 `40/40`、实际 fail-closed 链 `3/3`、入口 / 传播补验 `7/7`，`FAILED_IDS=[]`；夹具假设造成的首轮失败已被独立排除，不能归因于用户代码。
- 真实 DeepSeek 正常链：通过标准输入加载当前模块、代理真实 client 记录 kwargs / `finish_reason`，并包装真实工具注册表计数；topic 与 `relative_path=sample.txt` 均 `PASS`。
  - topic：最终 `success`，摘要 169 字符、2 个真实来源；模型 4 次，`search_web` 1 次；结束原因为 `tool_calls → stop → stop → stop`。
  - relative_path：最终 `success`，摘要 65 字符、来源精确为 `sample.txt`；模型 4 次，`read_material` 1 次；结束原因为 `tool_calls → stop → stop → stop`。
  - 两条链第 3 次 Reflection 均无 `response_format`，第 4 次 Refinement 均为 `response_format={"type":"json_object"}`、`max_tokens=1600`；来源子集断言均通过。
- 静态范围：`git diff --check -- code/stage4/a4_gate_research_summary_agent.py` 退出码 0；只有 Git 的 LF/CRLF 提示，无空白错误。
- 评估文件边界：`Test-Path -LiteralPath code/stage4/eval_cases.json` 返回 `False`。

### 测试 / eval / 性能 / 安全摘要

- 测试：本次只保留真实产品代码与 daily 证据，没有新建一次性 runner、baseline、报告或日志转储。离线矩阵覆盖 prompt 非法、SDK 外壳缺失、`finish_reason != "stop"`、坏 JSON、虚构来源、factory 次数、shared client、异常传播、双路由与工具调用计数。
- eval：14 条正式数据集尚未创建，因此正常 10/10、失败 3/3、危险 1/1 与 holdout 3/3 都不能宣称结果。Problem Contract 中预设阈值仍保持不变。
- 性能 / 成本：两条真实输出分别为 169、65 字符且都以 `stop` 完成，只能证明当前两个样本未截断；没有采集 token usage、延迟分布或长期占用，不能据此调小 `max_tokens=1600`。调参仍按“反复 `length` 才增大、长期代表性低占用且质量通过才下调”。
- 安全：非法输入在 client / 模型 / 工具前早返回；topic/path 各只执行一个匹配工具；Reflection / Refinement 不暴露工具；虚构来源由实际 validator 拒绝。HITL、审计日志与安全停止仍未执行，不能升级成完整安全 Gate。

### 本次工作场景 / 复合故障

- 覆盖 `WS-01 / WS-03 / WS-05 / WS-07`：同一完整集成事故里同时出现 SDK 属性对象被当 dict、`finish_reason` 所属层级错误、缺字段泄漏 `AttributeError`、client factory 早于纯函数校验、入口返回合同误接和内部字典接力错误。
- 修复路径：按真实 `response → choice → message` 层级守门；非 `stop` / 坏 JSON 统一 fail-closed；先跑确定性纯函数再创建 client；入口解包 `(normalized_request, invalid_result)` 并按 `candidate_summary/tool_name/tool_result` 三键接力；同一 client 贯穿四次模型调用。
- 回归证据：主审 129/129、独立合同 40/40 + fail-closed 3/3 + 入口 7/7，以及两条真实 DeepSeek 正常链。
- 等级边界：这足以把本切片记录为 `DIAGNOSED_AND_FIXED`；今天没有“模型 / 工具失败 + 七字段日志定位 + 重试后安全停止”的完整事故包，因此不升级 `WS-02`，也不冒充 A4-Gate 的恢复覆盖。

### Gate 预设阈值与 holdout 结果

- Problem Contract 已预设：正常路由 / 参数 / 最终合同 / 来源真实性 / 有据非空摘要均 10/10，摘要关键点至少 9/10；失败 3/3；危险 allow/deny 两个子分支同时通过才计 1/1；至少 3 条 holdout。
- 本日没有运行这些 Gate 阈值：`eval_cases.json` 尚不存在，holdout 结果为“未执行”，不能用今天的 C3d 129 项组件断言代替。

### 算法内容块状态与证据

- `tracker/algorithm-progress.md` 仍为 W4 4/4、内容块 `PASS`、结转债务 0；今天没有算法预算或新算法证据，不启动下一内容块，也不改算法台账。

### 岗位证据候选

- 本次是 Gate 内部切片，已把 `job-readiness.md` 中 Agent / Tool Calling / 结构化输出 / 安全的已有证据更新到 C3d，但等级不变：Agent 控制循环仍为 `LEARNING`，Tool Calling 与 LLM API 仍为 `COURSE_PASS`。
- `A4-Gate` 尚未完成日志、恢复、HITL、14 条 eval 和正式整关；没有 README / 架构 / 版本化完整评估与独立交叉复核，故不产生 `JOB_EVIDENCE` 候选。

## 批改判定

`PASS（限定 2026-08-11 完成范围）`：`A4-Gate-C3d-I1～I4` 与当日 C3d 模型 / JSON 集成正式通过；`A4-Gate` 父任务继续 `DOING`。`WP-04` 题目 PASS 但不升档；`WP-24` 正式 `PARTIAL → PASS` 并升至 `+1月`。

## 主审工具

Codex `ai-agent-learning-review`。

## 复核判定

## 当天进度小结

- S1：Reflection、Refinement JSON Output、两段编排、完整 wrapper、忠实 fake 和真实 topic/path 正常链全部闭合，C3d 正式 PASS。
- S2：两个旧目标均完成真实作答与执行核对；`WP-24` 退出到期集合，`WP-04` 因近端提示保持到期。
- 回炉调度：2026-08-11 这份 pending 实例已完成；重算后 `WP-04` 仍自 2026-07-19 起逾期 23 天，因此重新签发 `RECOVERY_BLOCK_PENDING`，而不是把积压写成全部清除。
- 今日计划范围全部完成，没有当日学习者补答或编码欠项。

## 总进度小结

- `A4-Gate` 继续 `DOING`；已经完成 Problem Contract、C1/C2、C3a～C3d。
- 任务总剩余：C4a 七字段日志 / 统一步数 / 一次重试与失败状态收口；随后 HITL allow/deny、14 条 eval、正式 Gate 复核。
- `problem-contract.md` 继续保持 `DRAFT`，等整关通过后再定稿。

## 对问题 / 不确定点的解释

- 你问的 `finish_reason="length"` 不需要再写一个重复的独立分支：当前 Reflection / Refinement 用 `finish_reason != "stop"` 统一拒绝，所以 `length` 会在读取 / 解析正文之前变成 `RuntimeError`。
- `max_tokens=1600` 是输出上限，不是必然生成量；400 的主要风险是截断 JSON，10000 的主要风险是放大异常长输出的最坏成本、延迟和资源占用。今天两个 `stop` 样本不足以支持调小。
- 独立诊断发现一个不阻断 C3d 的后续风险：C3c 第二次候选响应当前只检查非空正文，没有检查它自己的 `finish_reason`。若候选以 `length` 截断，后续 Reflection / Refinement 仍可能补救并由最终 validator 守住结构，但事实遗漏风险仍存在；C4a 应明确把它拒绝或映射为受控可恢复失败。
- `search_web` 成功但 `results=[]` 当前最终不会假成功，却会多消耗后续模型调用并以异常结束；统一控制器应提前映射为 `insufficient_evidence`。

## 问答点评与补充

- `max_tokens` 取舍最后已经从“取助手估计范围的中间值”进展为可观测工程判断：先区分输出上限与实际生成，再同时看截断、质量、长期占用、成本和延迟。
- 正则题最重要的实际产物首答就是正确的；补答暴露的是自然语言里一字符转写风险。你最终能明确说出 `(.*?)` 的星号不能加反斜杠，以及 `\*` 只匹配字面星号，这才闭合了历史弱点。
- SDK 响应修复说明你已经能跟着真实对象层级调试，但本次多轮订正也说明“先读返回合同、再写属性守门”还没有稳定成默认动作。

## 今天最虚 / 下次重点复习

- 最虚：协议边界的精确层级与副作用顺序。看到 SDK / 内部函数返回值时，先列出“外层对象、容器、元素、字段归属、失败类型”，再写访问代码；client / 网络 / 工具等副作用必须放在所有可先完成的纯校验之后。
- 旧复习重点：`WP-04` 仍需在没有近端提示的 hybrid“规则优先、LLM fallback”新表面复测；`WP-24` 已升档，不在短期重复出题。

## 超前内容提示

无。今天没有把 C4a、HITL 或 eval 预先实现，也没有因正常链通过而提前进入 G8-00。

## 补救任务

- 当日无未闭合 RETRY，不追加补救题。
- 下一 Session 的 C4a 是父任务正常续接，不属于今天的补救：建立同一请求的 `request_id/step` 与七字段日志；注入首次 `retryable=True`、重试成功 / 再失败两条轨迹；证明第 6 步后不发起第 7 次调用，并返回精确三字段 `needs_manual`。

## 知识库同步

- 已按固定口令授权检查 `D:\AI-Knowledge` 的规则、命名规范与候选页面。
- no-op：现有 `反思机制(Reflection).md` 已覆盖 Observation → allowlist → Reflection / Refinement → JSON → validator；`结构化输出(Structured Output).md` 已覆盖 `finish_reason="length"`、`json_object`、`max_tokens`、空 content、`json.loads` 与业务校验分层；`函数调用(Function Calling).md` 已覆盖 SDK message 属性对象而非 dict；分类与 ReAct 页面已完整覆盖 `WP-04 / WP-24`。
- 正式检查时因父任务仍为 `DOING`，没有把 C3d 切片冒充整关 PASS，也没有新建 PASS 复盘页；随后按用户明确要求“记录到知识库中”，新增 `D:\AI-Knowledge\04-Projects\Agent\AI-Agent-Learning\a4-gate-research-summary-agent.md`，以 `status: doing` 记录进行中的 A4-Gate 项目证据。
- 同步范围：C3d 的 129/129 离线矩阵、独立 40/40 + 3/3 + 7/7、topic/path 两条真实 DeepSeek 正常链、关键订正、当前安全边界、C4a 后续风险与父任务剩余；同时更新项目 README、Stage 4 课程页、课程索引、AI-Agent-Learning 跳转索引、全局索引和 `log.md`。
- 已有概念页继续保持 no-op；知识库严格健康检查最终结果：`OK: True`，214 个 Markdown、1867 个链接、0 个断链、0 个缺失 frontmatter、7 个 raw 非索引文件。

## 当前交接

- 今日计划范围 / 今日剩余：S1 C3d 与 S2 两个回收目标均已正式收口；今天没有剩余学习者范围。
- 任务总剩余：A4-Gate 的 C4a 日志 / 步数 / 重试 / 失败状态，之后 HITL、14 条 eval 与正式 Gate 复核。
- 下次 Session 起点（不属于今天）：启动 `A4-Gate-C4a｜统一步数、七字段日志与一次可重试工具故障的安全收口`；新日期必须重新排入 ≤180 分钟计划，并按 `RECOVERY_BLOCK_PENDING` 预留 20～30 分钟旧知识回收块。
