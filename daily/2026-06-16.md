# 每日学习打卡

日期：2026-06-16（周二）

学习时长：目标 ~2h（工作日）

完成任务编号：L1-03 多轮聊天（设计 + 写核心，计划）

今日定位：

- 工作日：先设计消息历史结构（system/user/assistant 列表怎么存、保留最近 N 轮），再写核心循环。写不完没关系，周三机动日补。
- 加班 fallback：累了就只读 HA 第 3 章《大语言模型基础》要点 + 在纸上/笔记里画消息历史结构，代码挪到周三。

今日推荐资料（计划要读的，带链接；实际看了/读了填到下面两栏）：

- [OpenAI 文本生成（含多轮对话消息）](https://platform.openai.com/docs/guides/text)（DeepSeek 接口一致）
- Hello-Agents 第 3 章《大语言模型基础》要点（本地 `repos/hello-agents/docs/chapter3/`；在线 [GitHub](https://github.com/datawhalechina/hello-agents)）
- （可选视频）[DeepLearning.AI《ChatGPT Prompt Engineering for Developers》](https://www.deeplearning.ai/short-courses/chatgpt-prompt-engineering-for-developers/)：Chatbot 一节

看了哪些视频/哪一集：

（做完后填你实际看的）

读了哪些文档/哪一节：

（做完后填你实际读的）

代码/产物位置：

- code/stage1/l1_03_chat.py

运行提示：

```powershell
cd C:\Users\26823\Desktop\AI-Agent-Learning
.\.venv\Scripts\python.exe code\stage1\l1_03_chat.py
```

今日任务清单：

- [ ] 设计消息历史结构（保留最近 N 轮）
- [ ] 写 `l1_03_chat.py` 核心循环，支持 `exit` 退出
- [ ] 连续对话 3 轮以上
- [ ] 回答今日练习题（13 题，可顺延到周三）

今日练习题（按新基调重出：13 题 / A 档难度 / 含 debug + 改代码；6/16 当天用 daily-practice-builder 按真实进度增删；锚定 `l1_03_chat.py`）：

A. 快速回忆

1. `l1_03_chat.py` 的退出判定是这一段：
```python
question = input("你：").strip()
if question.lower() == "exit":
    break
```
其中 `input(...).strip()` 表示先读取这一行输入、再去掉首尾空白。

请回答：
(1) 如果把 `question.lower() == "exit"` 改成 `question == "exit"`，那当用户输入大写 `EXIT` 或 `Exit` 时，程序会退出吗？为什么？
(2) 加了 `.lower()` 之后，用户输入下面哪些会触发退出？逐个判断：`exit`、`EXIT`、`Exit`、` exit `（前后各带一个空格）、`exit now`。

2. 间隔复习 L1-01（凭记忆默写，不许翻代码）。

场景：你用 OpenAI 兼容的 `chat.completions` 接口（DeepSeek，base_url="https://api.deepseek.com"）发了一次请求，拿到了返回对象，存进了变量 `response`：
```python
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[
        {"role": "system", "content": "你是简洁的助手"},
        {"role": "user", "content": "用一句话介绍你自己"},
    ],
)
```
请回答两点：
(1) 要从 `response` 里取出模型回复的那段文字，完整表达式怎么写？（写到能直接 print 出来的那一行）
(2) 上面 messages 里 `"system"` 和 `"user"` 这两个 role 各自的作用是什么？如果把 system 那条删掉，会少了什么？

B. 解释与比较

3. 看 `code/stage1/l1_03_chat.py` 里现在的 `build_prompt`，它把历史拼成一大段纯文本：

```python
def build_prompt(history, question):
    lines = ["下面是最近对话，请继续回答。"]
    for role, content in history:
        lines.append(f"{role}: {content}")
    lines.append(f"user: {question}")
    return "\n".join(lines)

# 然后这样调用模型：
answer = call_model(build_prompt(history, question))
```

假设此刻 `history = [("user", "我叫三玖"), ("assistant", "你好三玖")]`，`question = "我叫什么"`。

做法 A（现在这样，纯文本拼历史）：`build_prompt` 返回的是一个**字符串**，整段当成一个 user 问题发出去，大致是：
```
下面是最近对话，请继续回答。
user: 我叫三玖
assistant: 你好三玖
user: 我叫什么
```

做法 B（真正带 role 的 messages 列表）：不拼字符串，而是构造（L1-01 学过的那种结构，这里**先只列出 history 部分**，顶部的 system 暂时省略，第 (3) 问会用到）：
```python
messages = [
    {"role": "user", "content": "我叫三玖"},
    {"role": "assistant", "content": "你好三玖"},
    {"role": "user", "content": "我叫什么"},
]
```

请对比 A 和 B：
(1) 两者发给模型的**数据结构**分别是什么类型（字符串 vs 列表）？
(2) 各说一条优点、一条缺点。
(3) 回忆 L1-01 的 `call_model(prompt)`：它内部到底把 `prompt` 包成了几条消息、分别是什么 role？（提示：不止 user 一条。）据此判断 A 和 B 哪一个更接近 `chat.completions` 接口真正期望的 `messages` 格式？

4. 多轮聊天里，`l1_03_chat.py` 每轮都把历史一起发给模型（做法 A 拼进 prompt，或做法 B 拼进 messages 列表）。考虑这样一段会话，第二轮故意不带历史：

```python
# 第 1 轮
call_model("我叫三玖，记住")          # 模型给了某个回复（具体内容无所谓）
# 第 2 轮——只发当前这句，不带第 1 轮历史
call_model("我叫什么名字？")          # 注意：prompt 里没有任何关于'三玖'的内容
```

请回答：
(1) 第 2 轮这样调用，模型能答出'三玖'吗？为什么？
(2) 这说明 `chat.completions` 这类接口在'记忆'上是什么特性（一次请求和下一次请求之间，服务端会不会自动记住上一次说过的话）？
(3) 既然如此，`l1_03_chat.py` 为什么必须在每轮都把 `history` 重新拼进去发出？请用'记忆在谁手里'来解释。

C. 代码/操作

5. 看 `l1_03_chat.py` 里的 `trim_history`：
```python
MAX_TURNS = 3

def trim_history(history):
    return history[-MAX_TURNS * 2:]
```
现在有这样一个 history 列表（每个元素是 (role, content) 元组，一轮对话 = 一条 user + 一条 assistant）：
```python
history = [
    ("user", "a1"), ("assistant", "b1"),
    ("user", "a2"), ("assistant", "b2"),
    ("user", "a3"), ("assistant", "b3"),
    ("user", "a4"), ("assistant", "b4"),
]
```
调用 `trim_history(history)` 的返回值是什么？请写出确切的 Python 列表字面量。并用一句话说明：MAX_TURNS=3 时为什么切片写的是 `[-6:]` 而不是 `[-3:]`（请具体写出 `[-3:]` 会取到哪 3 个元素）。

6. 下面是 `l1_03_chat.py` 的 `main()`，只改动了一处（带注释那一行）。其余完全不变：
```python
def main():
    history = []
    while True:
        question = input("你：").strip()
        if question.lower() == "exit":
            break
        if not question:
            print("请输入内容，或输入 exit。")
            continue
        answer = call_model(build_prompt(history, question))
        print("AI：", answer)
        history.append([("user", question), ("assistant", answer)])  # 改动处
        history = trim_history(history)
```
问：
(1) 这处改动引入了什么 bug？
(2) 它会让程序产生什么症状（具体到 `history` 里存进去的是什么、以及下一轮 `build_prompt` 里 `for role, content in history` 会发生什么）？
(3) 怎么改回正确写法？

7. 场景：你在项目根目录 `C:\Users\26823\Desktop\AI-Agent-Learning`，项目用的虚拟环境在 `.venv`，要运行今天写的 `code\stage1\l1_03_chat.py`。请写出在 PowerShell 里用项目 .venv 的 python 直接运行这个脚本的完整命令（一行）。

8. 读 `l1_03_chat.py` 的 `main()` 循环开头：
```python
question = input("你：").strip()
if question.lower() == "exit":
    break
if not question:
    print("请输入内容，或输入 exit。")
    continue
```
回答三个小问：
(1) 这里 `.strip()` 和 `question.lower()` 各自的作用是什么？如果用户输入的是 `  EXIT ` （前后有空格、大写），程序会走 break 还是会把它当普通问题发给模型？
(2) `if not question` 这个分支什么时候为真？它对应的是用户的哪种输入？
(3) 这个分支用的是 `continue` 而不是 `break`，二者在这里的行为差别是什么？

D. 应用与迁移

9. 现在 `code/stage1/l1_03_chat.py` 里的 `build_prompt` 是把历史**拼成一大段纯文本**再发给模型：

```python
def build_prompt(history, question):
    lines = ["下面是最近对话，请继续回答。"]
    for role, content in history:           # history 形如 [("user","你好"),("assistant","你好呀"), ...]
        lines.append(f"{role}: {content}")
    lines.append(f"user: {question}")
    return "\n".join(lines)
# 之后这样用：answer = call_model(build_prompt(history, question))
```

问题：模型其实更喜欢吃**带 role 的 messages 列表**（每条是 `{"role": ..., "content": ...}`），就像 L1-01 里的 `[{"role":"system",...},{"role":"user",...}]`。

请你写一个新函数 `build_messages(history, question)`，返回一个 messages **列表**，规则是：
1. 第 1 条固定是一条 system 消息（内容你自拟，比如「你是一个乐于助人的中文助手。」）；
2. 中间把 `history` 里的每一项 `(role, content)` 转成 `{"role": role, "content": content}`；
3. 最后追加一条本轮的 user 消息，content 是 `question`。

请直接写出这个函数的完整代码。再额外说明一句：如果 `history = [("user","你好"),("assistant","你好呀")]`、`question="今天几号"`，你的函数返回的列表里**一共有几条**消息、第一条和最后一条分别是什么。

10. 接 P0-08（`json.dumps` / `json.loads`）。现在 `l1_03_chat.py` 的 `history` 是一个内存里的列表，程序一退出（`exit`）历史就没了。你想做到「关掉程序、下次再开还能接着上次的对话」——也就是把 `history` **存到磁盘文件、下次启动时读回来**。

已知 `history` 长这样（注意元素是**元组** tuple）：
```python
history = [("user", "你好"), ("assistant", "你好呀")]
```

请回答两部分：
1. **说思路**：退出前怎么把 history 存进 `history.json`？下次启动时怎么从 `history.json` 读回成一个可继续用的列表？分别用到 `json` 模块的哪个函数。
2. **写最小代码**：写出「保存」和「读取」两小段代码（用 `open(..., encoding="utf-8")` + `json.dump` / `json.load`，或 `json.dumps` / `json.loads` 都行）。并指出一个**坑**：tuple 经过 json 存盘再读回来，类型会变成什么？这会不会影响 `for role, content in history` 这种解包用法？

11. `l1_03_chat.py` 里有这一行限制历史长度：

```python
MAX_TURNS = 3
def trim_history(history):
    return history[-MAX_TURNS * 2:]   # 只保留最近 3 轮（6 条）
```

假设我们**去掉**这个 `trim_history`，让 `history` 无限增长，每轮都把**全部**历史拼进 prompt 发给模型。请具体说出这样做会带来哪些问题——**至少 2 个**，并且要分别讲清楚它们和下面两点的关系：

- (a) 一个和 **token 计费成本** 有关：随着对话变长，每轮发出去的 prompt 会怎样变化？这对花钱/速度有什么影响？
- (b) 一个和 **模型上下文窗口（context window）上限** 有关：模型一次能接收的 token 是有上限的，历史无限长最后会怎样？

要求结合「prompt 里包含了从第 1 轮到当前的所有历史」来解释，不要只说「会变慢/会更好」这种空话。

E. 自评

12. 今天 L1-03 多轮聊天里，涉及好几个相互关联的点：① `trim_history` 用 `history[-MAX_TURNS*2:]` 只留最近 N 轮，为什么是 `*2` 而不是 `*1`；② `build_prompt` 现在是把历史拼成一大段纯文本发给模型，而不是用真正带 role 的 `messages` 列表（`[{"role":"user",...},{"role":"assistant",...}]`）；③ 上下文窗口为什么需要 `trim_history` 来截断。

请回答：上面这三点里，今天你心里最没底、最不确定的是哪一个？用一两句话说清楚到底是哪里卡住——是某个表达式看不懂、某个概念串不起来、还是知道结论但说不出原因。（这题没有标准答案，照实写，方便明天针对性复习。）

13. 回顾今天做过的题（包含间隔复习的 L1-01/L1-02 旧题，比如 `response.choices[0].message.content` 取回复、运行命令 `.\.venv\Scripts\python.exe code\stage1\xxx.py`，以及今天 L1-03 的 `trim_history` 切片 `history[-MAX_TURNS*2:]`、`build_prompt` 拼历史、exit 退出与空输入处理）。

请挑出其中【有一道你是"半猜"答对的】——也就是答案大致写对了，但其实没十足把握、靠语感或印象蒙对的那一题。写出：是哪一题、当时你不确定的具体点是什么（比如"不确定切片到底是 `[-6:]` 还是 `[:-6]`""记不准是 `.message.content` 还是 `.content.message`"）。如果今天每道题你都很有把握、没有半猜的，就直接写"没有"并说明你最有把握的依据。

遇到的问题：

我最不确定的点：

希望 Codex 检查：

- 检查 `l1_03_chat.py` 能否连续多轮对话、能否 `exit`
- 批改 L1-03 检查题
- 判定 L1-03 是 PASS 还是 RETRY（如未写完则标记进度）

Codex 判定：

Codex 当天进度总结：

Codex 总进度总结：

Codex 对问题/不确定点的解释：

Codex 问答点评与补充：

Codex 超前内容提示：

补救任务：
