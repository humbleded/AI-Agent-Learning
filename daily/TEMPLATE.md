# 每日学习打卡

日期：

计划学习预算（只统计用户主动学习内容；默认 ≤180 分钟，不记录实际时长）：

> 助手读取资料、处理卡顿、等待工具/网络/测试、自动整理与写回不计入预算。同日多任务各占一个 Session；跨日续学必须链接该任务全部前序 daily。

## 当日 Session / 任务索引

| Session | 任务 | 环节 | 关联前序 daily | 当前状态 / 下一步 |
| --- | --- | --- | --- | --- |
| S1 |  | 计划 / 带读 / 实现 / 正式练习 / 检查 |  |  |

## 任务证据接力（每个任务各复制一份）

### Session S1｜任务 `<任务ID>`

任务状态与学习单元：

关联前序 daily：

资料定位与版本（仅本地仓库或版本敏感官方资料适用：path + commit，或 URL + 检查日期/版本）：

看了哪些视频/哪一集：

读了哪些文档/哪一节：

已覆盖目标及证据位置：

RETRY / 仍需迁移：

未覆盖 PASS 标准 / 下次起点：

代码/产物位置：

notes 路径与状态（无 / DRAFT / FINAL）：

## 带读与即时考察

> 这里只记录实际发生的带读证据。每题保留原始回答、订正过程和单题状态；已 PASS 的目标会计入当前任务的覆盖预算，跨日不清零，正式练习不得重复。证据 ID 必须带任务命名空间。

### `<任务ID>-G1`｜考察目标

题目：

原始作答：

订正与补充：

单题状态：

当前任务累计即时考察有效证据数（跨日）：

仍需迁移 / RETRY 的目标：

## 正式练习

> 由 `daily-practice-builder` 读取当前任务全部关联 daily，根据“已覆盖 / 需迁移 / RETRY / 未覆盖”生成。概念/阅读任务只补任务级覆盖缺口；编码、Gate、项目以真实产物为主。正式题 ID 使用 `<任务ID>-F1` 起的命名空间。

已排除的重复目标：

- A 快速回忆：
- B 解释与比较：
- C 代码/操作：
- D 应用与迁移：

### `<任务ID>-F1`｜覆盖标签 / 考察目标

题目：

原始作答：

订正与补充：

单题状态：

我的作答：

遇到的问题：

我最不确定的点：

## 复习回炉（适用时）

> 旧复习目标从 `tracker/weak-points.md` 三池共享选择并计入当天总证据：概念/阅读日默认 1 个，编码/Gate/项目日通常 0–1 个，任何日最多 2 个。没有安排时写“不适用”，不要为填表临时加题。

本次复习目标 ID / 类型：

与当前题是否语义合并：

原始作答 / 执行证据：

订正路径：

单项结果：

台账去向 / 下次回炉：

## 工程验证（适用时填写）

运行提示：

验证命令与关键结果：

测试/eval/性能/安全摘要：

本次工作场景/复合故障：

场景 ID 与类别：

故障输入、复现命令与关键日志：

定位证据与全部根因：

修复/止损、定向测试与全量回归：

恢复/再次故障注入结果：

工作场景证据等级：

Gate 预设阈值与 holdout 结果：

算法内容块状态与证据（W4 起）：

岗位证据候选（仅里程碑填写，普通日留空）：

## 批改与小结

希望复核/检查：

批改判定：

主审工具（仅需交叉复核时填写）：

复核判定（普通任务默认留空；JOB_EVIDENCE、FINAL/J11-Gate、投递前必须由另一工具填写）：

当天进度小结：

总进度小结：

对问题/不确定点的解释：

问答点评与补充：

今天最虚 / 下次重点复习：

超前内容提示：

补救任务：
