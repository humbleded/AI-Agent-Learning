# 每日学习打卡

日期：2026-06-30（周二）

> 注：S-03 上下文工程的**概念带读 + 15 题练习**昨天（06-29）已全 PASS，笔记也写完；唯一没收尾的是**动手产物**——token 对比实验 `code/stage2/s03_context_experiment.py` 还是骨架（5 处 TODO）。它是 S-03 的通过标准之一，progress 里 S-03 仍挂 TODO。
> 今天是工作日（~3h）：主线=**收尾 S-03**（把实验写完跑通 → S-03 才算达成），副线=**预读下一个任务 PR2-04 结构化输出（JSON/Schema）**。daily-plan 规则 1 正好把「S-03 的 token 对比」点名为工作日适合做的自包含小实验；PR2-04 写代码与 PR2-Gate 留周末整块时间。

学习时长：

完成任务编号：S-03 上下文工程（收尾：token 对比实验）＋ PR2-04 概念预读

今日目标：

- 把 S-03 的 token 对比实验从骨架写完、跑通——三方案（全量 / 裁剪 / 压缩）都能算 token 并打印对比，预期 ① > ③ > ②，并能在输出里说清「② 最省但丢早期信息、③ 花一次摘要换回早期信息」。这一步做完 S-03 才达到通过标准。
- 巩固昨天判定「最虚」的点：5 种上下文手段（trim / JIT / Compaction / 结构化笔记 / 子代理）的**归类·分层·场景匹配**。
- 预读 PR2-04 结构化输出核心概念：搞清「JSON 模式 vs 严格 Schema」的区别，以及在 DeepSeek 上该怎么落地（为周中写 `pr2_04_extract_json.py` 铺垫）。

看了哪些视频/哪一集：

读了哪些文档/哪一节：

- 复习：`notes/stage2/s03_context_engineering.md` 第四节（五手段速查表）＋第六节（踩的坑）。
- 预读：DeepSeek JSON 输出模式（官方文档）＋ OpenAI Structured Outputs（概念，导读见下）。

代码/产物位置：

- `code/stage2/s03_context_experiment.py`（待填 5 处 TODO，复用 C3 的 `compact_history`）。

运行提示：

- 实验若真调 DeepSeek 统计 token：项目根目录用 `.venv`；中文乱码先设 `PYTHONUTF8=1`。先用 `fake_summarize` 假摘要跑通对比即可，不必真调。

今日任务清单：

- [ ] 复习自己的 S-03 笔记第四节＋第六节（手机可看）
- [ ] 预读 PR2-04：DeepSeek JSON 模式三条硬要求 ＋ OpenAI「JSON 模式 vs 严格 Schema」区别（手机可看）
- [ ] 收尾实验：填完 `s03_context_experiment.py` 的 5 处 TODO 并跑通（去「开始今天的练习」我带）
- [ ] 学完去「开始今天的练习」→ 我带填实验骨架 + 现出题

> 以下留给「开始今天的练习」「检查今天的学习」回填。

---

## 今日练习（S-03 实验收尾 + PR2-04 预读 · 15 题 · 聊天逐题作答）

> 锚定：S-03 token 实验（刚写完）＋ PR2-04 结构化输出预读（带读后作答）＋ 复习昨天最虚「5 手段归类·分层·场景匹配」。逐题作答、边批边回填。
> 说明：PR2-04 预读用户原本没读，先用 learning-coach 带读 `notes/stage2/pr2_04_structured_output_preview.md` 一遍，再回来作答。

### A. 快速回忆

**A1** JSON 模式 vs 严格 Schema 各保证 / 不保证什么？
```python
# 用户答：1. JSON 模式保证「是合法 JSON」；不保证缺字段 / 多字段 / 字段类型（即不保证字段齐、不多、类型对）。
#         2. 严格 Schema 在其之上还保证：不缺字段、不多余字段、字段类型对应。
# 补充：严格 Schema 共 5 件（字段齐 / 类型对 / 不许多余 / 枚举值不乱编 / 拒答可检测），题目要 2-3 件，已达标。
# 判定 PASS
```

**A2** DeepSeek 开 JSON 模式（`json_object`）的三条硬性要求？
```python
# 用户答：1. prompt 中必须出现 "json" 这个词；2. prompt 里要给一段 JSON 样例；3. 设好 max_tokens 防止 JSON 被截断。
# 判定 PASS（三条全准）
```

**A3** schema（结构契约）约束哪 4 件事？
```python
# 用户答：1. 必填字段——哪些字段必须出现、不能漏；2. 字段类型不能给错；3. 枚举值只能从给定集合里选、不能编造；4. 不能多/少字段。
# 判定 PASS（4 件全中：①必填 ②类型 ③枚举 ④不许多余字段。注：“不能少”=①必填重叠，“不能多”才对应 additionalProperties:false）
```

### B. 解释与比较

**B1**（复习弱点）把 5 手段按「层」归类。
```python
# 用户答：① 最朴素基础档=trim（截断）；② 长时程三件套=Compaction / 结构化笔记 / 子代理架构；③ 按需检索类=JIT 及时检索。
# 判定 PASS（昨天判定“最虚”的点，今天全对且干脆，复习见效）
```

**B2**（为什么）实验里 `fake_summarize` 真调 DeepSeek vs 规则版假摘要，对 token 对比实验的影响？
```python
# 用户答：真模型摘要会提炼、字数不固定（不一定刚好 20 字）；规则版硬截取、固定截到 20 字、不管信息丢没丢。
#         关键：同一段话，规则版输出一定一样（确定/可复现）；真调模型不一定每次相同（不确定）。
# 点评补充：方案③ token 数——规则版每次跑都一样（可复现、干净对照）；真模型每次波动（不可复现），
#           还引入花钱+延迟、失败时返回错误串污染结果；好处是真“提炼”、信息保留好（呼应 Compaction 代价：钱+延迟+可能失真）。
# 判定 PASS（抓住核心：确定性/可复现性差异）
```

**B3**（为什么）为什么方案③（压缩）一定 > 方案②（裁剪）、再压也省不过？
```python
# 初答（错·关键坑）：以为 compaction “把历史全丢给上下文窗口，输入 token 不减少、只减少输出 token”。
#   → 对 compaction 机制的根本误解。
# 提示1：回看自己代码 compact_history return [summary]+history[-keep:]，旧历史是被“摘要替换”不是全丢。
# 提示2：只数送进窗口的内容——②=[最近2条]，③=[摘要+最近2条]，差别只在③多挂的那条摘要。
# 订正（对）：①摘要 token 不可能是 0；②所以 ③ 永远比 ② 多一条摘要的 token，省不过 ②。
# 收口（错误理解 → 正确理解）：
#   ❌ compaction 全丢进窗口、只减输出 token；
#   ✅ compaction 把旧历史“替换”成短摘要，送进窗口=摘要+最近N条，输入 token 是减少的（故 ③<① 全量）。
#   三者 ①>③>②：③ 比① 省(旧6条压成1条摘要)、比② 费(多挂一条摘要)。trim 硬删省最狠但丢早期信息；compaction 省得少但保梗概。
# 判定 RETRY→PASS（经提示1/2 闭环；关键坑：输入token vs 输出token、“替换”非“全丢”）
```

**B4**（接 PR2-04）「优先级只能填 高/中/低」对应 schema 哪条约束、为什么防乱编值？
```python
# 第1问：枚举值（enum）。PASS
# 第2问初答（偏软·错位）：“拉高模型选中该值的概率？” → 把硬约束误解成软提示。
# 提示1（硬约束 vs 软建议 / 下拉菜单 vs 自由填空）→ 用户接受“硬约束”，但追问“怎么做到编不出特急”。
# 机制讲解：模型逐 token 生成、每步给全词表打分；严格 Schema(strict) 在生成该字段值时，把“高/中/低”外的所有 token 概率掐成 0(屏蔽)，
#   “特”根本不在候选里 → 物理上选不到 → “特急”生成不出来。术语：约束解码 constrained decoding。
# 用户复述（对）+ 主动迁移类比：像 top-k（只在给定候选里选）/ 低温（杜绝低概率词）。
# 类比校准：① top-k 贴切——同是“概率清零”硬动作，但依据不同：top-k 按概率排名留前 k 个，enum 按“在不在合法集合”留(不看概率)；
#   ② temperature 不贴——低温“软压低、不归零”，低概率词仍可能出现，不保证编不出特急；enum/top-k 是“硬归零”。
# ⚠️现实落差：OpenAI 严格 Schema=硬锁(编不出)；DeepSeek 弱档 json_object 无硬锁，prompt 写 enum 只是软请求(此时“提高概率”才对)→需 Python 端校验兜底。
# 判定 RETRY→PASS（机制懂了+迁移类比；坑：软提示 vs 硬屏蔽、temperature(软)≠enum/top-k(硬归零)、严格档 vs DeepSeek 弱档落差）
```

### C. 代码/操作练习

**C1**（读码预测）`plan_trim(SAMPLE_HISTORY, 2)` 返回哪几条、还有没有「三玖」、`name_in_trim` 打印啥？
```python
# 用户答：1. 第7、8条；2. 找不到「三玖」（在第1条被裁掉）；3. False。
# 判定 PASS（全对，呼应“裁剪按位置删、丢早期信息”）
```

**C2**（debug）DeepSeek JSON 调用违反三条硬要求的哪几条、后果、怎么改？
```python
# 用户答：1. 没带 "json" 关键词、没给示例（违反第1、2条；max_tokens 未提，短输出影响小）；
#         2. 后果：模型不按标准 JSON 输出、前后可能带文字，json.loads 读不出来（报错）；
#         3. 修正 system：加“只能输出JSON、前后不带文字”+给“输入→输出”示例。
# ⚠️订正点：用户修正的示例 JSON 用了单引号 {'城市':'武汉'} → 单引号不是合法 JSON！
#   订正：JSON 的 key/字符串值必须双引号；示例若给单引号，模型照学输出单引号“JSON”，json.loads 抛 JSONDecodeError。
#   正确示例：{"城市":"武汉","温度":"30"}。模型会照示例学 → 坑是自己埋的。
# 判定 PARTIAL→PASS（三条+后果+修正方向对；坑：示例 JSON 必须双引号，单引号 json.loads 会炸）
```

**C3**（debug）没处理「空 content」就直接 json.loads，问题 + 兜底？
```python
# 初答（坑记岔）：把那个坑记成“截断/不完整JSON”（截断其实是 finish_reason==length，属“异常查3处”，另一回事）。
#   → 预读专门标注的坑是“偶尔返回空 content”（content 为 "" 或 None）。
# 第2问对：出问题在 data = json.loads(content) 这行（json.loads("") 抛 JSONDecodeError）。
# 兜底初版（结构乱）：return 漏在 if 外、无条件执行 → 逻辑自相矛盾 + 死代码。
# 订正（对）：
#   content = response.choices[0].message.content
#   if not content:               # 真值判断，空串"" 和 None 都 falsy，一箭双雕
#       return "模型没返回内容，请重试"
#   data = json.loads(content)
# 判定 PARTIAL→PASS（坑订正：空content≠截断；结构订正：先挡异常 return 早退、非空再解析；if not content 挡空串+None）
```

**C4**（debug/改写）compact_history 传 keep_recent=0 会怎样、加守卫？
```python
# 用户答：1. history[-0:]=history[0:]=整个列表（-0==0）→返回全量；
#         2. 返回整个历史+本轮，没达到压缩省 token 目的；
#         3. 给 keep_recent 先判空/判零。
# 补充：keep_recent=0 时 history[:-0]=history[:0]=空 → old_history 空、摘要是对空文本的摘要(无意义)；
#       实际返回 [一条空摘要]+全量历史，比全量还多一条 = “负优化”。
# 校准：keep_recent 是数字，说“判零/判负”比“判空”准。
# 守卫示例：if keep_recent <= 0: 把 history 全部 content 拼成 old_text → summarize → return [一条摘要]（一条不留最近）；
#           或规定 keep_recent>=1，传 0 抛 ValueError。
# 判定 PASS（预测准 -0 切片坑 + 意识到加守卫；校准判零≠判空、补“负优化”）
```

**C5**（写代码）validate(data)：必填 6 字段 / 缺填 None / 去多余。
```python
# 用户答（逻辑对）：1.循环 REQUIRED 判断 data key 是否在其中；2.不在则补该字段=None；3.REQUIRED 没有而 data 有的去掉；4.返回 data。
# 更优写法（新建 dict，避坑）：
#   def validate(data):
#       return {k: data.get(k) for k in REQUIRED}
#   一箭三雕：只遍历 REQUIRED=去多余；data.get(k) 缺则 None=补 None（比 data[k] 安全、缺 key 不 KeyError）；必填全在。
# ⚠️坑：在原 data 上“边遍历边增删 key”会 RuntimeError: dictionary changed size during iteration；新建 dict 绕开。
# 判定 PASS（三件事逻辑全对；学到 dict.get + 字典推导一行版 + 遍历时改 size 坑）
```

### D. 应用与迁移

**D1**（真实场景）邮件助手：a 单封结构化提取 / b 长线程超窗口，各用什么？
```python
# 用户答：1. schema（结构化输出，方向对）；2. 压缩整合 Compaction——要连贯性 + 多轮超长 → 压缩整合。
# 补全：a=结构化输出全套（json_object + prompt 写 schema/样例/枚举/缺填 null + 代码 validate），因下游要存库/排程，需稳定 JSON。
#       b=Compaction 正确（长线程超窗口+要连贯性）；这次 compaction 用对了（旧往来压摘要保连贯、非“全丢”，B3 落地）。
#       可叠加：线程里关键决议/订单号跨轮长期记 → 再叠结构化笔记（真实 Agent 常几种叠用）。
# 判定 PASS（场景匹配准，昨天最虚点已稳）
```

**D2**（迁移）客服跨天：a 牢记订单号/诉求 / b 当天对话变长，各用哪个？
```python
# 用户答：1. 结构化文档（→ 应叫“结构化笔记”）——跨轮跨天不忘、持久化存储、下次调回；2. 压缩整合 Compaction——连贯+不超长。
# 校准：术语是“结构化笔记”(structured note-taking)，非“结构化文档”；概念/理由全对。
# 亮点：需求a 正是昨天 D1 错选“子代理”的坑，今天纠正到位（B1+D1+D2 连对，昨天最虚“场景匹配”补牢）。
# 判定 PASS
```

**D3**（迁移）count_tokens 中英混合不准，真实计费换成什么、为什么？
```python
# 第1问：本对比实验里“够用”（只看相对趋势 ①>③>② 的粗估），不能用于精确/计费。用户初说“不够用”→校准为“够对比、不够计费”。
# 第2问初答（错）：“改用中文算”没到点。
#   提示（回想 L1-05 prompt_tokens=24 从哪来）→ 订正：真实计费 token 从 API 响应的 usage 拿。
#   补：usage.prompt_tokens/completion_tokens/total_tokens = 服务端真 tokenizer 算的、实际扣费依据；发请求前本地预估可用 tiktoken 等分词器。
# 第3问（方向对+补全）：模型按 tokenizer 子词(subword)规则切——中文字可能≠1token、英文词可能拆几段、标点空格也占；故“字数×系数”只能毛估。
# 判定 PARTIAL→PASS（第2问经提示订正到 usage；第1问校准、第3问补全）
```

### 练习小结
- **判定**：15/15 PASS（B3 / B4 / C2 / C3 / D3 经 RETRY/PARTIAL 订正闭环；过程坑已逐题留存）。
- **今天最虚 / 下次重点复习**：**Compaction 的机制细节**——B3 曾误解“全丢进窗口、只减输出 token”，需记牢：compaction = 旧历史“替换”成摘要、**输入 token 减少**（③<①），且 ③=②+摘要（故 ③>②，省不过 trim）。PR2-04 预读概念、5 手段场景匹配掌握好；昨天最虚的“手段归类·场景匹配”今天 B1+D1+D2 连对，已补牢。
- **关键易错点（→ 概念卡）**：
  1. Compaction 是“替换非全丢”、输入 token 减少（③<①）、③=②+摘要（③>②）；trim 硬删省最狠但丢早期信息、compaction 省得少但保梗概。
  2. enum 硬屏蔽（候选概率归零）vs temperature 软压低（仍 >0）；DeepSeek 弱档 `json_object` 无硬 schema，prompt 写 enum 只是软请求 → 需 Python 端校验。
  3. 示例 JSON 必须**双引号**，单引号 `json.loads` 抛 `JSONDecodeError`（模型会照示例学）。
  4. DeepSeek JSON 模式坑 = 偶尔**空 content**（≠截断）；`loads` 前 `if not content` 兜底早退。
  5. `validate` 一行版 `{k: data.get(k) for k in REQUIRED}`（去多余+补 None+必填全）；别边遍历边增删 dict（RuntimeError）。
  6. 真实计费 token 用 **API `usage`**（真 tokenizer），`count_tokens` 只够粗估相对对比。
- **待办 / 下一步**：
  - S-03 实验代码今天已写完（练习步过目逻辑无误）；**正式判 S-03 PASS + 回填 `progress.md` 建议走「检查今天的学习」**（会真跑一遍验证 usage / 三方案 token）。
  - S-03 概念已完整（概念+练习+实验），可整理进 `D:\AI-Knowledge\02-Concepts\Agent\context-engineering.md`（含 B3“替换非全丢”坑）。
  - PR2-04 为**预读**，正式动手交付（`pr2_04_extract_json.py`）后再整理结构化输出概念卡。

Codex 判定：**S-03 PASS**（本次「检查今天的学习」真跑验证）。

- **判定依据（真跑）**：`python code/stage2/s03_context_experiment.py` 实跑成功——① 全量 8 条/100 token、② 裁剪 2 条/25 token、③ 压缩 3 条/51 token → **①>③>② 成立**；② 里“三玖”=False（被裁掉、丢早期信息），③ 里“三玖”=True（真调 DeepSeek 生成摘要「之前对话摘要：三玖向助手求推荐，获推《机器学习》和《星际穿越》。」保住早期信息）。S-03 通过标准（三方案 token 对比 + 说清 ②省但丢信息 / ③用摘要换回召回）全部达成。
- **Codex 当天进度总结**：S-03 上下文工程收尾完成（概念 06-29、实验 06-30）；PR2-04 结构化输出完成预读（带读 + 15 题锚定）。练习 15/15 PASS。亮点：昨天最虚“手段归类·场景匹配”补牢（B1+D1+D2 连对）；纠正 B3“compaction 全丢 → 替换成摘要、输入 token 减少”的关键误解。
- **Codex 总进度总结**：阶段 0/1 全 PASS；阶段 2 PR2-01/02/03 + S-03 全 PASS；下一个 PR2-04 JSON/Schema（已预读、待动手）。
- **Codex 对问题/不确定点的解释**：约束解码（constrained decoding）、tokenizer 子词切分为超前概念，今天了解即可；深入在阶段 3（工具入参校验）/阶段 6（RAG）再碰。
- **Codex 问答点评与补充**：15 题逐题点评见上“今日练习”各题作答框（含 B3/B4/C2/C3/D3 订正过程与坑）。
- **Codex 超前内容提示**：严格 Schema（`json_schema+strict`）是 OpenAI 能力，DeepSeek 目前弱档 `json_object`；强 schema 约束 / 工具入参校验在阶段 3 Tool Calling 深入。
- **补救任务**：无（S-03 达标）。PR2-04 正式动手 `pr2_04_extract_json.py` 按队列走。
