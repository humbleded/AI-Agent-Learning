# 每日学习打卡

日期：2026-06-30（周二）

> 注：S-03 上下文工程的**概念带读 + 15 题练习**昨天（06-29）已全 PASS，笔记也写完；唯一没收尾的是**动手产物**——token 对比实验 `code/stage2/s03_context_experiment.py` 还是骨架（5 处 TODO）。它是 S-03 的通过标准之一，progress 里 S-03 仍挂 TODO。
> 今天是工作日（~3h）：主线=**收尾 S-03**（把实验写完跑通 → S-03 才算达成），副线=**预读下一个任务 PR2-04 结构化输出（JSON/Schema）**。daily-plan 规则 1 正好把「S-03 的 token 对比」点名为工作日适合做的自包含小实验；PR2-04 写代码与 PR2-Gate 留周末整块时间。

学习时长：

完成任务编号：S-03 上下文工程（收尾：token 对比实验）＋ PR2-04 概念预读

今日目标：

- 把 S-03 的 token 对比实验从骨架写完、跑通——三方案（全量 / 裁剪 / 压缩）都能算 token 并打印对比，预期 ① > ③ > ②，并能在输出里说清「② 最省但丢早期信息、③ 花一次摘要换回早期信息」。这一步做完 S-03 才达到通过标准。
- 巩固昨天判定「最虚」的点：5 种上下文手段（trim / JIT / Compaction / 结构化笔记 / 子代理）的**归类·分层·场景匹配**。
- 预读 PR2-04 结构化输出核心概念：搞清「JSON 模式 vs 严格 Schema」的区别，以及在 DeepSeek 上该怎么落地（为周中写 `pr2_04_extract_json.py` 铺垫）。

看了哪些视频/哪一集：

读了哪些文档/哪一节：

- 复习：`notes/stage2/s03_context_engineering.md` 第四节（五手段速查表）＋第六节（踩的坑）。
- 预读：DeepSeek JSON 输出模式（官方文档）＋ OpenAI Structured Outputs（概念，导读见下）。

代码/产物位置：

- `code/stage2/s03_context_experiment.py`（待填 5 处 TODO，复用 C3 的 `compact_history`）。

运行提示：

- 实验若真调 DeepSeek 统计 token：项目根目录用 `.venv`；中文乱码先设 `PYTHONUTF8=1`。先用 `fake_summarize` 假摘要跑通对比即可，不必真调。

今日任务清单：

- [ ] 复习自己的 S-03 笔记第四节＋第六节（手机可看）
- [ ] 预读 PR2-04：DeepSeek JSON 模式三条硬要求 ＋ OpenAI「JSON 模式 vs 严格 Schema」区别（手机可看）
- [ ] 收尾实验：填完 `s03_context_experiment.py` 的 5 处 TODO 并跑通（去「开始今天的练习」我带）
- [ ] 学完去「开始今天的练习」→ 我带填实验骨架 + 现出题

> 以下留给「开始今天的练习」「检查今天的学习」回填。
