# 2026-08-09 学习记录

- 当前主任务：`A4-Gate` 研究总结 Agent
- 任务状态：`DOING`（沿用 `tracker/progress.md`；今天开始动手不等于 Gate 已通过）
- 今日计划学习预算：180 分钟
- 预算拆分：间隔回收 25 分钟 + 定向资料核对 30 分钟 + 用户实现 90 分钟 + 真实调用与讲解验收 35 分钟
- 实际学习时长：不记录；助手读取资料、自动整理记录和等待工具结果不计入学习预算

## 当日 Session / 任务索引

| Session | 范围 | 预算 | 状态 |
| --- | --- | ---: | --- |
| S1 | `RECOVERY_BLOCK_PENDING` 强制回收块 | 25 分钟 | 练习层 2/2 收口；正式台账复核：`CD-001` 升档，`CD-002` 转活跃 `WP-32` |
| S2 | A4-Gate：真实 DeepSeek 模型 / 工具正常闭环 | 155 分钟 | C3a、C3b `PASS`；C3c 跨日至 2026-08-10 收口 |

## Session S1｜间隔回收块

### 学习定位

- 本块来自 `tracker/weak-points.md` 的持久状态 `RECOVERY_BLOCK_PENDING`，不能因跨日或切换到编码任务而清除。
- 目标是确认两项已到期的核心定义能否脱离原题迁移，而不是重讲 A4-02；最多使用 2 个旧复习席位，聊天中严格一次一题。
- 这两项会直接服务 S2：区分“训练改变模型”与“请求内上下文改变输出”，并解释结构化 messages 如何真正变成模型读取的 Token 序列。

### Preflight 与语义去重

- 已核对：`tracker/progress.md`、`tracker/weekly-plan.md`、`tracker/daily-plan.md`、`tracker/ai-agent-learning-tracker.md`、`tracker/weak-points.md`、`tracker/work-scenario-coverage.md`、`tracker/job-readiness.md`、`tracker/algorithm-progress.md`。
- 已重新核对 A4-02 原始证据：`daily/2026-07-14.md`、`daily/2026-07-15.md` 与 `notes/stage4/a4_02_llm_agent_basics.md`。
- 今日不重复抽取 `WP-02`、`WP-04`、`WP-15`、`WP-24`：它们已在 2026-08-08 的 HTTP / JSON / 异常边界实现和“工具是不是模型”的追问中直接暴露或刚完成澄清，立即再问会主要测短期记忆。
- 本块选择 `CD-001` 与 `CD-002`：二者均已到期，且与昨天的实现表面不重合；先考 `CD-001`，批改完成后才展示 `CD-002`。

### `A4-Gate-R4`｜`CD-001`：训练数据、训练过程与请求内上下文

- 类型：到期核心定义回炉；换场景迁移；一次一题。
- 关联旧证据：`daily/2026-07-14.md` 的 G6 / “预训练、微调、Zero-shot 与 Few-shot”，以及 `daily/2026-07-15.md` 的 F1。
- 题目：某团队发现 Agent 经常选错工具，于是依次做了三件事：A. 把 200 条经过审核的“用户请求 → 正确 Tool Call”样本保存为 `jsonl`，除此之外什么也没做；B. 用这些样本继续训练，产出新模型 `agent-tools-v2`，但生产环境仍请求旧的 `base-agent-v1`；C. 只在某一次测试请求的 `messages` 中放入 2 条正确示例，下一次请求不再携带。请分别判断 A、B、C 属于 `Instruction Data`、`Instruction Tuning`、`Few-shot Prompting / In-context Learning` 中的哪一层；再说明每种情况下模型参数是否改变、效果保存在哪里，以及为什么生产环境或下一次请求仍可能没有改善。最后分别写出让 B、C 的效果真正生效所需的最小改动。
- 状态：第一次作答已批改，正在定向补齐遗漏项。
- 用户原始作答：

  > 1.A是Instruction Data，B是Instruction Tuning，C是Few-shot Prompting。说一下Instruction Data和Instruction Tuning的中文名称是什么
  >
  > 2.Instruction Tuning训练模型会改变参数的权重，保存在agent-tools-v2中
  >
  > 3.因为B训练的是agent-tools-v2，而生产还在用base-agent-v1。C的话下一次如果不带示例，可能还是会出错
  >
  > 4.B的生产环境模型换成agent-tools-v2，C的话每次请求都携带正确的示例

- 批改：A / B / C 分类正确；B 的参数更新位置、生产未改善的模型路由根因及最小修复正确；C 的跨请求边界与最小修复正确。第 2 问只回答了 B，尚未明确 A 与 C 是否改变参数，以及二者的效果分别保存在哪里。
- 术语校准：`Instruction Data` 中文为“指令数据”；`Instruction Tuning` 中文为“指令微调”。
- 提示 / 订正路径：只追问第 2 问遗漏的 A、C，不重答已通过内容。
- 单题结果：`PARTIAL`（分类、B、部署 / 跨请求诊断和修复均已通过；只缺 A、C 的参数与存放位置）。

#### `A4-Gate-R4-R1`｜参数与效果存放位置定向补齐

- 题目：只回答 A 和 C：它们会不会改变模型参数？各自的“效果”实际保存在哪里？
- 用户原始作答：

  > 1.A不会改变模型参数，实际保存在jsonl中
  >
  > 2.C不会改变模型的参数，实际保存在prompt中

- 批改：两项均正确。A 的样本作为指令数据保存在 `jsonl` 文件中，仅保存数据本身不会改变模型行为；C 的示例只存在于本次 Prompt，也就是当前请求的 `messages` / 上下文中，不会写入模型参数，也不会由后续请求自动继承。
- 单题结果：`PARTIAL → PASS`。

#### `A4-Gate-R4`｜最终结果

- 最终判定：`PASS`。
- 已覆盖：指令数据、指令微调、Few-shot Prompting 的层次；参数是否更新；数据 / 新模型权重 / 当前上下文的存放边界；生产模型路由与跨请求持久性。
- 提示使用：首次独立完成分类、B 的参数位置、失败诊断及最小修复；定向追问后补齐 A、C，未展示参考答案。

### `A4-Gate-R5`｜`CD-002`：messages 到 Token IDs 的边界

- 题目：一个本地多模型适配器先为模型 X 执行 `rendered_x = tokenizer_x.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)`。切换到模型 Y 时，它没有保留并重新处理原始 `messages`，而是直接执行 `token_ids_y = tokenizer_y(rendered_x).input_ids`。模型 Y 随后开始混淆 `system/user/assistant`，有时还继续扮演用户，而不是从 assistant 位置生成。请回答：① 从结构化 `messages` 到模型 Y 真正读取的输入，正确的完整转换链是什么；② 上述代码错误复用了哪一层的中间结果，为什么模型 X 的已渲染 Prompt，甚至 X 的 Token IDs，都不能直接交给模型 Y；③ 这种错误可能破坏哪些角色、消息边界或生成位置；④ 最小修复是什么；⑤ `apply_chat_template(..., tokenize=False, add_generation_prompt=True)` 返回什么，这一步有没有调用 LLM 或修改模型参数。
- 状态：第一次作答已批改，正在定向补齐遗漏项。
- 用户原始作答：

  > 1.先把messages通过tokenize转化成token id,然后模型读取的是这些token id
  >
  > 2.token_ids_y = tokenizer_y(rendered_x).input_ids使用的是模型X的模板
  >
  > 3.因为不同模型间使用的模板不一样，特殊token会不一样，模型解析的时候会不知道到哪里停止。每个模型的分词也会不同,X模型中的token id到Y模型中对应的结果会不一样
  >
  > 4.可能破坏user,assistant,system，因为使用的模板不同，模型不知道停止边界在哪，可能解析时把用户的上下文解析到了system或者assistant中，生成的回答也不是目标
  >
  > 5.把rendered_x换成rendered_y
  >
  > 6.返回的是对应模型的模板，没有真正调用LLM或者修改参数模型

- 批改：第 2、3、4 问核心正确：识别出错误复用了由 X 的 Chat Template 生成的 Prompt；能说明模型专属特殊标记 / 消息边界不匹配，以及不同 tokenizer / vocabulary 下 Token ID 语义不能跨模型复用；也能给出角色混淆与停止边界错误的具体后果。第 1 问跳过了 Chat Template 生成 Prompt 的中间层；第 5 问只改变量名不足以证明使用 Y 的配套模板从原始 messages 重建；第 6 问把“模板”与“模板渲染后的返回值”混淆，但“不调用 LLM、不修改参数”正确。
- 提示 / 订正路径：只补完整链路、从原始 messages 出发的两步修复，以及 `tokenize=False` 返回值类型和 `add_generation_prompt=True` 的作用；第 2–4 问不重答。
- 单题结果：`PARTIAL`。

#### `A4-Gate-R5-R1`｜转换层与最小修复定向补齐

- 题目：只补三项：① 写出不跳层的 `messages → … → … → Token IDs → 模型 Y` 完整链；② 用两行伪代码说明如何从原始 `messages` 为 Y 重新生成输入；③ 说明 `apply_chat_template(..., tokenize=False, add_generation_prompt=True)` 的 Python 返回类型 / 内容，以及 `add_generation_prompt=True` 增加什么位置。无需重答模板不匹配和跨词表 Token ID 的危害。
- 用户原始作答：

  > 1.
  > ```
  > 原始 messages
  > → 添加到模型X的模板prompt中
  > → 模板X的tokenizer进行分词
  > → Token IDs
  > → 模型 Y
  > ```
  > 2.
  > ```graphql
  > rendered_y = tokenizer_y.apply_chat_template(
  >     messages,
  >     tokenize=False,
  >     add_generation_prompt=True,
  > )
  > token_ids_y = tokenizer_y(rendered_y).input_ids
  > ```
  > 3.返回的是str类型，返回的是包含用户角色等消息。add_generation_prompt=True增加了模型开始生成的标记

- 批改：第 2 项代码正确地从原始 `messages` 使用 Y 配套的 `apply_chat_template` 与 tokenizer；第 3 项正确说明返回 `str`、内容含角色消息，并加入模型开始生成的位置。第 1 项却仍写为“X 的模板 / tokenizer → 模型 Y”，与第 2 项代码矛盾；不能把它静默视为笔误，需要用户明确最终链路中的模型归属。
- 单题结果：`PARTIAL`（修复代码与返回值已通过，只差文字链路中的 X / Y 一致性）。

#### `A4-Gate-R5-R2`｜模型归属一致性确认

- 题目：只重写第 1 条完整链路；目标模型是 Y，明确 Chat Template、Prompt 字符串和 tokenizer 分别属于谁。无需重写代码或第 3 项。
- 用户澄清问题：

  > 开始模型Y使用的不是模型X的模板吗

- 订正说明：不是。题面中的 `rendered_x` 是适配器切换模型后错误复用的旧中间结果，正是需要诊断和移除的故障。结构化原始 `messages` 可以作为跨模型的上层表示；渲染后的 Prompt 和 Token IDs 都是模型配套表示。目标改为 Y 后，应由 Y 配套的 Chat Template 把原始 messages 渲染成 Y 的 Prompt，再由 Y 的 tokenizer 转成 Y 的 Token IDs。`tokenizer_y(rendered_x)` 只会把带有 X 标记的字符串当作普通输入重新分词，不会自动把 X 的消息协议翻译成 Y 的协议。
- 用户第二次澄清：

  > 你的意思是我写的这个链路应该是模型Y的正确调用链路，是吗？

- 二次校准：用户第 2 项的两行伪代码确实是 Y 的正确调用方向；仍需修正的是第 1 项文字链把 Chat Template 与 tokenizer 错写为 X。当前只要求用自己的话统一这两种表达。
- 当前状态：最终链路已统一。
- 用户原始作答：

  > ```css
  > 原始 messages
  > → 模型Y的 Chat Template
  > → 模型Y的propmt
  > → 模型Y的 tokenizer
  > → 模型Y的 Token IDs
  > → 模型 Y
  > ```

- 批改：链路正确；`propmt` 仅是 `prompt` 的拼写问题，不影响概念判定。原始 `messages` 经 Y 配套的 Chat Template 形成 Y 格式 Prompt，再经 Y tokenizer 形成 Y 的 Token IDs，最后交给 Y。
- 单题结果：`PARTIAL → PASS`（经过定向提示与两次模型归属澄清；不是无提示一次通过）。

#### `A4-Gate-R5`｜最终结果

- 最终判定：`PASS`。
- 已覆盖：`messages → Chat Template → Prompt → Token IDs`；Chat Template / tokenizer 的模型配套关系；跨模型不能复用渲染 Prompt 或 Token IDs；角色、消息结束和 assistant 生成起点风险；`tokenize=False` 的字符串返回值与无模型调用 / 无参数更新边界。
- 提示使用：首次已独立诊断主要故障和危害；完整链路、Y 的模型归属及返回值精度经定向提示后闭合。

### S1 收尾

- 练习层结果：`CD-001` 为 `PARTIAL → PASS`；`CD-002` 为提示后的 `PARTIAL → PASS`。
- 本块没有抽取 2026-08-08 刚暴露的 `WP-02`、`WP-04`、`WP-15`、`WP-24`，避免短时重复。
- 2026-08-10 正式台账复核：`CD-001` 的遗漏项经未泄题的定向追问独立补齐，升至 `+1月`、下次回炉 2026-09-09；`CD-002` 的练习层订正虽然完成，但目标模型 Y 的完整归属链是在助手给出完整校准后复述，不构成独立间隔成功，故移出核心定义池并合并为活跃 `WP-32`。
- 本次 pending 实例已有真实作答并完成；重算后仍有 `WP-02`、`WP-04`、`WP-15`、`WP-24` 共 4 个到期项，最老为 2026-07-15，因此重新签发 `RECOVERY_BLOCK_PENDING`。

## Session S2｜A4-Gate 真实模型 / 工具正常闭环

### 接力依据

本 Session 继续同一跨日任务，并承认以下全部前序证据：

- `daily/2026-08-05.md`：A4-Gate 启动、官方资料定位、Agent 循环 / 停止 / 恢复的教学证据。
- `daily/2026-08-06.md`：Problem Contract 冻结，包含状态、上限、日志、恢复与 HITL 设计。
- `daily/2026-08-07.md`：C1 输入合同实现与验证闭合。
- `daily/2026-08-08.md`：C2a `read_material`、C2b `search_web` 实现与验证闭合；下次起点固定为真实模型工具闭环。

### 今日目标与边界

- 今日实现范围：把已经通过的 `read_material` 与 `search_web` 注册给真实 DeepSeek，跑通“模型选择工具 → 客户端严格校验并执行 → JSON Observation 携带 `tool_call_id` 回填 → 模型生成候选摘要”的正常主链。
- 至少真实运行两条：一条 `topic` 走 `search_web`，一条 `relative_path` 走 `read_material`。
- 今天不把以下总任务剩余冒充完成：Reflection / 来源真实性、完整七字段结构化日志、重试与恢复、HITL、14 条 eval、A4-Gate 正式核验。
- 当前代码产物：`code/stage4/a4_gate_research_summary_agent.py`。

### 定向资料范围

- 冻结合同：`code/stage4/problem-contract.md` 第 132–178 行，重点是正常链、`max_steps=6`、`tool_timeout=5`、`max_tool_retries=1`、七字段日志和失败收口。
- DeepSeek Tool Calls（非思考模式，2026-08-09 核对）：https://api-docs.deepseek.com/guides/tool_calls/#non-thinking-mode
- DeepSeek Chat Completions 字段（2026-08-09 核对）：https://api-docs.deepseek.com/api/create-chat-completion/#request
- DeepSeek Thinking Mode 工具调用（2026-08-09 核对）：https://api-docs.deepseek.com/guides/thinking_mode/#tool-calls
- 已 PASS 复用骨架：`code/stage3/t3_gate_tool_assistant.py`；只复用 schema / 客户端 / 白名单分发 / tool message 回填的形状，不照搬字符串 Observation、轮次计数和宽泛异常行为。

### 用户 / 助手分工

- 用户必须亲自完成：本次新增长链路中的关键代码与解释，包括工具 schema、严格参数校验 / 白名单分发、消息回填和真实运行结果判断。
- 助手自动完成：核对官方字段与既有合同、创建当前最小 TODO 骨架、运行聚焦检查、记录原始代码 / 订正轨迹，并在已证明理解后做机械整理。
- 用户最终审核：正常链是否符合自己的理解，以及候选摘要是否确实依据工具 Observation；尚未实现的 Gate 总剩余继续明确保留。

### 今日编码检查点

- S1 已完成，当前开始 `A4-Gate-C3a｜两工具注册合同`；只建立本检查点所需 TODO 骨架，一次只激活一个真实新判断，不提前填入可运行答案。

### `A4-Gate-C3a`｜两工具注册合同

- 当前在做什么：把已 PASS 的两个 Python 工具登记为同一组“客户端可执行白名单 + 模型可见描述”；这一步只建立注册事实，不调用模型或工具。
- 为什么需要：下一检查点中模型只会给出工具名和 JSON 参数，客户端必须用注册表找到真实函数；模型则需要 Schema 才知道有哪些工具和参数合同。二者名称不一致会造成模型能请求但客户端不能安全执行，或客户端暴露了模型不应看到的函数。
- 与前后步骤的关系：C1 已规范化请求，C2 已实现真实工具；C3a 完成注册，C3b 才接模型动作与规范化请求的交叉校验，C3c 再进入真实 DeepSeek 两轮消息闭环。
- TODO 骨架：`code/stage4/a4_gate_research_summary_agent.py` 文件末尾的 `TOOL_REGISTRY` 与 `build_tool_schemas()`。
- 骨架基线：`py_compile` 通过；模块导入输出 `IMPORT_OK [] True`，确认当前空注册表且导入无网络 / 模型调用副作用。

用户需要亲自完成：

1. 在 `TOOL_REGISTRY` 中只登记现有 `read_material`、`search_web` 两个函数；键与真实函数名一致，值是函数对象，不要调用函数。
2. 让 `build_tool_schemas()` 返回恰好两个 `type="function"` 的工具 Schema；函数名称与注册表两个键完全一致。
3. `read_material` 只声明 `relative_path: string`，`search_web` 只声明 `query: string`；各自只有这个唯一必填字段，object 参数拒绝额外字段。
4. 描述写清只读路由边界：前者读取已规范化的沙箱相对路径；后者搜索由已规范化 topic 得到的查询，且后端仅为中文 Wikipedia。
5. 不新增写文件、删除、命令执行工具；构造 Schema 时不得执行 `read_material` 或 `search_web`。

可观察完成标准：

- 编译和无副作用导入通过。
- Schema 列表长度为 2；Schema 名称集合、注册表键集合都恰好为 `{read_material, search_web}`，并且相等。
- 两个参数合同分别只有 `relative_path` / `query`；类型为 string、唯一 required、`additionalProperties` 为 false。
- 注册表值分别是当前真实函数对象；构建 Schema 时两个函数调用次数均为 0。
- C1、C2a、C2b 聚焦行为无回归。

- 当前状态：TODO 骨架已创建，等待用户实现；本检查点尚未判定。

#### `A4-Gate-C3a`｜用户第一次实现

用户完成的核心代码：

```python
TOOL_REGISTRY: dict[str, object] = {
    "read_material": read_material,
    "search_web": search_web,
}

def build_tool_schemas() -> list[dict[str, object]]:
    return [
        {
            "name": "read_material",
            "description": "读取沙箱内资料",
            "parameters": {
                "type": "object",
                "properties": {"relative_path": {"type": "string"}},
                "required": ["relative_path"],
                "additionalProperties": False,
            },
        },
        {
            "name": "search_web",
            "description": "搜索中文 Wikipedia",
            "parameters": {
                "type": "object",
                "properties": {"query": {"type": "string"}},
                "required": ["query"],
                "additionalProperties": False,
            },
        },
    ]
```

首次验证：

- `py_compile`：PASS。
- 注册表：两个键精确、值分别为真实函数对象，PASS。
- Schema 列表长度：2，PASS。
- 两个内层函数名、object 参数、唯一 string 属性、唯一 required、`additionalProperties=False`：全部 PASS。
- 聚焦检查：`17/21 PASS`。
- 四个失败共享同一根因：每个条目都直接返回了函数定义内层，缺少工具协议要求的外层 `type="function"` 和 `function={...}`；当前对象还不能直接作为 `tools=` 发送给模型。
- 当前教学检查点：`PARTIAL`。已通过的注册表和内层参数合同不重写。

#### `A4-Gate-C3a-R1`｜补齐 function tool 外层封装

- 当前唯一订正：为现有两个内层函数定义分别增加 function tool 外层，使每个列表元素都具有 `type="function"`，并让原有 `name / description / parameters` 整体位于对应的 `function` 对象中。
- 保持不变：注册表、两个名称、两个参数、required 与 `additionalProperties`。
- 完成后只回归四个失败以及 17 个既有 PASS；描述的路由精度在外层合同闭合后单独核对。
- 用户订正：已为两个条目添加 `"type": "function"` 与 `"function"`，并将原内层放入 `function`；但实际写成了 `"function": { { ... } }`。
- `py_compile`：PASS。该结果只证明语法可解析，不证明函数可运行。
- 直接调用 `build_tool_schemas()`：失败，`TypeError: unhashable type: 'dict'`，定位到第 328 行 `"function": {`。
- 根因：Python 中 `{键: 值}` 是字典，`{单个值}` 是集合；`{ {字典} }` 被解析成“集合包含字典”，而可变字典不可哈希，运行时无法放进集合。
- 当前状态：`RETRY`。外层方向正确，只修多余的一层集合花括号，不改已通过合同。

#### `A4-Gate-C3a-R1-R1`｜移除误建的 set 层

- 当前唯一订正：让每个 `"function"` 的值直接等于原有函数定义字典，即 `"function": {"name": ..., "description": ..., "parameters": ...}`；不能在函数定义字典外再包一对无键的 `{}`。
- 完成标准：直接调用 `build_tool_schemas()` 不抛异常，并恢复执行 21 项聚焦检查。
- 用户订正：已移除两个 `function` 值外多余的无键花括号；`function` 现在直接指向含 `name / description / parameters` 的字典。
- 直接调用：PASS，不再出现 `unhashable type: 'dict'`。
- 完整结构回归：`21/21 PASS`；原 17 个 PASS 保持，两个外层 `type="function"` 与两个 `function` 字典共 4 个失败全部闭合。
- `py_compile`：PASS。
- 当前结果：R1-R1 `PASS`；C3a 尚未结束，只剩 description 路由精度、零工具调用与 C1/C2 回归。

#### `A4-Gate-C3a-R2`｜收紧模型可见的路由描述

- 当前描述：`read_material = "读取沙箱内资料"`；`search_web = "搜索中文 Wikipedia"`。
- 已通过部分：两者均说明了大致用途，`search_web` 也明确了中文 Wikipedia 后端。
- 当前缺口：描述没有明确参数来自已规范化请求，也没有让模型清楚区分 `relative_path` 路由与 `topic → query` 路由；前者未说“沙箱内相对路径”，后者未说“不代表全网搜索”。
- 当前唯一订正：只改两个 `description` 字符串，用自己的话补齐上述输入来源与只读 / 后端边界；不改注册表、外层协议或 parameters。
- 用户订正：
  - `read_material`：说明只有读取权限、输入是规范化相对路径、落点在 `resources/sandbox/`，并读取对应资料内容。
  - `search_web`：说明是只读工具、`query` 来自规范化 `topic`、后端仅为中文 Wikipedia，且不代表整个公开互联网或通用网页搜索。
- description 语义检查：6/6 PASS。
- Schema 构造零调用 spy：`read_material=0`、`search_web=0`，PASS。
- C1 冒烟：topic / relative_path 规范化与非法 input_type 提前停止，3/3 PASS。
- C2a 冒烟：真实 `sample.txt` 读取与路径逃逸拒绝，2/2 PASS。
- C2b 内存 HTTP 冒烟：单次调用、query 规范化、pageid URL 映射与 HTML / entity 清理，3/3 PASS；未发起真实网络请求。
- C3a 最终聚焦：`35/35 PASS`；`py_compile` PASS，`git diff --check` 在机械清理后 PASS。
- 助手机械整理：在用户完成并通过后，移除 C3a 已完成 TODO、统一缩进与字典容器格式、移除行尾空格，并把模块状态更新为 C3a 已完成；未改变工具 Schema 行为。
- 教学检查点最终判定：`PASS`。A4-Gate 整体继续 `DOING`。

### `A4-Gate-C3b`｜模型 tool_call 严格校验与白名单执行

- 当前在做什么：实现一个纯客户端执行器，接收模型给出的工具名和 JSON 参数，在任何真实工具调用前完成白名单、结构与本轮请求一致性校验。
- 为什么需要：Schema 只是在模型侧描述期望格式，模型仍可能返回非法 JSON、未知工具、缺字段、多字段或偷偷更换查询 / 路径；真正的安全边界必须由客户端代码强制执行。
- 与前后步骤的关系：C3a 已让模型看见两个工具；C3b 负责把模型动作安全映射到真实函数；C3c 才把它接进真实 DeepSeek 的 assistant → tool → assistant 消息闭环。
- 当前骨架：`execute_tool_call(tool_name, raw_arguments, normalized_request)`；已加入本检查点需要的 `json` import，尚无可运行答案。

用户需要亲自完成：

1. `tool_name` 必须是字符串且属于 `TOOL_REGISTRY`；未知或异常类型必须返回稳定失败，且工具调用次数为 0。
2. `raw_arguments` 必须是 JSON 字符串；解析失败或解析结果不是 object 时稳定拒绝，不能使用 `eval()`。
3. 按 Schema 精确校验唯一必填字段、额外字段和字符串类型；所有非法参数都不得执行工具。
4. 增加 A4 新边界：`topic → search_web(query == normalized_request["value"])`；`relative_path → read_material(relative_path == normalized_request["value"])`。模型不得自行替换查询词、文件路径或工具。
5. 只有全部校验通过后才通过注册表调用真实函数并原样返回工具字典；不得使用动态 `eval`，也不得用宽泛 `except Exception` 吞掉意外程序错误。

当前完成标准：先实现并通过内存参数矩阵；这一步不调用 DeepSeek，正常 search 分支使用 spy，文件分支可调用现有沙箱 `sample.txt`。C3b 通过后才建立真实模型循环。

- 当前状态：TODO 骨架已创建，等待用户实现。

#### `A4-Gate-C3b`｜用户第一次实现与聚焦矩阵

用户实现范围：

- 通过 `TOOL_REGISTRY.get(tool_name)` 做白名单查找。
- 使用 `json.loads(raw_arguments)`，拒绝坏 JSON 与非 object。
- 从 `build_tool_schemas()` 读取 required、additionalProperties 和字段类型。
- 全部校验通过后执行 `tool_function(**arguments)`，未使用 `eval` 或宽泛异常捕获。
- 尚未使用 `normalized_request`。

首次内存 spy 矩阵：`8/14 PASS`。

| 场景 | 结果 | 决定性证据 |
| --- | --- | --- |
| 未注册字符串工具名 | PASS | 稳定失败，调用 0 次 |
| 工具名为 list | FAIL | 在 `dict.get()` 泄漏 `TypeError: unhashable type: 'list'` |
| raw_arguments 为 None | FAIL | `json.loads` 泄漏 TypeError |
| raw_arguments 为 bytes | FAIL | `json.loads` 接受 bytes，错误执行 search spy 1 次 |
| 坏 JSON / JSON list | PASS | 稳定失败，调用 0 次 |
| 缺字段 / 多字段 / 错类型 | PASS | 均稳定失败，调用 0 次 |
| topic 偷换成 read_material | FAIL | 错误执行 read spy 1 次 |
| topic 查询值由 AI 改成 ML | FAIL | 错误执行 search spy 1 次 |
| relative_path 改成 other.txt | FAIL | 错误执行 read spy 1 次 |
| 合法 topic / relative_path | PASS | 分别只调用对应 spy 1 次，并返回真实工具字典 |

首次判定：`PARTIAL`。8 个已通过行为保留；6 个失败分为两个订正单元：R1 是进入容器 / JSON 前的类型边界，R2 是 A4 新增的本轮请求交叉校验。

#### `A4-Gate-C3b-R1`｜前置类型边界

- 当前唯一订正：在 `TOOL_REGISTRY.get()` 前确认 `tool_name` 是字符串；在 `json.loads()` 前确认 `raw_arguments` 是字符串。任一不满足都返回稳定 `ok=False / error / retryable=False`，调用工具 0 次。
- 为什么单独处理：list 作为字典键会在白名单判断前抛错；`json.loads()` 原生会接受 bytes，但当前 API 合同只接受模型返回的 JSON 字符串，不能把库函数的宽松输入范围当成产品合同。
- 保持不变：已通过的坏 JSON、JSON object、Schema 字段校验和两个合法调用；`normalized_request` 三个失败留到 R2，不在本次混改。
- 预期回归：完成后从 `8/14` 提升到 `11/14`，剩余 3 个只应是工具 / 参数值与本轮请求不一致。
- 用户订正：在注册表查询前增加 `tool_name` 字符串检查；在 `json.loads` 前增加 `raw_arguments` 字符串与非空检查。非字符串的 list / None / bytes 均返回稳定不可重试失败。
- R1 回归：`11/14 PASS`，与预期完全一致。
  - list 工具名：由 TypeError 改为稳定失败，调用 0 次。
  - None / bytes 参数：均稳定失败，调用 0 次；bytes 不再因 `json.loads` 的宽松输入范围被接受。
  - 原 8 个 PASS 保持；合法 topic / path 仍各调用对应 spy 1 次。
- R1 判定：`PASS`。

#### `A4-Gate-C3b-R2`｜绑定本轮规范化请求

- 当前只剩 3 个失败：topic 错选 `read_material`、topic 把 `AI` 改成 `ML`、relative_path 把 `sample.txt` 改成 `other.txt`；三者都通过了白名单与 Schema，却错误执行工具 1 次。
- 根因：`normalized_request` 参数尚未在函数体中使用；当前代码只能证明动作“像一个合法工具调用”，不能证明它“忠实于本轮用户请求”。
- 当前唯一订正：在实际调用前建立并执行精确路由：
  - `topic → search_web + query`
  - `relative_path → read_material + relative_path`
  - 工具名必须等于期望工具，参数字典必须只有期望参数，参数值必须与 `normalized_request["value"]` 完全相等。
- 禁止行为：不得对模型参数重新 strip / 改写后再放行；不得在不一致时调用工具；不改已通过的类型 / JSON / Schema 校验。
- 完成标准：3 个失败全部变为稳定失败且调用 0 次；两个合法路径仍各调用 1 次，完整矩阵 `14/14 PASS`。
- 用户订正：在最终调用前按工具分支检查 `normalized_request.input_type` 与参数值；`read_material` 只接受 `relative_path` 路由，`search_web` 只接受 `topic` 路由，且参数值必须与规范化 value 精确相等。
- R2 主矩阵：`14/14 PASS`。
  - 错工具、改 topic、改 relative_path 三项均稳定拒绝，工具调用 0 次。
  - 两条合法路径仍分别只调用对应 spy 1 次并原样返回工具结果。
  - R1 与首次 8 个 PASS 全部保持。
- 追加边界：`9/9 PASS`。
  - 空 / 纯空白 JSON 字符串稳定拒绝。
  - query 前导空白与 path 尾随空白不会被执行器偷偷规范化，均因不精确相等而拒绝。
  - 未知 input_type、缺 value、非字符串 value 均不执行工具。
  - 工具返回 `ok=False, retryable=True` 字典时执行器原样传出。
  - 合法动作触发的意外 `RuntimeError` 继续向外传播，没有被宽泛异常捕获吞掉。
- R2 判定：`PASS`。

#### `A4-Gate-C3b`｜最终结果

- 最终判定：`PASS`；A4-Gate 整体继续 `DOING`。
- 决定性结果：主矩阵 `14/14 PASS` + 追加边界 `9/9 PASS`；`py_compile` PASS。
- 覆盖：工具名类型 / 白名单、JSON 字符串与 object、必填 / 多余 / 类型、规范化请求路由和值绑定、非法动作零调用、合法动作单次调用、工具结果透传与意外错误 fail-fast。
- 订正轨迹：首次 `8/14 PARTIAL`；R1 类型边界后 `11/14`；R2 请求绑定后 `14/14`，追加 `9/9`。
- 助手机械整理：移除 C3b 已完成 TODO，统一本次新增代码的缩进、长字典格式与行尾空格，更新模块完成状态；不改变用户已通过的行为。
- 清理后跨层回归：`13/13 PASS`，覆盖 C3a 两工具 / 注册表 / 外层协议、C1 两类规范化、C2a 真实 sample / 路径逃逸，以及 C3b 两条合法动作与未知工具 / 坏 JSON / 错路由 / 改值拒绝；`py_compile` 与 `git diff --check` PASS。

### `A4-Gate-C3c`｜真实 DeepSeek 两轮正常链

- 当前在做什么：让真实 `deepseek-v4-pro` 第一次根据规范化请求选择一个工具，客户端执行 C3b 并回填真实 Observation，再让同一模型第二次只依据 Observation 生成候选摘要。
- 为什么需要：C3a / C3b 只证明工具协议和客户端执行器正确；只有真实模型返回 `tool_calls`、客户端执行、按 ID 回填并再次调用模型，才形成可运行 Agent 的正常主链证据。
- 与最终产物的边界：本检查点只返回 `candidate_summary / tool_name / tool_result` 中间对象；Reflection、来源子集与最终三字段校验尚未实现，所以不得返回最终 `status="success"`。
- 已复用而不重复考察：从 A4-05 机械复用 `.env → DEEPSEEK_API_KEY → OpenAI(base_url)` 客户端创建；从 T3-Gate 承接 function tools、assistant 原消息、tool_call_id 与 JSON 字符串 Observation 的已 PASS 协议。
- 当前骨架：`generate_candidate_summary(normalized_request, client=None)`；`create_deepseek_client()` 已由助手按既有 PASS 代码机械接入，只创建客户端，不发起请求。

用户需要亲自完成：

1. 构造 system / user messages，把 `normalized_request` 作为明确数据交给模型；提示模型必须按 input_type 选择恰好一个已注册工具，不能直接凭常识写摘要或改写 value。
2. 第一次 `chat.completions.create`：固定模型、messages、tools、`stream=False`，并显式 `extra_body={"thinking":{"type":"disabled"}}`；正常链要求得到恰好一个 tool_call。
3. 把第一次完整 assistant message 追加到 messages；取 `id / function.name / function.arguments`，调用 `execute_tool_call(..., normalized_request)`，取得真实工具字典。
4. 追加 `role="tool"` 消息：`content` 必须是 `json.dumps(tool_result, ensure_ascii=False)` 得到的字符串，`tool_call_id` 必须与本次调用精确对应。
5. 第二次模型调用使用更新后的 messages、同一 tools 与 non-thinking 配置；正常链取得非空 `message.content`，并返回固定中间三字段 `candidate_summary / tool_name / tool_result`。

当前完成标准：先用 fake client 验证两次模型调用、消息顺序、精确 tool_call_id、字符串 Observation 和 non-thinking；随后才真跑 topic 与 relative_path。任何 fake 只用于协议测试，不能替代两条真实 DeepSeek 证据。

- 当前状态：TODO 骨架已创建，等待用户实现。

#### `A4-Gate-C3c`｜用户第一次实现与 fake 协议基线

用户第一次实现：

- 能使用注入 client，调用模型两次，并在中间调用 `execute_tool_call`。
- 能保存第一条 assistant message、追加 `role="tool"` 字典，并返回 `candidate_summary / tool_name / tool_result`。
- 但使用了旧版 `functions / function_call` 与字典式 `message.get("function_call")`，同时把 `thinking=False` 作为顶层参数；初始 messages 只有 user，tool message 缺 `tool_call_id`。

fake 协议基线：`8/21 PASS`。

已通过：

- 宽松 fake 下确实发生两次模型调用，两个 model 名正确。
- 第一次 assistant 原消息被保留，存在一条字符串 content 的 tool message。
- C3b 执行真实 search spy 恰好 1 次。
- 最终中间对象三字段及候选摘要正确。

主要失败：

- 忠实 SDK 消息对象不支持 `.get()`，当前在第一轮泄漏 `AttributeError`。
- 两次请求都错误使用 `functions / function_call / thinking=False`，缺 `tools / stream=False / extra_body.thinking.disabled`。
- 初始消息缺 system，user JSON 默认转义中文。
- tool message 缺精确 `tool_call_id`，Observation JSON 默认转义中文。

首次判定：`PARTIAL`。失败拆为：R1 当前 Tool Calls 请求 / 响应对象协议；R2 assistant + tool Observation 回填；R3 第二响应与候选内容验证。已通过的 C3b 执行和中间对象形状不重写。

#### `A4-Gate-C3c-R1`｜迁移到当前 tools / tool_calls 协议

- 当前唯一订正范围：
  1. 初始 messages 补 system + user；user JSON 使用 `ensure_ascii=False`。
  2. 两次请求都使用 `tools=tool_schemas`、`stream=False`、`extra_body={"thinking":{"type":"disabled"}}`；移除 `functions`、`function_call` 和顶层 `thinking`。第一次可省略 `tool_choice`（默认 auto）或显式 auto；第二次如需禁止再次选工具，可用当前 `tool_choice="none"`。
  3. 响应按 SDK 属性访问：第一响应先取得 `message`，再读取 `message.tool_calls`，且必须恰好一个；tool call 的 `id / function.name / function.arguments` 使用属性。第二响应正文也读取 `message.content`，不使用字典 `.get()`。
- 暂不处理：tool message 的 `tool_call_id` 与 ensure_ascii 属于 R2；第二响应是否再次请求工具、正文是否非空的完整验证属于 R3。
- 当前正常链遇到无 / 多 tool_calls 时抛出说明具体协议错误的 RuntimeError；不能返回尚未实现 Reflection 的最终三字段 `status` 对象。
- 用户第一次订正：把 `functions` 字段名改成 `tools`，但将 Schema list 转成按名称索引的 dict 后传入；仍使用顶层 `thinking=False`、缺 `stream=False`、缺 system、中文 JSON 默认转义，且响应 / tool_call 继续使用 `.get()`。
- 忠实 SDK fake：在第一响应 `message.get("tool_calls")` 处泄漏 `AttributeError`，只发生一次模型调用，工具调用 0 次。
- R1 子矩阵：`2/10 PASS`，仅“发生第一次调用”和“模型名正确”通过；其余当前 tools 请求合同、消息和 SDK 对象访问均未闭合。
- 阶段判定：`RETRY`。不继续叠加 R2 / R3；把 R1 再拆成请求封装与响应对象两个更小单元。

#### `A4-Gate-C3c-R1-R1`｜只修第一请求封装

- 当前唯一订正范围：
  1. messages 必须是 system + user；user content 使用 `json.dumps(normalized_request, ensure_ascii=False)`。
  2. 删除无用途的 `tool_schemas_dict`；把 `build_tool_schemas()` 返回的 list 原样传给 `tools`。
  3. 第一次 create 只使用 `model / messages / tools / stream=False / extra_body={"thinking":{"type":"disabled"}}`，可选 `tool_choice="auto"`；删除顶层 `thinking`。
- 暂时允许：第一响应后面的 `.get()` 仍会失败；本次只检查 fake 记录的请求 kwargs，不要求函数跑到工具执行。
- 完成标准：第一请求 6 项——model、tools list、stream、extra_body、system/user、中文 JSON——全部 PASS；随后才进入 R1-R2 的 SDK 属性访问。
- 状态：等待用户订正。

## 当前范围与交接

- 今日计划范围：先完成 S1 两项到期回收，再完成 S2 的真实 DeepSeek 正常工具闭环与两条真实调用。
- 今日完成：S1 两项回收题完成练习层收口；S2 的 C3a、C3b 已通过，C3c 完成第一请求封装后跨日续接。
- 今日剩余（历史收尾口径）：C3c 的 SDK Tool Call 属性、assistant / Tool Observation 回填、第二响应边界和两条真实调用顺延至下一 Session，不在 8 月 9 日伪记完成。
- A4-Gate 任务总剩余（截至 8 月 9 日）：闭合 C3c 正常链，再完成 Reflection 与来源真实性、七字段日志、重试 / 停止、HITL、14 条 eval、正式 Gate 复核。
- 下次 Session 起点（不属于 8 月 9 日）：从 `A4-Gate-C3c-R1-R1` 继续，先完成第一请求封装回归，再处理 SDK 属性对象与 Tool Observation 协议。
