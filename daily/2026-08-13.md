# 2026-08-13 学习记录

- 当前任务：`A4-Gate` 研究总结 Agent
- 任务状态：`DOING`（沿用 `tracker/progress.md`；本页只记录跨日续接的教学代码检查点，不产生父任务正式判定）
- 今日计划学习预算：用户已明确启用休息日双块；第一块为 `C4a-1` 的跨日完善与收口，按单块不超过 180 分钟管理；第二块为新启动的 `A4-Gate-C4a-2a`，计划 180 分钟；全天计划不超过 360 分钟
- 日期与预算归属校准：`C4a-1i` 于 2026-08-12 已启动，2026-08-13 只继续订正、复测与收口。此前“第一块 165 分钟 + 第二块 120 分钟 = 今日 285 分钟”以及“`C4a-1i～1k` 是今日完整第二块”的汇总不再作为当天预算事实；下文既有提交、订正和验证证据全部保留
- 今日新块范围：`C4a-2a` 只处理“首次可重试工具失败 → 使用完全相同的工具名和原始参数重试一次 → 第二次成功”的六步恢复轨迹；失败状态映射、HITL 和 eval 不在本块
- 实际学习时长：不记录；上述分钟只表示计划容量，不能根据提交次数、验证耗时或助手处理速度倒推实际学习时长

## 本页 Session / 任务索引（含跨日证据）

| Session | 任务 | 前序 | 范围 | 预算 | 状态 |
| --- | --- | --- | --- | ---: | --- |
| S1 | `A4-Gate-C4a-1f-R1` | [2026-08-12](2026-08-12.md) S2 | 删除 `finally` 后，闭合工具正常返回的计时、三路日志与原对象返回 | 45 分钟 | 教学 `PASS`；最终主审 `30/30`、独立 `75/75`，已打开 R2 |
| S2 | `A4-Gate-C4a-1f-R2` | 本页 S1 | 工具 Python exception 的安全失败日志与原异常传播 | 30 分钟 | 教学 `PASS`；R2-R1-R1 主审 `113/113`、独立 `136/136`，C4a-1f 已收口 |
| S3 | `A4-Gate-C4a-1g` | 本页 S2 | 第二次模型调用的共享占步、计时与成功 / 异常日志 | 45 分钟 | 教学 `PASS`；主审 `143/143`，两份独立审查均通过 |
| S4 | `A4-Gate-C4a-1h` | 本页 S3 | Reflection 模型调用的共享 context 传播与成功 / 异常日志 | 45 分钟 | 教学 `PASS`；独立完整矩阵 `158/158`，收口回归 `42/42` |
| S5 | `A4-Gate-C4a-1i` | 跨日接续本页 S4 | 2026-08-12 已启动 Refinement 日志接线；2026-08-13 完成 R1 / R2 订正、复测与收口 | 跨日原计划 45 分钟；不作实际时长 | 教学 `PASS`；`RETRY → R1 PARTIAL → R2 PASS`，最终 `35/35` |
| S6 | `A4-Gate-C4a-1j` | 本页 S5 | 首次模型响应严格要求 `finish_reason == "tool_calls"` | 30 分钟 | 教学 `PASS`；`RETRY → R1 RETRY → R2 PASS`，独立完整矩阵 `102/102` |
| S7 | `A4-Gate-C4a-1k` | 本页 S6 | topic/path 无重试五步轨迹、请求隔离与第 7 次调用硬门 | 45 分钟 | 教学 `PASS`；主审 115 条断言、独立矩阵 `46/46`，C4a-1 教学范围收口 |
| S8 | `A4-Gate-C4a-2a` | 本页 S7 | 首次可重试工具失败后，以完全相同的工具名和原始参数只重试一次，并在第二次成功后完成六步恢复轨迹 | 180 分钟 | `DOING`；用户已发送“开始动手”，当前唯一活动检查点 |

> 预算口径：S1～S7 的分钟保留各检查点当时的计划记录，不等于 2026-08-13 的实际学习时长，也不再机械相加为当天 285 分钟；本次新启动的 S8 是向前安排的 180 分钟第二块。

## 前序证据链与本次接力点

- [2026-08-05](2026-08-05.md)：A4-Gate 启动、官方资料定向带读与初始合同决策。
- [2026-08-06](2026-08-06.md)：Problem Contract 第 1～11 节设计证据闭合，合同随未完成 Gate 保持 `DRAFT`。
- [2026-08-07](2026-08-07.md)：C1 固定三字段结果与输入边界正式 `PASS`。
- [2026-08-08](2026-08-08.md)：C2 `read_material` / `search_web` 工具适配正式 `PASS`。
- [2026-08-09](2026-08-09.md)：C3a 工具 Schema、C3b 白名单调度正式 `PASS`，C3c 跨日。
- [2026-08-10](2026-08-10.md)：C3c 正式 `PASS`；C3d allowlist、两个 prompt builder 与最终 validator 纯函数切片正式 `PASS`。
- [2026-08-11](2026-08-11.md)：C3d Reflection / Refinement / JSON / validator 编排和 topic/path 真实正常链正式 `PASS`。
- [2026-08-12](2026-08-12.md)：C4a-1a～1e 教学检查点 `PASS`；C4a-1f-G1 `PARTIAL → PASS`；工具接线首次提交与 R1 均为 `RETRY`，最后冻结 R1-R1 的精确控制流订正。
- 本次精确起点：复核用户对 `finally return` 的订正，并继续验证 R1 的真正目标——工具正常返回后只记一条七字段工具事件，同时把同一个工具结果对象原样交给上层。

## 本次范围与边界

- 只覆盖已经通过全部客户端校验后的一次真实工具尝试：`ok=True`、`ok=False/retryable=False`、`ok=False/retryable=True` 三类**正常返回**。
- 本轮要求：工具恰好执行一次；共享 step 从 1 到 2；读取两次时钟并得到非负整数毫秒；按结果类别追加一条 `tool_call`；原始结果对象及其中的 `content/results/error/retryable` 等字段不得丢失；不重试。
- Python exception 本轮只要求裸抛同一异常对象且暂不写失败日志；完整异常耗时与失败事件留给 `C4a-1f-R2`。
- 不修改执行前 validation、首次模型日志、第二模型 / Reflection / Refinement 调用点，不实现工具 retry、最终失败状态映射、HITL、eval 或父 Gate 判定。
- 回炉 preflight：`weak-points.md` 当前持久状态为 `NORMAL`，2026-08-12 快照到期项为 0；`CD-005 / WP-08` 今天进入到期日，但单日新增到期不自动签发回收块。本次是用户直接续接的代码小切片，不追加旧复习题。
- 算法 preflight：W4 已 `PASS`、结转债务 0、待复测 0；本次没有另排算法时段。

## Session S1｜`A4-Gate-C4a-1f-R1-R1`

### 用户提交与当前实现

- 用户原始提交：`改完了 C4a-1f-R1-R1 移除 finally 并追加正常日志`。
- 已实际确认的改动：整个 `finally` 已删除，合同外的 `run_context["tool_call_duration"]` 也已删除；客户端 validation、共享占步、起始计时和一次真实工具调用仍保留。
- 与提交描述不一致的当前行为：代码没有调用 `append_call_log()`，没有第二次读取 `perf_counter()`，也没有 `try/except/else` 的正常返回分支；`try` 内仍执行结果分类并提前返回新字典，`except` 仍吞掉原异常并返回新字典。

### 决定性执行证据

- 主审无网络矩阵：`49/90 PASS`。
  - validation `12/12 PASS`；`MAX_STEPS` hard gate `3/3 PASS`。
  - 正常返回 `24/54`；负向时钟 `1/5`；Python exception 的 R1 传播要求 `4/5`；append sentinel `2/4`；静态结构 `3/7`。
- 独立只读复核：`49/82 PASS`。
  - validation `20/20 PASS`；hard gate `3/3 PASS`。
  - normal `16/36`；negative-clock `1/5`；Python-exception `1/3`；append-sentinel `0/3`；generate integration `4/6`；scope/structure `4/6`。
- `py_compile` 退出 0；`git diff --check` 退出 0。两套矩阵粒度不同，但对根因、保留部分和下一唯一修复结论一致。

### 判定与根因

- 教学检查点状态：`RETRY（继续保留正确前缀，不从头重写）`。
- 已通过并保留：执行前拒绝仍为零占步 / 零时钟 / 零工具 / 零日志且 context 不变；step 已耗尽时仍在时钟和工具之前阻断；合法工具只执行一次且没有 retry；旧 `finally` 和顶层 context 污染均已消失；没有越界接其他调用点。
- R1 核心产物仍为 0：所有正常返回都只有一次时钟读取、零条工具日志，并返回重新包装的字典而非原结果对象。
- 当前 `ok=True` 会丢失 `results/content/truncated` 等真实证据；两个 `ok=False` 分支会丢失真实 `error/retryable` 和对象 identity。
- `if result.get("ok") is not True` 会把不可重试失败也标成 `retryable_tool_failure`；随后的 `else → tool_failure` 实际不可达。
- `except` 返回新字典会吞掉工具抛出的原异常；因为正常分支没有调用日志 helper，append sentinel 也根本没有机会触发。集成轨迹因此只有模型 `step=1` 事件，没有工具 `step=2` 事件。

### 当前唯一订正

继续使用同一个编号 `C4a-1f-R1-R1`，不要打开 R2：

1. `try` 中只保留 `result = tool_function(**arguments)`，移除其中全部 `.get()` 分类和 `return`。
2. `except Exception` 本轮只使用无参数裸 `raise`，不返回新字典，也暂不追加异常日志。
3. 新增与 `except` 互斥的 `else`；只在真实工具正常返回后读取第二次时钟，并换算 `max(0, int((end - start) * 1000))`。
4. 三路判断只决定**日志的** `error`：`ok is True → None`；否则 `retryable is True → retryable_tool_failure`；其他 → `tool_failure`。分类分支本身不要 return。
5. 用同一本 `run_context` 调用既有 `append_call_log()`，传入当前 step、`event_type="tool_call"`、`model=None`、已验证的 `tool_name`、计算后的 `duration_ms` 和分类后的 `error`。
6. append 完成后只写一次 `return result`，原对象及其全部字段必须保留；仍然零 retry。

### R1-R1-R1｜正常返回日志与原对象返回复测

- 用户原始提交：`改完了 C4a-1f-R1-R1-R1 正常返回日志与原对象返回`。
- 当前实现已闭合的功能：工具调用 `try` 只含 `result = tool_function(**arguments)`；`except Exception` 使用裸 `raise`；正常返回读取结束时钟并换算非负整数毫秒；`ok=True / retryable=True / 其他失败` 分别得到 `None / retryable_tool_failure / tool_failure`；完整调用 `append_call_log()` 后原样 `return result`。
- 主审无网络功能矩阵：validation `12/12`、三类正常返回 `54/54`、浮点截断 / 负时钟 `8/8`、Python exception 的 R1 传播边界 `5/5`、MAX hard gate `3/3`、append sentinel `4/4`，合计动态 `86/86 PASS`；纠正测试脚本最初误选 JSON 解析 try 的定位错误后，目标工具 try 静态边界 `7/7 PASS`。
- 主审完整假客户端集成：`10/10 PASS`。最终业务结果仍恰好为 `status / summary / sources`；模型调用 4 次、真实 fake 工具 1 次；同一 context 只有模型 `step=1` 与工具 `step=2` 两条日志，request ID 相同，工具日志七字段和值精确。
- 独立只读矩阵：动态行为 `179/179 PASS`，总计 `187/190`；validation `36/36`、search/read × 三类正常返回 `84/84`、负时钟 `12/12`、MAX hard gate `10/10`、Python exception R1 边界 `12/12`、append sentinel `12/12`、generate 集成 `13/13`；`py_compile` 与 `git diff --check` 均退出 0。
- 唯一剩余 3 条静态断言：当前结束计时位于 `else` 内，但三路分类、`append_call_log()` 和 `return result` 与整个 `try / except / else` 同级。由于 `except` 必定裸抛，当前动态语义恰好等价并全部通过；但本检查点已明确要求把正常返回的完整后处理放入 `else`，使“只有工具正常返回才分类、记正常返回日志并返回”由控制流结构直接表达，而不依赖异常分支永远保持裸抛。
- 教学检查点状态：`PARTIAL（功能已通过，只剩无逻辑变化的缩进收口）`。这不是功能 RETRY，也不要求重写任何表达式；R1 在该纯缩进复测前暂不记最终 PASS、不打开 R2。
- 当前唯一订正 `C4a-1f-R1-R1-R1-R1`：把当前从 `if result.get("ok") is True:` 到最后 `return result` 的整段统一缩进一级，使三路分类、append 和原对象返回都属于 `else`；不改任何条件、字段、值或异常分支，不加入 exception 日志或 retry。

### R1-R1-R1-R1｜正常路径缩进收口

- 用户原始提交：`改完了 C4a-1f-R1-R1-R1-R1 正常路径缩进收口`。
- 主审最终快检：`30/30 PASS`。覆盖三类正常返回的原对象 identity、工具 1 次 / 零 retry、step 1→2、两次时钟、七字段和值、负时钟整数 0、Python exception 的 R1 裸抛、append sentinel，以及目标工具 `try` 的分类 / append / return 全部位于 `else`。
- 独立最终快检：`75/75 PASS`。validation `6/6`、search/read × 三类正常返回 `42/42`、负时钟 `4/4`、Python exception R1 边界 `3/3`、append sentinel `4/4`、MAX hard gate `2/2`、generate 同 context 的模型 step 1 / 工具 step 2 `5/5`、静态结构 `9/9`；`py_compile` 与 `git diff --check` 均退出 0。
- 教学检查点最终状态：`C4a-1f-R1 = PASS`。这是一次真实工具的正常返回 / 返回失败日志切片，不等于 Python exception 日志、整个 C4a-1 或父级 `A4-Gate` PASS。
- 助手机械收口：把该块 2 空格合法缩进统一为项目常用 4 空格；删除已完成的 R1 TODO，并将当前唯一 TODO 切换到 R2 的 `except`。未修改任何已经通过的表达式或运行逻辑。

## Session S2｜`A4-Gate-C4a-1f-R2`

### 学习定位

- 现在在做什么：补齐真实工具函数**抛出 Python 异常**时的一条失败调用日志；例如工具内部抛出 `TimeoutError`，这与工具正常返回 `{"ok": false, ...}` 是两条不同控制流。
- 为什么需要：R1 已证明正常返回无论成功或返回失败都能记账，但当前 Python exception 路径只占了 step 后直接裸抛，账本看得到 step 增长却看不到失败事件。R2 要让实际发生过的异常工具调用也可追踪，同时保持原异常给上层处理。
- 与前后关系：复用已通过的 R1 占步、起点时钟、七字段 helper 和第一次模型异常日志模式；本轮完成后才结束 C4a-1f。工具 retry 与最终状态映射仍留给 C4a-2，不在这里实现。

### 当前唯一实现任务

只修改 `execute_tool_call()` 当前 `except Exception` 分支：

1. 绑定捕获到的异常对象，供安全类型名使用。
2. 在分支内读取第二次 `perf_counter()`，按已通过的同一公式得到非负整数 `duration_ms`；耗时只覆盖真实工具函数，不把日志追加时间算进去。
3. 使用同一本 `run_context` 和已经占用的 `step` 追加恰好一条七字段 `tool_call` 失败事件；`model=None`、`tool_name` 使用已通过白名单校验的当前名称。
4. 日志 `error` 只能使用异常类型名，不记录异常消息、traceback、query、路径或 secret。
5. append 完成后保留无参数裸 `raise`，让上层捕获的仍是同一个异常对象；step 不回滚。

### 可观察完成标准

- fake 工具抛预存 `TimeoutError("secret=...")`：工具 1 次、时钟 2 次、step 从 1 到 2、失败日志恰好 1 条；`error == "TimeoutError"` 且整本日志不含 secret；上层捕获对象与预存异常 `is` 相同。
- 正向、浮点误差与负向时钟分别得到精确的非负 `int` 毫秒，负向为 0。
- 执行前 validation 失败仍为零占步 / 零时钟 / 零工具 / 零日志；初始 step 6 仍在工具和时钟前阻断且 context 不变。
- R1 的三类正常返回继续各工具 1 次、日志 1 条、原对象返回；异常路径不进入第二次模型或任何下游调用。
- 本轮不加入 retry、不返回包装错误字典、不记录 `str(exc)`、不捕获 `BaseException`，也不修改其他调用点。

### 用户提交与复测

- 用户原始提交：`改完了 C4a-1f-R2 工具 Python 异常日志`。
- 当前实现做对：`try` 仍只包一次真实工具调用；异常分支读取第二次时钟并传入完整工具事件字段；append 后保留无参数裸 `raise`；step 不回滚；R1 正常返回分支、validation 与 MAX hard gate 均未改；没有加入 retry 或越界接其他调用点。
- 主审无网络矩阵：`64/73 PASS`。R2 exception `17/24`、validation `9/9`、MAX hard gate `3/3`、R1 三类正常返回回归 `24/24`、generate 异常下游阻断 `6/7`、静态边界 `5/6`；`py_compile` 与 `git diff --check` 均退出 0。
- 独立只读矩阵：`113/137 PASS`。R2 exception `52/72`、validation `12/12`、MAX hard gate `6/6`、R1 search/read × 三类正常返回 `30/30`、generate exception `6/7`、static `7/10`；`py_compile` 与 `git diff --check` 均退出 0。
- 已通过证据：每个合法异常工具均实际执行 1 次、零 retry、读取时钟 2 次、step 1→2；正向与浮点时钟会写一条七字段日志并裸抛同一 sentinel，异常消息中的 secret 没有入账；validation 与 step 6 都保持零副作用；异常后模型只调用第一次，不进入第二模型；R1 完全无回归。
- 唯一同源缺口：① `except Exception` 没有绑定异常对象，日志 `error` 固定为 `tool_exception`，无法区分 `TimeoutError / ConnectionError / ValueError`；②耗时只执行 `int(diff * 1000)`，没有 `max(0, ...)`，负向时钟得到负数后被可信 helper 以 `ValueError` 拒绝，导致日志 0 条并遮蔽原工具异常。
- 教学检查点状态：`PARTIAL`。主体结构已经成立，不是 RETRY；当前不打开第二模型调用点。
- 当前唯一订正 `C4a-1f-R2-R1`：只修改现有 `except`。绑定异常对象；把耗时换算改成与 R1 / 首次模型相同的非负整数公式；日志 error 使用异常类型名；保留完整其余字段与末尾无参数裸 `raise`。不要修改 try、R1、validation、retry 或其他调用点。

### R2-R1｜异常类型名与负耗时钳制

- 用户原始提交：`改完了 C4a-1f-R2-R1 异常类型名与负耗时钳制`。
- 已正确修改：`except Exception as exc` 已绑定当前异常；日志 `error=type(exc).__name__` 已改为安全类型名；工具调用 try、末尾裸 `raise`、R1 正常分支、零 retry 和调用点范围在源码意图上未变。
- 当前最早失败层：`.venv/Scripts/python.exe -m py_compile code/stage4/a4_gate_research_summary_agent.py` 退出 1，在第 600 行报告 `SyntaxError: invalid syntax. Perhaps you forgot a comma?`。模块无法导入，因此当前保存状态的 R2 异常矩阵、R1 / validation / MAX 回归、generate 停止边界和 AST 解析均不能执行；不能沿用上一保存版本的绿灯冒充当前证据。`git diff --check` 退出 0。
- 两个阻断均集中在异常耗时表达式：① `duration_ms=max(...)` 作为 `append_call_log()` 参数后缺少与下一个 `error=` 参数分隔的逗号；②异常分支引用 `perf_counter_end`，但该变量只在后面的正常返回 `else` 中赋值，即使先补逗号也会在工具异常时产生 `UnboundLocalError`，遮蔽原工具异常。
- 教学检查点状态：`RETRY（保留已正确的异常绑定与安全类型名，不从头重写）`。当前文件不可执行，所以不能按功能小缺口继续记 PARTIAL，也不打开 C4a-1g。
- 当前唯一订正 `C4a-1f-R2-R1-R1`：只在现有 `except` 内先读取该异常路径自己的结束时钟，再单独计算非负整数 `duration_ms`；append 只传 `duration_ms=duration_ms`，确保它与 `error` keyword 用逗号分隔；保留 `error=type(exc).__name__` 和末尾无参数裸 `raise`。不改 try、R1、validation、retry 或其他调用点。
- 订正后验证顺序：先要求 `py_compile` 退出 0；再验证正向 / 负向异常各 step 2、日志 1、`error=TimeoutError`、原 sentinel identity，负向 duration 0；最后重跑 R1 三类最小回归和完整矩阵。

### R2-R1-R1｜异常结束计时可执行收口

- 用户原始提交：`改完了 C4a-1f-R2-R1-R1 异常结束计时可执行收口`。
- 当前实现已经恢复可编译：异常分支绑定 `exc`，在日志追加前读取该分支自己的结束时钟，使用 `max(0, int(...))` 生成非负整数毫秒，传入完整七字段工具失败事件，`error=type(exc).__name__`，最后无参数裸 `raise`。
- 主审无网络最终矩阵：`113/113 PASS`。R2 异常路径 `48/48`、异常类型安全性 `9/9`、validation `12/12`、step hard gate `6/6`、R1 正常返回回归 `24/24`、generate 集成 `8/8`、静态范围 `6/6`。
- 独立只读最终矩阵：`136/136 PASS`。R2 exception 的 search/read × 正向 / 浮点 / 负时钟 `72/72`、validation `12/12`、MAX hard gate `6/6`、R1 两工具 × 三类正常返回 `30/30`、generate exception `7/7`、静态控制流 `9/9`。另一独立聚焦复核为 `3/3 PASS`。
- 决定性行为：每个异常工具只真实执行 1 次且零 retry；时钟读取 2 次；step 1→2；恰好追加一条七字段 `tool_call`；正向 / 浮点误差 / 负向时钟分别得到精确非负整数毫秒，负向为 0；日志只保存 `TimeoutError` 等类型名且不包含异常消息中的 secret；上层捕获对象与预存 sentinel 相同。执行前 validation 和 step 6 硬门仍为零工具 / 零时钟 / 零日志，R1 三类原对象返回无回归，工具异常后不进入第二模型。
- `py_compile` 退出 0；`git diff --check` 退出 0，仅保留两个 tracker 文件既有的 LF/CRLF 转换提醒。
- 教学检查点最终状态：`C4a-1f-R2 = PASS`，因此整个 `C4a-1f = PASS`。这只证明一次白名单工具尝试的正常返回与 Python exception 两条记账路径，不等于整个 `C4a-1` 或父级 `A4-Gate` PASS。
- 助手机械收口：删除已完成的 R2 TODO，把异常耗时按已通过的正常分支样式拆成结束时钟与 `duration_ms` 两个局部变量；没有改变运行逻辑。收口后聚焦回归 `9/9 PASS`，`py_compile` 与 `git diff --check` 继续退出 0。当前唯一 TODO 已切换到 `C4a-1g` 的第二次模型调用。

## Session S3｜`A4-Gate-C4a-1g`

### 学习定位

- 现在在做什么：把已经在第一次模型调用证明过的“调用前占 step、只计真实 `.create()`、成功 / Python exception 各写一条七字段日志”迁移到第二次模型调用。
- 为什么需要：同一请求的正常轨迹已经有第一次模型 `step=1` 和工具 `step=2`；第二次模型目前真实发生却没有记账，统一调用预算和轨迹仍不完整。
- 与前后关系：直接复用同一个 `run_context`、`reserve_call_step()`、`append_call_log()` 与既有响应守门；本轮完成后再分别处理 Reflection / Refinement 调用点。不会在这里实现 retry、最终状态映射、HITL 或 eval。

### 当前唯一实现任务

只修改 `generate_candidate_summary()` 中当前第二个 `client.chat.completions.create(...)` 调用点：

1. 工具成功、两个历史消息都加入 `messages` 后，使用同一本 `run_context` 先占下一步；正常完整轨迹应得到 `step=3`。
2. 占步成功后读取起始时钟；`try` 只包第二次真实 `.create()`，不能把日志 helper 或后面的响应解析包进去。
3. `.create()` 抛 Python exception：读取结束时钟并换算非负整数毫秒，追加一条 `model_call` 失败事件；`model=DEEPSEEK_MODEL`、`tool_name=None`、`error` 只用异常类型名，随后裸抛同一个异常。
4. `.create()` 正常返回：在与 `except` 互斥的成功分支换算耗时，追加恰好一条相同字段的成功事件，唯一区别是 `error=None`。
5. 现有 `choices → finish_reason → message / tool_calls / content` 守门全部保留在成功调用日志之后；不创建第二本 context，也不改公开入口或最终三字段结果。

### 可观察完成标准

- 完整成功 fake：模型调用 2 次、工具调用 1 次，同一 request ID 的日志恰好依次为 `step 1 model / step 2 tool / step 3 model`；第二模型事件耗时为非负整数、`error=None`。
- 第二次 `.create()` 抛预存 `TimeoutError("secret=...")`：前两条日志保留，step 3 只追加一条失败模型事件；日志不含 secret，上层捕获对象仍是原 sentinel，并且不进入第二响应守门、Reflection 或 Refinement。
- 第二次 `.create()` 成功但响应协议错误（代表值：空 choices、`finish_reason="length"`、空 content）：先形成一条 `error=None` 的 step 3 成功调用日志，再由既有守门抛受控错误；不得补记第二条失败调用日志。
- 第二模型负向时钟钳制为 0；进入该调用点时若 step 已为 6，则在计时 / `.create()` / 日志前拒绝且 context 不变。组合边界可从 step 4 开始，让第一模型占 5、工具占 6、第二模型被硬门拒绝。
- 工具正常返回 `ok=False` 时仍在第二模型之前停止，因此不占 step 3、不计第二模型时钟、不调用模型、不写第二模型日志。本轮不重试，也不接 Reflection / Refinement。

### 用户提交与最终复测

- 用户原始提交：`改完了 C4a-1g 第二次模型调用日志`。
- 当前实现满足目标结构：工具成功且历史消息完成后才占下一步；第二次 `.create()` 位于只含该赋值的 `try`；异常分支记录安全类型名后裸抛；成功分支写 `error=None`；全部第二响应守门位于成功调用日志之后。没有创建新 context，也没有越界接 Reflection / Refinement。
- 主审无网络矩阵：`143/143 PASS`，其中动态行为 `133/133`、静态结构 `10/10`。覆盖 search/read 完整成功、第二模型异常的正向 / 浮点 / 负时钟、9 类坏响应、step 4 组合 MAX、工具正常返回失败、成功 / 异常 append sentinel、完整四模型既有业务链与调用点范围。第一次静态探针误把 AST 语句列表直接传给 `ast.walk()`，在所有动态断言已通过后由测试脚本自身抛 `AttributeError`；纠正遍历方式后静态 `10/10`，这是探针错误而非产品代码错误。
- 独立完整矩阵：`106/106 PASS`。完整成功三步 `8/8`、第二 create 异常正向 / 浮点 / 负时钟 `21/21`、8 类坏响应 `32/32`、hard gate `10/10`、工具正常返回失败 `8/8`、append sentinel `4/4`、最终三字段 / 接线范围 `9/9`、AST `14/14`；`py_compile` 与 `git diff --check` 退出 0。另一控制流复核的完整成功、坏 choices、第二 create 异常、初始 step 4 MAX、工具返回失败五组也全部通过；范围复核的成功 / 异常 / hard gate 内存探针同样全部通过。
- 决定性行为：正常链日志严格为同一 request ID 的 `step 1 model → step 2 tool → step 3 model`；第二 `.create()` 抛预存 `TimeoutError` 时 step 3 只记一条安全失败事件并保持异常 identity；成功但 choices / finish_reason / content / tool_calls 不合法时先记 step 3 成功事件再由现有守门抛错；初始 step 4 时第一模型占 5、工具占 6、第二模型在时钟 / `.create()` / 日志前被拒绝；工具返回 `ok=False` 时不产生 step 3。
- 教学检查点最终状态：`C4a-1g = PASS`。这只证明第二次候选模型调用的记账，不等于整个 C4a-1 或父级 `A4-Gate` PASS。
- 助手机械收口：删除已完成的 C4a-1g TODO、统一第二 `.create()` 与异常日志参数缩进并移除多余空行；未改变运行逻辑。随后为已经掌握的 context identity 传播做机械骨架更新：把同一本 `run_context` 从入口传到编排层和 Reflection helper，当前唯一 TODO 切换到 `C4a-1h`。接口传播后完整四模型 fake 聚焦回归 `9/9 PASS`，最终仍为三字段，当前账本仍恰好为 step 1 / 2 / 3；`py_compile` 与 `git diff --check` 继续退出 0。
- 范围审计另登记一项未覆盖协议债务：第一次模型响应目前没有显式要求 `finish_reason == "tool_calls"`。它不属于本次 C4a-1g，不影响本检查点 PASS；必须在整个 C4a-1 收口前另开一个极小守门点，且不能与当前 Reflection 日志 TODO 并行混改。

## Session S4｜`A4-Gate-C4a-1h`

### 学习定位

- 现在在做什么：把同一种调用记账模式迁移到 `request_reflection_feedback()` 内的 Reflection `.create()`；正常无重试轨迹中它是 `step=4`。
- 为什么需要：前三次真实副作用已经可追踪，但 Reflection 仍会真实调用模型却不占共享预算、不写日志；只有把日志放在真实 `.create()` 所在函数内，耗时和 Python exception 边界才准确。
- 与前后关系：入口、编排层和 Reflection helper 的同一 context identity 传播已由助手按既有 PASS 证据机械接好；你只实现当前 Reflection 调用点。Refinement 仍是下一独立检查点，不在本轮接线。

### 当前唯一实现任务

只修改 `request_reflection_feedback()` 中当前活动 TODO：

1. 保留 prompt 非空校验和 client 准备在前；只有 client 已准备成功、即将发生真实 Reflection 调用时，才用传入的同一本 `run_context` 占下一步。
2. reserve 成功后读取起始时钟；`try` 只包 Reflection 的一次 `.create()`，不能把日志 helper或现有响应解析包进去。
3. `.create()` 抛 Python exception 时，读取结束时钟、换算非负整数毫秒、追加完整 `model_call` 失败事件，`error` 只用异常类型名，然后无参数裸 `raise`。
4. `.create()` 正常返回时，在互斥成功分支记录同字段成功事件，`error=None`。
5. 现有 response / choices / message / finish_reason / tool_calls / content 守门全部留在成功日志之后；不修改这些协议规则，不接 Refinement。

### 可观察完成标准

- 直接成功 fake：context step 3→4，Reflection `.create()` 1 次、时钟 2 次、七字段日志 1 条、`error=None`，返回两端空白已去除的反馈文本。
- `.create()` 抛预存 `TimeoutError("secret=...")`：step 4 写一条失败日志，`error="TimeoutError"` 且不含 secret；上层捕获对象仍是原 sentinel，不访问 response，也不进入 Refinement。
- `.create()` 成功但响应为 None、空 choices、`finish_reason="length"`、出现 tool_calls 或空 content：先写 step 4 成功调用日志，再由既有守门抛 `RuntimeError`；不追加第二条失败日志，也不调用 Refinement。
- direct context 已为 step 6 时，reserve 在时钟 / `.create()` / 日志之前拒绝且 context 不变；组合验证可让入口 context 初始为 3，使前三次调用占 4 / 5 / 6，Reflection 的第 7 次尝试被硬门拒绝。
- prompt 构造或 client factory 在 reserve 前失败时零 Reflection step / 时钟 / 日志；工具失败或第二模型响应守门失败时同样不进入 Reflection。
- 正常集成轨迹应扩为同一 request ID 的 `step 1 model → step 2 tool → step 3 model → step 4 model`；最终业务返回仍恰好是 `status / summary / sources`，不加入 logs 或 request ID。本轮不接 Refinement、不重试、不改协议守门。

### 用户提交与最终复测

- 用户原始提交：`改完了 C4a-1h Reflection 模型调用日志`。
- 当前实现满足目标结构：prompt 非空校验与 client 准备均在 reserve 前；`try` 只包 Reflection `.create()`；异常分支追加安全类型名日志后裸抛；成功分支追加 `error=None`；response / choices / message / finish_reason / tool_calls / content 守门全部位于成功日志之后。入口只创建一次 context，生成层、编排层与 Reflection helper 收到同一对象；Refinement 没有新增 context、占步、计时或日志。
- 独立无网络完整矩阵：`158/158 PASS`。direct Reflection `100/100`：成功 `9/9`、异常正向 / 浮点 / 负时钟 `18/18`、15 类坏外壳 `60/60`、step 6 hard gate `3/3`、非法 prompt `3/3`、client factory 前置失败 `3/3`、append sentinel `4/4`；integration `37/37`：正常 step 1～4、入口初始 step 3 组合硬门、Reflection 异常 / prompt 构造 / client factory / 工具失败 / 第二模型异常或守门失败的下游阻断；AST / 范围 `21/21`。另两份独立聚焦 / 范围审查也均判功能通过。
- 主审聚焦矩阵在进入完整集成前已通过 direct 成功、异常、坏响应、MAX、preflight 与 append sentinel；集成断言最初把 `int((4.1 - 4.0) * 1000)` 误期望为 100，实际按已冻结的浮点截断合同正确得到 99。纠正测试期望后，机械清理后的完整聚焦回归为 `42/42 PASS`；另一条“代码无 TODO”探针起初又把模块说明中的文字“尚未创建活动 TODO”误当成真实注释，收窄为检查 `# TODO` 后通过。这两项均为测试探针问题，不是产品实现缺口。
- 决定性行为：direct success 将 context step 3→4，模型 1 次、时钟 2 次、七字段日志 1 条并返回 strip 后反馈；`.create()` 抛预存 `TimeoutError` 时记录安全类型名、不含 secret、负向时钟为 0且保持异常 identity；所有坏外壳均先留下 step 4 / `error=None` 的成功调用事件，再由现有守门受控失败；direct step 6 在时钟 / 模型 / 日志前拒绝；完整正常链账本严格为同 request ID 的 `step 1 model → step 2 tool → step 3 model → step 4 Reflection model`，最终仍恰好三字段，Refinement 虽按既有正常链执行但尚未记账。
- `py_compile` 与 `git diff --check` 最终退出 0；只有 code / tracker 工作区的 LF→CRLF 转换提醒。
- 教学检查点最终状态：`C4a-1h = PASS`。这只证明 Reflection 模型调用日志，不等于整个 C4a-1 或父级 `A4-Gate` PASS。
- 助手机械收口：删除已完成的 C4a-1h TODO，统一 reserve、`.create()`、append 与 duration 表达式的空格 / 缩进 / 多行格式；没有改变运行逻辑。当前代码中没有活动 `# TODO`。

## Session S5｜`A4-Gate-C4a-1i`

### 学习定位与实际启动

- 日期归属校准：`C4a-1i` 于 2026-08-12 已经启动并形成首次实现，2026-08-13 继续完成 R1 / R2 订正、复测与教学收口。此前把整个 S5 作为当天新启动的 45 分钟内容、并把 S5～S7 汇总为当天完整第二块的表述不准确；本节保留全部原始提交、失败、订正和最终通过证据，不据此记录实际学习时长。
- 当前在做什么：把正常链中唯一尚未统一记账的 Refinement `.create()` 接入入口创建的同一本 `run_context`，使无重试正常轨迹从 step 1～4 延伸到 step 5。
- 为什么需要：Refinement 目前实际调用模型但不占共享 step、不计时、不写七字段事件，导致调用总账缺最后一步，也无法证明第 7 次真实调用会在副作用前被统一硬门拒绝。
- 前置证据：C4a-1e / 1g / 1h 已分别证明前三个模型调用点的共享 context、`reserve → 计时 → 真实 create → 成功 / 异常互斥日志`、安全类型名与裸抛；C4a-1f 已证明工具调用使用同一口径。本单元只迁移既有机制，不重考其定义。
- 精确源码范围：`code/stage4/a4_gate_research_summary_agent.py` 的 `request_refinement_candidate()` 与 `reflect_refine_and_validate()` 唯一生产调用处；合同范围为 `code/stage4/problem-contract.md` 第 6 节数值护栏与最小结构化日志。
- 助手机械骨架：给 `request_refinement_candidate()` 增加必传 keyword-only `run_context`，并由编排层传入已有同一对象；在原 Refinement `.create()` 前建立当前唯一 `C4a-1i` TODO / `NotImplementedError`。没有实现用户应完成的占步、计时、成功 / 异常日志控制流。
- 用户要完成：只替换当前 TODO，使 prompt 校验与 client 准备仍在占步前；Refinement 正常调用成为下一共享 step；成功与 Python exception 各追加恰好一条完整 `model_call`；异常只记录安全类型名并裸抛；既有 response / choices / finish_reason / tool_calls / content / `json.loads` 守门全部保持在成功调用日志之后。
- 可观察完成标准：direct success、create exception、负向时钟、坏响应 / 坏 JSON、step 6 hard gate、append sentinel、完整正常链 context identity 与最终三字段均通过；Refinement 不新建 context，不加入 retry、状态映射、HITL、eval 或新的公开返回字段。
- 用户原始启动口令：`开始动手`。
- 用户首次提交原文：`改完了 C4a-1i Refinement 模型调用日志`。
- 已做对：`run_context` 为必传 keyword-only，入口只创建一次并把同一对象传到 Refinement；模型调用 `try` 只包含当前 `.create()`；成功 append 位于 response / choices / finish_reason / tool_calls / content / JSON 守门之前；JSON 解析仍使用独立 `try`；没有越界加入 retry、HITL 或 S6。`py_compile` 与 `git diff --check` 退出 0。
- 主审决定性探针：真实 helper 的正常响应执行模型 1 次、时钟 3 次，随后因 `step` 为浮点值抛 `ValueError("step 必须是非负整数")`；context 仍为 step 4、logs 为空。模型抛预存 `TimeoutError` 时也被日志 helper 的 `ValueError` 遮蔽，`caught is sentinel=False`。context 已为 step 6 时仍读取时钟 3 次并真实调用模型 1 次，没有形成调用前硬门。宽松 recorder 进一步确认 append 收到的 `step=40.1`，而不是共享整数 step。
- 独立无网络矩阵：`41/136 PASS`。direct success `2/8`；create exception 正向 / 浮点 / 负时钟 `3/21`；16 类坏 response / choices / message / finish_reason / tool_calls / content / JSON `32/96`；direct step 6 / MAX `1/5`；success append sentinel `3/6`。静态独立审查结论相同。
- 根因：当前连续执行 `perf_counter_start = perf_counter()` 与 `step_start = perf_counter()`，把第二个浮点时间戳当作日志 step，且完全没有调用 `reserve_call_step(run_context)`；异常分支又传 `error=e` 异常对象，违反 helper 只接受安全字符串或 `None` 的合同。这使成功路径无法写日志，异常路径遮蔽原异常，step 6 仍发生模型副作用。
- 教学检查点状态：`C4a-1i = RETRY（保留已正确的 context 接口、try / except / else、后置协议与 JSON 守门，不从头重写）`。
- 当前唯一订正 `C4a-1i-R1`：真实调用前先用 `reserve_call_step(run_context)` 得到整数 step，reserve 成功后再读取一次开始时钟；成功 / 异常 append 均使用该整数 step；异常日志改用 `type(e).__name__`。其余现有结构不动，S6 / S7 仍未打开。
- R1 用户提交原文：`改完了 C4a-1i-R1 共享占步与安全异常日志`。
- R1 已闭合：共享 step 由 `reserve_call_step()` 取得并在两处分支复用；正常 Refinement 将 context 从 step 4 推进到 5，真实 create 1 次、时钟 2 次、七字段成功日志 1 条并返回解析对象；三档异常保持原 sentinel identity，日志只写 `TimeoutError` 等类型名且不含 secret，负向时钟钳制为整数 0；坏 response / 坏 JSON 均先写 step 5 / `error=None` 的成功模型事件，再由既有守门抛错；append sentinel 不被模型异常分支捕获。
- R1 最小正常集成已形成同一 request ID 下严格的 `step 1 model → step 2 tool → step 3 model → step 4 Reflection model → step 5 Refinement model`，最终公开结果仍恰好 `status / summary / sources`。首次模型 finish_reason 守门和 C4a-2 retry 均未越界。
- R1 独立定向矩阵：`61/62 PASS`。direct success `8/8`、三档 create exception / identity / 安全 error `21/21`、坏 response 与坏 JSON 日志顺序 `12/12`、direct MAX `4/5`、append sentinel `6/6`、最小五步集成 `10/10`；`py_compile` 与 `git diff --check` 通过。
- R1 唯一剩余：当前先读取 `perf_counter_start`，再调用 `reserve_call_step()`。context 已为 step 6 时虽然模型 0、日志 0、context 不变，但时钟仍被提前读取 1 次；这不满足统一的 `reserve → clock → create` 和硬门前零计时要求，且正常耗时会额外包含 reserve 本身。
- R1 教学状态：`PARTIAL`。当前唯一订正 `C4a-1i-R2` 只交换这两行，先 reserve 取得 step，再读取起始时钟；其余已经通过的表达式、控制流和调用点全部不动。交换后只需快检 direct step 6 与正常五步回归。
- R2 用户提交原文：`改完了 C4a-1i-R2 硬门与计时顺序`。
- R2 最终结果：主审 direct 探针确认 step 6 时严格为 `clock=0 / create=0 / logs=0 / step保持6`，正常与异常路径无回归；独立最终快检 `35/35 PASS`，其中 direct MAX `5/5`、direct success `6/6`、负向时钟异常 / identity / 安全类型名 `6/6`、坏 JSON 日志顺序 `5/5`、append sentinel `4/4`、最小五步集成 `9/9`。
- 决定性集成证据：正常链为同一 request ID 的 `step 1 model → step 2 tool → step 3 model → step 4 Reflection model → step 5 Refinement model`，模型 4 次、工具 1 次、时钟 10 次；最终仍恰好 `status / summary / sources`。入口初始 step 2 时，前四次真实调用占 step 3～6，Refinement 第 7 次尝试在时钟 / `.create()` / 日志前被拒绝。
- `py_compile` 与 `git diff --check` 通过；首次模型 finish_reason 债务和 C4a-2 retry 均未越界。
- 教学检查点最终状态：`C4a-1i = PASS`。这只证明 Refinement 调用日志和共享硬门，不等于整个 C4a-1 或父级 A4-Gate PASS。
- 助手机械收口：删除已完成的 C4a-1i TODO，统一该块缩进与字典尾逗号；没有改变运行逻辑。当前唯一活动骨架切换到 S6 `C4a-1j`。

## Session S6｜`A4-Gate-C4a-1j`

### 学习定位与当前骨架

- 当前在做什么：补第一次模型响应的精确协议守门。第一次模型的职责是选择一个工具，因此只有 SDK choice 明确以工具调用方式结束，客户端才允许继续读取 message、解析 tool call 并执行工具。
- 为什么需要：当前只检查 `choices` 非空和 `message.tool_calls` 存在；若 fake / provider 返回 `finish_reason="stop"`、`"length"` 或 `"content_filter"`，但 message 恰好还带有 tool call，现有代码仍会执行工具。结束原因与载荷必须同时符合协议，不能只信其中一边。
- 与前后关系：第一次模型 API 已真实成功并已留下 step 1 / `error=None` 日志；本地协议守门不新增 step，也不补写第二条失败调用日志。守门通过后继续沿用既有 message / tool_call 校验；本点 PASS 后才做 S7 五步/MAX 整体验收。
- 助手机械骨架：保留第一次 `.create()`、成功日志与现有 choices 非空守门，只在其后的 `assistant_message = response1.choices[0].message` 前加入当前唯一 `C4a-1j` TODO / `NotImplementedError`；没有提供实现答案。
- 用户要完成：在读取 message / tool_calls 前，检查第一项 choice 是否具有结束原因字段，并只允许工具调用对应的结束原因继续；字段缺失或任何其他值都受控抛出 `RuntimeError`。不得读取 message 来替代 choice 的字段，也不得新增模型 / 工具调用、step、日志、retry 或最终状态映射。
- 可观察完成标准：合法工具调用结束原因保持完整五步正常链；字段缺失、`None`、空串、`stop`、`length`、`content_filter`、大小写变体和其他值均只留下 step 1 成功模型日志，工具 / 第二模型 / Reflection / Refinement 全部 0；即使坏结束原因同时携带可用 tool call 也不得执行；模块可编译且其余调用点不变。
- 用户首次提交原文：`改完了 C4a-1j 首次模型 finish_reason 守门`。
- 首次复核：教学 `RETRY`。动态矩阵 `94/102 PASS`；9 类非法 / 缺失值 `81/81`、`choices=None / []` 旧守门 `12/12`，但合法 `finish_reason="tool_calls"` 正常链仅 `1/9`。
- 已通过证据：字段缺失以及 `None`、空串、`stop`、`length`、`content_filter`、`TOOL_CALLS`、单数 `tool_call` 和其他值，即使携带合法形状的 tool call，也都在读取 message 前受控 `RuntimeError`；每例只保留 step 1 / `error=None` 的七字段模型成功日志，工具、第二模型、Reflection、Refinement 均为 0，且没有泄漏 `AttributeError`。既有空 choices 守门无回归，`py_compile` 与 `git diff --check` 通过。
- 唯一根因：实现先把合法值误写为单数 `"tool_call"`，随后又要求同一字段必须等于 `"stop"`；没有任何字符串能同时通过，后续 `length / content_filter` 专门分支也已被前置通用拒绝覆盖。
- 当前唯一订正 `C4a-1j-R1`：保留 choices 非空与字段存在性守门；只读取一次第一项 choice 的结束原因，并用一条严格规则统一判断——只有精确 `"tool_calls"` 才继续，其他任何值均受控停止。删除当前单数、`stop`、`length`、`content_filter` 四段互斥 / 冗余判断，不修改后续 message / tool_calls / 工具执行，不新增 step、日志或 retry。
- R1 用户提交原文：`改完了 C4a-1j-R1 合法 tool_calls 单一守门`。
- R1 复核：教学 `RETRY`，动态矩阵 `94/101 PASS`。负向 9 类 `81/81` 与 `choices=None / []` 的 `12/12` 继续通过；合法 `"tool_calls"` 完整链仍仅 `1/8`，停在 step 1，工具和全部下游为 0。
- R1 未闭合根因：当前保存代码只是把后续 `if` 改成 `elif`，但第一项仍比较单数 `"tool_call"`，第二项仍要求同一值等于 `"stop"`，并且 `length / content_filter` 枚举仍在。改变分支连接词没有消除互斥值条件，合法分支仍不可达。
- 当前唯一订正 `C4a-1j-R2`：不要再改 `if / elif` 连接方式；保留字段存在性检查后，把其后的整段四分支真正删除，只留下一个“值不精确等于 `"tool_calls"` 就抛 `RuntimeError`”的判断。后续从 `assistant_message` 开始的既有代码保持不动。
- R2 用户提交原文：`改完了 C4a-1j-R2 删除冗余分支并保留复数 tool_calls`。
- R2 最终复核：教学 `PASS`。当前只有精确 `"tool_calls"` 可以在字段存在性检查后继续，旧的 `stop / length / content_filter` 冗余分支已删除；独立完整矩阵 `102/102`，其中合法链 `9/9`、9 类非法 / 缺失值 `81/81`、`choices=None / []` `12/12`。
- 决定性负向证据：坏结束原因即使携带合法形状的 tool call，也只留下 step 1 / `error=None` 的模型成功日志；message 读取、工具、第二模型、Reflection、Refinement均为 0，异常统一受控为 `RuntimeError`。合法 `"tool_calls"` 恢复既有五步与最终三字段链路。
- 助手机械收口：删除已完成的 C4a-1j TODO，统一两处 `raise` 缩进；没有改变守门逻辑。
- 当前状态：`C4a-1j = PASS`，依赖满足后已执行本页 S7 纯验证。

## Session S7｜`A4-Gate-C4a-1k`

### 无重试五步、请求隔离与第 7 次硬门整体验收

- 本 Session 是对 C4a-1 已实现调用点的跨范围验证，不新增代码 TODO、retry、状态映射或长期测试 runner。
- 主审内存矩阵：`115` 条断言全部通过，覆盖 9 类非法 / 缺失结束原因、`choices=None / []`、topic / relative_path 两条正常链、两请求隔离和初始 step 2 的组合硬门。
- 独立复核：S7 `46/46 PASS`，其中 topic / path 正常链 `24/24`、双请求隔离 `7/7`、初始 step 2 硬门 `10/10`、首模非法短路 `5/5`；连同 S6 与静态检查，独立复核总计 `150/150`。
- 正常双路径：每个请求的最终 step 均为 5，日志步号严格 `[1, 2, 3, 4, 5]`，事件依次为 `model_call → tool_call → model_call → model_call → model_call`；同一请求五条日志共享同一非空 request ID，每条恰好固定七字段、非负整数耗时且 `error=None`。topic 工具为 `search_web`，path 工具为 `read_material`；最终结果恰好 `status / summary / sources`，不暴露内部 context 或日志，也不伪造 step 6。
- 请求隔离：两次公开入口调用使用不同 context 字典、不同 logs 列表和不同 request ID，各自从 step 1 开始并停在 step 5；修改一份账本不会污染另一份。
- 组合 MAX：把入口 context 初始 step 设为 2 后，首模 / 工具 / 第二模型 / Reflection 依次占 step 3～6并留下四条日志；Refinement 尝试第 7 次调用时在 `reserve_call_step()` 处被拒绝，Refinement 自身时钟 0、`.create()` 0、追加日志 0，context 保持 step 6，既有 `[3, 4, 5, 6]` 日志完整保留。
- 无重试边界：当前 `retryable=True` 工具失败仍只实际尝试一次，记录 `retryable_tool_failure` 后停止，不进入第二工具尝试或下游模型；真实一次工具 retry 明确留给 C4a-2。
- 静态与卫生：`py_compile`、`git diff --check` 均通过；当前仅有代码文件与两个 tracker 文件的 LF→CRLF 非阻塞提示。模块说明与内部日志 docstring 已按 C4a-1 收口事实机械更新。
- 教学结论：`C4a-1k = PASS`，因此 `C4a-1` 教学实现单元整体收口；这不等于父级 `A4-Gate` 正式 PASS。

## Session S8｜`A4-Gate-C4a-2a`

### 启动、计划与边界

- 用户时间归属校准原文：`C4a-1i是昨天的内容，今天完善了一下。今天还可以开一个新的学习块`。
- 用户启动口令原文：`开始动手`。
- 当前在做什么：在既有五步正常链上增加一次由上层编排负责的受控工具重试。第一次工具正常返回 `ok is False` 且 `retryable is True` 后，使用完全相同的工具名和原始参数只再真实尝试一次；第二次成功后继续候选摘要、Reflection 与 Refinement。
- 为什么需要：当前原子工具入口已经能让一次真实尝试独立占 step、计时并写七字段日志，但候选生成层遇到任何失败都会立刻停止，尚不能把瞬时可恢复故障变成有边界、可审计的恢复轨迹。
- 与前后关系：本块复用 C4a-1 已通过的一次调用原子边界和五步正常链；完成后六步恢复链才可作为后续二次失败 / 无剩余 step 的 `needs_manual` 映射、HITL 与 14 条 eval 的前置证据。
- 计划预算：180 分钟，包括关联旧复习 10 分钟、定向资料复核 20 分钟、六步恢复设计 20 分钟、用户编码 90 分钟、故障注入与真实链验证 30 分钟、用户最终审核 10 分钟。它是计划容量，不是实际学习时长。
- 当前唯一旧复习席位：`WP-08`，只结合本次真实失败链检查为什么不能让 Python `None` 进入 Tool Observation 或消息历史；`CD-005` 本块不抽取。
- 当前唯一实现范围：`step 1` 首模、`step 2` 首次工具失败、`step 3` 同参数重试成功、`step 4` 第二模型、`step 5` Reflection、`step 6` Refinement。
- 冻结边界：不实现 `retryable=False`、二次失败或无步数时的最终状态映射，不处理未知工具 / 参数纠正、候选校验恢复、HITL、14 条 eval 或父 Gate 判定。
- 当前状态：`DOING`。本节只记录已经启动与已安排范围；在用户提交和验证发生前，不预写实现结果、断言数字、真实 DeepSeek 成功或教学 `PASS`。

### 已核验资料与代码切入点

- 前序证据：已续接 [2026-08-05](2026-08-05.md) 至 [2026-08-13](2026-08-13.md) 的 A4-Gate 全部任务记录；C4a-1 已收口，父任务仍为 `DOING`。
- 冻结合同：`code/stage4/problem-contract.md` 第 4 节固定输出状态，以及第 6 节 `max_steps = 6`、`max_tool_retries = 1`、每次真实尝试独立计步 / 记日志和首次可重试失败恢复规则。
- 原子执行边界：`code/stage4/a4_gate_research_summary_agent.py` 的 `execute_tool_call()` 只负责一次真实工具尝试，不在内部隐藏 retry。
- 编排切入点：同文件 `generate_candidate_summary()` 已持有第一次模型产生的 `tool_name`、原始 `raw_arguments`、`tool_call_id` 和共享 `run_context`；当前失败守门前是本块唯一实现位置。
- 入口边界：`run_research_summary_once()` 继续只负责输入早拒绝、单 client / 单 context 和候选 → Reflection → Refinement 总编排，本块不改其公开签名。
- 旧复习证据：`WP-08` 原始 `RETRY → PASS` 主体在 [2026-07-29](2026-07-29.md)；[2026-07-30](2026-07-30.md) 又证明外部明确为 `str | None` 的合同可以合法返回 `None`，所以本次只迁移“非法值不得越过消息历史边界”，不机械改成“所有 None 都必须是空串”。

### 用户 / 助手分工与当前骨架

- 用户必须亲自完成：回答一次 `WP-08` 换表面检查；判断三层中哪一层负责重试并说明职责边界；替换当前唯一 TODO；解释为什么两次工具尝试同名同原始参数、各占一步，而第一次失败不进入成功 Observation。
- 助手自动完成：冻结合同与本块范围、建立不含答案的 TODO 骨架、维护 daily、运行无落盘 fake / spy 与必要真实 DeepSeek 验证、机械整理已证明的结果。
- 用户最终审核：确认实现符合“最多一次、同名同原始参数、失败记录可见但不污染模型历史”的意图，并核对本块没有越界实现后续失败状态或 HITL。
- 已建立最小骨架：模块常量区加入合同给定值 `MAX_TOOL_RETRIES = 1`；在第一次 `execute_tool_call()` 返回后、既有最终失败守门前加入唯一 `C4a-2a` TODO / `NotImplementedError`。`execute_tool_call()`、公开入口和其他已通过调用点未改。

### 当前第一检查点｜`WP-08` 旧知识回炉

- 题目只检查已经学过的“非法 Python 值不能污染模型历史”，不要求提前设计重试次数、重试层级、step 或失败状态映射。
- 待用户回答：给定失败结果 `{"ok": False, "retryable": True, "error": "timeout"}`，若把 `tool_result.get("content")` 直接写入 tool message，说明实际进入 `messages` 的值、最早违反契约的边界、为何下游 `.strip()` 只是末端症状、Python `None` 与字符串 `"None"` 的差别，并写一条观察下一次模型调用实际收到 `messages` 的 spy / 断言。
#### `A4-Gate-C4a-2a-G1`｜首次作答

用户原始答案：

> 1.实际进入 messages 的值是
> ```
> {
>     "role": "tool",
>     "tool_call_id": tool_call_id,
>     "content": tool_result.get("content"),
> }，最早违反契约在构造 Tool Observation时
> ```
> 2.因为是最后执行的，之前没有过滤，None是bool类型，表示空，"None"是字符串None，有值
> 3.assert captured_messages is "error"

教学判定：`RETRY`。

- 已做对：把最早的问题定位到构造 Tool Observation / 写入 `messages` 的状态边界，而不是把根因归到更晚的 `.strip()`；也意识到 Python `None` 与字符串 `"None"` 不是同一个值。
- 缺口 1：第一问仍保留了表达式 `tool_result.get("content")`，没有把它求值。当前失败字典没有 `content` 键，所以追加的是一个 `content` 值为 Python `None` 的完整消息字典。
- 缺口 2：`None` 不是 `bool`。它是 `NoneType` 的唯一值，通常表达“缺失 / 没有结果”；它是 falsy（`bool(None) is False`），但这不等于它的类型是布尔值。字符串 `"None"` 是四个字符组成的 `str`，对它调用 `.strip()` 不会发生本题的 `AttributeError`，但类型是字符串也不自动代表它是有效证据。
- 缺口 3：`.strip()` 只是后来读取已污染状态时暴露症状；真正的合同破坏已经发生在把不符合 tool-message `content` 字符串合同的值追加到共享历史之时。
- 缺口 4：`captured_messages` 是消息列表，不会与字符串 `"error"` 是同一个对象；`is` 检查对象 identity，也不能证明列表内部的 Tool Message 是否安全。断言必须筛出下一次模型实际收到的 `role == "tool"` 消息，再逐条检查其 `content`。
- 当前唯一订正：原题原位重答三点，并补全一个观察 `captured_messages` 内部 Tool Message 的断言；本题通过前不进入重试职责或代码实现。

## 当前交接

- 今日计划范围 / 今日剩余：`C4a-1` 的 S1～S7 已完成教学收口，其中 `C4a-1i` 是 2026-08-12 启动、2026-08-13 完善并收口的跨日内容。用户现已启动今日第二块 S8 `C4a-2a`；当前今日剩余就是 S8 尚未完成的 180 分钟计划范围，不代表实际还需学习 180 分钟。
- 任务总剩余：父任务 `A4-Gate` 仍为 `DOING`。当前先完成 `C4a-2a` 六步成功恢复轨迹；之后仍有不可重试 / 二次失败 / 步数不足的状态映射、未知动作与候选失败恢复、HITL allow / deny、14 条 `eval_cases.json` 和正式 Gate 复核。
- 当前 Session 起点：先完成与真实产物绑定的 `WP-08` 关联复习，再由用户判断重试应由哪一层负责，随后进入当前唯一 `C4a-2a` TODO。若本日中断，下次只从 S8 的第一个真实未完成检查点接续，不预先宣称已经完成六步链。

## 正式同步收口｜`A4-Gate-C4a-1`

- 批改判定：`C4a-1 = PASS`。C4a-1a～1j 的实现与 C4a-1k 跨范围验证均已闭合；父任务 `A4-Gate = DOING`，不因本切片通过而提前升级整关。
- 决定性验证：`.venv\Scripts\python.exe -m py_compile code\stage4\a4_gate_research_summary_agent.py` 退出 0；`git diff --check` 退出 0；S6 独立矩阵 `102/102`、S7 主审 115 条断言与独立矩阵 `46/46` 全通过，独立 S6/S7/静态合计 `150/150`。
- 运行轨迹：topic 与 relative_path 两路均在同一请求内得到 `[1, 2, 3, 4, 5]`，事件顺序固定为 `model_call → tool_call → model_call → model_call → model_call`；每条恰好七字段，同请求 ID 一致、不同请求的 context/logs/ID 互相隔离，最终公开结果仍严格只有 `status / summary / sources`。
- 安全停止：入口 context 初始 step 2 时，前四次真实调用占 3～6；Refinement 的第 7 次尝试由 `reserve_call_step()` 在计时、模型调用和日志写入之前拒绝，既有账本保持不变。首次模型结束原因缺失或不精确等于 `"tool_calls"` 时，只保留 step 1 的 API 成功日志，工具与全部下游调用为 0。
- 复合故障闭环：本轮实际复现并修复了“时间戳冒充 step、日志异常覆盖原异常、`finally return` 改写控制流、硬门晚于计时、`tool_call` / `tool_calls` 字面量混淆与互斥守门”链路；每个根因都经过对应 fake/spy 再注入和正常链回归。
- 测试 / eval / 安全边界：本轮只使用无落盘内存矩阵和现有源码，不新增一次性 runner；14 条正式 `eval_cases.json` 尚未创建，HITL 尚未实现，不能据此声称整关或求职证据 PASS。
- 弱点同步：`WP-33` 保持 🔴，新增 2026-08-13 的精确协议字面量与单一 allow 条件证据；间隔池当前到期 `CD-005 / WP-08` 两项、逾期 0 天，状态仍为 `NORMAL`，未触发回收块。
- 工作场景同步：WS-02 已补充 C4a-1 的 request ID、七字段调用轨迹、双请求隔离和 MAX 硬门证据，等级保持 `DIAGNOSED_AND_FIXED`；真正的失败恢复仍依赖 C4a-2，不冒充 `RECOVERED_UNDER_FAULT`。
- 岗位证据：不更新 `tracker/job-readiness.md`。父 Gate 尚未完成，且本轮不是 JOB_EVIDENCE / FINAL / J11-Gate 交叉复核。
- 算法状态：W4 内容块继续为 `PASS`，结转债务 0；本日没有安排独立算法时段，不新增算法债务。
- 今天最虚 / 下次重点复习：读取真实 SDK 字段后，仍容易把精确协议值写成近似单词或用多个互斥分支枚举；下次应先写“唯一允许值”，再用一个 fail-closed 条件统一拒绝其余输入。

## 跨会话接力摘要（供后续会话直接读取）

1. 事实状态：`tracker/progress.md` 中父级 `A4-Gate` 必须保持 `DOING`；已完成到 `C4a-1`，不是整关 PASS。
2. 当前代码：`code/stage4/a4_gate_research_summary_agent.py` 已有每请求独立 `run_context`、`MAX_STEPS = 6`、固定七字段日志及五个真实调用点；正常无重试链只用 step 1～5，不虚构第 6 步。
3. 当前活动实现点（本页 S8 已启动）：`C4a-2a`——工具返回 `ok=False, retryable=True` 后，以完全相同参数最多再真实尝试一次；两次工具尝试必须分别 reserve、计时和写日志，成功恢复后的完整轨迹应正好占 step 1～6。
4. 当前点的冻结边界：先只证明“首次可重试失败 → 第二次成功”；不要同时混入不可重试失败、二次失败 / 无预算的 `needs_manual` 映射、未知动作恢复、HITL 或 eval。
5. 后续依赖顺序：C4a-2a 六步成功恢复 → 失败状态 / action 与 candidate recovery → HITL allow/deny → 创建并运行 14 条 `eval_cases.json` → `A4-Gate` 正式检查；只有最后一步通过才可改父任务状态。
6. 复习状态：`weak-points.md` 为 `NORMAL`，截至 2026-08-13 到期 `CD-005 / WP-08`，未达到回收块阈值；新会话仍需按当天 preflight 重算，不得把本摘要当作自动提醒。
