# 每日学习打卡

- 日期：2026-07-22
- 当前任务：A4-03 ReAct
- 任务状态：PASS（2026-07-22 正式复核）
- 今日环节：F2 补救题收口 + A4-03 正式任务级核验

## 正式练习续答

### F2 补救题 — 精确返回元组

- 昨日已确认的语义结果：带两个前导空格的 `  Action:` 不匹配 `^Action:`；未识别行会继续进入 Thought，所以内容应包含 `ready`、换行、两个空格和整条 `Action: Weather[Singapore]`，而 `action=None`。
- 今日原始作答：

  ```text
  (thought, action) = (ready\n  Action: Weather[Singapore], None)
  ```

- 做对的部分：已使用 `\n` 表示换行，并保留换行后的两个空格；元组中第二项仍正确写为 `None`。
- 当前缺口：完整 Thought 值仍没有字符串引号。实际交给 Python 执行得到 `SyntaxError: unexpected character after line continuation character`；`\n` 只有位于字符串引号内部才表示换行字符。
- 本次状态：PARTIAL。只需给完整 Thought 内容补上英文字符串引号，不再重考解析原理。

- 最终订正：

  ```python
  (thought, action) = ("ready\n  Action: Weather[Singapore]", None)
  ```

- 直接验证：该表达式可由 Python 正常执行，实际 `repr` 为 `('ready\n  Action: Weather[Singapore]', None)`，与当前 `parse_llm_output` 的实测结果一致。
- 补救题结论：PARTIAL → PASS。学习者已能在换表面输入中准确追踪 `^Action:` 未命中后，未识别文本继续进入 Thought 的结果。
- 边界保留：此补救只证明行首锚点语义与实际返回追踪已经补上；原 F2 的完整 `thought_pattern` 是在完整答案支持下完成，因此原正式状态仍为 `RETRY`，不改记为独立 PASS。

### F3 — 确定性假 LLM 的证据边界（D：解释 + 迁移）

- 真实产物：`code/stage4/a4_03_react_agent.py` 中 `run_react(question, llm_call, max_steps)` 通过参数注入模型调用；`demo_llm(prompt)` 只根据 prompt 是否包含固定 Observation，返回两段写死的 ReAct 文本。
- 当前实测：直接运行文件得到 Weather 工具调用、真实本地 Observation 回填和第二轮非空 Finish 的完整两轮轨迹。
- 迁移场景：同事据此声称“这已经证明真实 DeepSeek 总会遵守 Thought/Action 格式，并且总能正确选择 Weather”。
- 作答要求：分别说明该确定性 demo 实际能证明什么（至少 3 点，绑定当前代码或轨迹）、不能证明真实 LLM 的什么（至少 3 点），以及把 `demo_llm` 换成真实模型适配器但保持 `run_react` 不变时，适配器必须维持的最小调用接口契约（输入、输出、注入方式）。
- 原始作答：

  > 1.证明了回答会把历史一起带给模型。证明了如果发起了调用请求且格式正确，会按照ReAct来走流程。证明了验证回答的校验必须性
  > 2.不能证明真实的LLM一定会按照prompt的格式返回。不能证明LLM实际发起了调用工具的请求。不能证明LLM进行了思考后返回的结果
  > 3.没有明白

- 做对的部分：
  - 已抓住固定 demo 能跑通“格式正确的 Action → 客户端 ReAct 循环”这一代码路径。
  - 正确指出固定返回不能证明真实 LLM 会遵守 prompt 格式。
  - 正确质疑 `Thought:` 文本不能证明真实 LLM 存在可验证的内部思考过程。
- 需要校准：
  - 当前 demo 只能证明完整 prompt（含历史）传入了被注入的 `llm_call` 可调用对象；`demo_llm` 不是模型，不能把这条证据直接表述成“已经带给真实模型”。
  - LLM 在此协议中只输出 Action，客户端才解析并执行 `weather_tool`。应把“不能证明 LLM 实际发起工具调用”精确为“不能证明真实 LLM 会稳定输出正确 Action”；LLM 本来就不负责执行工具。
  - “证明了验证回答的校验必须性”没有绑定当前 happy-path demo 的具体可观察证据，不能作为该 demo 已证明的第 3 点；需要换成轨迹中确实执行过的代码事实。
  - 第 3 问未作答，缺少 `llm_call` 的输入、输出及参数注入契约。
- 单题状态：PARTIAL。保留前两问的正确边界，先补“可调用接口契约”前置。
- 教学拆分：用 `response_text = llm_call(prompt)` 与 `LLMCall = Callable[[str], str]` 说明依赖注入插座；只考真实适配器应保持的函数形状与传入位置，不要求编写真正 API 代码。
- F3 补救 1 作答：

  ```python
  def deepseek_llm(prompt: str) -> str:
      # 内部调用真实 DeepSeek API
      ...

  run_react(
      question="新加坡今天的天气如何？",
      llm_call=deepseek_llm,
      max_steps=3,
  )
  ```

- 补救 1 结论：PASS。已准确写出 `str -> str` 的可调用接口，并通过 `llm_call=deepseek_llm` 完成参数注入；题面中的 `...` 只是 API 实现占位，不作为返回值实现错误处理。
- 尚待确认：真实 SDK 返回复杂响应对象时，适配器必须提取其中的模型原始文本再返回，不能把 SDK response 对象直接交给 `parse_llm_output`。
- F3 补救 2 作答：

  > 采用return response.choices[0].message.content，只需要模型的回答，不需要其他内容

- 补救 2 结论：PASS。正确选择模型生成的原始文本；精确原因是 `run_react` 会把该返回值交给 `parse_llm_output`，其正则输入必须是包含 `Thought/Action` 的 `str`，不能是 SDK 的复杂 response 对象。
- 接口契约当前证据：输入完整 prompt `str`、输出模型原始文本 `str`、以 `llm_call=deepseek_llm` 参数注入，三项均已补齐。
- F3 剩余缺口：原第 1 问的第三项“证明了验证回答的校验必须性”不是 happy-path demo 的可观察结论；需替换为当前两轮轨迹实际证明的一个代码事实。
- F3 最后订正第一次作答：

  > 这个 demo 证明了Action: Finish[...]中的Finish[...]的回答不应该是...，应该是具体的内容。

- 做对的部分：注意到了当前实现只会让非空 `Finish[...]` 进入最终返回分支，与 F1 的硬校验修复方向一致。
- 当前缺口：该句仍在陈述“应该怎样”的协议/校验要求，而不是这次 happy-path 轨迹已经直接观察到的执行事实；且把 `...` 当作被 demo 实际测试的输入，但当前 demo 没有运行该案例。
- 本次状态：PARTIAL。下一次只沿第一轮 `Weather Action → 客户端工具执行 → Observation` 描述实际动作，不再讨论 Finish 的规范要求。
- F3 最后订正第二次作答：

  > 这个 demo 证明了：当 demo_llm 返回格式正确的 Weather[Singapore] 时，
  > 客户端能够解析这个 Action，执行 weather_tool，
  > 并把工具返回值记录为 Observation，供下一轮读取。

- 订正结论：准确绑定当前代码与两轮轨迹，只陈述客户端已经实际解析、执行和回填的事实，没有把确定性 demo 外推为真实 LLM 的能力。
- F3 最终状态：PARTIAL → PASS（经接口前置教学与分步补救）。当前证据已覆盖：
  - 确定性 demo 能证明当前客户端解析、工具分发、Observation 回填、历史进入下一轮和非空 Finish 返回路径可跑通。
  - 它不能证明真实 LLM 总会遵守格式、稳定选择正确 Action，也不能把 `Thought:` 文本当作可验证的内部思考证据。
  - 保持 `run_react` 不变时，真实适配器接收完整 prompt `str`、返回模型原始文本 `str`，并通过 `llm_call=deepseek_llm` 注入。

## 练习阶段小结

- 正式题集已结束：F1 `PARTIAL → PASS`；F2 原正式题为 `RETRY`，随后完成教学订正并在新字段名场景无提示迁移 PASS，当前掌握链路为 `RETRY → PASS`；F3 `PARTIAL → PASS`。
- 当天有效证据：G1–G7 共 7 个教学检查点，加 F1、F3 和 F2 新字段名无提示迁移，共 10 个合格证据；提示后的重复订正不额外增加题量。
- 代码状态沿用已记录的直接证据：完整两轮 ReAct demo、`max_steps`、空 Finish、防正文内 `Action:` 误解析、`py_compile` 与 `git diff --check` 均已验证；任务级复核仍需重新执行决定性检查。
- 今天最虚：完整正则片段的首次组合、Python 字符串引号，以及区分“规范应该怎样 / demo 实际证明什么 / 不能外推什么”时的证据措辞精度；正则组合已由后续无提示迁移补证。
- 下次重点复习：继续用原始多行文本逐字符追踪正则捕获，并保持精确 Python 字符串语法；回答测试证据时先写可观察代码事实，再单列不能外推的真实模型行为。
- 练习阶段结束时的下一步：使用固定口令「检查今天的学习」，交由 `ai-agent-learning-review` 重新运行代码并决定任务状态；后续正式 PASS 结果见本文件下方“批改判定”。

## F2 独立复测（追加证据）

- 复测原因：原 F2 的实际缺陷已修复、换表面行为追踪也已 PASS，但完整 Thought 正则是在答案支持下组合，仍缺一次闭卷独立构造证据。
- 独立性规则：不打开当前代码、昨日 daily 或 notes；首答不提供正则片段和提示；不要求修改生产文件。
- 新输入：

  ```text
  Thought: 先整理已有信息。
  第二行引用了字面文本 Action: Search[demo]，但它不是字段。
  仍需查询发布日期。
  Action: Search[2026 latest phone release]
  ```

- 作答要求（同一个高价值检查点）：
  1. 写出完整的 `thought_pattern`、`action_pattern` 和两次 `re.search` 共用的 flags。
  2. 写出 `parse_llm_output` 返回的精确 Python 元组，用 `\n` 表示字符串内换行。
  3. 解释你的写法如何同时保证：字段标签只在行首生效；正文中的字面量 `Action:` 保留；多行 Thought 不在第一行结束；捕获在真实 Action 字段或全文结束前停止。
- 题面已在当前真实 parser 上做无泄漏预检，案例可答且预期唯一；未把预期 pattern 或返回元组写入题面。
- 独立首答：

  ```text
  1. thought_pattern = r"^Thought:\s*(.\**?)\s*(?=^Action:|\Z)"
     action_pattern = r"^Action:\s*(.*)"
     flags = re.MULTILINE | re.DOTALL
  2. (thought, action) = ("先整理已有信息。\n第二行引用了字面文本 Action: Search[demo]，但它不是字段。\n仍需查询发布日期。", None)
  3. ^表示只在行首生效，`*.\*?`会保留，re.DOTALL不在第一行结束，(?=^Action:|\\Z)捕获在真正的 Action 字段或全文结束前停止。
  ```

- 做对的部分：`action_pattern`、`re.MULTILINE | re.DOTALL` 均正确；能说明 `^` 的行首职责、DOTALL 的跨行职责和前瞻的停止职责。
- 决定性错误 1：捕获组写成 `(.\**?)`。其中 `\*` 把星号转成字面量星号，不再表示“让点号重复”；该组实际只允许一个任意字符后跟零个或多个字面量 `*`，所以无法捕获普通多行 Thought。
- 决定性错误 2：真实 `Action:` 位于行首，已写对的 `action_pattern` 会匹配它，因此 `action` 不可能是 `None`。
- 按学习者原 pattern 直接执行的精确结果：`(None, 'Search[2026 latest phone release]')`。
- 独立复测状态：RETRY。即使后续经提示订正，也不倒改本次独立首答；先修正捕获组含义，再决定是否安排新的无提示换表面复测。
- 提示 1：只看捕获组；应让 `*` 修饰前面的点号，不能用反斜杠把 `*` 转成普通星号。下一步只重写捕获组并重新计算元组。
- 提示 1 后订正：

  ```text
  捕获组 =r"^Thought:\s*(.*?)\s*(?=^Action:|\Z)"
  (thought, action) = (先整理已有信息。\n第二行引用了字面文本 Action: Search[demo]，但它不是字段。\n仍需查询发布日期。", Search[2026 latest phone release])
  ```

- 做对的部分：虽然变量标签写成“捕获组”，实际给出的是完整 `thought_pattern`，但其核心 `(.*?)` 及完整组合已经正确；Thought 与 Action 的文本内容也都判断正确。
- 当前缺口：精确 Python 元组中的 Thought 和 Action 都是字符串，必须分别使用引号定界；当前写法不是合法 Python，且 `\n` 只有位于字符串内部才表示换行。
- 订正状态：PARTIAL。只需把两个元组元素改写成合法字符串；这不会倒改前一轮独立首答的 RETRY。
- 提示后语法订正：

  ```python
  (thought, action) = ("先整理已有信息。\n第二行引用了字面文本 Action: Search[demo]，但它不是字段。\n仍需查询发布日期。", "Search[2026 latest phone release]")
  ```

- 语法订正结论：PASS。两个元素均为合法字符串，换行、内嵌字面量与真实 Action 内容精确；该结果属于提示后的补救，不倒改首次独立复测 RETRY。

### F2 新字段名无提示迁移

- 目的：在已校准 `(.*?)` 与字符串语法后，改用 `Reasoning/Decision` 字段，检查能否独立迁移完整组合，而不是复写当前 `Thought/Action` 答案。
- 输入：

  ```text
  Reasoning: 先检查库存。
  第二行引用 Decision: Lookup[demo]，这里只是示例。
  还需要真实查询。
  Decision: Lookup[laptop stock]
  ```

- 作答要求：不查看旧答案；独立写出完整 `reasoning_pattern`、`decision_pattern`、共用 flags、精确 `(reasoning, decision)` Python 元组，并用一句话解释内嵌 `Decision:` 为何不会成为字段边界。
- 预检：已以内存正则运行确认案例可答且输出唯一；题面不披露预期 pattern 或元组。
- 无提示首答：

  ```python
  reasoning_pattern = r"^Reasoning:\s*(.*?)\s*(?=^Decision:|\Z)"
  decision_pattern = r"^Decision:\s*(.*)"
  flags = re.MULTILINE | re.DOTALL
  (reasoning, decision) = ("先检查库存。\n第二行引用 Decision: Lookup[demo]，这里只是示例。\n还需要真实查询。", "Lookup[laptop stock]")
  ```

- 原因说明：`^` 匹配行首，`(?=^Decision:|\Z)` 只在真正的行首 Decision 字段或全文结束前停止，因此正文中非行首的字面量 `Decision:` 不会成为边界。
- 直接执行：按学习者提交的两条 pattern、flags 和输入运行，实际元组与提交值完全一致。
- 单题状态：PASS（新字段名、无提示、完整 pattern + flags + 精确输出 + 原因）。
- F2 当前证据结论：保留原正式题在答案支持前的 `RETRY` 与首次追加独立复测的错误路径；本次新字段名无提示迁移提供了新的独立 PASS，故当前掌握状态可记录为 `RETRY → PASS`。该练习阶段不自行判任务状态，后续正式 PASS 结果见下方“批改判定”。

## 批改判定

- **A4-03：PASS。**
- 产物 `code/stage4/a4_03_react_agent.py` 可直接运行，完整展示 `Thought → Action → Observation → 下一轮 Thought → Finish` 两轮轨迹。
- `max_steps` 对 LLM 调用次数形成确定上界；连续 `Weather`、空 `Finish[]` 均在规定轮数后返回 `None`，不会无限循环。
- 已能解释 Thought / Action / Observation 的职责、成功停止与安全停止的区别，以及 prompt 软约束和客户端硬校验的分工。
- 证据矩阵共 10 个合格检查点：G1–G7、F1、F3、F2 新字段名无提示迁移。原 F2 与首次独立复测的 `RETRY` 保留，当前掌握链路记为 `RETRY → PASS`，不倒改历史首答。
- 任务要求的项目 notes：不适用；tracker 只要求可运行代码产物，本次未为汇报另建项目 notes。

## 工程验证

### 验证命令与关键结果

1. 直接运行真实提交文件：

   ```powershell
   $env:PYTHONDONTWRITEBYTECODE='1'; & '.\.venv\Scripts\python.exe' -B 'code\stage4\a4_03_react_agent.py'
   ```

   关键结果：第 1 步执行 `Weather[Singapore]` 并得到 `Observation: 34°C, AQI=160`；第 2 步返回非空 `Finish[...]`；进程退出码为 0。

2. 以内存断言套件通过 stdin 调用当前模块，不保留一次性 runner：

   ```powershell
   @'<compile、parser、prompt、loop 共 15 项内存断言>'@ | & '.\.venv\Scripts\python.exe' -B -
   ```

   决定性结果：`FORMAL_REVIEW: 15/15 passed`。覆盖语法编译、普通/多行/正文内字面 `Action:`/缺失 Action/缩进标签、合法/多行/坏 Action、空与非空 history、完整 demo、精确 `max_steps`、空 Finish、未知工具、非空 Finish 和缺失 Action 安全停止。

3. 工作区文本检查：

   ```powershell
   git diff --check
   ```

   关键结果：退出码 0，无空白错误；仅有 Git 的 LF/CRLF 提示。

- 验证过程说明：首轮内存套件曾因 Windows PowerShell 原生管道把测试脚本中的中文源码字符转成 `?`，导致一条测试数据断言误报；打印码点确认是验证脚本输入编码而非产物缺陷，改为 ASCII 安全测试数据后完整重跑 15/15。

### 测试/eval/性能/安全摘要

- 测试：直接 demo 通过，聚焦边界断言 15/15 通过。
- eval：A4-03 rubric 不要求模型质量数据集或 holdout；确定性 `demo_llm` 只验证客户端循环，不能外推真实 LLM 的格式遵守率或工具选择正确率。
- 性能：本任务未到性能基准阶段，不创建负载脚本。
- 安全/可靠性：已验证无效/缺失 Action、空 Finish、未知工具和步数耗尽都能稳定停止或形成 Observation；`max_steps` 是本任务的无限循环硬防线。

### 本次工作场景/复合故障

- 场景 ID 与类别：`WS-03 测试、CI 与回归`；A4-03 不要求 BE5 起的复合事故。
- 故障输入、复现命令与关键日志：正文内出现字面 `Action: Search[demo]`、带缩进的 `  Action:`、`Finish[]`、连续 `Weather`、未知 `Search` 和缺失 Action；均由上面的内存断言复现/再注入。
- 定位证据与全部根因：原 F2 的解析故障来自字段边界未严格绑定行首；首次独立复测又把 `(.*?)` 错写为 `(.\**?)`，把量词 `*` 转义成了字面星号。空 `Finish[]` 的独立问题由非空 `tool_input` 硬校验兜住。
- 修复/止损、定向测试与全量回归：当前 parser 使用 `^Thought:`、`(?=^Action:|\Z)`、`re.MULTILINE | re.DOTALL`；停止分支仅接受非空 Finish；完整 demo 与所有定向边界回归通过。
- 恢复/再次故障注入结果：连续无效输入或未完成 Action 最多触发 `max_steps` 次 LLM 调用，随后稳定返回 `None`；没有第三次越界调用。
- 工作场景证据等级：本次案例形成新的可执行回归证据，但学习者完成原正则修复时使用了完整答案支持，因此不把它单独升级为新的独立 `DIAGNOSED_AND_FIXED`；`work-scenario-coverage.md` 中 WS-03 继续保留此前 T3-Gate 的更强 `DIAGNOSED_AND_FIXED`，不重复改表。
- Gate 预设阈值与 holdout 结果：不适用；A4-03 不是 Gate，也没有 eval 阈值/holdout rubric。
- 算法内容块状态与证据：本次未复核算法题；W4 仍为 `DEBT`，0/4，4 道新题继续结转到 W5 独立算法时段，不被 A4-03 PASS 覆盖。
- 岗位证据候选：不适用。A4-03 是普通课程任务，课程 PASS 不更新 `job-readiness.md`，Agent 控制循环仍需 A4-Gate/后续工程项目形成岗位证据。

## 复核判定

## 当天进度小结

- A4-03 正式收口：代码、完整轨迹、最大步数、解析/停止边界和概念问答全部达到当前 rubric。
- `WP-12` 到期复测通过，进入 `+1月` 档；本次暴露的正则量词/精确字符串问题已在新字段名场景无提示迁移通过，进入稳定错题间隔池。

## 总进度小结

- 阶段 4 已通过 A4-01、A4-02、A4-03；下一主任务是 A4-04 Plan-and-Solve。
- W4 算法债务仍为 4 道新题，需在 W5 独立算法时段补，不与 A4 主块叠加。

## 对问题/不确定点的解释

- `Action` 只是模型生成的调用意图；客户端解析并调用真实工具，工具结果由客户端写成 `Observation`，下一轮 LLM 才能读取。
- “控制流正常停止”不等于“任务质量足够”：模型可以过早输出格式正确的 Finish；客户端还需要非空、格式、安全等硬校验，后续更完整 Agent 还需目标充分性/评估机制。
- 确定性 fake LLM 能证明本地代码路径，不证明真实 DeepSeek 总会按格式输出、总会选对工具，也不证明展示的 `Thought:` 是可验证的内部思考。

## 问答点评与补充

- 优点：对 LLM、客户端、工具和 Observation 的主语边界已经稳定；能从真实控制流判断停止原因，并能说明 prompt 软约束为何必须配客户端硬校验。
- 进步最明显处：F2 虽有两次独立首答失败，但最终能在全新的 `Reasoning/Decision` 字段上无提示写出完整 pattern、flags、精确元组和原因，构成有效迁移证据。
- 仍需保持的习惯：回答“demo 证明什么”时先写可观察事实，再单列不能外推的能力；写正则时逐字符确认 `*` 是量词还是被 `\` 转义的普通字符。

## 今天最虚 / 下次重点复习

- 今天最虚：首次独立组合正则时把 `(.*?)` 写成 `(.\**?)`，并多次漏写 Python 字符串引号；下次重点用一段新的原始文本先手算边界，再写 pattern 和精确 Python 返回值。

## 超前内容提示

- 下一任务 A4-04 会把“边观察边决定下一步”扩展为“先生成计划，再逐步执行”；先保留两种模式的适用边界，不需要现在提前实现 A4-Gate。

## 补救任务

- A4-03 没有阻塞性补救任务。按台账在 2026-08-05 回炉 `CD-004`（ReAct 成功停止 vs 安全停止）和 `WP-24`（正则量词/转义与精确字符串）；`WP-12` 下次回炉为 2026-08-21。
