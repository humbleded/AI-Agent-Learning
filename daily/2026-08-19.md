# 2026-08-19 学习记录

- 当前任务：`A4-Gate` 研究摘要 Agent
- 当前学习单元：`A4-Gate-C4b-1｜Action correction`（本日教学 / 实现子单元已收口）
- 任务状态：`DOING`（沿用 `tracker/progress.md`；本页只记录教学与实现接力，不产生父任务正式判定）
- 今日计划学习预算：单块 180 分钟——定向阅读 25 分钟、控制流判断 20 分钟、用户实现 95 分钟、无落盘故障注入与回归 30 分钟、用户审核 10 分钟；助手读取资料、创建骨架、执行命令、等待模型/工具和自动写回不计入计划预算
- 实际学习时长：不记录

## 当日 Session / 任务索引

| Session | 任务 | 环节 | 关联前序 daily | 当前状态 / 下一步 |
| --- | --- | --- | --- | --- |
| S1 | `A4-Gate-C4b-1` | 非法 Action 的受限纠错与停止预算 | [2026-08-05](2026-08-05.md)、[2026-08-06](2026-08-06.md)、[2026-08-07](2026-08-07.md)、[2026-08-08](2026-08-08.md)、[2026-08-09](2026-08-09.md)、[2026-08-10](2026-08-10.md)、[2026-08-11](2026-08-11.md)、[2026-08-12](2026-08-12.md)、[2026-08-13](2026-08-13.md)、[2026-08-17](2026-08-17.md)、[2026-08-18](2026-08-18.md) | 本日教学 / 实现子单元已收口；`G1 / I1a～I2d / U1 = PASS`（其中 I1h 经 R1～R4 收口）；父 `A4-Gate` 仍为 `DOING` |

## 任务证据接力

### Session S1｜`A4-Gate-C4b-1`

任务状态与学习单元：父任务 `A4-Gate = DOING`。本 Session 只实现“未注册工具 / 坏 JSON / Schema 或参数错误 → 不执行真实工具 → 在受限 step 内把安全校验错误交回模型修正 Action → 无法形成可完成链时进入 `needs_manual`”。候选摘要 / Reflection / 最终 validator 失败恢复、HITL、14 条 eval 和 Gate 正式复核不在本单元。

关联前序 daily：上表列出的 11 份 A4-Gate 证据。8 月 14～16 日没有学习记录，不补建空白 daily。

资料与版本：

- `code/stage4/problem-contract.md` 第 6 节“失败、恢复与停止分支”，重点为非法 Action 不执行工具、剩余 step 内允许修正、停止后映射 `needs_manual`。
- `code/stage4/a4_gate_research_summary_agent.py` 的 `execute_tool_call()`、`generate_candidate_summary()`、`run_research_summary_once()`；当前仓库基线 `HEAD 1434adcdd6af3ab14c55dff547c08baea9059340`，该 Agent 文件与 `daily/2026-08-18.md` 均有必须保留的未提交既有改动。
- DeepSeek Tool Calls 与 Chat Completion 官方文档，2026-08-19 核验：模型返回 `tool_calls[].function.arguments`，参数可能不是合法 JSON 或含 Schema 外字段，客户端必须在执行真实工具前校验；Tool Observation 使用原 `tool_call_id` 接力。当前仍使用普通非 thinking、非 Beta strict 路径。

已经通过、不得换措辞重复考察：

- C1 请求输入、C2 两工具、C3a Schema/注册表、C3b 白名单/JSON/Schema/请求绑定校验。
- C3c 原生 Tool Calling、assistant 原消息与 Tool Observation 回填、topic/path 真实正常链。
- C3d 来源白名单、Reflection、Refinement、最终 validator 与真实双路径。
- C4a-1 单请求 `run_context`、七字段日志、五步正常链、请求隔离和 `MAX_STEPS=6` 调用前硬门。
- C4a-2a 首败后同参数一次重试并恢复；C4a-2b1 二次可重试失败 / 重试前无 step → `needs_manual`；C4a-2b2 真实不可重试失败 → `tool_failure`、搜索零证据 → `insufficient_evidence`。

本单元唯一新增判断：正常链在合法 Action 之后仍需真实工具、候选模型、Reflection、Refinement 共 4 次调用。第一次 Action 非法时，它所属的模型调用已占 step 1，但校验失败不占工具 step；纠错模型调用将占下一 step。因此，纠错后再次非法时，“继续纠错直到数字上恰好达到 `MAX_STEPS`”与“为一条可完成的成功链预留后续 4 次调用”发生冲突，必须由用户明确选择停止策略，不能由助手静默决定。

用户 / 助手职责：

- 用户必须亲自决定上述停止策略、解释调用预算，并在策略冻结后完成核心控制流代码。
- 助手根据已通过合同整理最小状态表与 TODO 骨架，准备无落盘 fake/spy，执行验证并记录原始证据；不替用户写核心答案。
- 用户最终审核调用次数、step / logs、真实工具副作用和公开三字段是否符合意图。

回炉与算法 preflight：`tracker/weak-points.md` 持久状态为 `NORMAL`。截至 2026-08-19 到期项为 `CD-005 / WP-08` 共 2 项、最老逾期 6 天，未触发强制回收块；本编码 Session 不追加正式旧题。W4 算法已 `PASS`、债务 0，今天不启动新算法块。

## 定向讲解与即时判断

### `A4-Gate-C4b-1-G1`｜纠错预算与停止策略

已知调用口径：

1. 正常链：Action 模型 1 → 真实工具 2 → 候选模型 3 → Reflection 4 → Refinement 5。
2. 非法 Action 是一次已经发生的模型响应，所以该模型调用照常占 step、写模型日志。
3. 客户端在工具执行前拒绝非法 Action：真实工具 0 次、工具 step 0、工具日志 0、工具副作用 0。
4. 向模型反馈校验错误并请求新 Action，是新的真实模型调用，会占下一 step、写一条模型日志。
5. 一旦得到合法 Action，仍需要后续 4 次调用才能形成最终可信三字段结果。

需要用户决策：若 step 1 的 Action 非法、step 2 的纠错 Action 仍非法，选择并解释下列一种 V1 策略：

- 策略 A（字面耗尽）：只要 Action 仍非法，就继续发起 Action correction，直到模型调用把 `step` 推到 `MAX_STEPS=6`；如果晚到的合法 Action 已没有足够的 4 个后续调用预算，则在执行工具前停止并进入 `needs_manual`，并把这个“合法但无法完成全链”的新增终点补入合同。
- 策略 B（可完成性优先）：step 2 再次非法时，因剩余 4 步不足以容纳“再纠错 1 次 + 合法 Action 后 4 次调用”，立即在工具前进入 `needs_manual`；同时把合同补成“必须为完整下游链预留预算 / V1 最多一次 Action correction”，不再声称一定等到数字上的 step 6 才停止。

回答还需分别给出两条轨迹的预期：

1. `非法 Action → 纠正为合法 Action → success`：模型调用次数、真实工具次数、最终 step 与日志事件顺序。
2. `非法 Action → 纠正后仍非法 → needs_manual`：按所选策略给出模型调用次数、真实工具次数、最终 step 与为何此处停止。

可观察完成标准：选择与理由一致；两条轨迹区分模型调用、工具调用、step、日志和副作用；明确指出合同需要补充的停止语义——策略 A 要补“晚到合法 Action 但下游预算不足”，策略 B 要补“为完整链预留预算 / 最多一次 Action correction”。当前状态：教学判断已完成，工程执行证据留给 `I1a` 及后续接线。

原始作答：

> 我选择策略 B，因为当 step 2 的纠错仍然非法时，虽然还剩 4 步，但剩下的步骤不支持完成剩下的调用

澄清往返：

- 用户追问：`哪种更贴近实际中使用的`。
- 教练结论：生产实践更接近策略 B 的原则，但通常不是删除全局 `MAX_STEPS`，而是采用双层预算：`MAX_STEPS` 继续作为整次请求的硬保险丝，Action correction 再有自己的局部上限 / 剩余预算门。全局上限是“绝不能超过”，不是“每次失败都要尽量烧到上限”。
- 推荐的 V1 行为：保留 `MAX_STEPS=6`；新增“最多一次 Action correction”或等价的完成性检查。当前 step 之后，只有在 `remaining_steps >= 1 次纠错模型调用 + 4 次合法 Action 后续调用` 时才允许继续纠错；得到合法 Action 后，也必须确认仍有至少 4 个调用预算再执行真实工具。否则在工具前发出窄停止信号并公开映射 `needs_manual`。
- 生产理由：这会减少注定无法完成的模型调用、延迟和成本；防止合法 Action 来得太晚后先发生工具副作用、最终却在 Reflection / Refinement 前撞上硬门；同时仍由全局 `MAX_STEPS` 兜住实现缺陷或未预料循环。
- 官方校准：LangGraph 官方 Graph API 把主动读取 `RemainingSteps`、在接近上限前路由到 fallback 称为推荐的 proactive handling；其全局 recursion limit 仍保留为最终硬门。AWS SDK 官方将全局重试配额与单请求重试策略分层，配额不足时 fail fast；LangGraph fault-tolerance 也用节点级 `max_attempts` 与整体图执行边界组合。来源于 2026-08-19 核验的官方文档：`https://docs.langchain.com/oss/python/langgraph/graph-api`、`https://docs.langchain.com/oss/python/langgraph/fault-tolerance`、`https://docs.aws.amazon.com/sdkref/latest/guide/feature-retry-behavior.html`。
- 用户随后明确选择策略 B，并独立指出“剩余步数”必须按能否完成剩余调用判断，因此澄清阶段结束。

订正与补充：

- 正确点：选择与已确认的生产策略一致；理由准确地区分了“数值上还有 4 步”与“这 4 步能否支持一条完整可信链”。
- 精确化：step 2 的纠错 Action 仍非法时，若再纠错一次，还需要 `1 次纠错模型 + 真实工具 + 候选摘要 + Reflection + Refinement = 5` 次调用，但全局只剩 4 次，必然无法到达可信终态。
- 两条冻结轨迹由助手按已确定合同整理，不要求用户机械复述：
  - 首次非法 → 一次纠正合法 → success：模型 5 次、真实工具 1 次、最终 `step = 6`，日志事件 `M → M → T → M → M → M`；首次非法校验不产生工具 step / 日志 / 副作用。
  - 首次非法 → 一次纠正仍非法 → needs_manual：模型 2 次、真实工具 0 次、最终 `step = 2`，日志事件 `M → M`；因局部纠错额度已耗尽，且再纠错后的完整链至少还需 5 步，所以在真实工具前停止。
- 合同写回：保留 `MAX_STEPS = 6` 作为全局硬上限；新增 `MAX_ACTION_CORRECTIONS = 1` 与 `MIN_STEPS_AFTER_VALID_ACTION = 4`。请求纠错前必须能容纳 `1 + 4` 步，执行合法 Action 前必须还能容纳 4 步。

单题状态：`A4-Gate-C4b-1-G1 = PASS`（用户完成行为决策与核心因果解释；精确调用计数由助手依据已冻结合同整理，后续由真实实现轨迹验证）。这只是教学检查点，不等于父任务 `A4-Gate` 正式 PASS。

### `A4-Gate-C4b-1-I1a`｜Action correction 预算门

当前在做什么与作用：把 G1 的策略 B 变成一个无副作用的纯函数。它会在控制流发起纠错模型调用之前回答“局部额度与完整链预算是否同时足够”，避免先占 step 或产生副作用后才发现无法收口。

用户完成信号：

> 改完了 I1a 预算门

用户原始实现：

```python
if not type(current_step) is int or current_step < 1 or current_step > MAX_STEPS:
    raise ValueError("current_step 必须是位于 1 到全局上限之间的整数")
if not type(corrections_used) is int or corrections_used < 0 or corrections_used > MAX_ACTION_CORRECTIONS:
    raise ValueError("corrections_used 必须是位于 0 到局部上限之间的整数")

remaining_steps = MAX_STEPS - current_step
if corrections_used >= MAX_ACTION_CORRECTIONS or remaining_steps < 1 + MIN_STEPS_AFTER_VALID_ACTION:
    return False
return True
```

检查结论：

- 正确性：严格内建 `int` 校验会拒绝 `bool`；step 与 correction 两组范围均 fail-closed；局部额度和 `1 + 4` 完整链预算同时成立才返回 `True`。
- 边界：本地无落盘探针覆盖 6 个合法组合、15 个非法类型 / 越界组合，全部通过；严格返回 `bool`，重复调用前后常量未变化。独立只读复核扩到 12 个合法组合、19 个非法组合，共 `31/31 PASS`；唯一 `True` 域为 `(current_step=1, corrections_used=0)`。
- 纯函数：源码不接收或修改 `run_context`，不调用 `reserve_call_step`、日志、模型或工具。
- 机械清理：用户任务只要求实现函数体；助手在行为通过后删除已完成的 I1a `TODO` 骨架注释，未改动用户判断逻辑，因此不把遗留 TODO 误判为学习 RETRY。

单题状态：`A4-Gate-C4b-1-I1a = PASS`。这是实现子检查点 PASS，不等于父任务 `A4-Gate` 正式 PASS。

### `A4-Gate-C4b-1-I1b`｜安全校验错误 Observation

当前在做什么：当 Action 在客户端预检阶段被拒绝时，构造一条与原 `tool_call_id` 配对的 `role="tool"` 消息，把安全、具体的校验错误交回模型。它是协议级 Observation，不是一次真实工具执行。

为什么需要：原 assistant 消息已经声明了一次 tool call；若要让模型在下一次调用中纠正它，消息历史必须同时包含这条完整 assistant tool call 和与同一 ID 对应的 Tool Observation。这里若误写真实工具日志或混入原始参数，会同时破坏调用账本与敏感信息边界。

当前用户任务：只实现 `build_action_validation_observation()`；严格校验非空 `tool_call_id` 与非空、最多 300 字符的内部安全错误，返回固定外层三字段消息，并把固定四字段内容编码成无 ASCII 转义的 JSON。不得调用模型、工具、占 step 或写日志。

用户完成信号：

> 改完了 I1b Observation

用户原始实现中的关键部分：

```python
if not type(tool_call_id) is str or not tool_call_id.strip():
    raise ValueError("tool_call_id 必须是非空字符串")
tool_call_id = tool_call_id.strip()

if not type(validation_error) is str or not validation_error.strip():
    raise ValueError("validation_error 必须是非空字符串")
validation_error = validation_error.strip()
if len(validation_error) > MAX_ACTION_VALIDATION_ERROR_CHARS:
    raise ValueError(f"validation_error 长度不得超过 {MAX_ACTION_VALIDATION_ERROR_CHARS}")

content = json.dumps({
    "ok": False,
    "error_type": "action_validation_error",
    "message": validation_error,
    "instruction": "请只返回一个符合 Schema 与规范化请求的修正工具调用"
}, ensure_ascii=False)

return {
    "role": "tool",
    "tool_call_id": tool_call_id,
    "content": content,
}
```

首次检查：

- 已通过：外层 exact keys、固定 `role="tool"`、`content` 为字符串、JSON 四字段、`ok=False`、固定 `error_type`、错误文本 trim、instruction 语义、`ensure_ascii=False`、300 字符上界、11 个非法类型 / 空白 / 301 字符输入的 `ValueError`，以及源码所示零 step / 日志 / 模型 / 工具调用。
- 唯一行为缺口：函数先正确地用 `tool_call_id.strip()` 判空，却又执行 `tool_call_id = tool_call_id.strip()`。输入 `"  call_exact_link  "` 时，原 assistant tool call 的 ID 与返回 Observation 的 ID 不再精确相同，会破坏协议关联。这里只能用 strip 判空，返回时必须保留原字符串。
- 机械清理：`git diff --check` 另发现函数 TODO 后有一处 trailing whitespace；这不是概念缺口，但须随本次最小修正一并清掉。独立的单行 `#` 也无信息价值，可以删除。

可观察完成标准：删除对 `tool_call_id` 的覆盖赋值，保留现有判空；`validation_error` 仍应 trim。清除行尾空格后，原 ID 精确关联测试、其余回归、`py_compile` 与 `git diff --check` 全部通过。

单题状态：`A4-Gate-C4b-1-I1b = RETRY`（`9/10` 组行为检查通过；只修原 ID 关联这一处，不扩范围、不创建下一个骨架）。

R1 用户完成信号：

> 改完了 I1b-R1

R1 修正证据：用户删除了 `tool_call_id = tool_call_id.strip()`，保留 `.strip()` 仅用于非空判断；`validation_error` 的 trim、长度门和 JSON 构造均未改动。输入 `"  call-keep-exact  "` 后，Observation 现在逐字符返回同一原 ID。

R1 复核：

- 本地无落盘协议矩阵 `15/15 PASS`：外层 / 内层 exact keys、原 ID 精确关联、JSON 可逆、固定状态、错误 trim、instruction 语义、中文不转义、1 / 299 / 300 上界、特殊字符往返、13 个非法输入以及 patched 执行原语零触发全部通过。
- 独立只读复核重复覆盖 16 个空白 / 错类型 / `str` 子类输入和零副作用，行为无失败；`py_compile` 通过。
- 助手只机械删除已完成的 I1b TODO 注释与 trailing whitespace，未修改用户函数逻辑；清理后 `git diff --check` 退出码 `0`。

最终单题状态：`A4-Gate-C4b-1-I1b = RETRY → PASS`。用户证明能区分“正文可规范化”与“协议关联 ID 必须原样保留”；这仍只是实现子检查点，不等于父任务 `A4-Gate` 正式 PASS。

### `A4-Gate-C4b-1-I1c`｜一次 correction 模型调用与记账

当前在做什么：消费后续编排层已经准备好的 correction 消息历史，发起恰好一次真实模型 API 调用，并让这次调用像其他模型调用一样占一个共享 step、写一条固定七字段日志。成功后只原样返回尚未验证的响应对象。

为什么需要：I1a 已证明“是否允许花这一步”，I1b 已证明“如何构造安全错误 Observation”；I1c 只把这份历史交给模型并维护调用账本。把响应解析和工具执行继续留在函数外，可以分别证明“调用确实发生并正确记账”与“返回 Action 是否可信”，避免一次函数同时承担策略、协议、执行和公开结果映射。

消息输入合同：`correction_messages` 必须是非空内建 `list`，元素可混合普通消息字典和 SDK assistant message 对象。调用点最终必须按 `system → user → 原始完整 invalid assistant message → 同 ID 的 I1b role=tool Observation` 排列；I1c 不重排、不复制、不追加，也不在内部重新验证每个元素。

当前用户任务：只实现 `request_action_correction()`。输入非法须在创建 client 与占 step 前 `ValueError`；合法时复用现有 DeepSeek 配置、Schema、step、计时与七字段日志模式，调用 `client.chat.completions.create()` 恰好一次。成功原样返回 response；API 异常写异常类型日志并原对象上抛。

调用参数合同：`model=DEEPSEEK_MODEL`、`messages=correction_messages`、`tools=build_tool_schemas()`、`stream=False`、`extra_body={"thinking": {"type": "disabled"}}`。本检查点不加 candidate 使用的 `tool_choice="none"`，也不新增 `tool_choice="required"`、`response_format` 或 `max_tokens`。

明确非目标：不调用 I1a 预算门或维护 `corrections_used`（后续编排层在调用前负责）；不构造 I1b Observation；不解析 `choices / finish_reason / tool_calls / name / arguments`；不执行工具、重试、candidate、Reflection、Refinement；不映射 `needs_manual` 或其他公开三字段结果；不重构现有已 PASS 的模型调用代码。

可观察完成标准：fake 正常返回时 create 恰好 1 次、返回对象 identity 不变、输入列表 identity / 内容 / 顺序不变、step / logs 从 1 / 1 精确到 2 / 2 且新日志为成功 model_call；fake API 抛异常时仍到 2 / 2、记录异常类名并原异常上抛；step 6 硬门或非法消息输入时 create=0、账本和消息不变；工具及后续入口均零触发。当前状态：`DOING / 待用户实现`。

用户完成信号：

> 改完了 I1c

用户原始实现要点：先以严格非空内建 list 守住输入；`client is None` 时复用工厂；随后 reserve step、计时并以首次 Action 相同参数调用模型；成功 / 异常分别写七字段 model_call 日志，成功原样返回 response，异常以裸 `raise` 原对象传播。未解析响应或触发任何工具 / 后续流程。

检查结论：

- 本地无落盘 fake / spy `22/22 PASS`：成功响应 identity、create 恰好一次、exact kwargs、messages identity / 四元素身份 / 顺序不变、Schema、stream / thinking、step / logs 1 / 1 → 2 / 2、七字段成功日志全部通过。
- API sentinel 异常保持同一对象上抛，仍精确到 2 / 2 并记录异常类名；step 6 时保留原 RuntimeError、create=0、账本 / 消息不变；4 类非法消息外壳在 client 工厂与 reserve 前 ValueError。
- 独立只读复核扩大到 `43/43 PASS`，另覆盖合法 `client=None` 工厂路径、更多非法容器及所有工具 / 后续入口 bomb 零命中；`py_compile`、`git diff --check` 均通过。
- 助手只机械删除已完成的 I1c TODO 注释，未修改用户调用 / 记账逻辑。

单题状态：`A4-Gate-C4b-1-I1c = PASS`。这证明的是一次 correction 模型调用与账本，不证明 response 内已有合法 Action，也不等于父任务 `A4-Gate` 正式 PASS。

### `A4-Gate-C4b-1-I1d`｜可复用的单 Action 响应外壳解析器

当前在做什么：把首次 Action 和 correction Action 共同需要的 SDK 响应结构守门提炼成一个纯 helper。它只回答“响应里是否有恰好一个结构明确、带可关联 ID 的 Tool Action”，并返回原始未信任字段；本检查点仍不执行工具。

为什么需要：I1c 成功只证明模型 API 返回了某个对象，不能把它直接当作合法 Action。结构错误（没有 choices、finish reason 错、0 / 多 tool calls、缺 ID / function）说明连一个可关联 Action 都不存在，应显式 RuntimeError；内容错误（未知工具、坏 JSON、Schema / 路由 / 值漂移）则必须原样交给 `execute_tool_call()`，由它产生可回填给模型的具体安全错误。若 parser 抢先做 JSON / 注册表校验，就会重复职责并切断既定 correction 路径。

函数合同：`parse_single_tool_action_response(response)` 成功时严格返回 `assistant_message`、`tool_call_id`、`tool_name`、`raw_arguments` 四键。assistant message 保持 SDK 原对象 identity；ID 只判空不 trim；name 和 arguments 保持原始未信任值。

结构硬门：按 `response → choices → choice.finish_reason / message → message.tool_calls → tool_call.id / function → function.name / arguments` 顺序检查。choices 和 tool_calls 都必须是内建 list 且恰好一个；finish reason 必须是内建字符串 `"tool_calls"`；ID 必须是非空内建字符串；function / name 必须存在且 name 非 None；arguments 属性必须存在，但值为 None 仍应透传。所有结构缺失统一显式抛 `ToolActionProtocolError`，它是 `RuntimeError` 的窄子类；错误文本不得拼入原始 arguments、prompt 或模型正文，也不得泄漏 AttributeError / IndexError / TypeError。

内容透传边界：name 已存在且非 None 后，非字符串、空白或未知工具名都不在 I1d 拦；arguments 属性存在后，None、非字符串、空白、坏 JSON、非对象或 Schema 错误都不在 I1d 拦。I1d 不 `json.loads`、不查 `TOOL_REGISTRY` / Schema、不比对 normalized request。

明确非目标：不调用 client / 模型、I1a / I1b、step / 日志、`execute_tool_call`、真实工具、candidate / Reflection / Refinement；不构造 Observation、`needs_manual` 或任何公开三字段结果；不检查 assistant content / role；暂不替换 `generate_candidate_summary()` 现有解析块。

可观察完成标准：正常 fake 返回 exact 四键、assistant identity 与带空格 ID 原样保留；name=123、arguments=None / 坏 JSON 的代表必须解析成功并原值透传；结构错误与 choices / tool_calls 错容器稳定抛 RuntimeError；所有输入对象不变，模型 / 工具 / step / 日志及后续入口零触发。当前状态：`DOING / 待用户实现`。

用户完成信号：

> 改完了 I1d

用户原始实现：

```python
if not isinstance(response, dict):
    raise ToolActionProtocolError("Response must be a dict.")
try:
    choices = response["choices"]
    if not isinstance(choices, list) or len(choices) != 1:
        raise ToolActionProtocolError("Response must have exactly one choice.")
    choice = choices[0]
    finish_reason = choice["finish_reason"]
    if finish_reason != "tool_calls":
        raise ToolActionProtocolError("Finish reason must be 'tool_calls'.")
    message = choice["message"]
    tool_calls = message["tool_calls"]
    if not isinstance(tool_calls, list) or len(tool_calls) != 1:
        raise ToolActionProtocolError("Message must have exactly one tool call.")
    tool_call = tool_calls[0]
    tool_call_id = tool_call["id"]
    if not type(tool_call_id) is str or not tool_call_id.strip():
        raise ToolActionProtocolError("Tool call ID must be a non-empty string.")
    tool_name = tool_call["function"]["name"]
    raw_arguments = tool_call["function"]["arguments"]
except KeyError as e:
    raise ToolActionProtocolError(f"Missing key: {e}")

return {
    "assistant_message": message,
    "tool_call_id": tool_call_id,
    "tool_name": tool_name,
    "raw_arguments": raw_arguments,
}
```

首次检查：

- 做对的边界：函数仍是纯 parser，没有触发模型、预算门、Observation builder、step / 日志、真实工具或后续摘要链；没有解析 JSON、查询注册表 / Schema 或比对规范化请求；在当前 dict fake 路径中保持了 assistant message identity、原始带空格 ID 与 raw arguments，没有泄漏原始 prompt / arguments 到固定错误文本。`py_compile` 与 `git diff --check` 均通过。
- 决定性缺口：真实 DeepSeek / OpenAI SDK 响应是属性对象，现有 Agent 也一直按 `response.choices[0].message` 读取；当前实现却先要求 `response` 是 dict，并全程使用 `[...]`。合法 SDK-like 属性对象因此立即得到 `ToolActionProtocolError: Response must be a dict.`，I1c 返回的真实响应与首次 Action 响应都无法被该 helper 解析。
- 同类结构门缺口：`isinstance(..., list)` 会接受 list 子类；只比较 finish reason 的值会接受值相等的 str 子类；`message / tool_call / function is None` 会从下标访问泄漏普通 `TypeError`；`function.name is None` 会被当作成功结果返回。它们分别违反严格内建容器 / 值门、结构错误统一窄异常和 name 非 None 合同。
- 本地无落盘探针：SDK 属性对象与三个内容透传代表共 `27/31 PASS`，4 个合法 / 应透传场景都因错误的 dict 外壳门失败；针对当前 dict 路径的严格结构补充探针 `0/8 PASS`，精确暴露 list / str 子类、四类 None 异常泄漏与 name None 放行。两路独立只读复核结论一致，其中完整矩阵报告 13 个行为失败；所有副作用 bomb 均为 0。

最小修正范围 `I1d-R1`：只重写当前解析守门段，按 SDK 属性层级显式检查 `response.choices → choice.finish_reason / message → message.tool_calls → tool_call.id / function → function.name / arguments`；choices / tool_calls 使用严格内建 list 且恰好一个，finish reason 使用严格内建 str 且精确为 `"tool_calls"`。message、tool call、function 与 name 要先守存在性 / 非 None；arguments 只守属性存在，值为 None 仍透传。每个结构缺口直接抛 `ToolActionProtocolError`，不要依赖下标异常或 broad catch。保持四键返回、assistant identity、ID 原样、name 非 str / 空 / 未注册和任意 arguments 的内容透传；不新增 role / content / tool-call type 检查。

单题状态：`A4-Gate-C4b-1-I1d = RETRY`。当前 TODO 不清理、不创建后续骨架；等待用户完成同一检查点的 R1。

R1 用户完成信号：

> 改完了 I1d-R1

R1 用户原始实现中的解析守门：

```python
if not response:
    raise ToolActionProtocolError("Response must not be empty.")
try:
    if not hasattr(response, "choices"):
      raise ToolActionProtocolError("Response must have 'choices' key.")
    choices = response.choices
    if not type(choices) is list or len(choices) != 1:
        raise ToolActionProtocolError("Response must have exactly one choice.")
    choice = choices[0]
    if not hasattr(choice, "finish_reason"):
        raise ToolActionProtocolError("Choice must have 'finish_reason' key.")
    finish_reason = choice.finish_reason
    if finish_reason != "tool_calls":
        raise ToolActionProtocolError("Finish reason must be 'tool_calls'.")
    if not hasattr(choice, "message"):
        raise ToolActionProtocolError("Choice must have 'message' key.")
    message = choice.message
    if not hasattr(message, "tool_calls"):
        raise ToolActionProtocolError("Message must have 'tool_calls' key.")
    tool_calls = message.tool_calls
    if not type(tool_calls) is list or len(tool_calls) != 1:
        raise ToolActionProtocolError("Message must have exactly one tool call.")
    tool_call = tool_calls[0]
    if not hasattr(tool_call, "id"):
        raise ToolActionProtocolError("Tool call must have 'id' key.")
    tool_call_id = tool_call.id
    if not type(tool_call_id) is str or not tool_call_id.strip():
        raise ToolActionProtocolError("Tool call ID must be a non-empty string.")
    if not hasattr(tool_call, "function"):
        raise ToolActionProtocolError("Tool call must have 'function' key.")
    if not hasattr(tool_call.function, "name"):
        raise ToolActionProtocolError("Function must have 'name' key.")
    if not hasattr(tool_call.function, "arguments"):
        raise ToolActionProtocolError("Function must have 'arguments' key.")
    tool_name = tool_call.function.name
    raw_arguments = tool_call.function.arguments
except KeyError as e:
    raise ToolActionProtocolError(f"Missing key: {e}")
```

R1 检查：

- 已修复：解析已切换到真实 SDK-like 属性层级；合法响应 exact 四键、assistant identity、原 ID、重复稳定与输入不变通过。choices / tool_calls 已严格限制为内建 list 且恰好一个；message / tool call / function 的缺失或 None、ID 的错类型 / 空白 / str 子类、arguments 属性缺失等结构错误都收敛为窄异常。name=123 / 空 / unknown 与 arguments=None / 非 str / 坏 JSON 均原样透传，零副作用边界保持。
- 剩余行为缺口 1：finish reason 仍只比较值，`str` 子类 `"tool_calls"` 会通过；必须同时守住严格内建 str 与精确值。
- 剩余行为缺口 2：仅检查 name 属性存在，`function.name is None` 仍会被返回；本合同只拒绝缺失 / None，非 str、空白或未知工具名继续透传。
- 守门精确性：`if not response` 会调用任意属性对象的真值协议，把结构完整但 falsey 的对象误判为空；这里只应拒绝 `response is None`。本地完整探针因此另暴露一个 falsey 合法对象失败。
- 机械遗留：全部改成属性读取后，旧 dict 路径的 `try / except KeyError` 已无合理职责，而且会把 getter 的 KeyError 文本拼入错误；R2 删除该层，继续使用逐层固定错误文本，不改成 broad catch。
- 本地无落盘矩阵 `34/37 PASS`，失败精确为 falsey 合法属性对象、finish reason str 子类和 name None；独立矩阵确认正常 / 六类内容透传、输入不变、28/30 核心结构等价类与所有副作用边界通过，剩余冻结行为缺口为 finish reason 类型门和 name None。`py_compile`、`git diff --check` 均通过。

单题状态：`A4-Gate-C4b-1-I1d = RETRY → RETRY（R1）`。R2 只改上述三个窄守门并删除无用 KeyError 包装；TODO 继续保留，不创建后续骨架。

R2 用户完成信号：

> 改完了 I1d-R2

R2 用户原始修正：

```python
if response is None:
      raise ToolActionProtocolError("Response must not be empty.")

# ...沿既有 SDK 属性层级逐项守门...

if type(finish_reason) is not str or finish_reason != "tool_calls":
    raise ToolActionProtocolError("Finish reason must be 'tool_calls'.")

# ...确认 function 具有 name / arguments 属性...

tool_name = tool_call.function.name
if tool_name is None:
    raise ToolActionProtocolError("Function name must not be None.")
raw_arguments = tool_call.function.arguments
```

用户同时删除了旧 dict 实现遗留的 `try / except KeyError`，保留逐层固定 `ToolActionProtocolError`，未新增 broad catch。

R2 最终复核：

- 本地最终无落盘矩阵 `40/40 PASS`：普通合法与 falsey 合法属性对象、六类未信任内容透传、全部结构错误等价类、exact 四键、assistant identity、原 ID、重复稳定、输入不变、安全错误文本与零副作用全部通过。
- 独立完整矩阵：合法 / 内容透传 `8/8 PASS`，每例两次解析与逐层输入 identity / value 不变；结构错误 `30/30 PASS`，每例均为 exact `ToolActionProtocolError`；模型、工具、step、日志、预算门、I1b 与后续流程 bomb 命中 0。
- R2 三处目标均闭合：response 只拒绝 None；finish reason 同时守内建 str 与精确值；name None 拒绝而 arguments None 继续透传。没有新增 role / content / tool-call type / SDK 具体类或 Action 内容校验。
- `py_compile` 与 `git diff --check` 均退出 `0`；仅剩已完成 TODO、一个过深缩进和一个多余空行，均由助手在行为通过后机械清理，不修改用户逻辑。
- 机械收口：助手删除已完成的 I1d TODO 注释、规范 `response is None` 分支缩进、删除多余空行，并把模块状态文字从“当前进行”改为“当前已完成”；判断式、固定错误文本与四键返回均未改。清理后 smoke `7/7 PASS`，`py_compile` / `git diff --check` 再次通过，`rg` 确认不存在本任务 TODO / `NotImplementedError`。

最终单题状态：`A4-Gate-C4b-1-I1d = RETRY → RETRY（R1）→ PASS（R2）`。这证明可复用响应外壳 parser 的孤立行为，不等于 action correction 已接线，也不等于父任务 `A4-Gate` 正式 PASS。

### `A4-Gate-C4b-1-I1e`｜一次 Action correction 的最小编排

当前在做什么：把已经分别通过的 I1a 预算门、I1b 安全 Observation、I1c 一次模型调用与 I1d 响应解析组合成一个独立 helper。它接收“首次 Action 已被 executor 在真实工具执行前拒绝”的内部事实，返回一份可继续使用的纠错历史与一个仍未信任的 corrected Action。

为什么需要独立 helper：如果现在直接把半条分支塞进 `generate_candidate_summary()`，要么临时改变该函数既有返回合同，要么被迫顺手执行修正工具。独立 helper 可以先证明消息顺序、预算、模型记账和响应接力正确，并精确停在 corrected `execute_tool_call()` 之前；下一检查点再处理真实工具副作用与再次非法停止。

用户当前任务：实现 `request_and_parse_action_correction()`。输入 `base_messages` 是首次非法 Action 前的 system / user 历史；`invalid_action` 必须是 I1d 的 exact 四键结果；`validation_error` 是 executor 预检产生的安全错误；`corrections_used` 是请求局部计数，不写入只负责 step / logs 的 `run_context`。

消息与返回合同：不得修改 `base_messages` 或 `invalid_action`，而是浅复制并按 `原历史 → 原完整 invalid assistant → 同 ID 的 I1b Observation` 构造新列表；当前不追加 corrected assistant，因为它还没有配对真实 Tool Result。成功严格返回三键：

```python
{
    "correction_messages": correction_messages,
    "corrected_action": corrected_action,
    "corrections_used": corrections_used + 1,
}
```

其中 `corrected_action` 保持 I1d exact 四键，name / arguments 仍未信任；I1e PASS 不能说明修正 Action 内容合法。

预算停止：新增内部窄信号 `ActionCorrectionBudgetError`，避免复用现有 `AutoRecoveryExhaustedError` 后被公开入口误写成“工具重试上限”。I1a 返回 false 后只分类为 `max_action_corrections` 或 `insufficient_completion_steps`，不得重新抄一套预算计算；预算不足时模型 / 工具 / step / logs / 原消息全部不变，本检查点不映射公开 `needs_manual`。

异常与非目标：I1c API 异常已经占一步并写失败 model log，原对象继续传播；I1d 协议异常发生在成功模型调用后，保留成功 model log 并原样传播。I1e 不 broad-catch、不执行 corrected Action、不验证 corrected name / arguments、不进入工具 retry、candidate、Reflection / Refinement，也不调用真实 DeepSeek。

最小完成证据：

1. 成功：起点 `step/log=1/1, corrections_used=0`，结束 `2/2, corrections_used=1`，事件 `M → M`，真实工具 0；返回 exact 三键、内层 exact 四键、消息四段顺序 / identity 与同 ID Observation 正确。
2. 局部额度耗尽与完整链预算不足：分别发出两个窄 reason；client / I1c / I1d / 工具均 0 新调用，step / logs / 原输入不变。
3. API 异常：第二个模型 step / 失败日志保留，同一异常对象传播，工具 0。
4. correction 响应协议错误：第二个模型 step / 成功日志保留，exact `ToolActionProtocolError` 传播，工具 0。

当前状态：`DOING / 待用户实现`。这是当前唯一 active TODO。

用户完成信号：

> 改完了 I1e

用户原始实现：

```python
if not base_messages or not isinstance(base_messages, list):
    raise ToolActionProtocolError("base_messages must be a non-empty list.")
if not invalid_action or not isinstance(invalid_action, dict):
    raise ToolActionProtocolError("invalid_action must be a non-empty dict.")
if not validation_error or not type(validation_error) is str:
    raise ToolActionProtocolError("validation_error must be a non-empty string.")
if not type(corrections_used) is int or corrections_used < 0:
    raise ToolActionProtocolError("corrections_used must be a non-negative integer.")
if not run_context or not isinstance(run_context, dict):
    raise ToolActionProtocolError("run_context must be a non-empty dict.")
if client is not None and not isinstance(client, OpenAI):
    raise ToolActionProtocolError("client must be an instance of OpenAI or None.")
observation = build_action_validation_observation(
    invalid_action["tool_call_id"],
    validation_error,
)

budget = has_action_correction_budget(
    run_context["step"],
    corrections_used,
)

if budget:
    reason = "max_action_corrections"
else:
    reason = "insufficient_action_correction_budget"
    raise ActionCorrectionBudgetError(
        reason=reason,
        current_step=run_context["step"],
        corrections_used=corrections_used,
        last_error=validation_error.strip(),
    )
correction_messages = list(base_messages)

correction_messages.append(
    {
        "role": "assistant",
        "content": observation,
    }
)

next_corrections_used = corrections_used + 1
raw_response = request_action_correction(
    correction_messages,
    client=client,
    run_context=run_context,
)
corrected_action = parse_single_tool_action_response(raw_response)
return {
    "correction_messages": correction_messages,
    "corrected_action": corrected_action,
    "corrections_used": next_corrections_used,
}
```

首次检查：

- 已通过：I1b 与 I1a 被实际复用，没有重抄预算数学；成功返回 exact 三键、内层由 I1d 返回 exact 四键、`corrections_used` 可从 0 变 1。I1c API 异常保持第二个失败 model step / log 并原对象传播；I1d 协议异常保持第二个成功 model step / log 并原样传播。所有真实工具、candidate、Reflection、Refinement 与公开映射均未触发。
- 缺口 1（预算）：budget 为 false 时统一写入异常类不接受的 `insufficient_action_correction_budget`，两条预算轨迹都因此变成普通 `ValueError`；局部额度耗尽与完整链步数不足没有分别得到 `max_action_corrections` / `insufficient_completion_steps`。当前 `if budget:` 分支还把 max reason 放在允许继续的路径，语义位置反了。
- 缺口 2（消息协议）：新历史只有三段，用户把 I1b 的 tool Observation 塞进了手工新建的 assistant `content`；正确历史必须是四段 `base system/user → 原完整 invalid assistant 对象 → 独立 role=tool Observation`。当前既丢失原 assistant 的 tool_calls，也没有独立同 ID Tool Observation。
- 缺口 3（依赖注入）：`isinstance(client, OpenAI)` 会拒绝项目本就需要的 fake/spy client，导致无真实 API 的确定性验收无法进入 I1c；I1c 已负责消费可调用 client，I1e 不应添加具体 SDK 类门。
- 输入合同：base_messages 应为非空内建 list；invalid_action 应为内建 dict 且 exact I1d 四键。当前 list/dict 子类会通过，invalid_action 缺键泄漏 KeyError、额外键会继续调用模型；这些合同错误应在 I1c 前以 ValueError 拒绝。validation_error 与 corrections_used 的精确类型 / 范围继续交给已经通过的 I1b / I1a，避免并行维护第二套不一致校验。
- 本地五轨迹加依赖注入探针 `2/6 PASS`：API 异常、协议异常通过；成功消息顺序、两类预算异常和 fake client 失败。`py_compile` 通过；`git diff --check` 因用户实现中的 5 处 trailing whitespace 失败，待行为修正后机械清理。

最小修正 `I1e-R1`：保留从 `next_corrections_used` 到 exact 三键 return 的已通过后半段；只重写输入、budget false 分类与 correction_messages 四段组装。不得执行 corrected Action、添加 broad catch 或创建下一个 TODO。

单题状态：`A4-Gate-C4b-1-I1e = RETRY`。

I1e-R1 用户完成信号：

> 改完了 I1e-R1

用户原始 R1 改动要点（复核前快照）：

```python
if type(base_messages) is not list or not base_messages:
    raise ValueError("base_messages must be a non-empty built-in list.")
expected_action_keys = {
    "assistant_message",
    "tool_call_id",
    "tool_name",
    "raw_arguments",
}
if type(invalid_action) is not dict or set(invalid_action) != expected_action_keys:
    raise ValueError("invalid_action must be the exact I1d four-key result.")

# R1 时仍保留的两段重复校验：
if not validation_error or not type(validation_error) is str:
    raise ToolActionProtocolError("validation_error must be a non-empty string.")
if not type(corrections_used) is int or corrections_used < 0:
    raise ToolActionProtocolError("corrections_used must be a non-negative integer.")

observation = build_action_validation_observation(
    invalid_action["tool_call_id"], validation_error
)
budget = has_action_correction_budget(run_context["step"], corrections_used)
if not budget:
    if corrections_used >= MAX_ACTION_CORRECTIONS:
        reason = "max_action_corrections"
    else:
        reason = "insufficient_completion_steps"
    raise ActionCorrectionBudgetError(...)

correction_messages = list(base_messages)
correction_messages.extend([
    invalid_action["assistant_message"],
    observation,
])
```

R1 复核结果：

- 已通过核心五轨：成功时四段历史严格为 `system → user → 原 invalid assistant → 同 ID role=tool Observation`，消息对象 identity 保持；`step/log 1/1 → 2/2`、事件 `M → M`、计数 `0 → 1`、真实工具与下游调用 0。局部次数耗尽与完整链不足分别得到 exact `ActionCorrectionBudgetError(reason=max_action_corrections / insufficient_completion_steps)`，且无新模型、step 或日志。API 同一异常与 I1d exact 协议异常均按既有账本合同传播。
- 已通过输入外壳：`base_messages` 的空值、tuple、list 子类，以及 `invalid_action` 的 dict 子类、缺任一四键或多键，均在 I1c 前以 `ValueError` 拒绝；显式 duck fake client 可注入。corrected assistant 没有提前进入历史，也没有执行 corrected Action。
- 唯一行为缺口：I1e 仍在 I1b / I1a 之前自行校验 `validation_error` 与 `corrections_used`，使同一类调用者合同出现不一致异常。无落盘矩阵为 `17/21 PASS`：`validation_error=None/int`、`corrections_used=True/-1` 实际得到 `ToolActionProtocolError`，而已通过的 I1b / I1a 应统一接收这些输入并抛 `ValueError`；空白错误和超过局部上限的计数又会走到 I1b / I1a，证明当前存在两套漂移的校验。
- `py_compile` / 模块导入通过；`git diff --check` 只报告当前函数中的 3 处 trailing whitespace。TODO、缩进和尾空白继续留到行为 PASS 后由助手机械清理。

R2 最小订正：只删除 I1e 内对 `validation_error` 和 `corrections_used` 的两段重复 `if`；不得替换成另一套判断。调用顺序自然变为 `exact 外壳 → I1b（ID / validation_error）→ I1a（step / corrections_used）→ 预算分类或 I1c`。其余已经通过的预算 reason、四段消息、计数、I1c / I1d 与 exact 三键返回保持不动。

单题状态更新：`A4-Gate-C4b-1-I1e = RETRY → RETRY（R1）`。

I1e-R2 用户完成信号：

> 改完了 I1e-R2

用户原始 R2 修正：只删除了 I1e 内对 `validation_error` 和 `corrections_used` 的两段本地 `if`，没有用另一套判断替换；R1 已通过的 exact 外壳、I1b → I1a 顺序、两个预算 reason、四段消息、I1c → I1d、局部计数与 exact 三键返回均未改。

R2 复核结果：

- 本地无落盘矩阵 `21/21 PASS`：原五条核心轨迹 `5/5`，exact 外壳回归 `9/9`，I1b / I1a 所有权边界 `6/6`，零工具 / 下游 bomb `1/1`。
- 所有权 spy 已证明：`validation_error=None/int/blank` 均实际进入 I1b 一次、I1a / I1c 为 0，并由 I1b 抛 exact `ValueError`；`corrections_used=True/-1/>MAX` 均先进入 I1b，再进入 I1a 一次，I1c 为 0，并由 I1a 抛 exact `ValueError`。六例的 model / tool / step / log delta 均为 0，输入与上下文不变。
- 两份独立只读复核均为 PASS：五轨 `5/5`、所有权 `6/6`；成功路径保持外层 exact 三键 / 内层 exact 四键、四段消息 identity、`M → M` 和计数 `0 → 1`，两个预算停止、API 异常、协议异常与 corrected Action 零执行均无回归。
- 行为通过后，助手获准只删除已完成 I1e TODO、统一本函数缩进 / 空行、清除 3 处 trailing whitespace，并更新模块状态文字；机械清理不得修改判断、消息、reason、调用或返回合同。

最终单题状态：`A4-Gate-C4b-1-I1e = RETRY → RETRY（R1）→ PASS（R2）`。这只证明一次 Action correction 的预算、消息接力、模型记账与未信任响应解析，不等于 corrected Action 已执行，也不等于父任务 `A4-Gate` 正式 PASS。

### `A4-Gate-C4b-1-I1f`｜识别执行前 Action validation rejection

当前在做什么：新增纯 helper `extract_preexecution_action_validation_error()`，判断 `execute_tool_call()` 返回的 `ok=False / retryable=False` 究竟是“客户端在真实工具执行前拒绝了非法 Action”，还是“真实工具已经执行后返回不可重试失败”。两者的 payload 可能相同，不能只看 `retryable=False`。

为什么需要：只有前一种情况才能安全进入 I1e，把校验错误作为 Tool Observation 交回模型修正参数；后一种情况必须继续走已通过的 `tool_failure`，否则会把真实后端故障误当成模型参数错误。当前 executor 的关键来源证据是：所有客户端 Action 校验都发生在 `reserve_call_step()` 之前；真实后端调用则一定先占工具 step，并在成功 / 返回失败 / 抛异常时写一条工具日志。

用户当前任务：实现代码中的唯一 TODO。helper 只在以下条件全部成立时返回 `error.strip()`：

1. `tool_result` 是严格三键内建 dict，键集 exact `{ok, error, retryable}`；
2. `ok is False` 且 `retryable is False`；
3. `error` 是可清理成非空内容的内建 str；
4. 当前 `run_context["step"] == step_before`；
5. 当前 `len(run_context["logs"]) == logs_length_before`。

调用快照、上下文或结果形状无法安全证明上述事实时一律返回 `None`；不把 malformed / 异常账本当成可纠错证据。函数不得修改 `tool_result` / `run_context`，不得调用模型、工具、I1b / I1e、retry、candidate、Reflection / Refinement，也不得映射 `needs_manual` 或其他公开状态。

最小完成证据：

- 正例：严格三键 preflight failure，step / log 均未变化，返回 trim 后错误。
- 来源反例：相同 failure payload 但 step / log 各增加 1，返回 `None`；只有 step 或只有日志发生变化也返回 `None`。
- 内容反例：success、retryable failure、缺 / 多键、dict / str / int / list 子类、`ok=0`、`retryable=0`、空白或非字符串 error 均返回 `None`。
- 纯函数：输入前后不变，重复调用稳定，模型 / 工具 / I1b / I1e / 下游 bomb 命中 0。

当前状态：`DOING / 待用户实现`。这是当前唯一 active TODO；本步不接 `generate_candidate_summary()`，不执行 corrected Action。

I1f 用户完成信号：

> 改完了 I1f

用户原始实现：

```python
if (
    not isinstance(tool_result, dict)
    or tool_result.get("ok") is not False
    or tool_result.get("retryable") is not False
    or type(tool_result.get("error")) is not str
    or not tool_result.get("error").strip()
):
    return None

step = run_context.get("step", 0)
logs = run_context.get("logs", [])
if step != step_before or len(logs) != logs_length_before:
    return None

return tool_result.get("error").strip()
```

首次复核：

- 已通过：严格普通正例返回 trim 后 error；相同 payload 在 step / log 同时增加、只动 step、只动日志时均返回 `None`；success、retryable failure、缺任一键、`ok=0`、`retryable=0`、空白 / 非字符串 / str 子类 error 均返回 `None`。重复调用稳定、输入不变，模型 / 工具 / I1b / I1e / candidate / Reflection / Refinement bomb 均为 0。
- 真实 executor 迁移证据通过：后端确实执行后，账本从 `1/1 → 2/2`、事件 `M → T`，即使后端返回相同三字段不可重试 payload，I1f 仍返回 `None`，不会把真实 `tool_failure` 当作 Action 参数错误。
- 缺口 1：当前使用 `isinstance(tool_result, dict)` 且未检查 exact keys；extra key 与 dict 子类会被误识别并返回错误文本。
- 缺口 2：`run_context.get("step", 0)` / `.get("logs", [])` 会把缺失账本伪装成可信的 `0/0` 未移动证据；空 context 配合 `0/0` 快照会错误返回 error。
- 缺口 3：没有在 `.get()` / `len()` 前守 context 形状；`run_context=None/list` 泄漏 `AttributeError`，`logs=None` 泄漏 `TypeError`，违反所有不确定输入都返回 `None` 的 fail-closed 合同。
- 缺口 4：当前 step / logs 和两个快照没有严格内建类型与非负边界；bool、int/list 子类、负 step 与匹配快照、tuple / list 子类 logs 都可能冒充可信来源。
- 本地扩展矩阵 `53/70 PASS`；独立聚焦矩阵 `24/43 PASS`。两者的失败都集中在上述 exact payload 与 context / snapshot fail-closed 外壳；`py_compile` 与 `git diff --check` 均通过。

I1f-R1 最小订正：在任何 `.get()`、索引、`len()` 或相等比较之前，依次守住：

1. `step_before`、`logs_length_before` 为非负内建 int；
2. `run_context` 为内建 dict，且显式含 `step` / `logs`；当前 step 为非负内建 int，logs 为内建 list；
3. `tool_result` 为内建 dict 且 exact keys 为 `{ok, error, retryable}`；
4. 再做 `ok is False`、`retryable is False`、非空内建 str error 与 step / log 双不变比较。

任一条件不成立只返回 `None`；不得使用默认 `0/[]` 证明来源，不加 broad catch，不检查每条日志内容，不接主控制流。保留已经通过的 trim 返回与零副作用边界。

单题状态：`A4-Gate-C4b-1-I1f = RETRY`。

I1f-R1 用户完成信号：

> 改完了 I1f-R1

用户原始 R1 改动要点：已补两个快照的严格非负内建 int 门、`run_context` 内建 dict 与 `step/logs` 显式存在门、当前 step / logs 的严格类型门，以及 `tool_result` 内建 dict exact 三键门；但当前顺序为：

```python
if "step" not in run_context or "logs" not in run_context:
    return None

if type(step) is not int or step < 0:
    return None
if type(logs) is not list:
    return None

# 中间还有 exact payload 守门

step = run_context.get("step")
logs = run_context.get("logs")
```

R1 复核结果：

- R1 的目标守门已经出现在源码中：不再用默认 `0/[]`，空 / 非 dict / 缺键 context 与非法快照能在更早位置安静返回 `None`；exact payload 的方向也正确。
- 决定性顺序错误：`step` / `logs` 在赋值前即被类型检查。Python 已把它们认定为函数局部变量，因此任何通过前置快照 / context 外壳的输入都会抛 `UnboundLocalError`，无法到达 payload 或双账本判断。
- 本地聚焦矩阵 `4/12 PASS`：四个通过项只是被前置门直接拒绝的空 / 非 dict context 与非法快照；普通正例、真实执行、两个单边移动、extra key / dict 子类、logs 畸形与 success 均被同一异常截断。
- 独立完整矩阵 `15/43 PASS`：source `0/4`、content `0/18`；真实 executor 已正确产生 `1/1 → 2/2`、`M → T` 与一次后端调用，但随后 I1f 仍抛同一 `UnboundLocalError`，没有按合同返回 `None`。
- 输入仍未修改，模型 / 工具 / I1b / I1e / 下游 bomb 为 0；`py_compile` 能通过但不能发现这种运行期“先读后赋值”。`git diff --check` 当前报告 helper 内 5 处 trailing whitespace，待行为 PASS 后机械清理。

I1f-R2 最小订正：在确认 `run_context` 是内建 dict 且显式包含 `step` / `logs` 后，立即用 `step = run_context["step"]`、`logs = run_context["logs"]` 绑定变量；随后再执行现有 step / logs 严格类型门。删除后面迟到的两行 `.get()` 赋值。其他快照、context、exact payload、False / error、双账本与 trim 返回逻辑全部保持不动；不要用 try / except 掩盖异常，也不要增加 `MAX_STEPS` 上界，本轮冻结合同只要求非负内建整数。

单题状态更新：`A4-Gate-C4b-1-I1f = RETRY → RETRY（R1）`。

复核过程中的后续稳定保存：在助手机械清理前，工作区中的 I1f 又发生一次用户侧保存。最终稳定快照已经把 `step/logs` 绑定移动到存在性门之后，前述 `UnboundLocalError` 因此闭合；但原有的 payload 内容守门整块同时被删除，只剩 exact key 检查、账本比较与直接 `tool_result.get("error").strip()`。

按最终稳定快照重新复核：

- 来源轨迹 `4/4 PASS`：普通正例、step / log 双移动与两个单边移动均能走到预期来源分支；真实 executor 再测仍为后端调用 1 次、`1/1 → 2/2`、`M → T`，I1f 返回 `None` 且无二次副作用。
- 当前内容 / 类型矩阵本地 `28/40 PASS`、独立 `28/43 PASS`。决定性失败为：success / retryable / `ok=0` / `retryable=0` 被错误返回 error；dict 子类被接受；error 为 None / int 及 tool_result 为 None / list 泄漏 `AttributeError`；空 / blank error 返回空字符串；str 子类被接受。
- extra / missing key 当前能返回 `None`，context / snapshot 的冻结严格门已经生效。独立矩阵中的 `step > MAX_STEPS` 是额外保守探针，不加入本轮冻结硬门。

当前 I1f-R2 以最终稳定快照为准：先用单独的负向门拒绝 `type(tool_result) is not dict`，再检查 exact 三键；随后恢复 `ok is False`、`retryable is False`、error 为 trim 后非空内建 str 的内容守门。全部通过后才比较 step / log，并返回同一个局部 `error.strip()`。不得让非 dict 落到 `.get()`，不得修改已经通过的 context / snapshot / 来源逻辑。

I1f-R2 用户完成信号：

> 改完了 I1f-R2

用户原始 R2 改动：保留已经通过的快照、context、`step/logs` 绑定和双账本来源判断；把 payload 部分恢复为先拒绝非内建 dict，再要求 exact 三键，并在进入账本比较前同时守住 `ok is False`、`retryable is False`、error 为 trim 后非空的内建 str。当前稳定保存版本 SHA-256 为 `574038BC1314506EE514AF918ADBDF40CC760D95DDACFBDC0D67A07631356A8D`；复核前连续两次读取一致。此处记录的是助手机械清理前的用户实现。

```python
if type(tool_result) is not dict:
    return None

if set(tool_result) != {"ok", "error", "retryable"}:
    return None

if (
    tool_result.get("ok") is not False
    or tool_result.get("retryable") is not False
    or type(tool_result.get("error")) is not str
    or not tool_result.get("error").strip()
):
    return None

if step != step_before or len(logs) != logs_length_before:
    return None

return tool_result.get("error").strip()
```

当前先保留 I1f 唯一 TODO；只有 R2 行为矩阵通过后，助手才可删除完成后的骨架注释、清理纯格式空白并更新模块状态，不得借清理改写上述判断。

I1f-R2 复核结果：

- 主流程冻结矩阵 `42/42 PASS`：来源 `4/4`、payload 形状 / 内容 `18/18`、context / 快照严格边界 `20/20`。每例连续调用两次均稳定，`tool_result` 与 `run_context` 不变；按本轮冻结合同，没有把 `step > MAX_STEPS` 的额外保守探针加入硬门。
- 真实 executor 迁移 PASS：合法 `search_web` Action 确实调用注入后端 1 次，账本从 `1/1 → 2/2`，事件为 `M → T`，工具日志标记 `tool_failure`；相同三字段不可重试结果交给 I1f 后返回 `None`，账本、结果和后端调用数均不再变化。
- 所有 reserve / log / execute / read / search / client / I1b / I1c / I1e / candidate / Reflection / Refinement / 公开入口 bomb 命中 `0`，证明 helper 只读分类，没有隐式模型、工具或业务终点副作用。
- 两份独立只读复核均为 PASS：一份同样得到冻结矩阵 `42/42` 与真实 executor 迁移通过；另一份生产边界矩阵 `36/36`，并额外确认真实 preflight rejection 保持 `1/1`、真实后端失败移动到 `2/2`，两者不会混淆。
- 稳定性：主流程与独立矩阵前后源码 SHA-256 均为 `574038BC1314506EE514AF918ADBDF40CC760D95DDACFBDC0D67A07631356A8D`。`py_compile` 退出 `0`；`git diff --check` 的失败仅为 helper 内 4 处 trailing whitespace，属于行为 PASS 后可清理的机械项。

最终单题状态：`A4-Gate-C4b-1-I1f = RETRY → RETRY（R1）→ PASS（R2）`。这只证明“执行前 Action validation rejection 的来源识别”纯 helper，不等于 corrected Action 已执行，不等于 action correction 主控制流或父 `A4-Gate` 已 PASS。

I1f 机械收口：行为 PASS 后，助手只删除已完成的 I1f TODO、统一 helper 缩进 / 空行、清除 4 处 trailing whitespace，并把模块状态更新为“当前已完成 I1f”；没有修改任何判断、比较或返回。清理后聚焦 smoke `14/14 PASS`，真实 executor 迁移再次 PASS，`py_compile` / `git diff --check` 均退出 `0`，活动 TODO / `NotImplementedError` 一度归零。

### `A4-Gate-C4b-1-I1g`｜执行并分类一次未信任 Tool Action

当前在做什么：新增内部 helper `execute_and_classify_tool_action()`，把“读取同一请求的调用前 step / logs 快照 → 执行 Action 恰好一次 → 把同一个结果与同一组快照交给 I1f 分类”绑定成一个不可拆开的动作。

为什么需要：I1f 已会识别来源，但如果未来首次 Action 与 corrected Action 各自手写快照和调用顺序，就可能出现“执行后才拍快照”“换了另一个 context”“同一个 Action 执行两次”等漂移。I1g 像给拍照和开门动作装上同一个联锁：先拍调用前现场，只开门一次，再用这张现场照判断后端究竟有没有运行。

与前后步骤的关系：I1g 复用既有 `execute_tool_call()` 和已 PASS 的 I1f，只输出内部分类结果；下一检查点才会把首次 preflight rejection 交给 I1e，并用同一个 I1g 尝试 corrected Action。本步不请求 correction、不做工具 retry、不进入 candidate / Reflection / Refinement，也不映射任何公开状态。

用户当前任务：实现代码中的唯一 I1g TODO，并保持以下顺序与所有权：

1. 在任何执行前确认 `run_context` 能生成可靠快照：它是内建 dict，显式包含 `step` / `logs`；step 是 `0..MAX_STEPS` 的内建 int，logs 是内建 list。畸形外壳抛 `ValueError`，且 executor / I1f 均为 0 次。`tool_name`、`raw_arguments`、`normalized_request` 的 Action 内容校验仍归 executor，不要在 I1g 重抄。
2. 先保存 `step_before = run_context["step"]` 与 `logs_length_before = len(run_context["logs"])`，再调用 `execute_tool_call()` 恰好一次；四个输入对象原样传递，不复制或规范化。
3. executor 正常返回后，调用 `extract_preexecution_action_validation_error()` 恰好一次，传入同一个 `tool_result`、同一个 `run_context` 和刚才两份快照。
4. 返回内建 dict，键集 exact `{"tool_result", "validation_error"}`；`tool_result` 保持 executor 返回对象的 identity，`validation_error` 只取 I1f 的原始返回值。
5. executor 若抛异常，不捕获、不包装、不调用 I1f，也不再执行；异常对象由 Python 原样向上传播。不得调用 I1b / I1e、模型、第二次工具、retry 或下游阶段。

最小完成证据：

- preflight rejection：executor 1 次、真实后端 / tool step / log 为 0，I1f 1 次，返回非空 `validation_error`。
- 真实工具成功、retryable failure、不可重试后端失败：每例 executor / I1f 各 1 次，真实工具 step / log 各增加 1，`validation_error is None`。
- executor 抛预存异常：同一异常对象上抛，已有工具 step / error log 保留，I1f 为 0，不发生第二次执行。
- 参数 identity / 顺序、exact 两键、输入不被 wrapper 额外修改、畸形 context 零调用，以及 I1b / I1e / 模型 / retry / 下游 bomb 全部通过。

当前状态：`DOING / 待用户实现`。当前只有 I1g 一个 active TODO；完成口令：`改完了 I1g`。

I1g 用户完成信号：

> 改完了 I1g

用户原始实现（助手机械清理前，稳定版本 SHA-256：`FCC0821E494CDA250F44339DD3784DFFC0022D01240164BBDD3CBBC9AE153A02`）：

```python
if isinstance(run_context, dict) is False:
    raise ValueError("Invalid run_context")
if hasattr(run_context, "step") is False or hasattr(run_context, "logs") is False:
    raise ValueError("Invalid run_context")
if not type(run_context.get("step")) is int or run_context.get("step") < 0:
    raise ValueError("Invalid run_context")
if not type(run_context.get("logs")) is list:
    raise ValueError("Invalid run_context")
step_before = run_context.get("step")
logs_length_before = len(run_context.get("logs"))
tool_result = execute_tool_call(tool_name, raw_arguments)

return {"tool_result": tool_result, "validation_error": None}
```

记录边界：当前只保存用户原始实现与完成口令，I1g TODO 暂时保留；以下复核必须基于同一稳定快照，行为通过前不得机械改写条件、调用参数或返回逻辑，也不得创建下一检查点。

I1g 首次复核结果：`RETRY`。

- 稳定性：主流程与独立探针前后源码 SHA-256 均为 `FCC0821E494CDA250F44339DD3784DFFC0022D01240164BBDD3CBBC9AE153A02`。
- 五条合法轨迹 `0/5 PASS`：preflight rejection、真实 success、retryable failure、真实 nonretryable failure、executor 预存异常都被同一个 `ValueError` 提前截断；每例 executor / I1f 均为 0 次，异常轨也没有传播原 sentinel。
- 普通 malformed context `13/13` 表面满足 `ValueError + 零调用`，但属性型 dict 子类能够绕过当前门：executor 实际 1 次、I1f 0 次，executor 只收到 2 个位置参数、0 个关键字参数，返回的 `validation_error` 被写死为 `None`。使用真实 executor 时立即抛 `TypeError`，提示缺少 `normalized_request`。
- 缺口 1：`step` / `logs` 是字典键，不是对象属性；`hasattr(run_context, "step")` 会拒绝普通合法 dict。必须用键成员检查。与此同时，`isinstance(..., dict)` 会接受 dict 子类，必须改成内建 dict exact 类型门。
- 缺口 2：step 只守了非负，没有守冻结的 `step <= MAX_STEPS`；属性型 dict 子类已证明超界状态可以穿门并触发 executor。
- 缺口 3：`execute_tool_call(tool_name, raw_arguments)` 漏传 `normalized_request` 和 keyword-only 的同一个 `run_context`；参数 identity 合同只有 `2/4`。
- 缺口 4：当前保存了快照却没有调用 I1f，直接把 `validation_error` 写死为 `None`，所以并未完成来源分类。
- 已做对并应保留：快照代码位于 executor 之前；绕过错误 context 门后 executor 只调用一次；没有 broad catch；返回容器是 exact 两键内建 dict 且 `tool_result` identity 保留；I1b / I1e / 模型 / retry / 下游调用为 0。
- 卫生：`py_compile` 退出 `0`；`git diff --check` 只报告 I1g 内一处 trailing whitespace。行为未通过，TODO、状态文字和空白均暂不机械清理。

I1g-R1 最小订正：只在当前 helper 内按以下职责顺序修正，不改 I1f、executor 或主控制流：

1. 用 `type(run_context) is dict` 守 exact 内建类型；用 `"step" in run_context` / `"logs" in run_context` 守字典键，不能使用 `isinstance` 或 `hasattr`。
2. 键存在后只读绑定 step / logs；step 必须是 `0..MAX_STEPS` 的内建 int，logs 必须是内建 list。任一非法都在 executor / I1f 前抛 `ValueError`。
3. 在执行前保存两份快照；用原 `tool_name`、原 `raw_arguments`、原 `normalized_request` 以及 `run_context=原对象` 调用 executor 恰好一次。
4. 只有 executor 正常返回后，才调用 I1f 恰好一次；传同一个 `tool_result`、同一个 context 和原两份快照。不要 catch executor 异常。
5. 保留 exact 两键返回与 tool_result identity，但 `validation_error` 必须使用 I1f 的原始返回，不能硬编码。

单题状态：`A4-Gate-C4b-1-I1g = RETRY`。当前唯一活动检查点改为 `I1g-R1`；完成口令：`改完了 I1g-R1`。

I1g-R1 用户完成信号：

> 改完了 I1g-R1

复核期间检测到用户连续保存；以下记录采用最终稳定 R1 快照，源码 SHA-256 为 `270163474A8AA50960333369EAE8BB7A62E4D56ECF4CE4255B400CA2BC18F15F`：

```python
if type(run_context) is not dict or "step" not in run_context or "logs" not in run_context:
    raise ValueError("Invalid run_context")
step = run_context.get("step")
if not type(step) is int or not (0 <= step <= MAX_STEPS):
    raise ValueError("Invalid run_context")
if not type(run_context.get("logs")) is list:
    raise ValueError("Invalid run_context")
step_before = run_context.get("step")
logs_length_before = len(run_context.get("logs"))
tool_result = execute_tool_call(
    tool_name,
    raw_arguments,
    normalized_request,
    run_context=run_context,
)
validation_error = extract_preexecution_action_validation_error(
    tool_result=tool_result,
    step=step,
    logs=run_context.get("logs"),
    step_before=step_before,
    logs_length_before=logs_length_before,
)
return {"tool_result": tool_result, "validation_error": validation_error}
```

记录边界：这是助手机械清理前的用户 R1 实现；I1g TODO 继续保留。先按真实 I1f 签名复核，行为通过前不得由助手修改调用参数或创建下一检查点。

I1g-R1 复核结果：`RETRY`，只剩一个窄的 I1f 调用合同缺口。

- 主流程与独立矩阵一致：五条合法轨迹 `1/5 PASS`，严格 malformed context `13/13 PASS`，合计 `14/18 PASS`；矩阵前后稳定源码 SHA-256 均为 `270163474A8AA50960333369EAE8BB7A62E4D56ECF4CE4255B400CA2BC18F15F`。
- 已通过且不应重写：exact 内建 context / 键门、step `0..MAX_STEPS`、logs 内建 list、执行前两份快照、executor 恰好一次及四个输入对象 identity、executor 同一异常传播且 I1f 为 0、exact 两键写法与 `tool_result` identity。13 个畸形 context 全部为 exact `ValueError`，executor / I1f 均为 0。
- 唯一失败：真实 I1f 签名只接受 `tool_result`，以及 keyword-only 的 `run_context / step_before / logs_length_before`；R1 却传入不存在的 `step=`、`logs=` 并漏掉 `run_context=`。因此 preflight / success / retryable / nonretryable 四条 executor 正常返回轨迹都会在一次 executor 之后抛 `TypeError: unexpected keyword argument 'step'`，没有两键结果。
- 宽松 spy 能证明 I1f 调用尝试恰为 1、`tool_result` 和两份执行前快照正确，但 `run_context` 缺失；不能把“spy 接受任意 kwargs”当作真实 API PASS。I1b / I1e / 模型 / retry / 下游 bomb 均为 0。
- 卫生：`py_compile` 与 `git diff --check` 均退出 `0`。因仍有行为缺口，I1g TODO 与“当前进行”状态继续保留，不做机械清理。

I1g-R2 最小订正：只修改 `extract_preexecution_action_validation_error()` 的参数接线——删除 `step=` 与 `logs=`，加入 `run_context=run_context`，保留同一个 `tool_result`、原 `step_before` 与原 `logs_length_before`。其余已经通过的 context 门、executor 四对象传递、异常自然传播、exact 两键返回全部保持不动；不要修改 I1f 本身，不加 catch，也不接主控制流。

单题状态更新：`A4-Gate-C4b-1-I1g = RETRY → RETRY（R1）`。当前唯一活动检查点为 `I1g-R2`；完成口令：`改完了 I1g-R2`。

I1g-R2 用户完成信号：

> 改完了 I1g-R2

用户原始 R2 改动只修 I1f 参数接线；其余 R1 逻辑保持。助手机械清理前的稳定源码 SHA-256 为 `3928398C5D736F3BFA2C4038E60F528436FCF2096A5CE78722A5B6E99BF6E276`：

```python
validation_error = extract_preexecution_action_validation_error(
    tool_result=tool_result,
    run_context=run_context,
    step_before=step_before,
    logs_length_before=logs_length_before,
)
```

记录边界：I1g TODO 暂时保留；先按真实 executor 与真实 I1f 合同复测。只有行为通过后，助手才可删除完成后的 TODO、规范纯格式缩进 / 空行并更新模块状态，不得借清理改写 context、调用、异常或返回逻辑。

I1g-R2 复核结果：`PASS`。

- 主流程总矩阵 `23/23 PASS`：内存 spy 五轨 `5/5`、严格 malformed context `13/13`、真实 executor 迁移 `5/5`；独立只读终审得到完全相同的 `23/23 PASS`。矩阵前后源码 SHA-256 均为 `3928398C5D736F3BFA2C4038E60F528436FCF2096A5CE78722A5B6E99BF6E276`。
- spy 四条正常结果均为 executor `1` / I1f `1`：executor 收到原 `tool_name / raw_arguments / normalized_request` 与同一个 context；I1f 收到同一个 `tool_result`、同一个 context 和执行前 `step/logs=1/1` 快照。返回是 exact 内建两键，`tool_result` identity 与 I1f 的 validation 原值均保持。
- executor 异常轨为 executor `1` / I1f `0`：预存异常对象原样传播，无第二次执行。13 类畸形 context 均为 exact `ValueError`、两调用 `0/0`、输入不变。
- 真实 executor：unknown preflight 后端 `0`、账本保持 `1/1`、validation 非空；success / retryable / nonretryable 各后端 `1`、账本 `2/2`、validation 为 `None`，工具日志 error 分别为 `None / retryable_tool_failure / tool_failure`；真实后端异常同一对象传播，`2/2` 错误日志保留，I1f 为 0。
- I1b / I1e / 模型 / retry / candidate / Reflection / Refinement / 公开终点 bomb 均为 `0`。`py_compile` 与 `git diff --check` 均退出 `0`。

最终单题状态：`A4-Gate-C4b-1-I1g = RETRY → RETRY（R1）→ PASS（R2）`。本结论只关闭“可靠快照 → 一次 executor → I1f 同源分类 → exact 两键”内部 wrapper，不等于 correction 主控制流、C4b-1 或父 `A4-Gate` 已 PASS。

I1g 机械收口：行为 PASS 后，助手只删除已完成 I1g TODO、规范 context 条件与两段调用的缩进 / 顶层空行，并把模块状态更新为“当前已完成 I1g”；没有修改任何条件、参数、顺序、异常或返回。清理后 smoke `7/7 PASS`，真实 preflight / success / backend exception 与四类畸形 context 均通过；`py_compile` / `git diff --check` 退出 `0`，活动 TODO / `NotImplementedError` 一度归零。

### `A4-Gate-C4b-1-I1h`｜首次 Action、一次 correction 与修正 Action 的私有编排

当前在做什么：新增 `run_tool_action_phase_with_one_correction()`，把已 PASS 的 I1g（执行并分类一次 Action）和 I1e（预算内请求并解析一次 correction）串成一个私有 Action phase。它只负责得到“最终要交给后续处理的 action 与 tool_result”，不会生成候选摘要。

为什么需要：I1e 和 I1g 单独都已可靠，但主流程仍没有决定“什么时候调用哪一个”。I1h 像一个只管换一次钥匙的门卫：先用原 Action 尝试；若客户端在开门前发现钥匙不合法，就允许模型换一次；新钥匙再不合法时，复用既有预算门停止，绝不能继续换第三把。

与前后步骤的关系：输入的 `initial_action` 来自 I1d，首次 / corrected Action 都通过同一个 I1g；preflight error 通过同一个 I1e。I1h 成功后只返回内部四键，下一检查点才把它机械接入 `generate_candidate_summary()`，再处理真实 Tool Observation、工具 retry 与 candidate。当前尤其不处理“correction 后 retryable 工具是否还有完整下游步数”，该预算问题留给后续独立检查点。

用户当前任务：实现代码中的唯一 I1h TODO。

1. 在任何 I1g / I1e 调用前，只守最小外壳：`base_messages` 是非空内建 list；`initial_action` 是内建 dict 且键集 exact `{"assistant_message", "tool_call_id", "tool_name", "raw_arguments"}`。不重验工具名 / 参数内容；非法外壳抛 `ValueError`，所有下层调用为 0。
2. 设请求局部 `corrections_used = 0`，用 `initial_action["tool_name"] / ["raw_arguments"]`、原 `normalized_request` 和原 context 调 I1g 恰好一次。
3. 若首次 I1g 的 `validation_error is None`，直接返回 exact 四键：`messages` 是原 `base_messages` 对象，`action` 是原 `initial_action` 对象，`tool_result` 是 I1g 原结果对象，`corrections_used == 0`。
4. 若首次是可信 preflight error，以原 base / initial action / validation、计数 0、原 client / context 调 I1e 恰好一次；不得自行构造 Observation、复制预算公式或递增另一份计数。
5. 取 I1e 返回的 `correction_messages / corrected_action / corrections_used`，用 corrected Action 调 I1g 恰好一次。若 validation 为 `None`，返回同样 exact 四键，其中 messages / action / count 必须原样来自 I1e，tool_result 来自第二次 I1g。不要把 corrected assistant 追加进 messages；它仍单独放在 action 中，等待后续真实 Tool Observation 配对。
6. 若 corrected Action 仍是 preflight error，再以 I1e 返回的 messages / corrected action / count 1 调 I1e。既有 I1a 预算门应发 `ActionCorrectionBudgetError(reason="max_action_corrections")`；I1h 不手写预算公式或异常，不允许第二次 correction 模型、第三次 I1g / 工具分派。
7. 不 catch 或改写 I1g / I1e / 模型 API / parser 的任何异常；不修改调用者的 base_messages，不做工具 retry、candidate、Reflection、Refinement、真实 Tool Observation 回填或公开状态映射。

成功返回必须是严格四键内建 dict：

```python
{
    "messages": ...,           # 原 base 或 I1e 的 correction_messages
    "action": ...,             # 原 initial 或 I1e 的 corrected_action
    "tool_result": ...,        # 对应那次 I1g 的原结果对象
    "corrections_used": 0 | 1,
}
```

最小完成证据：

- 首次 Action 有效：调用顺序 `I1g`，I1e 为 0，exact 四键 / identity / count 0。
- 首次无效、corrected 有效：调用顺序 `I1g → I1e → I1g`，一次 correction 模型、一次真实工具、exact 四键 / identity / count 1。
- 首次无效、corrected 仍无效：调用顺序 `I1g → I1e → I1g → I1e(预算停止)`；真实后端 0、correction 模型只有 1、无第三次 I1g，得到 exact `ActionCorrectionBudgetError(reason=max_action_corrections)`。
- 初始剩余步数不足：首次 I1g 后 I1e 直接发 `insufficient_completion_steps`，correction 模型与第二次 I1g 均为 0。
- 初始 executor 异常、correction API 异常或 correction 协议异常均保持同一 / 窄异常与既有账本，后续调用为 0；输入外壳错误在全部下层前拒绝。

当前状态：`DOING / 待用户实现`。当前只有 I1h 一个 active TODO；完成口令：`改完了 I1h`。

I1h 用户完成信号：

> 改完了 I1h

用户原始实现（助手机械清理前，稳定源码 SHA-256：`1900A90E4832CB6F3A1B6323D02CEB55A406DA1DA279570B098E75628E1D794A`）：

```python
corrections_used = 0
try:
    initial_result = request_and_parse_action_correction(
        base_messages=base_messages,
        action=initial_action,
        normalized_request=normalized_request,
        client=client,
        run_context=run_context,
        corrections_used=corrections_used,
    )
except Exception as e:
    correction_result = request_and_parse_action_correction(
        base_messages=base_messages,
        invalid_action=initial_action,
        observation=str(e),
        normalized_request=normalized_request,
        client=client,
        run_context=run_context,
        corrections_used=corrections_used,
    )
    corrected_action = correction_result["corrected_action"]
    corrections_used = correction_result["corrections_used"]
    final_result = request_and_parse_action_correction(
        base_messages=base_messages,
        action=corrected_action,
        normalized_request=normalized_request,
        client=client,
        run_context=run_context,
        corrections_used=corrections_used,
    )
    return {
        "messages": final_result["messages"],
        "action": final_result["action"],
        "tool_result": final_result["tool_result"],
        "corrections_used": corrections_used,
    }
else:
    return {
        "messages": initial_result["messages"],
        "action": initial_result["action"],
        "tool_result": initial_result["tool_result"],
        "corrections_used": corrections_used,
    }
```

记录边界：当前只保存用户原始实现与完成口令；I1h TODO 继续保留。以下复核必须以同一稳定快照的实际调用顺序、参数和异常对象为准，行为通过前不得机械改写控制流或创建下一检查点。

I1h 首次复核结果：`RETRY`。

- 稳定源码聚焦矩阵 `0/6 PASS`。真实 helper 调用得到 `TypeError: request_and_parse_action_correction() got an unexpected keyword argument 'observation'`；I1g 为 0，I1e 已被错误尝试 2 次，模型 / 工具仍为 0。
- 首次有效 spy：实际顺序为 I1e `1` / I1g `0`，而目标应为 I1g `1` / I1e `0`。首次非法、corrected 有效 spy：实际 I1e `3` / I1g `0`，而目标应为 `I1g → I1e → I1g`。
- 原异常 identity 失败：第一次 I1e 的预存异常被 broad `except Exception` 捕获，随后又调用 I1e，最终抛第二个异常；实际调用数 I1e `2`，原异常被重分类。非法 base_messages 也未在下层前拒绝，I1e 实际被调用 1 次。
- 缺口 1：首次和 corrected Action 都应由 I1g 执行 / 分类；当前三处都误调 I1e。第一次 I1e 还收到不存在的 `action=`、`normalized_request=`，catch 分支收到不存在的 `observation=`、`normalized_request=`。
- 缺口 2：`except Exception` 会把 executor、模型 API、协议、预算和程序错误全部伪装成“可纠正的 Action validation error”；`str(e)` 还可能把未安全化的异常正文回填进 correction 消息。可信 correction 条件只能是 I1g 正常返回的非空 `validation_error`。
- 缺口 3：没有做 base_messages 非空内建 list 与 initial_action exact 四键内建 dict 的调用前外壳门，也没有按 `validation_error is None` 分流。
- 缺口 4：没有使用 I1e 返回的 `correction_messages` 作为后续 messages；没有用 corrected Action 调第二次 I1g；corrected 再错时也没有以 count 1 再进 I1e 复用既有预算停止。当前还尝试从 I1e 的真实三键结果读取不存在的 `messages/action/tool_result`。
- 已保留的方向：`corrections_used` 是函数局部变量；外层成功返回字面量使用目标四键；没有扩入工具 retry、candidate、Reflection / Refinement 或公开映射。`py_compile` 与 `git diff --check` 均退出 `0`。

I1h-R1 最小订正：保留函数签名和唯一 TODO，只重写当前函数体的职责顺序，不修改 I1e / I1g：

1. 先完成 base_messages / initial_action exact 外壳门，失败为 `ValueError` 且 I1g / I1e 均 0。
2. 无 try / except 地调用 I1g 处理 initial Action；只读取其 `tool_result / validation_error`。validation 为 `None` 时返回原 base、原 action、同一 result、count 0。
3. 只有 validation 非空时，按 I1e 的真实参数名传 `base_messages / invalid_action / validation_error / corrections_used=0 / client / run_context`。不能传 `action / observation / normalized_request`，不能使用异常正文代替安全 validation。
4. 用 I1e 的 `correction_messages / corrected_action / corrections_used` 调第二次 I1g；validation 为 `None` 时返回这三个对应对象与第二次 result。
5. corrected validation 仍非空时，再按 I1e 真实合同传 I1e 返回的 messages / action / count 1，让既有预算门原样发 `max_action_corrections`；不得第三次 I1g、第二次 correction 模型或手写异常。
6. 所有下层异常自然传播；不 catch、不重分类、不做 retry / 下游 / 公开映射。

单题状态：`A4-Gate-C4b-1-I1h = RETRY`。当前唯一活动检查点为 `I1h-R1`；完成口令：`改完了 I1h-R1`。

I1h-R1 用户完成信号：

> 改完了 I1h-R1

用户原始 R1（助手机械清理前，稳定源码 SHA-256：`EBC0AAC6D39C6749CF421A2667F130AE4F66AD63C336065F78C03A6C57A2207C`）：

```python
corrections_used = 0
initial_result = request_and_parse_action_correction(
        base_messages=base_messages,
        action=initial_action,
        normalized_request=normalized_request,
        client=client,
        run_context=run_context,
        corrections_used=corrections_used,
    )
return {
        "messages": initial_result["messages"],
        "action": initial_result["action"],
        "tool_result": initial_result["tool_result"],
        "corrections_used": corrections_used,
    }
```

R1 原始改动边界：用户删除了首次版本的 broad `try / except Exception` 与后续两段错误分支，但保留了“首次直接调用 I1e”的当前实现；尚未做 exact 输入外壳门，也尚未接入 I1g。当前只留证，不把删除 broad catch 等同于整体行为通过；I1h TODO 继续保留，复核期间不机械改写控制流或创建下一检查点。

I1h-R1 复核结果：`RETRY`。

- 稳定源码 SHA-256 在主复核与独立终审前后均为 `EBC0AAC6D39C6749CF421A2667F130AE4F66AD63C336065F78C03A6C57A2207C`。主聚焦矩阵 `0/6 PASS`；独立冻结核心轨迹 `0/7 PASS`、严格输入外壳 `0/11 PASS`、输入不变 `11/11 PASS`、零 retry / candidate / downstream `1/1 PASS`。`py_compile` 与 `git diff --check` 均退出 `0`。
- 当前 I1h 的 AST 直接调用只有一次 I1e，没有 I1g。合法输入真实签名探针在参数绑定阶段得到 `TypeError: request_and_parse_action_correction() got an unexpected keyword argument 'action'`；I1g 为 `0`，I1e wrapper 尝试 `1` / body `0`，模型 / 工具为 `0/0`，step / log 变化为 `0/0`。
- 首次有效、首次 preflight 后修正有效、修正后仍非法三轨实际都只有 `E`，而目标分别应为 `G`、`G → E → G`、`G → E → G → E（预算停止）`。真实 I1e 返回 `correction_messages / corrected_action / corrections_used`，当前无条件 return 却读取 `messages / action / tool_result`，即使只修参数名仍会 `KeyError`。
- 11 类畸形外壳每例都在 I1e 被调用 1 次后由 spy 抛错，没有在任何下层前得到 exact `ValueError`；因此非空内建 list 与 exact 四键内建 dict 守门仍完全缺失。initial executor sentinel 也因 I1g 从未调用而不可能传播。
- R1 的有效进展是 broad catch 与 `str(e)` 已完全删除：由当前错误 I1e 直接抛出的预算 / API / 协议异常不再被二次捕获，且没有扩入 retry、candidate、Reflection / Refinement 或公开映射。不过这只是关闭了错误异常路径，尚未建立正确 Action phase。

I1h-R2 最小订正：继续只修改 I1h 函数体，不修改 I1e / I1g，不重新加入 catch，也不能只改 I1e 的参数名：

1. 在任何下层前补 `base_messages` 非空内建 list 与 `initial_action` exact 四键内建 dict 的外壳门；失败为 `ValueError`，I1g / I1e 都为 0。
2. 首次必须调用 I1g：传原 Action 的 tool name / raw arguments、原 normalized request 与原 context。validation 为 `None` 时返回原 base / action、同一个 tool result 与 count 0。
3. 只有首次 validation 非空才按真实 I1e 合同传原 base、`invalid_action=initial_action`、该安全 `validation_error`、count 0、原 client / context；不得构造 Observation 或手写预算。
4. 用 I1e 返回的 corrected action 调第二次 I1g。若 validation 为 `None`，返回 I1e 的 messages / action / count 和第二次 I1g 的同一个 result。
5. corrected validation 仍非空时，以 I1e 返回的 messages / action / count 1 再调用 I1e；只让既有 I1a 发 `max_action_corrections`，不允许第二个 correction 模型、第三次 I1g 或手写异常。
6. 继续保持所有下层异常自然传播，并停在 retry / candidate / Reflection / Refinement / 公开映射之前。

单题状态：`A4-Gate-C4b-1-I1h = RETRY → RETRY（R1）`。当前唯一活动检查点为 `I1h-R2`；完成口令：`改完了 I1h-R2`。

I1h-R2 用户完成信号：

> 改完了 I1h-R2

用户原始 R2（助手机械清理前，稳定源码 SHA-256：`B60FA9595DBDB83A59CB1FBFC31F22C1C6B85D915967B488796A79C746E64664`）：

```python
if type(base_messages) is not list or not base_messages:
    raise ValueError("base_messages must be a non-empty list")
expected_action_keys = {
  "assistant_message",
  "tool_call_id",
  "tool_name",
  "raw_arguments",
  }
if (type(initial_action) is not dict or set(initial_action) != expected_action_keys):
    raise ValueError("initial_action must be a dict with keys: " + ", ".join(expected_action_keys))
corrections_used = 0
initial_execution = execute_and_classify_tool_action(
initial_action["tool_name"],
initial_action["raw_arguments"],
normalized_request,
run_context=run_context,
)
if initial_execution["validation_error"] is None:
  return {
        "messages": initial_execution["messages"],
        "action": initial_execution["action"],
        "tool_result": initial_execution["tool_result"],
        "corrections_used": corrections_used,
    }
correction = request_and_parse_action_correction(
base_messages=base_messages,
invalid_action=initial_action,
validation_error=initial_execution["validation_error"],
corrections_used=0,
client=client,
run_context=run_context,
)
corrected_action = correction["corrected_action"]
corrected_execution = execute_and_classify_tool_action(
corrected_action["tool_name"],
corrected_action["raw_arguments"],
normalized_request,
run_context=run_context,
)
if corrected_execution["validation_error"] is None:
    return {
            "messages": correction["correction_messages"],
            "action": correction["corrected_action"],
            "tool_result": corrected_execution["tool_result"],
            "corrections_used": correction["corrections_used"],
          }
return {
    "messages": correction["correction_messages"],
    "action": correction["corrected_action"],
    "tool_result": corrected_execution["tool_result"],
    "corrections_used": correction["corrections_used"],
}
```

R2 原始实现边界：用户已经接入 exact base/action 外壳、首次 I1g、validation 分流、一次 I1e 与 corrected I1g，并继续保持无 catch / 无下游；当前只留证，不先按静态阅读改写首次成功返回或 corrected 再错分支。I1h TODO 继续保留，以下复核以该稳定快照的真实调用顺序、返回键、identity 与预算异常为准。

I1h-R2 复核结果：`RETRY`，但核心编排已接近完成。

- 稳定源码 SHA-256 在主复核与独立终审前后均为 `B60FA9595DBDB83A59CB1FBFC31F22C1C6B85D915967B488796A79C746E64664`。冻结核心轨迹 `5/7 PASS`、严格外壳 `11/11 PASS`、输入不变 `11/11 PASS`、零下游 `1/1 PASS`，按完整场景为 `17/19 PASS`；异常 identity `4/4 PASS`。`py_compile` 与 `git diff --check` 均退出 `0`。
- 已通过：exact 外壳在下层前拒绝；首次 I1g 与真实参数合同正确；`G → E → G` corrected-valid 轨迹、所有对象 identity 与 count 1 正确；初始 `insufficient_completion_steps`、executor 异常、correction API 异常、correction 协议异常均按原对象传播；没有 catch 或 retry / candidate / Reflection / Refinement / 公开映射。
- 缺口 1（initial valid）：当前顺序已是正确的单次 `G`，但 I1g exact 两键只有 `tool_result / validation_error`，代码却从 `initial_execution` 读取不存在的 `messages / action`，实际得到 `KeyError('messages')`。真实工具可能已经执行并记账后才发生该 KeyError，因此不能把它视为无副作用的小返回错误。该分支应返回原 `base_messages`、原 `initial_action`、同一个 `initial_execution['tool_result']` 与 count 0。
- 缺口 2（corrected 仍非法）：实际顺序为 `G → E → G` 后直接返回四键，把仍被 preflight 拒绝的 corrected Action 伪装成成功；目标应再进入 `E（预算停止）`。必须以 I1e 返回的 correction messages / corrected action / count 1、第二次 I1g 的 validation 和原 client / context 再调用 I1e，让既有 I1a 原样发 `max_action_corrections`；不发第二个模型、不做第三次 I1g。

I1h-R3 最小订正只改上述两处，不重写已经通过的外壳、首次 I1g、首次 I1e、corrected I1g、异常与非目标边界：

1. initial-valid 返回中的 `messages / action` 改为原 `base_messages / initial_action`；`tool_result` 继续取 `initial_execution['tool_result']`，count 保持 0。
2. 删除 corrected validation 非空后的末尾四键 return；改为按真实 I1e 合同再调用一次 I1e，参数来源依次为 `correction['correction_messages']`、`correction['corrected_action']`、`corrected_execution['validation_error']`、`correction['corrections_used']`、原 client 与原 context。不要接返回值、手写预算异常或加入 catch；该调用应在模型前自然停止。

单题状态：`A4-Gate-C4b-1-I1h = RETRY → RETRY（R1）→ RETRY（R2）`。当前唯一活动检查点为 `I1h-R3`；完成口令：`改完了 I1h-R3`。

I1h-R3 用户完成信号：

> 改完了 I1h-R3

用户原始 R3（助手机械清理前，稳定源码 SHA-256：`64B15363E40CF8002634FA2BFE65B64BF77526CE270AED0946CF730B8C8EDCAA`）：

```python
if initial_execution["validation_error"] is None:
  return {
        "messages": base_messages,
        "action": initial_action,
        "tool_result": initial_execution["tool_result"],
        "corrections_used": corrections_used,
    }

# 中间的首次 I1e 与 corrected I1g 保持原实现

if corrected_execution["validation_error"] is None:
    parse_single_tool_action_response(corrected_execution["tool_result"])
return {
    "tool_result": corrected_execution["tool_result"],
    "corrections_used": correction["corrections_used"],
}
```

R3 原始改动边界：initial-valid 已把 messages / action 改为原 base / initial action；用户同时把 corrected-valid 四键 return 与 corrected-invalid 末尾 return 改成“validation 为 None 时解析 tool_result，随后无条件返回两键”。当前只留证，不先恢复 R2 分支或补预算调用；I1h TODO 继续保留，以下复核以完整文件中的稳定 R3 为准。

I1h-R3 复核结果：`RETRY`。initial-valid 已通过，但 corrected 两条轨迹发生新回归。

- 稳定源码 SHA-256 在主复核与独立终审前后均为 `64B15363E40CF8002634FA2BFE65B64BF77526CE270AED0946CF730B8C8EDCAA`。核心 7 轨仍为 `5/7 PASS`、严格外壳 `11/11 PASS`、零下游 `1/1 PASS`，合计 `17/19 PASS`；输入不变与既有四类异常 identity 继续通过。新增 parser-zero 与 corrected exact4 两个定向检查均失败；`py_compile` / `git diff --check` 退出 `0`。
- initial-valid 现在正确完成单次 `G`，返回 exact 四键、原 base / initial action / 同一个 tool result 与 count 0；该 R3 修正应保留。
- corrected-valid 新增错误轨迹 `G → E → G → P`：`P` 是 `parse_single_tool_action_response(tool_result)`。I1d parser 消费模型 SDK response 并寻找 `.choices`，而 I1g 的 tool_result 是已执行工具的结果；实际抛 `ToolActionProtocolError: Response must have 'choices' key.`。即使把 parser 设为 no-op，当前无条件 return 也只有 `tool_result / corrections_used` 两键，缺 messages / action。
- corrected-invalid 实际仍为 `G → E → G → 两键 return`，没有第二个 I1e；真实组合探针中 correction 模型 1、真实后端 0、第二 E 为 0，没有 `max_action_corrections`。这会把未执行的非法 corrected Action 当普通结果交给下游。

I1h-R4 只替换 corrected 分支，不改已经通过的 initial-valid、外壳、首次 G / E、corrected G、异常与非目标边界：

1. 完全删除 `parse_single_tool_action_response(corrected_execution['tool_result'])`；tool_result 不需要、也不能再解析。
2. corrected validation 为 `None` 时恢复 exact 四键 return：messages / action / count 来自 `correction`，tool_result 来自同一个 `corrected_execution`。
3. corrected validation 非空时不 return；以 correction messages / action / count、第二次 validation、原 client / context 再调用 I1e，让 I1a 自然发 `max_action_corrections`。不接返回值、不手写异常、不加入 catch。

单题状态：`A4-Gate-C4b-1-I1h = RETRY → RETRY（R1）→ RETRY（R2）→ RETRY（R3）`。当前唯一活动检查点为 `I1h-R4`；完成口令：`改完了 I1h-R4`。

I1h-R4 用户完成信号：

> 改完了 I1h-R4

用户原始 R4（助手机械清理前，稳定源码 SHA-256：`55004BD5EC72B051C44BF456D7F49C9B93DCC7C6760B249E86EEDD1B4A8FEC1A`）：

```python
if corrected_execution["validation_error"] is None:
    return {
        "messages": correction["correction_messages"],
        "action": correction["corrected_action"],
        "tool_result": corrected_execution["tool_result"],
        "corrections_used": correction["corrections_used"],
    }

request_and_parse_action_correction(
    base_messages=correction["correction_messages"],
    invalid_action=correction["corrected_action"],
    validation_error=corrected_execution["validation_error"],
    corrections_used=correction["corrections_used"],
    client=client,
    run_context=run_context,
)
```

R4 原始改动边界：用户删除了 corrected tool_result 的错误 I1d parser，恢复 corrected-valid exact 四键，并在 corrected-invalid 时按真实 I1e 合同追加第二次预算门调用；同时统一了 I1h 函数体缩进 / 换行，但仍保留已完成 TODO 与模块“当前进行”状态。当前只留证；行为完整通过前不删除 TODO、不改模块状态、不创建主函数接线骨架。

I1h-R4 复核结果：`PASS`。

- 稳定源码 SHA-256 在主复核与独立终审前后均为 `55004BD5EC72B051C44BF456D7F49C9B93DCC7C6760B249E86EEDD1B4A8FEC1A`。核心 7 轨 `7/7 PASS`、严格外壳 `11/11 PASS`、zero direct parser / retry / candidate / Reflection / Refinement / public downstream `1/1 PASS`，冻结完整场景 `19/19 PASS`；输入不变 `11/11`、两条成功 exact 四键 / identity `2/2`、四类异常 identity `4/4`。`py_compile` 与 `git diff --check` 均退出 `0`。
- 精确轨迹：initial-valid 为 `G`；corrected-valid 为 `G → E → G`；corrected-invalid 为 `G → E → G → E（预算停止）`；initial-insufficient 为 `G → E`；executor 为 `G`；API / 协议异常均为 `G → E`。两次 G / E 的 name、args、normalized request、messages、action、validation、count、client、context 来源与 identity 全部通过。
- 真实 I1e + fake client corrected-invalid 集成通过：得到 exact `ActionCorrectionBudgetError(reason=max_action_corrections, current_step=2, corrections_used=1)`；I1g `2`、I1e `2`、correction 模型 `1`、真实 backend `0`，step / log `1/1 → 2/2`，无第三 G / 第二模型。parser 总计一次且唯一输入是 correction SDK response，没有 parser 收到 tool_result。
- 真实 corrected-valid 集成通过：correction 模型 `1`、内存 backend `1`、step / log 到 `3/3`，事件为初始 model → correction model_call → tool_call；返回 exact 四键、corrected messages / action / tool result identity 与 count 1 正确。initial-valid 的原 base / action / result identity 与 count 0 同样通过。
- 本判定只关闭私有 Action phase。尚未把 I1h 接入 `generate_candidate_summary()`，尚未解决 correction 后 retryable 工具的完整链预算，也不代表 C4b-1 或父 `A4-Gate` PASS。

最终单题状态：`A4-Gate-C4b-1-I1h = RETRY → RETRY（R1）→ RETRY（R2）→ RETRY（R3）→ PASS（R4）`。行为 PASS 后，助手只删除完成 TODO、更新模块状态并做清理后回归，不修改 I1h 任一行为行。

I1h 机械收口：助手只删除已完成的 I1h TODO 注释，并把模块状态改为“当前已完成 C4b-1-I1h”；R4 已由用户统一缩进，因此未再改函数体任一行为行。清理后聚焦 smoke `3/3 PASS`（initial-valid、corrected-valid、corrected-invalid 预算停止），`py_compile` / `git diff --check` 均退出 `0`；清理后源码 SHA-256 为 `16CAF1DACC33CF3EFB4A5E94A9F576F3D015E845916FCBB6A0F0EFEABB993790`，活动 TODO / `NotImplementedError` 暂时归零。

## A4-Gate-C4b-1-I2a｜I1h 主函数最小接线

编号说明：`I1a～I1h` 已完成私有 helper 构建；从 `I2a` 开始进入 `generate_candidate_summary()` 的生产主链接线组。当前只创建 `I2a`；correction 后真实工具返回 retryable 时的完整链预算属于后续独立边界，尚不创建第二个 TODO。

当前在做什么：首次 Action 模型响应已经由 I1d 解析为 `initial_action`，I1h 也已经能独立完成“首次执行 → 必要时一次 correction → 修正 Action 执行”。本检查点只把 I1h 接回候选摘要主函数，让它选出的最终 Action、消息历史和真实工具结果继续进入现有 retry / terminal outcome / Tool Observation / candidate 尾链。

为什么需要：旧主函数仍会直接拆 `initial_action` 并调用一次 `execute_tool_call()`，所以 I1h 虽已 PASS，生产路径还没有使用它。接线后，首次非法 Action 才会在真实后端调用前收到安全校验 Observation，并让模型在预算内修正一次；主函数不得再重复执行旧的首次工具调用。

用户本检查点只改 `generate_candidate_summary()` 中 `initial_action = parse_single_tool_action_response(response1)` 之后、既有 retry `if tool_result.get("ok") ...` 之前的局部段：

1. 在 I1h 调用前保存 Action phase 入口的 `step` 与日志长度。
2. 使用原 `messages / initial_action / normalized_request / client / run_context` 调用 I1h 恰好一次。
3. 从 I1h exact 四键接力最终 `messages / action / tool_result / corrections_used`；后续 assistant message、tool call ID、工具名与原始参数全部来自最终 Action，而不是始终来自 initial Action。
4. 保持 I1h 返回的 tool result 对象，不复制、不重造，也不额外执行一次首次工具。
5. 给既有 terminal classifier 提供最终真实工具调用的即时前置账本位置。关键区别：无 correction 时 phase 内只有工具调用；有一次 correction 时，phase 内会先多一条 correction 模型 step / log，而 preflight rejection 本身不占 step / log。因此必须利用 phase 入口和 `corrections_used` 定位最终工具之前的位置，不能直接把整个 phase 的入口快照传下去。
6. 保留下方既有工具 retry、terminal outcome、Observation、candidate 请求 / 解析与三键返回判断，不在本题重写。

本检查点完成后应同时成立：

- initial-valid 正常链保持 `M → T → M`，step / log `3/3`，真实 backend 一次；
- initial-invalid → corrected-valid 为 `M → M → T → M`，step / log `4/4`，correction 模型一次、真实 backend 一次；candidate 历史精确为 system → user → 原 invalid assistant → 同原 ID 的安全 validation Observation → corrected assistant → 同 corrected ID 的真实 Tool Observation；
- corrected-invalid 原样停止于 `ActionCorrectionBudgetError(reason=max_action_corrections)`，事件仅 `M → M`，无真实 backend / candidate；
- 原一次工具 retry 恢复链仍为 `M → T → T → M`，retry 使用最终 Action 的同名、同原始参数；
- corrected 后真实 nonretryable 失败或搜索零证据仍能被既有 terminal helper 识别，其收到的调用前 step / log 必须紧邻最终工具事件；
- correction API、协议解析和 executor 异常保持原异常 identity，不新增 broad catch；
- 完整 public corrected-success smoke 可继续形成 `M → M → T → M → M → M`、step / log `6/6`。

明确非目标：不修改 I1a～I1h；不修改 retry 条件 / 次数 / 常量；不处理 corrected Action 首次真实工具返回 retryable 后还需 retry + candidate + Reflection + Refinement、总计可能超过 `MAX_STEPS=6` 的可完成性问题；不做 `ActionCorrectionBudgetError → needs_manual` 公开映射；不做 candidate / Reflection / Refinement / validator 恢复、HITL 或 eval；不新建持久化测试 runner、baseline 或报告文件。

助手已即时在旧直接执行段前创建唯一内联 `TODO A4-Gate-C4b-1-I2a` 与 `NotImplementedError`；旧段暂时保留，供用户在本检查点中就地替换。完成口令：`改完了 I2a`。

I2a 用户完成信号：

> 改完了 I2a

用户原始 I2a（助手机械清理与行为复核前，稳定源码 SHA-256：`11F853A92555B0F509FE5EB4964F2B2D05EF94F606871AD6DC74FE6BCA725DEC`）：

```python
action_phase_step_before = run_context["step"]
action_phase_logs_length_before = len(run_context["logs"])
action_phase = run_tool_action_phase_with_one_correction(
    base_messages=messages,
    initial_action=initial_action,
    normalized_request=normalized_request,
    client=client,
    run_context=run_context,
)
messages = action_phase["messages"]
final_action = action_phase["action"]
tool_result = action_phase["tool_result"]
corrections_used = action_phase["corrections_used"]

assistant_message = final_action["assistant_message"]
tool_call_id = final_action["tool_call_id"]
tool_name = final_action["tool_name"]
raw_arguments = final_action["raw_arguments"]
tool_step_before = action_phase_step_before + corrections_used
tool_logs_length_before = action_phase_logs_length_before + corrections_used
```

原始改动边界：用户删除了 I2a 的 `NotImplementedError`，以一次 I1h 调用替换旧首次 direct executor，并从 I1h exact 四键接管最终 messages / Action / result / count；最终 Action 四字段与 provenance 快照均在既有 retry 分支之前完成。I2a TODO 说明仍保留；当前只保存原始证据，尚未机械删除 TODO、更新模块状态或改动 retry / terminal / candidate 尾链，以下复核以该稳定实现为准。

I2a 复核结果：`PASS`。

- 稳定源码 SHA-256 在独立矩阵前后均为 `11F853A92555B0F509FE5EB4964F2B2D05EF94F606871AD6DC74FE6BCA725DEC`。无落盘 fake / spy 共 `9/9` 条轨迹、`134/134` 个断言通过；`py_compile` / `git diff --check` 均退出 `0`，未创建 runner、fixture 或报告文件。
- initial-valid 为 `M → T → M`、模型 / 工具 `2/1`、step / log `3/3`；corrected-valid 为 `M → M → T → M`、模型 / 工具 `3/1`、`4/4`；无 correction 的旧 retry 为 `M → T → T → M`、模型 / 工具 `2/2`、`4/4`，两次 backend 收到同一最终工具名与原始参数，第一次失败没有进入 Observation。
- corrected-valid 的 candidate 历史严格为 system → user → initial invalid assistant → 同 initial ID 的 validation Observation → corrected assistant → 同 corrected ID 的真实 Tool Observation。两个 assistant 各出现一次且 identity 保持；I1h 返回的 tool result identity 保持；原 base messages 仍为两段；I1h 恰好一次，旧首次 direct executor 没有重复发生，candidate 仍使用 `tool_choice="none"`。
- corrected-invalid 为 `M → M`、模型 / 工具 `2/0`、`2/2`，原样抛 `ActionCorrectionBudgetError(reason=max_action_corrections, current_step=2, corrections_used=1)`；没有第二次 correction 模型、真实 backend、terminal 或 candidate。
- correction API 与协议异常均在 `2/2` 停止，异常对象 identity 与各自失败 / 成功模型日志保留，工具和 candidate 为 0。corrected nonretryable 与 search zero-evidence 均为 `M → M → T`、`3/3`；terminal helper 实际收到最终工具前 `2/2`、执行后 `3/3`，分别识别 `tool_failure` / `insufficient_evidence`，证明 phase 起点加 `corrections_used` 的 provenance 正确。
- 完整 public corrected-success smoke 为模型 `5`、真实工具 `1`、事件 `M → M → T → M → M → M`、step / log `6/6`，最终严格三键 `success`；恰好停在 `MAX_STEPS=6`，无隐藏第 7 次调用。该证据只覆盖 correction 后工具直接成功，没有把 correction 后仍需 retry 的 7 步链冒充已解决。
- 静态范围核对确认 I1h、预算常量、既有 retry 条件 / 次数、terminal / candidate / Reflection / Refinement / validator 与公开映射均未改；`corrections_used` 在 I2a 只用于 terminal provenance，没有越界改变 retry 决策。

最终单题状态：`A4-Gate-C4b-1-I2a = PASS`。行为无修正缺口；助手接下来只删除已完成的 I2a TODO、把模块状态改为“当前已完成 C4b-1-I2a”并做清理后 smoke，不修改用户接线实现或创建下一预算行为。

I2a 机械收口：助手只删除已完成的 I2a TODO 注释，并把模块状态改为“当前已完成 C4b-1-I2a”；用户的接线行为代码逐行保持不变。清理后独立回归 `7/7 PASS`：initial、corrected、双非法预算、旧 retry、两个 terminal outcome 与 public corrected success 均保持原轨迹；另一本地 public corrected-success / double-invalid 聚焦 smoke `2/2 PASS`，完整成功仍为 `M → M → T → M → M → M`、`6/6`。`py_compile` / `git diff --check` 均退出 `0`；清理后源码 SHA-256 为 `62A88434847DB184B80EE97DCFDA1B673821EEF5EBE1BA705011ED97BB6D8B52`，测试前后稳定，活动 TODO / `NotImplementedError` 再次归零。

## A4-Gate-C4b-1-I2b｜纯 Tool Retry 完整链预算门

当前在做什么：I2a 已让 corrected Action 真正进入旧 retry 尾链，但当前旧分支只检查“还有没有一次工具 retry 额度”，没有检查“即使 retry 成功，剩余全局 step 是否还够 candidate、Reflection 与 Refinement 全部完成”。I2b 先只建立一个无副作用的完成性预算函数，不接生产 retry。

两个预算必须分层：

- 本地 retry 额度回答“规则还允不允许再试一次”，由现有 `MAX_TOOL_RETRIES` 与 retry 分支所有；
- I2b 全局完成性预算回答“花掉这次 retry 后，能不能在 `MAX_STEPS` 内完成可信最终结果”。它不读取本地 retry 次数，避免未来把“次数耗尽”和“次数尚在但完整链预算不足”误报成同一停止原因。

最小函数签名：

```python
def has_tool_retry_completion_budget(current_step: int) -> bool:
    ...
```

`current_step` 的含义：第一次真实工具尝试已经完成、step / log 已记账，并由调用者确认结果是 retryable failure 时的累计全局 step。函数本身不接收 `run_context`、tool result、retry 次数、correction 次数、messages 或 client，也不重新证明调用来源。

输入合同：只接受内建 `int`，合法数学域与全局计数器一致，为 `1..MAX_STEPS`；`bool`、float、字符串、容器、`None`、int 子类、0、负数和大于全局上限都抛固定安全文本的 `ValueError`。step 1 在当前 retry 调用点不可达，但作为纯全局预算函数仍是合法计数器输入，不把流程拓扑硬编码进数学 helper。

完成性规则：retry 决策后若要得到可信最终结果，还需“retry 工具 → candidate → Reflection → Refinement”四次调用。现有 `MIN_STEPS_AFTER_VALID_ACTION=4` 已表达同一完整链长度：在首次合法 Action 场景是“工具 + 三个模型”，在 retry 场景由“retry 工具”占据工具位置。因此直接复用该常量，不新增同值常量，也不能再额外加一次 correction 步数。

当前常量下的真值表：

| `current_step` | 剩余 step | 预期 |
|---:|---:|:---|
| 1 | 5 | `True`（数学上充足，当前 retry 调用点不可达） |
| 2 | 4 | `True`（无 correction：恰好容纳 retry + 三个模型） |
| 3 | 3 | `False`（有 correction：差一步，必须在第二次工具副作用前停止） |
| 4 | 2 | `False` |
| 5 | 1 | `False` |
| 6 | 0 | `False` |

用户本检查点只实现该纯函数。完成标准：

- 上述六个合法输入返回 exact 内建 `bool`，临界点为 step 2 `True`、step 3 `False`；
- 严格类型 / 范围反例全部为 `ValueError`，错误文本不拼输入 repr；
- 结果随 `MAX_STEPS / MIN_STEPS_AFTER_VALID_ACTION` 常量变化，不硬编码 2、3、4 或 6；
- 重复调用稳定，输入与三个预算常量不变；
- reserve / log / model / tool / retry / candidate / I1a / I1h 等 spy 或 bomb 全部为 0；
- 不执行 retry，不抛业务停止异常，不做 `needs_manual` 映射，不调用真实 DeepSeek 或真实工具。

明确非目标：不修改 `generate_candidate_summary()`、现有 retry 条件 / 次数、任何预算常量、I1h / I2a、terminal / candidate / Reflection / Refinement；不检查 tool result 是否 retryable；不消费本地 retry quota；不设计新的停止 reason；不创建下一接线 TODO、测试 runner、baseline 或报告文件。

助手已在两个预算纯函数相邻区域即时创建唯一 I2b TODO / `NotImplementedError`。完成口令：`改完了 I2b`。

I2b 用户完成信号：

> 改完了 I2b

用户原始 I2b（助手机械清理与行为复核前，稳定源码 SHA-256：`E7F462A924A19F4E0450558B746681CCBF0BF684738249268D64380E30E0C210`）：

```python
if not type(current_step) is int or not (1 <= current_step <= MAX_STEPS):
    raise ValueError("current_step 必须是位于 1 到全局上限之间的整数")

remaining_steps = MAX_STEPS - current_step
return remaining_steps >= MIN_STEPS_AFTER_VALID_ACTION
```

原始改动边界：用户删除了 I2b 的 `NotImplementedError`，保留 TODO / docstring，只在纯函数体内加入严格输入门、剩余步数计算和内建 bool 比较；没有接收或修改 `run_context`，没有读取本地 retry quota，也没有接入 `generate_candidate_summary()`、执行 retry、构造停止异常或创建 I2c。当前只保存原始证据，行为完整通过前不机械清理 TODO / 模块状态。

I2b 复核结果：`PASS`。

- 稳定源码 SHA-256 在测试前后均为 `E7F462A924A19F4E0450558B746681CCBF0BF684738249268D64380E30E0C210`。独立终审 `32/32 PASS`，本地真值 / 输入 / 动态常量 / AST 聚焦探针 `37/37 PASS`；`py_compile` / `git diff --check` 均退出 `0`，未调用真实 DeepSeek / 工具，未创建测试文件。
- 当前常量真值表精确为 `1:T, 2:T, 3:F, 4:F, 5:F, 6:F`，每个返回值均为 exact 内建 bool。无 correction 的首次工具失败在 step 2，正好保留 retry + candidate + Reflection + Refinement 四步；corrected 工具失败在 step 3，只剩三步，预算门返回 False。
- 16 个严格反例覆盖 bool、float、str、None、list / dict / tuple / bytes、int 子类、0、负数与 `>MAX`，全部为 exact `ValueError` 且固定安全文本；没有把输入 repr 拼入异常。
- 多组内存动态配置与本地 exact-fit / 差一步探针均随 `MAX_STEPS / MIN_STEPS_AFTER_VALID_ACTION` 改变，证明实现没有硬编码 2、3、4 或 6；测试后常量完整恢复。
- 100 轮重复调用稳定，预算常量、registry 与日志字段不变；15 个 reserve / log / model / tool / retry / candidate / I1a / I1h / downstream seam bomb 均为 0。AST 只包含纯 builtin `type` 与 `ValueError` 构造，只读取两个预算常量，不读取 `MAX_TOOL_RETRIES` 或 correction 次数，也没有 global / nonlocal / async / yield。
- 第一次本地 AST 探针曾把 `ValueError(...)` 的异常构造也误列为副作用调用而退出 1；修正探针只禁止真实 reserve / log / model / tool 等副作用后，原实现无需修改即 `37/37 PASS`。该红灯属于测试定义错误，不是用户行为 RETRY。
- 范围核对确认 helper 仍未被任何生产控制流调用；现有 retry、I1h / I2a、预算常量、terminal / candidate / Reflection / Refinement / validator 与公开映射均未改，I2c 尚未开始。

最终单题状态：`A4-Gate-C4b-1-I2b = PASS`。行为无修正缺口；助手接下来只删除已完成 TODO、把模块状态改为“当前已完成 C4b-1-I2b”并做清理后纯函数回归，不改函数实现或接入 retry。

I2b 机械收口：助手只删除已完成的 I2b TODO 合同注释，并把模块状态改为“当前已完成 C4b-1-I2b”；`has_tool_retry_completion_budget()` 的判断、错误文本、常量读取和返回逻辑逐字未改，也没有接入生产 retry。清理后真值表仍为 `1:T, 2:T, 3:F, 4:F, 5:F, 6:F`，11 个聚焦非法输入全部为 exact `ValueError`，4 组动态常量表与 AST 纯度检查通过；`py_compile` / `git diff --check` 均退出 `0`，全文件 TODO / `NotImplementedError` 暂时归零。清理后源码 SHA-256 为 `B31EB05C4E1D2D5DDCC6C5C5562B2272C35FCA94719E6A67F657EC0F4982874C`。

## A4-Gate-C4b-1-I2c｜在第二次真实工具调用前接入完整链预算门

当前在做什么：I2b 已经能回答“从当前 step 出发，执行一次工具 retry 后是否仍有足够调用位完成 candidate、Reflection 与 Refinement”，但它还是未接线的纯函数。I2c 只把这个判断放到既有 retry 分支里，并且必须发生在第二次真实工具调用、retry 快照、消息追加和任何后续模型调用之前。

为什么需要：无 correction 时，首次模型 + 首次工具失败后位于 step 2，还剩 4 步，恰好能容纳 `retry 工具 + candidate + Reflection + Refinement`；发生过一次 Action correction 后，修正工具首次失败时已到 step 3，只剩 3 步。如果仍执行 retry，即使工具成功也无法在 `MAX_STEPS=6` 内产出经过 Reflection / Refinement 的可信最终结果，所以策略 B 要在已知无法完成时提前止损。

与前后步骤的关系：

- 前置 I2b 只负责预算数学；I2c 负责在真实控制流中消费它，不重算公式、不读取 correction 次数。
- 现有 `MAX_STEPS` 硬门仍先执行：已经到硬上限时继续使用旧 `AutoRecoveryExhaustedError(reason="max_steps")`，不能被新的完成性判断覆盖。
- 助手已补入同级内部窄异常 `ToolRetryCompletionBudgetError`。它只保存 `current_step / attempts / last_error`，异常文本固定且不回显工具错误；它不继承旧恢复异常，也不复用 Action correction 异常。
- 当前公开入口会把 `AutoRecoveryExhaustedError` 中除 `max_steps` 外的原因一律写成“工具重试上限”。因此 I2c 不修改公开 catch，也不返回 `needs_manual`；新窄异常先保持原对象向上传播，I2d 再单独负责正确公开映射。

用户需要亲自完成：只替换 retry 分支内唯一 I2c `NotImplementedError`。先用当前 `run_context["step"]` 恰好调用一次 `has_tool_retry_completion_budget()`；返回 False 时，在下方 retry 快照和第二次 `execute_tool_call()` 之前抛 `ToolRetryCompletionBudgetError`，其中 `current_step` 使用当前累计 step，`attempts=1` 表示已经发生一次真实后端尝试，`last_error` 使用首次 retryable 工具结果中的安全错误；返回 True 时不做额外处理，让下方既有 retry 路径原样继续。

不要改动：I2b 函数、三个预算 / retry 常量、I1h / I2a、`MAX_STEPS` 旧硬门、第二次工具调用及其二次失败耗尽逻辑、terminal / candidate / Reflection / Refinement / validator，以及 `run_research_summary_once()` 的公开 catch。不要新增 reason 字符串、手写预算算式、根据 correction 次数硬编码 step 2 / 3、捕获或改写 I2b 的 `ValueError`，也不要创建 I2d。

完成标准：

- 无 correction 的 retryable 首败在 step 2：I2b 恰好调用一次且参数为 2，返回 True，第二次真实工具调用仍执行一次，旧 `M → T → T → M` 候选链不回归；
- corrected Action 的 retryable 首败在 step 3：I2b 恰好调用一次且参数为 3，抛 exact `ToolRetryCompletionBudgetError`；`current_step=3`、`attempts=1`、`last_error` 正确，第二次 backend / candidate / Reflection / Refinement 均为 0，step / log 保持 `3/3`，messages 与首次结果不变；
- 已到 `MAX_STEPS` 时仍走旧 `max_steps` 信号，并且 I2b / 第二次工具均为 0；工具成功、不可重试失败、preflight rejection 与本地无 retry 额度时均不调用 I2b；
- I2b 抛出的异常原对象传播；新窄异常经过公开入口仍原对象传播，当前不被误映射为 `needs_manual`；既有 `max_steps / max_tool_retries` 行为不回归；
- 不调用真实 DeepSeek；使用内存 fake / spy 验证调用顺序、identity、step / log 与零副作用，最后通过 `py_compile` / `git diff --check`。

助手已新增窄异常脚手架，并在旧 hard-max 门之后、retry 快照与第二次工具调用之前即时创建唯一 I2c TODO / `NotImplementedError`；没有写入预算分支答案，也没有改公开映射。完成口令：`改完了 I2c`。

I2c 用户完成信号：

> 改完了 I2c

用户原始 I2c（助手机械清理与行为复核前，稳定源码 SHA-256：`ACDE2D6A4FEA5066404D24DED44DB4160E9C7C578823D14A7B61A7729952A104`）：

```python
retry_completion_budget = has_tool_retry_completion_budget(
    run_context["step"]
)
if not retry_completion_budget:
    raise ToolRetryCompletionBudgetError(
        current_step=run_context["step"],
        attempts=1,
        last_error=tool_result.get("error"),
    )
```

原始改动边界：用户只删除 I2c 的 `NotImplementedError`，保留 TODO，并在旧 `max_steps` 硬门之后、retry 快照与第二次 `execute_tool_call()` 之前加入一次 I2b 调用和 False 分支窄异常；没有改 I2b 数学、预算 / retry 常量、旧 hard-max、既有 retry / terminal / candidate 尾链、I1h / I2a 或公开 catch，也没有创建 I2d。当前先保存原始证据，行为完整通过前不删除 TODO、不改模块完成状态、不做机械整理。

I2c 复核结果：`PASS`。

- 用户原始源码 SHA-256 在冻结矩阵前后均为 `ACDE2D6A4FEA5066404D24DED44DB4160E9C7C578823D14A7B61A7729952A104`。独立终审为 `10/10` 场景、`113/113` 断言 PASS；生产语义复核同样 PASS；本地主审额外 8 项检查确认新异常字段 / 安全文本、静态调用顺序、唯一 I2b 调用和公开层同对象透传。
- 普通无 correction retry 精确为 `M → T → budget(2) → T → M`，I2b 在 step/log `2/2` 恰好调用一次，第二次 backend 与 candidate 各一次，最终 step/log `4/4`；同一最终工具名 / 原始参数和 retry 成功结果继续进入既有候选链。
- corrected retry 精确为 `M → M → T → budget(3) → stop`：抛 exact `ToolRetryCompletionBudgetError(current_step=3, attempts=1, last_error="temporary-one")`，固定异常文本不回显错误；第二次 backend、terminal、candidate、Reflection 与 Refinement 均为 0，step/log 保持 `3/3`，phase messages / Action / 首次 tool result identity 与内容不变。
- hard max 继续优先发旧 `AutoRecoveryExhaustedError(reason="max_steps")`，I2b 与第二次工具均为 0；成功、真实 nonretryable、preflight rejection 与本地 retry quota 为 0 时也不调用 I2b。I2b sentinel 异常保持同一对象上抛。
- 普通两次 retryable failure 仍走旧 `max_tool_retries`，公开结果继续为 `needs_manual`，轨迹 `M → T → T`、step/log `3/3`；新窄异常经过 `run_research_summary_once()` 时则保持同一对象向上传播，没有被误写成工具重试上限，也没有提前完成 I2d。
- 源码顺序精确为 `旧 hard max → I2b → retry 快照 → 第二次 execute_tool_call`；生产 I2b 调用点只有一个，没有在 I2c 重算预算、读取 correction 次数或改下游。`py_compile` / `git diff --check` 均退出 `0`。
- 第一次本地主审聚焦探针因助手拼接脚本时漏了一个换行，在载入待测模块前即产生探针自身 `SyntaxError`；同批源码 `py_compile` / diff-check 已通过。修正一次性探针后，用户源码无需修改即 `8/8 PASS`，因此该红灯不属于用户 RETRY。

最终单题状态：`A4-Gate-C4b-1-I2c = PASS`。没有行为修正缺口；助手接下来只删除已完成 TODO、把模块状态改为“当前已完成 C4b-1-I2c”并做清理后回归，不改用户实现、异常脚手架、I2b、retry 尾链或公开映射。

I2c 机械收口：助手只删除已完成的 I2c TODO 合同注释，并把模块状态改为“当前已完成 C4b-1-I2c”；用户的 I2b 调用、False 分支异常参数和相邻 hard-max / retry 控制流逐字未改。清理后决定性回归 `5/5` 场景、`45/45` 断言 PASS：initial retry 仍为 `M,T,T,M` 与 `4/4`，corrected retry 仍在 `M,M,T` / `3/3` 发 exact 新窄异常，hard max 继续优先，public 仍同对象透传新异常，旧双 retry 仍公开映射为工具重试上限。`py_compile` / `git diff --check` 均退出 `0`，TODO / `NotImplementedError` 归零；清理后源码 SHA-256 为 `ACEE12A3D2393480C862AD2C06DF51BB699215C22638D2ABC1E3BC3DF65A8B0C`。

## A4-Gate-C4b-1-I2d｜将预算窄异常公开映射为 needs_manual

当前在做什么：I1e / I1h 和 I2c 已能在内部精确区分三种预算停止事实，但公开入口目前只认识旧 `AutoRecoveryExhaustedError`。因此 Action correction 两种预算停止仍会抛出 Python 异常，I2c 的工具完整链预算停止也会原对象向上传播。I2d 只在 `run_research_summary_once()` 的表示层把这三个已识别信号转换为严格三字段 `needs_manual`，不再改变任何预算或工具行为。

为什么需要：内部异常适合程序分流，但调用者需要稳定的业务结果。映射必须保留因果差异，不能把“Action 修正次数耗尽”“Action 修正完整链预算不足”“工具 retry 完整链预算不足”都写成工具重试上限；否则日志看似停止正确，用户却会得到错误的接管原因。

三个公开原因必须区分：

| 内部信号 | 已发生的事实 | summary 必须说明 | 禁止误写 |
|---|---|---|---|
| `ActionCorrectionBudgetError(max_action_corrections)` | 两次 Action 都未通过执行前校验，真实工具 0 次 | 局部 Action 修正上限、已用次数 / 当前 step、最后一条安全校验错误、无真实证据、人工接管 | 工具执行失败 / 工具 retry 上限 |
| `ActionCorrectionBudgetError(insufficient_completion_steps)` | 局部额度可能仍在，但已无法容纳 correction + 后续可信链，真实工具 0 次 | 剩余调用预算不足、当前 step / 已用修正次数、最后安全校验错误、未继续纠错、无真实证据、人工接管 | 达到 Action 修正上限 / 达到总 step 上限 |
| `ToolRetryCompletionBudgetError` | 第一次真实工具已执行并返回 retryable；第二次工具尚未执行 | retry + 后续可信链预算不足、当前 step / 实际尝试次数、最后安全工具错误、第二次工具未执行、证据缺口、人工接管 | 工具 retry 次数已经耗尽 / 真实工具从未执行 |

用户需要亲自完成：只替换公开入口唯一 I2d `NotImplementedError`。

1. 先按异常的 exact 类型分流；如果是 `ActionCorrectionBudgetError`，再使用 `if / elif` 显式处理两个冻结 reason，未知 reason 必须 bare `raise`，不能默认归到任一文案。
2. 为三个已知原因分别构造含义不同的 `summary_text`。可以使用受控内部事实 `current_step / corrections_used / attempts`、可信常量以及上游已经安全化的 `last_error`；最后错误必须有明确标签。不得使用 `str(exc)` / `repr(exc)`，不得回显原始 tool arguments、topic/path、prompt、messages、request_id 或内部 logs。
3. 每个已知原因只通过 `make_result("needs_manual", summary_text, [])` 返回，顶层不能增加 reason、step、attempts 或 error 字段。

安全边界说明：本合同要求把“最后一条已经安全化的错误”交给人工，因此 summary 应包含受控的 `exc.last_error`；这不等于公开整个内部异常。尤其 `ActionCorrectionBudgetError.__str__()` 还混有内部 reason / step 等表示，不能直接拿来当公开文案。测试会分别验证需要的安全错误可见，同时请求原值、raw args、prompt、request_id 和日志不会被带入结果。

完成标准：

- 注入 Action 的两个 reason 与 Tool Retry Completion 三个异常时，分别返回 exact 内建 dict，键集只有 `status / summary / sources`，状态为 `needs_manual`、`sources` 是新的空内建 list，三条 summary 因果不同且包含对应阈值 / 计数、受控最后错误、证据缺口和人工接管；
- 篡改为未知 Action reason 时，同一异常对象继续上抛、`make_result` 为 0；普通 API / protocol / `ValueError` 等无关异常也保持同一对象上抛，证明没有 broad catch；
- 旧 `AutoRecoveryExhaustedError(max_steps / max_tool_retries)` 的状态和文案逐字不变，旧 `TerminalToolOutcomeError(tool_failure / insufficient_evidence)` 也不回归；
- 正常候选仍进入 Reflection / Refinement 并返回其原结果；真实 corrected-invalid public smoke 为 `M,M` / 工具 0 / `needs_manual`，真实 corrected-retry-budget public smoke 为 `M,M,T` / 第二次工具 0 / `needs_manual`；
- 不调用真实 DeepSeek，不创建持久化 runner；用内存异常注入和现有 fake / spy 做验证，最后通过 `py_compile` / `git diff --check`。

明确非目标：不改两个预算异常类、I1a～I2c、预算常量 / 数学、Action / 工具 / retry 控制流、旧 AutoRecovery / Terminal catch、candidate / Reflection / Refinement / validator；不映射 API、protocol、普通 `RuntimeError` 或 `ValueError`；不做 HITL、eval、知识库同步，也不创建后续 TODO。

助手已在旧 AutoRecovery catch 与 Terminal catch 之间即时创建一个只捕获 `ActionCorrectionBudgetError / ToolRetryCompletionBudgetError` 的 tuple catch，并放入唯一 I2d TODO / `NotImplementedError`；没有预填三条 summary 或分流答案。完成口令：`改完了 I2d`。

I2d 用户完成信号：

> 改完了 I2d

用户原始 I2d（助手机械清理与行为复核前，稳定源码 SHA-256：`30883C86D8A6D833C9E596B884121571544C9E494D4CE2E0F7D99448403AC079`）：

```python
if type(exc) is ActionCorrectionBudgetError:
    if exc.reason == "max_action_corrections":
        summary_text = (
            "Action 修正已停止：已达到局部 Action 修正上限 "
            f"{MAX_ACTION_CORRECTIONS} 次；已使用修正 {exc.corrections_used} 次，"
            f"当前累计 step 为 {exc.current_step}；"
            f"最后一条安全校验错误：{exc.last_error}。"
            "尚未执行真实工具，因此没有可用于生成可信摘要的真实证据；"
            "已停止自治并交由人工决定下一步。"
        )
    elif exc.reason == "insufficient_completion_steps":
        summary_text = (
            "Action 修正已停止：当前累计 step 为 "
            f"{exc.current_step}，全局上限为 {MAX_STEPS}；"
            "继续还需 1 次 Action correction，并在取得合法 Action 后"
            f"至少保留 {MIN_STEPS_AFTER_VALID_ACTION} 个调用 step，"
            "剩余全局预算无法容纳这条完整可信链；"
            f"已使用 Action 修正 {exc.corrections_used} 次；"
            f"最后一条安全校验错误：{exc.last_error}。"
            "本次未继续纠错，也尚未执行真实工具，"
            "因此没有可用于生成可信摘要的真实证据；"
            "已停止自治并交由人工决定下一步。"
        )
    else:
        raise
elif type(exc) is ToolRetryCompletionBudgetError:
    summary_text = (
        "工具完整链重试已停止：第一次真实工具尝试返回可重试失败；"
        f"当前累计 step 为 {exc.current_step}，全局上限为 {MAX_STEPS}，"
        "而工具 retry、candidate、Reflection 与 Refinement "
        f"的完整可信链至少需要 {MIN_STEPS_AFTER_VALID_ACTION} 个调用 step，"
        "剩余全局预算不足；"
        f"工具实际尝试 {exc.attempts} 次；"
        f"最后一条安全工具错误：{exc.last_error}。"
        "第二次工具调用尚未执行，当前仍缺少可用于生成可信摘要的成功工具证据；"
        "已停止自治并交由人工决定下一步。"
    )
else:
    raise
return make_result("needs_manual", summary_text, [])
```

原始改动边界：用户只删除 I2d 的 `NotImplementedError`，保留 TODO，并在既有窄 tuple catch 内完成 exact 类型、Action 两 reason、三个不同 summary、两个 bare raise 与统一 `make_result("needs_manual", ..., [])`；没有改异常类、I1a～I2c、预算 / retry / 工具控制流、旧 AutoRecovery / Terminal catch、正常 Reflection 链或创建后续 TODO。当前先保存原始证据，行为完整通过前不删除 TODO、不改模块完成状态、不做机械整理。

I2d 复核结果：`PASS`。

- 用户原始源码 SHA-256 在测试前后均为 `30883C86D8A6D833C9E596B884121571544C9E494D4CE2E0F7D99448403AC079`。独立终审 `15/15` 场景、`138/138` 断言 PASS；生产语义矩阵全部 PASS；本地主审异常注入 / 旧回归 / 正常接力为 `36/36` 检查通过。
- 三个新预算信号都返回 exact 内建 dict，键集只有 `status / summary / sources`，状态为 `needs_manual`，每次 `sources` 都是新的空内建 list。三段 summary 两两不同，分别准确说明 Action 局部修正上限、Action correction + 后续完整链预算不足、工具 retry + 后续完整链预算不足。
- 三段 summary 都公开了受控 step / 次数 / 阈值和上游已经安全化的 `last_error`，并说明证据缺口与人工接管；没有使用 `str/repr(exc)`，也没有泄漏 raw arguments、request 原值、prompt、messages、request_id、client secret 或内部 logs。工具预算文案明确第一次工具已经执行、第二次尚未执行，没有误写成 retry quota 耗尽。
- 未知 Action reason、两个预算异常的子类以及无关 `ToolActionProtocolError` / API / `ValueError` 等异常保持同一对象上抛，映射结果为 0；exact 类型与 reason 均 fail-closed，没有 broad catch。
- 旧 `AutoRecoveryExhaustedError(max_steps / max_tool_retries)` 两条文案逐字不变；旧 `TerminalToolOutcomeError(tool_failure / insufficient_evidence)` 状态、detail 与文案逐字不变；正常 gen result 仍只进入一次 Reflection / Refinement，并返回其结果对象 identity。
- 真实 corrected-invalid smoke 为 `M,M`、真实工具 0、step/log `2/2`、Reflection 0、严格 `needs_manual`；真实 corrected-retry-budget smoke 为 `M,M,T`、真实工具 1（第二次 0）、step/log `3/3`、Reflection 0、严格 `needs_manual`。两条都只包含相应安全错误，没有第 7 次或其他越界调用。
- `py_compile` / `git diff --check` 均退出 `0`，`NotImplementedError=0`；唯一遗留是已完成 I2d TODO 与模块“当前进行”文字。

最终单题状态：`A4-Gate-C4b-1-I2d = PASS`。没有行为修正缺口；助手接下来只删除完成 TODO、把模块状态改为“当前已完成 C4b-1-I2d”并做清理后回归，不改用户三段 summary、两个 bare raise、tuple catch、旧映射或生产控制流。

I2d 机械收口：助手只删除已完成的 I2d TODO，并把模块状态改为“当前已完成 C4b-1-I2d”；用户的三段 summary、两个 bare `raise`、窄 tuple catch、旧 AutoRecovery / Terminal catches 与生产控制流逐字未改。清理后独立决定性回归 `15/15` 场景、`150/150` 断言 PASS：三种新映射、未知 / 无关异常 identity、四条旧公开映射、正常成功路径与两条真实组合 smoke 全部保持；corrected-invalid 仍为 `M,M` / 工具 0 / `2/2`，corrected retry-budget 仍为 `M,M,T` / 工具 1（第二次 0）/ `3/3`。项目 `.venv` 下 `py_compile` 与 `git diff --check` 均退出 `0`，TODO / `NotImplementedError` 为 `0/0`；清理后稳定源码 SHA-256 为 `90334CAC4589E08FA1346065E34FFC79CBBE6A18F99407339A5798CF6ED1FC7C`。

## A4-Gate-C4b-1-U1｜端到端轨迹与公开结果用户审核

当前在做什么：实现、故障注入和回归都已完成；本检查点不再写代码，而是让用户审核最终行为是否仍符合自己选择的“可完成性优先（策略 B）”，并确认工具副作用边界与公开结果没有偏离原意。

已验证轨迹：

| 场景 | 调用轨迹 | step / logs | 真实工具 | 公开结果 |
|---|---|---:|---:|---|
| 首次 Action 非法 → correction 合法 → 工具成功 | `M → M → T → M → M → M` | `6 / 6` | 1 次 | `success` |
| 首次 Action 非法 → correction 仍非法 | `M → M` | `2 / 2` | 0 次 | `needs_manual`；Action 修正上限；`sources=[]` |
| 初始 Action 合法 → 工具首败 → retry 成功 | `M → T → T → M → M → M` | `6 / 6` | 2 次 | `success` |
| 首次 Action 非法 → correction 合法 → 工具返回 retryable 首败 | `M → M → T` | `3 / 3` | 1 次；第二次为 0 | `needs_manual`；完整可信链预算不足；`sources=[]` |

用户审核问题：这四条轨迹、调用次数、工具副作用边界和公开结果是否符合你选择的“可完成性优先（策略 B）”？如果符合，回复“审核通过”；如果不符合，指出具体哪一行以及希望改变什么行为。

用户原始审核答复：

> 审核通过

审核判定：`PASS`。用户明确确认四条端到端轨迹、调用次数、真实工具副作用边界与公开 `success / needs_manual` 结果符合其选择的“可完成性优先（策略 B）”；没有提出合同变化或实现异议，因此不创建修正检查点。

完成标准：用户明确确认四行行为与自己的策略意图一致；若有异议，先冻结具体异议并区分合同变化与实现缺陷，不直接改代码。`U1` 不创建源码 TODO / `NotImplementedError`，不重复测试已经通过的实现细节。

单题状态：`A4-Gate-C4b-1-U1 = PASS`。`C4b-1` 本日教学 / 实现子单元收口；这不是父 `A4-Gate` 的正式 PASS，`tracker/progress.md` 保持 `DOING`。

U1 收口同步：助手仅把源码模块的后续范围说明从已经完成的 Action correction 接线改为仍未完成的 candidate / Reflection / 最终 validator 失败恢复、安全确认与 eval；没有修改任何运行逻辑。同步后项目 `.venv` 下 `py_compile` 与 `git diff --check` 继续退出 `0`，TODO / `NotImplementedError` 为 `0/0`，当前源码 SHA-256 为 `D212A8A6A5B1C70F80B33B420C2253AD2A1F19714FA98984689A81A8CD57B173`。

## 工程验证

运行提示：I1c 行为通过并清除旧 TODO 后，已即时创建当前唯一活动骨架 `parse_single_tool_action_response()`；助手只固定响应层级、结构 / 内容职责边界、返回四键和非目标，核心解析逻辑待用户实现。

验证命令与关键结果：

- 骨架创建后运行 `.venv/Scripts/python.exe -m py_compile code/stage4/a4_gate_research_summary_agent.py`，退出码 `0`；说明文件语法与导入阶段无错误，尚不代表 TODO 行为通过。
- I1a 骨架创建时，`rg` 曾确认只有一个 I1a TODO / `NotImplementedError`；I1a 通过后已清理该标记，当前只保留一个 I1b TODO / `NotImplementedError`，没有提前创建模型调用或接线骨架。
- `git diff --check` 退出码 `0`；只有 Git 的 LF → CRLF 工作区提示，无空白错误。
- I1a 用户实现后运行无落盘边界探针：本地 `21/21 PASS`，独立只读复核 `31/31 PASS`；`py_compile` 退出码 `0`。
- I1b 首次无落盘探针：`9/10` 组通过；唯一失败为 `preserve_original_tool_call_id`，探针退出码 `1`。`py_compile` 退出码 `0`；`git diff --check` 因第 205 行 trailing whitespace 退出码 `2`。完成最小修正后原样复测。
- I1b-R1 修后探针：本地 `15/15 PASS`，独立复核无失败，原 ID 精确关联恢复；机械清理后 `py_compile` 与 `git diff --check` 均为退出码 `0`。
- I1c 完成后使用内存 fake / spy 验证一次成功、同一异常上抛、step 6 硬门、输入边界、messages identity / 顺序不变和工具零触发；不在孤立响应阶段调用真实 DeepSeek。
- I1c 骨架创建后 `py_compile` 退出码 `0`；`rg` 确认只有 I1c 一个活动 TODO / `NotImplementedError`，`git diff --check` 退出码 `0`。
- I1c 用户实现后本地 `22/22 PASS`、独立复核 `43/43 PASS`；已清除旧 TODO，未调用真实 DeepSeek。
- I1d 完成后使用内存属性对象验证正常外壳、两个内容透传代表、结构错误等价类、错容器和零副作用；不接现有主控制流、不调用真实 DeepSeek。
- I1d 首次实现探针：合法 SDK 属性对象被错误的 dict 外壳门拒绝；本地两组探针分别为 `27/31 PASS` 与 `0/8 PASS`，独立矩阵报告 13 个行为失败；`py_compile` / `git diff --check` 通过，所有副作用 spy 为 0。保留同一 I1d TODO 进入 R1。
- I1d-R1 属性式解析探针：本地 `34/37 PASS`，仅剩 response 真值误判、finish reason str 子类放行与 name None 放行；独立矩阵确认合法 / 内容透传、28/30 核心结构错误、输入不变及零副作用通过。保留同一 TODO 进入 R2。
- I1d-R2 最终探针：本地 `40/40 PASS`；独立复核合法 / 内容透传 `8/8`、结构错误 `30/30`，输入不变与所有副作用 bomb 为 0；`py_compile` / `git diff --check` 通过。行为 PASS 后由助手机械清除旧骨架与空白。
- I1d 通过后的机械复用：助手把 `generate_candidate_summary()` 首次响应的重复手写解析替换为一次 `parse_single_tool_action_response(response1)`，再从 I1d 四键结果解出 assistant / ID / name / arguments；未改后续执行、工具重试或返回合同。无落盘回归 `13/13 PASS`：topic / path 正常链均为 `M → T → M`、step/log `3/3`，一次 retry 恢复为 `M → T → T → M`、step/log `4/4`；九类 Action 内容错误仍到达 executor 预检且真实后端 / tool step / log 为 0；首次结构错误为 exact `ToolActionProtocolError` 且轨迹只有首个模型日志。`py_compile` / `git diff --check` 通过。
- I1e 启动：助手根据 G1 与 I1a～I1d 已通过合同补入独立内部 `ActionCorrectionBudgetError` 脚手架，并即时创建 `request_and_parse_action_correction()` 唯一 TODO；未写用户核心编排答案，未改 `generate_candidate_summary()` 的纠错分支。创建后 `py_compile` / `git diff --check` 退出 `0`，`rg` 精确确认仅 I1e 一个 TODO / `NotImplementedError`。
- I1e 首次实现探针：五条核心轨迹加 fake client 注入共 `2/6 PASS`；API / 协议异常账本通过，成功消息接力、两类预算异常与 fake client 门失败；工具 / 后续调用为 0。`py_compile` 通过，`git diff --check` 报 5 处 trailing whitespace；保留同一 I1e TODO 进入 R1。
- I1e-R1 探针：核心五轨、四段消息、两个预算 reason、fake client、exact 外壳与零工具边界均通过；扩展输入所有权矩阵合计 `17/21 PASS`，四个失败均由 I1e 重复校验把 I1b / I1a 的 `ValueError` 合同改写为 `ToolActionProtocolError`。保留同一 TODO 进入 R2；没有真实 DeepSeek 调用。
- I1e-R2 探针：用户只删除两段重复校验；本地 `21/21 PASS`，独立复核五轨 `5/5`、所有权 `6/6`，所有真实工具和下游 bomb 为 0。行为 PASS 后进入助手机械收口；没有真实 DeepSeek 调用。
- I1e 机械收口：助手先记录用户 R2 删除型改动，再删除已完成 TODO、只规范 I1e 缩进 / 空行与 3 处尾空白、更新模块状态文字；判断与调用顺序未改。清理后 smoke `14/14 PASS`，`py_compile` / `git diff --check` 均退出 `0`，`rg` 确认活动 TODO 暂时归零。
- I1f 骨架：在 `execute_tool_call()` 与既有真实工具终点识别器之间即时创建一个纯识别 helper；只固定签名、来源判定职责、返回合同与非目标，核心逻辑由用户实现。创建后 `py_compile` 退出 `0`，`git diff --check` 退出 `0`，`rg` 只应命中 I1f 一个 TODO / `NotImplementedError`。
- I1f 首次实现探针：正例、真实执行与单边账本移动、核心内容反例、重复稳定和零副作用通过；本地扩展矩阵 `53/70 PASS`、独立聚焦矩阵 `24/43 PASS`，失败集中在 extra key / dict 子类、缺失或畸形 context、默认 `0/[]` 伪证据与快照 / 容器严格类型。保留唯一 I1f TODO 进入 R1；没有真实 DeepSeek 或外部工具调用。
- I1f-R1 探针：首次保存中 fail-closed 守门已补，但 `step/logs` 在绑定前被读取，本地聚焦 `4/12 PASS`、独立完整 `15/43 PASS`。复核期间检测到后续稳定保存：绑定顺序已修复，来源 `4/4` 与真实 executor 迁移 PASS，但 payload 内容守门被一并删除，最终稳定矩阵本地 `28/40`、独立 `28/43`；保留唯一 TODO 进入恢复严格 payload 内容门的 R2。
- I1f-R2 探针：稳定源码主流程冻结矩阵 `42/42 PASS`，独立复核分别为 `42/42 PASS` 与生产边界 `36/36 PASS`；真实后端一次调用后 `1/1 → 2/2`、`M → T`，I1f 返回 `None`，所有模型 / 工具 / 下游 bomb 为 0。行为 PASS 后进入仅删除旧 TODO、清空白和统一缩进的机械收口；没有真实 DeepSeek 调用。
- I1f 机械收口与 I1g 骨架：I1f 清理后 smoke `14/14 PASS`、真实 executor 迁移 PASS，`py_compile` / `git diff --check` 退出 `0`，旧 TODO 归零；随后即时创建唯一 I1g TODO，只固定一次执行 / 同快照分类的签名、职责、返回两键和非目标，没有提供用户核心实现。骨架创建后 `py_compile` / `git diff --check` 仍退出 `0`，`rg` 只命中 I1g 一个 TODO / `NotImplementedError`。
- I1g 首次实现探针：稳定源码五条合法轨迹 `0/5 PASS`、普通 malformed context `13/13 PASS`；属性型 dict 子类可绕过 `isinstance + hasattr` 门，executor 只收到 2 个位置参数、I1f 为 0，真实 executor 抛缺少 `normalized_request` 的 `TypeError`。独立复核结论一致；保留唯一 I1g TODO 进入 R1，不清理一处尾空白，不接主控制流。
- I1g-R1 探针：最终稳定源码五轨 `1/5 PASS`、严格 context `13/13 PASS`，executor 异常轨及四对象 identity 已通过；其余四轨都在一次 executor 后因 I1f 收到非法 `step/logs` 且缺 `run_context` 而抛真实 `TypeError`。保留唯一 TODO 进入只修 I1f 参数合同的 R2；`py_compile` / `git diff --check` 通过。
- I1g-R2 探针：用户只修 I1f 参数合同；主流程与独立终审均为 spy `5/5`、严格 context `13/13`、真实 executor `5/5`，合计 `23/23 PASS`。异常 identity、两次调用边界、step/log 来源、exact 两键与零下游均通过；进入 I1g 机械收口，没有真实 DeepSeek 调用。
- I1g 机械收口与 I1h 骨架：I1g 清理后 smoke `7/7 PASS`、`py_compile` / `git diff --check` 退出 `0`，旧 TODO 归零；随后即时创建唯一 I1h TODO，只固定首次 / corrected Action 的 I1g-I1e-I1g 编排、一次 correction 与预算停止边界、exact 四键和非目标，没有提供用户核心实现。骨架创建后 `py_compile` / `git diff --check` 仍退出 `0`，`rg` 只命中 I1h 一个 TODO / `NotImplementedError`。
- I1h 首次实现探针：稳定源码聚焦矩阵 `0/6 PASS`；真实签名先因首次误调 I1e 被 broad catch，再因第二次 I1e 的非法 `observation` 参数抛 `TypeError`，I1g 始终 0。首次有效 spy 为 I1e/I1g=`1/0`，纠正有效为 `3/0`；原异常 identity 和输入零调用门均失败。保留唯一 I1h TODO 进入 R1，不接主函数。
- I1h-R1 探针：用户删除 broad catch 后，稳定源码主矩阵仍为 `0/6 PASS`，独立核心 `0/7`、外壳 `0/11`；真实调用在首次错误 I1e 参数 `action=` 处直接 `TypeError`，I1g 始终 0。异常不再被二次捕获与零下游单项通过，但 Action phase 尚未建立；保留唯一 TODO 进入 R2。
- I1h-R2 探针：用户已接 exact 外壳、`G → E → G` 主链且不重新 catch；核心 `5/7`、外壳 `11/11`、完整场景 `17/19`。仅 initial-valid 误从 I1g 读取 messages/action，以及 corrected 再错缺最后 `E（预算停止）`；其余身份、参数、四类异常与零下游通过，保留唯一 TODO 进入 R3。
- I1h-R3 探针：initial-valid 已修复；corrected-valid 新增错误 I1d parser 调用并退化为两键 return，corrected-invalid 同样两键返回且仍缺第二 E。核心保持 `5/7`、外壳 `11/11`、完整 `17/19`；保留唯一 TODO，R4 只恢复 corrected 两分支。
- I1h-R4 探针：核心 `7/7`、外壳 `11/11`、完整 `19/19`；真实 corrected-valid 为模型 1 / backend 1 / step-log 3/3，真实 corrected-invalid 为模型 1 / backend 0 / second E 预算停止并发 `max_action_corrections`。两份独立终审一致判行为 PASS，进入仅删除 TODO 与更新模块状态的机械收口。
- I1h 机械收口回归：只删除完成 TODO、更新模块状态；清理后 smoke `3/3 PASS`，`py_compile` / `git diff --check` 退出 `0`，活动 TODO / `NotImplementedError` 为 0。未接 `generate_candidate_summary()`。
- I2a 骨架：I1h 清理并确认 TODO 归零后，助手只在 `generate_candidate_summary()` 的旧首次 Action 直接执行段前创建一个内联 I2a TODO / `NotImplementedError`，固定 phase 入口快照、I1h exact 四键接力、最终 Action 字段来源、terminal provenance 与尾链非目标，没有提供接线实现，也没有创建后续预算骨架。创建后 `py_compile` / `git diff --check` 均退出 `0`，`rg` 精确确认全文件只有 I2a 一个 TODO / `NotImplementedError`；源码 SHA-256 为 `246DF8F7852F500627170476592E62497E88F3236FD3A595CBB1F1DCCD96D6C3`。
- I2a 用户实现终审：稳定源码 `11F853...DEC`，9 条无落盘轨迹与 134 个断言全通过；覆盖 initial / corrected 正常链、双非法预算停止、API / 协议异常、旧 retry、两个 corrected terminal outcome 与完整六步 public success。行为 PASS 后仅进入 TODO / 模块状态机械收口。
- I2a 机械收口回归：只删除完成 TODO、更新模块状态；清理后独立 `7/7 PASS`，本地聚焦 `2/2 PASS`，`py_compile` / `git diff --check` 退出 `0`；源码稳定为 `62A884...B52`，TODO / `NotImplementedError` 为 0。
- I2b 骨架：I2a 清理并确认 TODO 归零后，助手在 `has_action_correction_budget()` 后即时创建唯一 `has_tool_retry_completion_budget(current_step)` TODO / `NotImplementedError`；只固定全局完成性预算、严格输入、纯函数与非目标，没有预填判断答案，也没有接入生产 retry 或创建 I2c。创建后 `py_compile` / `git diff --check` 均退出 `0`，全文件 TODO / `NotImplementedError` 精确为 `1/1`；源码 SHA-256 为 `32B0546511DB7DFEAA3ECF344CDB0C9F906FE74A147D9AE9DD846732992FA294`。
- I2b 用户实现终审：稳定源码 `E7F462...C210`；独立 `32/32`、本地修正测试定义后 `37/37`，覆盖六项真值、严格输入、动态常量、重复稳定、AST 与 15 个零副作用 bomb。行为 PASS 后仅进入 TODO / 模块状态机械收口，尚未接入 retry。
- I2b 机械收口回归：只删除完成 TODO、更新模块状态；清理后六项真值、11 个非法输入、4 组动态常量和 AST 纯度探针全部通过，`py_compile` / `git diff --check` 退出 `0`，源码稳定为 `B31EB0...2874C`，TODO / `NotImplementedError` 为 0；生产 retry 仍未接入。
- I2c 骨架：I2b 清理并确认 TODO 归零后，助手新增独立的 `ToolRetryCompletionBudgetError` 脚手架，并在旧 `max_steps` 硬门之后、retry 快照与第二次工具调用之前创建唯一 I2c TODO / `NotImplementedError`。未接入公开 `needs_manual` 映射、未改 I2b 数学或既有 retry 尾链；创建后 `py_compile` / `git diff --check` 退出 `0`，全文件 TODO / `NotImplementedError` 精确为 `1/1`，源码 SHA-256 为 `AB6C9F2C2B2272F2CE5D1793E3EA4DB8FBC833065EEC5D312F26CEF16E5FB021`。
- I2c 用户实现终审与机械收口：原始稳定源码 `ACDE2D...A104`，独立 `10/10` 场景、`113/113` 断言及生产语义复核全部 PASS；清理只删 TODO / 更新状态，清理后 `5/5`、`45/45` 回归，`py_compile` / `git diff --check` 退出 `0`，最终源码 `ACEE12...A8B0C`，TODO / `NotImplementedError` 为 0。公开预算映射尚未开始。
- I2d 骨架：I2c 清理并确认 TODO 归零后，助手只在公开入口新增一个窄 tuple catch，固定 exact 类型 / reason 分流、安全 summary、严格三字段与旧映射非目标；没有写用户文案或分支答案，也没有改生产控制流。创建后 `py_compile` / `git diff --check` 退出 `0`，全文件 TODO / `NotImplementedError` 精确为 `1/1`，源码 SHA-256 为 `4E7D320486B3E002C9721E104F0AC8EC28B03FB4B9612B6CFB988506DFCD7B50`。
- I2d 用户实现与清理后终审：原始源码 `30883C...AC079` 的独立矩阵为 `15/15` 场景、`138/138` 断言 PASS；机械清理只删 TODO / 更新模块状态，清理后独立回归为 `15/15` 场景、`150/150` 断言 PASS，`py_compile` / `git diff --check` 退出 `0`，最终源码 `90334C...1FC7C`，TODO / `NotImplementedError` 为 `0/0`。本代码单元只剩无代码的用户审核，不创建 runner、baseline 或报告文件。
- U1 用户审核：用户原始答复为“审核通过”，确认四条端到端轨迹、调用次数、工具副作用边界和公开结果符合策略 B；`U1 = PASS`，没有合同变化或修正项。随后只同步模块后续范围文字，`py_compile` / `git diff --check` 退出 `0`，当前源码 `D212A8...7B173`，TODO / `NotImplementedError` 继续为 `0/0`。

## 当前范围与接力

- 今日计划范围 / 今日剩余：今日已完成 preflight、资料定位、策略 B 决策、`I1a～I2d` 全部实现、故障注入、公开映射、清理后回归与 `U1` 用户审核；今日剩余 `0`，不再创建或启动下一实现检查点。
- 任务总剩余：候选摘要 / Reflection / 最终 validator 失败恢复、HITL allow / deny、14 条 `eval_cases.json`、五个 Gate 必答问题和正式 Gate 复核。
- 当前活动检查点：无；源码 TODO / `NotImplementedError` 保持 `0/0`。
- 下次 Session 起点（不属于今天）：重新排入不超过 180 分钟的独立学习块后，先界定“候选摘要 / Reflection / 最终 validator 失败恢复”的第一个可闭合行为切片；届时再确定检查点编号并即时创建唯一 TODO 骨架。
- notes / 知识库：本代码切片不新建项目 notes。U1 后用户明确要求同步知识库，已授权把 C4a-2b2 / C4b-1 的稳定成果增量合并到 `D:\AI-Knowledge`；同步仍明确保留父 `A4-Gate = DOING`，不新建整关 PASS 复盘或求职证据。
