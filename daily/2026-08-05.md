# 每日学习打卡

日期：2026-08-05

计划学习预算（只统计用户主动学习内容；默认 ≤180 分钟，不记录实际时长）：原始全天计划为 A4-Gate 关前设计准备 140 分钟 + W4 算法回收 40 分钟，共 180 分钟。A4-Gate 内部分为定向阅读 35 分钟、编写 `problem-contract.md` 55 分钟、设计 Agent 循环与 14 条评估蓝图 50 分钟；算法位用于 W4 第 2 道数组/哈希新题。助手自动复核与写回不消耗主动学习预算。

## 当日 Session / 任务索引

| Session | 任务 | 环节 | 关联前序 daily | 当前状态 / 下一步 |
| --- | --- | --- | --- | --- |
| S1 | W4-ALG-F1 两数之和 | 正式检查 | [2026-08-04](2026-08-04.md) Session 3 | `RETRY`；2026-08-07～2026-08-11 脱离原答案独立复测 |
| S2 | Agent 模拟面试 | 已学内容复测 | 本页 A4-05 之前的 PASS 任务 | 已完成；详见 [面试记录](../interviews/2026-08-05-agent-模拟面试.md)，下次复测安全边界与工程验证 |
| S3 | A4-Gate 最小 Agent 闯关 | 定向带读 → Problem Contract 设计 | 无；本任务首次 Session，前置 A4-01～A4-05 已 PASS | `DOING`；`A4-Gate-G1～G7`、`A4-Gate-I1～I2` PASS；I3 跨日续接并于 [2026-08-06](2026-08-06.md) 完成 |
| S4 | W4-ALG-F2 | 第 2 道数组/哈希新题 | [算法台账](../tracker/algorithm-progress.md) | 原计划 40 分钟；尚未开始，跨日结转到 [2026-08-06](2026-08-06.md) Session S3 |

## 任务证据接力

### Session S1｜任务 `W4-ALG-F1`

任务状态与学习单元：W4 算法债务回收第 1 题，数组/哈希“一次遍历 + 补数查询”。

关联前序 daily：[2026-08-04](2026-08-04.md) Session 3，包含原始实现、求助、复杂度讲解、错误状态追踪、两轮订正与练习层结果。

资料定位与版本：`code/algorithms/w4_01_two_sum.py`；当前工作树未提交，基线 `main@106b8d9`。

已覆盖目标及证据位置：函数实现、三个规定断言、讲解后的一次遍历思路、平均时间 `O(n)`、额外空间 `O(n)`，详见前序 daily。

RETRY / 仍需迁移：首次不能独立说明解题思路和复杂度；首次迁移追踪把补数误写为字典中的已存键，直接讲解并两轮订正后才闭合。

未覆盖 PASS 标准 / 下次起点：算法台账要求“独立说明思路、复杂度并通过测试”；需间隔 3～7 天，在不查看原文件、前序答案或讲解的情况下从空白重写、先解释再运行。

代码/产物位置：`code/algorithms/w4_01_two_sum.py`。

notes 路径与状态：无；算法单题不要求独立 notes。

### Session S2｜Agent 模拟面试

完整题目、作答、参考反馈与复测重点见：[2026-08-05 Agent 模拟面试](../interviews/2026-08-05-agent-模拟面试.md)。

本次结论：核心概念、控制循环与可靠性回答稳定；下次优先复测安全边界与工程验证。

### Session S3｜任务 `A4-Gate`

任务状态与学习单元：`A4-Gate` 首次正式学习 Session；已完成两篇官方资料中直接支撑 Gate 的定向带读，现进入交付冲刺，只创建当前硬检查点所需的 `problem-contract.md` TODO 骨架，Agent 代码与 eval 尚不提前创建。

关联前序 daily：无；本任务首次 Session。前置课程 A4-01～A4-05 均已在 `tracker/progress.md` 正式 PASS。

资料定位与检查日期：

- Anthropic《Building effective agents》：https://www.anthropic.com/engineering/building-effective-agents（2026-08-05 核验）；本次选读 `When (and when not) to use agents`、`Agents`、`Appendix 2: Prompt engineering your tools`。
- OpenAI《A practical guide to building agents》：https://openai.com/business/guides-and-resources/a-practical-guide-to-building-ai-agents/（2026-08-05 核验）；后续选读 `When should you build an agent?`、`Configuring instructions`、`Single-agent systems`、`Tool safeguards`、`Plan for human intervention`。

当前片段：Problem Contract 第 4 节“输出契约”；输入契约已闭合，接下来为成功、失败和人工接管结果定义统一的结构、类型与证据边界。

与任务/PASS 标准的映射：选型与清晰指令支撑 problem contract；工具分级和人工介入直接支撑本 Gate 的重试上限、高风险动作确认，以及允许/拒绝分支。

已覆盖：Anthropic `When (and when not) to use agents`、`Agents`、`Appendix 2: Prompt engineering your tools`，以及 OpenAI `When should you build an agent?`、`Configuring instructions`、`Single-agent systems`、`Tool safeguards`、`Plan for human intervention`。用户能完成 workflow/Agent 选型与成本取舍，使用 Ground truth 和停止条件控制循环，把模糊文件工具改成沙箱内 `relative_path` + 客户端最终落点校验 + 稳定成功/失败返回，将模糊任务指令改成有正常输出和异常分支的可执行步骤，以真实失败证据决定何时从单 Agent 升级多 Agent，按读写权限与可逆性为工具分级、识别人工确认点，并在达到失败阈值且证据不足时停止自治、交还控制权。任务级有效证据 7 个（`A4-Gate-G1～G7` PASS）。

剩余范围：本次选定官方资料范围无剩余；两篇文章其余章节不在本次定向范围，未冒充已读。Problem Contract 第 1～3 节已完成，第 4～11 节待用户逐步完成；Agent 循环实现、停止/恢复、最小日志、危险动作确认与 eval 设计均未开始。

代码/产物位置：`code/stage4/problem-contract.md` 已写入用户确认的第 1～3 节，其余保持 TODO；`code/stage4/a4_gate_research_summary_agent.py` 与 `code/stage4/eval_cases.json` 尚未创建。

notes 路径与状态：本 Gate 不要求独立概念 notes；`code/stage4/problem-contract.md` 为 `DRAFT`，第 1～3 节已有用户答案，第 4～11 节仍为 TODO。

#### `A4-Gate-G1`

即时考察：一个系统的任务步骤固定、输入格式统一、每次都按同样顺序执行；另一个系统面对开放主题，必须根据已有资料决定是否继续搜索、调用哪个工具以及何时停止。根据当前片段，哪一个优先用 workflow，哪一个更可能需要 Agent？请同时说明原因和 Agent 需要付出的代价。

用户原始回答：任务步骤固定、输入格式统一，每次都按相同顺序执行的使用Workflow，因为流程固定，执行顺序相同。开放主题，必须根据已有资料决定是否继续搜索、调用哪个工具以及何时停止使用Agent，因为需要根据当前情况判断需要下一步怎么做。使用Agent的代价一是成本会高，二是会有一定的延时

订正过程：无需重答。精度校准为“Agent 通常会增加成本与延迟”，具体幅度取决于模型/工具调用次数，不能理解成所有 Agent 都必然昂贵。

教学检查点状态：`PASS`。workflow/Agent 选型正确，原因落到固定路径与动态决策，并完整指出当前片段要求的成本、延迟取舍。

#### `A4-Gate-G2`

即时考察：研究 Agent 调用搜索工具时发生超时，没有拿到任何结果；模型却直接写出“根据搜索结果……”并继续循环，而且系统没有最大步数。根据 Anthropic `Agents` 小节，这里分别缺少哪两类控制？它们各自防什么问题？

用户原始回答：Ground truth和停止条件，Ground truth防止模型编造结果，停止条件防止一直调用

订正过程：无需重答。补充停止条件不只包含 `max_steps`，还可包含 `timeout`、重试上限、失败阈值和人工接管；它们共同限制错误累积、无限调用和成本失控。

教学检查点状态：`PASS`。能区分证据真实性控制与循环上限控制，并准确说明二者防护目标。

#### `A4-Gate-G3`

即时考察：现在有一个工具 `read_material(path)`，描述只有“读取资料”，参数允许任意字符串。模型因此会传入 URL、不存在的相对路径，甚至 `../secret.txt`。根据 Anthropic `Appendix 2`，你会从工具名称/参数、描述与边界、示例或返回结构中选择哪些地方改进？请至少写出三项具体改法，并说明这样如何减少误用。

用户原始回答：

1.描述改为：传入绝对路径，读取该路径下的文件，返回字符串。这样模型明白读取文件时调用这个工具，传入文件的绝对路径。
2.参数：read_material(path:str)。固定参数类型，让模型知道是字符串类型的绝对路径
3.示例可以写一个绝对路径。示例中可以写一个绝对路径，让模型知道怎么调用

用户订正回答：只接受沙箱内的relative_path，让客户端的代码去判断是否是沙箱内的地址，是沙箱的相对地址允许读取，否则不允许，返回一个字典，里面的参数去区分成功还是失败

订正过程：首次方向正确，已想到描述、类型和示例；但 `str` 不证明路径合法，任意绝对路径也扩大到系统敏感文件。订正后改为沙箱内 `relative_path`、客户端硬校验及字典成功/失败契约，关键边界闭合。精度补充：不能只看输入“形式上是相对路径”，必须先 `(SANDBOX / relative_path).resolve()`，再验证最终绝对落点仍位于 `SANDBOX` 内；返回建议稳定区分 `ok/content/truncated` 与 `ok/error`。

教学检查点状态：`RETRY → PASS`。已同时做到“模型知道允许格式 + 客户端阻止越界 + 下一轮能区分成功失败”。

#### `A4-Gate-G4`

即时考察：原始指令只有“读取用户资料，写一个尽量完整的高质量摘要；遇到问题自己处理”。根据 OpenAI `Configuring instructions`，请先指出这句话为什么含糊，再把它改成 3～5 条可执行指令；必须写清正常输出，并覆盖“路径读取失败”和“资料不足”两个异常分支。

用户原始回答：

1.先检查是否存在该用户
2.存在该用户时再去读取该用户资料
3.如果该用户资料齐全，把该用户的资料编写成一个完整高质量的摘要并输出
4.如果该用户资料不齐全，无法编写成一个完整高质量的摘要，提示该用户资料信息不全，无法生成完整的摘要
5.如果读取用户资料的路径失败了，提示读取该路径失败

用户订正回答：

1.没有规定完整的高质量摘要的边界，怎样才算完整高质量摘要太模糊了。遇到问题自己处理，怎么处理也没说，也是边界太过模糊
2.
1.先判断该路径是否合法，输入固定为relative_path
2.路径合法就去读取用户资料，并且把用户资料按照喜好，性格进行摘要
3.路径不合法就提示读取该路径失败
4.用户资料中如果没有喜好，性格，不能进行摘要时，不得编造摘要，返回提示用户资料信息不全，无法生成摘要。保留路径读取失败分支

订正过程：首次回答已覆盖正常输出、路径读取失败和资料不足三个分支，但没有解释含糊点；“资料齐全”“完整”“高质量”也没有判据，并新增了合同外的用户查询。订正后能指出质量边界和异常处理均未定义，删除用户查询，把输入限定为 `relative_path`，将正常摘要收敛到“喜好、性格”两个可检查维度，并明确路径失败、资料不足和不得编造。精度校准：实现中的“路径合法”仍须复用 G3 的客户端最终落点校验；输出最好固定为 `喜好`、`性格` 两个字段，某字段缺少时明确标为资料不足，避免把“没有喜好，性格”误解成只有两者都缺少才失败。

教学检查点状态：`RETRY → PASS`。已能把模糊要求拆成具体动作、可观察输出和条件分支；字段级精度作为实现校准，不再要求重答。

#### `A4-Gate-G5`

即时考察：研究摘要系统目前只有 `read_material`、`search_web`、`save_summary` 三个边界清楚的工具。你会先让一个 Agent 管理整个循环，还是一开始就拆成“阅读 Agent、搜索 Agent、写作 Agent”？为什么？如果后续在工具说明和提示已经改清楚后，它仍持续选错工具或无法遵循复杂分支，架构上再考虑什么变化？

用户原始回答：我会先让一个agent管理整个循环，因为阅读，搜索，写作更像是一个整体。一个agent也更容易去维护。如果优化工具说明和提示后仍持续选错工具或无法遵循复杂分支，在改成多agent。让每个agent保持单一的职责

订正过程：无需重答。能把阅读、搜索、写作视为一个连续工作流，并以维护难度说明先用单 Agent 的理由；也能在优化提示和工具说明后仍持续失败时，再拆分职责。精度校准：多 Agent 的触发依据是可观察到的复杂分支失控或持续选错工具，不是仅凭“可以划分单一职责”就提前拆分；拆分还会引入交接、上下文与评估开销。

教学检查点状态：`PASS`。正确应用“先最大化单 Agent，再由真实失败证据触发多 Agent”的渐进式设计原则。

#### `A4-Gate-G6`

即时考察：请按 OpenAI `Tool safeguards` 的风险维度，为下面三个工具判断低/中/高风险，并说明哪个必须在执行前获得人工确认：① 只读取沙箱内文件的 `read_material`；② 只在沙箱内新建、可删除恢复的 `save_summary`；③ 永久删除原始资料且没有回收站的 `delete_source`。判断时至少使用“读或写”和“是否可逆”两个依据。

用户原始回答：

1.低风险。因为是只读取沙箱内的文件
2.中风险。虽然是在沙箱中操作，但是新建，删除权限都有，但是可逆
3.高风险，必须执行前人工确认，有删除权限并且不可恢复

订正过程：无需重答。能依据只读/写入权限和是否可逆完成低、中、高风险分级，并明确永久删除必须在执行前人工确认。精度校准：风险等级取决于工具的实际权限范围和影响，不由名称永久决定；若写入工具扩大到覆盖重要文件或外部系统，风险可随之升高。题设中的 `save_summary` 仅限沙箱内新建且可恢复，中风险可用参数校验、目标预览和日志控制，不强制等同于每次人工审批。

教学检查点状态：`PASS`。能用至少两个风险维度作出一致判断，并选出必须人工确认的不可逆工具。

#### `A4-Gate-G7`

即时考察：`search_web` 连续超时并已经达到系统规定的重试上限，此时仍没有足够证据生成摘要。Agent 应该继续搜索、凭已有常识补写，还是停止并把控制权交还用户？请说明触发人工介入的原因，并写出交接给用户时至少应说明的两项信息。

用户原始回答：停止并把控制权交还用户，因为已经连续超时并达到重试上限，仍没有足够证据生成摘要。需要人工介入。交接给人工时要告诉人工已经连续超时和达到了重试上限，并且仍然没有足够证据生成摘要

订正过程：无需重答。正确选择停止而非继续调用或凭常识补写，并把“连续超时且达到重试上限”识别为失败阈值触发器；交接信息同时包含已发生的失败/阈值状态和当前证据缺口。精度补充：实现时可再附具体重试次数、最后错误及用户可选下一步，但不作为本题重答要求。

教学检查点状态：`PASS`。能在失败阈值与 Ground truth 约束共同出现时停止自治，并提供可供用户接管的最小现场信息。

#### `A4-Gate-I1`

实现检查点：只完成 Problem Contract 的第 1、2 节。请分别写出“目标用户与使用场景”和“要解决的任务”；任务句必须包含用户提供什么输入、Agent 做什么、输出什么，并避免“所有人”“尽量完整”“高质量”等不可直接验收的表述。

用户原始回答：

目标用户是需要研究主题，或者提供沙箱内的相对路径时使用该Agent.

当用户提供【研究主题或沙箱内资料相对路径】时，Agent调用工具去查找主题或者读取沙箱内的文件来生成摘要，输出固定的结构来观察是否成功

用户第一次订正回答：

目标用户与使用场景：[学生]在[学习场景]下使用。

解决的任务：当用户提供【研究主题或沙箱内资料相对路径】时，Agent读取沙箱内相对路径的资料，并输出包含 status、summary、sources 的结构化结果。

用户第二次订正回答：解决的任务：当输入为研究主题时，Agent调用工具去查找该主题的资料；当输入为沙箱内资料相对路径时，Agent读取沙箱内资料；两种情况都输出包含 status、summary、sources 的结构化结果。

订正过程：首次回答保留了两种输入和摘要动作，但缺少具体用户/场景与输出字段。第一次订正闭合了学生、学习场景及 `status`、`summary`、`sources`，但只定义路径读取。第二次订正补齐“研究主题 → 调用工具查找资料”和“相对路径 → 读取沙箱资料”两个分支，输入、动作、输出已一一对应；最终内容已写入合同第 1、2 节。

教学检查点状态：`RETRY → RETRY → PASS`。能把目标用户、使用场景、两类输入、对应动作和可观察输出写成一致合同。

#### `A4-Gate-I2`

实现检查点：只完成 Problem Contract 第 3 节“输入契约”。请设计一个能无歧义区分 `topic` 与 `relative_path` 的 JSON 对象，并写清字段名、类型、允许值、空字符串处理、“每次只能选择一种输入”的规则和 `topic` 的具体长度上限；`relative_path` 还必须由客户端解析并验证最终落点仍在沙箱内，拒绝绝对路径、URL 和路径逃逸。

用户原始回答：

```text
1.input_type和value的类型是str,value只允许字符串文本或者路径
2.每次只允许输入字符串文本或者路径
3.空字符串提示用户输入内容或者路径
4.规定topic长度不超过50
5.target = (SANDBOX / relative_path).resolve()
 target.relative_to(SANDBOX)来判断
6.不在沙箱内的绝对路径、URL、路径逃逸输入必须拒绝
```

用户第一次订正回答：

```python
{
  "input_type": "topic",
  "value": "agent是什么"
}
input_type，value类型是str,且都是必填字段，先执行 value.strip()；结果为空就拒绝。如果input_type是topic，那么value的长度不能超过50字符，如果input_type是relative_path，value所有绝对路径都拒绝，包括指向沙箱内部的绝对路径；同时拒绝 URL。解析相对路径后，target.relative_to(sandbox_root) 越界会抛出 ValueError，并非返回布尔值。
```

用户第二次订正回答：

```text
1.input_type 只允许 topic和 relative_path，其他值一律拒绝
2.客户端捕获 target.relative_to(sandbox_root) 的 ValueError 后，应当返回失败格式
```

订正过程：首答已确定 `input_type`、`value` 均为字符串，覆盖空字符串、`topic` 最大长度 50、解析后沙箱归属检查，以及 URL/路径逃逸拒绝，方向正确。首轮反馈指出四处歧义：输入类型枚举与单分支规则、空白规范化后的 1～50 字符边界、全部绝对路径拒绝、`relative_to()` 的异常语义。第一次订正已补齐两个字段必填、`value.strip()` 后拒绝空值、`topic` 最大 50 字符、全部绝对路径和 URL 拒绝，并能准确说明 `relative_to()` 越界抛出 `ValueError`；仍缺类型白名单和异常后的拒绝动作。第二次订正明确把 `input_type` 收敛为 `topic` / `relative_path` 白名单，其他值一律拒绝，并规定客户端捕获越界 `ValueError` 后返回失败格式，未知类型与路径逃逸两个分支均已闭合。第 3 节已据此写入合同；失败格式的具体字段由 I3 的第 4 节继续定义。

教学检查点状态：`RETRY → RETRY → PASS`。输入结构、类型白名单、必填/空值、主题长度、绝对路径与 URL 拒绝、解析后最终落点校验及稳定失败动作均可无歧义实现。

#### `A4-Gate-I3`（跨日续接至 2026-08-06）

实现检查点：只完成 Problem Contract 第 4 节“输出契约”。请设计一个始终包含 `status`、`summary`、`sources` 的 JSON 对象，并写清三个字段的类型与是否必填；为“成功”“输入无效”“工具失败”“证据不足”“需要人工接管”分别规定一个明确的 `status` 允许值；同时说明成功时 `summary` / `sources` 必须满足什么条件，失败时这两个字段如何保持固定类型，以及没有真实来源时为什么不得编造 `sources`。

用户原始回答：跨日作答及两轮订正证据见 [2026-08-06](2026-08-06.md) Session S2。

订正过程：I3 已在下一日拆为 I3a 成功结构与 I3b 失败结构逐步完成；完整原始回答、反馈和订正均保留在关联 daily。

教学检查点状态：跨日 `PASS`；不在本页复制后续证据。

### Session S4｜任务 `W4-ALG-F2`（原计划项，未开始）

任务状态与学习单元：W4 第 2 道数组/哈希新题；本题与 `W4-ALG-F1` 两数之和的到期复测是两个不同任务。

计划范围：40 分钟内独立说明思路、编写代码、运行规定与边界测试，并说明时间/空间复杂度。完成标准仍以算法台账为准：没有独立解释和可执行测试时不得记为 PASS。

边界：两数之和须等到 2026-08-07～2026-08-11 脱离原答案复测，本题不能用提前重做两数之和替代。

当前状态：未开始。A4-Gate 设计跨日后，该计划项没有被取消或算作完成，接力到 [2026-08-06](2026-08-06.md) Session S3。

## 工程验证

运行提示：使用项目 `.venv`；扩展边界测试只在标准输入内存脚本中执行，不新增一次性 runner。

验证命令与关键结果：

- `.venv\Scripts\python.exe -W error -m py_compile code\algorithms\w4_01_two_sum.py` → 通过。
- `.venv\Scripts\python.exe code\algorithms\w4_01_two_sum.py` → `All test cases passed.`，规定案例 3/3。
- 按文件路径加载 `two_sum()` 的内存断言 → `FOCUSED_ASSERTIONS=8 ALL_PASS`，覆盖规定案例、负数、零、重复值、单元素防复用与无解分支。
- 首次扩展测试尝试 `from code.algorithms...` 失败，原因为仓库 `code/` 不是 Python package；改为 `importlib.util.spec_from_file_location()` 后同一产物 8/8 通过，该入口错误不归因于用户实现。

测试/eval/性能/安全摘要：功能和边界行为通过；本题按 Python 字典平均查询/写入 `O(1)` 口径，平均时间 `O(n)`、额外空间 `O(n)`。性能基准、安全测试和 eval 不属于本算法单题 rubric。

本次工作场景/复合故障：不适用；这是基础算法单题，不形成生产故障复现、修复或恢复证据。

工作场景证据等级：不更新 `tracker/work-scenario-coverage.md`。

Gate 预设阈值与 holdout 结果：不适用。

算法内容块状态与证据：`W4-ALG-F1` 功能测试通过，但首次独立性未满足，正式判 `RETRY`；W4 仍为 `DEBT 4`，其中 1 题待复测、3 题未开始，实际新题 PASS 仍为 0。

岗位证据候选：不适用；基础算法练习不升级 `tracker/job-readiness.md`。

## 批改与小结

希望复核/检查：两数之和代码、边界测试、复杂度、解题思路与独立性是否满足 W4 算法台账。

批改判定：**W4-ALG-F1：RETRY（2026-08-05）。** 代码及 8 个聚焦断言通过，但用户在提交后明确表示不知道复杂度和解题思路；经直接讲解后，首次迁移又把“查询补数”误作“保存键”，需要两轮订正。根据算法台账的独立性硬标准，本次不能计入新题 PASS。

主审工具（仅需交叉复核时填写）：

复核判定（普通任务默认留空；JOB_EVIDENCE、FINAL/J11-Gate、投递前必须由另一工具填写）：

当天进度小结：完成跨日正式复核和扩展边界验证；练习层理解已闭合，但正式独立证据不足。

总进度小结：A4-05 保持 `PASS`，下一课程主任务仍为 A4-Gate；W4 算法保持 `DEBT 4`，不得由课程进度掩盖。

对问题/不确定点的解释：绿灯测试证明函数行为正确，不证明提交者能够独立推导算法。讲解后的正确复述是有效学习证据，但不能改写成首次无提示完成。

问答点评与补充：能在订正后区分“已遍历数字 → 下标”、补数查询和先查后存，也能正确说明平均时间/额外空间；原始状态轨迹与实际代码不一致是本次正式 RETRY 的决定性学习证据。

今天最虚 / 下次重点复习：查询对象 `target - num` 与保存对象 `num` 容易混淆；复杂度解释还要明确“平均 `O(1)` 来自哈希定位”，不能归因于键唯一。

超前内容提示：Python 字典的理论退化最坏情况不是本次复测必答项；当前先掌握算法题常用的平均复杂度口径。

补救任务：2026-08-07～2026-08-11 选择一个算法时段，不查看原代码或讲解，从空白重写两数之和；运行前先说明字典保存对象、补数公式、先查后存原因及时间/空间复杂度，再运行规定与边界测试。

## 知识库同步

- 授权来源：固定口令 `检查今天的学习`。
- 本次正式判定为 `RETRY`，按授权边界不把未完成的算法独立性证据同步到 `D:\AI-Knowledge`；无知识库文件改动。

## 复核后即时理解补充（不替代间隔复测）

- 用户补充原文：`使用循环和字典来保证时间复杂度，循环列表中的数，然后用目标减当前数，查找字典中是否包含这个数，不包含就把当前数和下标存到字典中，当有满足的时候返回下标，这样设计时间和空间的复杂度都是O(n)`。
- 教学批改：已完整说出单次遍历、补数 `target - num`、未命中后保存 `num → i`、命中返回下标，以及平均时间/额外空间均为 `O(n)`；即时理解检查通过。
- 精度校准：“保证时间复杂度”改为“在字典查询/写入平均 `O(1)` 的常用算法分析口径下，平均时间为 `O(n)`”；极端哈希退化不是本次复测必答项。
- 证据边界：该回答发生在正式结论和完整反馈之后，只证明当前理解已强化，不满足 3～7 天后脱离原答案的独立复测条件；`W4-ALG-F1` 正式 `RETRY`、复测窗口及算法台账均保持不变。
