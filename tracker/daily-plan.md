# 每日学习计划

配合 `weekly-plan.md` 使用。**核心：任务是有顺序的队列，但不绑死日期——每个学习日从 `tracker/progress.md` 看做到哪，往下拉下一个没做的任务。** 哪天做、做几个，看当天状态；有事耽误提前跟我说，那天顺延、任务吸收到后面，连续性优先。

不再按周一到周日把强度排死（废弃「周一周五特别轻」「满血 / 半血 / 空血」这类按具体星期定档），**但保留「工作日小块推进 / 周末整块攻关」的任务类型分工**——见下「二、固定规则」。

## 一、每天的节奏

- **主块**：先用 `weekly-plan.md` 定位当前内容块，再用 `progress.md` 找这个内容块里未完成的任务（概念阅读 / 写代码 / 过 Gate / 项目）；不要机械地取表格第一条 TODO，因为 B0/S 补充项按触发点穿插。
- **碎片阅读**：白天有空就用手机读当天的纯阅读项（中文文档 / 笔记 / 视频段落），不写代码、不依赖电脑；没读懂的下班整块时间我带你导读。
- **收尾**：算法（W4 起按周累计 2 新 + 1 旧错题）/ 复习昨天 / 写几行笔记 / 更新当天 `daily`，并填写目标时长与实际时长。
- 你超前时不必硬塞满；**连续性比单日强度重要**，哪怕只推进一点、写一行打卡，也别断。

## 二、几条固定规则（其余跟着队列走）

1. **Gate / 项目 / 部署 → 周末做**：要整块连续时间。判据是「**能否一坐做完、不留半成品过夜**」——工作日**可**做能起能收的自包含单元（小脚本 / 单函数 / 小实验，如 S-03 的 token 对比），但**不碰**会停在「跑一半、明天得重新捡上下文」的活（Gate 收尾 / 大整合 / 大 debug / 部署）。「Gate 和项目宁可慢，不要带着跑不通的代码往后走」。
2. **周复盘 / 阶段总结 → 周末做**：要回看整周 / 整阶段，沉淀进 `D:/AI-Knowledge/07-Reviews/`，和周末的大任务搭在一起。**周末时间不够时按此优先级保**：项目跑通 > debug 收尾 > 周复盘 > 知识库沉淀 > 下周预习；但**沉淀留一条保命线**——哪怕只 15 分钟，也要把本周最硬的那个坑 / 结论写进 `D:\AI-Knowledge`，这条不可被挤掉（否则「学得多、沉淀跟不上」就复发）。
3. **每日小结跟着任务走**：daily 文件里的「当天 / 总进度小结、问答点评」等，是「检查今天的学习」当天顺手回填的，**不攒到周末**。
4. **工作日遇到「下一个是 Gate」**：因为 Gate 要等周末，工作日就拉 **gate 无关的项**填上——B0 工程基础穿插补课 / 下一阶段的概念预习——别空等。
5. **关前工作日做「设计准备」**：周末要闯的 Gate，可先读资料、核对当前官方 API、规划结构和列函数；这时才创建该 Gate 的 TODO 骨架（**只设计不写实现**），让周末跑得顺。未来阶段不提前批量建骨架。
6. **强制顺序关卡**（`P0-Gate`、`L1-Gate`、`T3-Gate`、`A4-Gate`、`R6-Gate`、`G8-Gate`、`M9-Gate`、`FINAL-Gate`）没过，不进入后续同类阶段的**动手交付与该阶段 Gate**；其前的工作日按第 4 条填充（概念预习 / B0 穿插不受限）。
7. **算法 W4 起每周必滚（2 新 + 1 旧错题，不可静默跳过）**：记录到 `tracker/algorithm-progress.md`；哪周漏了下周补，但不靠一次刷很多题掩盖没复测。主线先做到 50–70 道高质量题，100–150 留在投递期继续累计。
8. **作品集分两档（别镀金玩具 Gate）**：PR2 等小 Gate 只做轻量卫生；全套打磨集中在 RAG 旗舰和从 A4→G8→FINAL 持续演化的可控 Agent 旗舰，另保留结构化抽取小项目。不要把每个 Gate 都复制成新仓库。
9. **代码骨架即时创建**：未开始任务只在 tracker 里保留目标路径和通过标准；进入当天动手或 Gate 设计准备时再建 TODO 骨架。这样函数签名、SDK 字段和评估要求都基于当时真实资料，不维护一堆会过期的空文件。

## 三、顺延规则

- 不预设机动日、不预设降档。哪天加班 / 有事，**提前告诉我，那天顺延**，任务挪到后面。
- 没特别说的日子一律正常排任务、照常出题。

## 四、任务从哪来 + 当前队列

每天该学什么，三个文件对齐：`weekly-plan.md`（W1–W17 内容块 → 任务）+ `progress.md`（唯一状态事实源）+ `ai-agent-learning-tracker.md`（该任务的资料 / 要做 / 通过标准 / 必答问题）。

**当前执行入口（2026-07-10 校准，不替代 progress）**：W4 收尾；T3-01~04 已 PASS，下一硬检查点是 `T3-Gate`。`A4-01` 已作为等待周末时的概念预习 PASS。

**接下来的队列（顺着拉，不钉日期）**：

```text
[当前工作日] T3-Gate 设计：核对 DeepSeek tools/tool_calls → 列 schema、分发器、消息回填、评估案例；只建 TODO 骨架
[当前周末] T3-Gate 实现：真实模型选三工具 → 客户端执行 → role="tool" 回填 → 第二次模型回答 → 跑 14 条评估
   ↓ Gate PASS 后
A4-02 LLM 与 Agent 基础 → A4-03 ReAct → A4-04 Plan-and-Solve → A4-05 Reflection
[周末] A4-Gate + 自动评估 + 最小日志 + 危险操作确认
   ↓
后续按 weekly-plan W7–W17；B0-01 在 W5 补，B0-03/B0-04/B0-Gate 固定放 W8
```

> 用法：每天开「学习计划」时，照 `progress.md` 找到队列里下一个没做的；若它是 Gate 就按规则 4 拉填充项，把 Gate 留给周末。

## 五、提醒

- 每天用 `daily/YYYY-MM-DD.md` 打卡：学完用 `daily-practice-builder` 出题，检查用 `ai-agent-learning-review` 判定并回填（**每日小结即在此回填**）。
- 周末除了 Gate / 项目，记得做一次**周复盘**沉淀进 `07-Reviews/`。
- 有事耽误就主动说，**顺延是计划内的，不是失败**。
