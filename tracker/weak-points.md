# 复习与易错点台账（统一排期）

本文件是旧知识复习排期的**唯一事实源**，统一保存 🔴/🟡 活跃错题、📌 核心定义间隔项和 ✅ 稳定错题间隔项。出题时从这里挑“该翻旧账复习”的目标；不要再建一套独立日历卡片。

原始作答与订正明细在 `daily/YYYY-MM-DD.md`，任务状态在 `tracker/progress.md`，完整正确内容在项目 `notes/` 和授权后的 `D:\AI-Knowledge`。本表只留最小语义目标、证据链接与排期，不复制完整答案。

## 状态与「间隔回炉」（关键：✅ 不等于永不再考）

- 🔴 **还容易错**：持续在候选池，按当前任务相关性和最久未测排序；不需要伪造“下次回炉日”。
- 🟡 **订正过一次**：持续候选，待下一次独立证据；答对 → 升 ✅，首个回炉日为本次 PASS `+2周`；答错 → 打回/保留 🔴。
- 📌 **核心定义间隔项**：只收任务正式 PASS 后、去重后的 `must-recall`。首个回炉日为任务 PASS `+2周`；答错时移出本池并合并/新增为 🟡 活跃弱点。
- ✅ **稳定错题间隔项**：按「下次回炉」日期到期抽查。
- 两类间隔项抽查**答对**后依次升档：`+1月 → +2月 → +3月封顶`，此后每 3 个月一次；抽查**答错**则回到活跃 🟡 并重置间隔。同一语义不得同时占活跃池和间隔池。

一句话：核心定义和错题共用一张排期表、共用每个正式练习集最多 2 项的旧复习席位；熟了拉长间隔，答错立即回到活跃池。

## 怎么维护（我在「检查/出题」这两步自动做，你不用手填）

- **出题时（拉取式）**：只有用户启动练习时才扫描三池；任何一天旧复习目标**总计最多 2 个**，概念/阅读日默认 1 个，编码/Gate/项目日通常 0–1 个且必须关联真实产物。先看与当前任务的语义相关性，再比较活跃项的最久未测和间隔项的逾期程度；一个题能同时覆盖当前标准和旧目标时合并计算，不叠加题量。
- **积压阈值**：到期的 📌/✅ 间隔项达到 4 个，或最老一项逾期达到 7 天时，先连续两次练习优先使用上述既有旧复习席位，不立即增加题量；若两次后仍高于阈值，才进入下一次合适的独立恢复 Session，每个 Session 最多推进 1–2 个目标，并且只有产生真实作答或执行证据后才重算状态。
- **不制造假债务**：🔴/🟡 活跃池持续可选，但没有“到期日”，不计入上面的逾期数量/天数；只有 📌/✅ 间隔池参与积压阈值判断。
- **批改/正式检查时**：新增或合并当天的坑/最虚，记录原始证据；正式检查者按上面的升降档规则更新池、上次证据与间隔日期。
- **提醒边界**：本台账不会自行弹窗或后台运行。除非用户另行明确要求自动提醒，否则不创建系统日历/自动化；到期项会在下一次“开始今天的练习”时被拉取。

## 当前回炉调度状态（跨会话必读）

这是旧知识回炉的**持久执行状态**，不是聊天提醒。任何新会话在生成当前知识单元预览或正式练习前都必须读取本节；换会话、换日期、只展示预览或没有实际作答，都不能清除待执行状态。只有 `ai-agent-learning-review` 在核对真实恢复 Session 证据后才能更新本节。

| 字段 | 当前值 |
|------|--------|
| 状态 | `NORMAL` |
| 快照日期 | 2026-08-24 |
| 到期 📌/✅ 项 | 3：`WP-28`、`WP-27`、`WP-29` |
| 最老到期日 | 2026-08-20（逾期 4 天） |
| 已完成的阈值流程 | 优先席位 `2/2`：2026-08-04 `CD-003`、2026-08-06 `CD-004`；2026-08-07 恢复 Session 完成 `WP-17` / `WP-20`；2026-08-08 恢复 Session 完成 `WP-05` / `WP-06`；2026-08-09 恢复 Session 完成 `CD-001` / `CD-002`；2026-08-10 恢复 Session 完成 `WP-15` / `WP-02`；2026-08-11 恢复 Session 完成 `WP-04` / `WP-24`，其中 `WP-04` 因同日近端语义提示不升档，`WP-24` 升至 `+1月`；2026-08-12 新 pending 恢复 Session 在无近端提示的 hybrid 规则优先 / LLM fallback 表面完成 `WP-04`。新一轮优先席位 `1/2`：2026-08-22 `CD-005` 题级 `PARTIAL → RETRY → PASS`，但到期间隔首答重现核心错误，正式移出 📌 并合并到活跃 `WP-26` |
| 本次重算 | 2026-08-24 A4-Gate 正式核对：今日五道 Gate 问题没有覆盖 `WP-28 / WP-27 / WP-29` 的算法语义，不能伪记为旧复习席位；到期项仍为 3 个，最老 2026-08-20、逾期 4 天，尚未达到“到期 ≥4 或最老逾期 ≥7 天”的积压阈值。A4 的 GQ4/GQ5 暴露项已分别合并到 `WP-12 / WP-25 / WP-33`，不新建重复 ID |
| 下一次必须执行 | 当前没有强制回收块；新一轮阈值流程仍为普通优先席位 `1/2`。下一次正式练习优先从 `WP-28 / WP-27 / WP-29` 中按相关性使用第 2 个旧复习席位，再由正式检查重算；只有第二次席位后仍达到阈值，才可签发 `RECOVERY_BLOCK_PENDING` |
| 清除条件 | 当前无 `RECOVERY_BLOCK_PENDING` 可清除；优先席位 `1/2` 不等于回收块。未来若第二次真实优先席位后仍达到阈值，才能签发 pending；签发后仍必须有实际作答 / 执行证据与逐项结果，才能由正式检查清除 |

---

## 🔴🟡 活跃池（持续候选）

| ID | 概念 / 坑 | 阶段 | 错误理解 → 正确理解 | 状态 | 首次 | 上次测 |
|----|-----------|------|---------------------|------|------|--------|
| WP-01 | 漏看 prompt「点名子项」 | PR2-02/A4-Gate | 只做主要求、漏掉点名子项 → 逐条核对字段和分支；2026-08-08 C2b 首稿漏冻结请求字段；2026-08-09～10 C3a/C3b/C3c 连续漏当前 tools 请求字段、请求绑定、`tool_call_id` 与响应外壳；C3d-C2a/C2b 又把“正文证据字段、禁止模型常识、allowlist 不替代正文、JSON-only、禁围栏、exact keys”等可观察规则只留在注释而未真正写进 prompt | 🔴 | 06-27 | 2026-08-10 |
| WP-03 | `strip()` 不是万能去空格 | PR2-03 | 把 `strip` 贴到 list 上想「去所有空格」→ 会崩；strip 只洗单个字符串的运行时脏数据 | 🟡 | 06-28 | 06-28 |
| WP-07 | 流式 `delta.content` 兜底 | L1-04 | 直接拼接可能是 None → `or ""` 兜底，防 print 出 "None" 和 `answer += None` 的 TypeError | 🟡 | 06-17 | 06-27 |
| WP-08 | 非字符串 `None` 不得越过 Tool Message / history 状态边界 | L1-04/A4-04/A4-Gate | 出错 `return None` 或读取缺失 `content` 得到 `None` 后，未经合同化便写入 history / Tool Message，会污染后续 prompt 并在字符串操作处崩溃；2026-08-13 到期首答把 `None` 当成 `bool`，并用 `captured_messages is "error"` 检查错误对象；2026-08-17 虽已纠正类型，仍写出检查整条 message、误用 identity 且恒真的 `or` 条件，经提示后才改为逐条验证 `isinstance(message.get("content"), str)` → 在值进入共享状态前守住精确字符串合同，并用 spy / 回归测试验证下一模型调用的真实消息中没有非字符串 `content` | 🟡 | 2026-06-17 | 2026-08-17 |
| WP-09 | 整合时 call_messages vs call_model | L1-Gate | 非流式误用 `call_model`（列表被当字符串 + 丢历史）→ 整盘对话要用 `call_messages` | 🟡 | 06-27 | 06-27 |
| WP-10 | trim_history 切片要保整轮 | L1-03 | 随手切片会留残缺开头 → 用 `-MAX_TURNS*2` 按「轮」切，保成对 | 🟡 | 06-16 | 06-27 |
| WP-11 | prompt 里的 JSON 花括号 | PR2-01 | f-string / `.format` 撞 prompt 里的 `{}` → KeyError / 静默坏；改字符串拼接或转义 | 🟡 | 06-27 | 06-27 |
| WP-13 | 上下文工程 vs 提示工程（跨层） | S-03 | 把 Compaction 和 trim 混为一谈 → Compaction 是上下文工程压缩、trim 是 truncation，别互套 | 🟡 | 06-29 | 06-29 |
| WP-14 | 模型自编 Observation 的代码实景识别 | T3-01/A4-01 | 概念能背，但见「一次 call_model 吐出 Action+Observation+答案」的代码仍信结果来自工具 → 判别法=看代码有没有真执行（TOOLS/分发器/真调用）；假 Observation 文本上不可辨。2026-07-09 C1 首答只说“模型编造”，经订正补全“代码没有真实调用工具，所以不是真实返回值” | 🟡 | 07-01 | 2026-07-09 |
| WP-16 | JSON 字符串 / Python dict / 文件 JSON 三层边界 | PR2-Gate/T3-Gate | 把 `json.dump` 误说成“打印给用户”、把模型 JSON 字符串直接拿去校验；2026-07-12 A3 又把 Python 外层单引号误当成 JSON 内容的一部分 → 先分清字符串定界符，再由 `json.loads` 得 dict、按 schema 校验；`json.dumps` 转字符串，`json.dump` 写文件 | 🟡 | 2026-07-05 | 2026-07-12 |
| WP-18 | 工具返回结构字段名要按真实代码 | T3-02/T3-03/A4-Gate | 把 `{"ok": True/False}` 说成 `{"code": "成功/失败"}`，或把文件工具返回说成“返回最大字符数”；A4-Gate 又一度混淆工具的 `content/results` 与最终对象的 `summary/sources`，并把搜索失败写成路径错误 → 先按真实工具合同读取字段，再只把实际采用的路径或 `results[*].url` 投影为最终来源 | 🟡 | 2026-07-06 | 2026-08-06 |
| WP-19 | 沙箱路径要分输入形式与 `resolve()` 后真实落点 | T3-03/A4-Gate | 先按 `..` 字面猜路径，A4 又把路径拼到 `ROOT`、用 `.parents` 漏掉根目录自身并反向写 URL 包含判断 → 先拒绝 URL/绝对输入，再以 `(SANDBOX / relative_path).resolve()` 和 `target.relative_to(SANDBOX.resolve())` 判断最终归属；文件存在性留给工具层 | 🟡 | 2026-07-07 | 2026-08-07 |
| WP-20 | API 工具异常分支不能依赖 `response`，也别把捕获说成抛出 | T3-04/A4-Gate | 请求异常时可能根本没有 `response`，不能读外层 `response`；捕获 Timeout/RequestException 后应返回稳定错误字典，404 是已有响应中的状态码。2026-08-08 C2b 首稿的 HTTPError 分支再次读取外层 `response.status_code`，经故障注入后改为取得响应即显式分类状态，直接 HTTPError 不再泄漏未绑定变量 | 🟡 | 2026-07-08 | 2026-08-08 |
| WP-21 | Action JSON 严格格式 | A4-01/T3-Gate | 写工具调用请求时用了弯引号、工具名未加字符串引号、字段名写成 `arg`，订正后又留下尾逗号 → Action 要写成客户端可解析的严格结构：`{"tool_name": "...", "arguments": {...}}`，JSON 最后一个字段后不能有尾逗号 | 🟡 | 2026-07-09 | 2026-07-09 |
| WP-22 | Tool Calling 五种数量分层 | T3-Gate | 反复把模型 API 次数、工具轮数、`tool_call` 数、Python 真执行数和 `role="tool"` 回填数混为一谈；还把 `tool_rounds` 写成调用 ID → 沿轨迹逐层计数：一次响应可含多个调用，执行前拒绝仍需逐 ID 回填；最大轮数场景可能多发起一次模型 API 请求并收到一个不会执行的 `tool_call` | 🔴 | 2026-07-11 | 2026-07-12 |
| WP-23 | `urlparse` 是宽松解析器 | T3-Gate | 以为 `https://host:` 已被自动赋默认 443 → `.port is None` 同时可能表示未写端口或空端口；先检查 `netloc` 的空端口，再允许 `None/443`，显式 `:443` 的 `.port` 是整数 443 | 🟡 | 2026-07-12 | 2026-07-12 |
| WP-25 | 内部步骤结果、direct validator 与外部编排返回规则 | A4-04/A4-Gate | 把编排函数空计划返回的 `None` 当成会污染 history，又在成功路径返回整个 `StepResult`，并混淆 prompts/return/stdout；A4-Gate I4 又误读入口与中间字典；2026-08-24 GQ5 一度把 direct validator 的候选拒绝写成公开 `needs_manual` → 每层先读真实返回规则：direct `validate_success_result` 成功返回三字段对象、坏模型候选直接抛 `InvalidSuccessCandidateError`、非法客户端权威参数抛内建 `ValueError`；只有公开编排层才捕获候选错误并决定 recovery 或 `needs_manual`，普通程序/不变量异常继续上抛 | 🟡 | 2026-07-30 | 2026-08-24 |
| WP-26 | 历史 / 约束数据不等于自动 Replanning 或可执行剩余计划 | A4-04/A4-Gate | 只复述“不能再买票/不能用酒店 A”或提前写死酒店 B，却没有形成可执行动作；2026-08-22 `CD-005` 到期首答又把静态 Executor 的 `history` 误当成会自动生成新步骤 → `history` 只保存 `(step, result)` 并进入后续 prompt；客户端必须显式读取并分类结果，经 `if` 分支、Replanning 次数与全局 step 双护栏后调用 Replanner，保留已发生结果和不可重复副作用，只重写包含动作、约束、真实产出与后续依赖的未完成路线 | 🟡 | 2026-07-30 | 2026-08-22 |
| WP-12 | 模型软评审、代码硬门槛与外部证据分层 | PR2-02/A4-02/A4-03/A4-05/A4-Gate | 把 Reflection prompt 缩窄成“只规定输出格式”，并一度把模型反馈、非空字段或未覆盖语义维度的硬检查当成整体质量证明；2026-08-24 GQ4 又先断言 Candidate 幻觉“不可能”逃逸 → prompt/Reflection 都是软约束；validator 只能机械证明最终 `summary/sources` 与客户端 `approved_summary/allowed_sources` 一致。若工具证据本身错误、恶意、注入或不相关，而客户端仍批准它，exact gate 仍可能放行；因此一致性、支持性、外部真实性与内容安全必须分层验证 | 🟡 | 2026-06-27 | 2026-08-24 |
| WP-30 | 测试代码存在不等于测试已经执行 | W4 算法/Python 测试 | 把 `assert` 放在函数内无条件 `return` 之后导致不可达，移到函数外时又保留顶层空格触发 `IndentationError` → 测试必须调用交付接口、位于可达路径、顶层缩进合法，并以真实运行输出/退出码作为证据 | 🟡 | 2026-08-07 | 2026-08-07 |
| WP-31 | 在线哈希判断要维护最近一次状态 | W4 算法 | 首答只保存“元素和下标”，计划循环结束后再计算距离，未说明如何避免漏掉后续更近的一对或退化为重复比较 → 扫描时先用当前下标减该值最近下标判断，未命中也立即把记录更新为当前下标 | 🟡 | 2026-08-08 | 2026-08-08 |
| WP-32 | 跨模型 Chat Template / Tokenizer 的归属链 | A4-02/A4-Gate | 首答跳过 Chat Template，订正时仍把 X 的模板 / tokenizer 接到 Y，并认为切到 Y 后可能继续使用 X 模板 → 只有原始结构化 messages 可作为跨模型上层表示；目标 Y 必须用 Y 配套 Chat Template 渲染 Y 格式 Prompt，再由 Y tokenizer 生成 Y 的 Token IDs，渲染 Prompt / Token IDs 都不能跨模型复用 | 🟡 | 2026-08-09 | 2026-08-09 |
| WP-33 | 协议字段、异常名与评估分母必须读取精确生产证据 | A4-Gate | 2026-08-08～13 曾让 `bool`/整数冒充协议值、误读 SDK 层级并把 `finish_reason` 写成单数；2026-08-24 GQ5 又连续猜造 `ValidationError / AgentContractError / AgentInvariantError`，并把 10 条 normal、3 条 holdout 与 14 条总套件混成 `14/14` → 守门前按真实层级与精确类型/字面量读取生产代码；direct 候选拒绝是 `InvalidSuccessCandidateError`，非法客户端权威参数是内建 `ValueError`；冻结指标分别是 exact `10/10`、supportedness `10/10`、semantic 至少 `9/10 normal cases`、holdout `3/3`，完整套件才是 `14/14` | 🔴 | 2026-08-08 | 2026-08-24 |

## 📌 核心定义间隔池（正式 PASS 后才入池）

ID 使用 `CD-001` 起的递增编号。`状态/版本` 可写 `active`、`due` 或 `version-check`；版本敏感项复测前必须先核对当前官方资料。既有历史 PASS 不默认批量回填。

| ID | 语义目标 | 来源任务 | 最低 PASS 标准 | notes / KB 关联 | 上次证据 | 间隔档 | 下次回炉 | 状态 / 版本 |
|----|----------|----------|----------------|-----------------|----------|--------|----------|-------------|
| CD-001 | 区分 Instruction Data、Instruction Tuning、Prompting 与 Few-shot | A4-02 | 分清训练数据、更新参数的训练过程和当前上下文示例；说明参数是否改变及效果何时跨请求保留 | `notes/stage4/a4_02_llm_agent_basics.md`；KB Prompt 工程页 | `daily/2026-08-09.md` A4-Gate-R4 `PARTIAL → PASS` | +1月 | 2026-09-09 | active |
| CD-004 | ReAct 的成功停止与安全停止 | A4-03 | 区分“证据足够后返回非空 Finish”的成功停止，与缺失/无效 Action、步数耗尽等安全停止；说明 `max_steps` 如何给循环和 LLM 调用次数设上界 | `code/stage4/a4_03_react_agent.py`；KB ReAct 页 | `interviews/2026-08-06-agent-模拟面试.md` Q8/Q14；`daily/2026-08-06.md` 正式回炉 PASS | +1月 | 2026-09-06 | active |

## ✅ 稳定错题间隔池（已稳定，但到期仍抽查）

| ID | 概念 / 坑 | 阶段 | 错误理解 → 正确理解 | 上次测 | 间隔档 | 下次回炉 |
|----|-----------|------|---------------------|--------|--------|----------|
| WP-02 | requests 不对 4xx/5xx 抛异常 | B0-02 | 以为 4xx/5xx 会自动抛异常 → 不会；要主动 `check status_code` 或 `raise_for_status()` | 2026-08-10（A4-Gate-R7 `PARTIAL → PARTIAL → PASS`；核心机制首答正确） | +1月 | 2026-09-10 |
| WP-04 | 规则版 vs 模型版分类 | PR2-03 | 一度以为规则版也调模型 → 规则版=纯代码字面匹配、零模型调用；模型版才调 LLM | 2026-08-12（A4-Gate-R10 `PARTIAL → PASS`；C4a 前 hybrid 规则优先 / LLM fallback 新表面，能沿提前返回证明规则命中为零模型调用） | +1月 | 2026-09-12 |
| WP-05 | temperature 的作用 | L1-05 | 以为「只是随机」→ 是把候选概率的差距压平/拉大；低温=可复现（代码/数学）；A4 工具选择 vs 多风格摘要迁移复测通过 | 2026-08-08 | +1月 | 2026-09-08 |
| WP-06 | 贪心解码在温度哪一端 | L1-05 | 把 `k=1 / T→0` 误接高温端 → 是贪心/低温到底（最确定那端）；A4 真实循环配置迁移复测通过 | 2026-08-08 | +1月 | 2026-09-08 |
| WP-15 | 术语↔概念绑定（Observation 槽位名 / JSONDecodeError） | T3-01 | 机制会说但名词接不上：「被占槽位」叫不出 Observation；坏 JSON 异常答成 TypeError → 是 `json.JSONDecodeError`（ValueError 子类）；TypeError=类型不匹配操作（如 `answer += None`） | 2026-08-10（A4-Gate-R6 独立 PASS；同日 C3c/C3d 合并 Observation 执行证据） | +1月 | 2026-09-10 |
| WP-17 | 工具菜单与 Observation 回填的主语 | T3-02/T3-03/A4-02 | 曾说成“模型查工具 / 模型回填结果”或把 Observation 当执行者 → 客户端提供工具菜单、解析校验并执行，工具返回真实结果，客户端回填 Observation，模型只选择并读取；A4 来源冲突场景能按真实 `results` 剔除虚构 URL | 2026-08-07 | +1月 | 2026-09-07 |
| WP-24 | 正则量词/转义与精确 Python 字符串 | A4-03 | 把 `(.*?)` 写成 `(.\**?)`，误把量词 `*` 转义成字面星号；返回元组又漏字符串引号 → 让 `*?` 修饰前面的 `.`，并把含 `\n` 的精确结果放在引号内；新 `Reasoning/Decision` 字段无提示迁移通过 | 2026-08-11（A4-Gate-R9 `PARTIAL → PASS`；新 `Draft* / Final*` 表面下 pattern/flags/精确元组执行一致，补答闭合 `\Z`、DOTALL 与 `\*` / `*?` 边界） | +1月 | 2026-09-11 |
| WP-27 | 哈希表补数查询、保存对象与复杂度因果 | W4 算法 | 把需要查找的补数写成 `lookup` 已保存键，并把字典平均 `O(1)` 归因于“键唯一” → 查询 `target - num`，未命中后保存 `num → i`；先查后存防同一下标自用；平均时间/额外空间均为 `O(n)` | 2026-08-07（W4-ALG-F1 独立复测 PASS） | +2周 | 2026-08-21 |
| WP-28 | 测试预期必须服从输入语义 | W4 算法 | 把绝对值相同的 `-2` 与 `2` 当成重复，导致正确实现被错误断言判失败 → “重复”要求两个元素按整数相等；后续能为新题补入“字符种类相同但次数不同”和长度不同的精确反例，并让所有断言调用规定接口 | 2026-08-06（W4-ALG-F3 正式复核） | +2周 | 2026-08-20 |
| WP-29 | 单次操作、循环扫描量与容器规模要分开计 | W4 算法 | 曾因单次字典操作平均 `O(1)` 而险些把整段循环判成常量时间 → 先数最多 `n` 次循环，再数每次常数次平均 `O(1)` 哈希操作和至多 `n` 项容器规模，推出平均时间/额外空间均为 `O(n)` | 2026-08-08（W4-ALG-F4-G3 正式复核 PASS） | +2周 | 2026-08-22 |
