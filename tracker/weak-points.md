# 复习与易错点台账（统一排期）

本文件是旧知识复习排期的**唯一事实源**，统一保存 🔴/🟡 活跃错题、📌 核心定义间隔项和 ✅ 稳定错题间隔项。出题时从这里挑“该翻旧账复习”的目标；不要再建一套独立日历卡片。

原始作答与订正明细在 `daily/YYYY-MM-DD.md`，任务状态在 `tracker/progress.md`，完整正确内容在项目 `notes/` 和授权后的 `D:\AI-Knowledge`。本表只留最小语义目标、证据链接与排期，不复制完整答案。

## 状态与「间隔回炉」（关键：✅ 不等于永不再考）

- 🔴 **还容易错**：持续在候选池，按当前任务相关性和最久未测排序；不需要伪造“下次回炉日”。
- 🟡 **订正过一次**：持续候选，待下一次独立证据；答对 → 升 ✅，首个回炉日为本次 PASS `+2周`；答错 → 打回/保留 🔴。
- 📌 **核心定义间隔项**：只收任务正式 PASS 后、去重后的 `must-recall`。首个回炉日为任务 PASS `+2周`；答错时移出本池并合并/新增为 🟡 活跃弱点。
- ✅ **稳定错题间隔项**：按「下次回炉」日期到期抽查。
- 两类间隔项抽查**答对**后依次升档：`+1月 → +2月 → +3月封顶`，此后每 3 个月一次；抽查**答错**则回到活跃 🟡 并重置间隔。同一语义不得同时占活跃池和间隔池。

一句话：核心定义和错题共用一张排期表、共用当天最多 2 项的旧复习预算；熟了拉长间隔，答错立即回到活跃池。

## 怎么维护（我在「检查/出题」这两步自动做，你不用手填）

- **出题时（拉取式）**：只有用户启动练习时才扫描三池；任何一天旧复习目标**总计最多 2 个**，概念/阅读日默认 1 个，编码/Gate/项目日通常 0–1 个且必须关联真实产物。先看与当前任务的语义相关性，再比较活跃项的最久未测和间隔项的逾期程度；一个题能同时覆盖当前标准和旧目标时合并计算，不叠加题量。
- **积压阈值**：到期的 📌/✅ 间隔项达到 4 个，或最老一项逾期达到 7 天时，先连续两次练习优先使用上述既有旧复习席位，不立即增加题量；若两次后仍高于阈值，才在后续 ≤180 分钟日计划内安排一个 20–30 分钟回收块，每天最多一个，并替换部分主块而非追加到预算外。
- **不制造假债务**：🔴/🟡 活跃池持续可选，但没有“到期日”，不计入上面的逾期数量/天数；只有 📌/✅ 间隔池参与积压阈值判断。
- **批改/正式检查时**：新增或合并当天的坑/最虚，记录原始证据；正式检查者按上面的升降档规则更新池、上次证据与间隔日期。
- **提醒边界**：本台账不会自行弹窗或后台运行。除非用户另行明确要求自动提醒，否则不创建系统日历/自动化；到期项会在下一次“开始今天的练习”时被拉取。

---

## 🔴🟡 活跃池（持续候选）

| ID | 概念 / 坑 | 阶段 | 错误理解 → 正确理解 | 状态 | 首次 | 上次测 |
|----|-----------|------|---------------------|------|------|--------|
| WP-01 | 漏看 prompt「点名子项」 | PR2-02 | 只做主要求、漏掉「取值也 `strip`」这类子项 → 逐条核对点名清单，子项也要落实（2026-07-05 C3 重现：只写了切分方式，漏过滤空行/取 3 条/return） | 🔴 | 06-27 | 2026-07-05 |
| WP-03 | `strip()` 不是万能去空格 | PR2-03 | 把 `strip` 贴到 list 上想「去所有空格」→ 会崩；strip 只洗单个字符串的运行时脏数据 | 🟡 | 06-28 | 06-28 |
| WP-07 | 流式 `delta.content` 兜底 | L1-04 | 直接拼接可能是 None → `or ""` 兜底，防 print 出 "None" 和 `answer += None` 的 TypeError | 🟡 | 06-17 | 06-27 |
| WP-09 | 整合时 call_messages vs call_model | L1-Gate | 非流式误用 `call_model`（列表被当字符串 + 丢历史）→ 整盘对话要用 `call_messages` | 🟡 | 06-27 | 06-27 |
| WP-10 | trim_history 切片要保整轮 | L1-03 | 随手切片会留残缺开头 → 用 `-MAX_TURNS*2` 按「轮」切，保成对 | 🟡 | 06-16 | 06-27 |
| WP-11 | prompt 里的 JSON 花括号 | PR2-01 | f-string / `.format` 撞 prompt 里的 `{}` → KeyError / 静默坏；改字符串拼接或转义 | 🟡 | 06-27 | 06-27 |
| WP-13 | 上下文工程 vs 提示工程（跨层） | S-03 | 把 Compaction 和 trim 混为一谈 → Compaction 是上下文工程压缩、trim 是 truncation，别互套 | 🟡 | 06-29 | 06-29 |
| WP-14 | 模型自编 Observation 的代码实景识别 | T3-01/A4-01 | 概念能背，但见「一次 call_model 吐出 Action+Observation+答案」的代码仍信结果来自工具 → 判别法=看代码有没有真执行（TOOLS/分发器/真调用）；假 Observation 文本上不可辨。2026-07-09 C1 首答只说“模型编造”，经订正补全“代码没有真实调用工具，所以不是真实返回值” | 🟡 | 07-01 | 2026-07-09 |
| WP-16 | JSON 字符串 / Python dict / 文件 JSON 三层边界 | PR2-Gate/T3-Gate | 把 `json.dump` 误说成“打印给用户”、把模型 JSON 字符串直接拿去校验；2026-07-12 A3 又把 Python 外层单引号误当成 JSON 内容的一部分 → 先分清字符串定界符，再由 `json.loads` 得 dict、按 schema 校验；`json.dumps` 转字符串，`json.dump` 写文件 | 🟡 | 2026-07-05 | 2026-07-12 |
| WP-18 | 工具返回结构字段名要按真实代码 | T3-02/T3-03 | 把 `{"ok": True/False}` 说成 `{"code": "成功/失败"}`，或把文件工具返回说成“返回最大字符数” → 按真实代码说字段：计算器成功 `ok/result`，文件工具成功 `ok/content/truncated`，失败统一 `ok/error` | 🟡 | 2026-07-06 | 2026-07-07 |
| WP-19 | 沙箱路径要看 `resolve()` 后真实落点 | T3-03 | 先按 `..` 字面猜路径、又把 `resolve()`/`relative_to()` 混成不存在的 `resolve_to()` → 先 `(SANDBOX / relative_path).resolve()` 算最终绝对路径，再用 `target.relative_to(SANDBOX)` 判断是否仍在沙箱内 | 🟡 | 2026-07-07 | 2026-07-07 |
| WP-21 | Action JSON 严格格式 | A4-01/T3-Gate | 写工具调用请求时用了弯引号、工具名未加字符串引号、字段名写成 `arg`，订正后又留下尾逗号 → Action 要写成客户端可解析的严格结构：`{"tool_name": "...", "arguments": {...}}`，JSON 最后一个字段后不能有尾逗号 | 🟡 | 2026-07-09 | 2026-07-09 |
| WP-22 | Tool Calling 五种数量分层 | T3-Gate | 反复把模型 API 次数、工具轮数、`tool_call` 数、Python 真执行数和 `role="tool"` 回填数混为一谈；还把 `tool_rounds` 写成调用 ID → 沿轨迹逐层计数：一次响应可含多个调用，执行前拒绝仍需逐 ID 回填；最大轮数场景可能多发起一次模型 API 请求并收到一个不会执行的 `tool_call` | 🔴 | 2026-07-11 | 2026-07-12 |
| WP-23 | `urlparse` 是宽松解析器 | T3-Gate | 以为 `https://host:` 已被自动赋默认 443 → `.port is None` 同时可能表示未写端口或空端口；先检查 `netloc` 的空端口，再允许 `None/443`，显式 `:443` 的 `.port` 是整数 443 | 🟡 | 2026-07-12 | 2026-07-12 |
| WP-25 | 内部步骤结果 vs 外部编排返回契约 | A4-04 | 把编排函数空计划返回的 `None` 当成会污染 history，又在成功路径返回整个 `StepResult`，并混淆 prompts/return/stdout → `execute_plan` 内部必须维持 `(step, str)`，外层函数按 `str \| None` 返回，屏幕输出另用捕获文本验证 | 🟡 | 2026-07-30 | 2026-07-30 |
| WP-26 | 约束集合不等于可执行剩余计划 | A4-04 | 只复述“不能再买票/不能用酒店 A”或提前写死酒店 B → 每一步都要写成动作，并携带约束、真实产出及后续数据依赖；已完成副作用必须保留且不可重复 | 🟡 | 2026-07-30 | 2026-07-30 |
| WP-12 | 模型软评审、代码硬门槛与外部证据分层 | PR2-02/A4-02/A4-03/A4-05 | 把 Reflection prompt 缩窄成“只规定输出格式”，并一度把模型反馈或未覆盖语义维度的硬检查当成整体质量证明 → prompt 负责写明评审标准与反馈协议但仍是软约束；客户端只对已编码条件作硬判定，工具事实/外部测试提供独立证据，高风险动作还需人工确认，失败/超时必须保持未知。A4-05-F1 首答走错控制流并经两次订正闭合；失败的 `CD-003` 回炉合并到本项 | 🟡 | 2026-06-27 | 2026-08-04 |
| WP-27 | 哈希表补数查询、保存对象与复杂度因果 | W4 算法 | 把需要查找的补数写成 `lookup` 已保存键，并把字典平均 `O(1)` 归因于“键唯一” → `if` 查询 `target - num`，未命中后才保存 `num → i`；平均 `O(1)` 来自按键哈希定位，同一下标不可复用但不同下标可有相同值 | 🟡 | 2026-08-04 | 2026-08-04 |

## 📌 核心定义间隔池（正式 PASS 后才入池）

ID 使用 `CD-001` 起的递增编号。`状态/版本` 可写 `active`、`due` 或 `version-check`；版本敏感项复测前必须先核对当前官方资料。既有历史 PASS 不默认批量回填。

| ID | 语义目标 | 来源任务 | 最低 PASS 标准 | notes / KB 关联 | 上次证据 | 间隔档 | 下次回炉 | 状态 / 版本 |
|----|----------|----------|----------------|-----------------|----------|--------|----------|-------------|
| CD-001 | 区分 Instruction Data、Instruction Tuning、Prompting 与 Few-shot | A4-02 | 分清训练数据、更新参数的训练过程和当前上下文示例；说明参数是否改变及效果何时跨请求保留 | `notes/stage4/a4_02_llm_agent_basics.md`；KB Prompt 工程页 | `daily/2026-07-15.md` F1 PASS | +2周 | 2026-07-29 | active |
| CD-002 | messages → Chat Template → Prompt → Token IDs 与边界 | A4-02 | 能说明模板如何序列化角色/顺序/边界，以及模板不匹配为何会破坏模型理解 | `notes/stage4/a4_02_llm_agent_basics.md`；KB Special Tokens 页 | `daily/2026-07-14.md` G8/C2 PASS | +2周 | 2026-07-29 | active |
| CD-004 | ReAct 的成功停止与安全停止 | A4-03 | 区分“证据足够后返回非空 Finish”的成功停止，与缺失/无效 Action、步数耗尽等安全停止；说明 `max_steps` 如何给循环和 LLM 调用次数设上界 | `code/stage4/a4_03_react_agent.py`；KB ReAct 页 | `daily/2026-07-21.md` G5/F1、`daily/2026-07-22.md` 正式复核 PASS | +2周 | 2026-08-05 | active |
| CD-005 | Plan-and-Solve 的两阶段职责与执行上下文 | A4-04 | Planner 只把目标拆成有序可执行步骤；Executor 逐步执行并同时看到原始目标、完整计划、当前步骤和绑定步骤名的历史结果；能说明静态执行器为何不会自动 Replanning | `code/stage4/a4_04_plan_solve_demo.py`；`D:\AI-Knowledge\02-Concepts\Agent\计划与求解(Plan-and-Solve).md` | `daily/2026-07-22.md` G1/G3、`daily/2026-07-30.md` 正式复核 PASS | +2周 | 2026-08-13 | active |

## ✅ 稳定错题间隔池（已稳定，但到期仍抽查）

| ID | 概念 / 坑 | 阶段 | 错误理解 → 正确理解 | 上次测 | 间隔档 | 下次回炉 |
|----|-----------|------|---------------------|--------|--------|----------|
| WP-02 | requests 不对 4xx/5xx 抛异常 | B0-02 | 以为 4xx/5xx 会自动抛异常 → 不会；要主动 `check status_code` 或 `raise_for_status()` | 2026-07-01 | +2周 | 2026-07-15 |
| WP-04 | 规则版 vs 模型版分类 | PR2-03 | 一度以为规则版也调模型 → 规则版=纯代码字面匹配、零模型调用；模型版才调 LLM | 2026-07-05 | +2周 | 2026-07-19 |
| WP-05 | temperature 的作用 | L1-05 | 以为「只是随机」→ 是把候选概率的差距压平/拉大；低温=可复现（代码/数学） | 2026-07-01 | +2周 | 2026-07-15 |
| WP-06 | 贪心解码在温度哪一端 | L1-05 | 把 `k=1 / T→0` 误接高温端 → 是贪心/低温到底（最确定那端） | 2026-07-01 | +2周 | 2026-07-15 |
| WP-15 | 术语↔概念绑定（Observation 槽位名 / JSONDecodeError） | T3-01 | 机制会说但名词接不上：「被占槽位」叫不出 Observation；坏 JSON 异常答成 TypeError → 是 `json.JSONDecodeError`（ValueError 子类）；TypeError=类型不匹配操作（如 `answer += None`） | 2026-07-05 | +2周 | 2026-07-19 |
| WP-20 | API 工具异常分支不能依赖 `response`，也别把捕获说成抛出 | T3-04 | 把 `response` 说成响应体、异常分支想用 `response.ok`，并把 `RequestException`/timeout 说成“捕获并抛出/返回异常” → `response` 是响应对象；请求异常时可能没有 `response`，应捕获 `requests.Timeout` / `requests.RequestException` 后返回稳定 `{"ok": False, "error": ...}`；404 是已有响应里的状态码，不进 except。2026-07-09 D2/D4 独立复测通过 | 2026-07-09 | +2周 | 2026-07-23 |
| WP-17 | 工具菜单与 Observation 回填的主语 | T3-02/T3-03/A4-02 | 曾说成“模型查工具 / 模型回填结果”或把 Observation 当执行者 → 客户端提供工具菜单、解析校验并执行，工具返回真实结果，客户端回填 Observation，模型只选择并读取；A4-02 C1 新文件场景已独立订正通过 | 2026-07-14（7/15 正式复核） | +2周 | 2026-07-29 |
| WP-24 | 正则量词/转义与精确 Python 字符串 | A4-03 | 把 `(.*?)` 写成 `(.\**?)`，误把量词 `*` 转义成字面星号；返回元组又漏字符串引号 → 让 `*?` 修饰前面的 `.`，并把含 `\n` 的精确结果放在引号内；新 `Reasoning/Decision` 字段无提示迁移通过 | 2026-07-22 | +2周 | 2026-08-05 |
| WP-08 | except 别返回 None 毒化 history | L1-04/A4-04 | 出错 `return None` → 拼进 history 后污染后续 prompt 并在字符串操作处崩溃；应在进入状态前转成满足契约的稳定字符串，并用 spy/回归测试证明 history 无 `None` | 2026-07-30（A4-04-F1 正式复核） | +2周 | 2026-08-13 |
